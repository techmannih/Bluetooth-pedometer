{
  "name": "bluetooth-pedometer",
  "version": "1.0.0",
  "private": true,
  "description": "Ultra-low-power CC2340R5 Bluetooth pedometer built with tscircuit",
  "main": "index.circuit.tsx",
  "keywords": ["tscircuit", "circuit", "pcb", "electronics"],
  "scripts": {
    "dev": "tsci dev",
    "start": "tsci dev",
    "build": "tsci check netlist index.circuit.tsx && tsci build index.circuit.tsx --all-images --autorouter-timeout 15m && node scripts/audit-circuit.mjs",
    "snapshot": "tsci snapshot",
    "snapshot:update": "tsci snapshot --update",
    "typecheck": "tsc --noEmit",
    "audit": "node scripts/audit-circuit.mjs",
    "power-budget": "node scripts/power-budget.mjs",
    "test": "node --test scripts/*.test.mjs"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "tscircuit": "^0.0.2297",
    "typescript": "^5.0.0"
  }
}
