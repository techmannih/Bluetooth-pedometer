import { Matrix } from 'transformation-matrix';

type Point = {
    x: number;
    y: number;
};
interface PinchInput {
    initialTransform: Matrix;
    initialTouch1: Point;
    initialTouch2: Point;
    finalTouch1: Point | null;
    finalTouch2: Point | null;
}
declare const computePinchTransform: ({ initialTransform, initialTouch1, initialTouch2, finalTouch1, finalTouch2, }: PinchInput) => Matrix;

export { computePinchTransform };
