import { Matrix } from 'transformation-matrix';

interface Point {
    x: number;
    y: number;
}
declare function svgPathToPoints(svgPath: string, samplesPerUnit?: number): Point[][];

declare const SVG_MIMETYPE = "image/svg+xml";
declare const PNG_MIMETYPE = "image/png";
interface BRepShape {
    outer_ring: {
        vertices: Point[];
    };
    inner_rings: Array<{
        vertices: Point[];
    }>;
}
declare function loadImageSource(imageUrl: string): Promise<{
    mimetype: string;
    text: string;
    dataUrl: string;
    projectRelativePath: string;
}>;
declare function getSvgBRepShapes({ svg, width, height, transform, }: {
    svg: string;
    width: number;
    height: number;
    transform: Matrix;
}): BRepShape[];
declare function getTransformedSvgPathRoutes({ svg, width, height, transform, }: {
    svg: string;
    width: number;
    height: number;
    transform: Matrix;
}): Point[][];
declare function ensureClockwise(points: Point[]): Point[];

export { type BRepShape, PNG_MIMETYPE, type Point, SVG_MIMETYPE, ensureClockwise, getSvgBRepShapes, getTransformedSvgPathRoutes, loadImageSource, svgPathToPoints };
