var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// lib/components/index.ts
var components_exports = {};
__export(components_exports, {
  Ammeter: () => Ammeter,
  AnalogAcSweepSimulation: () => AnalogAcSweepSimulation,
  AnalogAnalysisSimulation: () => AnalogAnalysisSimulation,
  AnalogDcOperatingPointSimulation: () => AnalogDcOperatingPointSimulation,
  AnalogDcSweepSimulation: () => AnalogDcSweepSimulation,
  AnalogSimulation: () => AnalogSimulation,
  AnalogSweepParameter: () => AnalogSweepParameter,
  AnalogTransientSimulation: () => AnalogTransientSimulation,
  AutoroutingPhase: () => AutoroutingPhase,
  Battery: () => Battery,
  Board: () => Board,
  Breakout: () => Breakout,
  BreakoutPoint: () => BreakoutPoint,
  Bus: () => Bus,
  CadAssembly: () => CadAssembly,
  CadModel: () => CadModel,
  Capacitor: () => Capacitor,
  Chip: () => Chip,
  Connector: () => Connector,
  Constraint: () => Constraint3,
  CopperPour: () => CopperPour,
  CopperText: () => CopperText,
  CourtyardCircle: () => CourtyardCircle,
  CourtyardOutline: () => CourtyardOutline,
  CourtyardRect: () => CourtyardRect,
  Crystal: () => Crystal,
  CurrentSource: () => CurrentSource,
  Cutout: () => Cutout,
  DifferentialPair: () => DifferentialPair,
  Diode: () => Diode,
  DrcCheck: () => DrcCheck,
  EnclosureCutoutAperture: () => EnclosureCutoutAperture,
  EnclosureFdmBox: () => EnclosureFdmBox,
  FabricationNoteDimension: () => FabricationNoteDimension,
  FabricationNotePath: () => FabricationNotePath,
  FabricationNoteRect: () => FabricationNoteRect,
  FabricationNoteText: () => FabricationNoteText,
  Fanout: () => Breakout,
  FanoutPoint: () => BreakoutPoint,
  Fiducial: () => Fiducial,
  Footprint: () => Footprint,
  Fuse: () => Fuse,
  Group: () => Group5,
  Hole: () => Hole,
  Inductor: () => Inductor,
  Interconnect: () => Interconnect,
  Jumper: () => Jumper,
  Keepout: () => Keepout,
  Led: () => Led,
  Mosfet: () => Mosfet,
  MountedBoard: () => MountedBoard,
  Net: () => Net,
  NetLabel: () => NetLabel,
  NormalComponent: () => NormalComponent3,
  OpAmp: () => OpAmp,
  Panel: () => Panel,
  PcbNoteDimension: () => PcbNoteDimension,
  PcbNoteLine: () => PcbNoteLine,
  PcbNotePath: () => PcbNotePath,
  PcbNoteRect: () => PcbNoteRect,
  PcbNoteText: () => PcbNoteText,
  PcbTrace: () => PcbTrace,
  PcbVia: () => PcbVia,
  PinHeader: () => PinHeader,
  Pinout: () => Pinout,
  PlatedHole: () => PlatedHole,
  Port: () => Port,
  Potentiometer: () => Potentiometer,
  PowerSource: () => PowerSource,
  PrimitiveComponent: () => PrimitiveComponent2,
  PushButton: () => PushButton,
  Renderable: () => Renderable,
  Resistor: () => Resistor,
  Resonator: () => Resonator,
  SchematicArc: () => SchematicArc,
  SchematicBox: () => SchematicBox,
  SchematicCell: () => SchematicCell,
  SchematicCircle: () => SchematicCircle,
  SchematicLine: () => SchematicLine,
  SchematicPath: () => SchematicPath,
  SchematicRect: () => SchematicRect,
  SchematicRow: () => SchematicRow,
  SchematicSection: () => SchematicSection,
  SchematicSheet: () => SchematicSheet,
  SchematicSymbol: () => SchematicSymbol,
  SchematicTable: () => SchematicTable,
  SchematicText: () => SchematicText,
  SilkscreenCircle: () => SilkscreenCircle,
  SilkscreenGraphic: () => SilkscreenGraphic,
  SilkscreenLine: () => SilkscreenLine,
  SilkscreenPath: () => SilkscreenPath,
  SilkscreenRect: () => SilkscreenRect,
  SilkscreenText: () => SilkscreenText,
  SmtPad: () => SmtPad,
  SolderJumper: () => SolderJumper,
  SpiceModel: () => SpiceModel,
  Subcircuit: () => Subcircuit,
  Subpanel: () => Subpanel,
  Switch: () => Switch,
  Symbol: () => SymbolComponent,
  TestPoint: () => TestPoint,
  Trace: () => Trace3,
  TraceHint: () => TraceHint,
  Transistor: () => Transistor,
  Via: () => Via,
  VoltageProbe: () => VoltageProbe,
  VoltageSource: () => VoltageSource
});

// lib/components/base-components/NormalComponent/NormalComponent.ts
import { fp } from "@tscircuit/footprinter";
import { normalizeDegrees as normalizeDegrees2 } from "@tscircuit/math-utils";
import {
  distance as distance8,
  pcb_component_invalid_layer_error,
  pcb_manual_edit_conflict_warning,
  point3 as point32,
  rotation as rotation2,
  source_part_not_found_warning as source_part_not_found_warning2
} from "circuit-json";
import Debug5 from "debug";

// lib/components/primitive-components/Trace/Trace.ts
import "@tscircuit/infgrid-ijump-astar";
import { traceProps } from "@tscircuit/props";
import "circuit-json";
import "circuit-json-to-connectivity-map";

// lib/utils/autorouting/DirectLineRouter.ts
var DirectLineRouter = class {
  input;
  constructor({ input }) {
    this.input = input;
  }
  solveAndMapToTraces() {
    const traces = [];
    for (const connection of this.input.connections) {
      if (connection.pointsToConnect.length !== 2) continue;
      const [start, end] = connection.pointsToConnect;
      const trace = {
        type: "pcb_trace",
        pcb_trace_id: "",
        connection_name: connection.name,
        route: [
          {
            route_type: "wire",
            x: start.x,
            y: start.y,
            layer: "top",
            width: 0.1
          },
          {
            route_type: "wire",
            x: end.x,
            y: end.y,
            layer: "top",
            width: 0.1
          }
        ]
      };
      traces.push(trace);
    }
    return traces;
  }
};

// lib/utils/autorouting/computeObstacleBounds.ts
var computeObstacleBounds = (obstacles) => {
  const minX = Math.min(...obstacles.map((o) => o.center.x));
  const maxX = Math.max(...obstacles.map((o) => o.center.x));
  const minY = Math.min(...obstacles.map((o) => o.center.y));
  const maxY = Math.max(...obstacles.map((o) => o.center.y));
  return { minX, maxX, minY, maxY };
};

// lib/utils/autorouting/findPossibleTraceLayerCombinations.ts
var DEFAULT_LAYER_SELECTION_PREFERENCE = ["top", "bottom", "inner1", "inner2"];
var findPossibleTraceLayerCombinationsRecursive = (hints, layerSelectionPreference, layer_path) => {
  const candidates = [];
  if (layer_path.length === 0) {
    const starting_layers = hints[0].layers;
    for (const layer of starting_layers) {
      candidates.push(
        ...findPossibleTraceLayerCombinationsRecursive(
          hints.slice(1),
          layerSelectionPreference,
          [layer]
        )
      );
    }
    return candidates;
  }
  if (hints.length === 0) return [];
  const current_hint = hints[0];
  const is_possibly_via = current_hint.via || current_hint.optional_via;
  const last_layer = layer_path[layer_path.length - 1];
  if (hints.length === 1) {
    const last_hint = current_hint;
    if (last_hint.layers && is_possibly_via) {
      return last_hint.layers.map((layer) => ({
        layer_path: [...layer_path, layer]
      }));
    }
    if (last_hint.layers?.includes(last_layer)) {
      return [{ layer_path: [...layer_path, last_layer] }];
    }
    return [];
  }
  if (!is_possibly_via) {
    if (current_hint.layers) {
      if (!current_hint.layers.includes(last_layer)) {
        return [];
      }
    }
    return findPossibleTraceLayerCombinationsRecursive(
      hints.slice(1),
      layerSelectionPreference,
      layer_path.concat([last_layer])
    );
  }
  const candidate_next_layers = (current_hint.optional_via ? layerSelectionPreference : layerSelectionPreference.filter((layer) => layer !== last_layer)).filter(
    (layer) => !current_hint.layers || current_hint.layers?.includes(layer)
  );
  for (const candidate_next_layer of candidate_next_layers) {
    candidates.push(
      ...findPossibleTraceLayerCombinationsRecursive(
        hints.slice(1),
        layerSelectionPreference,
        layer_path.concat(candidate_next_layer)
      )
    );
  }
  return candidates;
};
var findPossibleTraceLayerCombinations = (hints, options = {}) => findPossibleTraceLayerCombinationsRecursive(
  hints,
  options.layerSelectionPreference ?? DEFAULT_LAYER_SELECTION_PREFERENCE,
  []
);

// lib/utils/autorouting/getDominantDirection.ts
function getDominantDirection(edge) {
  const delta = {
    x: edge.to.x - edge.from.x,
    y: edge.to.y - edge.from.y
  };
  const absX = Math.abs(delta.x);
  const absY = Math.abs(delta.y);
  if (absX > absY) {
    return delta.x > 0 ? "right" : "left";
  }
  return delta.y > 0 ? "down" : "up";
}

// lib/utils/pcb-trace-route-point-utils.ts
var getRoutePointPosition = (point6) => {
  if (point6.route_type === "through_pad") return point6.start;
  return { x: point6.x, y: point6.y };
};
var getRoutePointPositions = (point6) => {
  if (point6.route_type === "through_pad") return [point6.start, point6.end];
  return [{ x: point6.x, y: point6.y }];
};

// lib/utils/autorouting/mergeRoutes.ts
function pdist(a, b) {
  const aPosition = getRoutePointPosition(a);
  const bPosition = getRoutePointPosition(b);
  return Math.hypot(aPosition.x - bPosition.x, aPosition.y - bPosition.y);
}
var mergeRoutes = (routes) => {
  if (routes.length === 1) return routes[0];
  if (routes.some((r) => r.length === 0)) {
    throw new Error("Cannot merge routes with zero length");
  }
  const merged = [];
  const first_route_fp = routes[0][0];
  const first_route_lp = routes[0][routes[0].length - 1];
  const second_route_fp = routes[1][0];
  const second_route_lp = routes[1][routes[1].length - 1];
  const best_reverse_dist = Math.min(
    pdist(first_route_fp, second_route_fp),
    pdist(first_route_fp, second_route_lp)
  );
  const best_normal_dist = Math.min(
    pdist(first_route_lp, second_route_fp),
    pdist(first_route_lp, second_route_lp)
  );
  if (best_reverse_dist < best_normal_dist) {
    merged.push(...routes[0].reverse());
  } else {
    merged.push(...routes[0]);
  }
  for (let i = 1; i < routes.length; i++) {
    const last_merged_point = merged[merged.length - 1];
    const next_route = routes[i];
    const next_first_point = next_route[0];
    const next_last_point = next_route[next_route.length - 1];
    const distance_to_first = pdist(last_merged_point, next_first_point);
    const distance_to_last = pdist(last_merged_point, next_last_point);
    if (distance_to_first < distance_to_last) {
      merged.push(...next_route);
    } else {
      merged.push(...next_route.reverse());
    }
  }
  for (let i = 1; i < merged.length - 1; i++) {
    const lastPoint = merged[i - 1];
    const currentPoint = merged[i];
    if (lastPoint.route_type !== "wire") continue;
    if (currentPoint.route_type !== "wire") continue;
    if (lastPoint.layer !== currentPoint.layer) {
      merged.splice(i, 0, {
        x: lastPoint.x,
        y: lastPoint.y,
        from_layer: lastPoint.layer,
        to_layer: currentPoint.layer,
        route_type: "via"
      });
    }
  }
  return merged;
};

// lib/components/primitive-components/Net.ts
import { netProps } from "@tscircuit/props";

// lib/components/base-components/PrimitiveComponent/PrimitiveComponent.ts
import { selectAll, selectOne } from "css-select";
import "debug";

// lib/components/base-components/Renderable.ts
import Debug from "debug";
var debug = Debug("tscircuit:renderable");
var orderedRenderPhases = [
  "ReactSubtreesRender",
  "RenderIsolatedSubcircuits",
  "InflateSubcircuitCircuitJson",
  "SourceNameDuplicateComponentRemoval",
  "PcbFootprintStringRender",
  "FetchPartFootprint",
  "ResolveFootprintPinLabels",
  "InitializePortsFromChildren",
  "CreateNetsFromProps",
  "AssignFallbackProps",
  "CreateTracesFromProps",
  "CreateTracesFromNetLabels",
  "CreateTraceHintsFromProps",
  "CreateAutoplacedBreakoutPoints",
  "SourceGroupRender",
  "AssignNameToUnnamedComponents",
  "SourceRender",
  "CheckRefDesConvention",
  "SourceComponentPropertyValidation",
  "SourceParentAttachment",
  "PortMatching",
  "OptimizeSelectorCache",
  "SourceTraceRender",
  "SourceAddConnectivityMapKey",
  "SourceDesignRuleChecks",
  "SimulationRender",
  "SchematicComponentRender",
  "SchematicPortRender",
  "SymbolContainerRender",
  "SchematicPrimitiveRender",
  "SchematicSymbolResize",
  "SchematicComponentSizeCalculation",
  "SchematicLayout",
  "SchematicSectionRender",
  "SchematicTraceRender",
  "SchematicSheetRender",
  "SchematicReplaceNetLabelsWithSymbols",
  "PanelBoardLayout",
  "ValidatePcbCoordinates",
  "PcbComponentRender",
  "PcbPrimitiveRender",
  "PcbFootprintLayout",
  "PcbPortRender",
  "PcbPortAttachment",
  "PcbComponentSizeCalculation",
  "PcbComponentAnchorAlignment",
  "PcbCalcPlacementResolution",
  "PcbLayout",
  "PcbBoardAutoSize",
  "PanelLayout",
  "PcbAutoplaceBreakoutPoints",
  "PcbTraceHintRender",
  "PcbManualTraceRender",
  "PcbPlacementDesignRuleChecks",
  "PcbTraceRender",
  "PcbRouteNetIslands",
  "PcbCopperPourRender",
  "PcbDesignRuleChecks",
  "SilkscreenOverlapAdjustment",
  "CadModelRender",
  "PartsEngineRender",
  "PartOrientationAnalysis",
  "SupplierFootprintMismatchWarning",
  "SimulationSpiceEngineRender"
];
var renderPhaseIndexMap = new Map(
  orderedRenderPhases.map((phase, index) => [phase, index])
);
var asyncPhaseDependencies = {
  InflateSubcircuitCircuitJson: ["RenderIsolatedSubcircuits"],
  ResolveFootprintPinLabels: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicComponentRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicPortRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SymbolContainerRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicPrimitiveRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicSymbolResize: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicComponentSizeCalculation: [
    "PcbFootprintStringRender",
    "FetchPartFootprint"
  ],
  SchematicLayout: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicSectionRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicTraceRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicSheetRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SchematicReplaceNetLabelsWithSymbols: [
    "PcbFootprintStringRender",
    "FetchPartFootprint"
  ],
  PcbFootprintLayout: ["PcbFootprintStringRender", "FetchPartFootprint"],
  PcbComponentSizeCalculation: [
    "PcbFootprintStringRender",
    "FetchPartFootprint"
  ],
  PcbLayout: ["PcbFootprintStringRender", "FetchPartFootprint"],
  PcbBoardAutoSize: ["PcbFootprintStringRender", "FetchPartFootprint"],
  PcbTraceHintRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  PcbManualTraceRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  PcbCopperPourRender: [
    "PcbFootprintStringRender",
    "FetchPartFootprint",
    "PcbPlacementDesignRuleChecks",
    "PcbTraceRender",
    "PcbRouteNetIslands"
  ],
  PcbPlacementDesignRuleChecks: [
    "PcbFootprintStringRender",
    "FetchPartFootprint"
  ],
  PcbTraceRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  PcbRouteNetIslands: [
    "PcbFootprintStringRender",
    "FetchPartFootprint",
    "PcbPlacementDesignRuleChecks"
  ],
  PcbDesignRuleChecks: [
    "PcbFootprintStringRender",
    "FetchPartFootprint",
    "PcbPlacementDesignRuleChecks"
  ],
  SilkscreenOverlapAdjustment: [
    "PcbFootprintStringRender",
    "FetchPartFootprint"
  ],
  CadModelRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  PartsEngineRender: ["PcbFootprintStringRender", "FetchPartFootprint"],
  PartOrientationAnalysis: [
    "PcbFootprintStringRender",
    "FetchPartFootprint",
    "PartsEngineRender"
  ],
  SupplierFootprintMismatchWarning: [
    "PcbFootprintStringRender",
    "FetchPartFootprint",
    "PartsEngineRender"
  ],
  SourceAddConnectivityMapKey: [
    "PcbFootprintStringRender",
    "FetchPartFootprint"
  ],
  PcbComponentAnchorAlignment: [
    "PcbFootprintStringRender",
    "FetchPartFootprint"
  ],
  PcbCalcPlacementResolution: [
    "PcbFootprintStringRender",
    "FetchPartFootprint"
  ],
  ValidatePcbCoordinates: ["PcbFootprintStringRender", "FetchPartFootprint"],
  SourceTraceRender: ["PcbFootprintStringRender", "FetchPartFootprint"]
};
var globalRenderCounter = 0;
var globalAsyncEffectCounter = 0;
var Renderable = class _Renderable {
  renderPhaseStates;
  shouldBeRemoved = false;
  children;
  /** PCB-only SMTPads, PlatedHoles, Holes, Silkscreen elements etc. */
  isPcbPrimitive = false;
  /** Schematic-only, lines, boxes, indicators etc. */
  isSchematicPrimitive = false;
  _renderId;
  _currentRenderPhase = null;
  _pcbTraceRenderWaitingForPlacementChecks = false;
  _asyncEffects = [];
  parent = null;
  constructor(props) {
    this._renderId = `${globalRenderCounter++}`;
    this.children = [];
    this.renderPhaseStates = {};
    for (const phase of orderedRenderPhases) {
      this.renderPhaseStates[phase] = {
        initialized: false,
        dirty: false
      };
    }
  }
  _markDirty(phase) {
    this.renderPhaseStates[phase].dirty = true;
    const phaseIndex = renderPhaseIndexMap.get(phase);
    for (let i = phaseIndex + 1; i < orderedRenderPhases.length; i++) {
      this.renderPhaseStates[orderedRenderPhases[i]].dirty = true;
    }
    if (this.parent?._markDirty) {
      this.parent._markDirty(phase);
    }
  }
  _queueAsyncEffect(effectName, effect) {
    const asyncEffectId = `${this._renderId}:${globalAsyncEffectCounter++}`;
    const asyncEffect = {
      asyncEffectId,
      promise: effect(),
      // TODO don't start effects until end of render cycle
      phase: this._currentRenderPhase,
      effectName,
      complete: false
    };
    this._asyncEffects.push(asyncEffect);
    const root = this._getRootCircuit();
    if (root) {
      root.emit("asyncEffect:start", {
        asyncEffectId,
        effectName,
        componentDisplayName: this.getString(),
        phase: asyncEffect.phase
      });
    }
    asyncEffect.promise.then(() => {
      asyncEffect.complete = true;
      const root2 = this._getRootCircuit();
      if (root2) {
        root2.emit("asyncEffect:end", {
          asyncEffectId,
          effectName,
          componentDisplayName: this.getString(),
          phase: asyncEffect.phase
        });
      }
    }).catch((error) => {
      console.error(
        `Async effect error in ${asyncEffect.phase} "${effectName}":
${error.stack}`
      );
      asyncEffect.complete = true;
      const root2 = this._getRootCircuit();
      if (root2) {
        root2.emit("asyncEffect:end", {
          asyncEffectId,
          effectName,
          componentDisplayName: this.getString(),
          phase: asyncEffect.phase,
          error: error.toString()
        });
      }
    });
  }
  _emitRenderLifecycleEvent(phase, startOrEnd) {
    debug(`${phase}:${startOrEnd} ${this.getString()}`);
    const granular_event_type = `renderable:renderLifecycle:${phase}:${startOrEnd}`;
    const eventPayload = {
      renderId: this._renderId,
      componentDisplayName: this.getString(),
      type: granular_event_type
    };
    const root = this._getRootCircuit();
    if (root) {
      root.emit(granular_event_type, eventPayload);
      root.emit("renderable:renderLifecycle:anyEvent", {
        ...eventPayload,
        type: granular_event_type
      });
    }
  }
  getString() {
    return this.constructor.name;
  }
  _hasIncompleteAsyncEffects() {
    if (this._asyncEffects.some((effect) => !effect.complete)) return true;
    return this.children.some(
      (child) => typeof child._hasIncompleteAsyncEffects === "function" ? child._hasIncompleteAsyncEffects() : false
    );
  }
  _hasIncompleteAsyncEffectsInSubtreeForPhase(phase) {
    for (const e of this._asyncEffects) {
      if (!e.complete && e.phase === phase) return true;
    }
    for (const child of this.children) {
      const renderableChild = child;
      if (renderableChild._hasIncompleteAsyncEffectsInSubtreeForPhase(phase))
        return true;
    }
    return false;
  }
  _hasIncompleteAsyncEffectsForPhase(phase) {
    const root = this._getRootCircuit();
    if (root?._hasIncompleteAsyncEffectsForPhase) {
      return root._hasIncompleteAsyncEffectsForPhase(phase);
    }
    return this._hasIncompleteAsyncEffectsInSubtreeForPhase(phase);
  }
  getCurrentRenderPhase() {
    return this._currentRenderPhase;
  }
  getRenderGraph() {
    return {
      id: this._renderId,
      currentPhase: this._currentRenderPhase,
      renderPhaseStates: this.renderPhaseStates,
      shouldBeRemoved: this.shouldBeRemoved,
      children: this.children.map(
        (child) => child.getRenderGraph()
      )
    };
  }
  getTopLevelRenderable() {
    let current = this;
    while (current.parent && current.parent instanceof _Renderable) {
      current = current.parent;
    }
    return current;
  }
  runRenderCycle() {
    for (const renderPhase of orderedRenderPhases) {
      this.runRenderPhaseForChildren(renderPhase);
      this.runRenderPhase(renderPhase);
    }
  }
  /**
   * This runs all the render methods for a given phase, calling one of:
   * - doInitial*
   * - update*
   *  -remove*
   *  ...depending on the current state of the component.
   */
  runRenderPhase(phase) {
    this._currentRenderPhase = phase;
    const phaseState = this.renderPhaseStates[phase];
    const isInitialized = phaseState.initialized;
    const isDirty = phaseState.dirty;
    if (!isInitialized && this.shouldBeRemoved) return;
    if (this.shouldBeRemoved && isInitialized) {
      this._emitRenderLifecycleEvent(phase, "start");
      this?.[`remove${phase}`]?.();
      phaseState.initialized = false;
      phaseState.dirty = false;
      this._emitRenderLifecycleEvent(phase, "end");
      return;
    }
    const prevPhaseIndex = renderPhaseIndexMap.get(phase) - 1;
    if (prevPhaseIndex >= 0) {
      const prevPhase = orderedRenderPhases[prevPhaseIndex];
      const hasIncompleteEffects = this._asyncEffects.filter((e) => e.phase === prevPhase).some((e) => !e.complete);
      if (hasIncompleteEffects) return;
    }
    const deps = asyncPhaseDependencies[phase] || [];
    if (deps.length > 0) {
      for (const depPhase of deps) {
        if (this._hasIncompleteAsyncEffectsForPhase(depPhase)) {
          return;
        }
      }
    }
    this._emitRenderLifecycleEvent(phase, "start");
    if (isInitialized) {
      if (isDirty) {
        ;
        this?.[`update${phase}`]?.();
        phaseState.dirty = false;
      }
      this._emitRenderLifecycleEvent(phase, "end");
      return;
    }
    phaseState.dirty = false;
    this?.[`doInitial${phase}`]?.();
    phaseState.initialized = true;
    this._emitRenderLifecycleEvent(phase, "end");
  }
  runRenderPhaseForChildren(phase) {
    for (const child of this.children) {
      if ("_isIsolatedSubcircuit" in this && this._isIsolatedSubcircuit && "doInitialRenderIsolatedSubcircuits" in this && phase === "RenderIsolatedSubcircuits") {
        continue;
      }
      child.runRenderPhaseForChildren(phase);
      child.runRenderPhase(phase);
    }
  }
  _getRootCircuit() {
    if ("root" in this) {
      return this.root ?? null;
    }
    return null;
  }
  renderError(message) {
    if (typeof message === "string") {
      throw new Error(message);
    }
    throw new Error(JSON.stringify(message, null, 2));
  }
};

// lib/errors/InvalidProps.ts
var InvalidProps = class extends Error {
  constructor(componentName, originalProps, formattedError) {
    let message;
    const propsWithError = Object.keys(formattedError).filter(
      (k) => k !== "_errors"
    );
    const invalidPinLabelMessages = [];
    const pinLabels = originalProps.pinLabels;
    if (pinLabels) {
      for (const [pin, labelOrLabels] of Object.entries(pinLabels)) {
        const labels = Array.isArray(labelOrLabels) ? labelOrLabels : [labelOrLabels];
        for (const label of labels) {
          if (typeof label === "string" && (label.startsWith(" ") || label.endsWith(" "))) {
            invalidPinLabelMessages.push(
              `pinLabels.${pin} ("${label}" has leading or trailing spaces)`
            );
          }
        }
      }
    }
    const propMessage = propsWithError.map((k) => {
      if (k === "pinLabels" && invalidPinLabelMessages.length > 0) {
        return invalidPinLabelMessages.join(", ");
      }
      if (formattedError[k]._errors[0]) {
        return `${k} (${formattedError[k]._errors[0]})`;
      }
      return `${k} (${JSON.stringify(formattedError[k])})`;
    }).join(", ");
    if ("name" in originalProps) {
      message = `Invalid props for ${componentName} "${originalProps.name}": ${propMessage}`;
    } else if ("footprint" in originalProps && typeof originalProps.footprint === "string") {
      message = `Invalid props for ${componentName} (unnamed ${originalProps.footprint} component): ${propMessage}`;
    } else {
      message = `Invalid props for ${componentName} (unnamed): ${propMessage}`;
    }
    super(message);
    this.componentName = componentName;
    this.originalProps = originalProps;
    this.formattedError = formattedError;
  }
  componentName;
  originalProps;
  formattedError;
};

// lib/utils/evaluateCalcString.ts
var defaultUnits = {
  mm: 1
  // You can add more if you need them:
  // cm: 10,
  // in: 25.4,
  // mil: 0.0254,
};
function evaluateCalcString(input, options) {
  const { knownVariables, units: userUnits } = options;
  const units = { ...defaultUnits, ...userUnits ?? {} };
  const expr = normalizeLegacyBoundsIdentifierCase(extractExpression(input));
  const tokens = tokenize(expr, units);
  const result = parseExpression(tokens, knownVariables);
  return result;
}
function extractCalcIdentifiers(input) {
  const expr = normalizeLegacyBoundsIdentifierCase(extractExpression(input));
  const tokens = tokenize(expr, defaultUnits);
  return tokens.filter((token) => token.type === "identifier").map((token) => token.value);
}
function normalizeLegacyBoundsIdentifierCase(expr) {
  const legacyBoundNameToCamelCase = {
    minx: "minX",
    maxx: "maxX",
    miny: "minY",
    maxy: "maxY"
  };
  return expr.replace(/\.(minx|maxx|miny|maxy)\b/gi, (match, boundName) => {
    const normalizedBoundName = legacyBoundNameToCamelCase[String(boundName).toLowerCase()];
    return `.${normalizedBoundName ?? match.slice(1)}`;
  });
}
function extractExpression(raw) {
  const trimmed = raw.trim();
  if (!trimmed.toLowerCase().startsWith("calc")) {
    return trimmed;
  }
  const match = trimmed.match(/^calc\s*\((.*)\)$/is);
  if (!match) {
    throw new Error(`Invalid calc() expression: "${raw}"`);
  }
  return match[1].trim();
}
function tokenize(expr, units) {
  const tokens = [];
  let i = 0;
  const isDigit = (ch) => ch >= "0" && ch <= "9";
  const isIdentStart = (ch) => ch >= "A" && ch <= "Z" || ch >= "a" && ch <= "z" || ch === "_";
  const isIdentChar = (ch) => isIdentStart(ch) || isDigit(ch) || ch === ".";
  while (i < expr.length) {
    const ch = expr[i];
    if (ch === " " || ch === "	" || ch === "\n" || ch === "\r") {
      i++;
      continue;
    }
    if (isDigit(ch) || ch === "." && i + 1 < expr.length && isDigit(expr[i + 1])) {
      const start = i;
      i++;
      while (i < expr.length) {
        const c = expr[i];
        if (isDigit(c) || c === ".") {
          i++;
        } else {
          break;
        }
      }
      const numberText = expr.slice(start, i);
      let num = Number(numberText);
      if (Number.isNaN(num)) {
        throw new Error(`Invalid number: "${numberText}"`);
      }
      const unitStart = i;
      while (i < expr.length && /[A-Za-z]/.test(expr[i])) {
        i++;
      }
      if (i > unitStart) {
        const unitText = expr.slice(unitStart, i);
        const factor = units[unitText];
        if (factor == null) {
          throw new Error(`Unknown unit: "${unitText}"`);
        }
        num *= factor;
      }
      tokens.push({ type: "number", value: num });
      continue;
    }
    if (isIdentStart(ch)) {
      const start = i;
      i++;
      while (i < expr.length && isIdentChar(expr[i])) {
        i++;
      }
      const ident = expr.slice(start, i);
      tokens.push({ type: "identifier", value: ident });
      continue;
    }
    if (ch === "(" || ch === ")") {
      tokens.push({ type: "paren", value: ch });
      i++;
      continue;
    }
    if (ch === "+" || ch === "-" || ch === "*" || ch === "/") {
      tokens.push({ type: "operator", value: ch });
      i++;
      continue;
    }
    throw new Error(`Unexpected character "${ch}" in expression "${expr}"`);
  }
  return tokens;
}
function parseExpression(tokens, vars) {
  let index = 0;
  const peek = () => tokens[index];
  const consume = () => tokens[index++];
  const parsePrimary = () => {
    const token = peek();
    if (!token) {
      throw new Error("Unexpected end of expression");
    }
    if (token.type === "number") {
      consume();
      return token.value;
    }
    if (token.type === "identifier") {
      consume();
      const value = vars[token.value];
      if (value == null) {
        throw new Error(`Unknown variable: "${token.value}"`);
      }
      return value;
    }
    if (token.type === "paren" && token.value === "(") {
      consume();
      const value = parseExpr();
      const next = peek();
      if (!next || next.type !== "paren" || next.value !== ")") {
        throw new Error('Expected ")"');
      }
      consume();
      return value;
    }
    throw new Error(`Unexpected token "${token.value}"`);
  };
  const parseFactor = () => {
    const token = peek();
    if (token && token.type === "operator" && (token.value === "+" || token.value === "-")) {
      consume();
      const value = parseFactor();
      return token.value === "+" ? value : -value;
    }
    return parsePrimary();
  };
  const parseTerm = () => {
    let value = parseFactor();
    while (true) {
      const token = peek();
      if (!token || token.type !== "operator") break;
      if (token.value !== "*" && token.value !== "/") break;
      consume();
      const rhs = parseFactor();
      if (token.value === "*") {
        value *= rhs;
      } else {
        value /= rhs;
      }
    }
    return value;
  };
  const parseExpr = () => {
    let value = parseTerm();
    while (true) {
      const token = peek();
      if (!token || token.type !== "operator") break;
      if (token.value !== "+" && token.value !== "-") break;
      consume();
      const rhs = parseTerm();
      if (token.value === "+") {
        value += rhs;
      } else {
        value -= rhs;
      }
    }
    return value;
  };
  const result = parseExpr();
  if (index < tokens.length) {
    const leftover = tokens.slice(index).map((t) => "value" in t ? t.value : "?").join(" ");
    throw new Error(`Unexpected tokens at end of expression: ${leftover}`);
  }
  return result;
}

// lib/utils/getSubcircuitPcbCalcVariables.ts
function setSubcircuitPcbComponentCalcVariables(params) {
  const { vars, componentName, position, size } = params;
  vars[`${componentName}.x`] = position.x;
  vars[`${componentName}.y`] = position.y;
  vars[`${componentName}.width`] = size.width;
  vars[`${componentName}.height`] = size.height;
  vars[`${componentName}.minX`] = position.x - size.width / 2;
  vars[`${componentName}.maxX`] = position.x + size.width / 2;
  vars[`${componentName}.minY`] = position.y - size.height / 2;
  vars[`${componentName}.maxY`] = position.y + size.height / 2;
}
function getSubcircuitPcbCalcVariables(db) {
  const vars = {};
  for (const sourceComponent of db.source_component.list()) {
    if (!sourceComponent.name) continue;
    const pcbComponent = db.pcb_component.getWhere({
      source_component_id: sourceComponent.source_component_id
    });
    if (!pcbComponent) continue;
    setSubcircuitPcbComponentCalcVariables({
      vars,
      componentName: sourceComponent.name,
      position: {
        x: pcbComponent.center.x,
        y: pcbComponent.center.y
      },
      size: {
        width: pcbComponent.width ?? 0,
        height: pcbComponent.height ?? 0
      }
    });
  }
  return vars;
}

// lib/utils/pcb/transform-footprint-insertion-direction.ts
import { normalizeDegrees } from "@tscircuit/math-utils";
import {
  insertionDirectionToCanonical
} from "circuit-json";
import {
  applyToPoint,
  compose,
  flipY,
  identity,
  rotate
} from "transformation-matrix";
var inPlaneDirectionToVector = {
  from_left: { x: -1, y: 0 },
  from_right: { x: 1, y: 0 },
  from_top: { x: 0, y: 1 },
  from_bottom: { x: 0, y: -1 }
};
var isFootprintFlipped = (params) => {
  const { componentLayer, originalLayer } = params;
  return componentLayer === "bottom" !== (originalLayer === "bottom");
};
var transformFootprintInsertionDirection = (params) => {
  const { insertionDirection, rotationDegrees = 0, isFlipped = false } = params;
  if (!insertionDirection) return void 0;
  const direction = insertionDirectionToCanonical[insertionDirection];
  if (direction === "from_above" || direction === "from_below") {
    if (!isFlipped) return direction;
    return direction === "from_above" ? "from_below" : "from_above";
  }
  const transform = compose(
    rotate(normalizeDegrees(rotationDegrees) * Math.PI / 180),
    isFlipped ? flipY() : identity()
  );
  const finalVector = applyToPoint(
    transform,
    inPlaneDirectionToVector[direction]
  );
  if (Math.abs(finalVector.x) >= Math.abs(finalVector.y)) {
    return finalVector.x >= 0 ? "from_right" : "from_left";
  }
  return finalVector.y >= 0 ? "from_top" : "from_bottom";
};

// lib/utils/pcbSx/convert-pcb-style-to-pcb-sx.ts
function convertPcbStyleToPcbSx(pcbStyle) {
  if (!pcbStyle) return void 0;
  const sx = {};
  const silkscreenTextSx = {};
  if (pcbStyle.silkscreenFontSize !== void 0) {
    silkscreenTextSx.fontSize = pcbStyle.silkscreenFontSize;
  }
  if (pcbStyle.silkscreenTextVisibility !== void 0) {
    silkscreenTextSx.visibility = pcbStyle.silkscreenTextVisibility;
  }
  if (Object.keys(silkscreenTextSx).length > 0) {
    sx["& silkscreentext"] = silkscreenTextSx;
  }
  if (Object.keys(sx).length === 0) return void 0;
  return sx;
}

// lib/utils/pcbSx/get-resolved-pcb-sx.ts
function getResolvedPcbSx({
  parentResolvedPcbSx,
  pcbStyle,
  ownPcbSx
}) {
  const styleSx = convertPcbStyleToPcbSx(pcbStyle);
  const result = {};
  const allKeys = /* @__PURE__ */ new Set();
  if (parentResolvedPcbSx) {
    for (const k of Object.keys(parentResolvedPcbSx)) allKeys.add(k);
  }
  if (styleSx) {
    for (const k of Object.keys(styleSx)) allKeys.add(k);
  }
  if (ownPcbSx) {
    for (const k of Object.keys(ownPcbSx)) allKeys.add(k);
  }
  for (const key of allKeys) {
    result[key] = {
      ...parentResolvedPcbSx?.[key],
      ...styleSx?.[key],
      ...ownPcbSx?.[key]
    };
  }
  return result;
}

// lib/utils/schematic/getRotatedSymbolName.ts
function rotateDirection(direction, ccwRotationDegrees) {
  const directions = ["right", "up", "left", "down"];
  const currentIndex = directions.indexOf(direction);
  if (currentIndex === -1) return direction;
  const steps = Math.round(ccwRotationDegrees / 90);
  const newIndex = (currentIndex + steps) % 4;
  return directions[newIndex < 0 ? newIndex + 4 : newIndex];
}
function getRotatedSymbolName(symbolName, ccwRotationDegrees) {
  if (!symbolName) return symbolName;
  let rotatedSymbolName = symbolName;
  const schematicSymbolDirection = rotatedSymbolName.match(
    /_(right|left|up|down)$/
  );
  if (schematicSymbolDirection) {
    rotatedSymbolName = rotatedSymbolName.replace(
      schematicSymbolDirection[0],
      `_${rotateDirection(schematicSymbolDirection[1], ccwRotationDegrees)}`
    );
  }
  const schematicSymbolOrientation = rotatedSymbolName.match(/_(horz|vert)$/);
  if (schematicSymbolOrientation && shouldSwapOrientation(ccwRotationDegrees)) {
    rotatedSymbolName = rotatedSymbolName.replace(
      schematicSymbolOrientation[0],
      schematicSymbolOrientation[1] === "horz" ? "_vert" : "_horz"
    );
  }
  return rotatedSymbolName;
}
function shouldSwapOrientation(ccwRotationDegrees) {
  const normalizedRotation = (Math.round(ccwRotationDegrees) % 360 + 360) % 360;
  return normalizedRotation === 90 || normalizedRotation === 270;
}
function getRotatedSize(size, ccwRotationDegrees) {
  if (!shouldSwapOrientation(ccwRotationDegrees)) return size;
  return { width: size.height, height: size.width };
}

// lib/utils/selector-matching/is-matching-selector.ts
function isMatchingSelector(component, selector) {
  const idMatch = selector.match(/^#(\w+)/);
  if (idMatch) {
    return component.props.id === idMatch[1];
  }
  const classMatch = selector.match(/^\.(\w+)/);
  if (classMatch) {
    return component.isMatchingNameOrAlias(classMatch[1]);
  }
  let [type, ...conditions] = selector.split(/(?=[#.[])/);
  if (type === "pin") type = "port";
  if (type && type !== "*" && component.lowercaseComponentName !== type.toLowerCase()) {
    return false;
  }
  return conditions.every((condition) => {
    if (condition.startsWith("#")) {
      return component.props.id === condition.slice(1);
    }
    if (condition.startsWith(".")) {
      return component.isMatchingNameOrAlias(condition.slice(1));
    }
    const match = condition.match(/\[(\w+)=['"]?(.+?)['"]?\]/);
    if (!match) return true;
    const [, prop, value] = match;
    return component.props[prop].toString() === value;
  });
}

// lib/components/base-components/PrimitiveComponent/PrimitiveComponent.ts
import { symbols } from "schematic-symbols";
import {
  applyToPoint as applyToPoint2,
  compose as compose2,
  flipY as flipY2,
  identity as identity2,
  rotate as rotate2,
  translate
} from "transformation-matrix";
import { z } from "zod";

// lib/components/base-components/PrimitiveComponent/cssSelectPrimitiveComponentAdapter.ts
var cssSelectPrimitiveComponentAdapter = {
  // Is the node an element?
  isTag: (node) => true,
  // Get the parent of the node
  getParent: (node) => node.parent,
  // Get the children of the node
  getChildren: (node) => node.children,
  // Get the name of the tag
  getName: (node) => node.lowercaseComponentName,
  // Get the attribute value
  getAttributeValue: (node, name) => {
    if (name === "class" && "getNameAndAliases" in node) {
      return node.getNameAndAliases().join(" ");
    }
    if (name === "name" && node._parsedProps?.name) {
      return node._parsedProps.name;
    }
    if (node._parsedProps && name in node._parsedProps) {
      const value = node._parsedProps[name];
      return typeof value === "string" ? value : value !== null && value !== void 0 ? String(value) : null;
    }
    const matchingParsedPropName = Object.keys(node._parsedProps ?? {}).find(
      (propName) => propName.toLowerCase() === name
    );
    if (matchingParsedPropName) {
      const value = node._parsedProps[matchingParsedPropName];
      return typeof value === "string" ? value : value !== null && value !== void 0 ? String(value) : null;
    }
    if (name in node) {
      const value = node[name];
      return typeof value === "string" ? value : value !== null && value !== void 0 ? String(value) : null;
    }
    const reverseMap = node._attributeLowerToCamelNameMap;
    if (reverseMap) {
      const camelCaseName = reverseMap[name];
      if (camelCaseName && camelCaseName in node) {
        const value = node[camelCaseName];
        return typeof value === "string" ? value : value !== null && value !== void 0 ? String(value) : null;
      }
    }
    return null;
  },
  // Check if a node has an attribute
  hasAttrib: (node, name) => {
    if (name === "class") {
      return !!node._parsedProps?.name;
    }
    if (node._parsedProps && name in node._parsedProps) {
      return true;
    }
    if (Object.keys(node._parsedProps ?? {}).some(
      (propName) => propName.toLowerCase() === name
    )) {
      return true;
    }
    if (name in node) {
      return true;
    }
    const reverseMap = node._attributeLowerToCamelNameMap;
    if (reverseMap) {
      const camelCaseName = reverseMap[name];
      if (camelCaseName && camelCaseName in node) {
        return true;
      }
    }
    return false;
  },
  // Get the siblings of the node
  getSiblings: (node) => {
    if (!node.parent) return [];
    return node.parent.children;
  },
  // Get the previous sibling
  prevElementSibling: (node) => {
    if (!node.parent) return null;
    const siblings = node.parent.children;
    const idx = siblings.indexOf(node);
    return idx > 0 ? siblings[idx - 1] : null;
  },
  // Get the text content
  getText: () => "",
  // Remove the node
  removeSubsets: (nodes) => {
    return nodes.filter(
      (node, i) => !nodes.some(
        (other, j) => i !== j && other !== node && other.getDescendants().includes(node)
      )
    );
  },
  // Determine if element a is a subset of element b
  existsOne: (test, nodes) => {
    return nodes.some(test);
  },
  // Find all elements matching a selector
  findAll: (test, nodes) => {
    const result = [];
    const recurse = (node) => {
      if (test(node)) {
        result.push(node);
      }
      for (const child of node.children) {
        recurse(child);
      }
    };
    for (const node of nodes) {
      recurse(node);
    }
    return result;
  },
  // Find one element matching a selector
  findOne: (test, nodes) => {
    for (const node of nodes) {
      if (test(node)) return node;
      const children = node.children;
      if (children.length > 0) {
        const result = cssSelectPrimitiveComponentAdapter.findOne(
          test,
          children
        );
        if (result) return result;
      }
    }
    return null;
  },
  equals: (a, b) => {
    return a._renderId === b._renderId;
  },
  isHovered: (elem) => false,
  isVisited: (elem) => false,
  isActive: (elem) => false
};
var cssSelectPrimitiveComponentAdapterWithoutSubcircuits = {
  ...cssSelectPrimitiveComponentAdapter,
  getChildren: (node) => node.children.filter((c) => !c.isSubcircuit)
};
var cssSelectPrimitiveComponentAdapterOnlySubcircuits = {
  ...cssSelectPrimitiveComponentAdapter,
  getChildren: (node) => node.children.filter((c) => c.isSubcircuit)
};

// lib/components/base-components/PrimitiveComponent/preprocessSelector.ts
var buildPlusMinusNetErrorMessage = (selector, component) => {
  const netName = selector.split("net.")[1]?.split(/[ >]/)[0] ?? selector;
  const componentName = component?.componentName ?? "Unknown component";
  return `Net names cannot contain "+" or "-" (component "${componentName}" received "${netName}" via "${selector}"). Try using underscores instead, e.g. VCC_P`;
};
var preprocessSelector = (selector, component) => {
  if (/net\.[^\s>]*\./.test(selector)) {
    throw new Error(
      'Net names cannot contain a period, try using "sel.net..." to autocomplete with conventional net names, e.g. V3_3'
    );
  }
  if (/net\.[^\s>]*[+-]/.test(selector)) {
    throw new Error(buildPlusMinusNetErrorMessage(selector, component));
  }
  if (/net\.[0-9]/.test(selector)) {
    const match = selector.match(/net\.([^ >]+)/);
    const netName = match ? match[1] : "";
    throw new Error(
      `Net name "${netName}" cannot start with a number, try using a prefix like "VBUS1"`
    );
  }
  return selector.replace(/ pin(?=[\d.])/g, " port").replace(/ subcircuit\./g, " group[isSubcircuit=true]").replace(/([^ ])\>([^ ])/g, "$1 > $2").replace(
    /(^|[ >])(?!pin\.)(?!port\.)(?!net\.)([A-Z][A-Za-z0-9_-]*)\.([A-Za-z0-9_-]+)/g,
    (_, sep, name, pin) => {
      const pinPart = /^\d+$/.test(pin) ? `pin${pin}` : pin;
      return `${sep}.${name} > .${pinPart}`;
    }
  ).trim();
};

// lib/components/base-components/PrimitiveComponent/PrimitiveComponent.ts
var cssSelectOptionsInsideSubcircuit = {
  adapter: cssSelectPrimitiveComponentAdapterWithoutSubcircuits,
  cacheResults: true
};
var basePrimitiveComponentConfig = {
  componentName: "",
  zodProps: z.object({}).passthrough()
};
var PrimitiveComponent2 = class extends Renderable {
  parent = null;
  children;
  childrenPendingRemoval;
  get config() {
    return basePrimitiveComponentConfig;
  }
  props;
  _parsedProps;
  get componentName() {
    return this.config.componentName;
  }
  getInheritedProperty(propertyName) {
    let current = this;
    while (current) {
      if (current._parsedProps && propertyName in current._parsedProps) {
        return current._parsedProps[propertyName];
      }
      current = current.parent;
    }
    if (this.root?.platform && propertyName in this.root.platform) {
      return this.root.platform[propertyName];
    }
    return void 0;
  }
  getInheritedMergedProperty(propertyName) {
    const parentPropertyObject = this.parent?.getInheritedMergedProperty?.(propertyName);
    const myPropertyObject = this._parsedProps?.[propertyName];
    return { ...parentPropertyObject, ...myPropertyObject };
  }
  getResolvedPcbSx() {
    return getResolvedPcbSx({
      parentResolvedPcbSx: this.parent?.getResolvedPcbSx?.(),
      pcbStyle: this._parsedProps?.pcbStyle,
      ownPcbSx: this._parsedProps?.pcbSx
    });
  }
  getSchematicSectionName() {
    return this._parsedProps?.schSectionName ?? null;
  }
  get lowercaseComponentName() {
    return this.componentName.toLowerCase();
  }
  externallyAddedAliases;
  /**
   * An subcircuit is self-contained. All the selectors inside
   * a subcircuit are relative to the subcircuit group. You can have multiple
   * subcircuits and their selectors will not interact with each other (even if the
   * components share the same names) unless you explicitly break out some ports
   */
  get isSubcircuit() {
    return Boolean(this.props.subcircuit) || this.lowercaseComponentName === "group" && this?.parent?.isRootCircuit;
  }
  get isGroup() {
    return this.lowercaseComponentName === "group";
  }
  get isRoutingDirective() {
    return false;
  }
  get name() {
    return this._parsedProps.name ?? this.fallbackUnassignedName;
  }
  /**
   * A primitive container is a component that contains one or more ports and
   * primitive components that are designed to interact.
   *
   * For example a resistor contains ports and smtpads that interact, so the
   * resistor is a primitive container. Inside a primitive container, the ports
   * and pads are likely to reference each other and look for eachother during
   * the port matching phase.
   *
   */
  isPrimitiveContainer = false;
  canHaveTextChildren = false;
  source_group_id = null;
  source_component_id = null;
  schematic_component_id = null;
  schematic_sheet_id = null;
  pcb_component_id = null;
  cad_component_id = null;
  _reportedInvalidPcbCalcWarnings = /* @__PURE__ */ new Set();
  _reportInvalidComponentPropertyError(propertyName, message) {
    if (!this.root || this._reportedInvalidPcbCalcWarnings.has(propertyName)) {
      return;
    }
    this.root.db.source_invalid_component_property_error.insert({
      source_component_id: this.source_component_id || "",
      property_name: propertyName,
      message,
      error_type: "source_invalid_component_property_error"
    });
    this._reportedInvalidPcbCalcWarnings.add(propertyName);
  }
  fallbackUnassignedName;
  constructor(props) {
    super(props);
    this.children = [];
    this.childrenPendingRemoval = [];
    this.props = props ?? {};
    this.externallyAddedAliases = [];
    const zodProps = "partial" in this.config.zodProps ? this.config.zodProps.partial({
      name: true
    }) : this.config.zodProps;
    const parsePropsResult = zodProps.safeParse(props ?? {});
    if (parsePropsResult.success) {
      this._parsedProps = parsePropsResult.data;
    } else {
      throw new InvalidProps(
        this.lowercaseComponentName,
        this.props,
        parsePropsResult.error.format()
      );
    }
  }
  setProps(props) {
    const newProps = this.config.zodProps.parse({
      ...this.props,
      ...props
    });
    const oldProps = this.props;
    this.props = newProps;
    this._parsedProps = this.config.zodProps.parse(props);
    this.onPropsChange({
      oldProps,
      newProps,
      changedProps: Object.keys(props)
    });
    this.parent?.onChildChanged?.(this);
  }
  _getPcbRotationBeforeLayout() {
    const { pcbRotation } = this.props;
    if (typeof pcbRotation === "string") {
      return parseFloat(pcbRotation);
    }
    return pcbRotation ?? null;
  }
  getResolvedPcbPositionProp() {
    return {
      pcbX: this._resolvePcbCoordinate(this._parsedProps.pcbX, "pcbX"),
      pcbY: this._resolvePcbCoordinate(this._parsedProps.pcbY, "pcbY")
    };
  }
  doInitialValidatePcbCoordinates() {
    if (this.root?.pcbDisabled) return;
    const rawProps = this.props;
    const rawPcbX = rawProps.pcbX;
    const rawPcbY = rawProps.pcbY;
    this._validatePcbCoordinateReferences({
      rawValue: rawPcbX,
      axis: "pcbX",
      propertyName: "pcbX"
    });
    this._validatePcbCoordinateReferences({
      rawValue: rawPcbY,
      axis: "pcbY",
      propertyName: "pcbY"
    });
  }
  _validatePcbCoordinateReferences(params) {
    const { rawValue, axis, propertyName = axis } = params;
    if (typeof rawValue !== "string") return;
    const isNormalComponent = this._isNormalComponent === true;
    const allowComponentVariables = !isNormalComponent && !this._isInsideFootprint();
    let calcIdentifiers = [];
    try {
      calcIdentifiers = extractCalcIdentifiers(rawValue);
    } catch {
      this._reportInvalidComponentPropertyError(
        propertyName,
        `Invalid ${propertyName} value for ${this.componentName}: Invalid calc() expression. expression="${rawValue}"`
      );
      return;
    }
    const includesComponentVariable = calcIdentifiers.some(
      (identifier) => !identifier.startsWith("board.")
    );
    if (includesComponentVariable && !allowComponentVariables) {
      this._reportInvalidComponentPropertyError(
        propertyName,
        `Invalid ${propertyName} value for ${this.componentName}: component-relative calc references are not supported for footprint elements (${this.componentName}); ${propertyName} will be ignored. expression="${rawValue}"`
      );
    }
  }
  _resolvePcbCoordinate(rawValue, axis, options = {}) {
    if (rawValue == null) return 0;
    const propertyName = options.propertyName ?? axis;
    if (typeof rawValue === "number") {
      if (Number.isFinite(rawValue)) return rawValue;
      return 0;
    }
    if (typeof rawValue !== "string") {
      throw new Error(
        `Invalid ${axis} value for ${this.componentName}: ${String(rawValue)}`
      );
    }
    const allowBoardVariables = options.allowBoardVariables ?? this._shouldAllowBoardVariablesByDefault();
    const isNormalComponent = this._isNormalComponent === true;
    const allowComponentVariables = options.allowComponentVariables ?? (!isNormalComponent && !this._isInsideFootprint());
    const includesBoardVariable = rawValue.includes("board.");
    const knownVariables = {};
    if (allowBoardVariables) {
      const board = this._getBoard();
      const boardVariables = board?._getBoardCalcVariables() ?? {};
      if (includesBoardVariable && !board) {
        this._reportInvalidComponentPropertyError(
          propertyName,
          `Invalid ${propertyName} value for ${this.componentName}: no board found for board.* variables. expression="${rawValue}"`
        );
        return 0;
      }
      if (includesBoardVariable && board && Object.keys(boardVariables).length === 0) {
        this._reportInvalidComponentPropertyError(
          propertyName,
          `Invalid ${propertyName} value for ${this.componentName}: Cannot do calculations based on board size when the board is auto-sized. expression="${rawValue}"`
        );
        return 0;
      }
      Object.assign(knownVariables, boardVariables);
    }
    if (allowComponentVariables) {
      const db = this.root?.db;
      if (db) {
        Object.assign(knownVariables, getSubcircuitPcbCalcVariables(db));
      }
      Object.assign(knownVariables, options.componentVariables ?? {});
    }
    try {
      const calcIdentifiers = extractCalcIdentifiers(rawValue);
      const includesComponentVariable = calcIdentifiers.some(
        (identifier) => !identifier.startsWith("board.")
      );
      if (includesComponentVariable && !allowComponentVariables) {
        if (this._isInsideFootprint() && this.root && !this._reportedInvalidPcbCalcWarnings.has(axis)) {
          this.root.db.source_invalid_component_property_error.insert({
            source_component_id: this.source_component_id || "",
            property_name: axis,
            message: `component-relative calc references are not supported for footprint elements (${this.componentName}); ${axis} will be ignored. expression="${rawValue}"`,
            error_type: "source_invalid_component_property_error"
          });
          this._reportedInvalidPcbCalcWarnings.add(axis);
        }
        return 0;
      }
      return evaluateCalcString(rawValue, { knownVariables });
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      this._reportInvalidComponentPropertyError(
        propertyName,
        `Invalid ${propertyName} value for ${this.componentName}: ${message}. expression="${rawValue}"`
      );
      return 0;
    }
  }
  _shouldAllowBoardVariablesByDefault() {
    const isNormalComponent = this._isNormalComponent === true;
    if (isNormalComponent) return true;
    return !this._isInsideFootprint() && !this._isInsideNonBoardSubcircuit();
  }
  _isInsideFootprint() {
    let current = this.parent;
    while (current) {
      if (current.componentName === "Footprint") {
        return true;
      }
      current = current.parent;
    }
    return false;
  }
  _isInsideNonBoardSubcircuit() {
    let current = this.parent;
    while (current) {
      const componentName = current.componentName;
      if (componentName === "Board" || componentName === "MountedBoard") {
        return false;
      }
      if (current.isSubcircuit) {
        return true;
      }
      current = current.parent;
    }
    return false;
  }
  /**
   * Check if this component has a user-defined PCB position.
   * Position can be specified via pcbX/pcbY or edge-based props.
   */
  _hasUserDefinedPcbPosition() {
    const props = this._parsedProps;
    return props.pcbX !== void 0 || props.pcbY !== void 0 || props.pcbLeftEdgeX !== void 0 || props.pcbRightEdgeX !== void 0 || props.pcbTopEdgeY !== void 0 || props.pcbBottomEdgeY !== void 0;
  }
  resolvePcbCoordinate(params) {
    const {
      rawValue,
      axis,
      allowBoardVariables,
      allowComponentVariables,
      componentVariables,
      propertyName
    } = params;
    return this._resolvePcbCoordinate(rawValue, axis, {
      allowBoardVariables,
      allowComponentVariables,
      componentVariables,
      propertyName
    });
  }
  /**
   * Computes a transformation matrix from the props of this component for PCB
   * components
   */
  computePcbPropsTransform() {
    const rotation4 = this._getPcbRotationBeforeLayout() ?? 0;
    const { pcbX, pcbY } = this.getResolvedPcbPositionProp();
    const matrix = compose2(
      translate(pcbX, pcbY),
      rotate2(rotation4 * Math.PI / 180)
    );
    return matrix;
  }
  /**
   * Compute a transformation matrix combining all parent transforms for PCB
   * components, including this component's translation and rotation.
   *
   * This is used to compute this component's position as well as all children
   * components positions before layout is applied
   */
  _computePcbGlobalTransformBeforeLayout() {
    const manualPlacement = this.getSubcircuit()._getPcbManualPlacementForComponent(this);
    if (manualPlacement && this.props.pcbX === void 0 && this.props.pcbY === void 0) {
      const rotation4 = this._getPcbRotationBeforeLayout() ?? 0;
      return compose2(
        this.parent?._computePcbGlobalTransformBeforeLayout() ?? identity2(),
        compose2(
          translate(manualPlacement.x, manualPlacement.y),
          rotate2(rotation4 * Math.PI / 180)
        )
      );
    }
    if (this.isPcbPrimitive) {
      const { isFlipped } = this._getPcbPrimitiveFlippedHelpers();
      if (isFlipped) {
        return compose2(
          this.parent?._computePcbGlobalTransformBeforeLayout() ?? identity2(),
          flipY2(),
          this.computePcbPropsTransform()
        );
      }
    }
    return compose2(
      this.parent?._computePcbGlobalTransformBeforeLayout() ?? identity2(),
      this.computePcbPropsTransform()
    );
  }
  _getEnclosingFootprint() {
    let current = this.parent;
    while (current) {
      if (current.componentName === "Footprint") {
        return current;
      }
      current = current.parent;
    }
    return null;
  }
  getPrimitiveContainer() {
    if (this.isPrimitiveContainer) return this;
    return this.parent?.getPrimitiveContainer?.() ?? null;
  }
  /**
   * Get the Symbol ancestor if this component is inside a Symbol primitive container.
   * Used by schematic primitives to access the symbol's resize transform.
   */
  _getSymbolAncestor() {
    const container = this.getPrimitiveContainer();
    if (container?.componentName === "Symbol") {
      return container;
    }
    return null;
  }
  /**
   * Walk up the component hierarchy to find the nearest NormalComponent ancestor.
   * This is useful for primitive components that need access to component IDs
   * (pcb_component_id, schematic_component_id, source_component_id) from their
   * parent NormalComponent, even when there are intermediate primitive containers
   * like Symbol in the hierarchy.
   */
  getParentNormalComponent() {
    let current = this.parent;
    while (current) {
      if (current.isPrimitiveContainer && current.doInitialPcbComponentRender) {
        return current;
      }
      current = current.parent;
    }
    return null;
  }
  /**
   * Returns the nearest ancestor group that should be collapsed into a single
   * schematic box. Schematic render phases use this to suppress internal
   * schematic elements while still allowing direct group ports to render.
   */
  getCollapsedSchematicBoxAncestor() {
    let current = this.parent;
    while (current) {
      if (current.componentName === "Group" && current._parsedProps?.showAsSchematicBox) {
        return current;
      }
      current = current.parent;
    }
    return null;
  }
  /**
   * Replaces text like {NAME}, {REF}, and {REFERENCE} with the
   * reference designator (name) of the parent NormalComponent.
   */
  _resolveText() {
    const text = this._parsedProps.text;
    if (!text) return "";
    if (!text.includes("{NAME}") && !text.includes("{REF}") && !text.includes("{REFERENCE}")) {
      return text;
    }
    const parentNormalComponent = this.getParentNormalComponent();
    const refdes = parentNormalComponent?.name;
    if (!refdes) return text;
    return text.replace(/\{NAME\}/g, refdes).replace(/\{REF\}/g, refdes).replace(/\{REFERENCE\}/g, refdes);
  }
  /**
   * Emit a warning when coveredWithSolderMask is true but solderMaskMargin is also set
   */
  emitSolderMaskMarginWarning(isCoveredWithSolderMask, solderMaskMargin) {
    if (isCoveredWithSolderMask && solderMaskMargin !== void 0) {
      const parentNormalComponent = this.getParentNormalComponent();
      if (parentNormalComponent?.source_component_id) {
        this.root.db.source_property_ignored_warning.insert({
          source_component_id: parentNormalComponent.source_component_id,
          property_name: "solderMaskMargin",
          message: `solderMaskMargin is set but coveredWithSolderMask is true. When a component is fully covered with solder mask, a margin doesn't apply.`,
          error_type: "source_property_ignored_warning"
        });
      }
    }
  }
  /**
   * Compute the PCB bounds of this component the circuit json elements
   * associated with it.
   */
  _getPcbCircuitJsonBounds() {
    return {
      center: { x: 0, y: 0 },
      bounds: { left: 0, top: 0, right: 0, bottom: 0 },
      width: 0,
      height: 0
    };
  }
  _getPcbComponentLayer() {
    return this._parsedProps.layer;
  }
  /**
   * Determine if this pcb primitive should be flipped because the primitive
   * container is flipped
   *
   * TODO use footprint.originalLayer instead of assuming everything is defined
   * relative to the top layer
   */
  _getPcbPrimitiveFlippedHelpers() {
    const container = this.getPrimitiveContainer();
    const footprint = this.componentName === "Footprint" ? this : this._getEnclosingFootprint();
    const componentLayer = container?._getPcbComponentLayer();
    const isFlipped = !container ? false : isFootprintFlipped({
      componentLayer,
      originalLayer: footprint?._parsedProps.originalLayer
    });
    const maybeFlipLayer = (layer) => {
      if (isFlipped) {
        return layer === "top" ? "bottom" : "top";
      }
      return layer;
    };
    return { isFlipped, maybeFlipLayer };
  }
  /**
   * Set the position of this component from the layout solver. This method
   * should operate using CircuitJson associated with this component, like
   * _getPcbCircuitJsonBounds it can be called multiple times as different
   * parents apply layout to their children.
   */
  _setPositionFromLayout(newCenter) {
    throw new Error(
      `_setPositionFromLayout not implemented for ${this.componentName}`
    );
  }
  /**
   * Computes a transformation matrix from the props of this component for
   * schematic components
   */
  computeSchematicPropsTransform() {
    const { _parsedProps: props } = this;
    return compose2(translate(props.schX ?? 0, props.schY ?? 0));
  }
  /**
   * Compute a transformation matrix combining all parent transforms for this
   * component
   */
  computeSchematicGlobalTransform() {
    const manualPlacementTransform = this._getSchematicGlobalManualPlacementTransform(this);
    if (manualPlacementTransform) return manualPlacementTransform;
    return compose2(
      this.parent?.computeSchematicGlobalTransform?.() ?? identity2(),
      this.computeSchematicPropsTransform()
    );
  }
  _getSchematicSymbolName() {
    const { _parsedProps: props } = this;
    const base_symbol_name = this.config.schematicSymbolName;
    const orientationRotationMap = {
      horizontal: 0,
      pos_left: 0,
      neg_right: 0,
      pos_right: 180,
      neg_left: 180,
      pos_top: 270,
      neg_bottom: 90,
      vertical: 270,
      pos_bottom: 90,
      neg_top: 90
    };
    let normalizedRotation = props.schOrientation !== void 0 ? orientationRotationMap[props.schOrientation] : props.schRotation;
    if (normalizedRotation === void 0) {
      normalizedRotation = 0;
    }
    normalizedRotation = normalizedRotation % 360;
    if (normalizedRotation < 0) {
      normalizedRotation += 360;
    }
    if (props.schRotation !== void 0 && normalizedRotation % 90 !== 0) {
      throw new Error(
        `Schematic rotation ${props.schRotation} is not supported for ${this.componentName}`
      );
    }
    const hasOrientationOrDirectionSuffix = /_(horz|vert|up|down|left|right)$/.test(base_symbol_name);
    if (hasOrientationOrDirectionSuffix && base_symbol_name in symbols) {
      const rotatedSymbolName = getRotatedSymbolName(
        base_symbol_name,
        normalizedRotation
      );
      if (rotatedSymbolName && rotatedSymbolName in symbols) {
        return rotatedSymbolName;
      }
      return base_symbol_name;
    }
    const symbol_name_horz = `${base_symbol_name}_horz`;
    const symbol_name_vert = `${base_symbol_name}_vert`;
    const symbol_name_up = `${base_symbol_name}_up`;
    const symbol_name_down = `${base_symbol_name}_down`;
    const symbol_name_left = `${base_symbol_name}_left`;
    const symbol_name_right = `${base_symbol_name}_right`;
    if (symbol_name_right in symbols && normalizedRotation === 0) {
      return symbol_name_right;
    }
    if (symbol_name_up in symbols && normalizedRotation === 90) {
      return symbol_name_up;
    }
    if (symbol_name_left in symbols && normalizedRotation === 180) {
      return symbol_name_left;
    }
    if (symbol_name_down in symbols && normalizedRotation === 270) {
      return symbol_name_down;
    }
    if (symbol_name_horz in symbols) {
      if (normalizedRotation === 0) return symbol_name_horz;
      if (normalizedRotation === 180) return symbol_name_horz;
    }
    if (symbol_name_vert in symbols) {
      if (normalizedRotation === 90) return symbol_name_vert;
      if (normalizedRotation === 270) return symbol_name_vert;
    }
    if (base_symbol_name in symbols) return base_symbol_name;
    return void 0;
  }
  _getSchematicSymbolNameOrThrow() {
    const symbol_name = this._getSchematicSymbolName();
    if (!symbol_name) {
      throw new Error(
        `No schematic symbol found (given: "${this.config.schematicSymbolName}")`
      );
    }
    return symbol_name;
  }
  getSchematicSymbol() {
    const symbol_name = this._getSchematicSymbolName();
    if (!symbol_name) return null;
    return symbols[symbol_name] ?? null;
  }
  /**
   * Subcircuit groups have a prop called "layout" that can include manual
   * placements for pcb components. These are typically added from an IDE
   */
  _getPcbManualPlacementForComponent(component) {
    if (!this.isSubcircuit) return null;
    const manualEdits = this.props.manualEdits;
    if (!manualEdits) return null;
    const placementConfigPositions = manualEdits?.pcb_placements;
    if (!placementConfigPositions) return null;
    for (const position of placementConfigPositions) {
      if (isMatchingSelector(component, position.selector) || component.props.name === position.selector) {
        const center = applyToPoint2(
          this._computePcbGlobalTransformBeforeLayout(),
          position.center
        );
        return center;
      }
    }
    return null;
  }
  _getSchematicManualPlacementForComponent(component) {
    if (!this.isSubcircuit) return null;
    const manualEdits = this.props.manualEdits;
    if (!manualEdits) return null;
    const placementConfigPositions = manualEdits.schematic_placements;
    if (!placementConfigPositions) return null;
    for (const position of placementConfigPositions) {
      if (isMatchingSelector(component, position.selector) || component.props.name === position.selector) {
        const center = applyToPoint2(
          this.computeSchematicGlobalTransform(),
          position.center
        );
        return center;
      }
    }
    return null;
  }
  _getSchematicGlobalManualPlacementTransform(component) {
    const manualEdits = this.getSubcircuit()?._parsedProps.manualEdits;
    if (!manualEdits) return null;
    for (const position of manualEdits.schematic_placements ?? []) {
      if (isMatchingSelector(component, position.selector) || component.props.name === position.selector) {
        if (position.relative_to === "group_center") {
          return compose2(
            this.parent?._computePcbGlobalTransformBeforeLayout() ?? identity2(),
            translate(position.center.x, position.center.y)
          );
        }
      }
    }
    return null;
  }
  _getGlobalPcbPositionBeforeLayout() {
    return applyToPoint2(this._computePcbGlobalTransformBeforeLayout(), {
      x: 0,
      y: 0
    });
  }
  _getGlobalSchematicPositionBeforeLayout() {
    return applyToPoint2(this.computeSchematicGlobalTransform(), { x: 0, y: 0 });
  }
  _getBoard() {
    let current = this;
    while (current) {
      const maybePrimitive = current;
      const componentName = maybePrimitive.componentName;
      if (componentName === "Board" || componentName === "MountedBoard") {
        return maybePrimitive;
      }
      current = current.parent ?? null;
    }
    return this.root?._getBoard();
  }
  get root() {
    return this.parent?.root ?? null;
  }
  onAddToParent(parent) {
    this.parent = parent;
  }
  /**
   * Called whenever the props change
   */
  onPropsChange(params) {
  }
  onChildChanged(child) {
    this.parent?.onChildChanged?.(child);
  }
  add(component) {
    const textContent = component.__text;
    if (typeof textContent === "string") {
      if (this.canHaveTextChildren || textContent.trim() === "") {
        return;
      }
      const parentName = this._parsedProps?.name;
      const parentDescriptor = parentName ? `<${this.componentName} name="${parentName}">` : `<${this.componentName}>`;
      const isNaNValuedText = /^NaN/.test(textContent.trim());
      const nanHint = isNaNValuedText ? ` This looks like a numeric expression that evaluated to NaN (e.g. \`\${value}${textContent.trim().replace(
        /^NaN/,
        ""
      )}\` where \`value\` is NaN) \u2014 check the computation that produces this value.` : "";
      throw new Error(
        `Invalid JSX Element: ${parentDescriptor} received stray text "${textContent}" as a child, but it cannot hold text children. Remove the text or wrap it in an appropriate component.${nanHint}`
      );
    }
    if (Object.keys(component).length === 0) {
      return;
    }
    if (component.lowercaseComponentName === "panel") {
      throw new Error("<panel> must be a root-level element");
    }
    if (!component.onAddToParent) {
      throw new Error(
        `Invalid JSX Element: Expected a React component but received "${JSON.stringify(
          component
        )}"`
      );
    }
    component.onAddToParent(this);
    component.parent = this;
    this.children.push(component);
  }
  addAll(components) {
    for (const component of components) {
      this.add(component);
    }
  }
  remove(component) {
    this.children = this.children.filter((c) => c !== component);
    this.childrenPendingRemoval.push(component);
    component.shouldBeRemoved = true;
  }
  getSubcircuitSelector() {
    const name = this.name;
    const endPart = name ? `${this.lowercaseComponentName}.${name}` : this.lowercaseComponentName;
    if (!this.parent) return endPart;
    if (this.parent.isSubcircuit) return endPart;
    return `${this.parent.getSubcircuitSelector()} > ${endPart}`;
  }
  getFullPathSelector() {
    const name = this.name;
    const endPart = name ? `${this.lowercaseComponentName}.${name}` : this.lowercaseComponentName;
    const parentSelector = this.parent?.getFullPathSelector?.();
    if (!parentSelector) return endPart;
    return `${parentSelector} > ${endPart}`;
  }
  getNameAndAliases() {
    return [this.name, ...this._parsedProps.portHints ?? []].filter(Boolean);
  }
  isMatchingNameOrAlias(name) {
    return this.getNameAndAliases().includes(name);
  }
  isMatchingAnyOf(aliases) {
    return this.getNameAndAliases().some(
      (a) => aliases.map((a2) => a2.toString()).includes(a)
    );
  }
  getPcbSize() {
    throw new Error(`getPcbSize not implemented for ${this.componentName}`);
  }
  doesSelectorMatch(selector) {
    const myTypeNames = [this.componentName, this.lowercaseComponentName];
    const myClassNames = [this.name].filter(Boolean);
    const parts = selector.trim().split(/\> /)[0];
    const firstPart = parts[0];
    if (parts.length > 1) return false;
    if (selector === "*") return true;
    if (selector[0] === "#" && selector.slice(1) === this.props.id) return true;
    if (selector[0] === "." && myClassNames.includes(selector.slice(1)))
      return true;
    if (/^[a-zA-Z0-9_]/.test(firstPart) && myTypeNames.includes(firstPart))
      return true;
    return false;
  }
  getSubcircuit() {
    if (this.isSubcircuit) return this;
    const group = this.parent?.getSubcircuit?.();
    if (!group)
      throw new Error("Component is not inside an opaque group (no board?)");
    return group;
  }
  getGroup() {
    if (this.isGroup) return this;
    return this.parent?.getGroup?.() ?? null;
  }
  _resolveSchematicSheetId() {
    const schSheetName = this._parsedProps?.schSheetName ?? this.props.schSheetName;
    if (schSheetName) {
      const matchingSheet = this.root?.db.schematic_sheet.list().find((sheet) => sheet.name === schSheetName);
      if (matchingSheet) return matchingSheet.schematic_sheet_id;
    }
    if (this.schematic_sheet_id) return this.schematic_sheet_id;
    return this.parent?._resolveSchematicSheetId?.();
  }
  doInitialAssignNameToUnnamedComponents() {
    if (!this._parsedProps.name) {
      this.fallbackUnassignedName = this.getSubcircuit().getNextAvailableName(this);
    }
  }
  doInitialOptimizeSelectorCache() {
    if (!this.isSubcircuit) return;
    const ports = this.selectAll("port");
    const isValidShorthandName = (name) => /^[A-Za-z_][A-Za-z0-9_-]*$/.test(name);
    for (const port of ports) {
      const parentComponent = port.getParentNormalComponent?.() ?? port.parent;
      const parentAliases = parentComponent?.getNameAndAliases();
      const portAliases = port.getNameAndAliases();
      if (!parentAliases) continue;
      for (const parentAlias of parentAliases) {
        for (const portAlias of portAliases) {
          const selectors = [
            `.${parentAlias} > .${portAlias}`,
            `.${parentAlias} .${portAlias}`
          ];
          if (isValidShorthandName(parentAlias) && isValidShorthandName(portAlias)) {
            selectors.push(`${parentAlias}.${portAlias}`);
          }
          for (const selector of selectors) {
            const ar = this._cachedSelectAllQueries.get(selector);
            if (ar) {
              ar.push(port);
            } else {
              this._cachedSelectAllQueries.set(selector, [port]);
            }
          }
        }
      }
    }
    for (const [selector, ports2] of this._cachedSelectAllQueries.entries()) {
      if (ports2.length === 1) {
        this._cachedSelectOneQueries.set(selector, ports2[0]);
      }
    }
  }
  /**
   * The selector caches memoize results assuming the component tree is stable.
   * When the tree changes after the cache is first built (e.g. a connector adds
   * its ports asynchronously once a parts-engine fetch resolves), this phase is
   * re-marked dirty via _markDirty and re-runs as an update. Drop the stale
   * memoized results so later queries see the current tree, then re-warm the
   * subcircuit cache.
   */
  updateOptimizeSelectorCache() {
    this._cachedSelectAllQueries.clear();
    this._cachedSelectOneQueries.clear();
    this.doInitialOptimizeSelectorCache();
  }
  _cachedSelectAllQueries = /* @__PURE__ */ new Map();
  selectAll(selectorRaw) {
    if (this._cachedSelectAllQueries.has(selectorRaw)) {
      return this._cachedSelectAllQueries.get(selectorRaw);
    }
    const selector = preprocessSelector(selectorRaw, this);
    const result = selectAll(
      selector,
      this,
      cssSelectOptionsInsideSubcircuit
    );
    if (result.length > 0) {
      this._cachedSelectAllQueries.set(selectorRaw, result);
      return result;
    }
    const [firstpart, ...rest] = selector.split(" ");
    const subcircuit = selectOne(firstpart, this, {
      adapter: cssSelectPrimitiveComponentAdapterOnlySubcircuits
    });
    if (!subcircuit) return [];
    const result2 = subcircuit.selectAll(rest.join(" "));
    this._cachedSelectAllQueries.set(selectorRaw, result2);
    return result2;
  }
  _cachedSelectOneQueries = /* @__PURE__ */ new Map();
  selectOne(selectorRaw, options) {
    if (this._cachedSelectOneQueries.has(selectorRaw)) {
      return this._cachedSelectOneQueries.get(selectorRaw);
    }
    const selector = preprocessSelector(selectorRaw, this);
    if (options?.port) {
      options.type = "port";
    }
    let result = null;
    if (options?.type) {
      const allMatching = selectAll(
        selector,
        this,
        cssSelectOptionsInsideSubcircuit
      );
      result = allMatching.find(
        (n) => n.lowercaseComponentName === options.type
      );
    }
    if (!options?.type) {
      result ??= selectOne(
        selector,
        this,
        cssSelectOptionsInsideSubcircuit
      );
    }
    if (result) {
      this._cachedSelectOneQueries.set(selectorRaw, result);
      return result;
    }
    const [firstpart, ...rest] = selector.split(" ");
    const subcircuit = selectOne(firstpart, this, {
      adapter: cssSelectPrimitiveComponentAdapterOnlySubcircuits
    });
    if (!subcircuit) return null;
    result = subcircuit.selectOne(rest.join(" "), options);
    this._cachedSelectOneQueries.set(selectorRaw, result);
    return result;
  }
  getAvailablePcbLayers() {
    if (this.isPcbPrimitive) {
      const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
      if ("layer" in this._parsedProps || this.componentName === "SmtPad") {
        const layer = maybeFlipLayer(this._parsedProps.layer ?? "top");
        return [layer];
      }
      if ("layers" in this._parsedProps) {
        return this._parsedProps.layers;
      }
      if (this.componentName === "PlatedHole") {
        return [...this.root?._getBoard()?.allLayers ?? ["top", "bottom"]];
      }
      return [];
    }
    return [];
  }
  /**
   * Returns all descendants
   *
   * NOTE: This crosses subcircuit boundaries, you may want to use
   * getSelectableDescendants instead
   */
  getDescendants() {
    const descendants = [];
    for (const child of this.children) {
      descendants.push(child);
      descendants.push(...child.getDescendants());
    }
    return descendants;
  }
  /**
   * Returns all descendants that are accessible without crossing a subcircuit
   * boundary
   */
  getSelectableDescendants() {
    const descendants = [];
    for (const child of this.children) {
      if (child.isSubcircuit) {
        descendants.push(child);
      } else {
        descendants.push(child);
        descendants.push(...child.getSelectableDescendants());
      }
    }
    return descendants;
  }
  /**
   * Return the number of primary pins in this component, this is important for
   * NormalComponents
   */
  _getPrimaryPinCount() {
    return 0;
  }
  /**
   * If this component represents a SchematicBox (like a Chip), return the
   * dimensions of the box, which allows computing the position of ports etc.
   */
  _getSchematicBoxDimensions() {
    return null;
  }
  _getSchematicBoxComponentDimensions() {
    if (this.getSchematicSymbol()) return null;
    if (!this.config.shouldRenderAsSchematicBox) return null;
    const { _parsedProps: props } = this;
    const dimensions = {
      schWidth: props.schWidth,
      schHeight: props.schHeight
    };
    return dimensions;
  }
  // TODO we shouldn't need to override this, errors can be rendered and handled
  // by the Renderable class, however, the Renderable class currently doesn't
  // have access to the database or cleanup
  renderError(message) {
    if (typeof message === "string") {
      return super.renderError(message);
    }
    switch (message.type) {
      case "pcb_placement_error":
        this.root?.db.pcb_placement_error.insert(message);
        break;
      case "pcb_via_clearance_error":
        this.root?.db.pcb_via_clearance_error.insert(message);
        break;
      case "pcb_trace_error":
        this.root?.db.pcb_trace_error.insert(message);
        break;
      case "pcb_manual_edit_conflict_warning":
        this.root?.db.pcb_manual_edit_conflict_warning.insert(message);
        break;
      default:
        this.root?.db.pcb_placement_error.insert(message);
    }
  }
  getString() {
    const { lowercaseComponentName: cname, _parsedProps: props, parent } = this;
    if (props?.pinNumber !== void 0 && parent?.props?.name && props?.name) {
      return `<${cname}#${this._renderId}(pin:${props.pinNumber} .${parent?.props.name}>.${props.name}) />`;
    }
    if (parent?.props?.name && props?.name) {
      return `<${cname}#${this._renderId}(.${parent?.props.name}>.${props?.name}) />`;
    }
    if (props?.from && props?.to) {
      return `<${cname}#${this._renderId}(from:${props.from} to:${props?.to}) />`;
    }
    if (props?.name) {
      return `<${cname}#${this._renderId} name=".${props?.name}" />`;
    }
    if (props?.portHints) {
      return `<${cname}#${this._renderId}(${props.portHints.map((ph) => `.${ph}`).join(", ")}) />`;
    }
    return `<${cname}#${this._renderId} />`;
  }
  getDisplayName() {
    if (this.name) {
      return `${this.lowercaseComponentName} "${this.name}"`;
    }
    return `${this.lowercaseComponentName} (unnamed)`;
  }
  get [Symbol.toStringTag]() {
    return this.getString();
  }
  [/* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom")]() {
    return this.getString();
  }
};

// lib/utils/pairs.ts
function pairs(arr) {
  const result = [];
  for (let i = 0; i < arr.length - 1; i++) {
    result.push([arr[i], arr[i + 1]]);
  }
  return result;
}

// lib/utils/gnd-power-net-regex.ts
var GROUND_NET_REGEX = /^(GND|AGND|DGND|PGND|VSS)/i;
var POWER_NET_REGEX = /^V(?!SS)|^\d+(?:[_.]\d+)?[Vv]/i;

// lib/components/primitive-components/Net.ts
import { autoroute } from "@tscircuit/infgrid-ijump-astar";
var Net = class extends PrimitiveComponent2 {
  source_net_id;
  subcircuit_connectivity_map_key = null;
  get config() {
    return {
      componentName: "Net",
      zodProps: netProps
    };
  }
  getPortSelector() {
    return `net.${this.props.name}`;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const isGround = props.isGroundNet ?? GROUND_NET_REGEX.test(props.name);
    const isPositiveVoltageSource = props.isPowerNet ?? POWER_NET_REGEX.test(props.name);
    const net = db.source_net.insert({
      name: props.name,
      member_source_group_ids: [],
      is_ground: isGround,
      is_power: isPositiveVoltageSource,
      // @ts-ignore
      is_positive_voltage_source: isPositiveVoltageSource
    });
    this.source_net_id = net.source_net_id;
  }
  doInitialSourceParentAttachment() {
    const subcircuit = this.getSubcircuit();
    if (!subcircuit) return;
    const { db } = this.root;
    db.source_net.update(this.source_net_id, {
      subcircuit_id: subcircuit.subcircuit_id
    });
  }
  /**
   * Get all ports connected to this net.
   *
   * TODO currently we're not checking for indirect connections (traces that are
   * connected to other traces that are in turn connected to the net)
   */
  getAllConnectedPorts() {
    const allPorts = this.getSubcircuit().selectAll("port");
    const connectedPorts = [];
    for (const port of allPorts) {
      const traces = port._getDirectlyConnectedTraces();
      for (const trace of traces) {
        if (trace._isExplicitlyConnectedToNet(this)) {
          connectedPorts.push(port);
          break;
        }
      }
    }
    return connectedPorts;
  }
  /**
   * Get all traces that are directly connected to this net, i.e. they list
   * this net in their path, from, or to props
   */
  _getAllDirectlyConnectedTraces() {
    const allTraces = this.getSubcircuit().selectAll("trace");
    const connectedTraces = [];
    for (const trace of allTraces) {
      if (trace._isExplicitlyConnectedToNet(this)) {
        connectedTraces.push(trace);
      }
    }
    return connectedTraces;
  }
  /**
   * Add PCB Traces to connect net islands together. A net island is a set of
   * ports that are connected to each other. If a there are multiple net islands
   * that means that the net is not fully connected and we need to add traces
   * such that the nets are fully connected
   *
   * Sometimes this phase doesn't find any net islands if the autorouter did
   * a good job and connected the islands. In some sense this is a "backup"
   * routing phase for autorouters that don't care about connecting nets.
   *
   * This should only run if the autorouter is sequential-trace
   */
  doInitialPcbRouteNetIslands() {
    if (this.root?.pcbDisabled) return;
    const subcircuit = this.getSubcircuit();
    if (this.root?.pcbRoutingDisabled || subcircuit.getInheritedProperty("routingDisabled"))
      return;
    if (subcircuit._isInflatedFromCircuitJson) return;
    if (subcircuit._getAutorouterConfig().groupMode !== "sequential-trace")
      return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const traces = this._getAllDirectlyConnectedTraces().filter(
      (trace) => (trace._portsRoutedOnPcb?.length ?? 0) > 0
    );
    const islands = [];
    for (const trace of traces) {
      const tracePorts = trace._portsRoutedOnPcb;
      const traceIsland = islands.find(
        (island) => tracePorts.some((port) => island.ports.includes(port))
      );
      if (!traceIsland) {
        islands.push({ ports: [...tracePorts], traces: [trace] });
        continue;
      }
      traceIsland.traces.push(trace);
      traceIsland.ports.push(...tracePorts);
    }
    if (islands.length === 0) {
      return;
    }
    const islandPairs = pairs(islands);
    for (const [A, B] of islandPairs) {
      const Apositions = A.ports.map(
        (port) => port._getGlobalPcbPositionBeforeLayout()
      );
      const Bpositions = B.ports.map(
        (port) => port._getGlobalPcbPositionBeforeLayout()
      );
      let closestDist = Infinity;
      let closestPair = [-1, -1];
      for (let i = 0; i < Apositions.length; i++) {
        const Apos = Apositions[i];
        for (let j = 0; j < Bpositions.length; j++) {
          const Bpos = Bpositions[j];
          const dist = Math.sqrt(
            (Apos.x - Bpos.x) ** 2 + (Apos.y - Bpos.y) ** 2
          );
          if (dist < closestDist) {
            closestDist = dist;
            closestPair = [i, j];
          }
        }
      }
      const Aport = A.ports[closestPair[0]];
      const Bport = B.ports[closestPair[1]];
      const pcbElements = db.toArray().filter(
        (elm) => elm.type === "pcb_smtpad" || elm.type === "pcb_trace" || elm.type === "pcb_plated_hole" || elm.type === "pcb_hole" || elm.type === "source_port" || elm.type === "pcb_port"
      );
      const { solution } = autoroute(
        pcbElements.concat([
          {
            type: "source_trace",
            source_trace_id: "__net_trace_tmp",
            connected_source_port_ids: [
              Aport.source_port_id,
              Bport.source_port_id
            ]
          }
        ])
        // Remove as any when autorouting-dataset has been updated
      );
      const trace = solution[0];
      if (!trace) {
        this.renderError({
          pcb_trace_error_id: "",
          pcb_trace_id: "__net_trace_tmp",
          pcb_component_ids: [
            Aport.pcb_component_id,
            Bport.pcb_component_id
          ].filter(Boolean),
          pcb_port_ids: [Aport.pcb_port_id, Bport.pcb_port_id].filter(
            Boolean
          ),
          type: "pcb_trace_error",
          error_type: "pcb_trace_error",
          message: `Failed to route net islands for "${this.getString()}"`,
          source_trace_id: "__net_trace_tmp"
        });
        return;
      }
      db.pcb_trace.insert(trace);
    }
  }
  renderError(message) {
    if (typeof message === "string") {
      return super.renderError(message);
    }
    this.root?.db.pcb_trace_error.insert(message);
  }
};

// lib/utils/components/createNetsFromProps.ts
var createNetsFromProps = (component, props) => {
  for (const prop of props) {
    if (typeof prop === "string" && prop.startsWith("net.")) {
      if (/net\.[^\s>]*\./.test(prop)) {
        throw new Error(
          'Net names cannot contain a period, try using "sel.net..." to autocomplete with conventional net names, e.g. V3_3'
        );
      }
      if (/net\.[^\s>]*[+-]/.test(prop)) {
        const netName = prop.split("net.")[1];
        const message = `Net names cannot contain "+" or "-" (component "${component.componentName}" received "${netName}" via "${prop}"). Try using underscores instead, e.g. VCC_P`;
        throw new Error(message);
      }
      if (/net\.[0-9]/.test(prop)) {
        const netName = prop.split("net.")[1];
        throw new Error(
          `Net name "${netName}" cannot start with a number, try using a prefix like "VBUS1"`
        );
      }
      const subcircuit = component.getSubcircuit();
      if (!subcircuit.selectOne(prop)) {
        const net = new Net({
          name: prop.split("net.")[1]
        });
        subcircuit.add(net);
      }
    }
  }
};

// lib/utils/getClosest.ts
var getDistance = (a, b) => {
  const aPos = "_getGlobalPcbPositionBeforeLayout" in a ? a._getGlobalPcbPositionBeforeLayout() : a;
  const bPos = "_getGlobalPcbPositionBeforeLayout" in b ? b._getGlobalPcbPositionBeforeLayout() : b;
  return Math.sqrt((aPos.x - bPos.x) ** 2 + (aPos.y - bPos.y) ** 2);
};
function getClosest(point6, candidates) {
  if (candidates.length === 0)
    throw new Error("No candidates given to getClosest method");
  let closest = candidates[0];
  let closestDist = Infinity;
  for (const candidate of candidates) {
    const dist = getDistance(point6, candidate);
    if (dist < closestDist) {
      closest = candidate;
      closestDist = dist;
    }
  }
  return closest;
}

// lib/utils/is-route-outside-board.ts
import { point as point2 } from "@flatten-js/core";

// lib/utils/get-pcb-board-outline-polygon.ts
import { Box, point, Polygon } from "@flatten-js/core";
var getPcbBoardOutlinePolygon = (pcbBoard) => {
  if (pcbBoard.outline && pcbBoard.outline.length > 0) {
    return new Polygon(pcbBoard.outline.map((p) => point(p.x, p.y)));
  }
  return new Polygon(
    new Box(
      pcbBoard.center.x - pcbBoard.width / 2,
      pcbBoard.center.y - pcbBoard.height / 2,
      pcbBoard.center.x + pcbBoard.width / 2,
      pcbBoard.center.y + pcbBoard.height / 2
    ).toPoints()
  );
};

// lib/utils/is-route-outside-board.ts
var isRouteOutsideBoard = ({
  mergedRoute,
  db,
  pcbBoardId
}) => {
  const pcbBoard = db.pcb_board.get(pcbBoardId);
  if (!pcbBoard) return false;
  const boardOutlinePolygon = getPcbBoardOutlinePolygon(pcbBoard);
  return !mergedRoute.flatMap(getRoutePointPositions).every(
    (routePointPosition) => boardOutlinePolygon.contains(
      point2(routePointPosition.x, routePointPosition.y)
    )
  );
};

// lib/utils/getViaSpanLayers.ts
var getViaSpanLayers = ({
  fromLayer,
  toLayer,
  layerCount
}) => {
  if (fromLayer === toLayer) return [fromLayer];
  const stack = ["top"];
  for (let i = 1; i <= layerCount - 2; i++) {
    stack.push(`inner${i}`);
  }
  stack.push("bottom");
  const fromIndex = stack.indexOf(fromLayer);
  const toIndex = stack.indexOf(toLayer);
  if (fromIndex === -1 || toIndex === -1) return [fromLayer, toLayer];
  return fromIndex <= toIndex ? stack.slice(fromIndex, toIndex + 1) : stack.slice(toIndex, fromIndex + 1).reverse();
};
var getViaBoardLayers = (layerCount) => {
  if (layerCount <= 1) return ["top"];
  return getViaSpanLayers({
    fromLayer: "top",
    toLayer: "bottom",
    layerCount
  });
};
var getBoardAvailableLayers = (layerCount) => {
  const physicalLayerStack = getViaBoardLayers(layerCount);
  if (physicalLayerStack.length <= 1) return physicalLayerStack;
  return [
    physicalLayerStack[0],
    physicalLayerStack[physicalLayerStack.length - 1],
    ...physicalLayerStack.slice(1, -1)
  ];
};

// lib/utils/obstacles/fillCircleWithRects.ts
function fillCircleWithRects(circle, options = {}) {
  const { center, radius } = circle;
  const { rectHeight = 0.1 } = options;
  const rects = [];
  const numSlices = Math.ceil(radius * 2 / rectHeight);
  for (let i = 0; i < numSlices; i++) {
    const y = center.y - radius + (i + 0.5) * rectHeight;
    const dy = y - center.y;
    const halfWidth = Math.sqrt(radius * radius - dy * dy);
    if (halfWidth > 0) {
      rects.push({
        center: {
          x: center.x,
          y
        },
        width: halfWidth * 2,
        height: rectHeight
      });
    }
  }
  return rects;
}

// lib/utils/obstacles/fillPolygonWithRects.ts
function fillPolygonWithRects(polygon, options = {}) {
  if (polygon.length < 3) return [];
  const { rectHeight = 0.1 } = options;
  const rects = [];
  const yCoords = polygon.map((p) => p.y);
  const minY = Math.min(...yCoords);
  const maxY = Math.max(...yCoords);
  for (let y = minY; y < maxY; y += rectHeight) {
    const scanlineY = y + rectHeight / 2;
    const intersections = [];
    for (let i = 0; i < polygon.length; i++) {
      const p1 = polygon[i];
      const p2 = polygon[(i + 1) % polygon.length];
      if (p1.y <= scanlineY && p2.y > scanlineY || p2.y <= scanlineY && p1.y > scanlineY) {
        const x = (scanlineY - p1.y) * (p2.x - p1.x) / (p2.y - p1.y) + p1.x;
        intersections.push(x);
      }
    }
    intersections.sort((a, b) => a - b);
    for (let i = 0; i < intersections.length; i += 2) {
      if (i + 1 < intersections.length) {
        const x1 = intersections[i];
        const x2 = intersections[i + 1];
        const width = x2 - x1;
        if (width > 1e-6) {
          rects.push({
            center: {
              x: x1 + width / 2,
              y: scanlineY
            },
            width,
            height: rectHeight
          });
        }
      }
    }
  }
  return rects;
}

// lib/utils/obstacles/getAxisAlignedRectFromPolygon.ts
var AXIS_ALIGNED_RECT_TOLERANCE_MM = 1e-3;
var getAxisAlignedRectFromPolygon = (points) => {
  const isCloseTo2 = (a, b) => Math.abs(a - b) <= AXIS_ALIGNED_RECT_TOLERANCE_MM;
  const normalizedPoints = points.filter(
    (point6, index) => index === 0 || !isCloseTo2(point6.x, points[index - 1].x) || !isCloseTo2(point6.y, points[index - 1].y)
  );
  if (normalizedPoints.length > 1 && isCloseTo2(normalizedPoints[0].x, normalizedPoints.at(-1).x) && isCloseTo2(normalizedPoints[0].y, normalizedPoints.at(-1).y)) {
    normalizedPoints.pop();
  }
  if (normalizedPoints.length < 4) return null;
  const minX = Math.min(...normalizedPoints.map((point6) => point6.x));
  const maxX = Math.max(...normalizedPoints.map((point6) => point6.x));
  const minY = Math.min(...normalizedPoints.map((point6) => point6.y));
  const maxY = Math.max(...normalizedPoints.map((point6) => point6.y));
  const expectedCorners = [
    { x: minX, y: minY },
    { x: minX, y: maxY },
    { x: maxX, y: minY },
    { x: maxX, y: maxY }
  ];
  if (!normalizedPoints.every(
    (point6) => isCloseTo2(point6.x, minX) || isCloseTo2(point6.x, maxX) || isCloseTo2(point6.y, minY) || isCloseTo2(point6.y, maxY)
  ) || !expectedCorners.every(
    (corner) => normalizedPoints.some(
      (point6) => isCloseTo2(point6.x, corner.x) && isCloseTo2(point6.y, corner.y)
    )
  )) {
    return null;
  }
  return {
    center: { x: (minX + maxX) / 2, y: (minY + maxY) / 2 },
    width: maxX - minX,
    height: maxY - minY
  };
};

// lib/utils/obstacles/getObstaclesFromRoute.ts
var isCloseTo = (a, b) => Math.abs(a - b) < 1e-4;
var getObstaclesFromRoute = (route, source_trace_id, { viaDiameter = 0.5 } = {}) => {
  const obstacles = [];
  for (let i = 0; i < route.length - 1; i++) {
    const [start, end] = [route[i], route[i + 1]];
    const prev = i - 1 >= 0 ? route[i - 1] : null;
    const isHorz = isCloseTo(start.y, end.y);
    const isVert = isCloseTo(start.x, end.x);
    if (!isHorz && !isVert) {
      throw new Error(
        `getObstaclesFromTrace currently only supports horizontal and vertical traces (not diagonals) Conflicting trace: ${source_trace_id}, start: (${start.x}, ${start.y}), end: (${end.x}, ${end.y})`
      );
    }
    const obstacle = {
      type: "rect",
      layers: [start.layer],
      center: {
        x: (start.x + end.x) / 2,
        y: (start.y + end.y) / 2
      },
      width: isHorz ? Math.abs(start.x - end.x) : 0.1,
      // TODO use route width
      height: isVert ? Math.abs(start.y - end.y) : 0.1,
      // TODO use route width
      connectedTo: [source_trace_id]
    };
    obstacles.push(obstacle);
    if (prev && prev.layer === start.layer && start.layer !== end.layer) {
      const via = {
        type: "rect",
        layers: [start.layer, end.layer],
        center: {
          x: start.x,
          y: start.y
        },
        connectedTo: [source_trace_id],
        width: viaDiameter,
        height: viaDiameter
      };
      obstacles.push(via);
    }
  }
  return obstacles;
};

// lib/utils/obstacles/getObstaclesFromCircuitJson.ts
var QUARTER_TURN_TOLERANCE_DEGREES = 0.01;
var getAxisAlignedRectFromRotatedRect = (rotatedRect) => {
  const normalizedRotation = (rotatedRect.rotation % 360 + 360) % 360;
  const axisAlignedAngles = [0, 90, 180, 270];
  for (const angle of axisAlignedAngles) {
    const angularDistance = Math.min(
      Math.abs(normalizedRotation - angle),
      360 - Math.abs(normalizedRotation - angle)
    );
    if (angularDistance > QUARTER_TURN_TOLERANCE_DEGREES) continue;
    const isVertical = angle === 90 || angle === 270;
    return {
      center: rotatedRect.center,
      width: isVertical ? rotatedRect.height : rotatedRect.width,
      height: isVertical ? rotatedRect.width : rotatedRect.height
    };
  }
  return null;
};
var getObstaclesFromCircuitJson = (circuitJson, connMap) => {
  const board = circuitJson.find((element) => element.type === "pcb_board");
  const everyLayer = getViaBoardLayers(board?.num_layers ?? 4);
  const pcbComponentById = new Map(
    circuitJson.flatMap(
      (element) => element.type === "pcb_component" ? [[element.pcb_component_id, element]] : []
    )
  );
  const pcbPortById = new Map(
    circuitJson.flatMap(
      (element) => element.type === "pcb_port" ? [[element.pcb_port_id, element]] : []
    )
  );
  const sourceComponentById = new Map(
    circuitJson.flatMap(
      (element) => element.type === "source_component" ? [[element.source_component_id, element]] : []
    )
  );
  const sourcePortById = new Map(
    circuitJson.flatMap(
      (element) => element.type === "source_port" ? [[element.source_port_id, element]] : []
    )
  );
  const getReadableCircuitJsonMetadata = ({
    pcbComponentId,
    pcbPortId
  }) => {
    const pcbPort = pcbPortId ? pcbPortById.get(pcbPortId) : void 0;
    const sourcePort = pcbPort ? sourcePortById.get(pcbPort.source_port_id) : void 0;
    const sourceComponentId = (pcbComponentId ? pcbComponentById.get(pcbComponentId)?.source_component_id : void 0) ?? sourcePort?.source_component_id;
    const sourceComponent = sourceComponentId ? sourceComponentById.get(sourceComponentId) : void 0;
    return {
      ...sourceComponent?.name ? { source_component_name: sourceComponent.name } : {},
      ...sourcePort?.name ? { source_port_name: sourcePort.name } : {}
    };
  };
  const withNetId = (idList) => connMap ? idList.concat(
    idList.map((id) => connMap?.getNetConnectedToId(id)).filter(Boolean)
  ) : idList;
  const obstacles = [];
  for (const element of circuitJson) {
    const pcbComponentId = element.pcb_component_id ?? void 0;
    if (element.type === "pcb_smtpad") {
      const pcbSmtPadCircuitJsonMetadata = {
        pcb_smtpad_id: element.pcb_smtpad_id,
        pcb_port_id: element.pcb_port_id,
        ...getReadableCircuitJsonMetadata({
          pcbComponentId,
          pcbPortId: element.pcb_port_id
        })
      };
      if (element.shape === "circle") {
        obstacles.push({
          circuitJsonMetadata: pcbSmtPadCircuitJsonMetadata,
          componentId: pcbComponentId,
          type: "rect",
          shape: "circle",
          layers: [element.layer],
          center: {
            x: element.x,
            y: element.y
          },
          width: element.radius * 2,
          height: element.radius * 2,
          connectedTo: withNetId([element.pcb_smtpad_id])
        });
      } else if (element.shape === "rect") {
        obstacles.push({
          circuitJsonMetadata: pcbSmtPadCircuitJsonMetadata,
          componentId: pcbComponentId,
          type: "rect",
          layers: [element.layer],
          center: {
            x: element.x,
            y: element.y
          },
          width: element.width,
          height: element.height,
          connectedTo: withNetId([element.pcb_smtpad_id])
        });
      } else if (element.shape === "rotated_rect") {
        const rotatedRect = {
          center: { x: element.x, y: element.y },
          width: element.width,
          height: element.height,
          rotation: element.ccw_rotation
        };
        const axisAlignedRect = getAxisAlignedRectFromRotatedRect(rotatedRect);
        const rect = axisAlignedRect ?? rotatedRect;
        obstacles.push({
          circuitJsonMetadata: pcbSmtPadCircuitJsonMetadata,
          componentId: pcbComponentId,
          type: "rect",
          layers: [element.layer],
          center: rect.center,
          width: rect.width,
          height: rect.height,
          ccwRotationDegrees: axisAlignedRect ? void 0 : element.ccw_rotation,
          connectedTo: withNetId([element.pcb_smtpad_id])
        });
      } else if (element.shape === "pill" || element.shape === "rotated_pill") {
        obstacles.push({
          circuitJsonMetadata: pcbSmtPadCircuitJsonMetadata,
          componentId: pcbComponentId,
          type: "rect",
          layers: [element.layer],
          center: {
            x: element.x,
            y: element.y
          },
          width: element.width,
          height: element.height,
          ...element.shape === "rotated_pill" ? { ccwRotationDegrees: element.ccw_rotation } : {},
          connectedTo: withNetId([element.pcb_smtpad_id])
        });
      } else if (element.shape === "polygon") {
        const axisAlignedRect = getAxisAlignedRectFromPolygon(element.points);
        if (axisAlignedRect) {
          obstacles.push({
            circuitJsonMetadata: pcbSmtPadCircuitJsonMetadata,
            componentId: pcbComponentId,
            type: "rect",
            layers: [element.layer],
            ...axisAlignedRect,
            connectedTo: withNetId([element.pcb_smtpad_id])
          });
          continue;
        }
        const approximatingRects = fillPolygonWithRects(element.points, {
          rectHeight: 0.1
        });
        for (const rect of approximatingRects) {
          obstacles.push({
            circuitJsonMetadata: pcbSmtPadCircuitJsonMetadata,
            componentId: pcbComponentId,
            type: "rect",
            layers: [element.layer],
            center: rect.center,
            width: rect.width,
            height: rect.height,
            connectedTo: withNetId([element.pcb_smtpad_id])
          });
        }
      }
    } else if (element.type === "pcb_keepout") {
      if (element.shape === "circle") {
        obstacles.push({
          componentId: pcbComponentId,
          // @ts-ignore
          type: "oval",
          layers: element.layers,
          center: {
            x: element.center.x,
            y: element.center.y
          },
          width: element.radius * 2,
          height: element.radius * 2,
          connectedTo: []
        });
      } else if (element.shape === "rect") {
        obstacles.push({
          componentId: pcbComponentId,
          type: "rect",
          layers: element.layers,
          center: {
            x: element.center.x,
            y: element.center.y
          },
          width: element.width,
          height: element.height,
          connectedTo: []
        });
      }
    } else if (element.type === "pcb_cutout") {
      if (element.shape === "rect") {
        obstacles.push({
          componentId: pcbComponentId,
          type: "rect",
          layers: everyLayer,
          center: {
            x: element.center.x,
            y: element.center.y
          },
          width: element.width,
          height: element.height,
          connectedTo: []
        });
      } else if (element.shape === "circle") {
        const approximatingRects = fillCircleWithRects(
          {
            center: element.center,
            radius: element.radius
          },
          { rectHeight: 0.6 }
        );
        for (const rect of approximatingRects) {
          obstacles.push({
            componentId: pcbComponentId,
            type: "rect",
            layers: everyLayer,
            center: rect.center,
            width: rect.width,
            height: rect.height,
            connectedTo: []
          });
        }
      } else if (element.shape === "polygon") {
        const approximatingRects = fillPolygonWithRects(element.points, {
          rectHeight: 0.6
        });
        for (const rect of approximatingRects) {
          obstacles.push({
            componentId: pcbComponentId,
            type: "rect",
            layers: everyLayer,
            center: rect.center,
            width: rect.width,
            height: rect.height,
            connectedTo: []
          });
        }
      }
    } else if (element.type === "pcb_hole") {
      if (element.hole_shape === "oval") {
        obstacles.push({
          componentId: pcbComponentId,
          // @ts-ignore
          type: "oval",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.hole_width,
          height: element.hole_height,
          connectedTo: []
        });
      } else if (element.hole_shape === "pill" || element.hole_shape === "rotated_pill") {
        obstacles.push({
          componentId: pcbComponentId,
          type: "rect",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.hole_width,
          height: element.hole_height,
          ...element.hole_shape === "rotated_pill" ? { ccwRotationDegrees: element.ccw_rotation } : {},
          connectedTo: []
        });
      } else if (element.hole_shape === "rect") {
        obstacles.push({
          componentId: pcbComponentId,
          type: "rect",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.hole_width,
          height: element.hole_height,
          connectedTo: []
        });
      } else if (element.hole_shape === "square") {
        obstacles.push({
          componentId: pcbComponentId,
          type: "rect",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.hole_diameter,
          height: element.hole_diameter,
          connectedTo: []
        });
      } else if (
        // @ts-ignore
        element.hole_shape === "round" || element.hole_shape === "circle"
      ) {
        obstacles.push({
          componentId: pcbComponentId,
          type: "rect",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.hole_diameter,
          height: element.hole_diameter,
          connectedTo: []
        });
      }
    } else if (element.type === "pcb_plated_hole") {
      const pcbPlatedHoleCircuitJsonMetadata = {
        pcb_plated_hole_id: element.pcb_plated_hole_id,
        pcb_port_id: element.pcb_port_id,
        ...getReadableCircuitJsonMetadata({
          pcbComponentId,
          pcbPortId: element.pcb_port_id
        })
      };
      if (element.shape === "circle") {
        obstacles.push({
          circuitJsonMetadata: pcbPlatedHoleCircuitJsonMetadata,
          componentId: pcbComponentId,
          // @ts-ignore
          type: "oval",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.outer_diameter,
          height: element.outer_diameter,
          connectedTo: withNetId([element.pcb_plated_hole_id])
        });
      } else if (element.shape === "circular_hole_with_rect_pad") {
        obstacles.push({
          circuitJsonMetadata: pcbPlatedHoleCircuitJsonMetadata,
          componentId: pcbComponentId,
          // @ts-ignore
          type: "rect",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.rect_pad_width,
          height: element.rect_pad_height,
          connectedTo: withNetId([element.pcb_plated_hole_id])
        });
      } else if (element.shape === "oval") {
        obstacles.push({
          circuitJsonMetadata: pcbPlatedHoleCircuitJsonMetadata,
          componentId: pcbComponentId,
          // @ts-ignore
          type: "oval",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.outer_width,
          height: element.outer_height,
          connectedTo: withNetId([element.pcb_plated_hole_id])
        });
      } else if (element.shape === "pill") {
        obstacles.push({
          circuitJsonMetadata: pcbPlatedHoleCircuitJsonMetadata,
          componentId: pcbComponentId,
          type: "rect",
          layers: everyLayer,
          center: {
            x: element.x,
            y: element.y
          },
          width: element.outer_width,
          height: element.outer_height,
          ccwRotationDegrees: element.ccw_rotation,
          connectedTo: withNetId([element.pcb_plated_hole_id])
        });
      } else if (element.shape === "hole_with_polygon_pad") {
        if ("pad_outline" in element && element.pad_outline && element.pad_outline.length > 0) {
          const xs = element.pad_outline.map((p) => element.x + p.x);
          const ys = element.pad_outline.map((p) => element.y + p.y);
          const minX = Math.min(...xs);
          const maxX = Math.max(...xs);
          const minY = Math.min(...ys);
          const maxY = Math.max(...ys);
          const centerX = (minX + maxX) / 2;
          const centerY = (minY + maxY) / 2;
          obstacles.push({
            circuitJsonMetadata: pcbPlatedHoleCircuitJsonMetadata,
            componentId: pcbComponentId,
            // @ts-ignore
            type: "rect",
            layers: everyLayer,
            center: {
              x: centerX,
              y: centerY
            },
            width: maxX - minX,
            height: maxY - minY,
            connectedTo: withNetId([element.pcb_plated_hole_id])
          });
        }
      }
    } else if (element.type === "pcb_trace") {
      const traceObstacles = getObstaclesFromRoute(
        element.route.flatMap((rp) => {
          if (rp.route_type === "through_pad") {
            return [
              {
                x: rp.start.x,
                y: rp.start.y,
                layer: rp.start_layer
              },
              {
                x: rp.end.x,
                y: rp.end.y,
                layer: rp.end_layer
              }
            ];
          }
          return [
            {
              x: rp.x,
              y: rp.y,
              layer: rp.route_type === "wire" ? rp.layer : rp.from_layer
            }
          ];
        }),
        element.source_trace_id ?? element.pcb_trace_id
      );
      obstacles.push(
        ...traceObstacles.map((obstacle) => ({
          ...obstacle,
          componentId: pcbComponentId
        }))
      );
    } else if (element.type === "pcb_via") {
      const netIsAssignable = Boolean(
        element.net_is_assignable ?? element.netIsAssignable
      );
      obstacles.push({
        circuitJsonMetadata: {
          pcb_via_id: element.pcb_via_id,
          ...getReadableCircuitJsonMetadata({ pcbComponentId })
        },
        componentId: pcbComponentId,
        type: "rect",
        shape: "circle",
        layers: element.layers,
        center: {
          x: element.x,
          y: element.y
        },
        connectedTo: withNetId([element.pcb_via_id]),
        width: element.outer_diameter,
        height: element.outer_diameter,
        netIsAssignable: netIsAssignable || void 0
      });
    }
  }
  return obstacles;
};

// lib/utils/schematic/computeSchematicNetLabelCenter.ts
var getSchematicNetLabelTextWidth = ({
  text,
  font_size = 0.18
}) => {
  const fontScale = font_size / 0.18;
  const charWidth = 0.12 * fontScale;
  const horizontalPadding = 0.12 * fontScale;
  return text.length * charWidth + horizontalPadding;
};
var computeSchematicNetLabelCenter = ({
  anchor_position,
  anchor_side,
  text,
  font_size = 0.18
}) => {
  const width = getSchematicNetLabelTextWidth({ text, font_size });
  const height = font_size;
  const center = { ...anchor_position };
  switch (anchor_side) {
    case "right":
      center.x -= width / 2;
      break;
    case "left":
      center.x += width / 2;
      break;
    case "top":
      center.y -= height / 2;
      break;
    case "bottom":
      center.y += height / 2;
      break;
  }
  return center;
};

// lib/utils/schematic/countComplexElements.ts
var countComplexElements = (junctions, edges) => {
  let count = 0;
  count += junctions.length ?? 0;
  count += edges.filter((edge) => edge.is_crossing).length;
  for (let i = 1; i < edges.length; i++) {
    const prev = edges[i - 1];
    const curr = edges[i];
    const prevVertical = Math.abs(prev.from.x - prev.to.x) < 0.01;
    const currVertical = Math.abs(curr.from.x - curr.to.x) < 0.01;
    if (prevVertical !== currVertical) count++;
  }
  return count;
};

// lib/utils/schematic/getEnteringEdgeFromDirection.ts
var getEnteringEdgeFromDirection = (direction) => {
  return {
    up: "bottom",
    down: "top",
    left: "right",
    right: "left"
  }[direction] ?? null;
};

// lib/utils/schematic/getStubEdges.ts
import { distance } from "@tscircuit/math-utils";
var getStubEdges = ({
  firstEdge,
  firstEdgePort,
  firstDominantDirection,
  lastEdge,
  lastEdgePort,
  lastDominantDirection
}) => {
  if (firstEdge && firstEdgePort) {
    return getStubEdges({
      lastEdge: {
        from: firstEdge.to,
        to: firstEdge.from
      },
      lastEdgePort: firstEdgePort,
      lastDominantDirection: firstDominantDirection
    }).reverse().map((e) => ({
      from: e.to,
      to: e.from
    }));
  }
  let edges = [];
  if (lastEdge && lastEdgePort) {
    const intermediatePoint = { x: lastEdge.to.x, y: lastEdge.to.y };
    if (lastDominantDirection === "left" || lastDominantDirection === "right") {
      intermediatePoint.x = lastEdgePort.position.x;
      edges.push({
        from: lastEdge.to,
        to: { ...intermediatePoint }
      });
      edges.push({
        from: intermediatePoint,
        to: { ...lastEdgePort.position }
      });
    } else {
      intermediatePoint.y = lastEdgePort.position.y;
      edges.push({
        from: lastEdge.to,
        to: { ...intermediatePoint }
      });
      edges.push({
        from: intermediatePoint,
        to: { ...lastEdgePort.position }
      });
    }
  }
  edges = edges.filter((e) => distance(e.from, e.to) > 0.01);
  return edges;
};

// lib/utils/try-now.ts
function tryNow(fn) {
  try {
    return [fn(), null];
  } catch (e) {
    return [null, e];
  }
}

// lib/components/primitive-components/Trace/Trace.ts
import "zod";

// lib/errors/AutorouterError.ts
import packageJson from "@tscircuit/capacity-autorouter/package.json" with { type: "json" };
var autorouterVersion = packageJson.version ?? "unknown";
var AutorouterError = class extends Error {
  constructor(message) {
    super(`${message} (capacity-autorouter@${autorouterVersion})`);
    this.name = "AutorouterError";
  }
};

// lib/errors/TraceConnectionError.ts
var TraceConnectionError = class extends Error {
  constructor(errorData) {
    super(errorData.message);
    this.errorData = errorData;
    this.name = "TraceConnectionError";
  }
  errorData;
};

// lib/components/primitive-components/Trace/Trace__doInitialSchematicTraceRenderWithDisplayLabel.ts
function Trace__doInitialSchematicTraceRenderWithDisplayLabel(trace) {
  if (trace.root?.schematicDisabled) return;
  const { db } = trace.root;
  const { _parsedProps: props, parent } = trace;
  if (!parent) throw new Error("Trace has no parent");
  const { allPortsFound, portsWithSelectors: connectedPorts } = trace._findConnectedPorts();
  if (!allPortsFound) return;
  const portsWithPosition = connectedPorts.map(({ port }) => ({
    port,
    position: port._getGlobalSchematicPositionAfterLayout(),
    schematic_port_id: port.schematic_port_id,
    facingDirection: port.facingDirection
  }));
  if (portsWithPosition.length < 2) {
    throw new Error("Expected at least two ports in portsWithPosition.");
  }
  let fromPortName;
  let toPortName;
  const fromAnchorPos = portsWithPosition[0].position;
  const fromPort = portsWithPosition[0].port;
  if ("path" in trace.props) {
    if (trace.props.path.length !== 2) {
      throw new Error("Invalid 'path': Must contain exactly two elements.");
    }
    ;
    [fromPortName, toPortName] = trace.props.path;
  } else {
    if (!("from" in trace.props && "to" in trace.props)) {
      throw new Error("Missing 'from' or 'to' properties in props.");
    }
    fromPortName = trace.props.from;
    toPortName = trace.props.to;
  }
  if (!fromPort.source_port_id) {
    throw new Error(
      `Missing source_port_id for the 'from' port (${fromPortName}).`
    );
  }
  const toAnchorPos = portsWithPosition[1].position;
  const toPort = portsWithPosition[1].port;
  if (!toPort.source_port_id) {
    throw new Error(`Missing source_port_id for the 'to' port (${toPortName}).`);
  }
  const existingFromNetLabel = db.schematic_net_label.list().find((label) => label.source_net_id === fromPort.source_port_id);
  const existingToNetLabel = db.schematic_net_label.list().find((label) => label.source_net_id === toPort.source_port_id);
  const [firstPort, secondPort] = connectedPorts.map(({ port }) => port);
  const isFirstPortSchematicBox = firstPort.parent?.config.shouldRenderAsSchematicBox;
  const pinFullName = isFirstPortSchematicBox ? `${firstPort?.parent?.props.name}_${firstPort?.props.name}` : `${secondPort?.parent?.props.name}_${secondPort?.props.name}`;
  const netLabelText = trace._getSchematicNetLabelText() ?? pinFullName;
  if (existingFromNetLabel && existingFromNetLabel.text !== netLabelText) {
    existingFromNetLabel.text = `${netLabelText} / ${existingFromNetLabel.text}`;
  }
  if (existingToNetLabel && existingToNetLabel?.text !== netLabelText) {
    existingToNetLabel.text = `${netLabelText} / ${existingToNetLabel.text}`;
  }
  if (!existingToNetLabel) {
    const toSide = getEnteringEdgeFromDirection(toPort.facingDirection) ?? "bottom";
    db.schematic_net_label.insert({
      text: netLabelText,
      source_net_id: toPort.source_port_id,
      anchor_position: toAnchorPos,
      center: computeSchematicNetLabelCenter({
        anchor_position: toAnchorPos,
        anchor_side: toSide,
        text: netLabelText
      }),
      anchor_side: toSide,
      schematic_sheet_id: trace._resolveSchematicSheetId()
    });
  }
  if (!existingFromNetLabel) {
    const fromSide = getEnteringEdgeFromDirection(fromPort.facingDirection) ?? "bottom";
    db.schematic_net_label.insert({
      text: netLabelText,
      source_net_id: fromPort.source_port_id,
      anchor_position: fromAnchorPos,
      center: computeSchematicNetLabelCenter({
        anchor_position: fromAnchorPos,
        anchor_side: fromSide,
        text: netLabelText
      }),
      anchor_side: fromSide,
      schematic_sheet_id: trace._resolveSchematicSheetId()
    });
  }
}

// lib/components/primitive-components/Trace/Trace__findConnectedPorts.ts
function Trace__findConnectedPorts(trace) {
  const { _parsedProps: props, parent } = trace;
  if (!parent) throw new Error("Trace has no parent");
  const portSelectors = trace.getTracePortPathSelectors();
  const resolveImplicitSinglePort = (selector) => {
    const hasExplicitPortToken = selector.lastIndexOf(".") > selector.lastIndexOf(" ");
    if (hasExplicitPortToken) return null;
    let targetComponent = trace.getSubcircuit().selectOne(selector);
    if (!targetComponent && !/[.#\[]/.test(selector)) {
      targetComponent = trace.getSubcircuit().selectOne(`.${selector}`);
    }
    if (!targetComponent) return null;
    const ports = targetComponent.children.filter(
      (c) => c.componentName === "Port"
    );
    return ports.length === 1 ? ports[0] : null;
  };
  const portsWithSelectors = portSelectors.map((selector) => ({
    selector,
    port: trace.getSubcircuit().selectOne(selector, { type: "port" }) ?? resolveImplicitSinglePort(selector)
  }));
  for (const { selector, port } of portsWithSelectors) {
    if (!port) {
      let parentSelector;
      let portToken;
      const dotIndex = selector.lastIndexOf(".");
      if (dotIndex !== -1 && dotIndex > selector.lastIndexOf(" ")) {
        parentSelector = selector.slice(0, dotIndex);
        portToken = selector.slice(dotIndex + 1);
      } else {
        const match = selector.match(/^(.*[ >])?([^ >]+)$/);
        parentSelector = match?.[1]?.trim() ?? "";
        portToken = match?.[2] ?? selector;
      }
      let targetComponent = parentSelector ? trace.getSubcircuit().selectOne(parentSelector) : null;
      if (!targetComponent && parentSelector && !/[.#\[]/.test(parentSelector)) {
        targetComponent = trace.getSubcircuit().selectOne(`.${parentSelector}`);
      }
      if (!targetComponent) {
        const errorMessage2 = parentSelector ? `Could not find port for selector "${selector}". Component "${parentSelector}" not found` : `Could not find port for selector "${selector}"`;
        const subcircuit2 = trace.getSubcircuit();
        const sourceGroup2 = subcircuit2.getGroup();
        throw new TraceConnectionError({
          error_type: "source_trace_not_connected_error",
          message: errorMessage2,
          subcircuit_id: subcircuit2.subcircuit_id ?? void 0,
          source_group_id: sourceGroup2?.source_group_id ?? void 0,
          source_trace_id: trace.source_trace_id ?? void 0,
          selectors_not_found: [selector]
        });
      }
      const ports = targetComponent.children.filter(
        (c) => c.componentName === "Port"
      );
      const portLabel = portToken.includes(".") ? portToken.split(".").pop() ?? "" : portToken;
      const portNames = ports.flatMap((c) => c.getNameAndAliases());
      const hasCustomLabels = portNames.some((n) => !/^(pin\d+|\d+)$/.test(n));
      const labelList = Array.from(new Set(portNames)).join(", ");
      let detail;
      if (ports.length === 0) {
        detail = "It has no ports";
      } else if (!hasCustomLabels) {
        detail = `It has ${ports.length} pins and no pinLabels (consider adding pinLabels)`;
      } else {
        detail = `It has [${labelList}]`;
      }
      const errorMessage = `Could not find port for selector "${selector}". Component "${targetComponent.props.name ?? parentSelector}" found, but does not have pin "${portLabel}". ${detail}`;
      const subcircuit = trace.getSubcircuit();
      const sourceGroup = subcircuit.getGroup();
      throw new TraceConnectionError({
        error_type: "source_trace_not_connected_error",
        message: errorMessage,
        subcircuit_id: subcircuit.subcircuit_id ?? void 0,
        source_group_id: sourceGroup?.source_group_id ?? void 0,
        source_trace_id: trace.source_trace_id ?? void 0,
        selectors_not_found: [selector]
      });
    }
  }
  if (portsWithSelectors.some((p) => !p.port)) {
    return { allPortsFound: false };
  }
  return {
    allPortsFound: true,
    portsWithSelectors,
    ports: portsWithSelectors.map(({ port }) => port)
  };
}

// lib/components/primitive-components/Trace/trace-utils/compute-trace-length.ts
function getTraceLength(route) {
  let totalLength = 0;
  for (let i = 0; i < route.length; i++) {
    const point6 = route[i];
    if (point6.route_type === "wire") {
      const nextPoint = route[i + 1];
      if (nextPoint) {
        const nextPosition = getRoutePointPosition(nextPoint);
        const position = getRoutePointPosition(point6);
        const dx = nextPosition.x - position.x;
        const dy = nextPosition.y - position.y;
        totalLength += Math.sqrt(dx * dx + dy * dy);
      }
    } else if (point6.route_type === "via") {
      totalLength += 1.6;
    }
  }
  return totalLength;
}

// lib/components/primitive-components/Trace/Trace_doInitialPcbManualTraceRender.ts
import { applyToPoint as applyToPoint3, identity as identity3 } from "transformation-matrix";

// lib/utils/trace-clipping/computeLineRectIntersection.ts
function computeLineRectIntersection(params) {
  const { lineStart, lineEnd, rectCenter, rectWidth, rectHeight } = params;
  const left = rectCenter.x - rectWidth / 2;
  const right = rectCenter.x + rectWidth / 2;
  const top = rectCenter.y + rectHeight / 2;
  const bottom = rectCenter.y - rectHeight / 2;
  const dx = lineEnd.x - lineStart.x;
  const dy = lineEnd.y - lineStart.y;
  const intersections = [];
  if (dx !== 0) {
    const t = (left - lineStart.x) / dx;
    if (t >= 0 && t <= 1) {
      const y = lineStart.y + t * dy;
      if (y >= bottom && y <= top) {
        intersections.push({ x: left, y, t });
      }
    }
  }
  if (dx !== 0) {
    const t = (right - lineStart.x) / dx;
    if (t >= 0 && t <= 1) {
      const y = lineStart.y + t * dy;
      if (y >= bottom && y <= top) {
        intersections.push({ x: right, y, t });
      }
    }
  }
  if (dy !== 0) {
    const t = (bottom - lineStart.y) / dy;
    if (t >= 0 && t <= 1) {
      const x = lineStart.x + t * dx;
      if (x >= left && x <= right) {
        intersections.push({ x, y: bottom, t });
      }
    }
  }
  if (dy !== 0) {
    const t = (top - lineStart.y) / dy;
    if (t >= 0 && t <= 1) {
      const x = lineStart.x + t * dx;
      if (x >= left && x <= right) {
        intersections.push({ x, y: top, t });
      }
    }
  }
  if (intersections.length === 0) return null;
  intersections.sort((a, b) => a.t - b.t);
  return { x: intersections[0].x, y: intersections[0].y };
}

// lib/utils/trace-clipping/computeLineCircleIntersection.ts
function computeLineCircleIntersection(params) {
  const { lineStart, lineEnd, circleCenter, circleRadius } = params;
  const x1 = lineStart.x - circleCenter.x;
  const y1 = lineStart.y - circleCenter.y;
  const x2 = lineEnd.x - circleCenter.x;
  const y2 = lineEnd.y - circleCenter.y;
  const dx = x2 - x1;
  const dy = y2 - y1;
  const a = dx * dx + dy * dy;
  const b = 2 * (x1 * dx + y1 * dy);
  const c = x1 * x1 + y1 * y1 - circleRadius * circleRadius;
  const discriminant = b * b - 4 * a * c;
  if (discriminant < 0) return null;
  const sqrtDisc = Math.sqrt(discriminant);
  const t1 = (-b - sqrtDisc) / (2 * a);
  const t2 = (-b + sqrtDisc) / (2 * a);
  let t = null;
  if (t1 >= 0 && t1 <= 1) {
    t = t1;
  } else if (t2 >= 0 && t2 <= 1) {
    t = t2;
  }
  if (t === null) return null;
  return {
    x: lineStart.x + t * dx,
    y: lineStart.y + t * dy
  };
}

// lib/utils/trace-clipping/clipTraceEndAtPad.ts
function clipTraceEndAtPad(params) {
  const { traceStart, traceEnd, traceWidth, port } = params;
  const pcbPrimitive = port.matchedComponents.find((c) => c.isPcbPrimitive);
  if (!pcbPrimitive) {
    return traceEnd;
  }
  const padBounds = pcbPrimitive._getPcbCircuitJsonBounds();
  const padWidth = padBounds.width;
  const padHeight = padBounds.height;
  const padCenter = padBounds.center;
  const smallestPadDimension = Math.min(padWidth, padHeight);
  if (traceWidth <= smallestPadDimension / 2) {
    return traceEnd;
  }
  let clippedPoint = null;
  if (pcbPrimitive.componentName === "SmtPad") {
    const smtPad = pcbPrimitive;
    const padShape = smtPad._parsedProps.shape;
    if (padShape === "circle") {
      const radius = smtPad._parsedProps.radius;
      clippedPoint = computeLineCircleIntersection({
        lineStart: traceStart,
        lineEnd: traceEnd,
        circleCenter: padCenter,
        circleRadius: radius
      });
    } else if (padShape === "rect" || padShape === "rotated_rect" || padShape === "pill" || padShape === "polygon") {
      clippedPoint = computeLineRectIntersection({
        lineStart: traceStart,
        lineEnd: traceEnd,
        rectCenter: padCenter,
        rectWidth: padWidth,
        rectHeight: padHeight
      });
    }
  } else if (pcbPrimitive.componentName === "PlatedHole") {
    const platedHole = pcbPrimitive;
    const holeShape = platedHole._parsedProps.shape;
    if (holeShape === "circle") {
      const outerDiameter = platedHole._parsedProps.outerDiameter;
      clippedPoint = computeLineCircleIntersection({
        lineStart: traceStart,
        lineEnd: traceEnd,
        circleCenter: padCenter,
        circleRadius: outerDiameter / 2
      });
    } else {
      clippedPoint = computeLineRectIntersection({
        lineStart: traceStart,
        lineEnd: traceEnd,
        rectCenter: padCenter,
        rectWidth: padWidth,
        rectHeight: padHeight
      });
    }
  }
  return clippedPoint ?? traceEnd;
}

// lib/utils/pcbStyle/getViaDiameterDefaults.ts
var DEFAULT_VIA_HOLE_DIAMETER = 0.2;
var DEFAULT_VIA_PAD_DIAMETER = 0.3;
var parseDistance = (value, fallback) => {
  if (value === void 0) return fallback;
  if (typeof value === "number") return value;
  const parsed = parseFloat(value);
  return Number.isFinite(parsed) ? parsed : fallback;
};
var getViaDiameterDefaults = (pcbStyle) => {
  return {
    holeDiameter: parseDistance(
      pcbStyle?.viaHoleDiameter,
      DEFAULT_VIA_HOLE_DIAMETER
    ),
    padDiameter: parseDistance(
      pcbStyle?.viaPadDiameter,
      DEFAULT_VIA_PAD_DIAMETER
    )
  };
};
var getViaDiameterDefaultsWithOverrides = (overrides, pcbStyle) => {
  const defaults = getViaDiameterDefaults(pcbStyle);
  return {
    holeDiameter: overrides.holeDiameter ?? defaults.holeDiameter,
    padDiameter: overrides.padDiameter ?? defaults.padDiameter
  };
};

// lib/components/primitive-components/Port/areAllPcbPrimitivesOverlapping.ts
var areAllPcbPrimitivesOverlapping = (pcbPrimitives) => {
  if (pcbPrimitives.length <= 1) return true;
  const bounds = pcbPrimitives.map((p) => {
    const circuitBounds = p._getPcbCircuitJsonBounds();
    return {
      left: circuitBounds.bounds.left,
      right: circuitBounds.bounds.right,
      top: circuitBounds.bounds.top,
      bottom: circuitBounds.bounds.bottom
    };
  });
  const overlaps = Array(bounds.length).fill(false).map(() => Array(bounds.length).fill(false));
  for (let i = 0; i < bounds.length; i++) {
    for (let j = i + 1; j < bounds.length; j++) {
      const a = bounds[i];
      const b = bounds[j];
      overlaps[i][j] = overlaps[j][i] = !(a.right < b.left || a.left > b.right || a.bottom > b.top || a.top < b.bottom);
    }
  }
  const visited = /* @__PURE__ */ new Set();
  const dfs = (node) => {
    visited.add(node);
    for (let i = 0; i < bounds.length; i++) {
      if (overlaps[node][i] && !visited.has(i)) {
        dfs(i);
      }
    }
  };
  dfs(0);
  return visited.size === bounds.length;
};

// lib/components/primitive-components/Trace/getPcbSelectorErrorForTracePort.ts
function formatPcbPrimitiveForError(component) {
  const portHints = component._parsedProps?.portHints ?? [];
  const aliases = portHints.filter(Boolean);
  if (aliases.length > 0) {
    return `<${component.lowercaseComponentName}(${aliases.map((alias) => `.${alias}`).join(", ")}) />`;
  }
  return `<${component.lowercaseComponentName} />`;
}
function getPcbSelectorErrorForTracePort(selector, port) {
  const pcbMatches = port.matchedComponents.filter((c) => c.isPcbPrimitive);
  const parentName = port.getParentNormalComponent()?.props.name ?? port.parent?.props.name ?? "unknown";
  const parentNormalComponent = port.getParentNormalComponent();
  const selectorToken = selector.trim().split(/\s+|>/).filter(Boolean).at(-1)?.replace(/^port\./, "").replace(/^\./, "");
  if (selectorToken && !/^(pin)?\d+$/.test(selectorToken)) {
    const siblingPorts = (parentNormalComponent?.children ?? []).filter(
      (child) => child.componentName === "Port" && child.isMatchingAnyOf([selectorToken])
    );
    if (siblingPorts.length > 1) {
      const internallyConnectedGroups = parentNormalComponent?._getInternallyConnectedPins?.() ?? [];
      const matchingPortsAreInternallyConnected = internallyConnectedGroups.some(
        (group) => siblingPorts.every(
          (siblingPort) => group.includes(siblingPort)
        )
      );
      if (!matchingPortsAreInternallyConnected) {
        const rawPinSelectors2 = Array.from(
          new Set(
            siblingPorts.flatMap(
              (siblingPort) => siblingPort.getNameAndAliases().filter((alias) => /^pin\d+$/.test(alias)).map((alias) => `${parentName}.${alias}`)
            )
          )
        );
        const suggestion = rawPinSelectors2.length > 0 ? ` Use a raw pin selector like ${rawPinSelectors2.map((s) => `"${s}"`).join(" or ")}.` : "";
        const siblingPcbMatches = siblingPorts.flatMap(
          (siblingPort) => siblingPort.matchedComponents.filter(
            (component) => component.isPcbPrimitive
          )
        );
        return `Trace selector "${selector}" resolved to "${parentName}.${port.props.name}", but alias "${selectorToken}" matches multiple PCB pads: ${siblingPcbMatches.map((component) => formatPcbPrimitiveForError(component)).join(", ")}.${suggestion}`;
      }
    }
  }
  if (port.pcb_port_id) return null;
  const rawPinSelectors = Array.from(
    new Set(
      port.getNameAndAliases().filter((alias) => /^pin\d+$/.test(alias)).map((alias) => `${parentName}.${alias}`)
    )
  );
  if (pcbMatches.length > 1 && !areAllPcbPrimitivesOverlapping(pcbMatches)) {
    const suggestion = rawPinSelectors.length > 0 ? ` Use a raw pin selector like ${rawPinSelectors.map((s) => `"${s}"`).join(" or ")}.` : "";
    return `Trace selector "${selector}" resolved to "${parentName}.${port.props.name}", but that target maps to multiple non-overlapping PCB pads: ${pcbMatches.map((c) => formatPcbPrimitiveForError(c)).join(", ")}.${suggestion}`;
  }
  return null;
}

// node_modules/@tscircuit/jlcpcb-manufacturing-specs/lib/jlcpcb-manufacturing-specs.ts
var jlcMinTolerances = {
  min_trace_width: 0.1,
  min_via_hole_edge_to_via_hole_edge_clearance: 0.1,
  min_plated_hole_drill_edge_to_drill_edge_clearance: 0.15,
  min_trace_to_pad_edge_clearance: 0.1,
  min_pad_edge_to_pad_edge_clearance: 0.1,
  min_board_edge_clearance: 0.2,
  min_via_hole_diameter: 0.2,
  min_via_pad_diameter: 0.3
};

// lib/components/primitive-components/Trace/Trace_doInitialPcbManualTraceRender.ts
var findInflatedPcbViaForPoint = (vias, point6) => {
  if (point6.route_type !== "via") return void 0;
  return vias?.find(
    (via) => Math.abs(via.x - point6.x) < 1e-4 && Math.abs(via.y - point6.y) < 1e-4 && (!via.from_layer || via.from_layer === point6.from_layer) && (!via.to_layer || via.to_layer === point6.to_layer)
  );
};
var getViaDiameterFromRoutePoint = (point6) => {
  const viaPoint = point6;
  return {
    holeDiameter: viaPoint.hole_diameter ?? viaPoint.via_hole_diameter,
    outerDiameter: viaPoint.outer_diameter ?? viaPoint.via_diameter
  };
};
function Trace_doInitialPcbManualTraceRender(trace) {
  if (trace.root?.pcbDisabled) return;
  const { db } = trace.root;
  const { _parsedProps: props } = trace;
  const subcircuit = trace.getSubcircuit();
  const hasPcbPath = props.pcbPath !== void 0;
  const wantsStraightLine = Boolean(props.pcbStraightLine);
  const inflatedPcbTraces = trace._inflatedPcbTraces ?? [];
  if (!hasPcbPath && !wantsStraightLine && inflatedPcbTraces.length === 0)
    return;
  let allPortsFound;
  let ports;
  let portsWithSelectors;
  try {
    const connectedPorts = trace._findConnectedPorts();
    allPortsFound = connectedPorts.allPortsFound;
    ports = connectedPorts.ports ?? [];
    portsWithSelectors = connectedPorts.portsWithSelectors ?? [];
  } catch (error) {
    if (error instanceof TraceConnectionError) {
      db.source_trace_not_connected_error.insert({
        ...error.errorData,
        error_type: "source_trace_not_connected_error"
      });
      trace._couldNotFindPort = true;
      return;
    }
    throw error;
  }
  if (!allPortsFound) return;
  const pcbSelectorError = portsWithSelectors.map(
    ({ selector, port }) => getPcbSelectorErrorForTracePort(selector, port)
  ).find(Boolean);
  if (pcbSelectorError) {
    db.pcb_trace_error.insert({
      error_type: "pcb_trace_error",
      source_trace_id: trace.source_trace_id,
      message: pcbSelectorError,
      pcb_trace_id: trace.pcb_trace_id,
      pcb_component_ids: [],
      pcb_port_ids: ports.map((p) => p.pcb_port_id).filter(Boolean)
    });
    return;
  }
  const portsWithoutMatchedPcbPrimitive = [];
  for (const port of ports) {
    if (!port._hasMatchedPcbPrimitive()) {
      portsWithoutMatchedPcbPrimitive.push(port);
    }
  }
  if (portsWithoutMatchedPcbPrimitive.length > 0) {
    db.pcb_trace_error.insert({
      error_type: "pcb_trace_error",
      source_trace_id: trace.source_trace_id,
      message: `Some ports did not have a matching PCB primitive (e.g. a pad or plated hole), this can happen if a footprint is missing. As a result, ${trace} wasn't routed. Missing ports: ${portsWithoutMatchedPcbPrimitive.map((p) => p.getString()).join(", ")}`,
      pcb_trace_id: trace.pcb_trace_id,
      pcb_component_ids: [],
      pcb_port_ids: portsWithoutMatchedPcbPrimitive.map((p) => p.pcb_port_id).filter(Boolean)
    });
    return;
  }
  const width = trace._getExplicitTraceThickness() ?? trace.getSubcircuit()._parsedProps.minTraceWidth ?? jlcMinTolerances.min_trace_width;
  if (inflatedPcbTraces.length > 0) {
    const { maybeFlipLayer } = trace._getPcbPrimitiveFlippedHelpers();
    const transform2 = trace._computePcbGlobalTransformBeforeLayout();
    const insertedRoutes = [];
    const subcircuitConnectivityMapKey2 = trace.subcircuit_connectivity_map_key ?? db.source_trace.get(trace.source_trace_id)?.subcircuit_connectivity_map_key;
    for (const inflatedPcbTrace of inflatedPcbTraces) {
      const transformedRoute = inflatedPcbTrace.route.map((point6) => {
        if (point6.route_type === "wire") {
          const { x, y, ...restOfPoint } = point6;
          const transformedPoint = applyToPoint3(transform2, { x, y });
          return {
            ...restOfPoint,
            ...transformedPoint,
            layer: maybeFlipLayer(point6.layer)
          };
        }
        if (point6.route_type === "via") {
          const { x, y, ...restOfPoint } = point6;
          const transformedPoint = applyToPoint3(transform2, { x, y });
          return {
            ...restOfPoint,
            ...transformedPoint,
            from_layer: maybeFlipLayer(point6.from_layer),
            to_layer: maybeFlipLayer(point6.to_layer)
          };
        }
        return {
          ...point6,
          start: applyToPoint3(transform2, point6.start),
          end: applyToPoint3(transform2, point6.end),
          start_layer: maybeFlipLayer(point6.start_layer),
          end_layer: maybeFlipLayer(point6.end_layer)
        };
      });
      const pcb_trace2 = db.pcb_trace.insert({
        ...inflatedPcbTrace,
        route: transformedRoute,
        source_trace_id: trace.source_trace_id,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: trace.getGroup()?.pcb_group_id ?? void 0
      });
      const pcbStyle2 = trace.getInheritedMergedProperty("pcbStyle");
      const { holeDiameter: holeDiameter2, padDiameter: padDiameter2 } = getViaDiameterDefaults(pcbStyle2);
      for (let index = 0; index < transformedRoute.length; index++) {
        const point6 = transformedRoute[index];
        if (point6.route_type === "via") {
          const originalPoint = inflatedPcbTrace.route[index];
          const inflatedPcbVia = findInflatedPcbViaForPoint(
            trace._inflatedPcbVias,
            originalPoint
          );
          const routePointViaDiameter = getViaDiameterFromRoutePoint(point6);
          const fromLayer = maybeFlipLayer(
            inflatedPcbVia?.from_layer ?? point6.from_layer
          );
          const toLayer = maybeFlipLayer(
            inflatedPcbVia?.to_layer ?? point6.to_layer
          );
          const layers = (inflatedPcbVia?.layers ?? getViaSpanLayers({
            fromLayer: point6.from_layer,
            toLayer: point6.to_layer,
            layerCount: subcircuit._getSubcircuitLayerCount()
          })).map((layer2) => maybeFlipLayer(layer2));
          db.pcb_via.insert({
            pcb_trace_id: pcb_trace2.pcb_trace_id,
            x: point6.x,
            y: point6.y,
            hole_diameter: inflatedPcbVia?.hole_diameter ?? routePointViaDiameter.holeDiameter ?? holeDiameter2,
            outer_diameter: inflatedPcbVia?.outer_diameter ?? routePointViaDiameter.outerDiameter ?? padDiameter2,
            layers,
            from_layer: fromLayer,
            to_layer: toLayer,
            subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
            pcb_group_id: trace.getGroup()?.pcb_group_id ?? void 0,
            subcircuit_connectivity_map_key: inflatedPcbVia?.subcircuit_connectivity_map_key ?? subcircuitConnectivityMapKey2,
            net_is_assignable: inflatedPcbVia?.net_is_assignable,
            net_assigned: inflatedPcbVia?.net_assigned,
            is_tented: inflatedPcbVia?.is_tented
          });
        }
      }
      if (!trace.pcb_trace_id) {
        trace.pcb_trace_id = pcb_trace2.pcb_trace_id;
      }
      insertedRoutes.push(pcb_trace2.route);
    }
    for (const route2 of insertedRoutes) {
      trace._insertErrorIfTraceIsOutsideBoard(route2, ports);
    }
    trace._portsRoutedOnPcb = ports;
    return;
  }
  if (wantsStraightLine && !hasPcbPath) {
    if (!ports || ports.length < 2) {
      trace.renderError("pcbStraightLine requires exactly two connected ports");
      return;
    }
    const [startPort, endPort] = ports;
    const startLayers = startPort.getAvailablePcbLayers();
    const endLayers = endPort.getAvailablePcbLayers();
    const sharedLayer = startLayers.find((layer3) => endLayers.includes(layer3));
    const layer2 = sharedLayer ?? startLayers[0] ?? endLayers[0] ?? "top";
    const startPos = startPort._getGlobalPcbPositionAfterLayout();
    const endPos = endPort._getGlobalPcbPositionAfterLayout();
    const clippedStartPos = clipTraceEndAtPad({
      traceStart: endPos,
      traceEnd: startPos,
      traceWidth: width,
      port: startPort
    });
    const clippedEndPos = clipTraceEndAtPad({
      traceStart: startPos,
      traceEnd: endPos,
      traceWidth: width,
      port: endPort
    });
    const route2 = [
      {
        route_type: "wire",
        x: clippedStartPos.x,
        y: clippedStartPos.y,
        width,
        layer: layer2,
        start_pcb_port_id: startPort.pcb_port_id
      },
      {
        route_type: "wire",
        x: clippedEndPos.x,
        y: clippedEndPos.y,
        width,
        layer: layer2,
        end_pcb_port_id: endPort.pcb_port_id
      }
    ];
    const traceLength2 = getTraceLength(route2);
    const pcb_trace2 = db.pcb_trace.insert({
      route: route2,
      source_trace_id: trace.source_trace_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: trace.getGroup()?.pcb_group_id ?? void 0,
      trace_length: traceLength2
    });
    trace._portsRoutedOnPcb = ports;
    trace.pcb_trace_id = pcb_trace2.pcb_trace_id;
    trace._insertErrorIfTraceIsOutsideBoard(route2, ports);
    return;
  }
  if (!props.pcbPath) return;
  let anchorPort;
  if (props.pcbPathRelativeTo) {
    anchorPort = portsWithSelectors.find(
      (p) => p.selector === props.pcbPathRelativeTo
    )?.port;
    if (!anchorPort) {
      anchorPort = trace.getSubcircuit().selectOne(props.pcbPathRelativeTo);
    }
  }
  if (!anchorPort) {
    anchorPort = ports[0];
  }
  const otherPort = ports.find((p) => p !== anchorPort) ?? ports[1];
  const layer = anchorPort.getAvailablePcbLayers()[0] || "top";
  let currentLayer = layer;
  const anchorPos = anchorPort._getGlobalPcbPositionAfterLayout();
  const otherPos = otherPort?._getGlobalPcbPositionAfterLayout();
  const route = [];
  route.push({
    route_type: "wire",
    x: anchorPos.x,
    y: anchorPos.y,
    width,
    layer: currentLayer,
    start_pcb_port_id: anchorPort.pcb_port_id
  });
  const transform = subcircuit._isInflatedFromCircuitJson ? trace._computePcbGlobalTransformBeforeLayout() : anchorPort?._computePcbGlobalTransformBeforeLayout?.() || identity3();
  const pcbPath = props.pcbPath;
  for (const pt of pcbPath) {
    let coordinates;
    let isGlobalPosition = false;
    const isViaPoint = typeof pt !== "string" && pt.via;
    let viaFromLayer;
    let viaToLayer;
    if (typeof pt === "string") {
      const resolvedPort = trace.getSubcircuit().selectOne(pt, {
        type: "port"
      });
      if (!resolvedPort) {
        db.pcb_trace_error.insert({
          error_type: "pcb_trace_error",
          source_trace_id: trace.source_trace_id,
          message: `Could not resolve pcbPath selector "${pt}" for ${trace}`,
          pcb_trace_id: trace.pcb_trace_id,
          pcb_component_ids: [],
          pcb_port_ids: []
        });
        continue;
      }
      const pcbTargetError = getPcbSelectorErrorForTracePort(pt, resolvedPort);
      if (pcbTargetError) {
        db.pcb_trace_error.insert({
          error_type: "pcb_trace_error",
          source_trace_id: trace.source_trace_id,
          message: pcbTargetError,
          pcb_trace_id: trace.pcb_trace_id,
          pcb_component_ids: [],
          pcb_port_ids: []
        });
        return;
      }
      const portPos = resolvedPort._getGlobalPcbPositionAfterLayout();
      coordinates = { x: portPos.x, y: portPos.y };
      isGlobalPosition = true;
    } else {
      coordinates = { x: pt.x, y: pt.y };
      isGlobalPosition = false;
      if (pt.via) {
        viaFromLayer = pt.fromLayer ?? currentLayer;
        viaToLayer = pt.toLayer;
      }
    }
    const finalCoordinates = isGlobalPosition ? coordinates : applyToPoint3(transform, coordinates);
    if (isViaPoint) {
      route.push({
        route_type: "via",
        x: finalCoordinates.x,
        y: finalCoordinates.y,
        from_layer: viaFromLayer ?? currentLayer,
        to_layer: viaToLayer ?? currentLayer
      });
      currentLayer = viaToLayer ?? currentLayer;
    } else {
      route.push({
        route_type: "wire",
        x: finalCoordinates.x,
        y: finalCoordinates.y,
        width,
        layer: currentLayer
      });
    }
  }
  if (otherPort && otherPos) {
    route.push({
      route_type: "wire",
      x: otherPos.x,
      y: otherPos.y,
      width,
      layer: currentLayer,
      end_pcb_port_id: otherPort.pcb_port_id
    });
  }
  const traceLength = getTraceLength(route);
  const pcb_trace = db.pcb_trace.insert({
    route,
    source_trace_id: trace.source_trace_id,
    subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
    pcb_group_id: trace.getGroup()?.pcb_group_id ?? void 0,
    trace_length: traceLength
  });
  const subcircuitConnectivityMapKey = trace.subcircuit_connectivity_map_key ?? db.source_trace.get(trace.source_trace_id)?.subcircuit_connectivity_map_key;
  const pcbStyle = trace.getInheritedMergedProperty("pcbStyle");
  const { holeDiameter, padDiameter } = getViaDiameterDefaults(pcbStyle);
  for (const point6 of route) {
    if (point6.route_type === "via") {
      const fromLayer = point6.from_layer;
      const toLayer = point6.to_layer;
      db.pcb_via.insert({
        pcb_trace_id: pcb_trace.pcb_trace_id,
        x: point6.x,
        y: point6.y,
        hole_diameter: holeDiameter,
        outer_diameter: padDiameter,
        layers: getViaSpanLayers({
          fromLayer,
          toLayer,
          layerCount: subcircuit._getSubcircuitLayerCount()
        }),
        from_layer: fromLayer,
        to_layer: toLayer,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: trace.getGroup()?.pcb_group_id ?? void 0,
        subcircuit_connectivity_map_key: subcircuitConnectivityMapKey
      });
    }
  }
  trace._portsRoutedOnPcb = ports;
  trace.pcb_trace_id = pcb_trace.pcb_trace_id;
  trace._insertErrorIfTraceIsOutsideBoard(route, ports);
}

// lib/components/primitive-components/Trace/Trace_doInitialPcbTraceRender.ts
import { MultilayerIjump } from "@tscircuit/infgrid-ijump-astar";
import "circuit-json";
import { getFullConnectivityMapFromCircuitJson } from "circuit-json-to-connectivity-map";

// lib/utils/autorouting/should-skip-autorouting-because-of-placement-errors.ts
var shouldSkipAutoroutingBecauseOfPlacementErrors = ({
  component,
  subcircuit
}) => {
  component._pcbTraceRenderWaitingForPlacementChecks = false;
  let ancestor = component;
  let placementErrorCount = 0;
  let placementCheckError = null;
  let placementChecksPending = false;
  while (ancestor) {
    if ("_pcbPlacementDrcErrorCount" in ancestor) {
      if ("_pcbPlacementDrcChecksPending" in ancestor && ancestor._pcbPlacementDrcChecksPending === true) {
        placementChecksPending = true;
        break;
      }
      if (typeof ancestor._pcbPlacementDrcErrorCount === "number") {
        placementErrorCount = ancestor._pcbPlacementDrcErrorCount;
        break;
      }
      if ("_pcbPlacementDrcCheckError" in ancestor && typeof ancestor._pcbPlacementDrcCheckError === "string") {
        placementCheckError = ancestor._pcbPlacementDrcCheckError;
        break;
      }
    }
    ancestor = ancestor.parent;
  }
  if (placementChecksPending) {
    component._pcbTraceRenderWaitingForPlacementChecks = true;
    return true;
  }
  if (placementErrorCount === 0 && !placementCheckError) return false;
  const { db } = component.root;
  const pcbErrorId = `pcb_autorouting_skipped_placement_errors_${subcircuit.subcircuit_id}`;
  const errorAlreadyExists = db.pcb_autorouting_error.list().some((error) => error.pcb_error_id === pcbErrorId);
  if (!errorAlreadyExists) {
    db.pcb_autorouting_error.insert({
      pcb_error_id: pcbErrorId,
      error_type: "pcb_autorouting_error",
      subcircuit_id: subcircuit.subcircuit_id ?? void 0,
      message: placementCheckError ? `Autorouting was skipped because PCB placement checks could not be completed: ${placementCheckError}` : `Autorouting was skipped because ${placementErrorCount} PCB placement error${placementErrorCount === 1 ? " was" : "s were"} found. Fix the placement errors or set placementDrcChecksDisabled to true to route anyway.`
    });
  }
  return true;
};

// lib/utils/autorouting/should-skip-autorouting-because-of-trace-length-violations.ts
import { ConnectivityMap } from "circuit-json-to-connectivity-map";
var TRACE_LENGTH_COMPARISON_TOLERANCE_MM = 1e-6;
var getGreatestEndpointDistance = (pcbEndpoints) => {
  let greatestEndpointDistance = 0;
  for (let firstEndpointIndex = 0; firstEndpointIndex < pcbEndpoints.length; firstEndpointIndex++) {
    const firstEndpoint = pcbEndpoints[firstEndpointIndex];
    for (let secondEndpointIndex = firstEndpointIndex + 1; secondEndpointIndex < pcbEndpoints.length; secondEndpointIndex++) {
      const secondEndpoint = pcbEndpoints[secondEndpointIndex];
      greatestEndpointDistance = Math.max(
        greatestEndpointDistance,
        Math.hypot(
          secondEndpoint.x - firstEndpoint.x,
          secondEndpoint.y - firstEndpoint.y
        )
      );
    }
  }
  return greatestEndpointDistance;
};
var getClosestEndpointDistance = (sourcePcbEndpoints, targetPcbEndpoints) => {
  let closestEndpointDistance = Number.POSITIVE_INFINITY;
  for (const sourcePcbEndpoint of sourcePcbEndpoints) {
    for (const targetPcbEndpoint of targetPcbEndpoints) {
      closestEndpointDistance = Math.min(
        closestEndpointDistance,
        Math.hypot(
          targetPcbEndpoint.x - sourcePcbEndpoint.x,
          targetPcbEndpoint.y - sourcePcbEndpoint.y
        )
      );
    }
  }
  return closestEndpointDistance;
};
var getStraightLineTraceLengthViolations = ({
  component,
  subcircuit
}) => {
  const { db } = component.root;
  const subcircuitSourceTraces = db.source_trace.list().filter(
    (sourceTrace) => sourceTrace.subcircuit_id === subcircuit.subcircuit_id
  );
  const sourceConnectivityMap = new ConnectivityMap({});
  sourceConnectivityMap.addConnections(
    subcircuitSourceTraces.map((sourceTrace) => [
      sourceTrace.source_trace_id,
      ...sourceTrace.connected_source_port_ids,
      ...sourceTrace.connected_source_net_ids
    ])
  );
  const pcbPortsBySourcePortId = /* @__PURE__ */ new Map();
  for (const pcbPort of db.pcb_port.list()) {
    if (typeof pcbPort.source_port_id !== "string" || typeof pcbPort.x !== "number" || typeof pcbPort.y !== "number")
      continue;
    pcbPortsBySourcePortId.set(pcbPort.source_port_id, {
      pcb_port_id: pcbPort.pcb_port_id,
      source_port_id: pcbPort.source_port_id,
      x: pcbPort.x,
      y: pcbPort.y
    });
  }
  const violations = [];
  for (const sourceTrace of subcircuitSourceTraces) {
    if (typeof sourceTrace.max_length !== "number") continue;
    const pcbEndpoints = sourceTrace.connected_source_port_ids.map((sourcePortId) => pcbPortsBySourcePortId.get(sourcePortId)).filter((pcbPort) => Boolean(pcbPort));
    let straightLineDistance;
    if (pcbEndpoints.length >= 2) {
      straightLineDistance = getGreatestEndpointDistance(pcbEndpoints);
    } else {
      if (pcbEndpoints.length !== 1 || sourceTrace.connected_source_net_ids.length === 0)
        continue;
      const sourceNetworkId = sourceConnectivityMap.getNetConnectedToId(
        sourceTrace.source_trace_id
      );
      if (!sourceNetworkId) continue;
      const sourcePortIdsOnNetwork = sourceConnectivityMap.getIdsConnectedToNet(sourceNetworkId).filter(
        (connectedId) => !sourceTrace.connected_source_port_ids.includes(connectedId) && pcbPortsBySourcePortId.has(connectedId)
      );
      const targetPcbEndpoints = sourcePortIdsOnNetwork.map((sourcePortId) => pcbPortsBySourcePortId.get(sourcePortId)).filter((pcbPort) => Boolean(pcbPort));
      if (targetPcbEndpoints.length === 0) continue;
      const networkHasPlane = db.toArray().some(
        (element) => (element.type === "source_pcb_ground_plane" || element.type === "pcb_ground_plane" || element.type === "pcb_copper_pour") && typeof element.source_net_id === "string" && sourceConnectivityMap.areIdsConnected(
          sourceTrace.source_trace_id,
          element.source_net_id
        )
      );
      if (networkHasPlane) continue;
      straightLineDistance = getClosestEndpointDistance(
        pcbEndpoints,
        targetPcbEndpoints
      );
    }
    if (straightLineDistance <= sourceTrace.max_length + TRACE_LENGTH_COMPARISON_TOLERANCE_MM)
      continue;
    violations.push({
      sourceTraceId: sourceTrace.source_trace_id,
      traceDisplayName: sourceTrace.display_name,
      straightLineDistance,
      maximumTraceLength: sourceTrace.max_length
    });
  }
  return violations;
};
var shouldSkipAutoroutingBecauseOfTraceLengthViolations = ({
  component,
  subcircuit
}) => {
  const violations = getStraightLineTraceLengthViolations({
    component,
    subcircuit
  });
  if (violations.length === 0) return false;
  const { db } = component.root;
  const pcbErrorId = `pcb_autorouting_skipped_trace_length_violations_${subcircuit.subcircuit_id}`;
  const errorAlreadyExists = db.pcb_autorouting_error.list().some((error) => error.pcb_error_id === pcbErrorId);
  if (!errorAlreadyExists) {
    const firstViolation = violations[0];
    const traceName = firstViolation.traceDisplayName ?? firstViolation.sourceTraceId;
    const additionalViolationCount = violations.length - 1;
    const additionalViolationMessage = additionalViolationCount > 0 ? ` (${additionalViolationCount} additional violation${additionalViolationCount === 1 ? "" : "s"})` : "";
    db.pcb_autorouting_error.insert({
      pcb_error_id: pcbErrorId,
      error_type: "pcb_autorouting_error",
      subcircuit_id: subcircuit.subcircuit_id ?? void 0,
      message: `Autorouting was skipped because the ${firstViolation.maximumTraceLength}mm maximum length for ${traceName} cannot be satisfied: its endpoints are ${firstViolation.straightLineDistance.toFixed(2)}mm apart${additionalViolationMessage}.`
    });
  }
  return true;
};

// lib/components/primitive-components/Trace/Trace_doInitialPcbTraceRender.ts
var portToObjective = (port) => {
  const portPosition = port._getGlobalPcbPositionAfterLayout();
  return {
    ...portPosition,
    layers: port.getAvailablePcbLayers()
  };
};
var SHOULD_USE_SINGLE_LAYER_ROUTING = false;
var getDistanceToRouteObjective = (point6, objective) => {
  const position = getRoutePointPosition(point6);
  return Math.hypot(position.x - objective.x, position.y - objective.y);
};
var reverseRouteDirection = (route) => route.slice().reverse().map((point6) => {
  if (point6.route_type === "via") {
    return { ...point6 };
  }
  if (point6.route_type === "through_pad") {
    return {
      ...point6,
      start: point6.end,
      end: point6.start,
      start_layer: point6.end_layer,
      end_layer: point6.start_layer
    };
  }
  return { ...point6 };
});
var ensureRouteStartsAtObjective = (route, objective) => {
  if (route.length < 2) return route;
  const firstPoint = route[0];
  const lastPoint = route[route.length - 1];
  return getDistanceToRouteObjective(lastPoint, objective) < getDistanceToRouteObjective(firstPoint, objective) ? reverseRouteDirection(route) : route;
};
function Trace_doInitialPcbTraceRender(trace) {
  if (trace.root?.pcbDisabled) return;
  const { db } = trace.root;
  const { _parsedProps: props, parent } = trace;
  const subcircuit = trace.getSubcircuit();
  if (!parent) throw new Error("Trace has no parent");
  if (trace.root?.pcbRoutingDisabled || subcircuit.getInheritedProperty("routingDisabled")) {
    return;
  }
  if (subcircuit._isInflatedFromCircuitJson) {
    return;
  }
  const cachedRoute = subcircuit._parsedProps.pcbRouteCache?.pcbTraces;
  if (cachedRoute) {
    if (subcircuit._isLegacyAutorouterDisabled()) return;
    const pcb_trace2 = db.pcb_trace.insert({
      route: cachedRoute.flatMap((trace2) => trace2.route),
      source_trace_id: trace.source_trace_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: trace.getGroup()?.pcb_group_id ?? void 0
    });
    trace.pcb_trace_id = pcb_trace2.pcb_trace_id;
    return;
  }
  if (props.pcbPath && props.pcbPath.length > 0) {
    return;
  }
  if (props.pcbStraightLine) {
    return;
  }
  if (!subcircuit._shouldUseTraceByTraceRouting()) {
    return;
  }
  let allPortsFound;
  let ports;
  let portsWithSelectors;
  try {
    const connectedPorts = trace._findConnectedPorts();
    allPortsFound = connectedPorts.allPortsFound;
    ports = connectedPorts.ports ?? [];
    portsWithSelectors = connectedPorts.portsWithSelectors ?? [];
  } catch (error) {
    if (error instanceof TraceConnectionError) {
      db.source_trace_not_connected_error.insert({
        ...error.errorData,
        error_type: "source_trace_not_connected_error"
      });
      trace._couldNotFindPort = true;
      return;
    }
    throw error;
  }
  const portsConnectedOnPcbViaNet = [];
  if (!allPortsFound) return;
  const pcbSelectorError = portsWithSelectors.map(
    ({ selector, port }) => getPcbSelectorErrorForTracePort(selector, port)
  ).find(Boolean);
  if (pcbSelectorError) {
    db.pcb_trace_error.insert({
      error_type: "pcb_trace_error",
      source_trace_id: trace.source_trace_id,
      message: pcbSelectorError,
      pcb_trace_id: trace.pcb_trace_id,
      pcb_component_ids: [],
      pcb_port_ids: ports.map((p) => p.pcb_port_id).filter(Boolean)
    });
    return;
  }
  const portsWithoutMatchedPcbPrimitive = [];
  for (const port of ports) {
    if (!port._hasMatchedPcbPrimitive()) {
      portsWithoutMatchedPcbPrimitive.push(port);
    }
  }
  if (portsWithoutMatchedPcbPrimitive.length > 0) {
    db.pcb_trace_error.insert({
      error_type: "pcb_trace_error",
      source_trace_id: trace.source_trace_id,
      message: `Some ports did not have a matching PCB primitive (e.g. a pad or plated hole), this can happen if a footprint is missing. As a result, ${trace} wasn't routed. Missing ports: ${portsWithoutMatchedPcbPrimitive.map((p) => p.getString()).join(", ")}`,
      pcb_trace_id: trace.pcb_trace_id,
      pcb_component_ids: [],
      pcb_port_ids: portsWithoutMatchedPcbPrimitive.map((p) => p.pcb_port_id).filter(Boolean)
    });
    return;
  }
  const shouldSkipBecauseOfPlacementErrors = shouldSkipAutoroutingBecauseOfPlacementErrors({
    component: trace,
    subcircuit
  });
  const shouldSkipBecauseOfTraceLengthViolations = shouldSkipAutoroutingBecauseOfTraceLengthViolations({
    component: trace,
    subcircuit
  });
  if (shouldSkipBecauseOfPlacementErrors || shouldSkipBecauseOfTraceLengthViolations)
    return;
  const nets = trace._findConnectedNets().netsWithSelectors;
  if (ports.length === 0 && nets.length === 2) {
    const [netA, netB] = nets.map(({ net }) => net);
    const netAPorts = netA.getAllConnectedPorts();
    const netBPorts = netB.getAllConnectedPorts();
    if (netAPorts.length === 0 || netBPorts.length === 0) {
      console.log(
        "Nothing to connect this net to, one of the nets is empty. TODO should emit a warning!"
      );
      return;
    }
    let closestPortPair = [netAPorts[0], netBPorts[0]];
    let closestDistance = Infinity;
    for (const netAPort of netAPorts) {
      const closestNetBPort = getClosest(netAPort, netBPorts);
      const netAPortPosition = netAPort._getGlobalPcbPositionBeforeLayout();
      const netBPortPosition = closestNetBPort._getGlobalPcbPositionBeforeLayout();
      const distance20 = Math.sqrt(
        (netAPortPosition.x - netBPortPosition.x) ** 2 + (netAPortPosition.y - netBPortPosition.y) ** 2
      );
      if (distance20 < closestDistance) {
        closestDistance = distance20;
        closestPortPair = [netAPort, closestNetBPort];
      }
    }
    ports.push(...closestPortPair);
  } else if (ports.length === 1 && nets.length === 1) {
    const port = ports[0];
    const portsInNet = nets[0].net.getAllConnectedPorts();
    const otherPortsInNet = portsInNet.filter((p) => p !== port);
    if (otherPortsInNet.length === 0) {
      console.log(
        "Nothing to connect this port to, the net is empty. TODO should emit a warning!"
      );
      return;
    }
    const closestPortInNet = getClosest(port, otherPortsInNet);
    portsConnectedOnPcbViaNet.push(closestPortInNet);
    ports.push(closestPortInNet);
  } else if (ports.length > 1 && nets.length >= 1) {
    trace.renderError(
      `Trace has more than one port and one or more nets, we don't currently support this type of complex trace routing`
    );
    return;
  }
  const hints = ports.flatMap(
    (port) => port.matchedComponents.filter((c) => c.componentName === "TraceHint")
  );
  const pcbRouteHints = (trace._parsedProps.pcbRouteHints ?? []).concat(
    hints.flatMap((h) => h.getPcbRouteHints())
  );
  if (ports.length > 2) {
    trace.renderError(
      `Trace has more than two ports (${ports.map((p) => p.getString()).join(
        ", "
      )}), routing between more than two ports for a single trace is not implemented`
    );
    return;
  }
  const alreadyRoutedTraces = trace.getSubcircuit().selectAll("trace").filter(
    (trace2) => trace2.renderPhaseStates.PcbTraceRender.initialized
  );
  const isAlreadyRouted = alreadyRoutedTraces.some(
    (trace2) => trace2._portsRoutedOnPcb.length === ports.length && trace2._portsRoutedOnPcb.every(
      (portRoutedByOtherTrace) => ports.includes(portRoutedByOtherTrace)
    )
  );
  if (isAlreadyRouted) {
    return;
  }
  let orderedRouteObjectives = [];
  if (pcbRouteHints.length === 0) {
    orderedRouteObjectives = [
      portToObjective(ports[0]),
      portToObjective(ports[1])
    ];
  } else {
    orderedRouteObjectives = [
      portToObjective(ports[0]),
      ...pcbRouteHints,
      portToObjective(ports[1])
    ];
  }
  const candidateLayerCombinations = findPossibleTraceLayerCombinations(
    orderedRouteObjectives,
    {
      layerSelectionPreference: getBoardAvailableLayers(
        subcircuit._getSubcircuitLayerCount()
      )
    }
  );
  if (SHOULD_USE_SINGLE_LAYER_ROUTING && candidateLayerCombinations.length === 0) {
    trace.renderError(
      `Could not find a common layer (using hints) for trace ${trace.getString()}`
    );
    return;
  }
  const connMap = getFullConnectivityMapFromCircuitJson(
    trace.root.db.toArray()
  );
  const [obstacles, errGettingObstacles] = tryNow(
    () => getObstaclesFromCircuitJson(trace.root.db.toArray())
    // Remove as any when autorouting-dataset gets updated
  );
  if (errGettingObstacles) {
    trace.renderError({
      type: "pcb_trace_error",
      error_type: "pcb_trace_error",
      pcb_trace_error_id: trace.pcb_trace_id,
      message: `Error getting obstacles for autorouting: ${errGettingObstacles.message}`,
      source_trace_id: trace.source_trace_id,
      center: { x: 0, y: 0 },
      pcb_port_ids: ports.map((p) => p.pcb_port_id),
      pcb_trace_id: trace.pcb_trace_id,
      pcb_component_ids: []
    });
    return;
  }
  for (const obstacle of obstacles) {
    const connectedTo = obstacle.connectedTo;
    if (connectedTo.length > 0) {
      const netId = connMap.getNetConnectedToId(obstacle.connectedTo[0]);
      if (netId) {
        obstacle.connectedTo.push(netId);
      }
    }
  }
  let orderedRoutePoints = [];
  if (candidateLayerCombinations.length === 0) {
    orderedRoutePoints = orderedRouteObjectives;
  } else {
    const candidateLayerSelections = candidateLayerCombinations[0].layer_path;
    orderedRoutePoints = orderedRouteObjectives.map((t, idx) => {
      if (t.via) {
        return {
          ...t,
          via_to_layer: candidateLayerSelections[idx]
        };
      }
      return { ...t, layers: [candidateLayerSelections[idx]] };
    });
  }
  ;
  orderedRoutePoints[0].pcb_port_id = ports[0].pcb_port_id;
  orderedRoutePoints[orderedRoutePoints.length - 1].pcb_port_id = ports[1].pcb_port_id;
  const routes = [];
  for (const [a, b] of pairs(orderedRoutePoints)) {
    const dominantLayer = "via_to_layer" in a ? a.via_to_layer : null;
    const BOUNDS_MARGIN = 2;
    const aLayer = "layers" in a && a.layers.length === 1 ? a.layers[0] : dominantLayer ?? "top";
    const bLayer = "layers" in b && b.layers.length === 1 ? b.layers[0] : dominantLayer ?? "top";
    const pcbPortA = "pcb_port_id" in a ? a.pcb_port_id : null;
    const pcbPortB = "pcb_port_id" in b ? b.pcb_port_id : null;
    const minTraceWidth = trace._getExplicitTraceThickness() ?? trace.getSubcircuit()._parsedProps.minTraceWidth ?? jlcMinTolerances.min_trace_width;
    const ijump = new MultilayerIjump({
      OBSTACLE_MARGIN: minTraceWidth * 2,
      isRemovePathLoopsEnabled: true,
      optimizeWithGoalBoxes: Boolean(pcbPortA && pcbPortB),
      connMap,
      input: {
        obstacles,
        minTraceWidth,
        connections: [
          {
            name: trace.source_trace_id,
            pointsToConnect: [
              { ...a, layer: aLayer, pcb_port_id: pcbPortA },
              { ...b, layer: bLayer, pcb_port_id: pcbPortB }
            ]
          }
        ],
        layerCount: trace.getSubcircuit()._getSubcircuitLayerCount(),
        bounds: {
          minX: Math.min(a.x, b.x) - BOUNDS_MARGIN,
          maxX: Math.max(a.x, b.x) + BOUNDS_MARGIN,
          minY: Math.min(a.y, b.y) - BOUNDS_MARGIN,
          maxY: Math.max(a.y, b.y) + BOUNDS_MARGIN
        }
      }
    });
    let traces = null;
    try {
      traces = ijump.solveAndMapToTraces();
    } catch (e) {
      trace.renderError({
        type: "pcb_trace_error",
        pcb_trace_error_id: trace.source_trace_id,
        error_type: "pcb_trace_error",
        message: `error solving route: ${e.message}`,
        source_trace_id: trace.pcb_trace_id,
        center: { x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 },
        pcb_port_ids: ports.map((p) => p.pcb_port_id),
        pcb_trace_id: trace.pcb_trace_id,
        pcb_component_ids: ports.map((p) => p.pcb_component_id)
      });
    }
    if (!traces) return;
    if (traces.length === 0) {
      trace.renderError({
        type: "pcb_trace_error",
        error_type: "pcb_trace_error",
        pcb_trace_error_id: trace.pcb_trace_id,
        message: `Could not find a route for ${trace}`,
        source_trace_id: trace.source_trace_id,
        center: { x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 },
        pcb_port_ids: ports.map((p) => p.pcb_port_id),
        pcb_trace_id: trace.pcb_trace_id,
        pcb_component_ids: ports.map((p) => p.pcb_component_id)
      });
      return;
    }
    const [autoroutedTrace] = traces;
    autoroutedTrace.route = ensureRouteStartsAtObjective(
      autoroutedTrace.route,
      a
    );
    if (dominantLayer) {
      autoroutedTrace.route = autoroutedTrace.route.map((p) => {
        if (p.route_type === "wire" && !p.layer) {
          p.layer = dominantLayer;
        }
        return p;
      });
    }
    if (pcbPortA && autoroutedTrace.route[0].route_type === "wire") {
      autoroutedTrace.route[0].start_pcb_port_id = pcbPortA;
    }
    const lastRoutePoint = autoroutedTrace.route[autoroutedTrace.route.length - 1];
    if (pcbPortB && lastRoutePoint.route_type === "wire") {
      lastRoutePoint.end_pcb_port_id = pcbPortB;
    }
    routes.push(autoroutedTrace.route);
  }
  const mergedRoute = mergeRoutes(routes);
  const traceLength = getTraceLength(mergedRoute);
  const pcbStyle = trace.getInheritedMergedProperty("pcbStyle");
  const { holeDiameter, padDiameter } = getViaDiameterDefaults(pcbStyle);
  const pcb_trace = db.pcb_trace.insert({
    route: mergedRoute,
    source_trace_id: trace.source_trace_id,
    subcircuit_id: trace.getSubcircuit()?.subcircuit_id,
    trace_length: traceLength
  });
  const subcircuitConnectivityMapKey = trace.subcircuit_connectivity_map_key ?? db.source_trace.get(trace.source_trace_id)?.subcircuit_connectivity_map_key;
  trace._portsRoutedOnPcb = ports;
  trace.pcb_trace_id = pcb_trace.pcb_trace_id;
  for (const point6 of mergedRoute) {
    if (point6.route_type === "via") {
      const fromLayer = point6.from_layer;
      const toLayer = point6.to_layer;
      db.pcb_via.insert({
        pcb_trace_id: pcb_trace.pcb_trace_id,
        x: point6.x,
        y: point6.y,
        hole_diameter: holeDiameter,
        outer_diameter: padDiameter,
        layers: getViaSpanLayers({
          fromLayer,
          toLayer,
          layerCount: subcircuit._getSubcircuitLayerCount()
        }),
        from_layer: fromLayer,
        to_layer: toLayer,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: trace.getGroup()?.pcb_group_id ?? void 0,
        subcircuit_connectivity_map_key: subcircuitConnectivityMapKey
      });
    }
  }
  trace._insertErrorIfTraceIsOutsideBoard(mergedRoute, ports);
}

// lib/components/primitive-components/Trace/Trace_doInitialSchematicTraceRender.ts
import { MultilayerIjump as MultilayerIjump2 } from "@tscircuit/infgrid-ijump-astar";
import { doesLineIntersectLine as doesLineIntersectLine3 } from "@tscircuit/math-utils";
import { calculateElbow } from "calculate-elbow";
import "circuit-json";

// lib/utils/schematic/convertFacingDirectionToElbowDirection.ts
var convertFacingDirectionToElbowDirection = (facingDirection) => {
  switch (facingDirection) {
    case "up":
      return "y+";
    case "down":
      return "y-";
    case "left":
      return "x-";
    case "right":
      return "x+";
    default:
  }
};

// lib/components/primitive-components/Trace/trace-utils/create-schematic-trace-crossing-segments.ts
import { distance as distance2, doesLineIntersectLine } from "@tscircuit/math-utils";

// lib/components/primitive-components/Trace/trace-utils/get-other-schematic-traces.ts
var getOtherSchematicTraces = ({
  db,
  source_trace_id,
  schematic_sheet_id,
  sameNetOnly,
  differentNetOnly
}) => {
  if (!sameNetOnly && !differentNetOnly) {
    differentNetOnly = true;
  }
  const mySourceTrace = db.source_trace.get(source_trace_id);
  const traces = [];
  for (const otherSchematicTrace of db.schematic_trace.list()) {
    if (otherSchematicTrace.source_trace_id === source_trace_id) continue;
    if (schematic_sheet_id !== void 0 && otherSchematicTrace.schematic_sheet_id !== schematic_sheet_id) {
      continue;
    }
    const otherSourceTrace = db.source_trace.get(
      otherSchematicTrace.source_trace_id
    );
    const isSameNet2 = otherSourceTrace?.subcircuit_connectivity_map_key === mySourceTrace.subcircuit_connectivity_map_key;
    if (differentNetOnly && isSameNet2) {
      continue;
    }
    if (sameNetOnly && !isSameNet2) {
      continue;
    }
    traces.push(otherSchematicTrace);
  }
  return traces;
};

// lib/components/primitive-components/Trace/trace-utils/create-schematic-trace-crossing-segments.ts
import { getUnitVectorFromPointAToB } from "@tscircuit/math-utils";
var createSchematicTraceCrossingSegments = ({
  edges: inputEdges,
  otherEdges
}) => {
  const edges = [...inputEdges];
  for (let i = 0; i < edges.length; i++) {
    if (i > 2e3) {
      throw new Error(
        "Over 2000 iterations spent inside createSchematicTraceCrossingSegments, you have triggered an infinite loop, please report this!"
      );
    }
    const edge = edges[i];
    const edgeOrientation = Math.abs(edge.from.x - edge.to.x) < 0.01 ? "vertical" : edge.from.y === edge.to.y ? "horizontal" : "not-orthogonal";
    if (edgeOrientation === "not-orthogonal") {
      continue;
    }
    const otherEdgesIntersections = [];
    for (const otherEdge of otherEdges) {
      const otherOrientation = otherEdge.from.x === otherEdge.to.x ? "vertical" : otherEdge.from.y === otherEdge.to.y ? "horizontal" : "not-orthogonal";
      if (otherOrientation === "not-orthogonal") continue;
      if (edgeOrientation === otherOrientation)
        continue;
      const hasIntersection = doesLineIntersectLine(
        [edge.from, edge.to],
        [otherEdge.from, otherEdge.to],
        { lineThickness: 0.01 }
      );
      if (hasIntersection) {
        const intersectX = edgeOrientation === "vertical" ? edge.from.x : otherEdge.from.x;
        const intersectY = edgeOrientation === "vertical" ? otherEdge.from.y : edge.from.y;
        const crossingPoint2 = { x: intersectX, y: intersectY };
        otherEdgesIntersections.push({
          otherEdge,
          crossingPoint: crossingPoint2,
          distanceFromEdgeFrom: distance2(edge.from, crossingPoint2)
        });
      }
    }
    if (otherEdgesIntersections.length === 0) continue;
    let closestIntersection = otherEdgesIntersections[0];
    for (const intersection of otherEdgesIntersections) {
      if (intersection.distanceFromEdgeFrom < closestIntersection.distanceFromEdgeFrom) {
        closestIntersection = intersection;
      }
    }
    const crossingPoint = closestIntersection.crossingPoint;
    const crossingSegmentLength = 0.075;
    if (crossingPoint.x === edge.from.x && crossingPoint.y === edge.from.y) {
      continue;
    }
    const crossingUnitVec = getUnitVectorFromPointAToB(edge.from, crossingPoint);
    const beforeCrossing = {
      x: crossingPoint.x - crossingUnitVec.x * crossingSegmentLength / 2,
      y: crossingPoint.y - crossingUnitVec.y * crossingSegmentLength / 2
    };
    const afterCrossing = {
      x: crossingPoint.x + crossingUnitVec.x * crossingSegmentLength / 2,
      y: crossingPoint.y + crossingUnitVec.y * crossingSegmentLength / 2
    };
    const overshot = distance2(afterCrossing, edge.to) < crossingSegmentLength;
    const newEdges = [
      { from: edge.from, to: beforeCrossing },
      { from: beforeCrossing, to: afterCrossing, is_crossing: true },
      { from: afterCrossing, to: edge.to }
    ];
    edges.splice(i, 1, ...newEdges);
    i += newEdges.length - 2;
    if (overshot) {
      i++;
    }
  }
  return edges;
};

// lib/components/primitive-components/Trace/trace-utils/create-schematic-trace-junctions.ts
var TOLERANCE = 1e-3;
var isPointWithinEdge = (point6, edge) => {
  const minX = Math.min(edge.from.x, edge.to.x);
  const maxX = Math.max(edge.from.x, edge.to.x);
  const minY = Math.min(edge.from.y, edge.to.y);
  const maxY = Math.max(edge.from.y, edge.to.y);
  return point6.x >= minX && point6.x <= maxX && point6.y >= minY && point6.y <= maxY;
};
var getEdgeOrientation = (edge) => {
  const isVertical = Math.abs(edge.from.x - edge.to.x) < TOLERANCE;
  const isHorizontal3 = Math.abs(edge.from.y - edge.to.y) < TOLERANCE;
  if (isVertical) return "vertical";
  if (isHorizontal3) return "horizontal";
  return "diagonal";
};
var getIntersectionPoint = (edge1, edge2) => {
  const orientation1 = getEdgeOrientation(edge1);
  const orientation2 = getEdgeOrientation(edge2);
  if (orientation1 === orientation2) {
    return null;
  }
  if (orientation1 === "vertical" && orientation2 === "horizontal" || orientation1 === "horizontal" && orientation2 === "vertical") {
    const verticalEdge = orientation1 === "vertical" ? edge1 : edge2;
    const horizontalEdge = orientation1 === "horizontal" ? edge1 : edge2;
    const x2 = verticalEdge.from.x;
    const y2 = horizontalEdge.from.y;
    const intersection2 = { x: x2, y: y2 };
    return isPointWithinEdge(intersection2, edge1) && isPointWithinEdge(intersection2, edge2) ? intersection2 : null;
  }
  if (orientation1 === "vertical" || orientation2 === "vertical") {
    const verticalEdge = orientation1 === "vertical" ? edge1 : edge2;
    const diagonalEdge = orientation1 === "vertical" ? edge2 : edge1;
    const x2 = verticalEdge.from.x;
    const m = (diagonalEdge.to.y - diagonalEdge.from.y) / (diagonalEdge.to.x - diagonalEdge.from.x);
    const b = diagonalEdge.from.y - m * diagonalEdge.from.x;
    const y2 = m * x2 + b;
    const intersection2 = { x: x2, y: y2 };
    return isPointWithinEdge(intersection2, edge1) && isPointWithinEdge(intersection2, edge2) ? intersection2 : null;
  }
  const m1 = (edge1.to.y - edge1.from.y) / (edge1.to.x - edge1.from.x);
  const b1 = edge1.from.y - m1 * edge1.from.x;
  const m2 = (edge2.to.y - edge2.from.y) / (edge2.to.x - edge2.from.x);
  const b2 = edge2.from.y - m2 * edge2.from.x;
  if (Math.abs(m1 - m2) < TOLERANCE) {
    return null;
  }
  const x = (b2 - b1) / (m1 - m2);
  const y = m1 * x + b1;
  const intersection = { x, y };
  return isPointWithinEdge(intersection, edge1) && isPointWithinEdge(intersection, edge2) ? intersection : null;
};
var createSchematicTraceJunctions = ({
  edges: myEdges,
  db,
  source_trace_id,
  schematic_sheet_id
}) => {
  const otherEdges = getOtherSchematicTraces({
    db,
    source_trace_id,
    schematic_sheet_id,
    sameNetOnly: true
  }).flatMap((t) => t.edges);
  const junctions = /* @__PURE__ */ new Map();
  for (const myEdge of myEdges) {
    for (const otherEdge of otherEdges) {
      const intersection = getIntersectionPoint(myEdge, otherEdge);
      if (intersection) {
        const key = `${intersection.x.toFixed(6)},${intersection.y.toFixed(6)}`;
        if (!junctions.has(key)) {
          junctions.set(key, intersection);
        }
      }
    }
  }
  return Array.from(junctions.values());
};

// lib/components/primitive-components/Trace/trace-utils/get-obstacles-for-trace.ts
import { getUnitVectorFromDirection } from "@tscircuit/math-utils";

// lib/utils/autorouting/getBoundsForSchematic.ts
function normalizeAngle(angle) {
  const normalized = angle % 360;
  return normalized < 0 ? normalized + 360 : normalized;
}
function isAngleBetween(angle, start, end, direction) {
  if (direction === "counterclockwise") {
    if (end >= start) {
      return angle >= start && angle <= end;
    }
    return angle >= start || angle <= end;
  }
  if (end <= start) {
    return angle <= start && angle >= end;
  }
  return angle <= start || angle >= end;
}
function getArcBounds(elm) {
  const center = elm.center;
  const radius = elm.radius;
  const startAngle = elm.start_angle_degrees;
  const endAngle = elm.end_angle_degrees;
  const direction = elm.direction ?? "counterclockwise";
  if (!center || typeof center.x !== "number" || typeof center.y !== "number" || typeof radius !== "number" || typeof startAngle !== "number" || typeof endAngle !== "number") {
    return null;
  }
  const start = normalizeAngle(startAngle);
  const end = normalizeAngle(endAngle);
  const consideredAngles = /* @__PURE__ */ new Set([start, end]);
  const cardinalAngles = [0, 90, 180, 270];
  for (const cardinal of cardinalAngles) {
    if (isAngleBetween(cardinal, start, end, direction)) {
      consideredAngles.add(cardinal);
    }
  }
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  for (const angle of consideredAngles) {
    const radians = angle * Math.PI / 180;
    const x = center.x + radius * Math.cos(radians);
    const y = center.y + radius * Math.sin(radians);
    minX = Math.min(minX, x);
    maxX = Math.max(maxX, x);
    minY = Math.min(minY, y);
    maxY = Math.max(maxY, y);
  }
  if (!Number.isFinite(minX) || !Number.isFinite(minY)) {
    return null;
  }
  return { minX, maxX, minY, maxY };
}
function getBoundsForSchematic(db) {
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  for (const elm of db) {
    let cx;
    let cy;
    let w;
    let h;
    if (elm.type === "schematic_component") {
      cx = elm.center?.x;
      cy = elm.center?.y;
      w = elm.size?.width;
      h = elm.size?.height;
    } else if (elm.type === "schematic_box") {
      cx = elm.x;
      cy = elm.y;
      w = elm.width;
      h = elm.height;
    } else if (elm.type === "schematic_port") {
      cx = elm.center?.x;
      cy = elm.center?.y;
      w = 0.2;
      h = 0.2;
    } else if (elm.type === "schematic_text") {
      cx = elm.position?.x;
      cy = elm.position?.y;
      w = (elm.text?.length ?? 0) * 0.1;
      h = 0.2;
    } else if (elm.type === "schematic_line") {
      const x1 = elm.x1 ?? 0;
      const y1 = elm.y1 ?? 0;
      const x2 = elm.x2 ?? 0;
      const y2 = elm.y2 ?? 0;
      cx = (x1 + x2) / 2;
      cy = (y1 + y2) / 2;
      w = Math.abs(x2 - x1);
      h = Math.abs(y2 - y1);
    } else if (elm.type === "schematic_rect") {
      cx = elm.center?.x;
      cy = elm.center?.y;
      w = elm.width;
      h = elm.height;
    } else if (elm.type === "schematic_circle") {
      cx = elm.center?.x;
      cy = elm.center?.y;
      const radius = elm.radius;
      if (typeof radius === "number") {
        w = radius * 2;
        h = radius * 2;
      }
    } else if (elm.type === "schematic_arc") {
      const bounds = getArcBounds(elm);
      if (bounds) {
        minX = Math.min(minX, bounds.minX);
        maxX = Math.max(maxX, bounds.maxX);
        minY = Math.min(minY, bounds.minY);
        maxY = Math.max(maxY, bounds.maxY);
      }
      continue;
    } else if (elm.type === "schematic_path") {
      const points = elm.points;
      if (Array.isArray(points)) {
        for (const point6 of points) {
          if (typeof point6.x === "number" && typeof point6.y === "number") {
            minX = Math.min(minX, point6.x);
            maxX = Math.max(maxX, point6.x);
            minY = Math.min(minY, point6.y);
            maxY = Math.max(maxY, point6.y);
          }
        }
      }
      continue;
    }
    if (typeof cx === "number" && typeof cy === "number" && typeof w === "number" && typeof h === "number") {
      minX = Math.min(minX, cx - w / 2);
      maxX = Math.max(maxX, cx + w / 2);
      minY = Math.min(minY, cy - h / 2);
      maxY = Math.max(maxY, cy + h / 2);
    }
  }
  return { minX, maxX, minY, maxY };
}

// lib/utils/autorouting/getObstaclesFromBounds.ts
function getObstaclesFromBounds(bounds, opts = {}) {
  const { minX, maxX, minY, maxY } = bounds;
  const PADDING = opts.padding ?? 1;
  if (!isFinite(minX) || !isFinite(maxX) || !isFinite(minY) || !isFinite(maxY))
    return [];
  const left = minX - PADDING;
  const right = maxX + PADDING;
  const top = maxY + PADDING;
  const bottom = minY - PADDING;
  const thickness = 0.01;
  return [
    // Top border (horizontal)
    {
      type: "rect",
      layers: ["top"],
      center: { x: (left + right) / 2, y: top },
      width: right - left,
      height: thickness,
      connectedTo: []
    },
    // Bottom border (horizontal)
    {
      type: "rect",
      layers: ["top"],
      center: { x: (left + right) / 2, y: bottom },
      width: right - left,
      height: thickness,
      connectedTo: []
    },
    // Left border (vertical)
    {
      type: "rect",
      layers: ["top"],
      center: { x: left, y: (top + bottom) / 2 },
      width: thickness,
      height: top - bottom,
      connectedTo: []
    },
    // Right border (vertical)
    {
      type: "rect",
      layers: ["top"],
      center: { x: right, y: (top + bottom) / 2 },
      width: thickness,
      height: top - bottom,
      connectedTo: []
    }
  ];
}

// lib/components/primitive-components/Trace/trace-utils/get-obstacles-for-trace.ts
var getSchematicObstaclesForTrace = (trace) => {
  const db = trace.root.db;
  const connectedPorts = trace._findConnectedPorts().ports ?? [];
  const connectedPortIds = new Set(
    connectedPorts.map((p) => p.schematic_port_id)
  );
  const obstacles = [];
  for (const elm of db.toArray()) {
    if (elm.type === "schematic_component") {
      const isSymbol = Boolean(elm.symbol_name);
      const dominateAxis = elm.size.width > elm.size.height ? "horz" : "vert";
      obstacles.push({
        type: "rect",
        layers: ["top"],
        center: elm.center,
        width: elm.size.width + (isSymbol && dominateAxis === "horz" ? -0.5 : 0),
        height: elm.size.height + (isSymbol && dominateAxis === "vert" ? -0.5 : 0),
        connectedTo: []
      });
    }
    if (elm.type === "schematic_port") {
      if (connectedPortIds.has(elm.schematic_port_id)) {
        continue;
      }
      const dirVec = elm.facing_direction ? getUnitVectorFromDirection(elm.facing_direction) : {
        x: 0,
        y: 0
      };
      obstacles.push({
        type: "rect",
        layers: ["top"],
        center: {
          x: elm.center.x - dirVec.x * 0.1,
          y: elm.center.y - dirVec.y * 0.1
        },
        width: 0.1 + Math.abs(dirVec.x) * 0.3,
        height: 0.1 + Math.abs(dirVec.y) * 0.3,
        connectedTo: []
      });
    }
    if (elm.type === "schematic_text") {
      obstacles.push({
        type: "rect",
        layers: ["top"],
        center: elm.position,
        width: (elm.text?.length ?? 0) * 0.1,
        height: 0.2,
        connectedTo: []
      });
    }
    if (elm.type === "schematic_box") {
      obstacles.push({
        type: "rect",
        layers: ["top"],
        center: { x: elm.x, y: elm.y },
        width: elm.width,
        height: elm.height,
        connectedTo: []
      });
    }
  }
  const bounds = getBoundsForSchematic(db.toArray());
  obstacles.push(...getObstaclesFromBounds(bounds, { padding: 1 }));
  return obstacles;
};

// lib/components/primitive-components/Trace/trace-utils/push-edges-of-schematic-trace-to-prevent-overlap.ts
import { doesLineIntersectLine as doesLineIntersectLine2 } from "@tscircuit/math-utils";
var pushEdgesOfSchematicTraceToPreventOverlap = ({
  edges,
  db,
  source_trace_id,
  schematic_sheet_id
}) => {
  const otherEdges = getOtherSchematicTraces({
    db,
    source_trace_id,
    schematic_sheet_id,
    differentNetOnly: true
  }).flatMap((t) => t.edges);
  const edgeOrientation = (edge) => {
    const { from, to } = edge;
    return from.x === to.x ? "vertical" : "horizontal";
  };
  for (const mySegment of edges) {
    const mySegmentOrientation = edgeOrientation(mySegment);
    const findOverlappingParallelSegment = () => otherEdges.find(
      (otherEdge) => edgeOrientation(otherEdge) === mySegmentOrientation && doesLineIntersectLine2(
        [mySegment.from, mySegment.to],
        [otherEdge.from, otherEdge.to],
        {
          lineThickness: 0.05
        }
      )
    );
    let overlappingParallelSegmentFromOtherTrace = findOverlappingParallelSegment();
    while (overlappingParallelSegmentFromOtherTrace) {
      if (mySegmentOrientation === "horizontal") {
        mySegment.from.y += 0.1;
        mySegment.to.y += 0.1;
      } else {
        mySegment.from.x += 0.1;
        mySegment.to.x += 0.1;
      }
      overlappingParallelSegmentFromOtherTrace = findOverlappingParallelSegment();
    }
  }
};

// lib/components/primitive-components/Trace/Trace_doInitialSchematicTraceRender.ts
var Trace_doInitialSchematicTraceRender = (trace) => {
  if (trace.root?._featureMspSchematicTraceRouting) return;
  if (trace._couldNotFindPort) return;
  if (trace.root?.schematicDisabled) return;
  if (trace.getCollapsedSchematicBoxAncestor()) return;
  const { db } = trace.root;
  const { _parsedProps: props, parent } = trace;
  const schematicNetLabelText = trace._getSchematicNetLabelText();
  const schematicSheetId = trace._resolveSchematicSheetId();
  if (!parent) throw new Error("Trace has no parent");
  let allPortsFound;
  let connectedPorts;
  try {
    const result = trace._findConnectedPorts();
    allPortsFound = result.allPortsFound;
    connectedPorts = result.portsWithSelectors ?? [];
  } catch (error) {
    if (error instanceof TraceConnectionError) {
      db.source_trace_not_connected_error.insert({
        ...error.errorData,
        error_type: "source_trace_not_connected_error"
      });
      return;
    }
    throw error;
  }
  const { netsWithSelectors } = trace._findConnectedNets();
  if (!allPortsFound) return;
  const resolveSchematicPortForSheet = (port) => {
    if (schematicSheetId && port.source_port_id) {
      const sheetSchematicPort = db.schematic_port.getWhere({
        source_port_id: port.source_port_id,
        schematic_sheet_id: schematicSheetId
      });
      if (sheetSchematicPort) return sheetSchematicPort;
    }
    if (port.schematic_port_id) {
      return db.schematic_port.get(port.schematic_port_id) ?? null;
    }
    if (!schematicSheetId && port.source_port_id) {
      const unassignedSchematicPorts = db.schematic_port.list({ source_port_id: port.source_port_id }).filter((schematicPort) => !schematicPort.schematic_sheet_id);
      if (unassignedSchematicPorts.length === 1) {
        return unassignedSchematicPorts[0];
      }
    }
    return null;
  };
  const portsWithPosition = connectedPorts.flatMap(({ port }) => {
    const schematicPort = resolveSchematicPortForSheet(port);
    if (!schematicPort) return [];
    return [
      {
        port,
        position: schematicPort.center,
        schematic_port_id: schematicPort.schematic_port_id,
        facingDirection: schematicPort.facing_direction ?? null
      }
    ];
  });
  const portIds = portsWithPosition.map(({ schematic_port_id }) => schematic_port_id).sort();
  const hasSheetLocalSchematicPortRepresentation = portsWithPosition.some(
    ({ port, schematic_port_id }) => port.schematic_port_id !== schematic_port_id
  );
  const portPairKey = portIds.join(",");
  const board = trace.root?._getBoard();
  if (board?._connectedSchematicPortPairs) {
    if (board._connectedSchematicPortPairs.has(portPairKey)) {
      return;
    }
  }
  const connection = {
    name: trace.source_trace_id,
    pointsToConnect: []
  };
  const obstacles = getSchematicObstaclesForTrace(trace);
  const isPortAndNetConnection = portsWithPosition.length === 1 && netsWithSelectors.length === 1;
  if (isPortAndNetConnection) {
    const net = netsWithSelectors[0].net;
    const { port, position: anchorPos } = portsWithPosition[0];
    let connectedNetLabel = trace.getSubcircuit().selectAll("netlabel").find((nl) => {
      const conn = nl._parsedProps.connection ?? nl._parsedProps.connectsTo;
      if (!conn) return false;
      if (Array.isArray(conn)) {
        return conn.some((selector) => {
          const targetPort2 = trace.getSubcircuit().selectOne(selector, {
            port: true
          });
          return targetPort2 === port;
        });
      }
      const targetPort = trace.getSubcircuit().selectOne(conn, {
        port: true
      });
      return targetPort === port;
    });
    if (!connectedNetLabel) {
      const dbNetLabel = db.schematic_net_label.getWhere({
        source_trace_id: trace.source_trace_id
      });
      if (dbNetLabel) {
        connectedNetLabel = dbNetLabel;
      }
    }
    if (connectedNetLabel) {
      const labelPos = "_getGlobalSchematicPositionBeforeLayout" in connectedNetLabel ? connectedNetLabel._getGlobalSchematicPositionBeforeLayout() : connectedNetLabel.anchor_position;
      const edges2 = [];
      if (anchorPos.x === labelPos.x || anchorPos.y === labelPos.y) {
        edges2.push({ from: anchorPos, to: labelPos });
      } else {
        edges2.push({ from: anchorPos, to: { x: labelPos.x, y: anchorPos.y } });
        edges2.push({ from: { x: labelPos.x, y: anchorPos.y }, to: labelPos });
      }
      const junctions2 = createSchematicTraceJunctions({
        edges: edges2,
        db,
        source_trace_id: trace.source_trace_id,
        schematic_sheet_id: schematicSheetId
      });
      const dbTrace2 = db.schematic_trace.insert({
        source_trace_id: trace.source_trace_id,
        edges: edges2,
        junctions: junctions2,
        subcircuit_connectivity_map_key: trace.subcircuit_connectivity_map_key ?? void 0,
        schematic_sheet_id: schematicSheetId
      });
      trace.schematic_trace_id = dbTrace2.schematic_trace_id;
      return;
    }
    if (schematicNetLabelText) {
      const side2 = getEnteringEdgeFromDirection(port.facingDirection) ?? "bottom";
      db.schematic_net_label.insert({
        text: schematicNetLabelText,
        source_net_id: net.source_net_id,
        anchor_position: anchorPos,
        center: computeSchematicNetLabelCenter({
          anchor_position: anchorPos,
          anchor_side: side2,
          text: schematicNetLabelText
        }),
        anchor_side: side2,
        schematic_sheet_id: trace._resolveSchematicSheetId()
      });
      return;
    }
    const side = getEnteringEdgeFromDirection(port.facingDirection) ?? "bottom";
    const netLabel = db.schematic_net_label.insert({
      text: net._parsedProps.name,
      source_net_id: net.source_net_id,
      anchor_position: anchorPos,
      center: computeSchematicNetLabelCenter({
        anchor_position: anchorPos,
        anchor_side: side,
        text: net._parsedProps.name
      }),
      anchor_side: side,
      schematic_sheet_id: trace._resolveSchematicSheetId()
    });
    return;
  }
  if (schematicNetLabelText && !hasSheetLocalSchematicPortRepresentation) {
    if ("from" in trace.props && "to" in trace.props || "path" in trace.props) {
      trace._doInitialSchematicTraceRenderWithDisplayLabel();
      return;
    }
  }
  if (portsWithPosition.length < 2) {
    return;
  }
  const attemptElbowEdges = () => {
    const elbowEdges = [];
    for (let i = 0; i < portsWithPosition.length - 1; i++) {
      const start = portsWithPosition[i];
      const end = portsWithPosition[i + 1];
      const path = calculateElbow(
        {
          x: start.position.x,
          y: start.position.y,
          facingDirection: convertFacingDirectionToElbowDirection(
            start.facingDirection
          )
        },
        {
          x: end.position.x,
          y: end.position.y,
          facingDirection: convertFacingDirectionToElbowDirection(
            end.facingDirection
          )
        }
      );
      for (let j = 0; j < path.length - 1; j++) {
        elbowEdges.push({ from: path[j], to: path[j + 1] });
      }
    }
    const doesSegmentIntersectRect2 = (edge, rect) => {
      const halfW = rect.width / 2;
      const halfH = rect.height / 2;
      const left = rect.center.x - halfW;
      const right = rect.center.x + halfW;
      const top = rect.center.y - halfH;
      const bottom = rect.center.y + halfH;
      const inRect = (p) => p.x >= left && p.x <= right && p.y >= top && p.y <= bottom;
      if (inRect(edge.from) || inRect(edge.to)) return true;
      const rectEdges = [
        [
          { x: left, y: top },
          { x: right, y: top }
        ],
        [
          { x: right, y: top },
          { x: right, y: bottom }
        ],
        [
          { x: right, y: bottom },
          { x: left, y: bottom }
        ],
        [
          { x: left, y: bottom },
          { x: left, y: top }
        ]
      ];
      return rectEdges.some(
        (r) => doesLineIntersectLine3([edge.from, edge.to], r, { lineThickness: 0 })
      );
    };
    for (const edge of elbowEdges) {
      for (const obstacle of obstacles) {
        if (doesSegmentIntersectRect2(edge, obstacle)) {
          return null;
        }
      }
    }
    return elbowEdges;
  };
  let edges = attemptElbowEdges();
  if (edges && edges.length === 0) {
    edges = null;
  }
  connection.pointsToConnect = portsWithPosition.map(({ position }) => ({
    ...position,
    layer: "top"
  }));
  const bounds = computeObstacleBounds(obstacles);
  const BOUNDS_MARGIN = 2;
  const simpleRouteJsonInput = {
    minTraceWidth: 0.1,
    obstacles,
    connections: [connection],
    bounds: {
      minX: bounds.minX - BOUNDS_MARGIN,
      maxX: bounds.maxX + BOUNDS_MARGIN,
      minY: bounds.minY - BOUNDS_MARGIN,
      maxY: bounds.maxY + BOUNDS_MARGIN
    },
    layerCount: 1
  };
  let Autorouter = MultilayerIjump2;
  let skipOtherTraceInteraction = false;
  if (trace.getSubcircuit().props._schDirectLineRoutingEnabled) {
    Autorouter = DirectLineRouter;
    skipOtherTraceInteraction = true;
  }
  if (!edges) {
    const autorouter = new Autorouter({
      input: simpleRouteJsonInput,
      MAX_ITERATIONS: 100,
      OBSTACLE_MARGIN: 0.1,
      isRemovePathLoopsEnabled: true,
      isShortenPathWithShortcutsEnabled: true,
      marginsWithCosts: [
        {
          margin: 1,
          enterCost: 0,
          travelCostFactor: 1
        },
        {
          margin: 0.3,
          enterCost: 0,
          travelCostFactor: 1
        },
        {
          margin: 0.2,
          enterCost: 0,
          travelCostFactor: 2
        },
        {
          margin: 0.1,
          enterCost: 0,
          travelCostFactor: 3
        }
      ]
    });
    let results = autorouter.solveAndMapToTraces();
    if (results.length === 0) {
      if (!hasSheetLocalSchematicPortRepresentation && (trace._isSymbolToChipConnection() || trace._isSymbolToSymbolConnection() || trace._isChipToChipConnection())) {
        trace._doInitialSchematicTraceRenderWithDisplayLabel();
        return;
      }
      const directLineRouter = new DirectLineRouter({
        input: simpleRouteJsonInput
      });
      results = directLineRouter.solveAndMapToTraces();
      skipOtherTraceInteraction = true;
    }
    const [{ route }] = results;
    edges = [];
    const wireAndViaRoutes = route.filter(
      (r) => r.route_type === "wire" || r.route_type === "via"
    );
    for (let i = 0; i < wireAndViaRoutes.length - 1; i++) {
      edges.push({
        from: wireAndViaRoutes[i],
        to: wireAndViaRoutes[i + 1]
      });
    }
  }
  const source_trace_id = trace.source_trace_id;
  let junctions = [];
  if (!skipOtherTraceInteraction) {
    pushEdgesOfSchematicTraceToPreventOverlap({
      edges,
      db,
      source_trace_id,
      schematic_sheet_id: schematicSheetId
    });
    const otherEdges = getOtherSchematicTraces({
      db,
      source_trace_id,
      schematic_sheet_id: schematicSheetId,
      differentNetOnly: true
    }).flatMap((t) => t.edges);
    edges = createSchematicTraceCrossingSegments({ edges, otherEdges });
    junctions = createSchematicTraceJunctions({
      edges,
      db,
      source_trace_id: trace.source_trace_id,
      schematic_sheet_id: schematicSheetId
    });
  }
  if (!edges || edges.length === 0) {
    return;
  }
  const lastEdge = edges[edges.length - 1];
  const lastEdgePort = portsWithPosition[portsWithPosition.length - 1];
  const lastDominantDirection = getDominantDirection(lastEdge);
  edges.push(...getStubEdges({ lastEdge, lastEdgePort, lastDominantDirection }));
  const firstEdge = edges[0];
  const firstEdgePort = portsWithPosition[0];
  const firstDominantDirection = getDominantDirection(firstEdge);
  edges.unshift(
    ...getStubEdges({
      firstEdge,
      firstEdgePort,
      firstDominantDirection
    })
  );
  if (!trace.source_trace_id) {
    throw new Error("Missing source_trace_id for schematic trace insertion.");
  }
  if (!hasSheetLocalSchematicPortRepresentation && trace.getSubcircuit()._parsedProps.schTraceAutoLabelEnabled && countComplexElements(junctions, edges) >= 5 && (trace._isSymbolToChipConnection() || trace._isSymbolToSymbolConnection() || trace._isChipToChipConnection())) {
    trace._doInitialSchematicTraceRenderWithDisplayLabel();
    return;
  }
  const dbTrace = db.schematic_trace.insert({
    source_trace_id: trace.source_trace_id,
    edges,
    junctions,
    subcircuit_connectivity_map_key: trace.subcircuit_connectivity_map_key ?? void 0,
    schematic_sheet_id: schematicSheetId
  });
  trace.schematic_trace_id = dbTrace.schematic_trace_id;
  for (const { schematic_port_id } of portsWithPosition) {
    db.schematic_port.update(schematic_port_id, { is_connected: true });
  }
  if (board?._connectedSchematicPortPairs)
    board._connectedSchematicPortPairs.add(portPairKey);
};

// lib/components/primitive-components/Trace/trace-utils/get-max-length-from-connected-components.ts
var getMaxLengthFromConnectedComponents = (ports, { db }) => {
  const componentMaxLengths = ports.map((port) => {
    const sourcePort = db.source_port.get(port.source_port_id);
    if (!sourcePort?.source_component_id) return null;
    const sourceComponent = db.source_component.get(
      sourcePort.source_component_id
    );
    if (sourceComponent?.ftype === "simple_capacitor") {
      return sourceComponent.max_decoupling_trace_length;
    }
    return null;
  }).filter((length7) => typeof length7 === "number");
  if (componentMaxLengths.length === 0) return void 0;
  return Math.min(...componentMaxLengths);
};

// lib/components/primitive-components/Trace/trace-utils/get-trace-display-name.ts
function getTraceDisplayName({
  ports,
  nets
}) {
  if (ports.length >= 2) {
    return `${ports[0]?.selector} to ${ports[1]?.selector}`;
  }
  if (ports.length === 1 && nets.length === 1) {
    return `${ports[0]?.selector} to net.${nets[0]._parsedProps.name}`;
  }
  return void 0;
}

// lib/components/primitive-components/Trace/Trace.ts
var Trace3 = class extends PrimitiveComponent2 {
  source_trace_id = null;
  pcb_trace_id = null;
  schematic_trace_id = null;
  _inflatedPcbTraces;
  _inflatedPcbVias;
  _portsRoutedOnPcb;
  subcircuit_connectivity_map_key = null;
  _exposesSubcircuitConnection = false;
  _traceConnectionHash = null;
  _couldNotFindPort;
  constructor(props) {
    super(props);
    this._portsRoutedOnPcb = [];
  }
  /**
   * Get the explicit trace thickness, supporting 'width' as an alias for 'thickness'
   */
  _getExplicitTraceThickness() {
    return this._parsedProps.thickness ?? this._parsedProps.width;
  }
  _getSchematicNetLabelText() {
    return this._parsedProps.schDisplayLabel ?? this._parsedProps.displayName ?? this._parsedProps.name;
  }
  get config() {
    return {
      zodProps: traceProps,
      componentName: "Trace"
    };
  }
  _getTracePortOrNetSelectorListFromProps() {
    if ("from" in this.props && "to" in this.props) {
      return [
        typeof this.props.from === "string" ? this.props.from : this.props.from.getPortSelector(),
        typeof this.props.to === "string" ? this.props.to : this.props.to.getPortSelector()
      ];
    }
    if ("path" in this.props) {
      return this.props.path.map(
        (p) => typeof p === "string" ? p : p.getPortSelector()
      );
    }
    return [];
  }
  getTracePortPathSelectors() {
    return this._getTracePortOrNetSelectorListFromProps().filter(
      (selector) => !selector.includes("net.")
    );
  }
  getTracePathNetSelectors() {
    return this._getTracePortOrNetSelectorListFromProps().filter(
      (selector) => selector.includes("net.")
    );
  }
  _findConnectedPorts() {
    return Trace__findConnectedPorts(this);
  }
  _resolveNet(selector) {
    const direct = this.getSubcircuit().selectOne(selector, {
      type: "net"
    });
    if (direct) return direct;
    const match = selector.match(/^net\.(.+)$/);
    const netName = match ? match[1] : null;
    if (!netName) return null;
    const board = this.root?._getBoard();
    if (!board) {
      this.renderError(
        `Could not find a <board> ancestor for ${this}, so net "${selector}" cannot be resolved`
      );
      return null;
    }
    const allDescendants = board.getDescendants();
    return allDescendants.find(
      (d) => d.componentName === "Net" && d._parsedProps.name === netName
    ) || null;
  }
  _findConnectedNets() {
    const netsWithSelectors = this.getTracePathNetSelectors().map(
      (selector) => ({
        selector,
        net: this._resolveNet(selector)
      })
    );
    const undefinedNets = netsWithSelectors.filter((n) => !n.net);
    if (undefinedNets.length > 0) {
      this.renderError(
        `Could not find net for selector "${undefinedNets[0].selector}" inside ${this}`
      );
    }
    return { netsWithSelectors, nets: netsWithSelectors.map((n) => n.net) };
  }
  /**
   * Get all the traces that are connected in any degree to this trace, this is
   * used during autorouting to routes to pass through traces connected to the
   * same net.
   */
  _getAllTracesConnectedToSameNet() {
    const traces = this.getSubcircuit().selectAll("trace");
    const myNets = this._findConnectedNets().nets;
    const myPorts = this._findConnectedPorts().ports ?? [];
    return traces.filter((t) => {
      if (t === this) return false;
      const tNets = t._findConnectedNets().nets;
      const tPorts = t._findConnectedPorts().ports ?? [];
      return tNets.some((n) => myNets.includes(n)) || tPorts.some((p) => myPorts.includes(p));
    });
  }
  /**
   * Determine if a trace is explicitly connected to a port (not via a net)
   */
  _isExplicitlyConnectedToPort(port) {
    const { allPortsFound, portsWithSelectors: portsWithMetadata } = this._findConnectedPorts();
    if (!allPortsFound) return false;
    const ports = portsWithMetadata.map((p) => p.port);
    return ports.includes(port);
  }
  /**
   * Determine if a trace is explicitly connected to a net (not via a port)
   */
  _isExplicitlyConnectedToNet(net) {
    const nets = this._findConnectedNets().nets;
    return nets.includes(net);
  }
  doInitialCreateNetsFromProps() {
    createNetsFromProps(this, this.getTracePathNetSelectors());
  }
  _computeTraceConnectionHash() {
    const { allPortsFound, ports } = this._findConnectedPorts();
    if (!allPortsFound || !ports) return null;
    const sortedPorts = [...ports].sort(
      (a, b) => (a.pcb_port_id || "").localeCompare(b.pcb_port_id || "")
    );
    const allIds = sortedPorts.map((p) => p.pcb_port_id);
    return allIds.join(",");
  }
  doInitialSourceTraceRender() {
    const { db } = this.root;
    const { _parsedProps: props, parent } = this;
    if (!parent) {
      this.renderError("Trace has no parent");
      return;
    }
    let allPortsFound;
    let ports;
    try {
      const result = this._findConnectedPorts();
      allPortsFound = result.allPortsFound;
      ports = result.portsWithSelectors ?? [];
    } catch (error) {
      if (error instanceof TraceConnectionError) {
        db.source_trace_not_connected_error.insert({
          ...error.errorData,
          error_type: "source_trace_not_connected_error"
        });
        this._couldNotFindPort = true;
        return;
      }
      throw error;
    }
    if (!allPortsFound) return;
    this._traceConnectionHash = this._computeTraceConnectionHash();
    const existingTraces = db.source_trace.list();
    const existingTrace = existingTraces.find(
      (t) => t.subcircuit_connectivity_map_key === this.subcircuit_connectivity_map_key && [...t.connected_source_port_ids].sort().join(",") === this._traceConnectionHash
    );
    if (existingTrace) {
      this.source_trace_id = existingTrace.source_trace_id;
      return;
    }
    const nets = this._findConnectedNets().nets;
    const displayName = getTraceDisplayName({ ports, nets });
    const trace = db.source_trace.insert({
      connected_source_port_ids: ports.map((p) => p.port.source_port_id),
      connected_source_net_ids: nets.map((n) => n.source_net_id),
      subcircuit_id: this.getSubcircuit()?.subcircuit_id,
      name: props.name,
      max_length: props.maxLength ?? getMaxLengthFromConnectedComponents(
        ports.map((p) => p.port),
        { db }
      ),
      display_name: props.displayName ?? displayName,
      min_trace_thickness: this._getExplicitTraceThickness()
    });
    this.source_trace_id = trace.source_trace_id;
    if (!props.name) {
      db.source_unnamed_trace_warning.insert({
        message: `${this.getString()} is missing a name. Add a name prop to make the trace easier to identify.`,
        source_trace_id: trace.source_trace_id,
        subcircuit_id: this.getSubcircuit()?.subcircuit_id ?? void 0,
        warning_type: "source_unnamed_trace_warning"
      });
    }
  }
  _insertErrorIfTraceIsOutsideBoard(mergedRoute, ports) {
    const { db } = this.root;
    const board = this._getBoard();
    if (!board) return;
    const pcbBoardId = board.pcb_board_id;
    if (!pcbBoardId) return;
    const isOutsideBoard = isRouteOutsideBoard({
      mergedRoute,
      db,
      pcbBoardId
    });
    if (isOutsideBoard) {
      db.pcb_trace_error.insert({
        error_type: "pcb_trace_error",
        source_trace_id: this.source_trace_id,
        message: `Trace ${this.getString()} routed outside the board boundaries.`,
        pcb_trace_id: this.pcb_trace_id,
        pcb_component_ids: [],
        pcb_port_ids: ports.map((p) => p.pcb_port_id)
      });
    }
  }
  doInitialPcbManualTraceRender() {
    Trace_doInitialPcbManualTraceRender(this);
  }
  doInitialPcbTraceRender() {
    Trace_doInitialPcbTraceRender(this);
  }
  _doInitialSchematicTraceRenderWithDisplayLabel() {
    Trace__doInitialSchematicTraceRenderWithDisplayLabel(this);
  }
  _isSymbolToChipConnection() {
    const { allPortsFound, ports } = this._findConnectedPorts();
    if (!allPortsFound || ports.length !== 2) return false;
    const [port1, port2] = ports;
    if (!port1?.parent || !port2?.parent) return false;
    const isPort1Chip = port1.parent.config.shouldRenderAsSchematicBox;
    const isPort2Chip = port2.parent.config.shouldRenderAsSchematicBox;
    return isPort1Chip && !isPort2Chip || !isPort1Chip && isPort2Chip;
  }
  _isSymbolToSymbolConnection() {
    const { allPortsFound, ports } = this._findConnectedPorts();
    if (!allPortsFound || ports.length !== 2) return false;
    const [port1, port2] = ports;
    if (!port1?.parent || !port2?.parent) return false;
    const isPort1Symbol = !port1.parent.config.shouldRenderAsSchematicBox;
    const isPort2Symbol = !port2.parent.config.shouldRenderAsSchematicBox;
    return isPort1Symbol && isPort2Symbol;
  }
  _isChipToChipConnection() {
    const { allPortsFound, ports } = this._findConnectedPorts();
    if (!allPortsFound || ports.length !== 2) return false;
    const [port1, port2] = ports;
    if (!port1?.parent || !port2?.parent) return false;
    const isPort1Chip = port1.parent.config.shouldRenderAsSchematicBox;
    const isPort2Chip = port2.parent.config.shouldRenderAsSchematicBox;
    return isPort1Chip && isPort2Chip;
  }
  doInitialSchematicTraceRender() {
    Trace_doInitialSchematicTraceRender(this);
  }
};

// lib/fiber/create-instance-from-react-element.ts
import "react";
import ReactReconciler from "react-reconciler";
import { DefaultEventPriority } from "react-reconciler/constants.js";

// lib/fiber/catalogue.ts
var catalogue = {};
var extendCatalogue = (objects) => {
  const altKeys = Object.fromEntries(
    Object.entries(objects).map(([key, v]) => [key.toLowerCase(), v])
  );
  Object.assign(catalogue, objects);
  Object.assign(catalogue, altKeys);
};

// lib/fiber/create-instance-from-react-element.ts
import { identity as identity4 } from "transformation-matrix";

// lib/components/primitive-components/ErrorPlaceholder.ts
import { z as z3 } from "zod";
var ErrorPlaceholderComponent = class extends PrimitiveComponent2 {
  constructor(props, error) {
    super(props);
    const resolveCoordinate = (value, axis) => {
      if (typeof value === "number") return value;
      if (typeof value === "string") {
        try {
          return this._resolvePcbCoordinate(value, axis, {
            allowBoardVariables: false
          });
        } catch (err) {
          return 0;
        }
      }
      return 0;
    };
    this._parsedProps = {
      ...props,
      error,
      type: props.type || "unknown",
      component_name: props.name,
      error_type: "source_failed_to_create_component_error",
      message: error instanceof Error ? error.message : String(error),
      pcbX: resolveCoordinate(props.pcbX, "pcbX"),
      pcbY: resolveCoordinate(props.pcbY, "pcbY"),
      schX: props.schX,
      schY: props.schY
    };
  }
  formatErrorDetails() {
    const selectorValue = this._parsedProps.selector ?? this._parsedProps.selectors ?? this._parsedProps.in ?? this._parsedProps.within;
    const selectorText = selectorValue === void 0 ? void 0 : typeof selectorValue === "string" ? selectorValue : this.safeStringify(selectorValue);
    const propsText = this.safeStringify(this._parsedProps);
    return {
      selectorText,
      propsText
    };
  }
  safeStringify(value) {
    const seen = /* @__PURE__ */ new WeakSet();
    const json = JSON.stringify(
      value,
      (_key, item) => {
        if (typeof item === "bigint") {
          return item.toString();
        }
        if (typeof item === "function") {
          return `[Function ${item.name || "anonymous"}]`;
        }
        if (typeof item === "symbol") {
          return item.toString();
        }
        if (typeof item === "object" && item !== null) {
          if (seen.has(item)) return "[Circular]";
          seen.add(item);
        }
        return item;
      },
      2
    );
    if (!json) {
      return "undefined";
    }
    if (json.length > 2e3) {
      return `${json.slice(0, 2e3)}\u2026`;
    }
    return json;
  }
  get config() {
    return {
      componentName: "ErrorPlaceholder",
      zodProps: z3.object({}).passthrough()
    };
  }
  doInitialSourceRender() {
    if (this.root?.db) {
      const pcbPosition = this._getGlobalPcbPositionBeforeLayout();
      const schematicPosition = this._getGlobalSchematicPositionBeforeLayout();
      const { selectorText, propsText } = this.formatErrorDetails();
      const messageDetails = [
        selectorText ? `Within selector: ${selectorText}` : null,
        `Props: ${propsText}`
      ].filter(Boolean).join(" ");
      const baseMessage = `Could not create ${this._parsedProps.componentType ?? "component"}${this._parsedProps.name ? ` "${this._parsedProps.name}"` : ""}. ${this._parsedProps.error?.formattedError?._errors?.join("; ") || this._parsedProps.message}`;
      this.root.db.source_failed_to_create_component_error.insert({
        component_name: this._parsedProps.component_name,
        error_type: "source_failed_to_create_component_error",
        message: `${baseMessage}${messageDetails ? ` Details: ${messageDetails}` : ""}`,
        pcb_center: pcbPosition,
        schematic_center: schematicPosition
      });
    }
  }
};
function createErrorPlaceholderComponent(props, error) {
  return new ErrorPlaceholderComponent(props, error);
}

// lib/fiber/create-instance-from-react-element.ts
function prepare(object, state) {
  const instance = object;
  instance.__tsci = {
    ...state
  };
  return object;
}
var hostConfig = {
  supportsMutation: true,
  createInstance(type, props) {
    const target = catalogue[type];
    if (!target) {
      if (Object.keys(catalogue).length === 0) {
        throw new Error(
          "No components registered in catalogue, did you forget to import lib/register-catalogue in your test file?"
        );
      }
      throw new Error(
        `Unsupported component type "${type}". No element with this name is registered in the @tscircuit/core catalogue. Check for typos or see https://docs.tscircuit.com/category/built-in-elements for a list of valid components. To add your own component, see docs/CREATING_NEW_COMPONENTS.md`
      );
    }
    try {
      const instance = prepare(new target(props), {});
      return instance;
    } catch (error) {
      return createErrorPlaceholderComponent(
        { ...props, componentType: type },
        error
      );
    }
  },
  createTextInstance(text) {
    return { __text: text };
  },
  appendInitialChild(parentInstance, child) {
    parentInstance.add(child);
  },
  appendChild(parentInstance, child) {
    parentInstance.add(child);
  },
  appendChildToContainer(container, child) {
    container.add(child);
  },
  finalizeInitialChildren() {
    return false;
  },
  prepareUpdate() {
    return null;
  },
  shouldSetTextContent() {
    return false;
  },
  getRootHostContext() {
    return {};
  },
  getChildHostContext() {
    return {};
  },
  prepareForCommit() {
    return null;
  },
  resetAfterCommit() {
  },
  commitMount() {
  },
  commitUpdate() {
  },
  removeChild() {
  },
  clearContainer() {
  },
  supportsPersistence: false,
  getPublicInstance(instance) {
    return instance;
  },
  preparePortalMount(containerInfo) {
    throw new Error("Function not implemented.");
  },
  scheduleTimeout(fn, delay) {
    throw new Error("Function not implemented.");
  },
  cancelTimeout(id) {
    throw new Error("Function not implemented.");
  },
  noTimeout: void 0,
  isPrimaryRenderer: false,
  getInstanceFromNode(node) {
    throw new Error("Function not implemented.");
  },
  beforeActiveInstanceBlur() {
    throw new Error("Function not implemented.");
  },
  afterActiveInstanceBlur() {
    throw new Error("Function not implemented.");
  },
  prepareScopeUpdate: (scopeInstance, instance) => {
    throw new Error("Function not implemented.");
  },
  getInstanceFromScope: (scopeInstance) => {
    throw new Error("Function not implemented.");
  },
  detachDeletedInstance: (node) => {
    throw new Error("Function not implemented.");
  },
  // https://github.com/pmndrs/react-three-fiber/pull/2360#discussion_r916356874
  getCurrentEventPriority: () => DefaultEventPriority,
  // @ts-expect-error
  // https://github.com/diegomura/react-pdf/blob/fabecc56727dfb6d590a3fa1e11f50250ecbbea1/packages/reconciler/src/reconciler-31.js#L57
  getCurrentUpdatePriority: () => DefaultEventPriority,
  resolveUpdatePriority: () => DefaultEventPriority,
  setCurrentUpdatePriority: () => {
  },
  maySuspendCommit: () => false,
  supportsHydration: false
};
var reconciler = ReactReconciler(hostConfig);
var createInstanceFromReactElement = (reactElm) => {
  const rootContainer = {
    children: [],
    props: {
      name: "$root"
    },
    add(instance) {
      instance.parent = this;
      this.children.push(instance);
    },
    computePcbGlobalTransform() {
      return identity4();
    }
  };
  const containerErrors = [];
  const container = reconciler.createContainer(
    // TODO Replace with store like react-three-fiber
    // https://github.com/pmndrs/react-three-fiber/blob/a457290856f57741bf8beef4f6ff9dbf4879c0a5/packages/fiber/src/core/index.tsx#L172
    // https://github.com/pmndrs/react-three-fiber/blob/master/packages/fiber/src/core/store.ts#L168
    rootContainer,
    0,
    null,
    false,
    null,
    "tsci",
    (error) => {
      console.log("Error in createContainer");
      console.error(error);
      containerErrors.push(error);
    },
    null
  );
  reconciler.updateContainerSync(reactElm, container, null, () => {
  });
  reconciler.flushSyncWork();
  if (containerErrors.length > 0) {
    throw containerErrors[0];
  }
  const rootInstance = reconciler.getPublicRootInstance(
    container
  );
  if (rootInstance) return rootInstance;
  return rootContainer.children[0];
};

// lib/soup/underscorifyPinStyles.ts
import "circuit-json";
import "zod";

// lib/utils/schematic/getPinNumberFromPinLabelsKey.ts
var PIN_LABELS_KEY_RE = /^(?:pin)?(\d+)$/;
var getPinNumberFromPinLabelsKey = (pinKey) => {
  const match = pinKey.match(PIN_LABELS_KEY_RE);
  if (!match) return null;
  return Number.parseInt(match[1], 10);
};

// lib/utils/filterPinLabels.ts
function filterPinLabels(pinLabels) {
  if (!pinLabels)
    return {
      validPinLabels: pinLabels,
      invalidPinLabelsMessages: []
    };
  const validPinLabels = {};
  const invalidPinLabelsMessages = [];
  for (const [pin, labelOrLabels] of Object.entries(pinLabels)) {
    const labels = Array.isArray(labelOrLabels) ? labelOrLabels.slice() : [labelOrLabels];
    const validLabels = [];
    for (const label of labels) {
      if (isValidPinLabel(pin, label)) {
        validLabels.push(label);
      } else {
        const recommendation = getPinLabelRecommendation(label);
        const recommendationMessage = recommendation ? ` Try using "${recommendation}" instead.` : "";
        invalidPinLabelsMessages.push(
          `Invalid pin label: ${pin} = '${label}' - excluding from component. Pin labels can only contain letters, numbers and underscores.${recommendationMessage}`
        );
      }
    }
    if (validLabels.length > 0) {
      validPinLabels[pin] = Array.isArray(labelOrLabels) ? validLabels : validLabels[0];
    }
  }
  return {
    validPinLabels: Object.keys(validPinLabels).length > 0 ? validPinLabels : void 0,
    invalidPinLabelsMessages
  };
}
function isValidPinLabel(_pin, label) {
  return /^[A-Za-z0-9_]+$/.test(label);
}
function getPinLabelRecommendation(label) {
  const decimalVoltage = label.match(/^\+?(\d+)\.(\d+)V(_\d+)?$/i);
  if (decimalVoltage) {
    return `V${decimalVoltage[1]}_${decimalVoltage[2]}${decimalVoltage[3] ?? ""}`;
  }
  const splitVoltage = label.match(/^\+?(\d+)V(\d+)(_\d+)?$/i);
  if (splitVoltage) {
    return `V${splitVoltage[1]}_${splitVoltage[2]}${splitVoltage[3] ?? ""}`;
  }
  const wholeVoltage = label.match(/^\+?(\d+)V(_\d+)?$/i);
  if (wholeVoltage) {
    return `V${wholeVoltage[1]}${wholeVoltage[2] ?? ""}`;
  }
  return void 0;
}

// lib/utils/schematic/parsePinNumberFromLabelsOrThrow.ts
var getInvalidPinLabelMessage = (pinNumberOrLabel) => {
  const recommendation = getPinLabelRecommendation(pinNumberOrLabel);
  const recommendationMessage = recommendation ? ` Try using "${recommendation}" instead.` : "";
  return `No pin labels provided and pin number or label is not a number: "${pinNumberOrLabel}".${recommendationMessage}`;
};
var parsePinNumberFromLabelsOrThrow = (pinNumberOrLabel, pinLabels) => {
  if (typeof pinNumberOrLabel === "number") {
    return pinNumberOrLabel;
  }
  const directPinNumber = getPinNumberFromPinLabelsKey(pinNumberOrLabel);
  if (directPinNumber !== null) {
    return directPinNumber;
  }
  if (pinNumberOrLabel.startsWith("pin")) {
    throw new Error(
      `Invalid pinLabels key "${pinNumberOrLabel}". Expected "pin\${number}" (e.g. pin1, pin2).`
    );
  }
  if (!pinLabels) {
    throw new Error(getInvalidPinLabelMessage(pinNumberOrLabel));
  }
  for (const pinNumberKey in pinLabels) {
    const aliases = Array.isArray(pinLabels[pinNumberKey]) ? pinLabels[pinNumberKey] : [pinLabels[pinNumberKey]];
    if (aliases.includes(pinNumberOrLabel)) {
      const pinNumber = getPinNumberFromPinLabelsKey(pinNumberKey);
      if (pinNumber === null) {
        throw new Error(
          `Invalid pinLabels key "${pinNumberKey}". Expected "pin\${number}" (e.g. pin1, pin2).`
        );
      }
      return pinNumber;
    }
  }
  throw new Error(getInvalidPinLabelMessage(pinNumberOrLabel));
};

// lib/soup/underscorifyPinStyles.ts
var underscorifyPinStyles = (pinStyles, pinLabels) => {
  if (!pinStyles) return void 0;
  const underscorePinStyles = {};
  const mergedStyles = {};
  for (const [pinNameOrLabel, pinStyle] of Object.entries(pinStyles)) {
    const pinNumber = parsePinNumberFromLabelsOrThrow(pinNameOrLabel, pinLabels);
    mergedStyles[pinNumber] = {
      ...mergedStyles[pinNumber],
      ...pinStyle
    };
  }
  for (const [pinNumber, pinStyle] of Object.entries(mergedStyles)) {
    const pinKey = `pin${pinNumber}`;
    underscorePinStyles[pinKey] = {
      bottom_margin: pinStyle.bottomMargin,
      left_margin: pinStyle.leftMargin,
      right_margin: pinStyle.rightMargin,
      top_margin: pinStyle.topMargin
    };
  }
  return underscorePinStyles;
};

// lib/soup/underscorifyPortArrangement.ts
var underscorifyPortArrangement = (portArrangement) => {
  if (!portArrangement) return void 0;
  if ("leftSide" in portArrangement || "rightSide" in portArrangement || "topSide" in portArrangement || "bottomSide" in portArrangement) {
    return {
      left_side: portArrangement.leftSide,
      right_side: portArrangement.rightSide,
      top_side: portArrangement.topSide,
      bottom_side: portArrangement.bottomSide
    };
  }
  if ("leftPinCount" in portArrangement || "rightPinCount" in portArrangement || "topPinCount" in portArrangement || "bottomPinCount" in portArrangement) {
    return {
      left_size: portArrangement.leftPinCount,
      right_size: portArrangement.rightPinCount,
      top_size: portArrangement.topPinCount,
      bottom_size: portArrangement.bottomPinCount
    };
  }
  if ("leftSize" in portArrangement || "rightSize" in portArrangement || "topSize" in portArrangement || "bottomSize" in portArrangement) {
    return {
      left_size: portArrangement.leftSize,
      right_size: portArrangement.rightSize,
      top_size: portArrangement.topSize,
      bottom_size: portArrangement.bottomSize
    };
  }
  return void 0;
};

// lib/utils/createComponentsFromCircuitJson.ts
import { getUnitVectorFromDirection as getUnitVectorFromDirection2 } from "@tscircuit/math-utils";

// lib/components/primitive-components/CourtyardCircle.ts
import { courtyardCircleProps } from "@tscircuit/props";
var CourtyardCircle = class extends PrimitiveComponent2 {
  pcb_courtyard_circle_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "CourtyardCircle",
      zodProps: courtyardCircleProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for CourtyardCircle. Must be "top" or "bottom".`
      );
    }
    const subcircuit = this.getSubcircuit();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const pcb_courtyard_circle = db.pcb_courtyard_circle.insert({
      pcb_component_id,
      layer,
      center: {
        x: position.x,
        y: position.y
      },
      radius: props.radius,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
    });
    this.pcb_courtyard_circle_id = pcb_courtyard_circle.pcb_courtyard_circle_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const diameter = props.radius * 2;
    return { width: diameter, height: diameter };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_courtyard_circle_id) return;
    const circle = db.pcb_courtyard_circle.get(this.pcb_courtyard_circle_id);
    if (circle) {
      db.pcb_courtyard_circle.update(this.pcb_courtyard_circle_id, {
        center: {
          x: circle.center.x + deltaX,
          y: circle.center.y + deltaY
        }
      });
    }
  }
};

// lib/components/primitive-components/CourtyardOutline.ts
import { courtyardOutlineProps } from "@tscircuit/props";
import { getBoundsFromPoints } from "@tscircuit/math-utils";
import { applyToPoint as applyToPoint4 } from "transformation-matrix";
var CourtyardOutline = class extends PrimitiveComponent2 {
  pcb_courtyard_outline_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "CourtyardOutline",
      zodProps: courtyardOutlineProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for CourtyardOutline. Must be "top" or "bottom".`
      );
    }
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const subcircuit = this.getSubcircuit();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const pcb_courtyard_outline = db.pcb_courtyard_outline.insert({
      pcb_component_id,
      layer,
      outline: props.outline.map((p) => {
        const transformedPosition = applyToPoint4(transform, {
          x: p.x,
          y: p.y
        });
        return {
          x: transformedPosition.x,
          y: transformedPosition.y
        };
      }),
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
    });
    this.pcb_courtyard_outline_id = pcb_courtyard_outline.pcb_courtyard_outline_id;
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    const currentOutline = db.pcb_courtyard_outline.get(
      this.pcb_courtyard_outline_id
    );
    if (!currentOutline) return;
    let currentCenterX = 0;
    let currentCenterY = 0;
    for (const point6 of currentOutline.outline) {
      currentCenterX += point6.x;
      currentCenterY += point6.y;
    }
    currentCenterX /= currentOutline.outline.length;
    currentCenterY /= currentOutline.outline.length;
    const offsetX = newCenter.x - currentCenterX;
    const offsetY = newCenter.y - currentCenterY;
    const newOutline = currentOutline.outline.map((point6) => ({
      x: point6.x + offsetX,
      y: point6.y + offsetY
    }));
    db.pcb_courtyard_outline.update(this.pcb_courtyard_outline_id, {
      outline: newOutline
    });
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_courtyard_outline_id) return;
    const outline = db.pcb_courtyard_outline.get(this.pcb_courtyard_outline_id);
    if (outline) {
      db.pcb_courtyard_outline.update(this.pcb_courtyard_outline_id, {
        outline: outline.outline.map((p) => ({
          x: p.x + deltaX,
          y: p.y + deltaY
        }))
      });
    }
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (!props.outline || props.outline.length === 0) {
      return { width: 0, height: 0 };
    }
    const bounds = getBoundsFromPoints(props.outline);
    if (!bounds) {
      return { width: 0, height: 0 };
    }
    return {
      width: bounds.maxX - bounds.minX,
      height: bounds.maxY - bounds.minY
    };
  }
};

// lib/components/primitive-components/CourtyardRect.ts
import { decomposeTSR } from "transformation-matrix";
import { courtyardRectProps } from "@tscircuit/props";
var CourtyardRect = class extends PrimitiveComponent2 {
  pcb_courtyard_rect_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "CourtyardRect",
      zodProps: courtyardRectProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const { maybeFlipLayer, isFlipped } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for CourtyardRect. Must be "top" or "bottom".`
      );
    }
    const subcircuit = this.getSubcircuit();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const decomposedTransform = decomposeTSR(
      this._computePcbGlobalTransformBeforeLayout()
    );
    const ccwRotationDegrees = decomposedTransform.rotation.angle * 180 / Math.PI;
    let ccw_rotation = (ccwRotationDegrees % 360 + 360) % 360;
    if (isFlipped) {
      ccw_rotation = (180 - ccw_rotation + 360) % 360;
    }
    const pcb_courtyard_rect = db.pcb_courtyard_rect.insert({
      pcb_component_id,
      layer,
      center: {
        x: position.x,
        y: position.y
      },
      width: props.width,
      height: props.height,
      ccw_rotation: ccw_rotation || void 0,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
    });
    this.pcb_courtyard_rect_id = pcb_courtyard_rect.pcb_courtyard_rect_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    return { width: props.width, height: props.height };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_courtyard_rect_id) return;
    const rect = db.pcb_courtyard_rect.get(this.pcb_courtyard_rect_id);
    if (rect) {
      db.pcb_courtyard_rect.update(this.pcb_courtyard_rect_id, {
        center: {
          x: rect.center.x + deltaX,
          y: rect.center.y + deltaY
        }
      });
    }
  }
};

// lib/utils/normalizeTextForCircuitJson.ts
function normalizeTextForCircuitJson(text) {
  return text.replace(/\\n/g, "\n");
}

// lib/components/primitive-components/CopperText.ts
import { copperTextProps } from "@tscircuit/props";
var CopperText = class extends PrimitiveComponent2 {
  isPcbPrimitive = true;
  pcb_copper_text_id = null;
  get config() {
    return {
      componentName: "CopperText",
      zodProps: copperTextProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const container = this.getPrimitiveContainer();
    const position = this._getGlobalPcbPositionBeforeLayout();
    const subcircuit = this.getSubcircuit();
    const pcb_copper_text = db.pcb_copper_text.insert({
      anchor_alignment: props.anchorAlignment,
      anchor_position: {
        x: position.x,
        y: position.y
      },
      font: "tscircuit2024",
      font_size: props.fontSize,
      layer: props.layer ?? "top",
      text: normalizeTextForCircuitJson(props.text),
      ccw_rotation: props.pcbRotation,
      is_mirrored: props.mirrored,
      is_knockout: props.knockout,
      pcb_component_id: container.pcb_component_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
    });
    this.pcb_copper_text_id = pcb_copper_text.pcb_copper_text_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const fontSize = props.fontSize ?? 1;
    const text = props.text ?? "";
    const textWidth = text.length * fontSize;
    const textHeight = fontSize;
    return { width: textWidth * fontSize, height: textHeight * fontSize };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_copper_text_id) return;
    const text = db.pcb_copper_text.get(this.pcb_copper_text_id);
    if (text) {
      db.pcb_copper_text.update(this.pcb_copper_text_id, {
        anchor_position: {
          x: text.anchor_position.x + deltaX,
          y: text.anchor_position.y + deltaY
        }
      });
    }
  }
};

// lib/components/primitive-components/Cutout.ts
import { applyToPoint as applyToPoint5 } from "transformation-matrix";
import { cutoutProps } from "@tscircuit/props";
var Cutout = class extends PrimitiveComponent2 {
  pcb_cutout_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "Cutout",
      zodProps: cutoutProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const subcircuit = this.getSubcircuit();
    const pcb_group_id = this.getGroup()?.pcb_group_id ?? void 0;
    const globalPosition = this._getGlobalPcbPositionBeforeLayout();
    const container = this.getPrimitiveContainer();
    const parentRotation = container?._parsedProps.pcbRotation ?? 0;
    let inserted_pcb_cutout = void 0;
    if (props.shape === "rect") {
      const rotationDeg = typeof parentRotation === "string" ? parseInt(parentRotation.replace("deg", ""), 10) : parentRotation;
      const isRotated90 = Math.abs(rotationDeg % 180) === 90;
      const rectData = {
        shape: "rect",
        center: globalPosition,
        width: isRotated90 ? props.height : props.width,
        height: isRotated90 ? props.width : props.height,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id
      };
      inserted_pcb_cutout = db.pcb_cutout.insert(rectData);
    } else if (props.shape === "circle") {
      const circleData = {
        shape: "circle",
        center: globalPosition,
        radius: props.radius,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id
      };
      inserted_pcb_cutout = db.pcb_cutout.insert(circleData);
    } else if (props.shape === "polygon") {
      const transform = this._computePcbGlobalTransformBeforeLayout();
      const transformedPoints = props.points.map(
        (p) => applyToPoint5(transform, p)
      );
      const polygonData = {
        shape: "polygon",
        points: transformedPoints,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id
      };
      inserted_pcb_cutout = db.pcb_cutout.insert(polygonData);
    }
    if (inserted_pcb_cutout) {
      this.pcb_cutout_id = inserted_pcb_cutout.pcb_cutout_id;
    }
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (props.shape === "rect") {
      return { width: props.width, height: props.height };
    }
    if (props.shape === "circle") {
      return { width: props.radius * 2, height: props.radius * 2 };
    }
    if (props.shape === "polygon") {
      if (props.points.length === 0) return { width: 0, height: 0 };
      let minX = Infinity, maxX = -Infinity, minY = Infinity, maxY = -Infinity;
      for (const point6 of props.points) {
        minX = Math.min(minX, point6.x);
        maxX = Math.max(maxX, point6.x);
        minY = Math.min(minY, point6.y);
        maxY = Math.max(maxY, point6.y);
      }
      return { width: maxX - minX, height: maxY - minY };
    }
    return { width: 0, height: 0 };
  }
  _getPcbCircuitJsonBounds() {
    if (!this.pcb_cutout_id) return super._getPcbCircuitJsonBounds();
    const { db } = this.root;
    const cutout = db.pcb_cutout.get(this.pcb_cutout_id);
    if (!cutout) return super._getPcbCircuitJsonBounds();
    if (cutout.shape === "rect") {
      return {
        center: cutout.center,
        bounds: {
          left: cutout.center.x - cutout.width / 2,
          top: cutout.center.y + cutout.height / 2,
          // Assuming Y is up
          right: cutout.center.x + cutout.width / 2,
          bottom: cutout.center.y - cutout.height / 2
        },
        width: cutout.width,
        height: cutout.height
      };
    } else if (cutout.shape === "circle") {
      return {
        center: cutout.center,
        bounds: {
          left: cutout.center.x - cutout.radius,
          top: cutout.center.y + cutout.radius,
          right: cutout.center.x + cutout.radius,
          bottom: cutout.center.y - cutout.radius
        },
        width: cutout.radius * 2,
        height: cutout.radius * 2
      };
    } else if (cutout.shape === "polygon") {
      if (cutout.points.length === 0) return super._getPcbCircuitJsonBounds();
      let minX = Infinity, maxX = -Infinity, minY = Infinity, maxY = -Infinity;
      for (const point6 of cutout.points) {
        minX = Math.min(minX, point6.x);
        maxX = Math.max(maxX, point6.x);
        minY = Math.min(minY, point6.y);
        maxY = Math.max(maxY, point6.y);
      }
      return {
        center: { x: (minX + maxX) / 2, y: (minY + maxY) / 2 },
        bounds: { left: minX, top: maxY, right: maxX, bottom: minY },
        width: maxX - minX,
        height: maxY - minY
      };
    }
    return super._getPcbCircuitJsonBounds();
  }
  _setPositionFromLayout(newCenter) {
    if (!this.pcb_cutout_id) return;
    const { db } = this.root;
    const cutout = db.pcb_cutout.get(this.pcb_cutout_id);
    if (!cutout) return;
    if (cutout.shape === "rect" || cutout.shape === "circle") {
      db.pcb_cutout.update(this.pcb_cutout_id, {
        ...cutout,
        center: newCenter
      });
    } else if (cutout.shape === "polygon") {
      const oldCenter = this._getPcbCircuitJsonBounds().center;
      const dx = newCenter.x - oldCenter.x;
      const dy = newCenter.y - oldCenter.y;
      const newPoints = cutout.points.map((p) => ({
        x: p.x + dx,
        y: p.y + dy
      }));
      db.pcb_cutout.update(this.pcb_cutout_id, {
        ...cutout,
        points: newPoints
      });
    }
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (!this.pcb_cutout_id) return;
    const { db } = this.root;
    const cutout = db.pcb_cutout.get(this.pcb_cutout_id);
    if (!cutout) return;
    if (cutout.shape === "rect" || cutout.shape === "circle") {
      db.pcb_cutout.update(this.pcb_cutout_id, {
        center: { x: cutout.center.x + deltaX, y: cutout.center.y + deltaY }
      });
    } else if (cutout.shape === "polygon") {
      db.pcb_cutout.update(this.pcb_cutout_id, {
        points: cutout.points.map((p) => ({
          x: p.x + deltaX,
          y: p.y + deltaY
        }))
      });
    }
  }
};

// lib/components/primitive-components/FabricationNotePath.ts
import { fabricationNotePathProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint6 } from "transformation-matrix";
var FabricationNotePath = class extends PrimitiveComponent2 {
  fabrication_note_path_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "FabricationNotePath",
      zodProps: fabricationNotePathProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const subcircuit = this.getSubcircuit();
    const { _parsedProps: props } = this;
    const layer = props.layer ?? "top";
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for SilkscreenPath. Must be "top" or "bottom".`
      );
    }
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const fabrication_note_path = db.pcb_fabrication_note_path.insert({
      pcb_component_id,
      layer,
      color: props.color,
      route: props.route.map((p) => {
        const transformedPosition = applyToPoint6(transform, {
          x: p.x,
          y: p.y
        });
        return {
          ...p,
          x: transformedPosition.x,
          y: transformedPosition.y
        };
      }),
      stroke_width: props.strokeWidth ?? 0.1,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0
    });
    this.fabrication_note_path_id = fabrication_note_path.pcb_fabrication_note_path_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (props.route.length === 0) return { width: 0, height: 0 };
    const xs = props.route.map(
      (point6) => typeof point6.x === "string" ? parseFloat(point6.x) : point6.x
    );
    const ys = props.route.map(
      (point6) => typeof point6.y === "string" ? parseFloat(point6.y) : point6.y
    );
    const minX = Math.min(...xs);
    const maxX = Math.max(...xs);
    const minY = Math.min(...ys);
    const maxY = Math.max(...ys);
    return { width: maxX - minX, height: maxY - minY };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.fabrication_note_path_id) return;
    const path = db.pcb_fabrication_note_path.get(this.fabrication_note_path_id);
    if (path) {
      db.pcb_fabrication_note_path.update(this.fabrication_note_path_id, {
        route: path.route.map((p) => ({
          ...p,
          x: p.x + deltaX,
          y: p.y + deltaY
        }))
      });
    }
  }
};

// lib/components/primitive-components/FabricationNoteRect.ts
import { fabricationNoteRectProps } from "@tscircuit/props";
import "transformation-matrix";
var FabricationNoteRect = class extends PrimitiveComponent2 {
  fabrication_note_rect_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "FabricationNoteRect",
      zodProps: fabricationNoteRectProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for FabricationNoteRect. Must be "top" or "bottom".`
      );
    }
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const subcircuit = this.getSubcircuit();
    const hasStroke = props.hasStroke ?? (props.strokeWidth !== void 0 && props.strokeWidth !== null);
    const fabrication_note_rect = db.pcb_fabrication_note_rect.insert({
      pcb_component_id,
      layer,
      color: props.color,
      center: {
        x: position.x,
        y: position.y
      },
      width: props.width,
      height: props.height,
      stroke_width: props.strokeWidth ?? 1,
      is_filled: props.isFilled ?? false,
      has_stroke: hasStroke,
      is_stroke_dashed: props.isStrokeDashed ?? false,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
      corner_radius: props.cornerRadius ?? void 0
    });
    this.fabrication_note_rect_id = fabrication_note_rect.pcb_fabrication_note_rect_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    return { width: props.width, height: props.height };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.fabrication_note_rect_id) return;
    const rect = db.pcb_fabrication_note_rect.get(this.fabrication_note_rect_id);
    if (rect) {
      db.pcb_fabrication_note_rect.update(this.fabrication_note_rect_id, {
        center: {
          x: rect.center.x + deltaX,
          y: rect.center.y + deltaY
        }
      });
    }
  }
};

// lib/components/primitive-components/FabricationNoteText.ts
import { fabricationNoteTextProps } from "@tscircuit/props";

// lib/utils/pcbSx/resolve-pcb-property.ts
function parseSegment(seg) {
  const bracketIdx = seg.indexOf("[");
  if (bracketIdx === -1) return { tag: seg };
  const tag = seg.slice(0, bracketIdx);
  const attrPart = seg.slice(bracketIdx + 1, seg.lastIndexOf("]"));
  const m = attrPart.match(/^(\w+)(\^=|\$=|=)['"]?(.*?)['"]?$/);
  if (!m) return { tag };
  return { tag, attrName: m[1], attrOp: m[2], attrValue: m[3] };
}
function componentMatchesSegment(component, seg) {
  if (component.lowercaseComponentName !== seg.tag) return false;
  if (!seg.attrName) return true;
  const attrVal = component.props?.[seg.attrName];
  if (attrVal === void 0) return false;
  const valStr = String(attrVal);
  switch (seg.attrOp) {
    case "^=":
      return valStr.startsWith(seg.attrValue);
    case "$=":
      return valStr.endsWith(seg.attrValue);
    case "=":
      return valStr === seg.attrValue;
    default:
      return false;
  }
}
function matchesCompoundSelector(component, segments) {
  if (!componentMatchesSegment(component, segments[segments.length - 1])) {
    return false;
  }
  let segIdx = segments.length - 2;
  let current = component.parent;
  while (segIdx >= 0 && current) {
    if (componentMatchesSegment(current, segments[segIdx])) {
      segIdx--;
    }
    current = current.parent;
  }
  return segIdx < 0;
}
function resolvePcbProperty({
  propertyName,
  resolvedPcbSx,
  pathFromAmpersand,
  component
}) {
  if (!resolvedPcbSx) return void 0;
  let result;
  let bestSpecificity = 0;
  for (const [key, entry] of Object.entries(resolvedPcbSx)) {
    if (!entry || !(propertyName in entry)) continue;
    const selectorBody = key.startsWith("& ") ? key.slice(2) : key;
    if (selectorBody === pathFromAmpersand) {
      const specificity = 1;
      if (specificity > bestSpecificity) {
        bestSpecificity = specificity;
        result = entry[propertyName];
      }
      continue;
    }
    if (!component) continue;
    const segments = selectorBody.split(/\s+/).map(parseSegment);
    if (segments.length < 2) continue;
    if (matchesCompoundSelector(component, segments)) {
      const specificity = segments.length;
      if (specificity > bestSpecificity) {
        bestSpecificity = specificity;
        result = entry[propertyName];
      }
    }
  }
  return result;
}

// lib/components/primitive-components/FabricationNoteText.ts
import { decomposeTSR as decomposeTSR2 } from "transformation-matrix";
var FabricationNoteText = class extends PrimitiveComponent2 {
  pcb_fabrication_note_text_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "FabricationNoteText",
      zodProps: fabricationNoteTextProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const decomposedTransform = decomposeTSR2(
      this._computePcbGlobalTransformBeforeLayout()
    );
    const ccwRotationDegrees = decomposedTransform.rotation.angle * 180 / Math.PI;
    const ccwRotation = (ccwRotationDegrees % 360 + 360) % 360;
    const container = this.getPrimitiveContainer();
    const subcircuit = this.getSubcircuit();
    const resolvedPcbSxVisibility = resolvePcbProperty({
      propertyName: "visibility",
      resolvedPcbSx: this.getResolvedPcbSx(),
      pathFromAmpersand: "fabricationnotetext",
      component: this
    });
    if (resolvedPcbSxVisibility === "hidden") return;
    const pcb_fabrication_note_text = db.pcb_fabrication_note_text.insert({
      anchor_alignment: props.anchorAlignment,
      anchor_position: {
        x: position.x,
        y: position.y
      },
      font: props.font ?? "tscircuit2024",
      font_size: props.fontSize ?? 1,
      layer: "top",
      color: props.color,
      ccw_rotation: ccwRotation,
      text: normalizeTextForCircuitJson(props.text ?? ""),
      pcb_component_id: container.pcb_component_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
    });
    this.pcb_fabrication_note_text_id = pcb_fabrication_note_text.pcb_fabrication_note_text_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const fontSize = typeof props.fontSize === "string" ? parseFloat(props.fontSize) : props.fontSize ?? 1;
    const charWidth = fontSize * 0.6;
    const width = (props.text ?? "").length * charWidth;
    const height = fontSize;
    return { width, height };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (this.pcb_fabrication_note_text_id) {
      const text = db.pcb_fabrication_note_text.get(
        this.pcb_fabrication_note_text_id
      );
      if (text) {
        db.pcb_fabrication_note_text.update(this.pcb_fabrication_note_text_id, {
          anchor_position: {
            x: text.anchor_position.x + deltaX,
            y: text.anchor_position.y + deltaY
          }
        });
      }
    }
  }
};

// lib/components/primitive-components/Hole.ts
import { holeProps } from "@tscircuit/props";
import "circuit-json";
var Hole = class extends PrimitiveComponent2 {
  pcb_hole_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "Hole",
      zodProps: holeProps
    };
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const isPill = props.shape === "pill";
    const isOval = props.shape === "oval";
    const isRect = props.shape === "rect";
    if (isPill || isOval) {
      return {
        width: props.width,
        height: props.height
      };
    } else if (isRect) {
      return {
        width: props.width,
        height: props.height
      };
    } else {
      return {
        width: props.diameter,
        height: props.diameter
      };
    }
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const subcircuit = this.getSubcircuit();
    const position = this._getGlobalPcbPositionBeforeLayout();
    const soldermaskMargin = props.solderMaskMargin;
    const isCoveredWithSolderMask = props.coveredWithSolderMask ?? false;
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    this.emitSolderMaskMarginWarning(isCoveredWithSolderMask, soldermaskMargin);
    if (props.shape === "pill") {
      if (props.pcbRotation && props.pcbRotation !== 0) {
        const inserted_hole = db.pcb_hole.insert({
          pcb_component_id,
          type: "pcb_hole",
          hole_shape: "rotated_pill",
          hole_width: props.width,
          hole_height: props.height,
          x: position.x,
          y: position.y,
          ccw_rotation: props.pcbRotation,
          soldermask_margin: soldermaskMargin,
          is_covered_with_solder_mask: isCoveredWithSolderMask,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: subcircuit?.getGroup()?.pcb_group_id ?? void 0
        });
        this.pcb_hole_id = inserted_hole.pcb_hole_id;
      } else {
        const inserted_hole = db.pcb_hole.insert({
          pcb_component_id,
          type: "pcb_hole",
          hole_shape: "pill",
          hole_width: props.width,
          hole_height: props.height,
          x: position.x,
          y: position.y,
          soldermask_margin: soldermaskMargin,
          is_covered_with_solder_mask: isCoveredWithSolderMask,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: subcircuit?.getGroup()?.pcb_group_id ?? void 0
        });
        this.pcb_hole_id = inserted_hole.pcb_hole_id;
      }
    } else if (props.shape === "oval") {
      const inserted_hole = db.pcb_hole.insert({
        pcb_component_id,
        type: "pcb_hole",
        hole_shape: "oval",
        hole_width: props.width,
        hole_height: props.height,
        x: position.x,
        y: position.y,
        soldermask_margin: soldermaskMargin,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: subcircuit?.getGroup()?.pcb_group_id ?? void 0
      });
      this.pcb_hole_id = inserted_hole.pcb_hole_id;
    } else if (props.shape === "rect") {
      const inserted_hole = db.pcb_hole.insert({
        pcb_component_id,
        type: "pcb_hole",
        hole_shape: "rect",
        hole_width: props.width,
        hole_height: props.height,
        x: position.x,
        y: position.y,
        soldermask_margin: soldermaskMargin,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: subcircuit?.getGroup()?.pcb_group_id ?? void 0
      });
      this.pcb_hole_id = inserted_hole.pcb_hole_id;
    } else {
      const inserted_hole = db.pcb_hole.insert({
        pcb_component_id,
        type: "pcb_hole",
        hole_shape: "circle",
        hole_diameter: props.diameter,
        x: position.x,
        y: position.y,
        soldermask_margin: soldermaskMargin,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: subcircuit?.getGroup()?.pcb_group_id ?? void 0
      });
      this.pcb_hole_id = inserted_hole.pcb_hole_id;
    }
  }
  _getPcbCircuitJsonBounds() {
    const { db } = this.root;
    const hole = db.pcb_hole.get(this.pcb_hole_id);
    const size = this.getPcbSize();
    return {
      center: { x: hole.x, y: hole.y },
      bounds: {
        left: hole.x - size.width / 2,
        top: hole.y - size.height / 2,
        right: hole.x + size.width / 2,
        bottom: hole.y + size.height / 2
      },
      width: size.width,
      height: size.height
    };
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    db.pcb_hole.update(this.pcb_hole_id, {
      x: newCenter.x,
      y: newCenter.y
    });
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_hole_id) return;
    const hole = db.pcb_hole.get(this.pcb_hole_id);
    if (hole) {
      db.pcb_hole.update(this.pcb_hole_id, {
        x: hole.x + deltaX,
        y: hole.y + deltaY
      });
    }
  }
};

// lib/components/primitive-components/Keepout.ts
import { pcbKeepoutProps } from "@tscircuit/props";
import { decomposeTSR as decomposeTSR3 } from "transformation-matrix";
var Keepout = class extends PrimitiveComponent2 {
  pcb_keepout_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "Keepout",
      zodProps: pcbKeepoutProps
    };
  }
  getExcludedPcbComponentIds() {
    const excludedPcbComponentIds = this._parsedProps.excludeRefs?.flatMap(
      (selector) => this.getSubcircuit().selectAll(selector).map((component) => component.pcb_component_id).filter((id) => id !== null)
    ) ?? [];
    return Array.from(new Set(excludedPcbComponentIds));
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const subcircuit = this.getSubcircuit();
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const decomposedMat = decomposeTSR3(
      this._computePcbGlobalTransformBeforeLayout()
    );
    const isRotated90 = Math.abs(decomposedMat.rotation.angle * (180 / Math.PI) - 90) % 180 < 0.01;
    let layers = props.layers;
    if (!layers && props.layer) {
      layers = [props.layer];
    }
    if (!layers) {
      layers = ["top"];
    }
    const excludedPcbComponentIds = this.getExcludedPcbComponentIds();
    const pcbKeepoutExclusionProps = excludedPcbComponentIds.length > 0 ? { excluded_pcb_component_ids: excludedPcbComponentIds } : {};
    let pcb_keepout = null;
    if (props.shape === "circle") {
      pcb_keepout = db.pcb_keepout.insert({
        layers,
        shape: "circle",
        ...pcbKeepoutExclusionProps,
        // @ts-ignore: no idea why this is triggering
        radius: props.radius,
        center: {
          x: position.x,
          y: position.y
        },
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: subcircuit?.getGroup()?.pcb_group_id ?? void 0
      });
    } else if (props.shape === "rect") {
      pcb_keepout = db.pcb_keepout.insert({
        layers,
        shape: "rect",
        ...pcbKeepoutExclusionProps,
        ...isRotated90 ? { width: props.height, height: props.width } : { width: props.width, height: props.height },
        // @ts-ignore: no idea why this is triggering
        center: {
          x: position.x,
          y: position.y
        },
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: subcircuit?.getGroup()?.pcb_group_id ?? void 0
      });
    }
    if (pcb_keepout) {
      this.pcb_keepout_id = pcb_keepout.pcb_keepout_id;
    }
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (props.shape === "circle") {
      return { width: props.radius * 2, height: props.radius * 2 };
    }
    if (props.shape === "rect") {
      return { width: props.width, height: props.height };
    }
    return { width: 0, height: 0 };
  }
};

// lib/components/primitive-components/PcbNoteLine.ts
import { pcbNoteLineProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint8 } from "transformation-matrix";
var PcbNoteLine = class extends PrimitiveComponent2 {
  pcb_note_line_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "PcbNoteLine",
      zodProps: pcbNoteLineProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const subcircuit = this.getSubcircuit();
    const group = this.getGroup();
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const start = applyToPoint8(transform, { x: props.x1, y: props.y1 });
    const end = applyToPoint8(transform, { x: props.x2, y: props.y2 });
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id ?? void 0;
    const pcb_note_line = db.pcb_note_line.insert({
      pcb_component_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: group?.pcb_group_id ?? void 0,
      x1: start.x,
      y1: start.y,
      x2: end.x,
      y2: end.y,
      layer: props.layer === "bottom" ? "bottom" : "top",
      stroke_width: props.strokeWidth ?? 0.1,
      color: props.color,
      is_dashed: props.isDashed
    });
    this.pcb_note_line_id = pcb_note_line.pcb_note_line_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    return {
      width: Math.abs(props.x2 - props.x1),
      height: Math.abs(props.y2 - props.y1)
    };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_note_line_id) return;
    const line = db.pcb_note_line.get(this.pcb_note_line_id);
    if (line) {
      db.pcb_note_line.update(this.pcb_note_line_id, {
        x1: line.x1 + deltaX,
        y1: line.y1 + deltaY,
        x2: line.x2 + deltaX,
        y2: line.y2 + deltaY
      });
    }
  }
};

// lib/components/primitive-components/PcbNotePath.ts
import { pcbNotePathProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint9 } from "transformation-matrix";
var PcbNotePath = class extends PrimitiveComponent2 {
  pcb_note_path_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "PcbNotePath",
      zodProps: pcbNotePathProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const subcircuit = this.getSubcircuit();
    const group = this.getGroup();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id ?? void 0;
    const transformedRoute = props.route.map((point6) => {
      const { x, y, ...rest } = point6;
      const numericX = typeof x === "string" ? parseFloat(x) : x;
      const numericY = typeof y === "string" ? parseFloat(y) : y;
      const transformed = applyToPoint9(transform, { x: numericX, y: numericY });
      return { ...rest, x: transformed.x, y: transformed.y };
    });
    const pcb_note_path = db.pcb_note_path.insert({
      pcb_component_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: group?.pcb_group_id ?? void 0,
      route: transformedRoute,
      layer: props.layer === "bottom" ? "bottom" : "top",
      stroke_width: props.strokeWidth ?? 0.1,
      color: props.color
    });
    this.pcb_note_path_id = pcb_note_path.pcb_note_path_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (props.route.length === 0) return { width: 0, height: 0 };
    const xs = props.route.map(
      (point6) => typeof point6.x === "string" ? parseFloat(point6.x) : point6.x
    );
    const ys = props.route.map(
      (point6) => typeof point6.y === "string" ? parseFloat(point6.y) : point6.y
    );
    const minX = Math.min(...xs);
    const maxX = Math.max(...xs);
    const minY = Math.min(...ys);
    const maxY = Math.max(...ys);
    return { width: maxX - minX, height: maxY - minY };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_note_path_id) return;
    const path = db.pcb_note_path.get(this.pcb_note_path_id);
    if (path) {
      db.pcb_note_path.update(this.pcb_note_path_id, {
        route: path.route.map((p) => ({
          ...p,
          x: p.x + deltaX,
          y: p.y + deltaY
        }))
      });
    }
  }
};

// lib/components/primitive-components/PcbNoteRect.ts
import { pcbNoteRectProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint10 } from "transformation-matrix";
var PcbNoteRect = class extends PrimitiveComponent2 {
  pcb_note_rect_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "PcbNoteRect",
      zodProps: pcbNoteRectProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const center = applyToPoint10(transform, { x: 0, y: 0 });
    const subcircuit = this.getSubcircuit();
    const group = this.getGroup();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id ?? void 0;
    const pcb_note_rect = db.pcb_note_rect.insert({
      pcb_component_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: group?.pcb_group_id ?? void 0,
      center,
      width: props.width,
      height: props.height,
      layer: props.layer === "bottom" ? "bottom" : "top",
      stroke_width: props.strokeWidth ?? 0.1,
      is_filled: props.isFilled ?? false,
      has_stroke: props.hasStroke ?? true,
      is_stroke_dashed: props.isStrokeDashed ?? false,
      color: props.color,
      corner_radius: props.cornerRadius ?? void 0
    });
    this.pcb_note_rect_id = pcb_note_rect.pcb_note_rect_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const width = typeof props.width === "string" ? parseFloat(props.width) : props.width;
    const height = typeof props.height === "string" ? parseFloat(props.height) : props.height;
    return { width, height };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_note_rect_id) return;
    const rect = db.pcb_note_rect.get(this.pcb_note_rect_id);
    if (rect) {
      db.pcb_note_rect.update(this.pcb_note_rect_id, {
        center: {
          x: rect.center.x + deltaX,
          y: rect.center.y + deltaY
        }
      });
    }
  }
};

// lib/components/primitive-components/PcbNoteText.ts
import { pcbNoteTextProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint11 } from "transformation-matrix";
var PcbNoteText = class extends PrimitiveComponent2 {
  pcb_note_text_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "PcbNoteText",
      zodProps: pcbNoteTextProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const anchorPosition = applyToPoint11(transform, { x: 0, y: 0 });
    const subcircuit = this.getSubcircuit();
    const group = this.getGroup();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id ?? void 0;
    const pcb_note_text = db.pcb_note_text.insert({
      pcb_component_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: group?.pcb_group_id ?? void 0,
      font: props.font ?? "tscircuit2024",
      font_size: props.fontSize ?? 1,
      text: normalizeTextForCircuitJson(props.text),
      anchor_position: anchorPosition,
      anchor_alignment: props.anchorAlignment ?? "center",
      layer: props.layer === "bottom" ? "bottom" : "top",
      color: props.color
    });
    this.pcb_note_text_id = pcb_note_text.pcb_note_text_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const fontSize = typeof props.fontSize === "string" ? parseFloat(props.fontSize) : props.fontSize ?? 1;
    const charWidth = fontSize * 0.6;
    const width = props.text.length * charWidth;
    const height = fontSize;
    return { width, height };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_note_text_id) return;
    const text = db.pcb_note_text.get(this.pcb_note_text_id);
    if (text) {
      db.pcb_note_text.update(this.pcb_note_text_id, {
        anchor_position: {
          x: text.anchor_position.x + deltaX,
          y: text.anchor_position.y + deltaY
        }
      });
    }
  }
};

// lib/components/primitive-components/PcbTrace.ts
import { z as z5 } from "zod";
import { pcb_trace_route_point } from "circuit-json";
import { applyToPoint as applyToPoint12 } from "transformation-matrix";
var pcbTraceProps = z5.object({
  route: z5.array(pcb_trace_route_point),
  // If this primitive PcbTrace needs to be associated with a source_trace_id
  // it can be added as a prop here. For footprints, it's often not needed.
  source_trace_id: z5.string().optional()
});
var PcbTrace = class extends PrimitiveComponent2 {
  pcb_trace_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "PcbTrace",
      zodProps: pcbTraceProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const container = this.getPrimitiveContainer();
    const subcircuit = this.getSubcircuit();
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const parentTransform = this._computePcbGlobalTransformBeforeLayout();
    const transformedRoute = props.route.map((point6) => {
      if (point6.route_type === "wire") {
        const { x, y, ...restOfPoint } = point6;
        const transformedPoint = applyToPoint12(parentTransform, { x, y });
        return {
          ...restOfPoint,
          ...transformedPoint,
          layer: maybeFlipLayer(point6.layer)
        };
      }
      if (point6.route_type === "via") {
        const { x, y, ...restOfPoint } = point6;
        const transformedPoint = applyToPoint12(parentTransform, { x, y });
        return {
          ...restOfPoint,
          ...transformedPoint,
          from_layer: maybeFlipLayer(point6.from_layer),
          to_layer: maybeFlipLayer(point6.to_layer)
        };
      }
      return {
        ...point6,
        start: applyToPoint12(parentTransform, point6.start),
        end: applyToPoint12(parentTransform, point6.end),
        start_layer: maybeFlipLayer(point6.start_layer),
        end_layer: maybeFlipLayer(point6.end_layer)
      };
    });
    const pcb_trace = db.pcb_trace.insert({
      pcb_component_id: container.pcb_component_id,
      source_trace_id: props.source_trace_id,
      route: transformedRoute,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
    });
    this.pcb_trace_id = pcb_trace.pcb_trace_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (!props.route || props.route.length === 0) {
      return { width: 0, height: 0 };
    }
    let minX = Infinity, maxX = -Infinity, minY = Infinity, maxY = -Infinity;
    for (const point6 of props.route) {
      if (point6.route_type === "through_pad") {
        minX = Math.min(minX, point6.start.x, point6.end.x);
        maxX = Math.max(maxX, point6.start.x, point6.end.x);
        minY = Math.min(minY, point6.start.y, point6.end.y);
        maxY = Math.max(maxY, point6.start.y, point6.end.y);
      } else {
        minX = Math.min(minX, point6.x);
        maxX = Math.max(maxX, point6.x);
        minY = Math.min(minY, point6.y);
        maxY = Math.max(maxY, point6.y);
      }
      if (point6.route_type === "wire") {
        minX = Math.min(minX, point6.x - point6.width / 2);
        maxX = Math.max(maxX, point6.x + point6.width / 2);
        minY = Math.min(minY, point6.y - point6.width / 2);
        maxY = Math.max(maxY, point6.y + point6.width / 2);
      } else if (point6.route_type === "through_pad") {
        minX = Math.min(
          minX,
          point6.start.x - point6.width / 2,
          point6.end.x - point6.width / 2
        );
        maxX = Math.max(
          maxX,
          point6.start.x + point6.width / 2,
          point6.end.x + point6.width / 2
        );
        minY = Math.min(
          minY,
          point6.start.y - point6.width / 2,
          point6.end.y - point6.width / 2
        );
        maxY = Math.max(
          maxY,
          point6.start.y + point6.width / 2,
          point6.end.y + point6.width / 2
        );
      }
    }
    if (minX === Infinity || maxX === -Infinity || minY === Infinity || maxY === -Infinity) {
      return { width: 0, height: 0 };
    }
    return {
      width: maxX - minX,
      height: maxY - minY
    };
  }
};

// lib/components/primitive-components/PcbVia.ts
import { viaProps } from "@tscircuit/props";
import {
  layer_ref
} from "circuit-json";
import { z as z6 } from "zod";
var pcbViaProps = viaProps.extend({
  layers: z6.array(layer_ref).optional(),
  isTented: z6.boolean().optional()
}).partial({
  fromLayer: true,
  toLayer: true
});
var PcbVia = class extends PrimitiveComponent2 {
  pcb_via_id = null;
  // pcb_trace_id copied from imported circuit-json; not a public JSX prop.
  _importedPcbTraceId;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "PcbVia",
      zodProps: pcbViaProps
    };
  }
  _getLayers() {
    const { layers, fromLayer = "top", toLayer = "bottom" } = this._parsedProps;
    if (layers && layers.length > 0) return layers;
    const layerCount = this.parent ? this.getSubcircuit()._getSubcircuitLayerCount() : 2;
    return getViaSpanLayers({
      fromLayer,
      toLayer,
      layerCount
    });
  }
  getAvailablePcbLayers() {
    return this._getLayers();
  }
  getPcbSize() {
    const outerDiameter = this._parsedProps.outerDiameter ?? 0.6;
    return { width: outerDiameter, height: outerDiameter };
  }
  _getPcbCircuitJsonBounds() {
    const { db } = this.root;
    const via = db.pcb_via.get(this.pcb_via_id);
    const size = this.getPcbSize();
    return {
      center: { x: via.x, y: via.y },
      bounds: {
        left: via.x - size.width / 2,
        top: via.y - size.height / 2,
        right: via.x + size.width / 2,
        bottom: via.y + size.height / 2
      },
      width: size.width,
      height: size.height
    };
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    db.pcb_via.update(this.pcb_via_id, {
      x: newCenter.x,
      y: newCenter.y
    });
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { holeDiameter, outerDiameter, netIsAssignable, isTented } = this._parsedProps;
    const subcircuit = this.getSubcircuit();
    const position = this._getGlobalPcbPositionBeforeLayout();
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layers = this._getLayers().map(
      (layer) => maybeFlipLayer(layer)
    );
    const fromLayer = this._parsedProps.fromLayer ? maybeFlipLayer(this._parsedProps.fromLayer) : layers[0];
    const toLayer = this._parsedProps.toLayer ? maybeFlipLayer(this._parsedProps.toLayer) : layers[layers.length - 1] ?? fromLayer;
    const pcbVia = db.pcb_via.insert({
      x: position.x,
      y: position.y,
      ...holeDiameter !== void 0 ? { hole_diameter: holeDiameter } : {},
      ...outerDiameter !== void 0 ? { outer_diameter: outerDiameter } : {},
      layers,
      from_layer: fromLayer,
      to_layer: toLayer,
      pcb_trace_id: this._importedPcbTraceId,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
      net_is_assignable: netIsAssignable,
      is_tented: isTented
    });
    this.pcb_via_id = pcbVia.pcb_via_id;
  }
};

// lib/components/primitive-components/PlatedHole.ts
import { platedHoleProps } from "@tscircuit/props";
import "circuit-json";
import { decomposeTSR as decomposeTSR4 } from "transformation-matrix";

// lib/components/primitive-components/Port/pcbPrimitiveOverlapBeforeRender.ts
function getPcbPrimitiveBoundsBeforeRender(primitive) {
  try {
    const center = primitive._getGlobalPcbPositionBeforeLayout();
    const size = primitive.getPcbSize();
    return {
      left: center.x - size.width / 2,
      right: center.x + size.width / 2,
      top: center.y + size.height / 2,
      bottom: center.y - size.height / 2
    };
  } catch {
    return null;
  }
}
function doPcbPrimitivesOverlapBeforeRender(a, b) {
  const aBounds = getPcbPrimitiveBoundsBeforeRender(a);
  const bBounds = getPcbPrimitiveBoundsBeforeRender(b);
  if (!aBounds || !bBounds) return false;
  return !(aBounds.right < bBounds.left || aBounds.left > bBounds.right || aBounds.bottom > bBounds.top || aBounds.top < bBounds.bottom);
}
function isPcbPrimitiveContainedWithinBeforeRender(inner, outer) {
  const innerBounds = getPcbPrimitiveBoundsBeforeRender(inner);
  const outerBounds = getPcbPrimitiveBoundsBeforeRender(outer);
  if (!innerBounds || !outerBounds) return false;
  return innerBounds.left >= outerBounds.left && innerBounds.right <= outerBounds.right && innerBounds.bottom >= outerBounds.bottom && innerBounds.top <= outerBounds.top;
}
function getConnectedPcbPrimitiveClustersBeforeRender(primitives) {
  const clusters = [];
  const visited = /* @__PURE__ */ new Set();
  for (const primitive of primitives) {
    if (visited.has(primitive)) continue;
    const cluster = [];
    const stack = [primitive];
    visited.add(primitive);
    while (stack.length > 0) {
      const current = stack.pop();
      cluster.push(current);
      for (const candidate of primitives) {
        if (visited.has(candidate)) continue;
        if (!doPcbPrimitivesOverlapBeforeRender(current, candidate)) continue;
        visited.add(candidate);
        stack.push(candidate);
      }
    }
    clusters.push(cluster);
  }
  return clusters;
}

// lib/components/primitive-components/Port/selectPortForPcbPrimitive.ts
var getPinNumberFromHints = (hints) => {
  for (const hint of hints) {
    const normalizedHint = hint.toString();
    if (!/^(pin)?\d+$/.test(normalizedHint)) continue;
    return Number.parseInt(normalizedHint.replace(/^pin/, ""));
  }
  return null;
};
function selectPortForPcbPrimitive(ports, primitive, hints) {
  const pinNumber = getPinNumberFromHints(hints);
  const matchingPorts = ports.filter((port) => port.isMatchingAnyOf(hints));
  if (matchingPorts.length === 0) return null;
  const pinMatchedPorts = pinNumber === null ? matchingPorts : matchingPorts.filter(
    (port) => port._parsedProps.pinNumber === pinNumber || port._primaryPinNumber === pinNumber
  );
  const candidates = pinMatchedPorts.length > 0 ? pinMatchedPorts : matchingPorts;
  const overlappingPort = candidates.find(
    (port) => port.matchedComponents.some(
      (component) => component.isPcbPrimitive && doPcbPrimitivesOverlapBeforeRender(component, primitive)
    )
  );
  if (overlappingPort) return overlappingPort;
  const unmatchedPort = candidates.find(
    (port) => !port.matchedComponents.some((component) => component.isPcbPrimitive)
  );
  return unmatchedPort ?? candidates[0];
}

// lib/components/primitive-components/PlatedHole.ts
var PlatedHole = class extends PrimitiveComponent2 {
  pcb_plated_hole_id = null;
  matchedPort = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "PlatedHole",
      zodProps: platedHoleProps
    };
  }
  getAvailablePcbLayers() {
    return [...this.root?._getBoard()?.allLayers ?? ["top", "bottom"]];
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (props.shape === "circle") {
      return { width: props.outerDiameter, height: props.outerDiameter };
    }
    if (props.shape === "oval" || props.shape === "pill") {
      return { width: props.outerWidth, height: props.outerHeight };
    }
    if (props.shape === "circular_hole_with_rect_pad") {
      return { width: props.rectPadWidth, height: props.rectPadHeight };
    }
    if (props.shape === "pill_hole_with_rect_pad") {
      return { width: props.rectPadWidth, height: props.rectPadHeight };
    }
    if (props.shape === "hole_with_polygon_pad") {
      if (!props.padOutline || props.padOutline.length === 0) {
        throw new Error(
          "padOutline is required for hole_with_polygon_pad shape"
        );
      }
      const xs = props.padOutline.map(
        (p) => typeof p.x === "number" ? p.x : parseFloat(String(p.x))
      );
      const ys = props.padOutline.map(
        (p) => typeof p.y === "number" ? p.y : parseFloat(String(p.y))
      );
      const minX = Math.min(...xs);
      const maxX = Math.max(...xs);
      const minY = Math.min(...ys);
      const maxY = Math.max(...ys);
      return { width: maxX - minX, height: maxY - minY };
    }
    throw new Error(
      `getPcbSize for shape "${props.shape}" not implemented for ${this.componentName}`
    );
  }
  _getPcbCircuitJsonBounds() {
    const { db } = this.root;
    const platedHole = db.pcb_plated_hole.get(this.pcb_plated_hole_id);
    const size = this.getPcbSize();
    return {
      center: { x: platedHole.x, y: platedHole.y },
      bounds: {
        left: platedHole.x - size.width / 2,
        top: platedHole.y + size.height / 2,
        right: platedHole.x + size.width / 2,
        bottom: platedHole.y - size.height / 2
      },
      width: size.width,
      height: size.height
    };
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    db.pcb_plated_hole.update(this.pcb_plated_hole_id, {
      x: newCenter.x,
      y: newCenter.y
    });
    this.matchedPort?._setPositionFromLayout(newCenter);
  }
  doInitialPortMatching() {
    const parentPorts = this.getPrimitiveContainer()?.selectAll(
      "port"
    );
    if (!this._parsedProps.portHints) {
      return;
    }
    const port = selectPortForPcbPrimitive(
      parentPorts,
      this,
      this._parsedProps.portHints
    );
    if (!port) return;
    this.matchedPort = port;
    port.registerMatch(this);
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const subcircuit = this.getSubcircuit();
    const platedHoleLayers = this.getAvailablePcbLayers();
    const soldermaskMargin = props.solderMaskMargin;
    const isCoveredWithSolderMask = props.coveredWithSolderMask ?? false;
    this.emitSolderMaskMarginWarning(isCoveredWithSolderMask, soldermaskMargin);
    const decomposedTransform = decomposeTSR4(
      this._computePcbGlobalTransformBeforeLayout()
    );
    const rotationDegrees = decomposedTransform.rotation.angle * 180 / Math.PI;
    const normalizedRotationDegrees = (rotationDegrees % 360 + 360) % 360;
    const finalRotationDegrees = normalizedRotationDegrees;
    if (props.shape === "circle") {
      const pcb_plated_hole = db.pcb_plated_hole.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        // @ts-ignore - some issue with circuit-json union type
        outer_diameter: props.outerDiameter,
        hole_diameter: props.holeDiameter,
        shape: "circle",
        port_hints: this.getNameAndAliases(),
        x: position.x,
        y: position.y,
        layers: platedHoleLayers,
        soldermask_margin: soldermaskMargin,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
      this.pcb_plated_hole_id = pcb_plated_hole.pcb_plated_hole_id;
      db.pcb_solder_paste.insert({
        layer: "top",
        shape: "circle",
        // @ts-ignore: no idea why this is triggering
        radius: props.outerDiameter / 2,
        x: position.x,
        y: position.y,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
      db.pcb_solder_paste.insert({
        layer: "bottom",
        shape: "circle",
        // @ts-ignore: no idea why this is triggering
        radius: props.outerDiameter / 2,
        x: position.x,
        y: position.y,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
    } else if (props.shape === "pill" && props.rectPad) {
      const pcb_plated_hole = db.pcb_plated_hole.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        outer_width: props.outerWidth,
        outer_height: props.outerHeight,
        hole_width: props.holeWidth,
        hole_height: props.holeHeight,
        shape: "rotated_pill_hole_with_rect_pad",
        type: "pcb_plated_hole",
        port_hints: this.getNameAndAliases(),
        pcb_plated_hole_id: this.pcb_plated_hole_id,
        x: position.x,
        y: position.y,
        layers: platedHoleLayers,
        soldermask_margin: soldermaskMargin,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
        hole_shape: "rotated_pill",
        pad_shape: "rect",
        hole_ccw_rotation: finalRotationDegrees,
        rect_ccw_rotation: finalRotationDegrees,
        rect_pad_width: props.outerWidth,
        rect_pad_height: props.outerHeight,
        hole_offset_x: props.holeOffsetX,
        hole_offset_y: props.holeOffsetY
      });
      this.pcb_plated_hole_id = pcb_plated_hole.pcb_plated_hole_id;
    } else if (props.shape === "pill" || props.shape === "oval") {
      const pcb_plated_hole = db.pcb_plated_hole.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        outer_width: props.outerWidth,
        outer_height: props.outerHeight,
        hole_width: props.holeWidth,
        hole_height: props.holeHeight,
        shape: props.shape,
        port_hints: this.getNameAndAliases(),
        x: position.x,
        y: position.y,
        layers: platedHoleLayers,
        soldermask_margin: soldermaskMargin,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
        ccw_rotation: finalRotationDegrees
        // NOTE: currently PcbPlatedHoleOval erroneously includes both the shape "pill" and "oval"
      });
      this.pcb_plated_hole_id = pcb_plated_hole.pcb_plated_hole_id;
      db.pcb_solder_paste.insert({
        layer: "top",
        shape: props.shape,
        // @ts-ignore: no idea why this is triggering
        width: props.outerWidth,
        height: props.outerHeight,
        x: position.x,
        y: position.y,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
      db.pcb_solder_paste.insert({
        layer: "bottom",
        shape: props.shape,
        // @ts-ignore: no idea why this is triggering
        width: props.outerWidth,
        height: props.outerHeight,
        x: position.x,
        y: position.y,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
    } else if (props.shape === "circular_hole_with_rect_pad") {
      const pcb_plated_hole = db.pcb_plated_hole.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        hole_diameter: props.holeDiameter,
        rect_pad_width: props.rectPadWidth,
        rect_pad_height: props.rectPadHeight,
        shape: "circular_hole_with_rect_pad",
        port_hints: this.getNameAndAliases(),
        x: position.x,
        y: position.y,
        layers: platedHoleLayers,
        soldermask_margin: soldermaskMargin,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
        hole_offset_x: props.holeOffsetX,
        hole_offset_y: props.holeOffsetY,
        rect_border_radius: props.rectBorderRadius ?? 0,
        rect_ccw_rotation: finalRotationDegrees
      });
      this.pcb_plated_hole_id = pcb_plated_hole.pcb_plated_hole_id;
    } else if (props.shape === "pill_hole_with_rect_pad") {
      if (finalRotationDegrees !== 0) {
        const pcb_plated_hole = db.pcb_plated_hole.insert({
          pcb_component_id,
          pcb_port_id: this.matchedPort?.pcb_port_id,
          hole_width: props.holeWidth,
          hole_height: props.holeHeight,
          rect_pad_width: props.rectPadWidth,
          rect_pad_height: props.rectPadHeight,
          hole_offset_x: props.holeOffsetX,
          hole_offset_y: props.holeOffsetY,
          shape: "rotated_pill_hole_with_rect_pad",
          type: "pcb_plated_hole",
          hole_shape: "rotated_pill",
          pad_shape: "rect",
          hole_ccw_rotation: finalRotationDegrees,
          rect_ccw_rotation: finalRotationDegrees,
          rect_border_radius: props.rectBorderRadius,
          port_hints: this.getNameAndAliases(),
          x: position.x,
          y: position.y,
          layers: platedHoleLayers,
          soldermask_margin: soldermaskMargin,
          is_covered_with_solder_mask: isCoveredWithSolderMask,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
        });
        this.pcb_plated_hole_id = pcb_plated_hole.pcb_plated_hole_id;
      } else {
        const pcb_plated_hole = db.pcb_plated_hole.insert({
          pcb_component_id,
          pcb_port_id: this.matchedPort?.pcb_port_id,
          hole_width: props.holeWidth,
          hole_height: props.holeHeight,
          rect_pad_width: props.rectPadWidth,
          rect_pad_height: props.rectPadHeight,
          hole_offset_x: props.holeOffsetX,
          hole_offset_y: props.holeOffsetY,
          shape: "pill_hole_with_rect_pad",
          rect_border_radius: props.rectBorderRadius,
          port_hints: this.getNameAndAliases(),
          x: position.x,
          y: position.y,
          layers: platedHoleLayers,
          soldermask_margin: soldermaskMargin,
          is_covered_with_solder_mask: isCoveredWithSolderMask,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
        });
        this.pcb_plated_hole_id = pcb_plated_hole.pcb_plated_hole_id;
      }
    } else if (props.shape === "hole_with_polygon_pad") {
      const padOutline = (props.padOutline || []).map((point6) => {
        const x = typeof point6.x === "number" ? point6.x : parseFloat(String(point6.x));
        const y = typeof point6.y === "number" ? point6.y : parseFloat(String(point6.y));
        return {
          x,
          y
        };
      });
      const pcb_plated_hole = db.pcb_plated_hole.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        shape: "hole_with_polygon_pad",
        hole_shape: props.holeShape || "circle",
        hole_diameter: props.holeDiameter,
        hole_width: props.holeWidth,
        hole_height: props.holeHeight,
        pad_outline: padOutline,
        hole_offset_x: typeof props.holeOffsetX === "number" ? props.holeOffsetX : parseFloat(String(props.holeOffsetX || 0)),
        hole_offset_y: typeof props.holeOffsetY === "number" ? props.holeOffsetY : parseFloat(String(props.holeOffsetY || 0)),
        port_hints: this.getNameAndAliases(),
        x: position.x,
        y: position.y,
        layers: platedHoleLayers,
        soldermask_margin: soldermaskMargin,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
      this.pcb_plated_hole_id = pcb_plated_hole.pcb_plated_hole_id;
    }
  }
  doInitialPcbPortAttachment() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    db.pcb_plated_hole.update(this.pcb_plated_hole_id, {
      pcb_port_id: this.matchedPort?.pcb_port_id
    });
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_plated_hole_id) return;
    const hole = db.pcb_plated_hole.get(this.pcb_plated_hole_id);
    if (hole) {
      const newCenter = {
        x: hole.x + deltaX,
        y: hole.y + deltaY
      };
      this._setPositionFromLayout(newCenter);
    }
  }
};

// lib/components/primitive-components/SchematicArc.ts
import { schematicArcProps } from "@tscircuit/props";

// lib/utils/constants.ts
var stringProxy = new Proxy(
  {},
  {
    get: (target, prop) => prop
  }
);
var FTYPE = stringProxy;
var SCHEMATIC_COMPONENT_OUTLINE_COLOR = "rgba(132, 0, 0)";
var SCHEMATIC_COMPONENT_OUTLINE_STROKE_WIDTH = 0.12;

// lib/components/primitive-components/SchematicArc.ts
import { applyToPoint as applyToPoint13 } from "transformation-matrix";
var SchematicArc = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  get config() {
    return {
      componentName: "SchematicArc",
      zodProps: schematicArcProps
    };
  }
  schematic_arc_id;
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const globalPos = this._getGlobalSchematicPositionBeforeLayout();
    const schematic_component_id = this.getPrimitiveContainer()?.parent?.schematic_component_id;
    const schematic_symbol_id = this._getSymbolAncestor()?.schematic_symbol_id;
    const schematic_arc = db.schematic_arc.insert({
      schematic_component_id,
      schematic_symbol_id,
      center: {
        x: props.center.x + globalPos.x,
        y: props.center.y + globalPos.y
      },
      radius: props.radius,
      start_angle_degrees: props.startAngleDegrees,
      end_angle_degrees: props.endAngleDegrees,
      direction: props.direction,
      stroke_width: props.strokeWidth ?? SCHEMATIC_COMPONENT_OUTLINE_STROKE_WIDTH,
      color: props.color ?? SCHEMATIC_COMPONENT_OUTLINE_COLOR,
      is_dashed: props.isDashed,
      subcircuit_id: this.getSubcircuit().subcircuit_id ?? void 0,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.schematic_arc_id = schematic_arc.schematic_arc_id;
  }
  doInitialSchematicSymbolResize() {
    if (this.root?.schematicDisabled) return;
    if (!this.schematic_arc_id) return;
    const symbol = this._getSymbolAncestor();
    const transform = symbol?.getUserCoordinateToResizedSymbolTransform();
    if (!transform) return;
    const { db } = this.root;
    const arc = db.schematic_arc.get(this.schematic_arc_id);
    if (!arc) return;
    const newCenter = applyToPoint13(transform, arc.center);
    const edgePoint = applyToPoint13(transform, {
      x: arc.center.x + arc.radius,
      y: arc.center.y
    });
    const newRadius = Math.abs(edgePoint.x - newCenter.x);
    db.schematic_arc.update(this.schematic_arc_id, {
      center: { x: newCenter.x, y: newCenter.y },
      radius: newRadius
    });
  }
};

// lib/components/primitive-components/SchematicCircle.ts
import { schematicCircleProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint14 } from "transformation-matrix";
var SchematicCircle = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  get config() {
    return {
      componentName: "SchematicCircle",
      zodProps: schematicCircleProps
    };
  }
  schematic_circle_id;
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const globalPos = this._getGlobalSchematicPositionBeforeLayout();
    const schematic_component_id = this.getPrimitiveContainer()?.parent?.schematic_component_id;
    const schematic_symbol_id = this._getSymbolAncestor()?.schematic_symbol_id;
    const schematic_circle = db.schematic_circle.insert({
      schematic_component_id,
      schematic_symbol_id,
      center: {
        x: props.center.x + globalPos.x,
        y: props.center.y + globalPos.y
      },
      radius: props.radius,
      stroke_width: props.strokeWidth ?? SCHEMATIC_COMPONENT_OUTLINE_STROKE_WIDTH,
      color: props.color ?? SCHEMATIC_COMPONENT_OUTLINE_COLOR,
      is_filled: props.isFilled,
      fill_color: props.fillColor,
      is_dashed: props.isDashed,
      subcircuit_id: this.getSubcircuit().subcircuit_id ?? void 0,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.schematic_circle_id = schematic_circle.schematic_circle_id;
  }
  doInitialSchematicSymbolResize() {
    if (this.root?.schematicDisabled) return;
    if (!this.schematic_circle_id) return;
    const symbol = this._getSymbolAncestor();
    const transform = symbol?.getUserCoordinateToResizedSymbolTransform();
    if (!transform) return;
    const { db } = this.root;
    const circle = db.schematic_circle.get(this.schematic_circle_id);
    if (!circle) return;
    const newCenter = applyToPoint14(transform, circle.center);
    const edgePoint = applyToPoint14(transform, {
      x: circle.center.x + circle.radius,
      y: circle.center.y
    });
    const newRadius = Math.abs(edgePoint.x - newCenter.x);
    db.schematic_circle.update(this.schematic_circle_id, {
      center: { x: newCenter.x, y: newCenter.y },
      radius: newRadius
    });
  }
};

// lib/components/primitive-components/SchematicLine.ts
import { schematicLineProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint15 } from "transformation-matrix";
var SchematicLine = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  get config() {
    return {
      componentName: "SchematicLine",
      zodProps: schematicLineProps
    };
  }
  schematic_line_id;
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const globalPos = this._getGlobalSchematicPositionBeforeLayout();
    const schematic_component_id = this.getPrimitiveContainer()?.parent?.schematic_component_id;
    const schematic_symbol_id = this._getSymbolAncestor()?.schematic_symbol_id;
    const schematic_line = db.schematic_line.insert({
      schematic_component_id,
      schematic_symbol_id,
      x1: props.x1 + globalPos.x,
      y1: props.y1 + globalPos.y,
      x2: props.x2 + globalPos.x,
      y2: props.y2 + globalPos.y,
      stroke_width: props.strokeWidth ?? SCHEMATIC_COMPONENT_OUTLINE_STROKE_WIDTH,
      color: props.color ?? SCHEMATIC_COMPONENT_OUTLINE_COLOR,
      is_dashed: props.isDashed || props.dashLength !== void 0 || props.dashGap !== void 0,
      dash_length: props.dashLength,
      dash_gap: props.dashGap,
      subcircuit_id: this.getSubcircuit().subcircuit_id ?? void 0,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.schematic_line_id = schematic_line.schematic_line_id;
  }
  doInitialSchematicSymbolResize() {
    if (this.root?.schematicDisabled) return;
    if (!this.schematic_line_id) return;
    const symbol = this._getSymbolAncestor();
    const transform = symbol?.getUserCoordinateToResizedSymbolTransform();
    if (!transform) return;
    const { db } = this.root;
    const line = db.schematic_line.get(this.schematic_line_id);
    if (!line) return;
    const p1 = applyToPoint15(transform, { x: line.x1, y: line.y1 });
    const p2 = applyToPoint15(transform, { x: line.x2, y: line.y2 });
    db.schematic_line.update(this.schematic_line_id, {
      x1: p1.x,
      y1: p1.y,
      x2: p2.x,
      y2: p2.y
    });
  }
};

// lib/components/primitive-components/SchematicPath.ts
import { schematicPathProps } from "@tscircuit/props";

// lib/utils/schematic/svgPathToPoints.ts
import SVGPathCommander from "svg-path-commander";
function sampleCurveSegment(segmentPath, samplesPerUnit) {
  const commander = new SVGPathCommander(segmentPath);
  const length7 = commander.getTotalLength();
  const numSamples = Math.max(2, Math.ceil(length7 * samplesPerUnit));
  const points = [];
  for (let i = 1; i <= numSamples; i++) {
    const t = i / numSamples * length7;
    const point6 = commander.getPointAtLength(t);
    points.push({ x: point6.x, y: point6.y });
  }
  return points;
}
function svgPathToPoints(svgPath, samplesPerUnit = 10) {
  const pathCommander = new SVGPathCommander(svgPath);
  pathCommander.toAbsolute();
  const segments = pathCommander.segments;
  const result = [];
  let currentPoints = [];
  let currentX = 0;
  let currentY = 0;
  let subpathStartX = 0;
  let subpathStartY = 0;
  for (const segment2 of segments) {
    const cmd = segment2[0];
    switch (cmd) {
      case "M": {
        if (currentPoints.length > 0) {
          result.push(currentPoints);
        }
        currentX = segment2[1];
        currentY = segment2[2];
        subpathStartX = currentX;
        subpathStartY = currentY;
        currentPoints = [{ x: currentX, y: currentY }];
        break;
      }
      case "L": {
        currentX = segment2[1];
        currentY = segment2[2];
        currentPoints.push({ x: currentX, y: currentY });
        break;
      }
      case "H": {
        currentX = segment2[1];
        currentPoints.push({ x: currentX, y: currentY });
        break;
      }
      case "V": {
        currentY = segment2[1];
        currentPoints.push({ x: currentX, y: currentY });
        break;
      }
      case "Z": {
        if (currentX !== subpathStartX || currentY !== subpathStartY) {
          currentPoints.push({ x: subpathStartX, y: subpathStartY });
        }
        currentX = subpathStartX;
        currentY = subpathStartY;
        break;
      }
      case "C": {
        const endX = segment2[5];
        const endY = segment2[6];
        const segmentPath = `M ${currentX} ${currentY} C ${segment2[1]} ${segment2[2]} ${segment2[3]} ${segment2[4]} ${endX} ${endY}`;
        const sampledPoints = sampleCurveSegment(segmentPath, samplesPerUnit);
        currentPoints.push(...sampledPoints);
        currentX = endX;
        currentY = endY;
        break;
      }
      case "S": {
        const endX = segment2[3];
        const endY = segment2[4];
        const segmentPath = `M ${currentX} ${currentY} S ${segment2[1]} ${segment2[2]} ${endX} ${endY}`;
        const sampledPoints = sampleCurveSegment(segmentPath, samplesPerUnit);
        currentPoints.push(...sampledPoints);
        currentX = endX;
        currentY = endY;
        break;
      }
      case "Q": {
        const endX = segment2[3];
        const endY = segment2[4];
        const segmentPath = `M ${currentX} ${currentY} Q ${segment2[1]} ${segment2[2]} ${endX} ${endY}`;
        const sampledPoints = sampleCurveSegment(segmentPath, samplesPerUnit);
        currentPoints.push(...sampledPoints);
        currentX = endX;
        currentY = endY;
        break;
      }
      case "T": {
        const endX = segment2[1];
        const endY = segment2[2];
        const segmentPath = `M ${currentX} ${currentY} T ${endX} ${endY}`;
        const sampledPoints = sampleCurveSegment(segmentPath, samplesPerUnit);
        currentPoints.push(...sampledPoints);
        currentX = endX;
        currentY = endY;
        break;
      }
      case "A": {
        const endX = segment2[6];
        const endY = segment2[7];
        const segmentPath = `M ${currentX} ${currentY} A ${segment2[1]} ${segment2[2]} ${segment2[3]} ${segment2[4]} ${segment2[5]} ${endX} ${endY}`;
        const sampledPoints = sampleCurveSegment(segmentPath, samplesPerUnit);
        currentPoints.push(...sampledPoints);
        currentX = endX;
        currentY = endY;
        break;
      }
    }
  }
  if (currentPoints.length > 0) {
    result.push(currentPoints);
  }
  return result.map((points) => {
    const deduped = [];
    for (const point6 of points) {
      if (deduped.length === 0 || Math.abs(deduped[deduped.length - 1].x - point6.x) > 1e-9 || Math.abs(deduped[deduped.length - 1].y - point6.y) > 1e-9) {
        deduped.push(point6);
      }
    }
    return deduped;
  });
}

// lib/components/primitive-components/SchematicPath.ts
import { applyToPoint as applyToPoint16 } from "transformation-matrix";
var SchematicPath = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  schematic_path_ids = [];
  get config() {
    return {
      componentName: "SchematicPath",
      zodProps: schematicPathProps
    };
  }
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const globalPos = this._getGlobalSchematicPositionBeforeLayout();
    const schematic_component_id = this.getPrimitiveContainer()?.parent?.schematic_component_id;
    const schematic_symbol_id = this._getSymbolAncestor()?.schematic_symbol_id;
    const subcircuit_id = this.getSubcircuit().subcircuit_id ?? void 0;
    this.schematic_path_ids = [];
    if (props.svgPath) {
      const subpaths = svgPathToPoints(props.svgPath);
      for (const subpathPoints of subpaths) {
        const schematic_path = db.schematic_path.insert({
          schematic_component_id,
          schematic_symbol_id,
          points: subpathPoints.map((point6) => ({
            x: point6.x + globalPos.x,
            y: point6.y + globalPos.y
          })),
          is_filled: props.isFilled,
          is_dashed: props.dashLength !== void 0 || props.dashGap !== void 0,
          fill_color: props.fillColor,
          stroke_color: props.strokeColor,
          stroke_width: props.strokeWidth,
          dash_length: props.dashLength,
          dash_gap: props.dashGap,
          subcircuit_id,
          schematic_sheet_id: this._resolveSchematicSheetId()
        });
        this.schematic_path_ids.push(schematic_path.schematic_path_id);
      }
    } else if (props.points && props.points.length > 0) {
      const schematic_path = db.schematic_path.insert({
        schematic_component_id,
        schematic_symbol_id,
        points: props.points.map((point6) => ({
          x: point6.x + globalPos.x,
          y: point6.y + globalPos.y
        })),
        is_filled: props.isFilled,
        is_dashed: props.dashLength !== void 0 || props.dashGap !== void 0,
        fill_color: props.fillColor,
        stroke_color: props.strokeColor,
        stroke_width: props.strokeWidth,
        dash_length: props.dashLength,
        dash_gap: props.dashGap,
        subcircuit_id,
        schematic_sheet_id: this._resolveSchematicSheetId()
      });
      this.schematic_path_ids.push(schematic_path.schematic_path_id);
    }
  }
  doInitialSchematicSymbolResize() {
    if (this.root?.schematicDisabled) return;
    if (this.schematic_path_ids.length === 0) return;
    const symbol = this._getSymbolAncestor();
    const transform = symbol?.getUserCoordinateToResizedSymbolTransform();
    if (!transform) return;
    const { db } = this.root;
    for (const pathId of this.schematic_path_ids) {
      const path = db.schematic_path.get(pathId);
      if (!path) continue;
      const newPoints = path.points.map((point6) => {
        const transformed = applyToPoint16(transform, point6);
        return { x: transformed.x, y: transformed.y };
      });
      db.schematic_path.update(pathId, {
        points: newPoints
      });
    }
  }
};

// lib/components/primitive-components/SchematicRect.ts
import { schematicRectProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint17 } from "transformation-matrix";
var SchematicRect = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  get config() {
    return {
      componentName: "SchematicRect",
      zodProps: schematicRectProps
    };
  }
  schematic_rect_id;
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const globalPos = this._getGlobalSchematicPositionBeforeLayout();
    const schematic_component_id = this.getPrimitiveContainer()?.parent?.schematic_component_id;
    const schematic_symbol_id = this._getSymbolAncestor()?.schematic_symbol_id;
    const schematic_rect = db.schematic_rect.insert({
      schematic_symbol_id,
      center: {
        x: globalPos.x,
        y: globalPos.y
      },
      width: props.width,
      height: props.height,
      stroke_width: props.strokeWidth ?? SCHEMATIC_COMPONENT_OUTLINE_STROKE_WIDTH,
      color: props.color ?? SCHEMATIC_COMPONENT_OUTLINE_COLOR,
      is_filled: props.isFilled,
      schematic_component_id,
      is_dashed: props.isDashed,
      rotation: props.rotation ?? 0,
      subcircuit_id: this.getSubcircuit().subcircuit_id ?? void 0,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.schematic_rect_id = schematic_rect.schematic_rect_id;
  }
  doInitialSchematicSymbolResize() {
    if (this.root?.schematicDisabled) return;
    if (!this.schematic_rect_id) return;
    const symbol = this._getSymbolAncestor();
    const transform = symbol?.getUserCoordinateToResizedSymbolTransform();
    if (!transform) return;
    const { db } = this.root;
    const rect = db.schematic_rect.get(this.schematic_rect_id);
    if (!rect) return;
    const topLeft = applyToPoint17(transform, {
      x: rect.center.x - rect.width / 2,
      y: rect.center.y + rect.height / 2
    });
    const bottomRight = applyToPoint17(transform, {
      x: rect.center.x + rect.width / 2,
      y: rect.center.y - rect.height / 2
    });
    const newWidth = Math.abs(bottomRight.x - topLeft.x);
    const newHeight = Math.abs(topLeft.y - bottomRight.y);
    const newCenter = {
      x: (topLeft.x + bottomRight.x) / 2,
      y: (topLeft.y + bottomRight.y) / 2
    };
    db.schematic_rect.update(this.schematic_rect_id, {
      center: newCenter,
      width: newWidth,
      height: newHeight
    });
  }
};

// lib/components/primitive-components/SchematicText.ts
import { schematicTextProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint18 } from "transformation-matrix";
var SchematicText = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  schematic_text_id;
  get config() {
    return {
      componentName: "SchematicText",
      zodProps: schematicTextProps
    };
  }
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const globalPos = this._getGlobalSchematicPositionBeforeLayout();
    const schematic_symbol_id = this._getSymbolAncestor()?.schematic_symbol_id;
    const text = this._resolveText();
    const schematic_text = db.schematic_text.insert({
      schematic_symbol_id,
      anchor: props.anchor ?? "center",
      text: normalizeTextForCircuitJson(text),
      font_size: props.fontSize,
      color: props.color || "#000000",
      position: {
        x: globalPos.x,
        y: globalPos.y
      },
      rotation: props.schRotation ?? 0,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.schematic_text_id = schematic_text.schematic_text_id;
  }
  doInitialSchematicSymbolResize() {
    if (this.root?.schematicDisabled) return;
    if (!this.schematic_text_id) return;
    const symbol = this._getSymbolAncestor();
    const transform = symbol?.getUserCoordinateToResizedSymbolTransform();
    if (!transform) return;
    const { db } = this.root;
    const text = db.schematic_text.get(this.schematic_text_id);
    if (!text) return;
    const newPosition = applyToPoint18(transform, text.position);
    db.schematic_text.update(this.schematic_text_id, {
      position: { x: newPosition.x, y: newPosition.y }
    });
  }
};

// lib/components/primitive-components/SilkscreenCircle.ts
import { silkscreenCircleProps } from "@tscircuit/props";
var SilkscreenCircle = class extends PrimitiveComponent2 {
  pcb_silkscreen_circle_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "SilkscreenCircle",
      zodProps: silkscreenCircleProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for SilkscreenCircle. Must be "top" or "bottom".`
      );
    }
    const subcircuit = this.getSubcircuit();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const pcb_silkscreen_circle = db.pcb_silkscreen_circle.insert({
      pcb_component_id,
      layer,
      center: {
        x: position.x,
        y: position.y
      },
      radius: props.radius,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
      stroke_width: props.strokeWidth ?? 0.1
    });
    this.pcb_silkscreen_circle_id = pcb_silkscreen_circle.pcb_silkscreen_circle_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const diameter = props.radius * 2;
    return { width: diameter, height: diameter };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_silkscreen_circle_id) return;
    const circle = db.pcb_silkscreen_circle.get(this.pcb_silkscreen_circle_id);
    if (circle) {
      db.pcb_silkscreen_circle.update(this.pcb_silkscreen_circle_id, {
        center: {
          x: circle.center.x + deltaX,
          y: circle.center.y + deltaY
        }
      });
    }
  }
};

// lib/components/primitive-components/SilkscreenLine.ts
import { silkscreenLineProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint19 } from "transformation-matrix";
var SilkscreenLine = class extends PrimitiveComponent2 {
  pcb_silkscreen_line_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "SilkscreenLine",
      zodProps: silkscreenLineProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for SilkscreenLine. Must be "top" or "bottom".`
      );
    }
    const subcircuit = this.getSubcircuit();
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const p1 = applyToPoint19(transform, { x: props.x1, y: props.y1 });
    const p2 = applyToPoint19(transform, { x: props.x2, y: props.y2 });
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const pcb_silkscreen_line = db.pcb_silkscreen_line.insert({
      pcb_component_id,
      layer,
      x1: p1.x,
      y1: p1.y,
      x2: p2.x,
      y2: p2.y,
      stroke_width: props.strokeWidth ?? 0.1,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: subcircuit?.getGroup()?.pcb_group_id ?? void 0
    });
    this.pcb_silkscreen_line_id = pcb_silkscreen_line.pcb_silkscreen_line_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const width = Math.abs(props.x2 - props.x1);
    const height = Math.abs(props.y2 - props.y1);
    return { width, height };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_silkscreen_line_id) return;
    const line = db.pcb_silkscreen_line.get(this.pcb_silkscreen_line_id);
    if (line) {
      db.pcb_silkscreen_line.update(this.pcb_silkscreen_line_id, {
        x1: line.x1 + deltaX,
        y1: line.y1 + deltaY,
        x2: line.x2 + deltaX,
        y2: line.y2 + deltaY
      });
    }
  }
};

// lib/components/primitive-components/SilkscreenPath.ts
import { silkscreenPathProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint20 } from "transformation-matrix";
var SilkscreenPath = class extends PrimitiveComponent2 {
  pcb_silkscreen_path_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "SilkscreenPath",
      zodProps: silkscreenPathProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for SilkscreenPath. Must be "top" or "bottom".`
      );
    }
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const subcircuit = this.getSubcircuit();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const pcb_silkscreen_path = db.pcb_silkscreen_path.insert({
      pcb_component_id,
      layer,
      route: props.route.map((p) => {
        const transformedPosition = applyToPoint20(transform, {
          x: p.x,
          y: p.y
        });
        return {
          ...p,
          x: transformedPosition.x,
          y: transformedPosition.y
        };
      }),
      stroke_width: props.strokeWidth ?? 0.1,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
    });
    this.pcb_silkscreen_path_id = pcb_silkscreen_path.pcb_silkscreen_path_id;
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const currentPath = db.pcb_silkscreen_path.get(this.pcb_silkscreen_path_id);
    if (!currentPath) return;
    let currentCenterX = 0;
    let currentCenterY = 0;
    for (const point6 of currentPath.route) {
      currentCenterX += point6.x;
      currentCenterY += point6.y;
    }
    currentCenterX /= currentPath.route.length;
    currentCenterY /= currentPath.route.length;
    const offsetX = newCenter.x - currentCenterX;
    const offsetY = newCenter.y - currentCenterY;
    const newRoute = currentPath.route.map((point6) => ({
      ...point6,
      x: point6.x + offsetX,
      y: point6.y + offsetY
    }));
    db.pcb_silkscreen_path.update(this.pcb_silkscreen_path_id, {
      route: newRoute
    });
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_silkscreen_path_id) return;
    const path = db.pcb_silkscreen_path.get(this.pcb_silkscreen_path_id);
    if (path) {
      db.pcb_silkscreen_path.update(this.pcb_silkscreen_path_id, {
        route: path.route.map((p) => ({
          ...p,
          x: p.x + deltaX,
          y: p.y + deltaY
        }))
      });
    }
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (!props.route || props.route.length === 0) {
      return { width: 0, height: 0 };
    }
    let minX = Infinity, maxX = -Infinity, minY = Infinity, maxY = -Infinity;
    for (const point6 of props.route) {
      minX = Math.min(minX, point6.x);
      maxX = Math.max(maxX, point6.x);
      minY = Math.min(minY, point6.y);
      maxY = Math.max(maxY, point6.y);
    }
    return {
      width: maxX - minX,
      height: maxY - minY
    };
  }
};

// lib/components/primitive-components/SilkscreenRect.ts
import { silkscreenRectProps } from "@tscircuit/props";
import { decomposeTSR as decomposeTSR5 } from "transformation-matrix";
var SilkscreenRect = class extends PrimitiveComponent2 {
  pcb_silkscreen_rect_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "SilkscreenRect",
      zodProps: silkscreenRectProps
    };
  }
  /**
   * Check if the component is rotated 90 or 270 degrees based on global transform.
   * For these rotations, we need to swap width/height instead of using ccw_rotation.
   */
  _isRotated90Degrees() {
    const globalTransform = this._computePcbGlobalTransformBeforeLayout();
    const decomposedTransform = decomposeTSR5(globalTransform);
    const rotationDegrees = decomposedTransform.rotation.angle * 180 / Math.PI;
    const normalizedRotationDegrees = (rotationDegrees % 360 + 360) % 360;
    const rotationTolerance = 0.01;
    return Math.abs(normalizedRotationDegrees - 90) < rotationTolerance || Math.abs(normalizedRotationDegrees - 270) < rotationTolerance;
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for SilkscreenRect. Must be "top" or "bottom".`
      );
    }
    const subcircuit = this.getSubcircuit();
    const position = this._getGlobalPcbPositionBeforeLayout();
    const isRotated90Degrees = this._isRotated90Degrees();
    const finalWidth = isRotated90Degrees ? props.height : props.width;
    const finalHeight = isRotated90Degrees ? props.width : props.height;
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const pcb_silkscreen_rect = db.pcb_silkscreen_rect.insert({
      pcb_component_id,
      layer,
      center: {
        x: position.x,
        y: position.y
      },
      width: finalWidth,
      height: finalHeight,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this?.getGroup()?.pcb_group_id ?? void 0,
      stroke_width: props.strokeWidth ?? 0.1,
      is_filled: props.filled ?? false,
      corner_radius: props.cornerRadius ?? void 0
    });
    this.pcb_silkscreen_rect_id = pcb_silkscreen_rect.pcb_silkscreen_rect_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const isRotated90Degrees = this._isRotated90Degrees();
    if (isRotated90Degrees) {
      return { width: props.height, height: props.width };
    }
    return { width: props.width, height: props.height };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_silkscreen_rect_id) return;
    const rect = db.pcb_silkscreen_rect.get(this.pcb_silkscreen_rect_id);
    if (rect) {
      db.pcb_silkscreen_rect.update(this.pcb_silkscreen_rect_id, {
        center: {
          x: rect.center.x + deltaX,
          y: rect.center.y + deltaY
        }
      });
    }
  }
};

// lib/components/primitive-components/SilkscreenText.ts
import { silkscreenTextProps } from "@tscircuit/props";
import {
  applyToPoint as applyToPoint21,
  compose as compose3,
  decomposeTSR as decomposeTSR6,
  flipY as flipY3,
  identity as identity5
} from "transformation-matrix";
var SilkscreenText = class extends PrimitiveComponent2 {
  pcb_silkscreen_text_ids = [];
  isPcbPrimitive = true;
  _footprinterFontSize;
  get config() {
    return {
      componentName: "SilkscreenText",
      zodProps: silkscreenTextProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const container = this.getPrimitiveContainer();
    const { maybeFlipLayer, isFlipped } = this._getPcbPrimitiveFlippedHelpers();
    const subcircuit = this.getSubcircuit();
    let rotation4 = 0;
    if (props.pcbRotation !== void 0 && props.pcbRotation !== 0) {
      rotation4 = props.pcbRotation;
    } else {
      const globalTransform = this._computePcbGlobalTransformBeforeLayout();
      const decomposedTransform = decomposeTSR6(globalTransform);
      rotation4 = decomposedTransform.rotation.angle * 180 / Math.PI;
    }
    if (isFlipped) {
      rotation4 = (rotation4 + 180) % 360;
    }
    const uniqueLayers = new Set(props.layers);
    if (props.layer) uniqueLayers.add(props.layer);
    const targetLayers = uniqueLayers.size > 0 ? Array.from(uniqueLayers) : ["top"];
    const resolvedPcbSxFontSize = resolvePcbProperty({
      propertyName: "fontSize",
      resolvedPcbSx: this.getResolvedPcbSx(),
      pathFromAmpersand: "silkscreentext",
      component: this
    });
    const resolvedPcbSxPcbX = resolvePcbProperty({
      propertyName: "pcbX",
      resolvedPcbSx: this.getResolvedPcbSx(),
      pathFromAmpersand: "silkscreentext",
      component: this
    });
    const resolvedPcbSxPcbY = resolvePcbProperty({
      propertyName: "pcbY",
      resolvedPcbSx: this.getResolvedPcbSx(),
      pathFromAmpersand: "silkscreentext",
      component: this
    });
    const resolvedPcbSxVisibility = resolvePcbProperty({
      propertyName: "visibility",
      resolvedPcbSx: this.getResolvedPcbSx(),
      pathFromAmpersand: "silkscreentext",
      component: this
    });
    if (resolvedPcbSxVisibility === "hidden") return;
    const fontSize = props.fontSize ?? resolvedPcbSxFontSize ?? this.getInheritedProperty("pcbStyle")?.silkscreenFontSize ?? this._footprinterFontSize ?? 1;
    const hasResolvedPcbSxPosition = resolvedPcbSxPcbX !== void 0 || resolvedPcbSxPcbY !== void 0;
    const position = hasResolvedPcbSxPosition && this._footprinterFontSize !== void 0 ? applyToPoint21(
      compose3(
        this.parent?._computePcbGlobalTransformBeforeLayout() ?? identity5(),
        isFlipped ? flipY3() : identity5()
      ),
      {
        x: this.resolvePcbCoordinate({
          rawValue: resolvedPcbSxPcbX ?? props.pcbX ?? 0,
          axis: "pcbX"
        }),
        y: this.resolvePcbCoordinate({
          rawValue: resolvedPcbSxPcbY ?? props.pcbY ?? 0,
          axis: "pcbY"
        })
      }
    ) : this._getGlobalPcbPositionBeforeLayout();
    const uniformPadding = props.knockoutPadding ?? 0;
    const hasKnockoutPadding = props.knockoutPadding !== void 0 || props.knockoutPaddingLeft !== void 0 || props.knockoutPaddingRight !== void 0 || props.knockoutPaddingTop !== void 0 || props.knockoutPaddingBottom !== void 0;
    const knockoutPadding = hasKnockoutPadding ? {
      left: props.knockoutPaddingLeft ?? uniformPadding,
      right: props.knockoutPaddingRight ?? uniformPadding,
      top: props.knockoutPaddingTop ?? uniformPadding,
      bottom: props.knockoutPaddingBottom ?? uniformPadding
    } : void 0;
    const text = this._resolveText();
    for (const layer of targetLayers) {
      const pcb_silkscreen_text = db.pcb_silkscreen_text.insert({
        anchor_alignment: props.anchorAlignment,
        anchor_position: {
          x: position.x,
          y: position.y
        },
        font: props.font ?? "tscircuit2024",
        font_size: fontSize,
        layer: maybeFlipLayer(layer),
        text: normalizeTextForCircuitJson(text),
        ccw_rotation: rotation4,
        pcb_component_id: container.pcb_component_id,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
        is_knockout: props.isKnockout,
        knockout_padding: knockoutPadding
      });
      this.pcb_silkscreen_text_ids.push(
        pcb_silkscreen_text.pcb_silkscreen_text_id
      );
    }
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const resolvedPcbSxFontSize = resolvePcbProperty({
      propertyName: "fontSize",
      resolvedPcbSx: this.getResolvedPcbSx(),
      pathFromAmpersand: "silkscreentext",
      component: this
    });
    const fontSize = props.fontSize ?? resolvedPcbSxFontSize ?? this.getInheritedProperty("pcbStyle")?.silkscreenFontSize ?? this._footprinterFontSize ?? 1;
    const text = this._resolveText();
    const textWidth = text.length * fontSize;
    const textHeight = fontSize;
    return { width: textWidth * fontSize, height: textHeight * fontSize };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    for (const id of this.pcb_silkscreen_text_ids) {
      const text = db.pcb_silkscreen_text.get(id);
      if (text) {
        db.pcb_silkscreen_text.update(id, {
          anchor_position: {
            x: text.anchor_position.x + deltaX,
            y: text.anchor_position.y + deltaY
          }
        });
      }
    }
  }
};

// lib/components/primitive-components/SmtPad.ts
import { smtPadProps } from "@tscircuit/props";
import {
  distance as distance5
} from "circuit-json";
import { applyToPoint as applyToPoint22, decomposeTSR as decomposeTSR7 } from "transformation-matrix";

// lib/utils/pcb/get-axis-aligned-size-from-rotated-rect.ts
function getAxisAlignedSizeFromRotatedRect({
  width,
  height,
  ccwRotationDegrees
}) {
  const angleRad = ccwRotationDegrees * Math.PI / 180;
  const cosAngle = Math.cos(angleRad);
  const sinAngle = Math.sin(angleRad);
  const w2 = width / 2;
  const h2 = height / 2;
  const xExtent = Math.abs(w2 * cosAngle) + Math.abs(h2 * sinAngle);
  const yExtent = Math.abs(w2 * sinAngle) + Math.abs(h2 * cosAngle);
  return {
    width: xExtent * 2,
    height: yExtent * 2,
    xExtent,
    yExtent
  };
}

// lib/components/primitive-components/SmtPad.ts
var SmtPad = class extends PrimitiveComponent2 {
  pcb_smtpad_id = null;
  matchedPort = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "SmtPad",
      zodProps: smtPadProps
    };
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    if (props.shape === "circle") {
      return { width: props.radius * 2, height: props.radius * 2 };
    }
    if (props.shape === "rect") {
      return { width: props.width, height: props.height };
    }
    if (props.shape === "rotated_rect") {
      const { width, height } = getAxisAlignedSizeFromRotatedRect({
        width: props.width,
        height: props.height,
        ccwRotationDegrees: props.ccwRotation ?? 0
      });
      return { width, height };
    }
    if (props.shape === "polygon") {
      const points = props.points;
      const xs = points.map((p) => p.x);
      const ys = points.map((p) => p.y);
      const minX = Math.min(...xs);
      const maxX = Math.max(...xs);
      const minY = Math.min(...ys);
      const maxY = Math.max(...ys);
      return { width: maxX - minX, height: maxY - minY };
    }
    if (props.shape === "pill") {
      return { width: props.width, height: props.height };
    }
    if (props.shape === "rotated_pill") {
      const { width, height } = getAxisAlignedSizeFromRotatedRect({
        width: props.width,
        height: props.height,
        ccwRotationDegrees: props.ccwRotation
      });
      return { width, height };
    }
    throw new Error(
      `getPcbSize for shape "${props.shape}" not implemented for ${this.componentName}`
    );
  }
  doInitialPortMatching() {
    const parentPorts = this.getPrimitiveContainer()?.selectAll(
      "port"
    );
    if (!this.props.portHints) {
      return;
    }
    const port = selectPortForPcbPrimitive(
      parentPorts,
      this,
      this.props.portHints
    );
    if (!port) return;
    this.matchedPort = port;
    port.registerMatch(this);
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const isCoveredWithSolderMask = props.coveredWithSolderMask ?? false;
    const shouldCreateSolderPaste = !isCoveredWithSolderMask;
    const soldermaskMargin = props.solderMaskMargin;
    const solderPasteMargin = props.solderPasteMargin;
    const getSolderPasteSize = (padSize, marginScale = 2) => solderPasteMargin !== void 0 ? Math.max(padSize + marginScale * solderPasteMargin, 0) : padSize * 0.7;
    this.emitSolderMaskMarginWarning(isCoveredWithSolderMask, soldermaskMargin);
    const subcircuit = this.getSubcircuit();
    const position = this._getGlobalPcbPositionBeforeLayout();
    const globalTransform = this._computePcbGlobalTransformBeforeLayout();
    const decomposedTransform = decomposeTSR7(
      this._computePcbGlobalTransformBeforeLayout()
    );
    const rotationDegrees = decomposedTransform.rotation.angle * 180 / Math.PI;
    const normalizedRotationDegrees = (rotationDegrees % 360 + 360) % 360;
    const rotationTolerance = 0.01;
    const isAxisAligned = Math.abs(normalizedRotationDegrees) < rotationTolerance || Math.abs(normalizedRotationDegrees - 180) < rotationTolerance || Math.abs(normalizedRotationDegrees - 360) < rotationTolerance;
    const isRotated90Degrees = Math.abs(normalizedRotationDegrees - 90) < rotationTolerance || Math.abs(normalizedRotationDegrees - 270) < rotationTolerance;
    let finalRotationDegrees = Math.abs(normalizedRotationDegrees - 360) < rotationTolerance ? 0 : normalizedRotationDegrees;
    const transformRotationBeforeFlip = finalRotationDegrees;
    const { maybeFlipLayer: maybeFlipDefaultLayer, isFlipped } = this._getPcbPrimitiveFlippedHelpers();
    const primitiveContainer = this.getPrimitiveContainer();
    const primitiveContainerLayer = primitiveContainer?.props?.layer !== void 0 ? primitiveContainer?._parsedProps.layer : void 0;
    const maybeFlipLayer = (layer) => primitiveContainerLayer ?? maybeFlipDefaultLayer(layer);
    if (isFlipped) {
      finalRotationDegrees = (360 - finalRotationDegrees + 360) % 360;
    }
    const portHints = props.portHints?.map((ph) => ph.toString()) ?? [];
    let pcb_smtpad = null;
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    if (props.shape === "circle") {
      pcb_smtpad = db.pcb_smtpad.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        // port likely isn't matched
        layer: maybeFlipLayer(props.layer ?? "top"),
        shape: "circle",
        radius: props.radius,
        port_hints: portHints,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        soldermask_margin: soldermaskMargin,
        x: position.x,
        y: position.y,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0
      });
      const pasteRadius = getSolderPasteSize(pcb_smtpad.radius, 1);
      if (shouldCreateSolderPaste && pasteRadius > 0)
        db.pcb_solder_paste.insert({
          layer: pcb_smtpad.layer,
          shape: "circle",
          radius: pasteRadius,
          x: pcb_smtpad.x,
          y: pcb_smtpad.y,
          pcb_component_id: pcb_smtpad.pcb_component_id,
          pcb_smtpad_id: pcb_smtpad.pcb_smtpad_id,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
        });
    } else if (props.shape === "rect") {
      const hasRotation = !isAxisAligned && !isRotated90Degrees;
      if (hasRotation) {
        pcb_smtpad = db.pcb_smtpad.insert({
          pcb_component_id,
          pcb_port_id: this.matchedPort?.pcb_port_id,
          layer: maybeFlipLayer(props.layer ?? "top"),
          shape: "rotated_rect",
          width: props.width,
          height: props.height,
          corner_radius: props.cornerRadius ?? void 0,
          x: position.x,
          y: position.y,
          ccw_rotation: finalRotationDegrees,
          port_hints: portHints,
          is_covered_with_solder_mask: isCoveredWithSolderMask,
          soldermask_margin: soldermaskMargin,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
        });
      } else {
        pcb_smtpad = db.pcb_smtpad.insert({
          pcb_component_id,
          pcb_port_id: this.matchedPort?.pcb_port_id,
          layer: maybeFlipLayer(props.layer ?? "top"),
          shape: "rect",
          width: isRotated90Degrees ? props.height : props.width,
          height: isRotated90Degrees ? props.width : props.height,
          corner_radius: props.cornerRadius ?? void 0,
          port_hints: portHints,
          is_covered_with_solder_mask: isCoveredWithSolderMask,
          soldermask_margin: soldermaskMargin,
          x: position.x,
          y: position.y,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
        });
      }
      const pasteWidth = getSolderPasteSize(pcb_smtpad.width);
      const pasteHeight = getSolderPasteSize(pcb_smtpad.height);
      if (shouldCreateSolderPaste && pasteWidth > 0 && pasteHeight > 0) {
        if (pcb_smtpad.shape === "rect") {
          db.pcb_solder_paste.insert({
            layer: maybeFlipLayer(props.layer ?? "top"),
            shape: "rect",
            width: pasteWidth,
            height: pasteHeight,
            x: pcb_smtpad.x,
            y: pcb_smtpad.y,
            pcb_component_id: pcb_smtpad.pcb_component_id,
            pcb_smtpad_id: pcb_smtpad.pcb_smtpad_id,
            subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
            pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
          });
        } else if (pcb_smtpad.shape === "rotated_rect") {
          db.pcb_solder_paste.insert({
            layer: maybeFlipLayer(props.layer ?? "top"),
            shape: "rotated_rect",
            width: pasteWidth,
            height: pasteHeight,
            x: pcb_smtpad.x,
            y: pcb_smtpad.y,
            ccw_rotation: pcb_smtpad.ccw_rotation,
            pcb_component_id: pcb_smtpad.pcb_component_id,
            pcb_smtpad_id: pcb_smtpad.pcb_smtpad_id,
            subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
            pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
          });
        }
      }
    } else if (props.shape === "rotated_rect") {
      const baseRotation = props.ccwRotation ?? 0;
      const combinedRotationBeforeFlip = (transformRotationBeforeFlip + baseRotation + 360) % 360;
      const padRotation = isFlipped ? (360 - combinedRotationBeforeFlip + 360) % 360 : combinedRotationBeforeFlip;
      pcb_smtpad = db.pcb_smtpad.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        layer: maybeFlipLayer(props.layer ?? "top"),
        shape: "rotated_rect",
        width: props.width,
        height: props.height,
        corner_radius: props.cornerRadius ?? void 0,
        x: position.x,
        y: position.y,
        ccw_rotation: padRotation,
        port_hints: portHints,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        soldermask_margin: soldermaskMargin,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
      const pasteWidth = getSolderPasteSize(pcb_smtpad.width);
      const pasteHeight = getSolderPasteSize(pcb_smtpad.height);
      if (shouldCreateSolderPaste && pasteWidth > 0 && pasteHeight > 0)
        db.pcb_solder_paste.insert({
          layer: maybeFlipLayer(props.layer ?? "top"),
          shape: "rotated_rect",
          width: pasteWidth,
          height: pasteHeight,
          x: position.x,
          y: position.y,
          ccw_rotation: padRotation,
          pcb_component_id,
          pcb_smtpad_id: pcb_smtpad.pcb_smtpad_id,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
        });
    } else if (props.shape === "polygon") {
      const transformedPoints = props.points.map((point6) => {
        const transformed = applyToPoint22(globalTransform, {
          x: distance5.parse(point6.x),
          y: distance5.parse(point6.y)
        });
        return {
          x: transformed.x,
          y: transformed.y
        };
      });
      pcb_smtpad = db.pcb_smtpad.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        // port likely isn't matched
        layer: maybeFlipLayer(props.layer ?? "top"),
        shape: "polygon",
        points: transformedPoints,
        port_hints: portHints,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        soldermask_margin: soldermaskMargin,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
    } else if (props.shape === "rotated_pill") {
      const combinedRotationBeforeFlip = (transformRotationBeforeFlip + props.ccwRotation + 360) % 360;
      const padRotation = isFlipped ? (360 - combinedRotationBeforeFlip + 360) % 360 : combinedRotationBeforeFlip;
      pcb_smtpad = db.pcb_smtpad.insert({
        pcb_component_id,
        pcb_port_id: this.matchedPort?.pcb_port_id,
        layer: maybeFlipLayer(props.layer ?? "top"),
        shape: "rotated_pill",
        x: position.x,
        y: position.y,
        radius: props.radius,
        height: props.height,
        width: props.width,
        ccw_rotation: padRotation,
        port_hints: portHints,
        is_covered_with_solder_mask: isCoveredWithSolderMask,
        soldermask_margin: soldermaskMargin,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
    } else if (props.shape === "pill") {
      if (finalRotationDegrees !== 0) {
        pcb_smtpad = db.pcb_smtpad.insert({
          pcb_component_id,
          pcb_port_id: this.matchedPort?.pcb_port_id,
          // port likely isn't matched
          layer: maybeFlipLayer(props.layer ?? "top"),
          shape: "rotated_pill",
          x: position.x,
          y: position.y,
          radius: props.radius,
          height: props.height,
          width: props.width,
          ccw_rotation: finalRotationDegrees,
          port_hints: portHints,
          is_covered_with_solder_mask: isCoveredWithSolderMask,
          soldermask_margin: soldermaskMargin,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
        });
      } else {
        pcb_smtpad = db.pcb_smtpad.insert({
          pcb_component_id,
          pcb_port_id: this.matchedPort?.pcb_port_id,
          // port likely isn't matched
          layer: maybeFlipLayer(props.layer ?? "top"),
          shape: "pill",
          x: position.x,
          y: position.y,
          radius: props.radius,
          height: props.height,
          width: props.width,
          port_hints: portHints,
          is_covered_with_solder_mask: isCoveredWithSolderMask,
          soldermask_margin: soldermaskMargin,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
        });
      }
    }
    if (pcb_smtpad) {
      this.pcb_smtpad_id = pcb_smtpad.pcb_smtpad_id;
    }
  }
  doInitialPcbPortAttachment() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    db.pcb_smtpad.update(this.pcb_smtpad_id, {
      pcb_port_id: this.matchedPort?.pcb_port_id
    });
  }
  getAvailablePcbLayers() {
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const primitiveContainer = this.getPrimitiveContainer();
    const primitiveContainerLayer = primitiveContainer?.props?.layer !== void 0 ? primitiveContainer?._parsedProps.layer : void 0;
    return [
      primitiveContainerLayer ?? maybeFlipLayer(this._parsedProps.layer ?? "top")
    ];
  }
  _getPcbCircuitJsonBounds() {
    const { db } = this.root;
    const smtpad = db.pcb_smtpad.get(this.pcb_smtpad_id);
    if (smtpad.shape === "rect") {
      return {
        center: { x: smtpad.x, y: smtpad.y },
        bounds: {
          left: smtpad.x - smtpad.width / 2,
          top: smtpad.y + smtpad.height / 2,
          right: smtpad.x + smtpad.width / 2,
          bottom: smtpad.y - smtpad.height / 2
        },
        width: smtpad.width,
        height: smtpad.height
      };
    }
    if (smtpad.shape === "rotated_rect") {
      const { width, height, xExtent, yExtent } = getAxisAlignedSizeFromRotatedRect({
        width: smtpad.width,
        height: smtpad.height,
        ccwRotationDegrees: smtpad.ccw_rotation
      });
      return {
        center: { x: smtpad.x, y: smtpad.y },
        bounds: {
          left: smtpad.x - xExtent,
          right: smtpad.x + xExtent,
          top: smtpad.y - yExtent,
          bottom: smtpad.y + yExtent
        },
        width,
        height
      };
    }
    if (smtpad.shape === "circle") {
      return {
        center: { x: smtpad.x, y: smtpad.y },
        bounds: {
          left: smtpad.x - smtpad.radius,
          top: smtpad.y - smtpad.radius,
          right: smtpad.x + smtpad.radius,
          bottom: smtpad.y + smtpad.radius
        },
        width: smtpad.radius * 2,
        height: smtpad.radius * 2
      };
    }
    if (smtpad.shape === "polygon") {
      const points = smtpad.points;
      const xs = points.map((p) => p.x);
      const ys = points.map((p) => p.y);
      const minX = Math.min(...xs);
      const maxX = Math.max(...xs);
      const minY = Math.min(...ys);
      const maxY = Math.max(...ys);
      return {
        center: { x: (minX + maxX) / 2, y: (minY + maxY) / 2 },
        bounds: {
          left: minX,
          top: maxY,
          right: maxX,
          bottom: minY
        },
        width: maxX - minX,
        height: maxY - minY
      };
    }
    if (smtpad.shape === "pill") {
      const halfWidth = smtpad.width / 2;
      const halfHeight = smtpad.height / 2;
      return {
        center: { x: smtpad.x, y: smtpad.y },
        bounds: {
          left: smtpad.x - halfWidth,
          top: smtpad.y - halfHeight,
          right: smtpad.x + halfWidth,
          bottom: smtpad.y + halfHeight
        },
        width: smtpad.width,
        height: smtpad.height
      };
    }
    if (smtpad.shape === "rotated_pill") {
      const { width, height, xExtent, yExtent } = getAxisAlignedSizeFromRotatedRect({
        width: smtpad.width,
        height: smtpad.height,
        ccwRotationDegrees: smtpad.ccw_rotation
      });
      return {
        center: { x: smtpad.x, y: smtpad.y },
        bounds: {
          left: smtpad.x - xExtent,
          right: smtpad.x + xExtent,
          top: smtpad.y - yExtent,
          bottom: smtpad.y + yExtent
        },
        width,
        height
      };
    }
    throw new Error(
      `circuitJson bounds calculation not implemented for shape "${smtpad.shape}"`
    );
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    db.pcb_smtpad.update(this.pcb_smtpad_id, {
      x: newCenter.x,
      y: newCenter.y
    });
    const solderPaste = db.pcb_solder_paste.list().find((elm) => elm.pcb_smtpad_id === this.pcb_smtpad_id);
    if (solderPaste) {
      db.pcb_solder_paste.update(solderPaste.pcb_solder_paste_id, {
        x: newCenter.x,
        y: newCenter.y
      });
    }
    this.matchedPort?._setPositionFromLayout(newCenter);
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_smtpad_id) return;
    const pad = db.pcb_smtpad.get(this.pcb_smtpad_id);
    if (pad.shape === "rect" || pad.shape === "circle" || pad.shape === "rotated_rect" || pad.shape === "pill" || pad.shape === "rotated_pill") {
      this._setPositionFromLayout({ x: pad.x + deltaX, y: pad.y + deltaY });
    } else if (pad.shape === "polygon") {
      db.pcb_smtpad.update(this.pcb_smtpad_id, {
        points: pad.points.map((p) => ({
          x: p.x + deltaX,
          y: p.y + deltaY
        }))
      });
      const newCenter = {
        x: this._getPcbCircuitJsonBounds().center.x + deltaX / 2,
        y: this._getPcbCircuitJsonBounds().center.y + deltaY / 2
      };
      this.matchedPort?._setPositionFromLayout(newCenter);
    }
  }
};

// lib/utils/get-relative-direction.ts
function getRelativeDirection(pointA, pointB) {
  const dx = pointB.x - pointA.x;
  const dy = pointB.y - pointA.y;
  if (Math.abs(dx) > Math.abs(dy)) {
    return dx >= 0 ? "right" : "left";
  }
  return dy >= 0 ? "up" : "down";
}

// lib/components/primitive-components/Port/Port.ts
import "schematic-symbols";
import { applyToPoint as applyToPoint23, compose as compose4, translate as translate2 } from "transformation-matrix";
import "zod";

// lib/components/primitive-components/Port/getCenterOfPcbPrimitives.ts
var getCenterOfPcbPrimitives = (pcbPrimitives) => {
  if (pcbPrimitives.length === 0) {
    throw new Error("Cannot get center of empty PCB primitives array");
  }
  const positions = pcbPrimitives.map((p) => p._getPcbCircuitJsonBounds().center).filter(Boolean);
  const sumX = positions.reduce((sum, pos) => sum + pos.x, 0);
  const sumY = positions.reduce((sum, pos) => sum + pos.y, 0);
  return {
    x: sumX / positions.length,
    y: sumY / positions.length
  };
};

// lib/components/primitive-components/Port/Port.ts
import { portProps } from "@tscircuit/props";

// lib/components/primitive-components/Port/apply-pin-attributes-to-source-port.ts
var setSupportedCapability = (sourcePortProps, capability) => {
  switch (capability) {
    case "i2c_sda":
      sourcePortProps.supports_i2c_sda = true;
      return;
    case "i2c_scl":
      sourcePortProps.supports_i2c_scl = true;
      return;
    case "spi_cs":
      sourcePortProps.supports_spi_cs = true;
      return;
    case "spi_sck":
      sourcePortProps.supports_spi_sck = true;
      return;
    case "spi_mosi":
      sourcePortProps.supports_spi_mosi = true;
      return;
    case "spi_miso":
      sourcePortProps.supports_spi_miso = true;
      return;
    case "uart_tx":
      sourcePortProps.supports_uart_tx = true;
      return;
    case "uart_rx":
      sourcePortProps.supports_uart_rx = true;
      return;
    default: {
      const unhandledCapability = capability;
      throw new Error(`Unhandled pin capability: ${unhandledCapability}`);
    }
  }
};
var setConfiguredCapability = (sourcePortProps, capability) => {
  switch (capability) {
    case "i2c_sda":
      sourcePortProps.is_configured_for_i2c_sda = true;
      return;
    case "i2c_scl":
      sourcePortProps.is_configured_for_i2c_scl = true;
      return;
    case "spi_cs":
      sourcePortProps.is_configured_for_spi_cs = true;
      return;
    case "spi_sck":
      sourcePortProps.is_configured_for_spi_sck = true;
      return;
    case "spi_mosi":
      sourcePortProps.is_configured_for_spi_mosi = true;
      return;
    case "spi_miso":
      sourcePortProps.is_configured_for_spi_miso = true;
      return;
    case "uart_tx":
      sourcePortProps.is_configured_for_uart_tx = true;
      return;
    case "uart_rx":
      sourcePortProps.is_configured_for_uart_rx = true;
      return;
    default: {
      const unhandledCapability = capability;
      throw new Error(
        `Unhandled configured pin capability: ${unhandledCapability}`
      );
    }
  }
};
var applyPinAttributesToSourcePort = (sourcePortProps, attributes) => {
  for (const capability of attributes.capabilities ?? []) {
    setSupportedCapability(sourcePortProps, capability);
  }
  const configuredCapabilities = /* @__PURE__ */ new Set([
    ...attributes.activeCapabilities ?? [],
    ...attributes.activeCapability ? [attributes.activeCapability] : []
  ]);
  for (const capability of configuredCapabilities) {
    setConfiguredCapability(sourcePortProps, capability);
  }
  if (attributes.mustBeConnected !== void 0) {
    sourcePortProps.must_be_connected = attributes.mustBeConnected;
  }
  if (attributes.providesPower !== void 0) {
    sourcePortProps.provides_power = attributes.providesPower;
  }
  if (attributes.requiresPower !== void 0) {
    sourcePortProps.requires_power = attributes.requiresPower;
  }
  if (attributes.providesGround !== void 0) {
    sourcePortProps.provides_ground = attributes.providesGround;
  }
  if (attributes.requiresGround !== void 0) {
    sourcePortProps.requires_ground = attributes.requiresGround;
  }
  if (attributes.providesVoltage !== void 0) {
    sourcePortProps.provides_voltage = attributes.providesVoltage;
  }
  if (attributes.requiresVoltage !== void 0) {
    sourcePortProps.requires_voltage = attributes.requiresVoltage;
  }
  if (attributes.doNotConnect !== void 0) {
    sourcePortProps.do_not_connect = attributes.doNotConnect;
  }
  if (attributes.includeInBoardPinout !== void 0) {
    sourcePortProps.include_in_board_pinout = attributes.includeInBoardPinout;
  }
  if (attributes.canUseInternalPullup !== void 0) {
    sourcePortProps.can_use_internal_pullup = attributes.canUseInternalPullup;
  }
  if (attributes.isUsingInternalPullup !== void 0) {
    sourcePortProps.is_using_internal_pullup = attributes.isUsingInternalPullup;
  }
  if (attributes.needsExternalPullup !== void 0) {
    sourcePortProps.needs_external_pullup = attributes.needsExternalPullup;
  }
  if (attributes.canUseInternalPulldown !== void 0) {
    sourcePortProps.can_use_internal_pulldown = attributes.canUseInternalPulldown;
  }
  if (attributes.isUsingInternalPulldown !== void 0) {
    sourcePortProps.is_using_internal_pulldown = attributes.isUsingInternalPulldown;
  }
  if (attributes.needsExternalPulldown !== void 0) {
    sourcePortProps.needs_external_pulldown = attributes.needsExternalPulldown;
  }
  if (attributes.canUseOpenDrain !== void 0) {
    sourcePortProps.can_use_open_drain = attributes.canUseOpenDrain;
  }
  if (attributes.isUsingOpenDrain !== void 0) {
    sourcePortProps.is_using_open_drain = attributes.isUsingOpenDrain;
  }
  if (attributes.canUsePushPull !== void 0) {
    sourcePortProps.can_use_push_pull = attributes.canUsePushPull;
  }
  if (attributes.isUsingPushPull !== void 0) {
    sourcePortProps.is_using_push_pull = attributes.isUsingPushPull;
  }
  if (attributes.shouldHaveDecouplingCapacitor !== void 0) {
    sourcePortProps.should_have_decoupling_capacitor = attributes.shouldHaveDecouplingCapacitor;
  }
  if (attributes.recommendedDecouplingCapacitorCapacitance !== void 0) {
    sourcePortProps.recommended_decoupling_capacitor_capacitance = attributes.recommendedDecouplingCapacitorCapacitance;
  }
};

// lib/components/primitive-components/Port/Port_doInitialCreateTracesFromProps.ts
var getConnectionTargets = (target) => {
  if (!target) return [];
  return typeof target === "string" ? [target] : [...target];
};
function Port_doInitialCreateTracesFromProps(port) {
  if (!port.isGroupPort()) return;
  const connectsTo = port._parsedProps.connectsTo;
  if (!connectsTo) return;
  const parent = port.parent;
  if (!parent) return;
  const parentConnections = parent._parsedProps?.connections;
  const connectionIsCreatedByParent = Object.keys(parentConnections ?? {}).some(
    (pinName) => port.isMatchingAnyOf([pinName])
  );
  if (connectionIsCreatedByParent) return;
  const parentName = parent.name;
  const portName = port.name;
  if (!parentName || !portName) return;
  const from = `.${parentName} > .${portName}`;
  for (const target of getConnectionTargets(connectsTo)) {
    const traceAlreadyExists = parent.children.some((child) => {
      if (!(child instanceof Trace3)) return false;
      const traceProps2 = child._parsedProps;
      if (!("from" in traceProps2) || !("to" in traceProps2)) return false;
      return traceProps2.from === from && traceProps2.to === target || traceProps2.from === target && traceProps2.to === from;
    });
    if (traceAlreadyExists) continue;
    parent.add(new Trace3({ from, to: target }));
  }
}

// lib/components/primitive-components/Port/Port_isConnectedToGround.ts
function portIsGround(port) {
  for (const pinAttributes of port._getMatchingPinAttributes()) {
    if (pinAttributes.requiresGround === true || pinAttributes.providesGround === true) {
      return true;
    }
  }
  for (const portName of port.getNameAndAliases()) {
    if (GROUND_NET_REGEX.test(portName)) return true;
  }
  return false;
}
function netIsGround(net) {
  return net._parsedProps.isGroundNet ?? GROUND_NET_REGEX.test(net._parsedProps.name);
}
function Port_isConnectedToGround(port) {
  for (const connectedTrace of port._getDirectlyConnectedTraces()) {
    const connectedPorts = connectedTrace._findConnectedPorts().ports ?? [];
    for (const connectedPort of connectedPorts) {
      if (connectedPort === port) continue;
      if (portIsGround(connectedPort)) return true;
    }
    for (const connectedNet of connectedTrace._findConnectedNets().nets) {
      if (connectedNet && netIsGround(connectedNet)) return true;
    }
  }
  return false;
}

// lib/components/primitive-components/Port/Port_isConnectedToPower.ts
function portShouldHaveDecouplingCapacitor(port) {
  if (port.getParentNormalComponent()?.config.componentName !== "Chip") {
    return false;
  }
  let shouldHaveDecouplingCapacitor;
  let requiresPower;
  let providesPower;
  for (const pinAttributes of port._getMatchingPinAttributes()) {
    if (pinAttributes.shouldHaveDecouplingCapacitor !== void 0) {
      shouldHaveDecouplingCapacitor = pinAttributes.shouldHaveDecouplingCapacitor;
    }
    if (pinAttributes.requiresPower !== void 0) {
      requiresPower = pinAttributes.requiresPower;
    }
    if (pinAttributes.providesPower !== void 0) {
      providesPower = pinAttributes.providesPower;
    }
  }
  if (shouldHaveDecouplingCapacitor !== void 0) {
    return shouldHaveDecouplingCapacitor;
  }
  if (requiresPower !== void 0) return requiresPower;
  if (providesPower === true) return false;
  for (const portName of port.getNameAndAliases()) {
    if (POWER_NET_REGEX.test(portName)) return true;
  }
  return false;
}
function Port_isConnectedToPower(port) {
  for (const connectedTrace of port._getDirectlyConnectedTraces()) {
    const connectedPorts = connectedTrace._findConnectedPorts().ports ?? [];
    for (const connectedPort of connectedPorts) {
      if (connectedPort === port) continue;
      if (portShouldHaveDecouplingCapacitor(connectedPort)) return true;
    }
  }
  return false;
}

// lib/components/primitive-components/Port/Port_tryRenderGroupPcbPort.ts
function Port_tryRenderGroupPcbPort(port) {
  if (port.root?.pcbDisabled) return false;
  if (port.pcb_port_id) return true;
  const { db } = port.root;
  const connectedPort = port._getConnectedPortsFromConnectsTo()[0];
  if (!connectedPort?.pcb_port_id) return false;
  const connectedPcbPort = db.pcb_port.get(connectedPort.pcb_port_id);
  if (!connectedPcbPort) return false;
  const subcircuit = port.getSubcircuit();
  const pcbPort = db.pcb_port.insert({
    pcb_component_id: void 0,
    layers: connectedPort.getAvailablePcbLayers(),
    subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
    pcb_group_id: port.getGroup()?.pcb_group_id ?? void 0,
    x: connectedPcbPort.x,
    y: connectedPcbPort.y,
    source_port_id: port.source_port_id,
    is_board_pinout: false
  });
  port.pcb_port_id = pcbPort.pcb_port_id;
  return true;
}

// lib/utils/schematic/getSourcePortNetLabelText.ts
var compareNamesAlphanumerically = (firstName, secondName) => firstName.localeCompare(secondName, void 0, {
  numeric: true,
  sensitivity: "base"
});
var getSourcePortOwnerName = (db, sourcePort) => {
  const sourceComponent = sourcePort.source_component_id ? db.source_component.get(sourcePort.source_component_id) : void 0;
  const sourceGroup = !sourceComponent ? db.source_group.list().find((group) => group.subcircuit_id === sourcePort.subcircuit_id) : void 0;
  return sourceComponent?.name ?? sourceGroup?.name;
};
var getSourcePortNetLabelText = (db, sourcePortId) => {
  const sourcePort = db.source_port.get(sourcePortId);
  if (!sourcePort?.name) return void 0;
  const ownerName = getSourcePortOwnerName(db, sourcePort);
  if (!ownerName) return void 0;
  return `${ownerName}_${sourcePort.name}`;
};
var getNetNameFromSourcePorts = (db, sourcePortIds) => {
  const sourcePorts = sourcePortIds.map((sourcePortId) => db.source_port.get(sourcePortId)).filter((sourcePort) => Boolean(sourcePort));
  if (sourcePorts.length === 0) return void 0;
  const pinCountByComponentId = /* @__PURE__ */ new Map();
  for (const sourcePort of db.source_port.list()) {
    if (!sourcePort.source_component_id) continue;
    pinCountByComponentId.set(
      sourcePort.source_component_id,
      (pinCountByComponentId.get(sourcePort.source_component_id) ?? 0) + 1
    );
  }
  const candidates = sourcePorts.map((sourcePort) => {
    const ownerName = getSourcePortOwnerName(db, sourcePort);
    if (!ownerName || !sourcePort.name) return null;
    return {
      sourcePort,
      ownerName,
      label: `${ownerName}_${sourcePort.name}`,
      ownerPinCount: sourcePort.source_component_id ? pinCountByComponentId.get(sourcePort.source_component_id) ?? 0 : 0
    };
  }).filter(
    (candidate) => Boolean(candidate)
  );
  candidates.sort((firstCandidate, secondCandidate) => {
    const pinCountDifference = secondCandidate.ownerPinCount - firstCandidate.ownerPinCount;
    if (pinCountDifference !== 0) return pinCountDifference;
    const ownerNameDifference = compareNamesAlphanumerically(
      firstCandidate.ownerName,
      secondCandidate.ownerName
    );
    if (ownerNameDifference !== 0) return ownerNameDifference;
    const firstPinNumber = firstCandidate.sourcePort.pin_number ?? Number.POSITIVE_INFINITY;
    const secondPinNumber = secondCandidate.sourcePort.pin_number ?? Number.POSITIVE_INFINITY;
    const pinNumberDifference = firstPinNumber - secondPinNumber;
    if (pinNumberDifference !== 0) return pinNumberDifference;
    return compareNamesAlphanumerically(
      firstCandidate.sourcePort.name,
      secondCandidate.sourcePort.name
    );
  });
  return candidates[0]?.label;
};

// lib/components/primitive-components/Port/Port.ts
var Port = class extends PrimitiveComponent2 {
  source_port_id = null;
  pcb_port_id = null;
  schematic_port_id = null;
  schematic_stem_line_id = null;
  /**
   * False when a parent component will assign an existing source_port_id
   * during a later render phase.
   */
  shouldCreateSourcePort;
  schematicSymbolPortDef = null;
  matchedComponents;
  _isPrimaryPort = true;
  _primaryPinNumber = null;
  facingDirection = null;
  originDescription = null;
  get config() {
    return {
      componentName: "Port",
      zodProps: portProps
    };
  }
  constructor(props, opts = {}) {
    if (!props.name && props.pinNumber !== void 0)
      props.name = `pin${props.pinNumber}`;
    if (!props.name) {
      throw new Error("Port must have a name or a pinNumber");
    }
    super(props);
    if (opts.originDescription) {
      this.originDescription = opts.originDescription;
    }
    this.shouldCreateSourcePort = opts.shouldCreateSourcePort ?? true;
    this.matchedComponents = [];
  }
  isGroupPort() {
    return this.parent?.componentName === "Group";
  }
  isComponentPort() {
    return !this.isGroupPort();
  }
  _getConnectedPortsFromConnectsTo() {
    const { _parsedProps: props } = this;
    const connectsTo = props.connectsTo;
    if (!connectsTo) return [];
    const connectedPorts = [];
    const connectsToArray = Array.isArray(connectsTo) ? connectsTo : [connectsTo];
    for (const connection of connectsToArray) {
      const port = this.getSubcircuit().selectOne(connection, {
        type: "port"
      });
      if (port) {
        connectedPorts.push(port);
      }
    }
    return connectedPorts;
  }
  _isBoardPinoutFromAttributes() {
    const parent = this.parent;
    if (parent?._parsedProps?.pinAttributes) {
      const pinAttributes = parent._parsedProps.pinAttributes;
      for (const alias of this.getNameAndAliases()) {
        if (pinAttributes[alias]?.includeInBoardPinout) {
          return true;
        }
      }
    }
  }
  _getGlobalPcbPositionBeforeLayout() {
    if (this.pcb_port_id) {
      const pcbPort = this.root?.db.pcb_port.get(this.pcb_port_id);
      if (pcbPort?.pcb_component_id) return { x: pcbPort.x, y: pcbPort.y };
    }
    const matchedPcbElm = this.matchedComponents.find((c) => c.isPcbPrimitive);
    const parentComponent = this.parent;
    if (parentComponent && !parentComponent.props.footprint) {
      throw new Error(
        `${parentComponent.componentName} "${parentComponent.props.name}" does not have a footprint. Add a footprint prop, e.g. <${parentComponent.componentName.toLowerCase()} footprint="..." />`
      );
    }
    if (!matchedPcbElm) {
      throw new Error(
        `Port ${this} has no matching PCB primitives. This often means the footprint's pads lack matching port hints.`
      );
    }
    return matchedPcbElm?._getGlobalPcbPositionBeforeLayout() ?? { x: 0, y: 0 };
  }
  _getPcbCircuitJsonBounds() {
    if (!this.pcb_port_id) {
      return super._getPcbCircuitJsonBounds();
    }
    const { db } = this.root;
    const pcb_port = db.pcb_port.get(this.pcb_port_id);
    return {
      center: { x: pcb_port.x, y: pcb_port.y },
      bounds: { left: 0, top: 0, right: 0, bottom: 0 },
      width: 0,
      height: 0
    };
  }
  _getGlobalPcbPositionAfterLayout() {
    return this._getPcbCircuitJsonBounds().center;
  }
  _getPortsInternallyConnectedToThisPort() {
    const parent = this.parent;
    if (!parent || !parent._getInternallyConnectedPins) return [];
    const internallyConnectedPorts = parent._getInternallyConnectedPins();
    for (const ports of internallyConnectedPorts) {
      if (ports.some((port) => port === this)) {
        return ports;
      }
    }
    return [];
  }
  /**
   * Return true if this port has a schematic representation and can be rendered
   * to the schematic.
   *
   * Sometimes things like mounting holes don't have a schematic representation
   * and aren't rendered to the schematic.
   *
   * It's common for a schematic symbol to not have a representation for all of
   * the pins on a footprint, e.g. a pushbutton has 4 pins but is typically
   * represented by a two-pin symbol. In these cases, it's best to use
   * internallyConnectedPorts or externallyConnectedPorts to ensure the things
   * are rendered properly.
   */
  _hasSchematicPort() {
    const { schX, schY } = this._parsedProps;
    if (schX !== void 0 && schY !== void 0) {
      return true;
    }
    const parentNormalComponent = this.getParentNormalComponent();
    const symbol = parentNormalComponent?.getSchematicSymbol();
    if (symbol) {
      if (this.schematicSymbolPortDef) return true;
      const portsInternallyConnectedToThisPort = this._getPortsInternallyConnectedToThisPort();
      if (portsInternallyConnectedToThisPort.some((p) => p.schematicSymbolPortDef))
        return true;
      return false;
    }
    const parentBoxDim = parentNormalComponent?._getSchematicBoxDimensions();
    if (parentBoxDim && this._parsedProps.pinNumber !== void 0) {
      const localPortPosition = parentBoxDim.getPortPositionByPinNumber(
        this._parsedProps.pinNumber
      );
      if (localPortPosition) return true;
    }
    return false;
  }
  _getGlobalSchematicPositionBeforeLayout() {
    const { schX, schY } = this._parsedProps;
    if (schX !== void 0 && schY !== void 0) {
      if (this._getSymbolAncestor()) {
        return applyToPoint23(this.computeSchematicGlobalTransform(), {
          x: 0,
          y: 0
        });
      }
      return { x: schX, y: schY };
    }
    const parentNormalComponent = this.getParentNormalComponent();
    const symbol = parentNormalComponent?.getSchematicSymbol();
    if (symbol) {
      let schematicSymbolPortDef = this.schematicSymbolPortDef;
      if (!schematicSymbolPortDef) {
        schematicSymbolPortDef = this._getPortsInternallyConnectedToThisPort().find(
          (p) => p.schematicSymbolPortDef
        )?.schematicSymbolPortDef ?? null;
        if (!schematicSymbolPortDef) {
          throw new Error(
            `Couldn't find schematicSymbolPortDef for port ${this.getString()}, searched internally connected ports and none had a schematicSymbolPortDef. Why are we trying to get the schematic position of this port?`
          );
        }
      }
      const transform = compose4(
        parentNormalComponent.computeSchematicGlobalTransform(),
        translate2(-symbol.center.x, -symbol.center.y)
      );
      return applyToPoint23(transform, schematicSymbolPortDef);
    }
    const parentBoxDim = parentNormalComponent?._getSchematicBoxDimensions();
    if (parentBoxDim && this._parsedProps.pinNumber !== void 0) {
      const localPortPosition = parentBoxDim.getPortPositionByPinNumber(
        this._parsedProps.pinNumber
      );
      if (!localPortPosition) {
        throw new Error(
          `Couldn't find position for schematic_port for port ${this.getString()} inside of the schematic box`
        );
      }
      return applyToPoint23(
        parentNormalComponent.computeSchematicGlobalTransform(),
        localPortPosition
      );
    }
    throw new Error(
      `Couldn't find position for schematic_port for port ${this.getString()}`
    );
  }
  _getGlobalSchematicPositionAfterLayout() {
    const { db } = this.root;
    if (!this.schematic_port_id) {
      throw new Error(
        `Can't get schematic port position after layout for "${this.getString()}", no schematic_port_id`
      );
    }
    const schematic_port = db.schematic_port.get(this.schematic_port_id);
    if (!schematic_port)
      throw new Error(
        `Schematic port not found when trying to get post-layout position: ${this.schematic_port_id}`
      );
    return schematic_port.center;
  }
  /**
   * Smtpads and platedholes call this method to register themselves as a match
   * for this port. All the matching is done by primitives other than the Port,
   * but everyone registers themselves as a match with their Port.
   */
  registerMatch(component) {
    this.matchedComponents.push(component);
  }
  getNameAndAliases() {
    const { _parsedProps: props } = this;
    return Array.from(
      /* @__PURE__ */ new Set([
        ...props.name ? [props.name] : [],
        ...props.aliases ?? [],
        ...typeof props.pinNumber === "number" ? [`pin${props.pinNumber}`, props.pinNumber.toString()] : [],
        ...this.externallyAddedAliases ?? []
      ])
    );
  }
  _getMatchingPinAttributes() {
    const parentProps = this.parent?._parsedProps;
    const pinAttributes = parentProps?.pinAttributes;
    const noConnect = parentProps?.noConnect;
    const matches = [];
    for (const alias of this.getNameAndAliases()) {
      if (pinAttributes) {
        const attributes = pinAttributes[alias];
        if (attributes) {
          matches.push(attributes);
        }
      }
      if (noConnect?.includes(alias)) {
        matches.push({ doNotConnect: true });
      }
    }
    if (matches.length === 0) {
      return [];
    }
    return matches;
  }
  _shouldIncludeInBoardPinout() {
    return this._getMatchingPinAttributes().some(
      (attributes) => attributes.includeInBoardPinout === true
    );
  }
  isMatchingPort(port) {
    return this.isMatchingAnyOf(port.getNameAndAliases());
  }
  getPortSelector() {
    const parentComponent = this.getParentNormalComponent() ?? this.parent;
    return `.${parentComponent?.props.name} > port.${this.props.name}`;
  }
  getAvailablePcbLayers() {
    const { layer, layers } = this._parsedProps;
    if (layers) return layers;
    if (layer) return [layer];
    if (this.pcb_port_id) {
      const pcbPort = this.root?.db.pcb_port.get(this.pcb_port_id);
      if (pcbPort?.pcb_component_id) return pcbPort.layers;
    }
    return Array.from(
      new Set(this.matchedComponents.flatMap((c) => c.getAvailablePcbLayers()))
    );
  }
  /**
   * Return traces that are explicitly connected to this port (not via a net)
   */
  _getDirectlyConnectedTraces() {
    const allSubcircuitTraces = this.getSubcircuit().selectAll(
      "trace"
    );
    const connectedTraces = [];
    for (const trace of allSubcircuitTraces) {
      if (trace._couldNotFindPort) continue;
      try {
        if (trace._isExplicitlyConnectedToPort(this)) {
          connectedTraces.push(trace);
        }
      } catch (error) {
        if (error instanceof TraceConnectionError) continue;
        throw error;
      }
    }
    return connectedTraces;
  }
  isConnectedToPower() {
    return Port_isConnectedToPower(this);
  }
  isConnectedToGround() {
    return Port_isConnectedToGround(this);
  }
  doInitialCreateTracesFromProps() {
    Port_doInitialCreateTracesFromProps(this);
  }
  doInitialSourceRender() {
    if (!this.shouldCreateSourcePort) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const port_hints = this.getNameAndAliases();
    const parentNormalComponent = this.getParentNormalComponent();
    const parentWithSourceId = this.parent?.source_component_id ? this.parent : parentNormalComponent;
    const source_component_id = parentWithSourceId?.source_component_id ?? null;
    const pinAttributes = this._getMatchingPinAttributes();
    const portAttributesFromParent = {};
    for (const attributes of pinAttributes) {
      applyPinAttributesToSourcePort(portAttributesFromParent, attributes);
    }
    const source_port = db.source_port.insert({
      name: props.name,
      pin_number: props.pinNumber,
      port_hints,
      source_component_id,
      subcircuit_id: this.getSubcircuit()?.subcircuit_id,
      ...portAttributesFromParent
    });
    this.source_port_id = source_port.source_port_id;
  }
  doInitialSourceParentAttachment() {
    if (!this.shouldCreateSourcePort) return;
    const { db } = this.root;
    const parentNormalComponent = this.getParentNormalComponent();
    const parentWithSourceId = this.parent?.source_component_id ? this.parent : parentNormalComponent;
    if (this.isGroupPort()) {
      db.source_port.update(this.source_port_id, {
        source_component_id: null,
        subcircuit_id: this.getSubcircuit()?.subcircuit_id
      });
      return;
    }
    if (!parentWithSourceId?.source_component_id) {
      throw new Error(
        `${this.getString()} has no parent source component (parent: ${this.parent?.getString()})`
      );
    }
    db.source_port.update(this.source_port_id, {
      source_component_id: parentWithSourceId.source_component_id,
      subcircuit_id: this.getSubcircuit()?.subcircuit_id
    });
    this.source_component_id = parentWithSourceId.source_component_id;
  }
  doInitialPcbPortRender() {
    if (this.root?.pcbDisabled) return;
    if (this.getPrimitiveContainer()?.isSchematicPrimitive) return;
    const { db } = this.root;
    const { matchedComponents } = this;
    if (this.isGroupPort()) {
      Port_tryRenderGroupPcbPort(this);
      return;
    }
    const parentNormalComponent = this.getParentNormalComponent();
    const parentWithPcbComponentId = this.parent?.pcb_component_id ? this.parent : parentNormalComponent;
    if (!parentWithPcbComponentId?.pcb_component_id) {
      throw new Error(
        `${this.getString()} has no parent pcb component, cannot render pcb_port (parent: ${this.parent?.getString()}, parentNormalComponent: ${parentNormalComponent?.getString()})`
      );
    }
    const pcbMatches = matchedComponents.filter((c) => c.isPcbPrimitive);
    if (pcbMatches.length === 0) return;
    let matchCenter = null;
    if (pcbMatches.length === 1) {
      matchCenter = pcbMatches[0]._getPcbCircuitJsonBounds().center;
    }
    if (pcbMatches.length > 1) {
      if (!areAllPcbPrimitivesOverlapping(pcbMatches)) {
        const portName = this.props.name;
        const componentName = this.getParentNormalComponent()?.props.name ?? "unknown";
        const altAliases = this.getNameAndAliases().filter(
          (h) => h !== portName
        );
        const altMsg = altAliases.length > 0 ? ` (consider using alternate aliases: ${altAliases.join(", ")})` : "";
        db.source_ambiguous_port_reference.insert({
          error_type: "source_ambiguous_port_reference",
          message: `${componentName}.${portName} is ambiguous: ${componentName}.${portName} references multiple non-overlapping pads: ${pcbMatches.map((c) => c.getString()).join(", ")}${altMsg}`,
          source_port_id: this.source_port_id ?? void 0,
          source_component_id: this.getParentNormalComponent()?.source_component_id ?? void 0
        });
        return;
      }
      matchCenter = getCenterOfPcbPrimitives(pcbMatches);
    }
    if (matchCenter) {
      const subcircuit = this.getSubcircuit();
      const isBoardPinout = this._shouldIncludeInBoardPinout();
      const pcb_port = db.pcb_port.insert({
        pcb_component_id: parentWithPcbComponentId.pcb_component_id,
        layers: this.getAvailablePcbLayers(),
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
        ...isBoardPinout ? { is_board_pinout: true } : {},
        ...matchCenter,
        source_port_id: this.source_port_id,
        is_board_pinout: this._isBoardPinoutFromAttributes()
      });
      this.pcb_port_id = pcb_port.pcb_port_id;
    } else {
      const pcbMatch = pcbMatches[0];
      throw new Error(
        `${pcbMatch.getString()} does not have a center or _getGlobalPcbPositionBeforeLayout method (needed for pcb_port placement)`
      );
    }
  }
  updatePcbPortRender() {
    if (this.root?.pcbDisabled) return;
    if (this.getPrimitiveContainer()?.isSchematicPrimitive) return;
    const { db } = this.root;
    if (this.pcb_port_id) return;
    if (this.isGroupPort()) {
      Port_tryRenderGroupPcbPort(this);
      return;
    }
    const pcbMatches = this.matchedComponents.filter((c) => c.isPcbPrimitive);
    if (pcbMatches.length === 0) return;
    let matchCenter = null;
    if (pcbMatches.length === 1) {
      matchCenter = pcbMatches[0]._getPcbCircuitJsonBounds().center;
    }
    if (pcbMatches.length > 1) {
      try {
        if (areAllPcbPrimitivesOverlapping(pcbMatches)) {
          matchCenter = getCenterOfPcbPrimitives(pcbMatches);
        }
      } catch {
      }
    }
    if (!matchCenter) return;
    const parentNormalComponent = this.getParentNormalComponent();
    const parentWithPcbComponentId = this.parent?.pcb_component_id ? this.parent : parentNormalComponent;
    const subcircuit = this.getSubcircuit();
    const isBoardPinout = this._shouldIncludeInBoardPinout();
    const pcb_port = db.pcb_port.insert({
      pcb_component_id: parentWithPcbComponentId?.pcb_component_id,
      layers: this.getAvailablePcbLayers(),
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
      ...isBoardPinout ? { is_board_pinout: true } : {},
      ...matchCenter,
      source_port_id: this.source_port_id,
      is_board_pinout: this._isBoardPinoutFromAttributes()
    });
    this.pcb_port_id = pcb_port.pcb_port_id;
  }
  doInitialPcbPortAttachment() {
    if (!this.pcb_port_id && this.source_port_id) {
      const existingPcbPort = this.root?.db.pcb_port.list({ source_port_id: this.source_port_id }).find((pcbPort) => Boolean(pcbPort.pcb_component_id));
      if (existingPcbPort) {
        this.pcb_port_id = existingPcbPort.pcb_port_id;
        return;
      }
    }
    if (!this.isGroupPort() || this.pcb_port_id) return;
    Port_tryRenderGroupPcbPort(this);
  }
  /**
   * Get the best display label for this port based on port_hints
   * Filters out generic patterns and applies showPinAliases logic
   */
  _getBestDisplayPinLabel() {
    const { db } = this.root;
    const sourcePort = db.source_port.get(this.source_port_id);
    const labelHints = [];
    for (const portHint of sourcePort?.port_hints ?? []) {
      if (portHint.match(/^(pin)?\d+$/)) continue;
      if (portHint.match(/^(left|right)/) && !sourcePort?.name.match(/^(left|right)/))
        continue;
      labelHints.push(portHint);
    }
    const parentNormalComponent = this.getParentNormalComponent();
    const showPinAliases = parentNormalComponent?.props?.showPinAliases;
    if (showPinAliases && labelHints.length > 0) {
      return labelHints.join("/");
    } else if (labelHints.length > 0) {
      return labelHints[0];
    }
    return void 0;
  }
  doInitialSchematicPortRender() {
    if (this.root?.schematicDisabled) return;
    const collapsedAncestor = this.getCollapsedSchematicBoxAncestor();
    if (collapsedAncestor && this.parent !== collapsedAncestor) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { schX, schY } = props;
    const container = schX !== void 0 && schY !== void 0 ? this.getParentNormalComponent() : this.getPrimitiveContainer();
    if (!container) return;
    if (!this._hasSchematicPort()) return;
    const containerCenter = container._getGlobalSchematicPositionBeforeLayout();
    const portCenter = this._getGlobalSchematicPositionBeforeLayout();
    let localPortInfo = null;
    const containerDims = container._getSchematicBoxDimensions();
    if (containerDims && props.pinNumber !== void 0) {
      localPortInfo = containerDims.getPortPositionByPinNumber(props.pinNumber);
    }
    if (this.getSubcircuit().props._schDebugObjectsEnabled) {
      db.schematic_debug_object.insert({
        shape: "rect",
        center: portCenter,
        size: {
          width: 0.1,
          height: 0.1
        },
        label: "obstacle"
      });
    }
    const isExplicitCustomSymbolPort = schX !== void 0 && schY !== void 0 && !!this._getSymbolAncestor();
    if (!localPortInfo?.side) {
      this.facingDirection = getRelativeDirection(containerCenter, portCenter);
      if (isExplicitCustomSymbolPort && props.direction) {
        this.facingDirection = props.direction;
      }
    } else {
      this.facingDirection = {
        left: "left",
        right: "right",
        top: "up",
        bottom: "down"
      }[localPortInfo.side];
    }
    const bestDisplayPinLabel = this._getBestDisplayPinLabel();
    const parentNormalComponent = this.getParentNormalComponent();
    const sideOfComponent = localPortInfo?.side ?? (props.direction === "up" ? "top" : props.direction === "down" ? "bottom" : props.direction);
    const schematicPortInsertProps = {
      type: "schematic_port",
      schematic_component_id: parentNormalComponent?.schematic_component_id,
      center: portCenter,
      source_port_id: this.source_port_id,
      facing_direction: this.facingDirection,
      distance_from_component_edge: props.schStemLength ?? 0.4,
      side_of_component: sideOfComponent,
      pin_number: props.pinNumber,
      true_ccw_index: localPortInfo?.trueIndex,
      display_pin_label: bestDisplayPinLabel,
      is_connected: false,
      schematic_sheet_id: this._resolveSchematicSheetId()
    };
    for (const attributes of this._getMatchingPinAttributes()) {
      if (attributes.requiresPower) {
        schematicPortInsertProps.has_input_arrow = true;
      }
      if (attributes.providesPower) {
        schematicPortInsertProps.has_output_arrow = true;
      }
    }
    const schematic_port = db.schematic_port.insert(schematicPortInsertProps);
    this.schematic_port_id = schematic_port.schematic_port_id;
    if (props.schStemLength !== void 0 && props.schStemLength !== 0) {
      const { schStemLength, direction } = props;
      let x2 = portCenter.x;
      let y2 = portCenter.y;
      if (direction === "right") x2 -= schStemLength;
      else if (direction === "left") x2 += schStemLength;
      else if (direction === "up") y2 -= schStemLength;
      else if (direction === "down") y2 += schStemLength;
      const stemLine = db.schematic_line.insert({
        schematic_component_id: parentNormalComponent?.schematic_component_id,
        x1: portCenter.x,
        y1: portCenter.y,
        x2,
        y2,
        stroke_width: 0.02,
        color: SCHEMATIC_COMPONENT_OUTLINE_COLOR,
        is_dashed: false,
        schematic_sheet_id: this._resolveSchematicSheetId()
      });
      this.schematic_stem_line_id = stemLine.schematic_line_id;
    }
  }
  doInitialSchematicSymbolResize() {
    if (this.root?.schematicDisabled) return;
    if (!this.schematic_port_id) return;
    const symbol = this._getSymbolAncestor();
    const transform = symbol?.getUserCoordinateToResizedSymbolTransform();
    if (!transform) return;
    const { db } = this.root;
    const schPort = db.schematic_port.get(this.schematic_port_id);
    if (schPort) {
      const newCenter = applyToPoint23(transform, schPort.center);
      db.schematic_port.update(this.schematic_port_id, {
        center: newCenter
      });
      if (this.schematic_stem_line_id) {
        const line = db.schematic_line.get(this.schematic_stem_line_id);
        if (line) {
          const p1 = applyToPoint23(transform, { x: line.x1, y: line.y1 });
          const p2 = applyToPoint23(transform, { x: line.x2, y: line.y2 });
          db.schematic_line.update(this.schematic_stem_line_id, {
            x1: p1.x,
            y1: p1.y,
            x2: p2.x,
            y2: p2.y
          });
          const scaledStemLength = Math.sqrt(
            (p1.x - p2.x) ** 2 + (p1.y - p2.y) ** 2
          );
          db.schematic_port.update(this.schematic_port_id, {
            distance_from_component_edge: scaledStemLength
          });
        }
      }
    }
  }
  _getSubcircuitConnectivityKey() {
    return this.root?.db.source_port.get(this.source_port_id)?.subcircuit_connectivity_map_key;
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    if (!this.pcb_port_id) return;
    db.pcb_port.update(this.pcb_port_id, {
      x: newCenter.x,
      y: newCenter.y
    });
  }
  _hasMatchedPcbPrimitive() {
    const pcbPort = this.pcb_port_id ? this.root?.db.pcb_port.get(this.pcb_port_id) : null;
    return Boolean(pcbPort?.pcb_component_id) || this.matchedComponents.some((c) => c.isPcbPrimitive);
  }
  /**
   * Return the text that should be used for the net label for this port if a
   * trace can't be drawn. This net label text usually doesn't appear at this
   * port, but appears at the port it connects to.
   */
  _getNetLabelText() {
    if (!this.root || !this.source_port_id) return void 0;
    return getSourcePortNetLabelText(this.root.db, this.source_port_id);
  }
};

// lib/components/primitive-components/Symbol/Symbol.ts
import { symbolProps } from "@tscircuit/props";
import { compose as compose5, translate as translate3, scale } from "transformation-matrix";
var SymbolComponent = class extends PrimitiveComponent2 {
  isPrimitiveContainer = true;
  schematic_symbol_id;
  userCoordinateToResizedSymbolTransformMat;
  schematicSymbolBoundsInUserCoordinates;
  get config() {
    return {
      componentName: "Symbol",
      zodProps: symbolProps
    };
  }
  hasExplicitSize() {
    const { _parsedProps: props } = this;
    return props.width !== void 0 || props.height !== void 0;
  }
  /**
   * Create the schematic_symbol element in SymbolContainerRender phase.
   * This runs before SchematicPrimitiveRender, ensuring children can
   * reference the schematic_symbol_id when they render.
   */
  doInitialSymbolContainerRender() {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const parentNormal = this.getParentNormalComponent();
    const kicadSymbolMetadata = parentNormal?._parsedProps?.kicadSymbolMetadata;
    const schematic_symbol = db.schematic_symbol.insert({
      name: props.name,
      metadata: kicadSymbolMetadata ? { kicad_symbol: kicadSymbolMetadata } : void 0
    });
    this.schematic_symbol_id = schematic_symbol.schematic_symbol_id;
  }
  getSchematicSymbolBounds() {
    if (this.schematicSymbolBoundsInUserCoordinates) {
      return this.schematicSymbolBoundsInUserCoordinates;
    }
    this._computeSchematicSymbolBounds();
    return this.schematicSymbolBoundsInUserCoordinates ?? null;
  }
  getUserCoordinateToResizedSymbolTransform() {
    if (!this.hasExplicitSize()) {
      return null;
    }
    if (this.userCoordinateToResizedSymbolTransformMat) {
      return this.userCoordinateToResizedSymbolTransformMat;
    }
    this._computeUserCoordinateToResizedSymbolTransform();
    return this.userCoordinateToResizedSymbolTransformMat ?? null;
  }
  _computeSchematicSymbolBounds() {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const schematicElements = [];
    for (const child of this.children) {
      if (child.componentName === "Port") {
        const portId = child.schematic_port_id;
        if (portId) {
          const port = db.schematic_port.get(portId);
          if (port) schematicElements.push(port);
        }
        continue;
      }
      if (!child.isSchematicPrimitive) continue;
      if (child.componentName === "SchematicLine") {
        const line = db.schematic_line.get(child.schematic_line_id);
        if (line) schematicElements.push(line);
      } else if (child.componentName === "SchematicRect") {
        const rect = db.schematic_rect.get(child.schematic_rect_id);
        if (rect) schematicElements.push(rect);
      } else if (child.componentName === "SchematicCircle") {
        const circle = db.schematic_circle.get(
          child.schematic_circle_id
        );
        if (circle) schematicElements.push(circle);
      } else if (child.componentName === "SchematicArc") {
        const arc = db.schematic_arc.get(child.schematic_arc_id);
        if (arc) schematicElements.push(arc);
      } else if (child.componentName === "SchematicText") {
        const text = db.schematic_text.get(child.schematic_text_id);
        if (text) schematicElements.push(text);
      } else if (child.componentName === "SchematicPath") {
        const pathIds = child.schematic_path_ids;
        if (pathIds) {
          for (const pathId of pathIds) {
            const path = db.schematic_path.get(pathId);
            if (path) schematicElements.push(path);
          }
        }
      }
    }
    if (schematicElements.length === 0) return;
    const bounds = getBoundsForSchematic(schematicElements);
    this.schematicSymbolBoundsInUserCoordinates = bounds;
  }
  _computeUserCoordinateToResizedSymbolTransform() {
    const bounds = this.getSchematicSymbolBounds();
    if (!bounds) return;
    const { _parsedProps: props } = this;
    const targetWidth = props.width;
    const targetHeight = props.height;
    if (targetWidth === void 0 && targetHeight === void 0) return;
    const currentWidth = bounds.maxX - bounds.minX;
    const currentHeight = bounds.maxY - bounds.minY;
    if (currentWidth === 0 && currentHeight === 0) return;
    const currentCenterX = (bounds.minX + bounds.maxX) / 2;
    const currentCenterY = (bounds.minY + bounds.maxY) / 2;
    const scaleX = targetWidth !== void 0 && currentWidth > 0 ? targetWidth / currentWidth : 1;
    const scaleY = targetHeight !== void 0 && currentHeight > 0 ? targetHeight / currentHeight : 1;
    const globalPos = this._getGlobalSchematicPositionBeforeLayout();
    this.userCoordinateToResizedSymbolTransformMat = compose5(
      translate3(globalPos.x, globalPos.y),
      scale(scaleX, scaleY),
      translate3(-currentCenterX, -currentCenterY)
    );
  }
};

// lib/utils/createPinrowSilkscreenText.ts
var createPinrowSilkscreenText = ({
  elm,
  pinLabels,
  layer,
  readableRotation,
  anchorAlignment
}) => {
  const pinNum = elm.text.replace(/[{}]/g, "").toLowerCase();
  let label = pinNum;
  if (Array.isArray(pinLabels)) {
    const index = parseInt(pinNum.replace(/[^\d]/g, ""), 10) - 1;
    label = String(pinLabels[index] ?? pinNum);
  } else if (typeof pinLabels === "object") {
    label = String(pinLabels[pinNum] ?? pinNum);
  }
  const silkscreenText = new SilkscreenText({
    anchorAlignment: anchorAlignment || "center",
    text: label ?? pinNum,
    layer: layer || "top",
    pcbX: isNaN(elm.anchor_position.x) ? 0 : elm.anchor_position.x,
    pcbY: elm.anchor_position.y,
    pcbRotation: readableRotation ?? 0
  });
  silkscreenText._footprinterFontSize = elm.font_size + 0.2;
  return silkscreenText;
};

// lib/utils/createComponentsFromCircuitJson.ts
var calculateCcwRotation = (componentRotationStr, elementCcwRotation) => {
  const componentAngle = parseInt(componentRotationStr || "0", 10);
  let totalRotation;
  if (elementCcwRotation !== void 0 && elementCcwRotation !== null) {
    totalRotation = elementCcwRotation - componentAngle;
  } else {
    totalRotation = componentAngle;
  }
  const normalizedRotation = (totalRotation % 360 + 360) % 360;
  return normalizedRotation;
};
var optional = (value) => value ?? void 0;
var getPinNumberFromPortHints = (portHints) => {
  for (const hint of portHints ?? []) {
    if (/^\d+$/.test(hint)) return Number(hint);
    const pinNameMatch = /^pin(\d+)$/i.exec(hint);
    if (pinNameMatch) return Number(pinNameMatch[1]);
  }
};
var getFacingDirectionFromSide = (side) => {
  switch (side) {
    case "left":
    case "right":
      return side;
    case "top":
      return "up";
    case "bottom":
      return "down";
  }
  return null;
};
var schematicPrimitiveTypesWithStrokeWidth = [
  "schematic_line",
  "schematic_rect",
  "schematic_circle",
  "schematic_arc",
  "schematic_path"
];
var isSchematicPrimitiveWithStrokeWidth = (elm) => schematicPrimitiveTypesWithStrokeWidth.includes(
  elm.type
);
var getSchematicSymbolId = (elm) => {
  if (!("schematic_symbol_id" in elm)) return void 0;
  return typeof elm.schematic_symbol_id === "string" ? elm.schematic_symbol_id : void 0;
};
var createComponentsFromCircuitJson = ({
  componentName,
  componentRotation,
  footprinterString,
  pinLabels,
  pcbPinLabels
}, circuitJson) => {
  const components = [];
  const schematicSymbolsByImportedId = /* @__PURE__ */ new Map();
  const schematicComponentsByImportedId = /* @__PURE__ */ new Map();
  const sourceComponentIdByPcbComponentId = /* @__PURE__ */ new Map();
  const sourcePortsByImportedId = /* @__PURE__ */ new Map();
  const sourcePortsByComponentAndPinNumber = /* @__PURE__ */ new Map();
  const schematicStrokeWidthBySymbolId = /* @__PURE__ */ new Map();
  for (const elm of circuitJson) {
    if (elm.type === "pcb_component" && typeof elm.source_component_id === "string") {
      sourceComponentIdByPcbComponentId.set(
        elm.pcb_component_id,
        elm.source_component_id
      );
    }
    if (elm.type === "source_port") {
      sourcePortsByImportedId.set(elm.source_port_id, elm);
      if (typeof elm.pin_number === "number" && typeof elm.source_component_id === "string") {
        let sourcePortsByPinNumber = sourcePortsByComponentAndPinNumber.get(
          elm.source_component_id
        );
        if (!sourcePortsByPinNumber) {
          sourcePortsByPinNumber = /* @__PURE__ */ new Map();
          sourcePortsByComponentAndPinNumber.set(
            elm.source_component_id,
            sourcePortsByPinNumber
          );
        }
        sourcePortsByPinNumber.set(elm.pin_number, elm);
      }
    }
  }
  const getPortHintsWithSourcePortAliases = (portHints, pcbComponentId) => {
    const pinNumber = getPinNumberFromPortHints(portHints);
    const sourceComponentId = typeof pcbComponentId === "string" ? sourceComponentIdByPcbComponentId.get(pcbComponentId) : void 0;
    const sourcePort = typeof pinNumber === "number" && sourceComponentId ? sourcePortsByComponentAndPinNumber.get(sourceComponentId)?.get(pinNumber) : void 0;
    if (!sourcePort) return portHints;
    const mergedPortHints = Array.from(
      new Set(
        [
          ...portHints ?? [],
          ...sourcePort.port_hints ?? [],
          sourcePort.name
        ].filter(
          (alias) => typeof alias === "string" && alias.length > 0
        )
      )
    );
    return mergedPortHints.length > 0 ? mergedPortHints : portHints;
  };
  for (const elm of circuitJson) {
    if (elm.type !== "schematic_symbol") continue;
    const schematicSymbol = new SymbolComponent({
      name: elm.name
    });
    schematicSymbolsByImportedId.set(elm.schematic_symbol_id, schematicSymbol);
    components.push(schematicSymbol);
  }
  for (const elm of circuitJson) {
    if (!isSchematicPrimitiveWithStrokeWidth(elm)) continue;
    const schematicSymbolId = getSchematicSymbolId(elm);
    const strokeWidth = elm.stroke_width;
    if (!schematicSymbolId || typeof strokeWidth !== "number" || !Number.isFinite(strokeWidth) || schematicStrokeWidthBySymbolId.has(schematicSymbolId)) {
      continue;
    }
    schematicStrokeWidthBySymbolId.set(schematicSymbolId, strokeWidth);
  }
  for (const elm of circuitJson) {
    if (elm.type !== "schematic_component") continue;
    schematicComponentsByImportedId.set(elm.schematic_component_id, elm);
  }
  const addSchematicPrimitive = (elm, primitive) => {
    const schematicSymbolId = getSchematicSymbolId(elm) ?? ("schematic_component_id" in elm && typeof elm.schematic_component_id === "string" ? schematicComponentsByImportedId.get(elm.schematic_component_id)?.schematic_symbol_id : void 0);
    const parentSymbol = typeof schematicSymbolId === "string" ? schematicSymbolsByImportedId.get(schematicSymbolId) : void 0;
    if (parentSymbol) {
      parentSymbol.add(primitive);
    } else {
      components.push(primitive);
    }
  };
  for (const elm of circuitJson) {
    const resolvedPortHints = "port_hints" in elm ? getPortHintsWithSourcePortAliases(
      elm.port_hints,
      "pcb_component_id" in elm ? elm.pcb_component_id : void 0
    ) : void 0;
    if (elm.type === "pcb_smtpad" && elm.shape === "rect") {
      components.push(
        new SmtPad({
          pcbX: elm.x,
          pcbY: elm.y,
          layer: elm.layer,
          shape: "rect",
          height: elm.height,
          width: elm.width,
          portHints: resolvedPortHints,
          rectBorderRadius: elm.rect_border_radius
        })
      );
    } else if (elm.type === "pcb_smtpad" && elm.shape === "circle") {
      components.push(
        new SmtPad({
          pcbX: elm.x,
          pcbY: elm.y,
          layer: elm.layer,
          shape: "circle",
          radius: elm.radius,
          portHints: resolvedPortHints
        })
      );
    } else if (elm.type === "pcb_smtpad" && elm.shape === "pill") {
      components.push(
        new SmtPad({
          shape: "pill",
          height: elm.height,
          width: elm.width,
          radius: elm.radius,
          portHints: resolvedPortHints,
          pcbX: elm.x,
          pcbY: elm.y,
          layer: elm.layer
        })
      );
    } else if (elm.type === "pcb_smtpad" && elm.shape === "rotated_pill") {
      components.push(
        new SmtPad({
          shape: "rotated_pill",
          height: elm.height,
          width: elm.width,
          radius: elm.radius,
          portHints: resolvedPortHints,
          pcbX: elm.x,
          pcbY: elm.y,
          layer: elm.layer,
          ccwRotation: elm.ccw_rotation
        })
      );
    } else if (elm.type === "pcb_smtpad" && elm.shape === "rotated_rect") {
      components.push(
        new SmtPad({
          pcbX: elm.x,
          pcbY: elm.y,
          layer: elm.layer,
          shape: "rotated_rect",
          height: elm.height,
          width: elm.width,
          ccwRotation: elm.ccw_rotation,
          cornerRadius: elm.corner_radius,
          portHints: resolvedPortHints
        })
      );
    } else if (elm.type === "pcb_smtpad" && elm.shape === "polygon") {
      components.push(
        new SmtPad({
          shape: "polygon",
          points: elm.points,
          portHints: resolvedPortHints,
          layer: elm.layer
        })
      );
    } else if (elm.type === "pcb_silkscreen_path") {
      components.push(
        new SilkscreenPath({
          layer: elm.layer,
          route: elm.route,
          strokeWidth: elm.stroke_width
        })
      );
    } else if (elm.type === "pcb_copper_text") {
      components.push(
        new CopperText({
          text: elm.text,
          pcbX: elm.anchor_position.x,
          pcbY: elm.anchor_position.y,
          pcbRotation: elm.ccw_rotation,
          anchorAlignment: elm.anchor_alignment,
          fontSize: elm.font_size,
          layer: elm.layer,
          mirrored: elm.is_mirrored,
          knockout: elm.is_knockout
        })
      );
    } else if (elm.type === "pcb_plated_hole") {
      if (elm.shape === "circle") {
        components.push(
          new PlatedHole({
            pcbX: elm.x,
            pcbY: elm.y,
            shape: "circle",
            holeDiameter: elm.hole_diameter,
            outerDiameter: elm.outer_diameter,
            portHints: resolvedPortHints
          })
        );
      } else if (elm.shape === "circular_hole_with_rect_pad") {
        components.push(
          new PlatedHole({
            pcbX: elm.x,
            pcbY: elm.y,
            shape: "circular_hole_with_rect_pad",
            holeDiameter: elm.hole_diameter,
            rectPadHeight: elm.rect_pad_height,
            rectPadWidth: elm.rect_pad_width,
            portHints: resolvedPortHints,
            rectBorderRadius: elm.rect_border_radius,
            holeOffsetX: elm.hole_offset_x,
            holeOffsetY: elm.hole_offset_y
          })
        );
      } else if (elm.shape === "pill" || elm.shape === "oval") {
        components.push(
          new PlatedHole({
            pcbX: elm.x,
            pcbY: elm.y,
            shape: elm.shape,
            holeWidth: elm.hole_width,
            holeHeight: elm.hole_height,
            outerWidth: elm.outer_width,
            outerHeight: elm.outer_height,
            portHints: resolvedPortHints
          })
        );
      } else if (elm.shape === "pill_hole_with_rect_pad") {
        components.push(
          new PlatedHole({
            pcbX: elm.x,
            pcbY: elm.y,
            shape: "pill_hole_with_rect_pad",
            holeShape: "pill",
            padShape: "rect",
            holeWidth: elm.hole_width,
            holeHeight: elm.hole_height,
            rectPadWidth: elm.rect_pad_width,
            rectPadHeight: elm.rect_pad_height,
            rectBorderRadius: elm.rect_border_radius,
            portHints: resolvedPortHints,
            holeOffsetX: elm.hole_offset_x,
            holeOffsetY: elm.hole_offset_y
          })
        );
      } else if (elm.shape === "rotated_pill_hole_with_rect_pad") {
        components.push(
          new PlatedHole({
            pcbX: elm.x,
            pcbY: elm.y,
            shape: "pill_hole_with_rect_pad",
            holeShape: "pill",
            padShape: "rect",
            holeWidth: elm.hole_width,
            holeHeight: elm.hole_height,
            rectPadWidth: elm.rect_pad_width,
            rectPadHeight: elm.rect_pad_height,
            rectBorderRadius: elm.rect_border_radius,
            portHints: resolvedPortHints,
            holeOffsetX: elm.hole_offset_x,
            holeOffsetY: elm.hole_offset_y,
            pcbRotation: elm.hole_ccw_rotation
          })
        );
      } else if (elm.shape === "hole_with_polygon_pad") {
        components.push(
          new PlatedHole({
            pcbX: elm.x,
            pcbY: elm.y,
            shape: "hole_with_polygon_pad",
            holeShape: elm.hole_shape || "circle",
            holeDiameter: elm.hole_diameter,
            holeWidth: elm.hole_width,
            holeHeight: elm.hole_height,
            padOutline: elm.pad_outline || [],
            holeOffsetX: elm.hole_offset_x,
            holeOffsetY: elm.hole_offset_y,
            portHints: resolvedPortHints
          })
        );
      }
    } else if (elm.type === "pcb_keepout" && elm.shape === "circle") {
      components.push(
        new Keepout({
          pcbX: elm.center.x,
          pcbY: elm.center.y,
          shape: "circle",
          radius: elm.radius
        })
      );
    } else if (elm.type === "pcb_keepout" && elm.shape === "rect") {
      components.push(
        new Keepout({
          pcbX: elm.center.x,
          pcbY: elm.center.y,
          shape: "rect",
          width: elm.width,
          height: elm.height
        })
      );
    } else if (elm.type === "pcb_hole" && elm.hole_shape === "circle") {
      components.push(
        new Hole({
          pcbX: elm.x,
          pcbY: elm.y,
          diameter: elm.hole_diameter
        })
      );
    } else if (elm.type === "pcb_hole" && elm.hole_shape === "rect") {
      components.push(
        new Hole({
          pcbX: elm.x,
          pcbY: elm.y,
          shape: "rect",
          width: elm.hole_width,
          height: elm.hole_height
        })
      );
    } else if (elm.type === "pcb_hole" && elm.hole_shape === "pill") {
      components.push(
        new Hole({
          pcbX: elm.x,
          pcbY: elm.y,
          shape: "pill",
          width: elm.hole_width,
          height: elm.hole_height
        })
      );
    } else if (elm.type === "pcb_hole" && elm.hole_shape === "oval") {
      components.push(
        new Hole({
          pcbX: elm.x,
          pcbY: elm.y,
          shape: "oval",
          width: elm.hole_width,
          height: elm.hole_height
        })
      );
    } else if (elm.type === "pcb_hole" && elm.hole_shape === "rotated_pill") {
      components.push(
        new Hole({
          pcbX: elm.x,
          pcbY: elm.y,
          shape: "pill",
          width: elm.hole_width,
          height: elm.hole_height,
          pcbRotation: elm.ccw_rotation
        })
      );
    } else if (elm.type === "pcb_cutout") {
      if (elm.shape === "rect") {
        components.push(
          new Cutout({
            pcbX: elm.center.x,
            pcbY: elm.center.y,
            shape: "rect",
            width: elm.width,
            height: elm.height
          })
        );
      } else if (elm.shape === "circle") {
        components.push(
          new Cutout({
            pcbX: elm.center.x,
            pcbY: elm.center.y,
            shape: "circle",
            radius: elm.radius
          })
        );
      } else if (elm.shape === "polygon") {
        components.push(
          new Cutout({
            shape: "polygon",
            points: elm.points
          })
        );
      }
    } else if (elm.type === "pcb_silkscreen_text") {
      const ccwRotation = calculateCcwRotation(
        componentRotation,
        elm.ccw_rotation
      );
      if ((footprinterString?.includes("pinrow") || footprinterString?.includes("headermodule")) && elm.text.includes("PIN")) {
        components.push(
          createPinrowSilkscreenText({
            elm,
            pinLabels: pcbPinLabels ?? pinLabels ?? {},
            layer: elm.layer,
            readableRotation: ccwRotation,
            anchorAlignment: elm.anchor_alignment
          })
        );
      } else {
        const silkscreenText = new SilkscreenText({
          anchorAlignment: elm.anchor_alignment || "center",
          // Footprinter-generated reference text is a placeholder that should
          // resolve to the component name. A literal from the explicit
          // `silkscreenlabel(...)` footprint option is an intentional custom
          // label (for example a module name) and must be preserved. Other
          // imported footprint text keeps the legacy component-name fallback.
          text: footprinterString?.includes("silkscreenlabel(") && elm.text !== "{REF}" && elm.text !== "{NAME}" && elm.text !== "{REFERENCE}" ? elm.text : componentName || elm.text,
          pcbX: Number.isNaN(elm.anchor_position.x) ? 0 : elm.anchor_position.x,
          pcbY: elm.anchor_position.y,
          pcbRotation: ccwRotation ?? 0
        });
        silkscreenText._footprinterFontSize = elm.font_size + 0.2;
        components.push(silkscreenText);
      }
    } else if (elm.type === "pcb_trace") {
      components.push(
        new PcbTrace({
          route: elm.route
        })
      );
    } else if (elm.type === "pcb_via") {
      const layers = elm.layers ?? [];
      const pcbVia = new PcbVia({
        pcbX: elm.x,
        pcbY: elm.y,
        holeDiameter: elm.hole_diameter,
        outerDiameter: elm.outer_diameter,
        fromLayer: elm.from_layer ?? layers[0],
        toLayer: elm.to_layer ?? layers[layers.length - 1],
        layers,
        netIsAssignable: elm.net_is_assignable,
        isTented: elm.is_tented
      });
      pcbVia._importedPcbTraceId = elm.pcb_trace_id;
      components.push(pcbVia);
    } else if (elm.type === "pcb_silkscreen_rect") {
      components.push(
        new SilkscreenRect({
          pcbX: elm.center.x,
          pcbY: elm.center.y,
          width: elm.width,
          height: elm.height,
          layer: elm.layer,
          strokeWidth: elm.stroke_width,
          filled: elm.is_filled,
          cornerRadius: elm.corner_radius
        })
      );
    } else if (elm.type === "pcb_silkscreen_circle") {
      components.push(
        new SilkscreenCircle({
          pcbX: elm.center.x,
          pcbY: elm.center.y,
          radius: elm.radius,
          layer: elm.layer,
          strokeWidth: elm.stroke_width
        })
      );
    } else if (elm.type === "pcb_silkscreen_line") {
      components.push(
        new SilkscreenLine({
          x1: elm.x1,
          y1: elm.y1,
          x2: elm.x2,
          y2: elm.y2,
          layer: elm.layer,
          strokeWidth: elm.stroke_width
        })
      );
    } else if (elm.type === "pcb_fabrication_note_text") {
      components.push(
        new FabricationNoteText({
          pcbX: elm.anchor_position.x,
          pcbY: elm.anchor_position.y,
          text: elm.text,
          fontSize: elm.font_size,
          anchorAlignment: elm.anchor_alignment,
          color: elm.color,
          font: elm.font,
          pcbRotation: elm.ccw_rotation
        })
      );
    } else if (elm.type === "pcb_fabrication_note_path") {
      components.push(
        new FabricationNotePath({
          route: elm.route,
          strokeWidth: elm.stroke_width,
          color: elm.color,
          layer: elm.layer
        })
      );
    } else if (elm.type === "pcb_fabrication_note_rect") {
      components.push(
        new FabricationNoteRect({
          pcbX: elm.center.x,
          pcbY: elm.center.y,
          width: elm.width,
          height: elm.height,
          strokeWidth: elm.stroke_width,
          isFilled: elm.is_filled,
          color: elm.color,
          layer: elm.layer,
          cornerRadius: elm.corner_radius,
          hasStroke: elm.has_stroke,
          isStrokeDashed: elm.is_stroke_dashed
        })
      );
    } else if (elm.type === "pcb_note_text") {
      components.push(
        new PcbNoteText({
          pcbX: elm.anchor_position.x,
          pcbY: elm.anchor_position.y,
          text: elm.text ?? "",
          fontSize: elm.font_size,
          anchorAlignment: elm.anchor_alignment,
          color: elm.color,
          font: elm.font
        })
      );
    } else if (elm.type === "pcb_note_rect") {
      components.push(
        new PcbNoteRect({
          pcbX: elm.center.x,
          pcbY: elm.center.y,
          width: elm.width,
          height: elm.height,
          strokeWidth: elm.stroke_width,
          isFilled: elm.is_filled,
          color: elm.color,
          cornerRadius: elm.corner_radius,
          hasStroke: elm.has_stroke,
          isStrokeDashed: elm.is_stroke_dashed
        })
      );
    } else if (elm.type === "pcb_note_path") {
      components.push(
        new PcbNotePath({
          route: elm.route,
          strokeWidth: elm.stroke_width,
          color: elm.color
        })
      );
    } else if (elm.type === "pcb_note_line") {
      components.push(
        new PcbNoteLine({
          x1: elm.x1,
          y1: elm.y1,
          x2: elm.x2,
          y2: elm.y2,
          strokeWidth: elm.stroke_width,
          color: elm.color,
          isDashed: elm.is_dashed
        })
      );
    } else if (elm.type === "pcb_courtyard_rect") {
      components.push(
        new CourtyardRect({
          pcbX: elm.center.x,
          pcbY: elm.center.y,
          width: elm.width,
          height: elm.height,
          pcbRotation: elm.ccw_rotation,
          layer: elm.layer
        })
      );
    } else if (elm.type === "pcb_courtyard_circle") {
      components.push(
        new CourtyardCircle({
          pcbX: elm.center.x,
          pcbY: elm.center.y,
          radius: elm.radius,
          layer: elm.layer
        })
      );
    } else if (elm.type === "pcb_courtyard_outline") {
      components.push(
        new CourtyardOutline({
          outline: elm.outline,
          layer: elm.layer
        })
      );
    } else if (elm.type === "schematic_line") {
      addSchematicPrimitive(
        elm,
        new SchematicLine({
          x1: elm.x1,
          y1: elm.y1,
          x2: elm.x2,
          y2: elm.y2,
          strokeWidth: optional(elm.stroke_width),
          color: elm.color,
          isDashed: elm.is_dashed
        })
      );
    } else if (elm.type === "schematic_rect") {
      addSchematicPrimitive(
        elm,
        new SchematicRect({
          schX: elm.center.x,
          schY: elm.center.y,
          width: elm.width,
          height: elm.height,
          rotation: elm.rotation,
          strokeWidth: optional(elm.stroke_width),
          color: elm.color,
          isFilled: elm.is_filled,
          fillColor: elm.fill_color,
          isDashed: elm.is_dashed
        })
      );
    } else if (elm.type === "schematic_circle") {
      addSchematicPrimitive(
        elm,
        new SchematicCircle({
          center: elm.center,
          radius: elm.radius,
          strokeWidth: optional(elm.stroke_width),
          color: elm.color,
          isFilled: elm.is_filled,
          fillColor: elm.fill_color,
          isDashed: elm.is_dashed
        })
      );
    } else if (elm.type === "schematic_arc") {
      addSchematicPrimitive(
        elm,
        new SchematicArc({
          center: elm.center,
          radius: elm.radius,
          startAngleDegrees: elm.start_angle_degrees,
          endAngleDegrees: elm.end_angle_degrees,
          direction: elm.direction,
          strokeWidth: optional(elm.stroke_width),
          color: elm.color,
          isDashed: elm.is_dashed
        })
      );
    } else if (elm.type === "schematic_text") {
      addSchematicPrimitive(
        elm,
        new SchematicText({
          schX: elm.position.x,
          schY: elm.position.y,
          text: elm.text,
          fontSize: elm.font_size,
          anchor: elm.anchor,
          color: elm.color,
          schRotation: elm.rotation
        })
      );
    } else if (elm.type === "schematic_path") {
      addSchematicPrimitive(
        elm,
        new SchematicPath({
          points: elm.points,
          strokeWidth: optional(elm.stroke_width),
          strokeColor: elm.stroke_color,
          dashLength: elm.dash_length,
          dashGap: elm.dash_gap,
          isFilled: elm.is_filled,
          fillColor: elm.fill_color
        })
      );
    } else if (elm.type === "schematic_port") {
      const schematicComponentId = elm.schematic_component_id;
      if (typeof schematicComponentId !== "string") continue;
      const schematicComponent = schematicComponentsByImportedId.get(schematicComponentId);
      const schematicSymbolId = schematicComponent?.schematic_symbol_id;
      const parentSymbol = typeof schematicSymbolId === "string" ? schematicSymbolsByImportedId.get(schematicSymbolId) : void 0;
      if (parentSymbol && schematicComponent?.is_box_with_pins === true && elm.center && elm.side_of_component) {
        const distance20 = elm.distance_from_component_edge;
        const facingDirection = getFacingDirectionFromSide(
          elm.side_of_component
        );
        if (!facingDirection) continue;
        const directionVector = getUnitVectorFromDirection2(facingDirection);
        const stemDirection = elm.facing_direction === facingDirection ? 1 : -1;
        const portCenter = {
          x: elm.center.x + directionVector.x * distance20 * stemDirection,
          y: elm.center.y + directionVector.y * distance20 * stemDirection
        };
        const sourcePort = sourcePortsByImportedId.get(elm.source_port_id);
        const aliases = Array.from(
          new Set(
            [
              elm.display_pin_label,
              sourcePort?.name,
              ...sourcePort?.port_hints ?? []
            ].filter(
              (alias) => typeof alias === "string" && alias.length > 0
            )
          )
        );
        const fallbackPortName = elm.pin_number ? `pin${elm.pin_number}` : elm.schematic_port_id;
        const portName = aliases[0] ?? elm.source_port_id ?? fallbackPortName;
        parentSymbol.add(
          new Port({
            pinNumber: elm.pin_number,
            name: portName,
            aliases,
            schX: elm.center.x,
            schY: elm.center.y,
            direction: facingDirection
          })
        );
        parentSymbol.add(
          new SchematicLine({
            x1: elm.center.x,
            y1: elm.center.y,
            x2: portCenter.x,
            y2: portCenter.y,
            strokeWidth: typeof schematicSymbolId === "string" ? optional(
              schematicStrokeWidthBySymbolId.get(schematicSymbolId)
            ) : void 0,
            isDashed: false
          })
        );
      }
    }
  }
  return components;
};

// lib/utils/get-bounds-of-pcb-components.ts
var NON_PHYSICAL_PCB_PRIMITIVE_PREFIXES = [
  "Silkscreen",
  "PcbNote",
  "Courtyard",
  "FabricationNote"
];
function getBoundsOfPcbComponents(components) {
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  let hasValidComponents = false;
  for (const child of components) {
    if (child.isPcbPrimitive && !NON_PHYSICAL_PCB_PRIMITIVE_PREFIXES.some(
      (prefix) => child.componentName.startsWith(prefix)
    )) {
      const { x, y } = child._getGlobalPcbPositionBeforeLayout();
      const { width: width2, height: height2 } = child.getPcbSize();
      minX = Math.min(minX, x - width2 / 2);
      minY = Math.min(minY, y - height2 / 2);
      maxX = Math.max(maxX, x + width2 / 2);
      maxY = Math.max(maxY, y + height2 / 2);
      hasValidComponents = true;
    } else if (child.children.length > 0) {
      const childBounds = getBoundsOfPcbComponents(child.children);
      if (childBounds.width > 0 || childBounds.height > 0) {
        minX = Math.min(minX, childBounds.minX);
        minY = Math.min(minY, childBounds.minY);
        maxX = Math.max(maxX, childBounds.maxX);
        maxY = Math.max(maxY, childBounds.maxY);
        hasValidComponents = true;
      }
    }
  }
  if (!hasValidComponents) {
    return {
      minX: 0,
      minY: 0,
      maxX: 0,
      maxY: 0,
      width: 0,
      height: 0
    };
  }
  let width = maxX - minX;
  let height = maxY - minY;
  if (width < 0) width = 0;
  if (height < 0) height = 0;
  return {
    minX,
    minY,
    maxX,
    maxY,
    width,
    height
  };
}

// lib/utils/getPortFromHints.ts
var getPinNumberFromLabels = (labels) => {
  const pinNumber = labels.find((p) => /^(pin)?\d+$/.test(p));
  if (!pinNumber) return null;
  return Number.parseInt(pinNumber.replace(/^pin/, ""));
};
function getPortFromHints(hints, opts) {
  const pinNumber = getPinNumberFromLabels(hints);
  if (!pinNumber) return null;
  const aliasesFromHints = hints.filter(
    (p) => p.toString() !== pinNumber.toString() && p !== `pin${pinNumber}` && p.trim() !== ""
  );
  const aliases = [
    ...aliasesFromHints,
    ...opts?.additionalAliases?.[`pin${pinNumber}`] ?? []
  ];
  return new Port({
    pinNumber,
    aliases
  });
}

// lib/utils/schematic/normalizePinSideDefinition.ts
function getPinsFromSideDefinition(sideDefinition) {
  if (!sideDefinition) return [];
  if (Array.isArray(sideDefinition)) return sideDefinition;
  return sideDefinition.pins ?? [];
}

// lib/utils/schematic/getSizeOfSidesFromPortArrangement.ts
var hasExplicitPinMapping = (pa) => {
  for (const side of [
    "leftSide",
    "rightSide",
    "topSide",
    "bottomSide"
  ]) {
    if (side in pa && typeof pa[side] === "number") {
      throw new Error(
        `A number was specified for "${side}", you probably meant to use "size" not "side"`
      );
    }
  }
  return "leftSide" in pa || "rightSide" in pa || "topSide" in pa || "bottomSide" in pa;
};
var getPinsFromPortArrangement = (pa) => {
  if (!pa || !hasExplicitPinMapping(pa)) return [];
  return [
    ...getPinsFromSideDefinition(pa.leftSide),
    ...getPinsFromSideDefinition(pa.rightSide),
    ...getPinsFromSideDefinition(pa.topSide),
    ...getPinsFromSideDefinition(pa.bottomSide)
  ];
};
var getSizeOfSidesFromPortArrangement = (pa) => {
  if (hasExplicitPinMapping(pa)) {
    return {
      leftSize: getPinsFromSideDefinition(pa.leftSide).length,
      rightSize: getPinsFromSideDefinition(pa.rightSide).length,
      topSize: getPinsFromSideDefinition(pa.topSide).length,
      bottomSize: getPinsFromSideDefinition(pa.bottomSide).length
    };
  }
  const { leftSize = 0, rightSize = 0, topSize = 0, bottomSize = 0 } = pa;
  return { leftSize, rightSize, topSize, bottomSize };
};

// lib/utils/schematic/getAllDimensionsForSchematicBox.ts
var DEFAULT_SCHEMATIC_BOX_PADDING_MM = 0.4;
var ESTIMATED_PIN_LABEL_CHARACTER_WIDTH = 0.1;
var PIN_LABEL_EDGE_PADDING = 0.1;
var getDisplayedPinLabel = (pinLabel) => typeof pinLabel === "string" ? pinLabel : pinLabel?.[0] ?? "";
var getEstimatedPinLabelWidth = (pinLabel) => Array.from(getDisplayedPinLabel(pinLabel)).length * ESTIMATED_PIN_LABEL_CHARACTER_WIDTH;
function isExplicitPinMappingArrangement(arrangement) {
  const a = arrangement;
  return a.leftSide !== void 0 || a.rightSide !== void 0 || a.topSide !== void 0 || a.bottomSide !== void 0;
}
var getAllDimensionsForSchematicBox = (params) => {
  const portDistanceFromEdge = params.portDistanceFromEdge ?? 0.4;
  let sidePinCounts = params.schPortArrangement ? getSizeOfSidesFromPortArrangement(params.schPortArrangement) : null;
  const sideLengths = {
    left: 0,
    right: 0,
    top: 0,
    bottom: 0
  };
  let pinCount = params.pinCount ?? null;
  if (pinCount === null) {
    if (sidePinCounts) {
      pinCount = sidePinCounts.leftSize + sidePinCounts.rightSize + sidePinCounts.topSize;
    } else {
      throw new Error("Could not determine pin count for the schematic box");
    }
  }
  if (pinCount && !sidePinCounts) {
    const rightSize = Math.floor(pinCount / 2);
    sidePinCounts = {
      leftSize: pinCount - rightSize,
      rightSize,
      topSize: 0,
      bottomSize: 0
    };
  }
  if (!sidePinCounts) {
    sidePinCounts = {
      leftSize: 0,
      rightSize: 0,
      topSize: 0,
      bottomSize: 0
    };
  }
  const getPinNumberUsingSideIndex = ({
    side,
    sideIndex,
    truePinIndex: truePinIndex2
  }) => {
    if (!params.schPortArrangement) return truePinIndex2 + 1;
    if (!isExplicitPinMappingArrangement(params.schPortArrangement))
      return truePinIndex2 + 1;
    const normalCcwDirection = {
      left: "top-to-bottom",
      bottom: "left-to-right",
      right: "bottom-to-top",
      top: "right-to-left"
    }[side];
    const directionAlongSide = params.schPortArrangement?.[`${side}Side`]?.direction ?? normalCcwDirection;
    const pinsDefinitionForSide = params.schPortArrangement?.[`${side}Side`]?.pins;
    let sideIndexWithDirectionCorrection = sideIndex;
    if (directionAlongSide !== normalCcwDirection) {
      sideIndexWithDirectionCorrection = pinsDefinitionForSide.length - sideIndex - 1;
    }
    return parsePinNumberFromLabelsOrThrow(
      pinsDefinitionForSide[sideIndexWithDirectionCorrection],
      params.pinLabels
    );
  };
  const orderedTruePorts = [];
  let currentDistanceFromEdge = 0;
  let truePinIndex = 0;
  for (let sideIndex = 0; sideIndex < sidePinCounts.leftSize; sideIndex++) {
    const pinNumber = getPinNumberUsingSideIndex({
      side: "left",
      sideIndex,
      truePinIndex
    });
    const pinStyle = params.numericSchPinStyle?.[`pin${pinNumber}`] ?? params.numericSchPinStyle?.[pinNumber];
    if (pinStyle?.topMargin) {
      currentDistanceFromEdge += pinStyle.topMargin;
    }
    orderedTruePorts.push({
      trueIndex: pinNumber - 1,
      pinNumber,
      side: "left",
      distanceFromOrthogonalEdge: currentDistanceFromEdge
    });
    if (pinStyle?.bottomMargin) {
      currentDistanceFromEdge += pinStyle.bottomMargin;
    }
    const isLastPinOnSide = sideIndex === sidePinCounts.leftSize - 1;
    if (!isLastPinOnSide) {
      currentDistanceFromEdge += params.schPinSpacing;
    } else {
      sideLengths.left = currentDistanceFromEdge;
    }
    truePinIndex++;
  }
  currentDistanceFromEdge = 0;
  for (let sideIndex = 0; sideIndex < sidePinCounts.bottomSize; sideIndex++) {
    const pinNumber = getPinNumberUsingSideIndex({
      side: "bottom",
      sideIndex,
      truePinIndex
    });
    const pinStyle = params.numericSchPinStyle?.[`pin${pinNumber}`] ?? params.numericSchPinStyle?.[pinNumber];
    if (pinStyle?.leftMargin) {
      currentDistanceFromEdge += pinStyle.leftMargin;
    }
    orderedTruePorts.push({
      trueIndex: pinNumber - 1,
      pinNumber,
      side: "bottom",
      distanceFromOrthogonalEdge: currentDistanceFromEdge
    });
    if (pinStyle?.rightMargin) {
      currentDistanceFromEdge += pinStyle.rightMargin;
    }
    const isLastPinOnSide = sideIndex === sidePinCounts.bottomSize - 1;
    if (!isLastPinOnSide) {
      currentDistanceFromEdge += params.schPinSpacing;
    } else {
      sideLengths.bottom = currentDistanceFromEdge;
    }
    truePinIndex++;
  }
  currentDistanceFromEdge = 0;
  for (let sideIndex = 0; sideIndex < sidePinCounts.rightSize; sideIndex++) {
    const pinNumber = getPinNumberUsingSideIndex({
      side: "right",
      sideIndex,
      truePinIndex
    });
    const pinStyle = params.numericSchPinStyle?.[`pin${pinNumber}`] ?? params.numericSchPinStyle?.[pinNumber];
    if (pinStyle?.bottomMargin) {
      currentDistanceFromEdge += pinStyle.bottomMargin;
    }
    orderedTruePorts.push({
      trueIndex: pinNumber - 1,
      pinNumber,
      side: "right",
      distanceFromOrthogonalEdge: currentDistanceFromEdge
    });
    if (pinStyle?.topMargin) {
      currentDistanceFromEdge += pinStyle.topMargin;
    }
    const isLastPinOnSide = sideIndex === sidePinCounts.rightSize - 1;
    if (!isLastPinOnSide) {
      currentDistanceFromEdge += params.schPinSpacing;
    } else {
      sideLengths.right = currentDistanceFromEdge;
    }
    truePinIndex++;
  }
  currentDistanceFromEdge = 0;
  for (let sideIndex = 0; sideIndex < sidePinCounts.topSize; sideIndex++) {
    const pinNumber = getPinNumberUsingSideIndex({
      side: "top",
      sideIndex,
      truePinIndex
    });
    const pinStyle = params.numericSchPinStyle?.[`pin${pinNumber}`] ?? params.numericSchPinStyle?.[pinNumber];
    if (pinStyle?.rightMargin) {
      currentDistanceFromEdge += pinStyle.rightMargin;
    }
    orderedTruePorts.push({
      trueIndex: pinNumber - 1,
      pinNumber,
      side: "top",
      distanceFromOrthogonalEdge: currentDistanceFromEdge
    });
    if (pinStyle?.leftMargin) {
      currentDistanceFromEdge += pinStyle.leftMargin;
    }
    const isLastPinOnSide = sideIndex === sidePinCounts.topSize - 1;
    if (!isLastPinOnSide) {
      currentDistanceFromEdge += params.schPinSpacing;
    } else {
      sideLengths.top = currentDistanceFromEdge;
    }
    truePinIndex++;
  }
  let resolvedSchWidth = params.schWidth;
  if (resolvedSchWidth === void 0) {
    resolvedSchWidth = Math.max(
      sideLengths.top + DEFAULT_SCHEMATIC_BOX_PADDING_MM,
      sideLengths.bottom + DEFAULT_SCHEMATIC_BOX_PADDING_MM
    );
    if (params.pinLabels) {
      const leftRightPins = orderedTruePorts.filter(
        (p) => p.side === "left" || p.side === "right"
      );
      const hasLeftRightLabels = leftRightPins.some(
        (p) => params.pinLabels?.[`pin${p.pinNumber}`] || params.pinLabels?.[p.pinNumber]
      );
      if (hasLeftRightLabels) {
        const MIN_WIDTH_FOR_SIDE_PINS = 0.5;
        resolvedSchWidth = Math.max(resolvedSchWidth, MIN_WIDTH_FOR_SIDE_PINS);
      }
    }
    const labelWidth = params.pinLabels ? Math.max(
      ...Object.values(params.pinLabels).map(
        (label) => label.length * ESTIMATED_PIN_LABEL_CHARACTER_WIDTH
      )
    ) : 0;
    const LABEL_PADDING = labelWidth > 0 ? 1.1 : 0;
    resolvedSchWidth = Math.max(resolvedSchWidth, labelWidth + LABEL_PADDING);
    const maxLabelWidthBySide = {
      left: 0,
      right: 0
    };
    for (const port of orderedTruePorts) {
      if (port.side !== "left" && port.side !== "right") continue;
      const pinLabel = params.pinLabels?.[`pin${port.pinNumber}`] ?? params.pinLabels?.[port.pinNumber];
      maxLabelWidthBySide[port.side] = Math.max(
        maxLabelWidthBySide[port.side],
        getEstimatedPinLabelWidth(pinLabel)
      );
    }
    resolvedSchWidth = Math.max(
      resolvedSchWidth,
      maxLabelWidthBySide.left + maxLabelWidthBySide.right + 2 * PIN_LABEL_EDGE_PADDING
    );
  }
  let schHeight = params.schHeight;
  if (!schHeight) {
    schHeight = Math.max(
      sideLengths.left + DEFAULT_SCHEMATIC_BOX_PADDING_MM,
      sideLengths.right + DEFAULT_SCHEMATIC_BOX_PADDING_MM
    );
  }
  if (params.pinLabels) {
    const CHAR_WIDTH = 0.13;
    const LABEL_EDGE_OFFSET = PIN_LABEL_EDGE_PADDING;
    const HALF_TEXT = 0.075;
    const getLabelLen = (pinNumber) => {
      const label = params.pinLabels[`pin${pinNumber}`] ?? params.pinLabels[`${pinNumber}`];
      return label ? label.length * CHAR_WIDTH : 0;
    };
    const leftLabels = orderedTruePorts.filter((p) => p.side === "left").map((p) => ({
      y: sideLengths.left / 2 - p.distanceFromOrthogonalEdge,
      len: getLabelLen(p.pinNumber)
    })).filter((p) => p.len > 0);
    const rightLabels = orderedTruePorts.filter((p) => p.side === "right").map((p) => ({
      y: -sideLengths.right / 2 + p.distanceFromOrthogonalEdge,
      len: getLabelLen(p.pinNumber)
    })).filter((p) => p.len > 0);
    const topLabels = orderedTruePorts.filter((p) => p.side === "top").map((p) => ({
      x: sideLengths.top / 2 - p.distanceFromOrthogonalEdge,
      len: getLabelLen(p.pinNumber)
    })).filter((p) => p.len > 0);
    const bottomLabels = orderedTruePorts.filter((p) => p.side === "bottom").map((p) => ({
      x: -sideLengths.bottom / 2 + p.distanceFromOrthogonalEdge,
      len: getLabelLen(p.pinNumber)
    })).filter((p) => p.len > 0);
    const rectsOverlap2 = (a, b) => Math.min(a.maxX, b.maxX) > Math.max(a.minX, b.minX) && Math.min(a.maxY, b.maxY) > Math.max(a.minY, b.minY);
    const getLeftRect = (label, width) => ({
      minX: -width / 2 + LABEL_EDGE_OFFSET,
      maxX: -width / 2 + LABEL_EDGE_OFFSET + label.len,
      minY: label.y - HALF_TEXT,
      maxY: label.y + HALF_TEXT
    });
    const getRightRect = (label, width) => ({
      minX: width / 2 - LABEL_EDGE_OFFSET - label.len,
      maxX: width / 2 - LABEL_EDGE_OFFSET,
      minY: label.y - HALF_TEXT,
      maxY: label.y + HALF_TEXT
    });
    const getTopRect = (label, height) => ({
      minX: label.x - HALF_TEXT,
      maxX: label.x + HALF_TEXT,
      minY: height / 2 - LABEL_EDGE_OFFSET - label.len,
      maxY: height / 2 - LABEL_EDGE_OFFSET
    });
    const getBottomRect = (label, height) => ({
      minX: label.x - HALF_TEXT,
      maxX: label.x + HALF_TEXT,
      minY: -height / 2 + LABEL_EDGE_OFFSET,
      maxY: -height / 2 + LABEL_EDGE_OFFSET + label.len
    });
    if (!params.schHeight) {
      let hasTopBottomLabelCollision = false;
      for (const topLabel of topLabels) {
        for (const bottomLabel of bottomLabels) {
          if (rectsOverlap2(
            getTopRect(topLabel, schHeight),
            getBottomRect(bottomLabel, schHeight)
          )) {
            hasTopBottomLabelCollision = true;
            schHeight = Math.max(
              schHeight,
              topLabel.len + bottomLabel.len + 2 * LABEL_EDGE_OFFSET
            );
          }
        }
      }
      if (hasTopBottomLabelCollision) {
        for (const topLabel of topLabels) {
          const topRect = getTopRect(topLabel, schHeight);
          for (const leftLabel of leftLabels) {
            const leftRect = getLeftRect(leftLabel, resolvedSchWidth);
            if (rectsOverlap2(topRect, leftRect)) {
              schHeight = Math.max(
                schHeight,
                2 * (leftRect.maxY + LABEL_EDGE_OFFSET + topLabel.len)
              );
            }
          }
          for (const rightLabel of rightLabels) {
            const rightRect = getRightRect(rightLabel, resolvedSchWidth);
            if (rectsOverlap2(topRect, rightRect)) {
              schHeight = Math.max(
                schHeight,
                2 * (rightRect.maxY + LABEL_EDGE_OFFSET + topLabel.len)
              );
            }
          }
        }
        for (const bottomLabel of bottomLabels) {
          const bottomRect = getBottomRect(bottomLabel, schHeight);
          for (const leftLabel of leftLabels) {
            const leftRect = getLeftRect(leftLabel, resolvedSchWidth);
            if (rectsOverlap2(bottomRect, leftRect)) {
              schHeight = Math.max(
                schHeight,
                2 * (LABEL_EDGE_OFFSET + bottomLabel.len - leftRect.minY)
              );
            }
          }
          for (const rightLabel of rightLabels) {
            const rightRect = getRightRect(rightLabel, resolvedSchWidth);
            if (rectsOverlap2(bottomRect, rightRect)) {
              schHeight = Math.max(
                schHeight,
                2 * (LABEL_EDGE_OFFSET + bottomLabel.len - rightRect.minY)
              );
            }
          }
        }
      }
    }
  }
  const trueEdgePositions = {
    // Top left corner
    left: {
      x: -resolvedSchWidth / 2 - portDistanceFromEdge,
      y: sideLengths.left / 2
    },
    // bottom left corner
    bottom: {
      x: -sideLengths.bottom / 2,
      y: -schHeight / 2 - portDistanceFromEdge
    },
    // bottom right corner
    right: {
      x: resolvedSchWidth / 2 + portDistanceFromEdge,
      y: -sideLengths.right / 2
    },
    // top right corner
    top: {
      x: sideLengths.top / 2,
      y: schHeight / 2 + portDistanceFromEdge
    }
  };
  const trueEdgeTraversalDirections = {
    left: { x: 0, y: -1 },
    right: { x: 0, y: 1 },
    top: { x: -1, y: 0 },
    bottom: { x: 1, y: 0 }
  };
  const truePortsWithPositions = orderedTruePorts.map((p) => {
    const { distanceFromOrthogonalEdge, side } = p;
    const edgePos = trueEdgePositions[side];
    const edgeDir = trueEdgeTraversalDirections[side];
    return {
      x: edgePos.x + distanceFromOrthogonalEdge * edgeDir.x,
      y: edgePos.y + distanceFromOrthogonalEdge * edgeDir.y,
      ...p
    };
  });
  return {
    getPortPositionByPinNumber(pinNumber) {
      const port = truePortsWithPositions.find(
        (p) => p.pinNumber.toString() === pinNumber.toString()
      );
      if (!port) {
        return null;
      }
      return port;
    },
    getSize() {
      return { width: resolvedSchWidth, height: schHeight };
    },
    getSizeIncludingPins() {
      return {
        width: resolvedSchWidth + (sidePinCounts.leftSize || sidePinCounts.rightSize ? 0.4 : 0),
        height: schHeight + (sidePinCounts.topSize || sidePinCounts.bottomSize ? 0.4 : 0)
      };
    },
    pinCount
  };
};

// lib/utils/schematic/getNumericSchPinStyle.ts
import { distance as distance6 } from "circuit-json";
var getNumericSchPinStyle = (pinStyles, pinLabels) => {
  if (!pinStyles) return void 0;
  const numericPinStyles = {};
  for (const [pinNameOrLabel, pinStyle] of Object.entries(pinStyles)) {
    const pinNumber = parsePinNumberFromLabelsOrThrow(pinNameOrLabel, pinLabels);
    const leftMargin = pinStyle.marginLeft ?? pinStyle.leftMargin;
    const rightMargin = pinStyle.marginRight ?? pinStyle.rightMargin;
    const topMargin = pinStyle.marginTop ?? pinStyle.topMargin;
    const bottomMargin = pinStyle.marginBottom ?? pinStyle.bottomMargin;
    const existingStyle = numericPinStyles[`pin${pinNumber}`] ?? {};
    const nextStyle = { ...existingStyle };
    if (leftMargin !== void 0)
      nextStyle.leftMargin = distance6.parse(leftMargin);
    if (rightMargin !== void 0) {
      nextStyle.rightMargin = distance6.parse(rightMargin);
    }
    if (topMargin !== void 0) nextStyle.topMargin = distance6.parse(topMargin);
    if (bottomMargin !== void 0) {
      nextStyle.bottomMargin = distance6.parse(bottomMargin);
    }
    numericPinStyles[`pin${pinNumber}`] = nextStyle;
  }
  return numericPinStyles;
};

// lib/utils/schematic/isCircuitJsonSymbol.ts
var isSymbolElementType = (elementType) => elementType === "schematic_symbol" || elementType === "schematic_component" || elementType === "schematic_line" || elementType === "schematic_rect" || elementType === "schematic_circle" || elementType === "schematic_arc" || elementType === "schematic_text" || elementType === "schematic_path";
var isCircuitJsonSymbol = (circuitJson) => {
  return Array.isArray(circuitJson) && circuitJson.length > 0 && circuitJson.some((element) => isSymbolElementType(element.type));
};

// lib/utils/schematic/normalizeSchematicSymbolCircuitJson.ts
var isFinitePoint = (point6) => typeof point6?.x === "number" && Number.isFinite(point6.x) && typeof point6.y === "number" && Number.isFinite(point6.y);
var translatePoint = (point6, origin) => ({
  x: point6.x - origin.x,
  y: point6.y - origin.y
});
var isTranslatableSchematicElement = (element) => element.type === "schematic_component" || element.type === "schematic_port" || element.type === "schematic_line" || element.type === "schematic_rect" || element.type === "schematic_circle" || element.type === "schematic_arc" || element.type === "schematic_text" || element.type === "schematic_path";
var getNormalizationOrigin = (symbolCircuitJson) => {
  const primarySchematicComponent = symbolCircuitJson.find(
    (element) => element.type === "schematic_component" && isFinitePoint(element.center)
  );
  if (primarySchematicComponent) {
    return primarySchematicComponent.center;
  }
  const schematicElements = symbolCircuitJson.filter(
    isTranslatableSchematicElement
  );
  if (schematicElements.length === 0) return null;
  const bounds = getBoundsForSchematic(schematicElements);
  if (!Number.isFinite(bounds.minX) || !Number.isFinite(bounds.maxX) || !Number.isFinite(bounds.minY) || !Number.isFinite(bounds.maxY)) {
    return null;
  }
  return {
    x: (bounds.minX + bounds.maxX) / 2,
    y: (bounds.minY + bounds.maxY) / 2
  };
};
var translateSchematicElement = (element, origin) => {
  switch (element.type) {
    case "schematic_component":
    case "schematic_port":
    case "schematic_rect":
    case "schematic_circle":
    case "schematic_arc":
      return isFinitePoint(element.center) ? {
        ...element,
        center: translatePoint(element.center, origin)
      } : element;
    case "schematic_line":
      return {
        ...element,
        x1: element.x1 - origin.x,
        y1: element.y1 - origin.y,
        x2: element.x2 - origin.x,
        y2: element.y2 - origin.y
      };
    case "schematic_text":
      return isFinitePoint(element.position) ? {
        ...element,
        position: translatePoint(element.position, origin)
      } : element;
    case "schematic_path":
      return {
        ...element,
        points: element.points.map((point6) => translatePoint(point6, origin))
      };
    default:
      return element;
  }
};
var normalizeSchematicSymbolCircuitJson = (symbolCircuitJson) => {
  const origin = getNormalizationOrigin(symbolCircuitJson);
  if (!origin) return [...symbolCircuitJson];
  if (origin.x === 0 && origin.y === 0) {
    return [...symbolCircuitJson];
  }
  return symbolCircuitJson.map(
    (element) => translateSchematicElement(element, origin)
  );
};

// lib/utils/schematic/selectSymbolCircuitJson.ts
var isImportedSymbolPrimitive = (elm) => elm.type === "schematic_line" || elm.type === "schematic_rect" || elm.type === "schematic_circle" || elm.type === "schematic_arc" || elm.type === "schematic_text" || elm.type === "schematic_path";
var getStringProp = (elm, propName) => {
  if (!(propName in elm)) return void 0;
  const rawProp = elm[propName];
  return typeof rawProp === "string" ? rawProp : void 0;
};
var selectSymbolCircuitJson = (circuitJson) => {
  const schematicSymbols = circuitJson.filter(
    (elm) => elm.type === "schematic_symbol"
  );
  const schematicComponents = circuitJson.filter(
    (elm) => elm.type === "schematic_component"
  );
  const primarySymbolId = schematicComponents.find(
    (elm) => typeof elm.schematic_symbol_id === "string"
  )?.schematic_symbol_id ?? schematicSymbols[0]?.schematic_symbol_id;
  const primaryComponent = (primarySymbolId ? schematicComponents.find(
    (elm) => elm.schematic_symbol_id === primarySymbolId
  ) : void 0) ?? schematicComponents[0];
  const primaryComponentId = primaryComponent?.schematic_component_id;
  const primarySourceComponentId = typeof primaryComponent?.source_component_id === "string" ? primaryComponent.source_component_id : void 0;
  const primarySymbol = (primarySymbolId ? schematicSymbols.find(
    (elm) => elm.schematic_symbol_id === primarySymbolId
  ) : void 0) ?? schematicSymbols[0];
  const relatedSourcePortIds = /* @__PURE__ */ new Set();
  const selected = /* @__PURE__ */ new Set();
  const selectedElements = [];
  const add = (elm) => {
    if (!elm || selected.has(elm)) return;
    selected.add(elm);
    selectedElements.push(elm);
  };
  add(primarySymbol);
  add(primaryComponent);
  for (const elm of circuitJson) {
    if (elm.type !== "schematic_port") continue;
    if (!primaryComponentId || elm.schematic_component_id !== primaryComponentId) {
      continue;
    }
    add(elm);
    if (typeof elm.source_port_id === "string") {
      relatedSourcePortIds.add(elm.source_port_id);
    }
  }
  for (const elm of circuitJson) {
    if (!isImportedSymbolPrimitive(elm)) continue;
    const schematicSymbolId = getStringProp(elm, "schematic_symbol_id");
    const schematicComponentId = getStringProp(elm, "schematic_component_id");
    const isRelatedPrimitive = primarySymbolId && schematicSymbolId === primarySymbolId || primaryComponentId && schematicComponentId === primaryComponentId || !primarySymbolId && !primaryComponentId;
    if (isRelatedPrimitive) {
      add(elm);
    }
  }
  for (const elm of circuitJson) {
    if (elm.type !== "source_port") continue;
    if (relatedSourcePortIds.has(elm.source_port_id) || primarySourceComponentId && elm.source_component_id === primarySourceComponentId) {
      add(elm);
    }
  }
  if (selectedElements.length > 0) {
    return selectedElements;
  }
  return circuitJson.filter(
    (elm) => elm.type === "schematic_symbol" || elm.type === "schematic_component" || elm.type === "schematic_port" || isImportedSymbolPrimitive(elm)
  );
};

// lib/components/base-components/NormalComponent/NormalComponent.ts
import {
  isValidElement as isReactElement3,
  isValidElement
} from "react";
import { symbols as symbols2 } from "schematic-symbols";
import { decomposeTSR as decomposeTSR9 } from "transformation-matrix";
import { z as z9 } from "zod";

// lib/components/primitive-components/CadAssembly.ts
import { cadassemblyProps } from "@tscircuit/props";
var CadAssembly = class extends PrimitiveComponent2 {
  isPrimitiveContainer = true;
  get config() {
    return {
      componentName: "CadAssembly",
      zodProps: cadassemblyProps
    };
  }
};

// lib/components/primitive-components/CadModel.ts
import { cadmodelProps, point3 } from "@tscircuit/props";
import { z as z8 } from "zod";
import { distance as distance7 } from "circuit-json";
import { decomposeTSR as decomposeTSR8 } from "transformation-matrix";

// lib/components/base-components/NormalComponent/utils/getFileExtension.ts
var getFileExtension = (filename) => {
  if (!filename) return null;
  const fragmentMatch = filename.match(/#ext=(\w+)$/);
  if (fragmentMatch) return fragmentMatch[1].toLowerCase();
  const withoutQuery = filename.split("?")[0];
  const sanitized = withoutQuery.split("#")[0];
  const lastSegment = sanitized.split("/").pop() ?? sanitized;
  if (!lastSegment.includes(".")) return null;
  const extension = lastSegment.split(".").pop();
  return extension?.toLowerCase() ?? null;
};

// lib/utils/constructAssetUrl.ts
var joinUrlPath = (base, path) => {
  const trimmedBase = base.replace(/\/+$/, "");
  const trimmedPath = path.replace(/^\/+/, "");
  return `${trimmedBase}/${trimmedPath}`;
};
var constructAssetUrl = (targetUrl, baseUrl) => {
  if (!baseUrl) {
    return targetUrl;
  }
  if (!targetUrl.startsWith("/")) {
    return targetUrl;
  }
  try {
    const baseUrlObj = new URL(baseUrl);
    if (baseUrlObj.pathname !== "/" && targetUrl.startsWith(baseUrlObj.pathname)) {
      return new URL(targetUrl, baseUrlObj.origin).toString();
    }
    return joinUrlPath(baseUrl, targetUrl);
  } catch (error) {
    return targetUrl;
  }
};

// lib/components/primitive-components/CadModel.ts
var rotation = z8.union([z8.number(), z8.string()]);
var rotation3 = z8.object({ x: rotation, y: rotation, z: rotation });
var CadModel = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "CadModel",
      zodProps: cadmodelProps
    };
  }
  doInitialCadModelRender() {
    const parent = this._findParentWithPcbComponent();
    if (!parent) return;
    if (!parent.pcb_component_id) return;
    const { db } = this.root;
    const { boardThickness = 0 } = parent._getBoard() ?? {};
    const bounds = parent._getPcbCircuitJsonBounds();
    const pcb_component = db.pcb_component.get(parent.pcb_component_id);
    const props = this._parsedProps;
    if (!props || typeof props.modelUrl !== "string" && typeof props.stepUrl !== "string")
      return;
    const parentTransform = parent._computePcbGlobalTransformBeforeLayout();
    const decomposedTransform = decomposeTSR8(parentTransform);
    const accumulatedRotation = decomposedTransform.rotation.angle * 180 / Math.PI;
    const sourceRotationOffset = props.pcbRotationOffset ?? props.rotationOffset;
    const rotationOffset = rotation3.parse({ x: 0, y: 0, z: 0 });
    if (typeof sourceRotationOffset === "number") {
      rotationOffset.z = Number(sourceRotationOffset);
    } else if (typeof sourceRotationOffset === "object") {
      const parsed = rotation3.parse(sourceRotationOffset);
      rotationOffset.x = Number(parsed.x);
      rotationOffset.y = Number(parsed.y);
      rotationOffset.z = Number(parsed.z);
    }
    const { pcbX, pcbY } = this.getResolvedPcbPositionProp();
    const positionOffset = point3.parse({
      x: pcbX,
      y: pcbY,
      z: props.pcbZ ?? 0,
      ...typeof props.positionOffset === "object" ? props.positionOffset : {}
    });
    const zOffsetFromSurface = props.zOffsetFromSurface !== void 0 ? distance7.parse(props.zOffsetFromSurface) : 0;
    const layer = parent.props.layer === "bottom" ? "bottom" : "top";
    const ext = props.modelUrl ? getFileExtension(props.modelUrl) : void 0;
    const modelUrlWithoutExtFragment = props.modelUrl?.replace(/#ext=\w+$/, "");
    const urlProps = {};
    if (ext === "stl")
      urlProps.model_stl_url = this._addCachebustToModelUrl(
        modelUrlWithoutExtFragment
      );
    else if (ext === "obj")
      urlProps.model_obj_url = this._addCachebustToModelUrl(
        modelUrlWithoutExtFragment
      );
    else if (ext === "gltf")
      urlProps.model_gltf_url = this._addCachebustToModelUrl(
        modelUrlWithoutExtFragment
      );
    else if (ext === "glb")
      urlProps.model_glb_url = this._addCachebustToModelUrl(
        modelUrlWithoutExtFragment
      );
    else if (ext === "step" || ext === "stp")
      urlProps.model_step_url = this._addCachebustToModelUrl(
        modelUrlWithoutExtFragment
      );
    else if (ext === "wrl" || ext === "vrml")
      urlProps.model_wrl_url = this._addCachebustToModelUrl(
        modelUrlWithoutExtFragment
      );
    else
      urlProps.model_stl_url = this._addCachebustToModelUrl(
        modelUrlWithoutExtFragment
      );
    if (props.stepUrl) {
      const transformed = this._addCachebustToModelUrl(props.stepUrl);
      if (transformed) urlProps.model_step_url = transformed;
    }
    const cad = db.cad_component.insert({
      position: {
        x: bounds.center.x + Number(positionOffset.x),
        y: bounds.center.y + Number(positionOffset.y),
        z: (layer === "bottom" ? -boardThickness / 2 : boardThickness / 2) + (layer === "bottom" ? -zOffsetFromSurface : zOffsetFromSurface) + Number(positionOffset.z)
      },
      rotation: {
        x: Number(rotationOffset.x),
        y: (layer === "top" ? 0 : 180) + Number(rotationOffset.y),
        z: layer === "bottom" ? -(accumulatedRotation + Number(rotationOffset.z)) + 180 : accumulatedRotation + Number(rotationOffset.z)
      },
      pcb_component_id: parent.pcb_component_id,
      model_board_normal_direction: props.modelBoardNormalDirection,
      model_origin_alignment: "center_of_component_on_board_surface",
      anchor_alignment: "center_of_component_on_board_surface",
      model_origin_position: props.modelOriginPosition,
      source_component_id: parent.source_component_id,
      model_unit_to_mm_scale_factor: typeof props.modelUnitToMmScale === "number" ? props.modelUnitToMmScale : void 0,
      show_as_translucent_model: props.showAsTranslucentModel ?? parent._parsedProps.showAsTranslucentModel,
      ...urlProps
    });
    this.cad_component_id = cad.cad_component_id;
  }
  _findParentWithPcbComponent() {
    let p = this.parent;
    while (p && !p.pcb_component_id) p = p.parent;
    return p;
  }
  _addCachebustToModelUrl(url) {
    if (!url) return url;
    const baseUrl = this.root?.platform?.projectBaseUrl;
    const transformedUrl = constructAssetUrl(url, baseUrl);
    if (!transformedUrl.includes("modelcdn.tscircuit.com"))
      return transformedUrl;
    const origin = this.root?.getClientOrigin() ?? "";
    return `${transformedUrl}${transformedUrl.includes("?") ? "&" : "?"}cachebust_origin=${encodeURIComponent(origin)}`;
  }
};

// lib/components/primitive-components/Footprint.ts
import { footprintProps } from "@tscircuit/props";
import * as kiwi from "@lume/kiwi";
import Debug3 from "debug";
var debug2 = Debug3("tscircuit:core:footprint");
var Footprint = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "Footprint",
      zodProps: footprintProps
    };
  }
  /**
   * A footprint is a constrainedlayout, the db elements are adjusted according
   * to any constraints that are defined.
   */
  doInitialPcbFootprintLayout() {
    if (this.root?.pcbDisabled) return;
    const constraints = this.children.filter(
      (child) => child.componentName === "Constraint"
    );
    if (constraints.length === 0) return;
    const { isFlipped } = this._getPcbPrimitiveFlippedHelpers();
    const maybeFlipLeftRight = (props) => {
      if (isFlipped) {
        if ("left" in props && "right" in props) {
          return {
            ...props,
            left: props.right,
            right: props.left
          };
        }
      }
      return props;
    };
    const involvedComponents = constraints.flatMap(
      (constraint) => constraint._getAllReferencedComponents().componentsWithSelectors
    ).map(({ component, selector, componentSelector, edge }) => ({
      component,
      selector,
      componentSelector,
      edge,
      bounds: component._getPcbCircuitJsonBounds()
    }));
    if (involvedComponents.some((c) => c.edge)) {
      throw new Error(
        "edge constraints not implemented yet for footprint layout, contributions welcome!"
      );
    }
    function getComponentDetails(selector) {
      return involvedComponents.find(({ selector: s }) => s === selector);
    }
    const solver = new kiwi.Solver();
    const kVars = {};
    function getKVar(name) {
      if (!(name in kVars)) {
        kVars[name] = new kiwi.Variable(name);
        solver.addEditVariable(kVars[name], kiwi.Strength.weak);
      }
      return kVars[name];
    }
    for (const { selector, bounds: bounds2 } of involvedComponents) {
      const kvx = getKVar(`${selector}_x`);
      const kvy = getKVar(`${selector}_y`);
      solver.suggestValue(kvx, bounds2.center.x);
      solver.suggestValue(kvy, bounds2.center.y);
    }
    for (const constraint of constraints) {
      const props = constraint._parsedProps;
      if ("xDist" in props) {
        const { xDist, left, right, edgeToEdge, centerToCenter } = maybeFlipLeftRight(props);
        const leftVar = getKVar(`${left}_x`);
        const rightVar = getKVar(`${right}_x`);
        const leftBounds = getComponentDetails(left)?.bounds;
        const rightBounds = getComponentDetails(right)?.bounds;
        if (centerToCenter) {
          const expr = new kiwi.Expression(rightVar, [-1, leftVar]);
          solver.addConstraint(
            new kiwi.Constraint(
              expr,
              kiwi.Operator.Eq,
              props.xDist,
              kiwi.Strength.required
            )
          );
        } else if (edgeToEdge) {
          const expr = new kiwi.Expression(
            rightVar,
            -rightBounds.width / 2,
            [-1, leftVar],
            -leftBounds.width / 2
          );
          solver.addConstraint(
            new kiwi.Constraint(
              expr,
              kiwi.Operator.Eq,
              props.xDist,
              kiwi.Strength.required
            )
          );
        }
      } else if ("yDist" in props) {
        const { yDist, top, bottom, edgeToEdge, centerToCenter } = props;
        const topVar = getKVar(`${top}_y`);
        const bottomVar = getKVar(`${bottom}_y`);
        const topBounds = getComponentDetails(top)?.bounds;
        const bottomBounds = getComponentDetails(bottom)?.bounds;
        if (centerToCenter) {
          const expr = new kiwi.Expression(topVar, [-1, bottomVar]);
          solver.addConstraint(
            new kiwi.Constraint(
              expr,
              kiwi.Operator.Eq,
              props.yDist,
              kiwi.Strength.required
            )
          );
        } else if (edgeToEdge) {
          const expr = new kiwi.Expression(
            topVar,
            topBounds.height / 2,
            [-1, bottomVar],
            -bottomBounds.height / 2
          );
          solver.addConstraint(
            new kiwi.Constraint(
              expr,
              kiwi.Operator.Eq,
              props.yDist,
              kiwi.Strength.required
            )
          );
        }
      } else if ("sameY" in props) {
        const { for: selectors } = props;
        if (selectors.length < 2) continue;
        const vars = selectors.map((selector) => getKVar(`${selector}_y`));
        const expr = new kiwi.Expression(...vars.slice(1));
        solver.addConstraint(
          new kiwi.Constraint(
            expr,
            kiwi.Operator.Eq,
            vars[0],
            kiwi.Strength.required
          )
        );
      } else if ("sameX" in props) {
        const { for: selectors } = props;
        if (selectors.length < 2) continue;
        const vars = selectors.map((selector) => getKVar(`${selector}_x`));
        const expr = new kiwi.Expression(...vars.slice(1));
        solver.addConstraint(
          new kiwi.Constraint(
            expr,
            kiwi.Operator.Eq,
            vars[0],
            kiwi.Strength.required
          )
        );
      }
    }
    solver.updateVariables();
    if (debug2.enabled) {
      console.log("Solution to layout constraints:");
      console.table(
        Object.entries(kVars).map(([key, kvar]) => ({
          var: key,
          val: kvar.value()
        }))
      );
    }
    const bounds = {
      left: Infinity,
      right: -Infinity,
      top: -Infinity,
      bottom: Infinity
    };
    for (const {
      selector,
      bounds: { width, height }
    } of involvedComponents) {
      const kvx = getKVar(`${selector}_x`);
      const kvy = getKVar(`${selector}_y`);
      const newLeft = kvx.value() - width / 2;
      const newRight = kvx.value() + width / 2;
      const newTop = kvy.value() + height / 2;
      const newBottom = kvy.value() - height / 2;
      bounds.left = Math.min(bounds.left, newLeft);
      bounds.right = Math.max(bounds.right, newRight);
      bounds.top = Math.max(bounds.top, newTop);
      bounds.bottom = Math.min(bounds.bottom, newBottom);
    }
    const globalOffset = {
      x: -(bounds.right + bounds.left) / 2,
      y: -(bounds.top + bounds.bottom) / 2
    };
    const containerPos = this.getPrimitiveContainer()._getGlobalPcbPositionBeforeLayout();
    globalOffset.x += containerPos.x;
    globalOffset.y += containerPos.y;
    for (const { component, selector } of involvedComponents) {
      const kvx = getKVar(`${selector}_x`);
      const kvy = getKVar(`${selector}_y`);
      component._setPositionFromLayout({
        x: kvx.value() + globalOffset.x,
        y: kvy.value() + globalOffset.y
      });
    }
  }
};

// lib/components/base-components/NormalComponent/NormalComponent__getMinimumFlexContainerSize.ts
import { getBoundsFromPoints as getBoundsFromPoints2 } from "@tscircuit/math-utils";
var NormalComponent__getMinimumFlexContainerSize = (component) => {
  const { db } = component.root;
  if (component.pcb_component_id) {
    const pcbComponent = db.pcb_component.get(component.pcb_component_id);
    if (!pcbComponent) return null;
    return {
      width: pcbComponent.width,
      height: pcbComponent.height
    };
  }
  if (component.pcb_group_id) {
    const pcbGroup = db.pcb_group.get(
      component.pcb_group_id
    );
    if (!pcbGroup) return null;
    if (pcbGroup.outline && pcbGroup.outline.length > 0) {
      const bounds = getBoundsFromPoints2(pcbGroup.outline);
      if (!bounds) return null;
      return {
        width: bounds.maxX - bounds.minX,
        height: bounds.maxY - bounds.minY
      };
    }
    return {
      width: pcbGroup.width ?? 0,
      height: pcbGroup.height ?? 0
    };
  }
  return null;
};

// lib/components/base-components/NormalComponent/NormalComponent__repositionOnPcb.ts
import {
  repositionPcbComponentTo,
  repositionPcbGroupTo
} from "@tscircuit/circuit-json-util";
var NormalComponent__repositionOnPcb = (component, position) => {
  const { db } = component.root;
  const allCircuitJson = db.toArray();
  if (component.pcb_component_id) {
    repositionPcbComponentTo(
      allCircuitJson,
      component.pcb_component_id,
      position
    );
    return;
  }
  if (component.source_group_id) {
    repositionPcbGroupTo(
      allCircuitJson,
      component.source_group_id,
      position
    );
    return;
  }
  throw new Error(
    `Cannot reposition component ${component.getString()}: no pcb_component_id or source_group_id`
  );
};

// lib/components/base-components/NormalComponent/NormalComponent_doInitialCheckRefDesConvention.ts
var defaultExpectedPrefixesByFtype = {
  simple_ammeter: ["A"],
  simple_battery: ["B", "BT"],
  simple_capacitor: ["C"],
  simple_chip: ["U", "IC"],
  simple_connector: ["J", "P", "CONN"],
  simple_crystal: ["Y", "X"],
  simple_current_source: ["I", "IS"],
  simple_diode: ["D"],
  simple_fuse: ["F"],
  simple_inductor: ["L", "FB"],
  simple_interconnect: ["ICN"],
  simple_led: ["D", "LED"],
  simple_mosfet: ["Q"],
  simple_op_amp: ["U", "IC"],
  simple_pin_header: ["J", "P", "JP"],
  simple_pinout: ["J", "P"],
  simple_potentiometer: ["RV", "RP"],
  simple_power_source: ["B", "BT", "V", "VS"],
  simple_push_button: ["SW", "S"],
  simple_resistor: ["R"],
  simple_resonator: ["Y", "X"],
  simple_switch: ["SW", "S"],
  simple_test_point: ["TP"],
  simple_transistor: ["Q"],
  simple_voltage_probe: ["VP"],
  simple_voltage_source: ["V", "VS"]
};
var getDefaultExpectedRefDesPrefixesForFtype = (ftype) => {
  if (!ftype) return void 0;
  return defaultExpectedPrefixesByFtype[ftype];
};
var getRefDesPrefix = (refDes) => refDes.match(/^[A-Za-z]+/)?.[0]?.toUpperCase();
var chipRefDesRecommendations = [
  { prefix: "J", recommendation: "a <connector /> or <jumper />" },
  { prefix: "Q", recommendation: "a <transistor />" },
  { prefix: "C", recommendation: "a <capacitor />" },
  { prefix: "R", recommendation: "a <resistor />" },
  { prefix: "L", recommendation: "an <inductor />" },
  { prefix: "Y", recommendation: "a <crystal />" },
  { prefix: "X", recommendation: "a <crystal />" },
  { prefix: "F", recommendation: "a <fuse />" },
  { prefix: "S", recommendation: "a <switch /> or <pushbutton />" },
  { prefix: "TP", recommendation: "a <testpoint />" }
];
var NormalComponent_doInitialCheckRefDesConvention = (component) => {
  const { db } = component.root;
  if (!component.source_component_id) return;
  const sourceComponent = db.source_component.get(component.source_component_id);
  if (!sourceComponent?.name || !sourceComponent.ftype) return;
  const actualPrefix = getRefDesPrefix(sourceComponent.name);
  const chipRefDesRecommendation = component.componentName === "Chip" && sourceComponent.ftype === "simple_chip" && actualPrefix !== void 0 ? chipRefDesRecommendations.find(
    ({ prefix }) => actualPrefix.startsWith(prefix)
  ) : void 0;
  const expectedPrefixes = component.getRefDesPrefixes() ?? (chipRefDesRecommendation ? getDefaultExpectedRefDesPrefixesForFtype("simple_chip") : void 0);
  if (!expectedPrefixes || expectedPrefixes.length === 0) return;
  if (actualPrefix && expectedPrefixes.includes(actualPrefix)) return;
  const expectedPrefixMessage = expectedPrefixes.length === 1 ? expectedPrefixes[0] : `one of ${expectedPrefixes.join(", ")}`;
  const message = chipRefDesRecommendation ? `The "${chipRefDesRecommendation.prefix}" prefix is being used with a <chip />, try using it with ${chipRefDesRecommendation.recommendation}` : `Component ${sourceComponent.name} has ftype="${sourceComponent.ftype}" but reference designator should start with ${expectedPrefixMessage}`;
  db.insert({
    type: "source_refdes_convention_warning",
    warning_type: "source_refdes_convention_warning",
    message,
    source_component_id: sourceComponent.source_component_id,
    refdes: sourceComponent.name,
    source_component_ftype: sourceComponent.ftype,
    expected_prefixes: expectedPrefixes,
    actual_prefix: actualPrefix,
    subcircuit_id: sourceComponent.subcircuit_id
  });
};

// lib/components/base-components/NormalComponent/NormalComponent_doInitialPartOrientationAnalysis.ts
import { analyzePcbPin1Location } from "@tscircuit/circuit-json-util";
import {
  pcb_pin1_location,
  source_component_misconfigured_error
} from "circuit-json";
var pendingSupplierPartOrientationAnalyses = /* @__PURE__ */ new WeakMap();
var getPendingSupplierPartOrientationAnalyses = (partsEngine) => {
  const existing = pendingSupplierPartOrientationAnalyses.get(partsEngine);
  if (existing) return existing;
  const pendingAnalyses = /* @__PURE__ */ new Map();
  pendingSupplierPartOrientationAnalyses.set(partsEngine, pendingAnalyses);
  return pendingAnalyses;
};
var ANODE_HINTS = /* @__PURE__ */ new Set(["a", "anode", "pos", "positive", "+"]);
var CATHODE_HINTS = /* @__PURE__ */ new Set(["c", "k", "cathode", "neg", "negative", "-"]);
var normalizePolarityHint = (hint) => hint.toLowerCase().replace(/[^a-z0-9+-]/g, "");
var getPin1SourcePort = (circuitJson, sourceComponentId) => {
  const sourcePorts = circuitJson.filter(
    (element) => element.type === "source_port" && (!sourceComponentId || element.source_component_id === sourceComponentId)
  );
  return sourcePorts.find((port) => port.pin_number === 1) ?? sourcePorts.find(
    (port) => [port.name, ...port.port_hints ?? []].map(normalizePolarityHint).includes("pin1")
  ) ?? null;
};
var getPin1Polarity = (sourcePort) => {
  if (!sourcePort) return null;
  const normalizedHints = [
    sourcePort.name,
    ...sourcePort.port_hints ?? []
  ].map(normalizePolarityHint);
  const hasAnodeHint = normalizedHints.some((hint) => ANODE_HINTS.has(hint));
  const hasCathodeHint = normalizedHints.some((hint) => CATHODE_HINTS.has(hint));
  if (hasAnodeHint === hasCathodeHint) return null;
  return hasAnodeHint ? "anode" : "cathode";
};
var getSupplierPartCandidates = (supplierPartNumbers) => {
  if (!supplierPartNumbers) return [];
  const candidates = [];
  for (const supplierName of Object.keys(
    supplierPartNumbers
  )) {
    const supplierPartNumber = supplierPartNumbers[supplierName]?.[0];
    if (supplierPartNumber) {
      candidates.push({ supplierName, supplierPartNumber });
    }
  }
  return candidates;
};
var getExplicitPcbPin1Location = (footprint) => {
  if (typeof footprint !== "string") return null;
  const match = footprint.match(
    /(?:^|_)pin1location\((leftside|rightside|topside|bottomside),(left|right|top|bottom)\)(?:_|$)/i
  );
  if (!match) return null;
  const result = pcb_pin1_location.safeParse(
    `${match[1].toLowerCase()}_${match[2].toLowerCase()}`
  );
  return result.success ? result.data : null;
};
var getUnrotatedLocalPcbElements = ({
  component,
  pcbComponent,
  pcbElements
}) => {
  const rotationRadians = -pcbComponent.rotation * Math.PI / 180;
  const cos = Math.cos(rotationRadians);
  const sin = Math.sin(rotationRadians);
  const isFlipped = isFootprintFlipped({
    componentLayer: pcbComponent.layer,
    originalLayer: component._getFootprintOriginalLayer()
  });
  const toLocalPoint = (point6) => {
    const x = point6.x - pcbComponent.center.x;
    const placedY = point6.y - pcbComponent.center.y;
    const y = isFlipped ? -placedY : placedY;
    return {
      x: x * cos - y * sin,
      y: x * sin + y * cos
    };
  };
  return pcbElements.map((pcbElement) => {
    const localElement = structuredClone(pcbElement);
    if (typeof localElement.x === "number" && typeof localElement.y === "number") {
      const localCenter = toLocalPoint(localElement);
      localElement.x = localCenter.x;
      localElement.y = localCenter.y;
    }
    if (localElement.points) {
      localElement.points = localElement.points.map(toLocalPoint);
    }
    return localElement;
  });
};
var getSupplierPartOrientationCacheKey = ({
  supplierName,
  supplierPartNumber
}) => `part-orientation-analysis:v2:${supplierName}:${supplierPartNumber}`;
var readCachedSupplierPartOrientationAnalysis = async ({
  cacheKey,
  component
}) => {
  const cachedValue = await component.root?.platform?.localCacheEngine?.getItem(cacheKey);
  if (!cachedValue) return { cacheHit: false };
  try {
    const cached = JSON.parse(
      cachedValue
    );
    const pin1LocationResult = pcb_pin1_location.safeParse(cached.pin1_location);
    const pin1Location = cached.pin1_location === null ? null : pin1LocationResult.success ? pin1LocationResult.data : void 0;
    const pin1Polarity = cached.pin1_polarity === null || cached.pin1_polarity === "anode" || cached.pin1_polarity === "cathode" ? cached.pin1_polarity : void 0;
    if (pin1Location === void 0 || pin1Polarity === void 0) {
      return { cacheHit: false };
    }
    return {
      cacheHit: true,
      analysis: { pin1Location, pin1Polarity }
    };
  } catch {
    return { cacheHit: false };
  }
};
var analyzeSupplierPartOrientation = async ({
  component,
  partsEngine,
  supplierPartCandidate
}) => {
  const cacheKey = getSupplierPartOrientationCacheKey(supplierPartCandidate);
  const cached = await readCachedSupplierPartOrientationAnalysis({
    cacheKey,
    component
  });
  if (cached.cacheHit) return cached.analysis;
  const pendingAnalyses = getPendingSupplierPartOrientationAnalyses(partsEngine);
  const existingAnalysis = pendingAnalyses.get(cacheKey);
  if (existingAnalysis) return existingAnalysis;
  const analysis = (async () => {
    const supplierCircuitJson = await Promise.resolve(
      partsEngine.fetchPartCircuitJson({
        supplierPartNumber: supplierPartCandidate.supplierPartNumber,
        platformFetch: component.root?.platform?.platformFetch
      })
    );
    if (!supplierCircuitJson?.length) {
      return { pin1Location: null, pin1Polarity: null };
    }
    const pin1Location = analyzePcbPin1Location(supplierCircuitJson);
    const pin1Polarity = getPin1Polarity(getPin1SourcePort(supplierCircuitJson));
    try {
      await component.root?.platform?.localCacheEngine?.setItem(
        cacheKey,
        JSON.stringify({
          pin1_location: pin1Location,
          pin1_polarity: pin1Polarity
        })
      );
    } catch {
    }
    return { pin1Location, pin1Polarity };
  })().finally(() => {
    pendingAnalyses.delete(cacheKey);
  });
  pendingAnalyses.set(cacheKey, analysis);
  return analysis;
};
var NormalComponent_doInitialPartOrientationAnalysis = (component) => {
  if (!component.root?.platform?.enablePartOrientationAnalysis) return;
  if (component.root.pcbDisabled || component.props.doNotPlace) return;
  if (!component.pcb_component_id) return;
  const { db } = component.root;
  const pcbComponent = db.pcb_component.get(component.pcb_component_id);
  if (!pcbComponent) return;
  const pcbElements = [
    ...db.pcb_smtpad.list({ pcb_component_id: component.pcb_component_id }),
    ...db.pcb_plated_hole.list({
      pcb_component_id: component.pcb_component_id
    })
  ];
  const localPin1Location = getExplicitPcbPin1Location(component.resolveFootprint()) ?? analyzePcbPin1Location(
    getUnrotatedLocalPcbElements({
      component,
      pcbComponent,
      pcbElements
    })
  );
  if (localPin1Location) {
    db.pcb_component.update(component.pcb_component_id, {
      pin1_location: localPin1Location
    });
  }
  if (component.getInheritedProperty("bomDisabled")) return;
  if (component.getInheritedProperty("partsEngineDisabled")) return;
  const partsEngine = component.getInheritedProperty("partsEngine");
  if (!partsEngine?.fetchPartCircuitJson) return;
  const sourceComponent = db.source_component.get(
    component.source_component_id
  );
  const shouldCheckPin1Polarity = sourceComponent?.ftype === "simple_diode" || sourceComponent?.ftype === "simple_led";
  const localPin1SourcePort = shouldCheckPin1Polarity ? getPin1SourcePort(
    db.source_port.list(),
    component.source_component_id ?? void 0
  ) : null;
  const localPin1Polarity = getPin1Polarity(localPin1SourcePort);
  const supplierPartCandidates = getSupplierPartCandidates(
    sourceComponent?.supplier_part_numbers
  );
  if (supplierPartCandidates.length === 0) return;
  if (component._hasStartedPartOrientationAnalysis) return;
  component._hasStartedPartOrientationAnalysis = true;
  component._queueAsyncEffect("analyze-part-orientation", async () => {
    const supplierPin1LocationMap = {};
    for (const supplierPartCandidate of supplierPartCandidates) {
      try {
        const { pin1Location: supplierPin1Location, pin1Polarity } = await analyzeSupplierPartOrientation({
          component,
          partsEngine,
          supplierPartCandidate
        });
        if (supplierPin1Location) {
          supplierPin1LocationMap[supplierPartCandidate.supplierName] = supplierPin1Location;
        }
        if (localPin1Polarity && pin1Polarity && localPin1Polarity !== pin1Polarity) {
          const error = source_component_misconfigured_error.parse({
            type: "source_component_misconfigured_error",
            error_type: "source_component_misconfigured_error",
            message: `${component.getString()} maps pin 1 to the ${localPin1Polarity}, but supplier part ${supplierPartCandidate.supplierName}:${supplierPartCandidate.supplierPartNumber} maps pin 1 to the ${pin1Polarity}. Update pinLabels or use a supplier part with matching diode polarity.`,
            source_component_ids: [component.source_component_id],
            source_port_ids: localPin1SourcePort?.source_port_id ? [localPin1SourcePort.source_port_id] : void 0
          });
          component.root.db.source_component_misconfigured_error.insert(error);
        }
      } catch {
      }
    }
    component._asyncSupplierPin1LocationMap = supplierPin1LocationMap;
    component._markDirty("PartOrientationAnalysis");
  });
};
var NormalComponent_updatePartOrientationAnalysis = (component) => {
  const supplierPin1LocationMap = component._asyncSupplierPin1LocationMap;
  if (!supplierPin1LocationMap || !component.pcb_component_id) return;
  const { db } = component.root;
  const pcbComponent = db.pcb_component.get(component.pcb_component_id);
  if (!pcbComponent || Object.keys(supplierPin1LocationMap).length === 0) return;
  db.pcb_component.update(component.pcb_component_id, {
    supplier_pin1_location_map: {
      ...pcbComponent.supplier_pin1_location_map,
      ...supplierPin1LocationMap
    }
  });
};

// lib/components/base-components/NormalComponent/NormalComponent_doInitialPcbComponentAnchorAlignment.ts
function NormalComponent_doInitialPcbComponentAnchorAlignment(component) {
  if (component.root?.pcbDisabled) return;
  if (!component.pcb_component_id) return;
  const { pcbX, pcbY } = component._parsedProps;
  const pcbPositionAnchor = component.props?.pcbPositionAnchor;
  if (!pcbPositionAnchor) return;
  if (pcbX === void 0 && pcbY === void 0) return;
  const bounds = getBoundsOfPcbComponents(component.children);
  if (bounds.width === 0 || bounds.height === 0) return;
  const center = {
    x: (bounds.minX + bounds.maxX) / 2,
    y: (bounds.minY + bounds.maxY) / 2
  };
  const currentCenter = { ...center };
  let anchorPos = null;
  const ninePointAnchors = /* @__PURE__ */ new Set([
    "center",
    "top_left",
    "top_center",
    "top_right",
    "center_left",
    "center_right",
    "bottom_left",
    "bottom_center",
    "bottom_right"
  ]);
  if (ninePointAnchors.has(pcbPositionAnchor)) {
    const b = {
      left: bounds.minX,
      right: bounds.maxX,
      top: bounds.minY,
      bottom: bounds.maxY
    };
    switch (pcbPositionAnchor) {
      case "center":
        anchorPos = currentCenter;
        break;
      case "top_left":
        anchorPos = { x: b.left, y: b.top };
        break;
      case "top_center":
        anchorPos = { x: currentCenter.x, y: b.top };
        break;
      case "top_right":
        anchorPos = { x: b.right, y: b.top };
        break;
      case "center_left":
        anchorPos = { x: b.left, y: currentCenter.y };
        break;
      case "center_right":
        anchorPos = { x: b.right, y: currentCenter.y };
        break;
      case "bottom_left":
        anchorPos = { x: b.left, y: b.bottom };
        break;
      case "bottom_center":
        anchorPos = { x: currentCenter.x, y: b.bottom };
        break;
      case "bottom_right":
        anchorPos = { x: b.right, y: b.bottom };
        break;
    }
  } else {
    try {
      const port = component.portMap[pcbPositionAnchor];
      if (port) {
        anchorPos = port._getGlobalPcbPositionBeforeLayout();
      }
    } catch {
    }
  }
  if (!anchorPos) return;
  const newCenter = { ...currentCenter };
  if (pcbX !== void 0) newCenter.x += pcbX - anchorPos.x;
  if (pcbY !== void 0) newCenter.y += pcbY - anchorPos.y;
  if (Math.abs(newCenter.x - currentCenter.x) > 1e-6 || Math.abs(newCenter.y - currentCenter.y) > 1e-6) {
    component._repositionOnPcb(newCenter);
  }
}

// lib/components/base-components/NormalComponent/NormalComponent_doInitialPcbFootprintStringRender.ts
import {
  supplierProps
} from "@tscircuit/props";
import {
  circuit_json_footprint_load_error,
  external_footprint_load_error
} from "circuit-json";

// lib/utils/connectors/extractCadModelFromCircuitJson.ts
import { cadModelProp } from "@tscircuit/props";
var extractCadModelFromCircuitJson = (circuitJson) => {
  const cadComponent = circuitJson.find(
    (elm) => elm.type === "cad_component"
  );
  if (!cadComponent) return void 0;
  const cadModelCandidate = {
    stlUrl: cadComponent.model_stl_url,
    objUrl: cadComponent.model_obj_url,
    gltfUrl: cadComponent.model_gltf_url,
    glbUrl: cadComponent.model_glb_url,
    stepUrl: cadComponent.model_step_url,
    wrlUrl: cadComponent.model_wrl_url,
    modelOriginPosition: cadComponent.model_origin_position ?? void 0,
    modelUnitToMmScale: cadComponent.model_unit_to_mm_scale_factor,
    modelBoardNormalDirection: cadComponent.model_board_normal_direction,
    size: cadComponent.size ?? void 0,
    rotationOffset: cadComponent.rotation ?? void 0,
    positionOffset: cadComponent.position ?? void 0,
    showAsTranslucentModel: cadComponent.show_as_translucent_model
  };
  if (!cadModelCandidate.stlUrl && !cadModelCandidate.objUrl && !cadModelCandidate.gltfUrl && !cadModelCandidate.glbUrl && !cadModelCandidate.stepUrl && !cadModelCandidate.wrlUrl && !cadComponent.model_jscad) {
    return void 0;
  }
  if (cadComponent.model_jscad && typeof cadComponent.model_jscad === "object") {
    cadModelCandidate.jscad = cadComponent.model_jscad;
  }
  const parsedCadModel = cadModelProp.safeParse(cadModelCandidate);
  return parsedCadModel.success ? parsedCadModel.data : void 0;
};

// lib/utils/resolveStaticFileImport.ts
import Debug4 from "debug";
var resolveStaticFileImportDebug = Debug4(
  "tscircuit:core:resolveStaticFileImport"
);
async function resolveStaticFileImport(path, platform) {
  if (!path) return path;
  const normalizedPath = path.startsWith("./") ? path.slice(1) : path;
  const resolver = platform?.resolveProjectStaticFileImportUrl;
  if (resolver && (path.startsWith("/") || path.startsWith("./"))) {
    try {
      const resolved = await resolver(normalizedPath);
      if (resolved) return resolved;
    } catch (error) {
      resolveStaticFileImportDebug(
        "failed to resolve static file via platform resolver",
        error
      );
    }
  }
  return constructAssetUrl(normalizedPath, platform?.projectBaseUrl);
}

// lib/components/base-components/NormalComponent/NormalComponent_doInitialPcbFootprintStringRender.ts
import { isValidElement as isReactElement } from "react";

// lib/components/base-components/NormalComponent/utils/isBlobUrl.ts
var isBlobUrl = (s) => s.startsWith("blob:");

// lib/components/base-components/NormalComponent/utils/isHttpUrl.ts
var isHttpUrl = (s) => s.startsWith("http://") || s.startsWith("https://");

// lib/components/base-components/NormalComponent/utils/isStaticAssetPath.ts
var isStaticAssetPath = (s) => s.startsWith("/") || s.startsWith("./");

// lib/components/base-components/NormalComponent/utils/parseLibraryFootprintRef.ts
var parseLibraryFootprintRef = (s) => {
  if (isHttpUrl(s) || isBlobUrl(s)) return null;
  const idx = s.indexOf(":");
  if (idx <= 0) return null;
  const footprintLib = s.slice(0, idx);
  const footprintName = s.slice(idx + 1);
  if (!footprintLib || !footprintName) return null;
  return { footprintLib, footprintName };
};

// lib/components/base-components/NormalComponent/NormalComponent_doInitialPcbFootprintStringRender.ts
var supplierNames = supplierProps.shape.supplierPartNumbers.unwrap().keySchema.options;
var shouldAddOutsideFootprintWrapper = (component) => component.componentName === "Symbol" || component.isSchematicPrimitive;
var getSupplierPartCircuitJsonResolver = (component, footprintLibrary, footprintName) => {
  if (component.getInheritedProperty("partsEngineDisabled")) return;
  const supplierPartNumbers = component.props.supplierPartNumbers;
  const supplierName = supplierNames.find(
    (candidateSupplierName) => candidateSupplierName === footprintLibrary
  );
  if (!supplierName) return;
  const matchingSupplierPartNumbers = supplierPartNumbers?.[supplierName];
  if (!matchingSupplierPartNumbers?.includes(footprintName)) return;
  const partsEngine = component.getInheritedProperty("partsEngine");
  const fetchPartCircuitJson = partsEngine?.fetchPartCircuitJson;
  if (!fetchPartCircuitJson) return;
  return async (supplierPartNumber) => {
    const footprintCircuitJson = await fetchPartCircuitJson({
      supplierPartNumber,
      platformFetch: component.root?.platform?.platformFetch
    });
    return { footprintCircuitJson: footprintCircuitJson ?? [] };
  };
};
var loadFootprintLibraryResultOrThrow = async (footprintLibraryResolver, footprintLibrary, footprintName) => {
  if (!footprintLibraryResolver) {
    throw new Error(
      `No footprint resolver is configured for library "${footprintLibrary}".`
    );
  }
  return footprintLibraryResolver(footprintName);
};
function NormalComponent_doInitialPcbFootprintStringRender(component, queueAsyncEffect) {
  const footprint = component.resolveFootprint();
  if (!footprint) return;
  const { pcbRotation, pcbPinLabels } = component.props;
  const pinLabels = component._resolvePinLabels();
  const fileExtension = getFileExtension(String(footprint));
  const footprintParser = fileExtension ? component.root?.platform?.footprintFileParserMap?.[fileExtension] : null;
  if (typeof footprint === "string" && (isHttpUrl(footprint) || isBlobUrl(footprint) || isStaticAssetPath(footprint)) && footprintParser) {
    if (component._hasStartedFootprintUrlLoad) return;
    component._hasStartedFootprintUrlLoad = true;
    queueAsyncEffect("load-footprint-from-platform-file-parser", async () => {
      const footprintUrl = isHttpUrl(footprint) || isBlobUrl(footprint) ? footprint : await resolveStaticFileImport(footprint, component.root?.platform);
      try {
        const result = await footprintParser.loadFromUrl(footprintUrl);
        const fpComponents = createComponentsFromCircuitJson(
          {
            componentName: component.name,
            componentRotation: pcbRotation,
            footprinterString: footprintUrl,
            pinLabels,
            pcbPinLabels
          },
          result.footprintCircuitJson
        );
        component.addAll(fpComponents);
        component._markDirty("ResolveFootprintPinLabels");
        component._markDirty("InitializePortsFromChildren");
      } catch (err) {
        const db = component.root?.db;
        if (db && component.source_component_id && component.pcb_component_id) {
          const subcircuit = component.getSubcircuit();
          const errorMsg = `${component.getString()} failed to load footprint "${footprintUrl}": ` + (err instanceof Error ? err.message : String(err));
          const errorObj = external_footprint_load_error.parse({
            type: "external_footprint_load_error",
            message: errorMsg,
            pcb_component_id: component.pcb_component_id,
            source_component_id: component.source_component_id,
            subcircuit_id: subcircuit.subcircuit_id ?? void 0,
            pcb_group_id: component.getGroup()?.pcb_group_id ?? void 0,
            footprinter_string: footprintUrl
          });
          db.external_footprint_load_error.insert(errorObj);
        }
        throw err;
      }
    });
    return;
  }
  if (typeof footprint === "string" && isHttpUrl(footprint)) {
    if (component._hasStartedFootprintUrlLoad) return;
    component._hasStartedFootprintUrlLoad = true;
    const url = footprint;
    queueAsyncEffect("load-footprint-url", async () => {
      try {
        const res = await fetch(url);
        if (!res.ok) {
          throw new Error(`Failed to fetch footprint: ${res.status}`);
        }
        const soup = await res.json();
        const fpComponents = createComponentsFromCircuitJson(
          {
            componentName: component.name,
            componentRotation: pcbRotation,
            footprinterString: url,
            pinLabels,
            pcbPinLabels
          },
          soup
        );
        component.addAll(fpComponents);
        component._markDirty("ResolveFootprintPinLabels");
        component._markDirty("InitializePortsFromChildren");
      } catch (err) {
        const db = component.root?.db;
        if (db && component.source_component_id && component.pcb_component_id) {
          const subcircuit = component.getSubcircuit();
          const errorMsg = `${component.getString()} failed to load external footprint "${url}": ` + (err instanceof Error ? err.message : String(err));
          const errorObj = external_footprint_load_error.parse({
            type: "external_footprint_load_error",
            message: errorMsg,
            pcb_component_id: component.pcb_component_id,
            source_component_id: component.source_component_id,
            subcircuit_id: subcircuit.subcircuit_id ?? void 0,
            pcb_group_id: component.getGroup()?.pcb_group_id ?? void 0,
            footprinter_string: url
          });
          db.external_footprint_load_error.insert(errorObj);
        }
        throw err;
      }
    });
    return;
  }
  if (typeof footprint === "string" && isBlobUrl(footprint)) {
    return;
  }
  if (typeof footprint === "string") {
    const libRef = parseLibraryFootprintRef(footprint);
    if (!libRef) return;
    if (component._hasStartedFootprintUrlLoad) return;
    component._hasStartedFootprintUrlLoad = true;
    const platform = component.root?.platform;
    const footprintLibraryEntry = platform?.footprintLibraryMap?.[libRef.footprintLib];
    const configuredFootprintLibraryResolver = typeof footprintLibraryEntry === "function" ? footprintLibraryEntry : void 0;
    const supplierPartCircuitJsonResolver = getSupplierPartCircuitJsonResolver(
      component,
      libRef.footprintLib,
      libRef.footprintName
    );
    const footprintLibraryResolver = configuredFootprintLibraryResolver ?? supplierPartCircuitJsonResolver;
    queueAsyncEffect("load-lib-footprint", async () => {
      try {
        const result = await loadFootprintLibraryResultOrThrow(
          footprintLibraryResolver,
          libRef.footprintLib,
          libRef.footprintName
        );
        let circuitJson = null;
        if (Array.isArray(result)) {
          circuitJson = result;
        } else if (Array.isArray(result.footprintCircuitJson)) {
          circuitJson = result.footprintCircuitJson;
        }
        if (!circuitJson || circuitJson.length === 0) {
          throw new Error(
            `Footprint resolver returned no circuit elements for "${footprint}".`
          );
        }
        const fpComponents = createComponentsFromCircuitJson(
          {
            componentName: component.name,
            componentRotation: pcbRotation,
            footprinterString: footprint,
            pinLabels,
            pcbPinLabels
          },
          circuitJson
        );
        const fpWrapper = new Footprint({ src: footprint });
        const componentsOutsideFootprint = [];
        for (const c of fpComponents) {
          if (shouldAddOutsideFootprintWrapper(c)) {
            componentsOutsideFootprint.push(c);
          } else {
            fpWrapper.add(c);
          }
        }
        component.add(fpWrapper);
        component.addAll(componentsOutsideFootprint);
        component._asyncFootprintCadModel = !Array.isArray(result) && result.cadModel || extractCadModelFromCircuitJson(circuitJson);
        for (const child of component.children) {
          if (child.componentName === "Port") {
            child._markDirty?.("PcbPortRender");
          }
        }
        component._markDirty("ResolveFootprintPinLabels");
        component._markDirty("InitializePortsFromChildren");
      } catch (err) {
        const db = component.root?.db;
        if (db && component.source_component_id && component.pcb_component_id) {
          const subcircuit = component.getSubcircuit();
          const errorMsg = `${component.getString()} failed to load external footprint "${footprint}": ` + (err instanceof Error ? err.message : String(err));
          const errorObj = external_footprint_load_error.parse({
            type: "external_footprint_load_error",
            message: errorMsg,
            pcb_component_id: component.pcb_component_id,
            source_component_id: component.source_component_id,
            subcircuit_id: subcircuit.subcircuit_id ?? void 0,
            pcb_group_id: component.getGroup()?.pcb_group_id ?? void 0,
            footprinter_string: footprint
          });
          db.external_footprint_load_error.insert(errorObj);
        }
        throw err;
      }
    });
    return;
  }
  if (!isReactElement(footprint) && footprint.componentName === "Footprint") {
    component.add(footprint);
  }
  if (Array.isArray(footprint) && !isReactElement(footprint) && footprint.length > 0) {
    try {
      const fpComponents = createComponentsFromCircuitJson(
        {
          componentName: component.name,
          componentRotation: pcbRotation,
          footprinterString: "",
          pinLabels,
          pcbPinLabels
        },
        footprint
      );
      component.addAll(fpComponents);
    } catch (err) {
      const db = component.root?.db;
      if (db && component.source_component_id && component.pcb_component_id) {
        const subcircuit = component.getSubcircuit();
        const errorMsg = `${component.getString()} failed to load json footprint: ` + (err instanceof Error ? err.message : String(err));
        const errorObj = circuit_json_footprint_load_error.parse({
          type: "circuit_json_footprint_load_error",
          message: errorMsg,
          pcb_component_id: component.pcb_component_id,
          source_component_id: component.source_component_id,
          subcircuit_id: subcircuit.subcircuit_id ?? void 0,
          pcb_group_id: component.getGroup()?.pcb_group_id ?? void 0
        });
        db.circuit_json_footprint_load_error.insert(errorObj);
      }
      throw err;
    }
    return;
  }
}

// lib/components/base-components/NormalComponent/NormalComponent_doInitialResolveFootprintPinLabels.ts
function NormalComponent_doInitialResolveFootprintPinLabels(component) {
  const pinLabelsBeforeFootprint = component._impliedFootprintPinLabels ?? {};
  const pinLabels = {};
  const portsFromFootprint = component.getPortsFromFootprint({
    allowImplicitPinNumbers: !component._parsedProps.pinLabels
  });
  for (const port of portsFromFootprint) {
    const pinNumber = port._parsedProps.pinNumber;
    if (pinNumber === void 0) continue;
    const genericPinLabels = /* @__PURE__ */ new Set([`pin${pinNumber}`, `${pinNumber}`]);
    const labels = port.getNameAndAliases().filter((label) => !genericPinLabels.has(label));
    if (labels.length > 0) {
      pinLabels[`pin${pinNumber}`] = labels;
    }
  }
  const impliedFootprintPinLabels = { ...pinLabels };
  for (const [pinKey, labels] of Object.entries(pinLabelsBeforeFootprint)) {
    impliedFootprintPinLabels[pinKey] = Array.isArray(labels) ? labels : [labels];
  }
  component._impliedFootprintPinLabels = impliedFootprintPinLabels;
  if (Object.keys(component._impliedFootprintPinLabels).length === 0) {
    return;
  }
  for (const child of component.children) {
    if (child.componentName !== "Port") continue;
    const port = child;
    const pinNumber = port._parsedProps.pinNumber;
    if (pinNumber === void 0) continue;
    const pinKey = `pin${pinNumber}`;
    const labels = component._impliedFootprintPinLabels[pinKey];
    if (!labels) continue;
    const labelList = Array.isArray(labels) ? labels : [labels];
    const existingLabels = port.getNameAndAliases();
    for (const label of labelList) {
      if (!existingLabels.includes(label)) {
        port.externallyAddedAliases.push(label);
      }
    }
  }
}

// lib/components/base-components/NormalComponent/NormalComponent_doInitialSchematicComponentRender.ts
import { schematic_manual_edit_conflict_warning } from "circuit-json";
import { isValidElement as isReactElement2 } from "react";
function NormalComponent_doInitialSchematicComponentRender(component) {
  if (component.root?.schematicDisabled) return;
  if (component.getCollapsedSchematicBoxAncestor()) return;
  const { db } = component.root;
  if (component._invalidPinLabelMessages?.length && component.root?.db) {
    for (const message of component._invalidPinLabelMessages) {
      let property_name = "pinLabels";
      const match = message.match(/^Invalid pin label:\s*([^=]+)=\s*'([^']+)'/);
      if (match) {
        const label = match[2];
        property_name = `pinLabels['${label}']`;
      }
      component.root.db.source_property_ignored_warning.insert({
        source_component_id: component.source_component_id,
        property_name,
        message,
        error_type: "source_property_ignored_warning"
      });
    }
  }
  if (component._parsedProps.schPinSpacing !== void 0 && component.root?.db) {
    component.root.db.source_property_ignored_warning.insert({
      source_component_id: component.source_component_id,
      property_name: "schPinSpacing",
      error_type: "source_property_ignored_warning",
      message: "schPinSpacing is deprecated and will be ignored. Pin spacing is always 0.2."
    });
  }
  const { schematicSymbolName } = component.config;
  const { _parsedProps: props } = component;
  const hasSymbolChild = component.children.some(
    (c) => c.componentName === "Symbol"
  );
  if (props.symbol && isReactElement2(props.symbol) || hasSymbolChild) {
    component._doInitialSchematicComponentRenderWithReactSymbol(props.symbol);
  } else if (schematicSymbolName) {
    component._doInitialSchematicComponentRenderWithSymbol();
  } else {
    const dimensions = component._getSchematicBoxDimensions();
    if (dimensions) {
      component._doInitialSchematicComponentRenderWithSchematicBoxDimensions();
    }
  }
  const manualPlacement = component.getSubcircuit()?._getSchematicManualPlacementForComponent(component);
  if (component.schematic_component_id && (component.props.schX !== void 0 || component.props.schY !== void 0) && !!manualPlacement) {
    if (!component.schematic_component_id) {
      return;
    }
    const warning = schematic_manual_edit_conflict_warning.parse({
      type: "schematic_manual_edit_conflict_warning",
      schematic_manual_edit_conflict_warning_id: `schematic_manual_edit_conflict_${component.source_component_id}`,
      message: `${component.getString()} has both manual placement and prop coordinates. schX and schY will be used. Remove schX/schY or clear the manual placement.`,
      schematic_component_id: component.schematic_component_id,
      source_component_id: component.source_component_id,
      subcircuit_id: component.getSubcircuit()?.subcircuit_id
    });
    db.schematic_manual_edit_conflict_warning.insert(warning);
  }
  const schSheetName = component._parsedProps?.schSheetName ?? component.props.schSheetName;
  if (schSheetName && component.source_component_id && !component._resolveSchematicSheetId()) {
    db.source_property_ignored_warning.insert({
      source_component_id: component.source_component_id,
      property_name: "schSheetName",
      error_type: "source_property_ignored_warning",
      subcircuit_id: component.getSubcircuit()?.subcircuit_id ?? void 0,
      message: `${component.getString()} references schSheetName "${schSheetName}", which does not match any schematic sheet and will be ignored.`
    });
  }
}

// lib/components/base-components/NormalComponent/utils/getPcbTextBounds.ts
function getPcbTextBounds(text) {
  const fontSize = text.font_size;
  const textWidth = text.text.length * fontSize * 0.6;
  const textHeight = fontSize;
  const anchorAlignment = text.anchor_alignment || "center";
  let centerX = text.anchor_position.x;
  let centerY = text.anchor_position.y;
  switch (anchorAlignment) {
    case "top_left":
      centerX = text.anchor_position.x + textWidth / 2;
      centerY = text.anchor_position.y + textHeight / 2;
      break;
    case "top_center":
      centerX = text.anchor_position.x;
      centerY = text.anchor_position.y + textHeight / 2;
      break;
    case "top_right":
      centerX = text.anchor_position.x - textWidth / 2;
      centerY = text.anchor_position.y + textHeight / 2;
      break;
    case "center_left":
      centerX = text.anchor_position.x + textWidth / 2;
      centerY = text.anchor_position.y;
      break;
    case "center":
      centerX = text.anchor_position.x;
      centerY = text.anchor_position.y;
      break;
    case "center_right":
      centerX = text.anchor_position.x - textWidth / 2;
      centerY = text.anchor_position.y;
      break;
    case "bottom_left":
      centerX = text.anchor_position.x + textWidth / 2;
      centerY = text.anchor_position.y - textHeight / 2;
      break;
    case "bottom_center":
      centerX = text.anchor_position.x;
      centerY = text.anchor_position.y - textHeight / 2;
      break;
    case "bottom_right":
      centerX = text.anchor_position.x - textWidth / 2;
      centerY = text.anchor_position.y - textHeight / 2;
      break;
    default:
      centerX = text.anchor_position.x;
      centerY = text.anchor_position.y;
      break;
  }
  return {
    x: centerX - textWidth / 2,
    y: centerY - textHeight / 2,
    width: textWidth,
    height: textHeight
  };
}

// lib/components/base-components/NormalComponent/NormalComponent_doInitialSilkscreenOverlapAdjustment.ts
import {
  getBoundingBox,
  doBoundsOverlap
} from "@tscircuit/math-utils";
function NormalComponent_doInitialSilkscreenOverlapAdjustment(component) {
  if (!component._adjustSilkscreenTextAutomatically) {
    return;
  }
  if (component.root?.pcbDisabled || !component.pcb_component_id) {
    return;
  }
  const { db } = component.root;
  const componentBounds = component._getPcbCircuitJsonBounds();
  const componentCenter = componentBounds.center;
  let silkscreenTexts = db.pcb_silkscreen_text.list({
    pcb_component_id: component.pcb_component_id
  }).filter((text) => text.text === component.name);
  if (silkscreenTexts.length === 0) {
    return;
  }
  const subcircuit = component.getSubcircuit();
  const allNormalComponents = subcircuit.selectAll("[_isNormalComponent=true]").filter((comp) => comp !== component && comp.pcb_component_id);
  const obstacleBounds = allNormalComponents.map((comp) => {
    const bounds = comp._getPcbCircuitJsonBounds();
    const box = {
      center: bounds.center,
      width: bounds.width,
      height: bounds.height
    };
    return getBoundingBox(box);
  });
  for (const silkscreenText of silkscreenTexts) {
    const currentPosition = silkscreenText.anchor_position;
    const textBounds = getPcbTextBounds(silkscreenText);
    const textBox = {
      center: {
        x: textBounds.x + textBounds.width / 2,
        y: textBounds.y + textBounds.height / 2
      },
      width: textBounds.width,
      height: textBounds.height
    };
    const textBoundsBox = getBoundingBox(textBox);
    const hasOverlap = obstacleBounds.some(
      (obstacle) => doBoundsOverlap(textBoundsBox, obstacle)
    );
    if (!hasOverlap) {
      continue;
    }
    const flippedX = 2 * componentCenter.x - currentPosition.x;
    const flippedY = 2 * componentCenter.y - currentPosition.y;
    const flippedTextBox = {
      center: { x: flippedX, y: flippedY },
      width: textBounds.width,
      height: textBounds.height
    };
    const flippedTextBounds = getBoundingBox(flippedTextBox);
    const flippedHasOverlap = obstacleBounds.some(
      (obstacle) => doBoundsOverlap(flippedTextBounds, obstacle)
    );
    if (!flippedHasOverlap) {
      db.pcb_silkscreen_text.update(silkscreenText.pcb_silkscreen_text_id, {
        anchor_position: {
          x: flippedX,
          y: flippedY
        }
      });
    }
  }
}

// lib/components/base-components/NormalComponent/NormalComponent_doInitialSourceDesignRuleChecks.ts
var NormalComponent_doInitialSourceDesignRuleChecks = (component) => {
  const { db } = component.root;
  if (!component.source_component_id) return;
  const ports = component.selectAll("port");
  const traces = db.source_trace.list();
  const connected = /* @__PURE__ */ new Set();
  for (const trace of traces) {
    for (const id of trace.connected_source_port_ids) {
      connected.add(id);
    }
  }
  const internalGroups = component._getInternallyConnectedPins();
  for (const group of internalGroups) {
    if (group.some((p) => p.source_port_id && connected.has(p.source_port_id))) {
      for (const p of group) {
        if (p.source_port_id) connected.add(p.source_port_id);
      }
    }
  }
  for (const port of ports) {
    if (!port.source_port_id) continue;
    if (!shouldCheckPortForMissingTrace(component, port)) continue;
    const sourcePort = db.source_port.get(port.source_port_id);
    if (sourcePort?.do_not_connect) continue;
    if (connected.has(port.source_port_id)) continue;
    db.source_pin_missing_trace_warning.insert({
      message: `Port ${port.getNameAndAliases()[0]} on ${component.props.name} is missing a trace`,
      source_component_id: component.source_component_id,
      source_port_id: port.source_port_id,
      subcircuit_id: component.getSubcircuit().subcircuit_id ?? void 0,
      warning_type: "source_pin_missing_trace_warning"
    });
  }
};
var shouldCheckPortForMissingTrace = (component, port) => {
  if (component.config.componentName === "Interconnect") {
    return false;
  }
  if (component.config.componentName === "Chip") {
    const pinAttributes = component.props.pinAttributes;
    if (!pinAttributes) return false;
    for (const alias of port.getNameAndAliases()) {
      const attrs = pinAttributes[alias];
      if (attrs?.requiresPower || attrs?.requiresGround || attrs?.requiresVoltage !== void 0) {
        return true;
      }
    }
    return false;
  }
  return true;
};

// lib/components/base-components/NormalComponent/NormalComponent_doInitialSupplierFootprintMismatchWarning.ts
import { getBoundsOfPcbElements } from "@tscircuit/circuit-json-util";
import {
  supplierProps as supplierProps2
} from "@tscircuit/props";
import {
  source_part_not_found_warning,
  supplier_footprint_mismatch_warning
} from "circuit-json";
var SUPPLIER_FOOTPRINT_IOU_WARNING_THRESHOLD = 0.8;
var supplierNames2 = supplierProps2.shape.supplierPartNumbers.unwrap().keySchema.options;
var isCopperElement = (elm) => elm.type === "pcb_smtpad" || elm.type === "pcb_plated_hole";
var isValidBounds = (bounds) => Number.isFinite(bounds.minX) && Number.isFinite(bounds.minY) && Number.isFinite(bounds.maxX) && Number.isFinite(bounds.maxY) && bounds.maxX > bounds.minX && bounds.maxY > bounds.minY;
var getCopperBounds = (elements) => {
  const copperElements = elements.filter(isCopperElement);
  if (copperElements.length === 0) return null;
  const bounds = getBoundsOfPcbElements(copperElements);
  if (!isValidBounds(bounds)) return null;
  return bounds;
};
var getBoundsSize = (bounds) => ({
  width: bounds.maxX - bounds.minX,
  height: bounds.maxY - bounds.minY
});
var getCenteredBoundsIou = (boundsA, boundsB, opts) => {
  const sizeA = getBoundsSize(boundsA);
  const sizeB = getBoundsSize(boundsB);
  const bWidth = opts?.rotateB ? sizeB.height : sizeB.width;
  const bHeight = opts?.rotateB ? sizeB.width : sizeB.height;
  const intersectionWidth = Math.min(sizeA.width, bWidth);
  const intersectionHeight = Math.min(sizeA.height, bHeight);
  const intersectionArea = intersectionWidth * intersectionHeight;
  const unionArea = sizeA.width * sizeA.height + bWidth * bHeight - intersectionArea;
  if (unionArea <= 0) return 0;
  return intersectionArea / unionArea;
};
var getBestBoundsIou = (localBounds, supplierBounds) => Math.max(
  getCenteredBoundsIou(localBounds, supplierBounds),
  getCenteredBoundsIou(localBounds, supplierBounds, { rotateB: true })
);
var getSupplierPartCandidates2 = (supplierPartNumbers) => {
  if (!supplierPartNumbers) return [];
  const candidates = [];
  for (const supplierName of supplierNames2) {
    const partNumbers = supplierPartNumbers[supplierName];
    if (!Array.isArray(partNumbers)) continue;
    for (const supplierPartNumber of partNumbers) {
      if (!supplierPartNumber) continue;
      candidates.push({ supplierName, supplierPartNumber });
    }
  }
  return candidates;
};
function NormalComponent_doInitialSupplierFootprintMismatchWarning(component) {
  if (component.root?.pcbDisabled) return;
  if (component.props.doNotPlace) return;
  if (component.props.footprint === void 0) return;
  if (component.getInheritedProperty("bomDisabled")) return;
  if (component.getInheritedProperty("partsEngineDisabled")) return;
  const partsEngine = component.getInheritedProperty("partsEngine");
  if (!partsEngine?.fetchPartCircuitJson) return;
  const { db } = component.root;
  const sourceComponent = db.source_component.get(
    component.source_component_id
  );
  if (!sourceComponent?.supplier_part_numbers) return;
  if (!component.pcb_component_id) return;
  if (component._hasStartedSupplierFootprintMismatchWarningCheck) return;
  const supplierPartCandidates = getSupplierPartCandidates2(
    sourceComponent.supplier_part_numbers
  );
  if (supplierPartCandidates.length === 0) return;
  const localCopperElements = [
    ...db.pcb_smtpad.list({ pcb_component_id: component.pcb_component_id }),
    ...db.pcb_plated_hole.list({
      pcb_component_id: component.pcb_component_id
    })
  ];
  const localBounds = getCopperBounds(localCopperElements);
  if (!localBounds) return;
  component._hasStartedSupplierFootprintMismatchWarningCheck = true;
  component._queueAsyncEffect("check-supplier-footprint-mismatch", async () => {
    const { db: db2 } = component.root;
    const fetchPartCircuitJson = partsEngine.fetchPartCircuitJson;
    for (const supplierPartCandidate of supplierPartCandidates) {
      const { supplierName, supplierPartNumber } = supplierPartCandidate;
      try {
        const supplierCircuitJson = await Promise.resolve(
          fetchPartCircuitJson({ supplierPartNumber })
        ) ?? null;
        if (!supplierCircuitJson?.length) continue;
        const supplierBounds = getCopperBounds(supplierCircuitJson);
        if (!supplierBounds) continue;
        const footprintCopperIou = getBestBoundsIou(localBounds, supplierBounds);
        if (footprintCopperIou >= SUPPLIER_FOOTPRINT_IOU_WARNING_THRESHOLD) {
          return;
        }
        const footprintLabel = typeof component.props.footprint === "string" ? `"${component.props.footprint}"` : "the provided footprint";
        const roundedIou = Number(footprintCopperIou.toFixed(4));
        const warning = supplier_footprint_mismatch_warning.parse({
          type: "supplier_footprint_mismatch_warning",
          message: `${component.getString()} footprint ${footprintLabel} does not match supplier footprint ${supplierName}:${supplierPartNumber} (copper IoU ${roundedIou}).`,
          source_component_id: component.source_component_id,
          pcb_component_id: component.pcb_component_id,
          pcb_group_id: component.getGroup()?.pcb_group_id ?? void 0,
          subcircuit_id: component.getSubcircuit()?.subcircuit_id ?? void 0,
          supplier_name: supplierName,
          supplier_part_number: supplierPartNumber,
          footprint_copper_intersection_over_union: roundedIou
        });
        db2.supplier_footprint_mismatch_warning.insert(warning);
        return;
      } catch (error) {
        const errorMessage = error instanceof Error ? error.message : String(error);
        const warning = source_part_not_found_warning.parse({
          type: "source_part_not_found_warning",
          message: `Failed to fetch supplier footprint for ${component.getString()} (${supplierName}:${supplierPartNumber}): ${errorMessage}`,
          source_component_id: component.source_component_id ?? void 0,
          subcircuit_id: component.getSubcircuit()?.subcircuit_id ?? void 0,
          supplier_name: supplierName,
          supplier_part_number: supplierPartNumber
        });
        db2.source_part_not_found_warning.insert(warning);
        return;
      }
    }
  });
}

// lib/components/base-components/NormalComponent/utils/canMergePortDefinitions.ts
var canMergePortDefinitions = (a, b) => {
  const aPinNumber = a._parsedProps.pinNumber;
  const bPinNumber = b._parsedProps.pinNumber;
  if (aPinNumber !== void 0 && bPinNumber !== void 0 && aPinNumber !== bPinNumber) {
    return false;
  }
  return a.isMatchingAnyOf(b.getNameAndAliases());
};

// lib/components/base-components/NormalComponent/utils/inferInternallyConnectedPinNamesFromPorts.ts
var hasExactSameMembers = (a, b) => {
  const sortedA = a.toSorted();
  const sortedB = b.toSorted();
  return sortedA.length === sortedB.length && sortedA.every((name, index) => name === sortedB[index]);
};
var getPinNumberedName = (port) => port.getNameAndAliases().find((alias) => /^pin\d+$/.test(alias)) ?? null;
var getPortReferenceName = (port, portNameCounts) => {
  if (!port._isPrimaryPort && port.props.name) {
    return port.props.name;
  }
  if (port.props.name && (portNameCounts.get(port.props.name) ?? 0) <= 1) {
    return port.props.name;
  }
  const pinNumberedName = getPinNumberedName(port);
  if (pinNumberedName) return pinNumberedName;
  return port.props.name ?? null;
};
function inferInternallyConnectedPinNamesFromPorts(ports, inferredInternallyConnectedPinNames) {
  const portsByAlias = /* @__PURE__ */ new Map();
  for (const port of ports) {
    for (const alias of new Set(port.getNameAndAliases())) {
      if (/^(pin)?\d+$/.test(alias)) continue;
      if (!port._isPrimaryPort && alias === port.props.name) {
        continue;
      }
      if (!portsByAlias.has(alias)) portsByAlias.set(alias, []);
      portsByAlias.get(alias).push(port);
    }
  }
  for (const aliasPorts of portsByAlias.values()) {
    const uniquePorts = Array.from(new Set(aliasPorts));
    if (uniquePorts.length < 2) continue;
    const portNameCounts = /* @__PURE__ */ new Map();
    for (const port of uniquePorts) {
      if (!port.props.name) continue;
      portNameCounts.set(
        port.props.name,
        (portNameCounts.get(port.props.name) ?? 0) + 1
      );
    }
    const portNames = uniquePorts.map((port) => getPortReferenceName(port, portNameCounts)).filter((portName) => Boolean(portName));
    const uniquePortNames = Array.from(new Set(portNames));
    if (uniquePortNames.length < 2) continue;
    if (inferredInternallyConnectedPinNames.some(
      (group) => hasExactSameMembers(group, uniquePortNames)
    )) {
      continue;
    }
    inferredInternallyConnectedPinNames.push(uniquePortNames);
  }
}

// lib/components/base-components/NormalComponent/utils/getPrimaryPortsFromPortHintGroups.ts
function getPrimaryPortsFromPortHintGroups(portHintGroups, opts) {
  let implicitPinNumber = 1;
  const entriesByPinNumber = /* @__PURE__ */ new Map();
  for (const portHintGroup of portHintGroups) {
    const filteredPortHints = portHintGroup.hints.filter(
      (hint) => hint && hint.trim() !== ""
    );
    if (filteredPortHints.length === 0) continue;
    let portHintsList = filteredPortHints;
    const hasPinIdentifier = portHintsList.some(
      (hint) => hint.startsWith("pin") || /^(?:pin)?\d+$/.test(hint)
    );
    if (!hasPinIdentifier) {
      const explicitPinNumber = portHintsList.map((hint) => opts?.implicitPinNumberByHint?.get(hint)).find((pinNumber2) => pinNumber2 !== void 0);
      if (explicitPinNumber !== void 0) {
        portHintsList = [...portHintsList, `pin${explicitPinNumber}`];
      } else if (opts?.allowImplicitPinNumbers === false) {
        continue;
      } else {
        portHintsList = [...portHintsList, `pin${implicitPinNumber}`];
      }
    }
    implicitPinNumber++;
    const newPort = getPortFromHints(portHintsList, opts);
    if (!newPort) continue;
    newPort.originDescription = portHintGroup.originDescription;
    const pinNumber = newPort._parsedProps.pinNumber;
    if (pinNumber === void 0) continue;
    if (!entriesByPinNumber.has(pinNumber))
      entriesByPinNumber.set(pinNumber, []);
    entriesByPinNumber.get(pinNumber).push({
      port: newPort,
      component: portHintGroup.component
    });
  }
  const ports = [];
  for (const [pinNumber, entries] of entriesByPinNumber) {
    const mergeAliases = (target, source) => {
      const mergedAliases = source.getNameAndAliases().filter((alias) => !target.getNameAndAliases().includes(alias));
      target.externallyAddedAliases.push(...mergedAliases);
    };
    const primaryPort = entries[0].port;
    const componentEntries = entries.filter(
      (entry) => Boolean(entry.component)
    );
    if (componentEntries.length !== entries.length) {
      for (const entry of entries.slice(1)) {
        mergeAliases(primaryPort, entry.port);
      }
      ports.push(primaryPort);
      continue;
    }
    const clusters = getConnectedPcbPrimitiveClustersBeforeRender(
      componentEntries.map((entry) => entry.component)
    );
    if (clusters.length <= 1) {
      for (const entry of entries.slice(1)) {
        mergeAliases(primaryPort, entry.port);
      }
      ports.push(primaryPort);
      continue;
    }
    const internallyConnectedPinNames = [primaryPort.props.name];
    for (const [clusterIndex, cluster] of clusters.entries()) {
      const clusterEntries = componentEntries.filter(
        (entry) => cluster.includes(entry.component)
      );
      if (clusterIndex === 0) {
        for (const entry of clusterEntries.slice(1)) {
          mergeAliases(primaryPort, entry.port);
        }
        ports.push(primaryPort);
        continue;
      }
      const internalPort = new Port({
        name: `pin${pinNumber}_internal_${clusterIndex}`,
        aliases: primaryPort.getNameAndAliases()
      });
      internalPort._isPrimaryPort = false;
      internalPort._primaryPinNumber = pinNumber;
      internalPort.originDescription = `implicitInternalPhysicalPort:pin${pinNumber}:${clusterIndex}`;
      for (const entry of clusterEntries) {
        mergeAliases(internalPort, entry.port);
      }
      internallyConnectedPinNames.push(internalPort.props.name);
      ports.push(internalPort);
    }
    opts?.inferredInternallyConnectedPinNames?.push(internallyConnectedPinNames);
  }
  if (opts?.inferredInternallyConnectedPinNames) {
    inferInternallyConnectedPinNamesFromPorts(
      ports,
      opts.inferredInternallyConnectedPinNames
    );
  }
  return ports;
}

// lib/components/base-components/NormalComponent/NormalComponent.ts
var debug3 = Debug5("tscircuit:core");
var rotation32 = z9.object({
  x: rotation2,
  y: rotation2,
  z: rotation2
});
var NormalComponent3 = class extends PrimitiveComponent2 {
  reactSubtrees = [];
  _impliedFootprint;
  _resolvedPcbCalcOffsetX;
  _resolvedPcbCalcOffsetY;
  isPrimitiveContainer = true;
  _isNormalComponent = true;
  // Mapping from camelCase attribute names to their lowercase equivalents
  // This is used by the CSS selector adapter for fast attribute lookups
  // Reverse mapping from lowercase to camelCase for O(1) lookups
  _attributeLowerToCamelNameMap = {
    _isnormalcomponent: "_isNormalComponent"
  };
  _asyncSupplierPartNumbers;
  _asyncFootprintCadModel;
  _isCadModelChild;
  _inferredInternallyConnectedPinNames = [];
  pcb_missing_footprint_error_id;
  _hasStartedFootprintUrlLoad = false;
  _hasStartedPartOrientationAnalysis = false;
  _asyncSupplierPin1LocationMap;
  _hasStartedSupplierFootprintMismatchWarningCheck = false;
  _hasInflatedCircuitJsonSymbol = false;
  _invalidFootprintPropMessages = [];
  _invalidPinLabelMessages = [];
  _impliedFootprintPinLabels;
  /**
   * Set to true to enable automatic silkscreen text adjustment when it overlaps with other components
   */
  _adjustSilkscreenTextAutomatically = false;
  /**
   * Override this property for component defaults
   */
  get defaultInternallyConnectedPinNames() {
    return [];
  }
  get internallyConnectedPinNames() {
    const rawPins = this._parsedProps.internallyConnectedPins ?? this.defaultInternallyConnectedPinNames;
    return [...rawPins, ...this._inferredInternallyConnectedPinNames].map(
      (pinGroup) => pinGroup.map(
        (pin) => typeof pin === "number" ? `pin${pin}` : pin
      )
    );
  }
  constructor(props) {
    const filteredProps = { ...props };
    let invalidPinLabelsMessages = [];
    if (filteredProps.pinLabels && !Array.isArray(filteredProps.pinLabels)) {
      const { validPinLabels, invalidPinLabelsMessages: messages } = filterPinLabels(filteredProps.pinLabels);
      filteredProps.pinLabels = validPinLabels;
      invalidPinLabelsMessages = messages;
    }
    super(filteredProps);
    if (filteredProps.pinLabels && !Array.isArray(filteredProps.pinLabels)) {
      const invalidPinKey = Object.keys(filteredProps.pinLabels).find(
        (pinKey) => getPinNumberFromPinLabelsKey(pinKey) === null
      );
      if (invalidPinKey) {
        throw new InvalidProps(this.lowercaseComponentName, this.props, {
          _errors: [],
          pinLabels: {
            _errors: [
              `Invalid pinLabels key "${invalidPinKey}". Expected "pin\${number}" (e.g. pin1, pin2).`
            ]
          }
        });
      }
    }
    this._invalidPinLabelMessages = invalidPinLabelsMessages;
    this._addChildrenFromStringFootprint();
    this._addChildrenFromCircuitJsonSymbol();
    this.initPorts();
  }
  doInitialSourceNameDuplicateComponentRemoval() {
    if (!this.name) return;
    const root = this.root;
    const nameMap = this.getSubcircuit().getNormalComponentNameMap?.();
    const componentsWithSameName = nameMap?.get(this.props.name) ?? [];
    const conflictingComponents = componentsWithSameName.filter(
      (component) => component !== this && component.renderPhaseStates.SourceNameDuplicateComponentRemoval.initialized
    );
    if (conflictingComponents.length > 0) {
      const pcbPosition = this._getGlobalPcbPositionBeforeLayout();
      const schematicPosition = this._getGlobalSchematicPositionBeforeLayout();
      root.db.source_failed_to_create_component_error.insert({
        component_name: this.name,
        error_type: "source_failed_to_create_component_error",
        message: `Cannot create component "${this.name}": A component with the same name already exists`,
        pcb_center: pcbPosition,
        schematic_center: schematicPosition
      });
      this.shouldBeRemoved = true;
      const childrenToRemove = [...this.children];
      for (const child of childrenToRemove) {
        this.remove(child);
      }
    }
  }
  /**
   * Override this method for better control over the auto-discovery of ports.
   *
   * If you override this method just do something like:
   * initPorts() {
   *   this.add(new Port({ pinNumber: 1, aliases: ["anode", "pos"] }))
   *   this.add(new Port({ pinNumber: 2, aliases: ["cathode", "neg"] }))
   * }
   *
   * By default, we'll pull the ports from the first place we find them:
   * 1. `config.schematicSymbolName`
   * 2. `props.footprint`
   *
   */
  initPorts(opts = {}) {
    const hasPendingReactSymbol = isValidElement(this.props.symbol) && !this.children.some((child) => child.componentName === "Symbol");
    if (hasPendingReactSymbol) return;
    this._inferredInternallyConnectedPinNames = [];
    const { config } = this;
    const portsToCreate = [];
    const pinLabels = this._resolvePinLabels();
    const propsPinLabels = this._parsedProps.pinLabels;
    const pinLabelsFromProps = propsPinLabels && Array.isArray(propsPinLabels) ? Object.fromEntries(
      propsPinLabels.map((label, index) => [`pin${index + 1}`, label])
    ) : propsPinLabels;
    const existingChildPorts = this._getAllPortsFromChildren();
    const hasExistingOrQueuedPortWithPinNumber = (pinNumber) => existingChildPorts.some((p) => p._parsedProps.pinNumber === pinNumber) || portsToCreate.some((p) => p._parsedProps.pinNumber === pinNumber);
    const schPortArrangement = this._getSchematicPortArrangement();
    if (schPortArrangement && !pinLabels) {
      for (const side in schPortArrangement) {
        const pins = schPortArrangement[side].pins;
        if (Array.isArray(pins)) {
          for (const pinNumberOrLabel of pins) {
            const pinNumber = parsePinNumberFromLabelsOrThrow(
              pinNumberOrLabel,
              pinLabels
            );
            if (hasExistingOrQueuedPortWithPinNumber(pinNumber)) continue;
            portsToCreate.push(
              new Port(
                {
                  pinNumber,
                  aliases: opts.additionalAliases?.[`pin${pinNumber}`] ?? []
                },
                {
                  originDescription: `schPortArrangement:${side}`
                }
              )
            );
          }
        }
      }
      const sides = ["left", "right", "top", "bottom"];
      let pinNum = 1;
      for (const side of sides) {
        const size = schPortArrangement[`${side}Size`];
        for (let i = 0; i < size; i++) {
          const nextPinNumber = pinNum++;
          if (hasExistingOrQueuedPortWithPinNumber(nextPinNumber)) continue;
          portsToCreate.push(
            new Port(
              {
                pinNumber: nextPinNumber,
                aliases: opts.additionalAliases?.[`pin${nextPinNumber}`] ?? []
              },
              {
                originDescription: `schPortArrangement:${side}`
              }
            )
          );
        }
      }
    }
    if (pinLabels) {
      for (const [pinKey, label] of Object.entries(pinLabels)) {
        const pinNumber = getPinNumberFromPinLabelsKey(pinKey);
        if (pinNumber === null) {
          throw new Error(
            `Invalid pinLabels key "${pinKey}". Expected "pin\${number}" (e.g. pin1, pin2).`
          );
        }
        let existingPort = existingChildPorts.find(
          (p) => p._parsedProps.pinNumber === pinNumber
        ) ?? portsToCreate.find((p) => p._parsedProps.pinNumber === pinNumber);
        const labelList = Array.isArray(label) ? label : [label];
        const propLabel = pinLabelsFromProps?.[pinKey];
        const hasPropLabel = propLabel !== void 0;
        const primaryLabel = hasPropLabel ? labelList[0] : `pin${pinNumber}`;
        const otherLabels = hasPropLabel ? labelList.slice(1) : labelList;
        if (!existingPort) {
          existingPort = new Port(
            {
              pinNumber,
              name: primaryLabel,
              aliases: [
                ...hasPropLabel ? [] : opts.additionalAliases?.[`pin${pinNumber}`] ?? [],
                ...otherLabels,
                ...hasPropLabel ? opts.additionalAliases?.[`pin${pinNumber}`] ?? [] : []
              ]
            },
            {
              originDescription: `pinLabels:pin${pinNumber}`
            }
          );
          portsToCreate.push(existingPort);
        } else {
          existingPort.externallyAddedAliases.push(primaryLabel, ...otherLabels);
        }
      }
    }
    if (config.schematicSymbolName && !opts.ignoreSymbolPorts) {
      const sym = symbols2[this._getSchematicSymbolNameOrThrow()];
      if (!sym) return;
      for (const symPort of sym.ports) {
        const pinNumber = getPinNumberFromLabels(symPort.labels);
        if (!pinNumber) continue;
        const existingPort = this._getAllPortsFromChildren().find(
          (p) => p._parsedProps.pinNumber === Number(pinNumber)
        ) ?? portsToCreate.find(
          (p) => p._parsedProps.pinNumber === Number(pinNumber)
        );
        if (existingPort) {
          existingPort.schematicSymbolPortDef = symPort;
        } else {
          const port = getPortFromHints(
            symPort.labels.concat(
              opts.additionalAliases?.[`pin${pinNumber}`] ?? []
            )
          );
          if (port) {
            port.originDescription = `schematicSymbol:labels[0]:${symPort.labels[0]}`;
            port.schematicSymbolPortDef = symPort;
            portsToCreate.push(port);
          }
        }
      }
      this.addAll(portsToCreate);
    }
    if (!this._getSchematicPortArrangement()) {
      const hasReactSymbol = isValidElement(this.props.symbol);
      const hasCircuitJsonSymbolProp = isCircuitJsonSymbol(this.props.symbol);
      if (hasReactSymbol || hasCircuitJsonSymbolProp) {
      } else {
        const portsFromFootprint = this.getPortsFromFootprint({
          ...opts,
          allowImplicitPinNumbers: !pinLabelsFromProps,
          collectInferredInternallyConnectedPins: true
        });
        const existingPorts = this._getAllPortsFromChildren();
        for (const port of portsFromFootprint) {
          if (!port._isPrimaryPort) {
            portsToCreate.push(port);
            continue;
          }
          const matchingPort = existingPorts.find((p) => canMergePortDefinitions(p, port)) ?? portsToCreate.find((p) => canMergePortDefinitions(p, port));
          if (matchingPort) {
            const mergedAliases = port.getNameAndAliases().filter(
              (alias) => !matchingPort.getNameAndAliases().includes(alias)
            );
            matchingPort.externallyAddedAliases.push(...mergedAliases);
          } else {
            portsToCreate.push(port);
          }
        }
      }
    }
    const requiredPinCount = opts.pinCount ?? this._getPrimaryPinCount() ?? 0;
    for (let pn = 1; pn <= requiredPinCount; pn++) {
      if (hasExistingOrQueuedPortWithPinNumber(pn)) continue;
      if (!schPortArrangement && pinLabelsFromProps && !Array.isArray(propsPinLabels) && !pinLabelsFromProps[`pin${pn}`]) {
        continue;
      }
      if (!schPortArrangement) {
        portsToCreate.push(
          new Port({
            pinNumber: pn,
            aliases: opts.additionalAliases?.[`pin${pn}`] ?? []
          })
        );
        continue;
      }
      let explicitlyListedPinNumbersInSchPortArrangement = getPinsFromPortArrangement(schPortArrangement).map(
        (pn2) => parsePinNumberFromLabelsOrThrow(pn2, pinLabels)
      );
      if ([
        "leftSize",
        "rightSize",
        "topSize",
        "bottomSize",
        "leftPinCount",
        "rightPinCount",
        "topPinCount",
        "bottomPinCount"
      ].some((key) => key in schPortArrangement)) {
        explicitlyListedPinNumbersInSchPortArrangement = Array.from(
          { length: this._getPrimaryPinCount() },
          (_, i) => i + 1
        );
      }
      if (!explicitlyListedPinNumbersInSchPortArrangement.includes(pn)) {
        continue;
      }
      portsToCreate.push(
        new Port(
          {
            pinNumber: pn,
            aliases: opts.additionalAliases?.[`pin${pn}`] ?? []
          },
          {
            originDescription: `notOtherwiseAddedButDeducedFromPinCount:${pn}`
          }
        )
      );
    }
    inferInternallyConnectedPinNamesFromPorts(
      Array.from(
        /* @__PURE__ */ new Set([...this._getAllPortsFromChildren(), ...portsToCreate])
      ),
      this._inferredInternallyConnectedPinNames
    );
    if (portsToCreate.length > 0) {
      this.addAll(portsToCreate);
    }
  }
  _resolvePinLabels() {
    const pinLabels = this._parsedProps.pinLabels;
    const parsedPinLabels = pinLabels && Array.isArray(pinLabels) ? Object.fromEntries(
      pinLabels.map((label, index) => [`pin${index + 1}`, label])
    ) : pinLabels;
    const resolvedPinLabels = {
      ...this._impliedFootprintPinLabels ?? {},
      ...parsedPinLabels ?? {}
    };
    if (Object.keys(resolvedPinLabels).length === 0) return void 0;
    return resolvedPinLabels;
  }
  _getImpliedFootprintString() {
    return null;
  }
  // Override to choose the effective footprint without rewriting user props.
  resolveFootprint() {
    return this._parsedProps.footprint ?? this.props.footprint ?? this._getImpliedFootprintString?.();
  }
  _addChildrenFromStringFootprint() {
    const { pcbRotation, pcbPinLabels } = this.props;
    const pinLabels = this._resolvePinLabels();
    const footprint = this.resolveFootprint();
    if (!footprint) return;
    if (typeof footprint === "string") {
      if (isHttpUrl(footprint)) return;
      if (isBlobUrl(footprint)) return;
      if (isStaticAssetPath(footprint)) return;
      if (parseLibraryFootprintRef(footprint)) return;
      let fpCircuitJson;
      try {
        fpCircuitJson = fp.string(footprint).soup();
      } catch (error) {
        this._queueInvalidFootprintPropMessage(footprint, error);
        return;
      }
      const fpComponents = createComponentsFromCircuitJson(
        {
          componentName: this.name ?? this.componentName,
          componentRotation: pcbRotation,
          footprinterString: footprint,
          pinLabels,
          pcbPinLabels
        },
        fpCircuitJson
      );
      this.addAll(fpComponents);
    }
  }
  _addChildrenFromCircuitJsonSymbol() {
    if (this._hasInflatedCircuitJsonSymbol) return;
    const symbol = this._parsedProps.symbol;
    if (!isCircuitJsonSymbol(symbol)) return;
    if (this.children.some((component) => component.componentName === "Symbol")) {
      this._hasInflatedCircuitJsonSymbol = true;
      return;
    }
    const selectedSymbolCircuitJson = selectSymbolCircuitJson(symbol);
    if (selectedSymbolCircuitJson.length === 0) return;
    const symbolCircuitJson = normalizeSchematicSymbolCircuitJson(
      selectedSymbolCircuitJson
    );
    const importedSymbolComponents = createComponentsFromCircuitJson(
      {
        componentName: this.name ?? this.componentName,
        componentRotation: String(this.props.schRotation ?? 0)
      },
      symbolCircuitJson
    );
    if (importedSymbolComponents.length === 0) return;
    this._hasInflatedCircuitJsonSymbol = true;
    const hasImportedSymbolContainer = importedSymbolComponents.some(
      (component) => component.componentName === "Symbol"
    );
    if (!hasImportedSymbolContainer) {
      const symbolComponent = new SymbolComponent({
        name: this.name ?? this.componentName
      });
      symbolComponent.addAll(importedSymbolComponents);
      this.add(symbolComponent);
      return;
    }
    this.addAll(importedSymbolComponents);
  }
  get portMap() {
    return new Proxy(
      {},
      {
        get: (target, prop) => {
          const port = this.children.find(
            (c) => c.componentName === "Port" && c.isMatchingNameOrAlias(prop)
          );
          if (!port) {
            throw new Error(
              `There was an issue finding the port "${prop.toString()}" inside of a ${this.componentName} component with name: "${this.props.name}". This is a bug in @tscircuit/core`
            );
          }
          return port;
        }
      }
    );
  }
  getInstanceForReactElement(element) {
    for (const subtree of this.reactSubtrees) {
      if (subtree.element === element) return subtree.component;
    }
    return null;
  }
  doInitialSourceRender() {
    const ftype = this.config.sourceFtype;
    if (!ftype) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype,
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  getRefDesPrefixes() {
    return getDefaultExpectedRefDesPrefixesForFtype(this.config.sourceFtype);
  }
  doInitialCheckRefDesConvention() {
    NormalComponent_doInitialCheckRefDesConvention(this);
  }
  /**
   * After ports have their source_port_id assigned, create
   * source_component_internal_connection records so that the connectivity
   * map (and therefore DRC) knows which pins are internally connected.
   */
  doInitialSourceParentAttachment() {
    const { db } = this.root;
    const internallyConnectedSourcePortIds = this._getInternallyConnectedPins().map(
      (ports) => ports.map((port) => port.source_port_id).filter((id) => id !== null)
    ).filter((sourcePortIds) => sourcePortIds.length >= 2);
    if (this.source_component_id && internallyConnectedSourcePortIds.length > 0) {
      db.source_component.update(this.source_component_id, {
        internally_connected_source_port_ids: internallyConnectedSourcePortIds
      });
    }
    for (const sourcePortIds of internallyConnectedSourcePortIds) {
      db.source_component_internal_connection.insert({
        source_component_id: this.source_component_id,
        subcircuit_id: this.getSubcircuit()?.subcircuit_id,
        source_port_ids: sourcePortIds
      });
    }
  }
  /**
   * Render the schematic component for this NormalComponent using the
   * config.schematicSymbolName if it exists, or create a generic box if
   * no symbol is defined.
   *
   * You can override this method to do more complicated things.
   */
  doInitialSchematicComponentRender() {
    NormalComponent_doInitialSchematicComponentRender(this);
  }
  _getSchematicSymbolDisplayValue() {
    return void 0;
  }
  _getInternallyConnectedPins() {
    if (this.internallyConnectedPinNames.length === 0) return [];
    const internallyConnectedPorts = [];
    for (const netPortNames of this.internallyConnectedPinNames) {
      const ports = [];
      for (const portName of netPortNames) {
        ports.push(this.portMap[portName]);
      }
      internallyConnectedPorts.push(ports);
    }
    return internallyConnectedPorts;
  }
  _doInitialSchematicComponentRenderWithSymbol() {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const symbol_name = this._getSchematicSymbolNameOrThrow();
    const symbol = symbols2[symbol_name];
    const center = this._getGlobalSchematicPositionBeforeLayout();
    if (symbol) {
      const schematic_component2 = db.schematic_component.insert({
        center,
        size: { ...symbol.size },
        source_component_id: this.source_component_id,
        is_box_with_pins: true,
        symbol_name,
        schematic_sheet_id: this._resolveSchematicSheetId(),
        symbol_display_value: this._getSchematicSymbolDisplayValue()
      });
      this.schematic_component_id = schematic_component2.schematic_component_id;
    }
  }
  _doInitialSchematicComponentRenderWithReactSymbol(symbolElement) {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const center = this._getGlobalSchematicPositionBeforeLayout();
    const schematic_component2 = db.schematic_component.insert({
      center,
      // width/height are computed in the SchematicComponentSizeCalculation phase
      size: { width: 0, height: 0 },
      source_component_id: this.source_component_id,
      symbol_display_value: this._getSchematicSymbolDisplayValue(),
      is_box_with_pins: false,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.schematic_component_id = schematic_component2.schematic_component_id;
  }
  _doInitialSchematicComponentRenderWithSchematicBoxDimensions() {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const dimensions = this._getSchematicBoxDimensions();
    const primaryPortLabels = {};
    if (Array.isArray(props.pinLabels)) {
      props.pinLabels.forEach((label, index) => {
        primaryPortLabels[String(index + 1)] = label;
      });
    } else {
      for (const [port, label] of Object.entries(props.pinLabels ?? {})) {
        primaryPortLabels[port] = Array.isArray(label) ? label[0] : label;
      }
    }
    const center = this._getGlobalSchematicPositionBeforeLayout();
    const schPortArrangement = this._getSchematicPortArrangement();
    const schematic_component2 = db.schematic_component.insert({
      center,
      rotation: props.schRotation ?? 0,
      size: dimensions.getSize(),
      // We should be using the full size, but circuit-to-svg incorrectly
      // uses the schematic_component size to draw boxes instead of the
      // schematic_box size
      // size: dimensions.getSizeIncludingPins(),
      port_arrangement: underscorifyPortArrangement(schPortArrangement),
      pin_spacing: 0.2,
      // @ts-ignore soup needs to support distance for pin_styles
      pin_styles: underscorifyPinStyles(props.schPinStyle, props.pinLabels),
      port_labels: primaryPortLabels,
      source_component_id: this.source_component_id,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    const hasTopPins = schPortArrangement?.topSide !== void 0;
    const hasTopOrBottomPins = hasTopPins || schPortArrangement?.bottomSide !== void 0;
    const schematic_box_width = dimensions?.getSize().width;
    const schematic_box_height = dimensions?.getSize().height;
    const manufacturer_part_number_schematic_text = db.schematic_text.insert({
      text: props.manufacturerPartNumber ?? "",
      schematic_component_id: schematic_component2.schematic_component_id,
      anchor: "left",
      rotation: 0,
      position: {
        x: hasTopPins ? center.x + (schematic_box_width ?? 0) / 2 + 0.1 : center.x - (schematic_box_width ?? 0) / 2,
        y: hasTopOrBottomPins ? center.y + (schematic_box_height ?? 0) / 2 + 0.35 : center.y - (schematic_box_height ?? 0) / 2 - 0.13
      },
      color: "#006464",
      font_size: 0.18,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    const component_name_text = db.schematic_text.insert({
      text: props.displayName ?? props.name ?? "",
      schematic_component_id: schematic_component2.schematic_component_id,
      anchor: "left",
      rotation: 0,
      position: {
        x: hasTopPins ? center.x + (schematic_box_width ?? 0) / 2 + 0.1 : center.x - (schematic_box_width ?? 0) / 2,
        y: hasTopOrBottomPins ? center.y + (schematic_box_height ?? 0) / 2 + 0.55 : center.y + (schematic_box_height ?? 0) / 2 + 0.13
      },
      color: "#006464",
      font_size: 0.18,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.schematic_component_id = schematic_component2.schematic_component_id;
  }
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const subcircuit = this.getSubcircuit();
    const componentLayer = this._getPcbComponentLayer();
    if (componentLayer !== "top" && componentLayer !== "bottom") {
      const error = pcb_component_invalid_layer_error.parse({
        type: "pcb_component_invalid_layer_error",
        message: `Component cannot be placed on layer '${componentLayer}'. Components can only be placed on 'top' or 'bottom' layers.`,
        source_component_id: this.source_component_id,
        layer: componentLayer,
        subcircuit_id: subcircuit.subcircuit_id ?? void 0
      });
      db.pcb_component_invalid_layer_error.insert(error);
    }
    const globalTransformRotation = this.getGlobalTransformRotation();
    const pcb_component = db.pcb_component.insert({
      center: this._getGlobalPcbPositionBeforeLayout(),
      // width/height are computed in the PcbComponentSizeCalculation phase
      width: 0,
      height: 0,
      layer: componentLayer === "top" || componentLayer === "bottom" ? componentLayer : "top",
      rotation: normalizeDegrees2(globalTransformRotation),
      insertion_direction: this._getPcbComponentInsertionDirection(
        componentLayer,
        globalTransformRotation
      ),
      source_component_id: this.source_component_id,
      subcircuit_id: subcircuit.subcircuit_id ?? void 0,
      do_not_place: props.doNotPlace ?? false,
      obstructs_within_bounds: props.obstructsWithinBounds ?? true,
      is_allowed_to_be_off_board: props.allowOffBoard ?? false,
      metadata: props.kicadFootprintMetadata ? { kicad_footprint: props.kicadFootprintMetadata } : void 0
    });
    const footprint = this.resolveFootprint();
    const hasFootprintChild = this.children.some(
      (c) => c.componentName === "Footprint"
    );
    if (!footprint && !hasFootprintChild && !this.isGroup) {
      const footprint_error = db.pcb_missing_footprint_error.insert({
        message: `No footprint specified for component: ${this.getString()}`,
        source_component_id: `${this.source_component_id}`,
        error_type: "pcb_missing_footprint_error"
      });
      this.pcb_missing_footprint_error_id = footprint_error.pcb_missing_footprint_error_id;
    }
    this.pcb_component_id = pcb_component.pcb_component_id;
    const manualPlacement = this.getSubcircuit()._getPcbManualPlacementForComponent(this);
    if ((this.props.pcbX !== void 0 || this.props.pcbY !== void 0) && !!manualPlacement) {
      const warning = pcb_manual_edit_conflict_warning.parse({
        type: "pcb_manual_edit_conflict_warning",
        pcb_manual_edit_conflict_warning_id: `pcb_manual_edit_conflict_${this.source_component_id}`,
        message: `${this.getString()} has both manual placement and prop coordinates. pcbX and pcbY will be used. Remove pcbX/pcbY or clear the manual placement.`,
        pcb_component_id: this.pcb_component_id,
        source_component_id: this.source_component_id,
        subcircuit_id: subcircuit.subcircuit_id ?? void 0
      });
      db.pcb_manual_edit_conflict_warning.insert(warning);
    }
  }
  /**
   * At this stage, the smtpads/pcb primitives are placed, so we can compute
   * the width/height of the component
   */
  doInitialPcbComponentSizeCalculation() {
    if (this.root?.pcbDisabled) return;
    if (!this.pcb_component_id) return;
    const { db } = this.root;
    const bounds = getBoundsOfPcbComponents(this.children);
    if (bounds.width === 0 || bounds.height === 0) return;
    const center = {
      x: (bounds.minX + bounds.maxX) / 2,
      y: (bounds.minY + bounds.maxY) / 2
    };
    db.pcb_component.update(this.pcb_component_id, {
      center,
      width: bounds.width,
      height: bounds.height
    });
  }
  updatePcbComponentSizeCalculation() {
    this.doInitialPcbComponentSizeCalculation();
  }
  /**
   * Calculate and update the size of a custom schematic symbol based on its children
   */
  doInitialSchematicComponentSizeCalculation() {
    if (this.root?.schematicDisabled) return;
    if (!this.schematic_component_id) return;
    const { db } = this.root;
    const schematic_component2 = db.schematic_component.get(
      this.schematic_component_id
    );
    if (!schematic_component2) return;
    const schematicElements = [];
    const collectSchematicPrimitives = (children) => {
      for (const child of children) {
        if (child.isSchematicPrimitive && child.componentName === "SchematicLine") {
          const line = db.schematic_line.get(child.schematic_line_id);
          if (line) schematicElements.push(line);
        }
        if (child.isSchematicPrimitive && child.componentName === "SchematicRect") {
          const rect = db.schematic_rect.get(child.schematic_rect_id);
          if (rect) schematicElements.push(rect);
        }
        if (child.isSchematicPrimitive && child.componentName === "SchematicCircle") {
          const circle = db.schematic_circle.get(
            child.schematic_circle_id
          );
          if (circle) schematicElements.push(circle);
        }
        if (child.isSchematicPrimitive && child.componentName === "SchematicArc") {
          const arc = db.schematic_arc.get(child.schematic_arc_id);
          if (arc) schematicElements.push(arc);
        }
        if (child.isSchematicPrimitive && child.componentName === "SchematicText") {
          const text = db.schematic_text.get(child.schematic_text_id);
          if (text) schematicElements.push(text);
        }
        if (child.isSchematicPrimitive && child.componentName === "SchematicPath") {
          const pathIds = child.schematic_path_ids;
          if (pathIds) {
            for (const pathId of pathIds) {
              const path = db.schematic_path.get(pathId);
              if (path) schematicElements.push(path);
            }
          }
        }
        if (child.children && child.children.length > 0) {
          collectSchematicPrimitives(child.children);
        }
      }
    };
    collectSchematicPrimitives(this.children);
    if (schematicElements.length === 0) return;
    const bounds = getBoundsForSchematic(schematicElements);
    const width = Math.abs(bounds.maxX - bounds.minX);
    const height = Math.abs(bounds.maxY - bounds.minY);
    if (width === 0 && height === 0) return;
    const centerX = (bounds.minX + bounds.maxX) / 2;
    const centerY = (bounds.minY + bounds.maxY) / 2;
    db.schematic_component.update(this.schematic_component_id, {
      center: {
        x: centerX,
        y: centerY
      },
      size: {
        width,
        height
      }
    });
  }
  updateSchematicComponentSizeCalculation() {
    this.doInitialSchematicComponentSizeCalculation();
  }
  doInitialPcbComponentAnchorAlignment() {
    NormalComponent_doInitialPcbComponentAnchorAlignment(this);
  }
  updatePcbComponentAnchorAlignment() {
    this.doInitialPcbComponentAnchorAlignment();
  }
  doInitialResolveFootprintPinLabels() {
    NormalComponent_doInitialResolveFootprintPinLabels(this);
  }
  updateResolveFootprintPinLabels() {
    this.doInitialResolveFootprintPinLabels();
  }
  _renderReactSubtree(element) {
    const component = createInstanceFromReactElement(element);
    return {
      element,
      component
    };
  }
  doInitialInitializePortsFromChildren() {
    this.initPorts();
  }
  updateInitializePortsFromChildren() {
    this.initPorts();
  }
  getGlobalTransformRotation() {
    const globalTransform = this._computePcbGlobalTransformBeforeLayout();
    const decomposedTransform = decomposeTSR9(globalTransform);
    return decomposedTransform.rotation.angle * 180 / Math.PI;
  }
  _getFootprintMetadataForPcbComponent() {
    const footprintChild = this.children.find(
      (c) => c.componentName === "Footprint"
    );
    if (footprintChild) {
      return {
        insertionDirection: footprintChild._parsedProps.insertionDirection,
        originalLayer: footprintChild._parsedProps.originalLayer
      };
    }
    const footprint = this.props.footprint;
    if (isValidElement(footprint)) {
      const footprintProps2 = footprint.props;
      return {
        insertionDirection: footprintProps2.insertionDirection,
        originalLayer: footprintProps2.originalLayer
      };
    }
    if (footprint?.componentName === "Footprint") {
      return {
        insertionDirection: footprint._parsedProps?.insertionDirection ?? footprint.props?.insertionDirection,
        originalLayer: footprint._parsedProps?.originalLayer ?? footprint.props?.originalLayer
      };
    }
    return void 0;
  }
  _getPcbComponentLayer() {
    return this._parsedProps.layer ?? (this._getFootprintOriginalLayer() === "bottom" ? "bottom" : "top");
  }
  _getFootprintOriginalLayer() {
    return this._getFootprintMetadataForPcbComponent()?.originalLayer;
  }
  _getPcbComponentInsertionDirection(componentLayer, rotationDegrees = this.getGlobalTransformRotation()) {
    const footprintMetadata = this._getFootprintMetadataForPcbComponent();
    if (!footprintMetadata?.insertionDirection) return void 0;
    return transformFootprintInsertionDirection({
      insertionDirection: footprintMetadata.insertionDirection,
      rotationDegrees,
      isFlipped: isFootprintFlipped({
        componentLayer,
        originalLayer: footprintMetadata.originalLayer
      })
    });
  }
  doInitialReactSubtreesRender() {
    const fpElm = this.props.footprint;
    if (isValidElement(fpElm)) {
      const hasFootprintChild = this.children.some(
        (c) => c.componentName === "Footprint"
      );
      if (!hasFootprintChild) {
        this.add(fpElm);
      }
    }
    const symElm = this.props.symbol;
    if (isValidElement(symElm)) {
      const hasSymbolChild = this.children.some(
        (c) => c.componentName === "Symbol"
      );
      if (!hasSymbolChild) {
        this.add(symElm);
      }
    }
    const cmElm = this.props.cadModel;
    if (isValidElement(cmElm)) {
      this._isCadModelChild = true;
      const hasCadAssemblyChild = this.children.some(
        (c) => c.componentName === "CadAssembly"
      );
      const hasCadModelChild = this.children.some(
        (c) => c.componentName === "CadModel"
      );
      if (!hasCadAssemblyChild && !hasCadModelChild) {
        this.add(cmElm);
      }
    }
    const spiceModelElm = this.props.spiceModel;
    if (isValidElement(spiceModelElm)) {
      const hasSpiceModelChild = this.children.some(
        (c) => c.componentName === "SpiceModel"
      );
      if (!hasSpiceModelChild) {
        this.add(spiceModelElm);
      }
    }
  }
  doInitialPcbFootprintStringRender() {
    NormalComponent_doInitialPcbFootprintStringRender(
      this,
      (name, effect) => this._queueAsyncEffect(name, effect)
    );
  }
  /**
   * Get all ports from children, including ports inside Symbol.
   * This doesn't use selectAll to avoid caching issues during initPorts.
   */
  _getAllPortsFromChildren() {
    const ports = [];
    const collectPorts = (component) => {
      if (component.componentName === "Port") {
        ports.push(component);
      }
      for (const child of component.children) {
        collectPorts(child);
      }
    };
    for (const child of this.children) {
      collectPorts(child);
    }
    return ports;
  }
  _hasExistingPortExactly(port1) {
    const existingPorts = this._getAllPortsFromChildren();
    return existingPorts.some((port2) => {
      const aliases1 = port1.getNameAndAliases();
      const aliases2 = port2.getNameAndAliases();
      return aliases1.length === aliases2.length && aliases1.every((alias) => aliases2.includes(alias));
    });
  }
  /**
   * Check if a port with the same pinNumber already exists.
   * This is used to prevent duplicate ports for the same physical pin.
   * Note: We only check pinNumber, not aliases, because different physical pins
   * can have the same alias (e.g., multiple GND pins).
   */
  _hasMatchingPort(port1) {
    const existingPorts = this._getAllPortsFromChildren();
    const pinNumber1 = port1._parsedProps.pinNumber;
    if (pinNumber1 === void 0) return false;
    return existingPorts.some(
      (port2) => port2._parsedProps.pinNumber === pinNumber1
    );
  }
  add(componentOrElm) {
    let component;
    if (isReactElement3(componentOrElm)) {
      const subtree = this._renderReactSubtree(componentOrElm);
      this.reactSubtrees.push(subtree);
      component = subtree.component;
    } else {
      component = componentOrElm;
    }
    if (component.componentName === "Port") {
      if (this._hasExistingPortExactly(component)) return;
      if (this._hasMatchingPort(component)) {
        debug3(
          `Skipping port ${component} because a matching port already exists`
        );
        return;
      }
    }
    super.add(component);
  }
  getPortsFromFootprint(opts) {
    let inferredInternallyConnectedPinNames = void 0;
    if (opts?.collectInferredInternallyConnectedPins) {
      inferredInternallyConnectedPinNames = this._inferredInternallyConnectedPinNames;
    }
    const implicitPinNumberByHint = /* @__PURE__ */ new Map();
    const explicitPinLabels = this._parsedProps?.pinLabels ?? this.props.pinLabels;
    if (explicitPinLabels) {
      const pinLabelEntries = Array.isArray(explicitPinLabels) ? explicitPinLabels.map((label, index) => [`pin${index + 1}`, label]) : Object.entries(explicitPinLabels);
      for (const [pinKey, labelOrLabels] of pinLabelEntries) {
        const pinNumber = getPinNumberFromPinLabelsKey(pinKey);
        if (pinNumber === null) continue;
        const labels = Array.isArray(labelOrLabels) ? labelOrLabels : [labelOrLabels];
        for (const label of labels) {
          if (typeof label === "string" && label.length > 0) {
            implicitPinNumberByHint.set(label, pinNumber);
          }
        }
      }
    }
    const primaryPortOpts = {
      ...opts,
      inferredInternallyConnectedPinNames,
      implicitPinNumberByHint
    };
    let { footprint } = this.props;
    if (typeof footprint === "string" && parseLibraryFootprintRef(footprint) && this.children.some((c) => c.componentName === "Footprint")) {
      footprint = this.children.find((c) => c.componentName === "Footprint");
    } else if (!footprint || isValidElement(footprint)) {
      footprint = this.children.find((c) => c.componentName === "Footprint");
    }
    if (typeof footprint === "string") {
      if (isHttpUrl(footprint)) return [];
      if (isBlobUrl(footprint)) return [];
      if (isStaticAssetPath(footprint)) return [];
      if (parseLibraryFootprintRef(footprint)) return [];
      let fpCircuitJson;
      try {
        fpCircuitJson = fp.string(footprint).soup();
      } catch (error) {
        this._queueInvalidFootprintPropMessage(footprint, error);
        return [];
      }
      return getPrimaryPortsFromPortHintGroups(
        fpCircuitJson.flatMap(
          (elm) => "port_hints" in elm && elm.port_hints ? [
            {
              hints: elm.port_hints,
              originDescription: `footprint:string:${footprint}:port_hints[0]:${elm.port_hints[0]}`
            }
          ] : []
        ),
        primaryPortOpts
      );
    }
    if (!isValidElement(footprint) && footprint && footprint.componentName === "Footprint") {
      const fp2 = footprint;
      return getPrimaryPortsFromPortHintGroups(
        fp2.children.flatMap(
          (fpChild) => fpChild.props.portHints ? [
            {
              hints: fpChild.props.portHints,
              originDescription: `footprint:${footprint}`,
              component: fpChild
            }
          ] : []
        ),
        primaryPortOpts
      );
    }
    const newPorts = [];
    if (!footprint) {
      for (const child of this.children) {
        if (child.props.portHints && child.isPcbPrimitive) {
          const port = getPortFromHints(child.props.portHints);
          if (port) newPorts.push(port);
        }
      }
    }
    return newPorts;
  }
  _queueInvalidFootprintPropMessage(footprint, error) {
    const rawErrorMessage = error instanceof Error ? error.message : String(error);
    const isLikelyMissingLibraryPrefix = footprint.includes("/") && !footprint.includes(":");
    const isLikelyJlcpcbPartNumber = /^c\d+$/i.test(footprint);
    const resolvedJlcpcbPartNumber = isLikelyJlcpcbPartNumber ? `C${footprint.replace(/^c/i, "")}` : null;
    const helpfulHint = isLikelyMissingLibraryPrefix ? ` If this is a KiCad footprint, use "kicad:${footprint}".` : resolvedJlcpcbPartNumber ? ` If this is a JLCPCB part number, use "jlcpcb:${resolvedJlcpcbPartNumber}".` : "";
    const message = `Invalid footprint prop on ${this.getDisplayName()}: "${footprint}".${helpfulHint} Parser details: ${rawErrorMessage}`;
    if (!this._invalidFootprintPropMessages.includes(message)) {
      this._invalidFootprintPropMessages.push(message);
    }
  }
  _insertInvalidFootprintPropErrors() {
    for (const message of this._invalidFootprintPropMessages) {
      this.root.db.source_invalid_component_property_error.insert({
        source_component_id: this.source_component_id || "",
        property_name: "footprint",
        message,
        error_type: "source_invalid_component_property_error"
      });
    }
    this._invalidFootprintPropMessages = [];
  }
  getPortsFromSchematicSymbol() {
    if (this.root?.schematicDisabled) return [];
    const { config } = this;
    if (!config.schematicSymbolName) return [];
    const symbol = symbols2[config.schematicSymbolName];
    if (!symbol) return [];
    const newPorts = [];
    for (const symbolPort of symbol.ports) {
      const port = getPortFromHints(symbolPort.labels);
      if (port) {
        port.schematicSymbolPortDef = symbolPort;
        newPorts.push(port);
      }
    }
    return newPorts;
  }
  doInitialCreateNetsFromProps() {
    this._createNetsFromProps(this._getNetsFromConnectionsProp());
  }
  _getNetsFromConnectionsProp() {
    const { _parsedProps: props } = this;
    const propsWithConnections = [];
    if (props.connections) {
      for (const [pinName, target] of Object.entries(props.connections)) {
        const targets = Array.isArray(target) ? target : [target];
        for (const targetPath of targets) {
          propsWithConnections.push(String(targetPath));
        }
      }
    }
    return propsWithConnections;
  }
  _createNetsFromProps(propsWithConnections) {
    createNetsFromProps(this, propsWithConnections);
  }
  _getPcbCircuitJsonBounds() {
    const { db } = this.root;
    if (!this.pcb_component_id) return super._getPcbCircuitJsonBounds();
    const pcb_component = db.pcb_component.get(this.pcb_component_id);
    return {
      center: { x: pcb_component.center.x, y: pcb_component.center.y },
      bounds: {
        left: pcb_component.center.x - pcb_component.width / 2,
        top: pcb_component.center.y - pcb_component.height / 2,
        right: pcb_component.center.x + pcb_component.width / 2,
        bottom: pcb_component.center.y + pcb_component.height / 2
      },
      width: pcb_component.width,
      height: pcb_component.height
    };
  }
  _getPinCountFromSchematicPortArrangement() {
    const schPortArrangement = this._getSchematicPortArrangement();
    if (!schPortArrangement) return 0;
    const isExplicitPinMapping = isExplicitPinMappingArrangement(schPortArrangement);
    if (!isExplicitPinMapping) {
      return (schPortArrangement.leftSize ?? schPortArrangement.leftPinCount ?? 0) + (schPortArrangement.rightSize ?? schPortArrangement.rightPinCount ?? 0) + (schPortArrangement.topSize ?? schPortArrangement.topPinCount ?? 0) + (schPortArrangement.bottomSize ?? schPortArrangement.bottomPinCount ?? 0);
    }
    const pins = getPinsFromPortArrangement(schPortArrangement);
    let maxPinNumber = 0;
    for (const pin of pins) {
      if (typeof pin === "number") {
        maxPinNumber = Math.max(maxPinNumber, pin);
        continue;
      }
      const directPinNumber = getPinNumberFromPinLabelsKey(pin);
      if (directPinNumber !== null) {
        maxPinNumber = Math.max(maxPinNumber, directPinNumber);
        continue;
      }
      const pinLabels = this._parsedProps.pinLabels;
      if (pinLabels) {
        maxPinNumber = Math.max(
          maxPinNumber,
          parsePinNumberFromLabelsOrThrow(pin, pinLabels)
        );
      }
    }
    return Math.max(maxPinNumber, pins.length);
  }
  _getPrimaryPinCount() {
    const schPortArrangement = this._getSchematicPortArrangement();
    if (schPortArrangement) {
      const pinCountFromSchematicPortArrangement = this._getPinCountFromSchematicPortArrangement();
      return pinCountFromSchematicPortArrangement;
    }
    const portsFromFootprint = this.getPortsFromFootprint();
    if (portsFromFootprint.length > 0) {
      return portsFromFootprint.filter((port) => port._isPrimaryPort).length;
    }
    const { pinLabels } = this._parsedProps;
    if (pinLabels) {
      if (Array.isArray(pinLabels)) {
        return pinLabels.length;
      }
      const pinNumbers = Object.keys(pinLabels).map((k) => getPinNumberFromPinLabelsKey(k)).filter((n) => n !== null && !Number.isNaN(n));
      if (pinNumbers.length > 0) {
        return Math.max(...pinNumbers);
      }
      return Object.keys(pinLabels).length;
    }
    return 0;
  }
  /**
   * Override the schematic port arrangement if you want to customize where pins
   * appear on a schematic box, e.g. for a pin header
   */
  _getSchematicPortArrangement() {
    return this._parsedProps.schPinArrangement ?? this._parsedProps.schPortArrangement;
  }
  /**
   * Extract pin labels from ports using existing Port logic
   */
  _getPinLabelsFromPorts() {
    const ports = this.selectAll("port");
    const pinLabels = {};
    for (const port of ports) {
      const pinNumber = port.props.pinNumber;
      if (pinNumber !== void 0) {
        const bestLabel = port._getBestDisplayPinLabel();
        if (bestLabel) {
          pinLabels[`pin${pinNumber}`] = bestLabel;
        }
      }
    }
    return pinLabels;
  }
  _getImplicitSparseSchematicPortArrangement(pinCount) {
    const pinNumbers = Array.from(
      new Set(
        this._getAllPortsFromChildren().filter((port) => port._isPrimaryPort).map((port) => port._parsedProps.pinNumber).filter((pinNumber) => pinNumber !== void 0)
      )
    ).sort((a, b) => a - b);
    if (pinNumbers.length !== pinCount) return void 0;
    const hasSparsePinNumbers = pinNumbers.some(
      (pinNumber, index) => pinNumber !== index + 1
    );
    if (!hasSparsePinNumbers) return void 0;
    const rightSize = Math.floor(pinCount / 2);
    const leftSize = pinCount - rightSize;
    return {
      leftSide: { pins: pinNumbers.slice(0, leftSize) },
      rightSide: { pins: pinNumbers.slice(leftSize) }
    };
  }
  _getSchematicBoxDimensions() {
    if (this.getSchematicSymbol()) return null;
    if (!this.config.shouldRenderAsSchematicBox && !this._parsedProps?.showAsSchematicBox) {
      return null;
    }
    const { _parsedProps: props } = this;
    const pinCount = this._getPrimaryPinCount();
    const pinSpacing = 0.2;
    const pinLabelsFromPorts = this._getPinLabelsFromPorts();
    const allPinLabels = {
      ...pinLabelsFromPorts,
      ...props.pinLabels
    };
    const schPortArrangement = this._getSchematicPortArrangement() ?? this._getImplicitSparseSchematicPortArrangement(pinCount);
    const dimensions = getAllDimensionsForSchematicBox({
      schWidth: props.schWidth,
      schHeight: props.schHeight,
      schPinSpacing: pinSpacing,
      numericSchPinStyle: getNumericSchPinStyle(
        props.schPinStyle,
        allPinLabels
      ),
      pinCount,
      schPortArrangement,
      pinLabels: allPinLabels
    });
    return dimensions;
  }
  getFootprinterString() {
    const footprint = this.resolveFootprint();
    if (typeof footprint === "string") {
      return footprint;
    }
    return null;
  }
  doInitialCadModelRender() {
    if (this._isCadModelChild) return;
    if (this.props.doNotPlace) return;
    const { db } = this.root;
    const { boardThickness = 0 } = this._getBoard() ?? {};
    const cadModelProp2 = this._parsedProps.cadModel;
    const cadModel = cadModelProp2 === void 0 ? this._asyncFootprintCadModel : cadModelProp2;
    const footprintString = this.getFootprinterString() ?? void 0;
    let footprintIsFootprinterString = false;
    if (footprintString) {
      footprintIsFootprinterString = !parseLibraryFootprintRef(footprintString) && !isHttpUrl(footprintString) && !isBlobUrl(footprintString) && !isStaticAssetPath(footprintString);
    }
    if (!this.pcb_component_id) return;
    if (cadModel === null) return;
    const bounds = this._getPcbCircuitJsonBounds();
    if (typeof cadModel === "string") {
      throw new Error("String cadModel not yet implemented");
    }
    const sourceRotationOffset = cadModel?.pcbRotationOffset ?? cadModel?.rotationOffset;
    const rotationOffset = rotation32.parse({
      x: 0,
      y: 0,
      z: typeof sourceRotationOffset === "number" ? sourceRotationOffset : 0,
      ...typeof sourceRotationOffset === "object" ? sourceRotationOffset : {}
    });
    const positionOffset = point32.parse({
      x: 0,
      y: 0,
      z: 0,
      ...typeof cadModel?.positionOffset === "object" ? cadModel.positionOffset : {}
    });
    const zOffsetFromSurface = cadModel && typeof cadModel === "object" && "zOffsetFromSurface" in cadModel ? cadModel.zOffsetFromSurface !== void 0 ? distance8.parse(
      cadModel.zOffsetFromSurface
    ) : 0 : 0;
    const computedLayer = this.props.layer === "bottom" ? "bottom" : "top";
    const pcbComponent = db.pcb_component.get(this.pcb_component_id);
    const globalTransform = this._computePcbGlobalTransformBeforeLayout();
    const decomposedTransform = decomposeTSR9(globalTransform);
    const preLayoutRotation = decomposedTransform.rotation.angle * 180 / Math.PI;
    const totalRotation = pcbComponent?.position_mode === "packed" ? pcbComponent.rotation ?? preLayoutRotation : preLayoutRotation;
    const isBottomLayer = computedLayer === "bottom";
    if (!cadModel && !footprintIsFootprinterString) {
      const cad_component = db.cad_component.insert({
        position: {
          x: bounds.center.x,
          y: bounds.center.y,
          z: computedLayer === "bottom" ? -boardThickness / 2 : boardThickness / 2
        },
        rotation: {
          x: 0,
          y: isBottomLayer ? 180 : 0,
          z: normalizeDegrees2(isBottomLayer ? -totalRotation : totalRotation)
        },
        pcb_component_id: this.pcb_component_id,
        source_component_id: this.source_component_id,
        model_origin_alignment: "center_of_component_on_board_surface",
        anchor_alignment: "center_of_component_on_board_surface",
        show_as_bounding_box: true,
        show_as_translucent_model: this._parsedProps.showAsTranslucentModel
      });
      this.cad_component_id = cad_component.cad_component_id;
      return;
    }
    const rotationWithOffset = totalRotation + (rotationOffset.z ?? 0);
    const cadRotationZ = normalizeDegrees2(rotationWithOffset);
    let footprinterStringForCadComponent;
    if (!cadModel && footprintIsFootprinterString) {
      footprinterStringForCadComponent = footprintString;
    }
    const cad_model = db.cad_component.insert({
      // TODO z maybe depends on layer
      position: {
        x: bounds.center.x + positionOffset.x,
        y: bounds.center.y + positionOffset.y,
        z: (computedLayer === "bottom" ? -boardThickness / 2 : boardThickness / 2) + (computedLayer === "bottom" ? -zOffsetFromSurface : zOffsetFromSurface) + positionOffset.z
      },
      rotation: {
        x: rotationOffset.x,
        y: rotationOffset.y + (isBottomLayer ? 180 : 0),
        z: normalizeDegrees2(isBottomLayer ? -cadRotationZ : cadRotationZ)
      },
      pcb_component_id: this.pcb_component_id,
      source_component_id: this.source_component_id,
      model_stl_url: "stlUrl" in (cadModel ?? {}) ? this._addCachebustToModelUrl(cadModel.stlUrl) : void 0,
      model_obj_url: "objUrl" in (cadModel ?? {}) ? this._addCachebustToModelUrl(cadModel.objUrl) : void 0,
      model_mtl_url: "mtlUrl" in (cadModel ?? {}) ? this._addCachebustToModelUrl(cadModel.mtlUrl) : void 0,
      model_gltf_url: "gltfUrl" in (cadModel ?? {}) ? this._addCachebustToModelUrl(cadModel.gltfUrl) : void 0,
      model_glb_url: "glbUrl" in (cadModel ?? {}) ? this._addCachebustToModelUrl(cadModel.glbUrl) : void 0,
      model_step_url: "stepUrl" in (cadModel ?? {}) ? this._addCachebustToModelUrl(cadModel.stepUrl) : void 0,
      model_wrl_url: "wrlUrl" in (cadModel ?? {}) ? this._addCachebustToModelUrl(cadModel.wrlUrl) : void 0,
      model_jscad: "jscad" in (cadModel ?? {}) ? cadModel.jscad : void 0,
      model_unit_to_mm_scale_factor: typeof cadModel?.modelUnitToMmScale === "number" ? cadModel.modelUnitToMmScale : void 0,
      model_board_normal_direction: cadModel?.modelBoardNormalDirection,
      model_origin_alignment: "center_of_component_on_board_surface",
      anchor_alignment: "center_of_component_on_board_surface",
      model_origin_position: cadModel?.modelOriginPosition,
      footprinter_string: footprinterStringForCadComponent,
      show_as_translucent_model: this._parsedProps.showAsTranslucentModel
    });
    this.cad_component_id = cad_model.cad_component_id;
  }
  _addCachebustToModelUrl(url) {
    if (!url || !url.includes("modelcdn.tscircuit.com")) return url;
    const origin = this.root?.getClientOrigin() ?? "";
    return `${url}${url.includes("?") ? "&" : "?"}cachebust_origin=${encodeURIComponent(origin)}`;
  }
  _getPartsEngineCacheKey(source_component, footprinterString) {
    return JSON.stringify({
      ftype: source_component.ftype,
      name: source_component.name,
      manufacturer_part_number: source_component.manufacturer_part_number,
      footprinterString
    });
  }
  async _getSupplierPartNumbers(partsEngine, source_component, footprinterString) {
    if (this.props.doNotPlace) return {};
    const cacheEngine = this.root?.platform?.localCacheEngine;
    const cacheKey = this._getPartsEngineCacheKey(
      source_component,
      footprinterString
    );
    if (cacheEngine) {
      const cached = await cacheEngine.getItem(cacheKey);
      if (cached) {
        try {
          return JSON.parse(cached);
        } catch {
        }
      }
    }
    const result = await Promise.resolve(
      partsEngine.findPart({
        sourceComponent: source_component,
        footprinterString
      })
    );
    if (typeof result === "string") {
      if (result.includes("<!DOCTYPE") || result.includes("<html")) {
        throw new Error(
          `Failed to fetch supplier part numbers: Received HTML response instead of JSON. Response starts with: ${result.substring(0, 100)}`
        );
      }
      if (result === "Not found") {
        throw new Error(
          `Part not found for ${this.getString()}${footprinterString ? ` with footprint "${footprinterString}"` : ""}`
        );
      }
      throw new Error(
        `Invalid supplier part numbers format: Expected object but got string: "${result}"`
      );
    }
    if (!result || Array.isArray(result) || typeof result !== "object") {
      const actualType = result === null ? "null" : Array.isArray(result) ? "array" : typeof result;
      throw new Error(
        `Invalid supplier part numbers format: Expected object but got ${actualType}`
      );
    }
    const supplierPartNumbers = result;
    if (cacheEngine) {
      try {
        await cacheEngine.setItem(cacheKey, JSON.stringify(supplierPartNumbers));
      } catch {
      }
    }
    return supplierPartNumbers;
  }
  doInitialPartsEngineRender() {
    if (this.props.doNotPlace) return;
    if (this.getInheritedProperty("bomDisabled")) return;
    if (this.getInheritedProperty("partsEngineDisabled")) return;
    const partsEngine = this.getInheritedProperty("partsEngine");
    if (!partsEngine) return;
    const { db } = this.root;
    const source_component = db.source_component.get(this.source_component_id);
    if (!source_component) return;
    if (source_component.supplier_part_numbers) return;
    const footprint = this.props.footprint ?? this._getImpliedFootprintString();
    const footprinterString = typeof footprint === "string" ? footprint : void 0;
    const supplierPartNumbersMaybePromise = this._getSupplierPartNumbers(
      partsEngine,
      source_component,
      footprinterString
    );
    if (!(supplierPartNumbersMaybePromise instanceof Promise)) {
      db.source_component.update(this.source_component_id, {
        supplier_part_numbers: supplierPartNumbersMaybePromise
      });
      return;
    }
    this._queueAsyncEffect("get-supplier-part-numbers", async () => {
      await supplierPartNumbersMaybePromise.then((supplierPartNumbers) => {
        this._asyncSupplierPartNumbers = supplierPartNumbers;
        this._markDirty("PartsEngineRender");
      }).catch((error) => {
        this._asyncSupplierPartNumbers = {};
        const warning = source_part_not_found_warning2.parse({
          type: "source_part_not_found_warning",
          message: `Failed to fetch supplier part numbers for ${this.getString()}: ${error.message}`,
          source_component_id: this.source_component_id ?? void 0,
          subcircuit_id: this.getSubcircuit()?.subcircuit_id ?? void 0,
          manufacturer_part_number: source_component.manufacturer_part_number ?? void 0,
          part_name: source_component.name ?? void 0
        });
        db.source_part_not_found_warning.insert(warning);
        this._markDirty("PartsEngineRender");
      });
    });
  }
  updatePartsEngineRender() {
    if (this.props.doNotPlace) return;
    if (this.getInheritedProperty("bomDisabled")) return;
    if (this.getInheritedProperty("partsEngineDisabled")) return;
    const { db } = this.root;
    const source_component = db.source_component.get(this.source_component_id);
    if (!source_component) return;
    if (source_component.supplier_part_numbers) return;
    if (this._asyncSupplierPartNumbers) {
      db.source_component.update(this.source_component_id, {
        supplier_part_numbers: this._asyncSupplierPartNumbers
      });
      return;
    }
  }
  doInitialPartOrientationAnalysis() {
    NormalComponent_doInitialPartOrientationAnalysis(this);
  }
  updatePartOrientationAnalysis() {
    NormalComponent_updatePartOrientationAnalysis(this);
  }
  doInitialSupplierFootprintMismatchWarning() {
    NormalComponent_doInitialSupplierFootprintMismatchWarning(this);
  }
  updateSupplierFootprintMismatchWarning() {
    this.doInitialSupplierFootprintMismatchWarning();
  }
  doInitialAssignFallbackProps() {
    const { _parsedProps: props } = this;
    if (props.connections && !this.name) {
      this.fallbackUnassignedName = this.getSubcircuit().getNextAvailableName(this);
    }
  }
  doInitialCreateTracesFromProps() {
    this._createTracesFromConnectionsProp();
  }
  _createTracesFromConnectionsProp() {
    const { _parsedProps: props } = this;
    if (props.connections) {
      for (const [pinName, target] of Object.entries(props.connections)) {
        const targets = Array.isArray(target) ? target : [target];
        for (const targetPath of targets) {
          this.add(
            new Trace3({
              from: `.${this.name} > .${pinName}`,
              to: String(targetPath)
            })
          );
        }
      }
    }
  }
  doInitialSourceDesignRuleChecks() {
    NormalComponent_doInitialSourceDesignRuleChecks(this);
  }
  doInitialSourceComponentPropertyValidation() {
    this._insertInvalidFootprintPropErrors();
  }
  doInitialValidatePcbCoordinates() {
    super.doInitialValidatePcbCoordinates();
    if (this.root?.pcbDisabled) return;
    const props = this._parsedProps;
    const rawProps = this.props;
    const pcbLeftEdgeX = typeof rawProps.pcbLeftEdgeX === "string" ? rawProps.pcbLeftEdgeX : props.pcbLeftEdgeX ?? rawProps.pcbLeftEdgeX;
    const pcbRightEdgeX = typeof rawProps.pcbRightEdgeX === "string" ? rawProps.pcbRightEdgeX : props.pcbRightEdgeX ?? rawProps.pcbRightEdgeX;
    const pcbTopEdgeY = typeof rawProps.pcbTopEdgeY === "string" ? rawProps.pcbTopEdgeY : props.pcbTopEdgeY ?? rawProps.pcbTopEdgeY;
    const pcbBottomEdgeY = typeof rawProps.pcbBottomEdgeY === "string" ? rawProps.pcbBottomEdgeY : props.pcbBottomEdgeY ?? rawProps.pcbBottomEdgeY;
    const hasExplicitPcbPosition = props.pcbX !== void 0 || props.pcbY !== void 0 || pcbLeftEdgeX !== void 0 || pcbRightEdgeX !== void 0 || pcbTopEdgeY !== void 0 || pcbBottomEdgeY !== void 0;
    if (!hasExplicitPcbPosition) return;
    if (pcbLeftEdgeX !== void 0) {
      this._validatePcbCoordinateReferences({
        rawValue: pcbLeftEdgeX,
        axis: "pcbX",
        propertyName: "pcbLeftEdgeX"
      });
    }
    if (pcbRightEdgeX !== void 0) {
      this._validatePcbCoordinateReferences({
        rawValue: pcbRightEdgeX,
        axis: "pcbX",
        propertyName: "pcbRightEdgeX"
      });
    }
    if (pcbTopEdgeY !== void 0) {
      this._validatePcbCoordinateReferences({
        rawValue: pcbTopEdgeY,
        axis: "pcbY",
        propertyName: "pcbTopEdgeY"
      });
    }
    if (pcbBottomEdgeY !== void 0) {
      this._validatePcbCoordinateReferences({
        rawValue: pcbBottomEdgeY,
        axis: "pcbY",
        propertyName: "pcbBottomEdgeY"
      });
    }
  }
  doInitialPcbLayout() {
    if (this.root?.pcbDisabled) return;
    if (!this.pcb_component_id) return;
    const { db } = this.root;
    const props = this._parsedProps;
    const rawProps = this.props;
    const pcbLeftEdgeX = props.pcbLeftEdgeX ?? rawProps.pcbLeftEdgeX;
    const pcbRightEdgeX = props.pcbRightEdgeX ?? rawProps.pcbRightEdgeX;
    const pcbTopEdgeY = props.pcbTopEdgeY ?? rawProps.pcbTopEdgeY;
    const pcbBottomEdgeY = props.pcbBottomEdgeY ?? rawProps.pcbBottomEdgeY;
    const hasExplicitPcbPosition = props.pcbX !== void 0 || props.pcbY !== void 0 || pcbLeftEdgeX !== void 0 || pcbRightEdgeX !== void 0 || pcbTopEdgeY !== void 0 || pcbBottomEdgeY !== void 0;
    if (!hasExplicitPcbPosition) return;
    if (pcbLeftEdgeX !== void 0 && pcbRightEdgeX !== void 0) {
      throw new Error(
        `${this.componentName} cannot set both pcbLeftEdgeX and pcbRightEdgeX`
      );
    }
    if (pcbTopEdgeY !== void 0 && pcbBottomEdgeY !== void 0) {
      throw new Error(
        `${this.componentName} cannot set both pcbTopEdgeY and pcbBottomEdgeY`
      );
    }
    const pcbComponent = db.pcb_component.get(this.pcb_component_id);
    const componentWidth = pcbComponent?.width ?? 0;
    const componentHeight = pcbComponent?.height ?? 0;
    const parentGroup = this.getGroup();
    const positionedRelativeToGroupId = parentGroup?.pcb_group_id ?? void 0;
    const positionedRelativeToBoardId = positionedRelativeToGroupId ? void 0 : this._getBoard()?.pcb_board_id ?? void 0;
    const resolvedPcbX = this._resolvedPcbCalcOffsetX ?? (props.pcbX !== void 0 ? this._resolvePcbCoordinate(props.pcbX, "pcbX") : pcbLeftEdgeX !== void 0 ? this._resolvePcbCoordinate(pcbLeftEdgeX, "pcbX", {
      propertyName: "pcbLeftEdgeX"
    }) + componentWidth / 2 : pcbRightEdgeX !== void 0 ? this._resolvePcbCoordinate(pcbRightEdgeX, "pcbX", {
      propertyName: "pcbRightEdgeX"
    }) - componentWidth / 2 : void 0);
    const resolvedPcbY = this._resolvedPcbCalcOffsetY ?? (props.pcbY !== void 0 ? this._resolvePcbCoordinate(props.pcbY, "pcbY") : pcbTopEdgeY !== void 0 ? this._resolvePcbCoordinate(pcbTopEdgeY, "pcbY", {
      propertyName: "pcbTopEdgeY"
    }) - componentHeight / 2 : pcbBottomEdgeY !== void 0 ? this._resolvePcbCoordinate(pcbBottomEdgeY, "pcbY", {
      propertyName: "pcbBottomEdgeY"
    }) + componentHeight / 2 : void 0);
    db.pcb_component.update(this.pcb_component_id, {
      position_mode: "relative_to_group_anchor",
      positioned_relative_to_pcb_group_id: positionedRelativeToGroupId,
      positioned_relative_to_pcb_board_id: positionedRelativeToBoardId,
      display_offset_x: resolvedPcbX,
      display_offset_y: resolvedPcbY
    });
  }
  /**
   * Get the minimum flex container size for this component on PCB
   */
  _getMinimumFlexContainerSize() {
    return NormalComponent__getMinimumFlexContainerSize(this);
  }
  /**
   * Reposition this component on the PCB to the specified coordinates
   */
  _repositionOnPcb(position) {
    return NormalComponent__repositionOnPcb(this, position);
  }
  doInitialSilkscreenOverlapAdjustment() {
    return NormalComponent_doInitialSilkscreenOverlapAdjustment(this);
  }
  /**
   * Returns true if this component has explicit PCB positioning (pcbX or pcbY)
   * and should not be moved by automatic packing/layout algorithms
   */
  isRelativelyPositioned() {
    const rawProps = this.props;
    return this._parsedProps.pcbX !== void 0 || this._parsedProps.pcbY !== void 0 || this._parsedProps.pcbLeftEdgeX !== void 0 || this._parsedProps.pcbRightEdgeX !== void 0 || this._parsedProps.pcbTopEdgeY !== void 0 || this._parsedProps.pcbBottomEdgeY !== void 0 || rawProps.pcbLeftEdgeX !== void 0 || rawProps.pcbRightEdgeX !== void 0 || rawProps.pcbTopEdgeY !== void 0 || rawProps.pcbBottomEdgeY !== void 0;
  }
};

// lib/components/normal-components/Board.ts
import {
  dedupePcbDrcErrors,
  runAllNetlistChecks,
  runAllPinSpecificationChecks,
  runAllPlacementChecks as runAllPlacementChecks2,
  runAllRoutingChecks
} from "@tscircuit/checks";
import { getBoundsFromPoints as getBoundsFromPoints7 } from "@tscircuit/math-utils";
import { boardProps } from "@tscircuit/props";
import { compose as compose8, translate as translate8 } from "transformation-matrix";

// lib/utils/autorouting/getAncestorSubcircuitIds.ts
var getDescendantSubcircuitIds = (db, root_subcircuit_id) => {
  const groups = db.source_group.list();
  const result = [];
  const findDescendants = (parentId) => {
    const children = groups.filter(
      (group) => group.parent_subcircuit_id === parentId
    );
    for (const child of children) {
      if (child.subcircuit_id) {
        result.push(child.subcircuit_id);
        findDescendants(child.subcircuit_id);
      }
    }
  };
  findDescendants(root_subcircuit_id);
  return result;
};

// lib/utils/boards/get-board-center-from-anchor.ts
var getBoardCenterFromAnchor = ({
  boardAnchorPosition,
  boardAnchorAlignment,
  width,
  height
}) => {
  const { x: ax, y: ay } = boardAnchorPosition;
  let cx = ax;
  let cy = ay;
  switch (boardAnchorAlignment) {
    case "top_left":
      cx = ax + width / 2;
      cy = ay - height / 2;
      break;
    case "top_right":
      cx = ax - width / 2;
      cy = ay - height / 2;
      break;
    case "bottom_left":
      cx = ax + width / 2;
      cy = ay + height / 2;
      break;
    case "bottom_right":
      cx = ax - width / 2;
      cy = ay + height / 2;
      break;
    case "top":
      cx = ax;
      cy = ay - height / 2;
      break;
    case "bottom":
      cx = ax;
      cy = ay + height / 2;
      break;
    case "left":
      cx = ax + width / 2;
      cy = ay;
      break;
    case "right":
      cx = ax - width / 2;
      cy = ay;
      break;
    case "center":
    default:
      break;
  }
  return { x: cx, y: cy };
};

// lib/utils/circuit-json/inflate-circuit-json.ts
import { cju } from "@tscircuit/circuit-json-util";

// lib/components/primitive-components/Group/Subcircuit/inflators/inflatePcbBoard.ts
function inflatePcbBoard(pcbBoard, inflatorContext) {
  const { subcircuit } = inflatorContext;
  if (subcircuit.lowercaseComponentName === "board" || subcircuit.lowercaseComponentName === "mountedboard") {
    return;
  }
  if (subcircuit.parent?.lowercaseComponentName === "board") {
    return;
  }
  const boardProps2 = {
    name: "inflated_board"
  };
  if (pcbBoard.width) boardProps2.width = pcbBoard.width;
  if (pcbBoard.height) boardProps2.height = pcbBoard.height;
  if (pcbBoard.center) {
    boardProps2.pcbX = pcbBoard.center.x;
    boardProps2.pcbY = pcbBoard.center.y;
  }
  if (pcbBoard.outline) boardProps2.outline = pcbBoard.outline;
  if (pcbBoard.thickness) boardProps2.thickness = pcbBoard.thickness;
  if (pcbBoard.material) boardProps2.material = pcbBoard.material;
  const board = new Board(boardProps2);
  board.pcb_board_id = pcbBoard.pcb_board_id;
  subcircuit.add(board);
  return board;
}

// lib/components/normal-components/Capacitor.ts
import { capacitorProps } from "@tscircuit/props";
import { formatSiUnit } from "format-si-unit";

// lib/components/normal-components/Capacitor_getAutomaticMaxDecouplingTraceLength.ts
var DEFAULT_MAX_DECOUPLING_TRACE_LENGTH_MM = 1;
function getCapacitorPorts(capacitor) {
  const capacitorPorts = [];
  for (const capacitorChild of capacitor.children) {
    if (capacitorChild instanceof Port) capacitorPorts.push(capacitorChild);
  }
  return capacitorPorts;
}
function Capacitor_getAutomaticMaxDecouplingTraceLength(capacitor) {
  const capacitorPorts = getCapacitorPorts(capacitor);
  if (capacitorPorts.length !== 2) return void 0;
  const firstPortIsConnectedToPower = capacitorPorts[0].isConnectedToPower();
  const firstPortIsConnectedToGround = capacitorPorts[0].isConnectedToGround();
  const secondPortIsConnectedToPower = capacitorPorts[1].isConnectedToPower();
  const secondPortIsConnectedToGround = capacitorPorts[1].isConnectedToGround();
  const isPowerToGroundTopology = firstPortIsConnectedToPower && !firstPortIsConnectedToGround && secondPortIsConnectedToGround && !secondPortIsConnectedToPower || secondPortIsConnectedToPower && !secondPortIsConnectedToGround && firstPortIsConnectedToGround && !firstPortIsConnectedToPower;
  return isPowerToGroundTopology ? DEFAULT_MAX_DECOUPLING_TRACE_LENGTH_MM : void 0;
}

// lib/components/normal-components/Capacitor.ts
var CAPACITOR_CHIP_FOOTPRINTS = /* @__PURE__ */ new Set([
  "01005",
  "0201",
  "0402",
  "0603",
  "0805",
  "1206",
  "1210",
  "2010",
  "2512"
]);
var Capacitor = class extends NormalComponent3 {
  _adjustSilkscreenTextAutomatically = true;
  get config() {
    return {
      componentName: "Capacitor",
      schematicSymbolName: this.props.polarized ? "capacitor_polarized" : this.props.symbolName ?? "capacitor",
      zodProps: capacitorProps,
      sourceFtype: FTYPE.simple_capacitor
    };
  }
  initPorts() {
    if (typeof this.props.footprint === "string") {
      super.initPorts({
        additionalAliases: {
          pin1: ["anode", "pos"],
          pin2: ["cathode", "neg"]
        }
      });
    } else {
      super.initPorts();
    }
  }
  _getSchematicSymbolDisplayValue() {
    const inputCapacitance = this.props.capacitance;
    let capacitanceDisplay;
    if (this._parsedProps.capacitance !== void 0 && !isNaN(this._parsedProps.capacitance)) {
      capacitanceDisplay = typeof inputCapacitance === "string" ? inputCapacitance : `${formatSiUnit(this._parsedProps.capacitance)}F`;
    } else {
      capacitanceDisplay = `${formatSiUnit(this._parsedProps.capacitance)}F`;
    }
    if (this._parsedProps.schShowRatings && this._parsedProps.maxVoltageRating) {
      return `${capacitanceDisplay}/${formatSiUnit(this._parsedProps.maxVoltageRating)}V`;
    }
    return capacitanceDisplay;
  }
  getFootprinterString() {
    const baseFootprint = super.getFootprinterString();
    if (baseFootprint && CAPACITOR_CHIP_FOOTPRINTS.has(baseFootprint)) {
      return `cap${baseFootprint}`;
    }
    return baseFootprint;
  }
  doInitialCreateNetsFromProps() {
    this._createNetsFromProps([
      this.props.decouplingFor,
      this.props.decouplingTo,
      ...this._getNetsFromConnectionsProp()
    ]);
  }
  doInitialCreateTracesFromProps() {
    if (this.props.decouplingFor && this.props.decouplingTo) {
      this.add(
        new Trace3({
          from: `${this.getSubcircuitSelector()} > port.1`,
          to: this.props.decouplingFor
        })
      );
      this.add(
        new Trace3({
          from: `${this.getSubcircuitSelector()} > port.2`,
          to: this.props.decouplingTo
        })
      );
    }
    this._createTracesFromConnectionsProp();
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_capacitor",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      capacitance: props.capacitance,
      max_voltage_rating: props.maxVoltageRating,
      max_decoupling_trace_length: props.maxDecouplingTraceLength ?? Capacitor_getAutomaticMaxDecouplingTraceLength(this),
      display_capacitance: this._getSchematicSymbolDisplayValue(),
      are_pins_interchangeable: !props.polarized,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/utils/extractPcbPrimitivesFromCircuitJson.ts
import { transformPCBElements } from "@tscircuit/circuit-json-util";
import { compose as compose6, inverse, rotate as rotate3, translate as translate4 } from "transformation-matrix";
var extractPcbPrimitivesFromCircuitJson = ({
  pcbComponent,
  db,
  componentName
}) => {
  const componentCenter = pcbComponent.center || { x: 0, y: 0 };
  const componentRotation = pcbComponent.rotation || 0;
  const absoluteToComponentRelativeTransform = inverse(
    compose6(
      translate4(componentCenter.x, componentCenter.y),
      rotate3(componentRotation * Math.PI / 180)
    )
  );
  const relativeElements = db.toArray().filter(
    (elm) => "pcb_component_id" in elm && elm.pcb_component_id === pcbComponent.pcb_component_id
  );
  const clonedRelativeElements = structuredClone(relativeElements);
  transformPCBElements(
    clonedRelativeElements,
    absoluteToComponentRelativeTransform
  );
  const components = createComponentsFromCircuitJson(
    {
      componentName,
      componentRotation: "0deg"
    },
    clonedRelativeElements
  );
  return components;
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateFootprintComponent.ts
var inflateFootprintComponent = (pcbElm, inflatorContext) => {
  const { injectionDb, normalComponent } = inflatorContext;
  if (!normalComponent) return null;
  const primitives = extractPcbPrimitivesFromCircuitJson({
    pcbComponent: pcbElm,
    db: injectionDb,
    componentName: normalComponent.name
  });
  if (primitives.length === 0) return null;
  const footprint = new Footprint({ originalLayer: pcbElm.layer });
  footprint.addAll(primitives);
  return footprint;
};

// lib/components/primitive-components/Group/Subcircuit/inflators/getInflatedPcbPlacement.ts
var getInflatedPcbPlacement = ({
  pcbComponent,
  sourceGroupId,
  inflatorContext
}) => {
  if (!pcbComponent?.center) {
    return { pcbX: void 0, pcbY: void 0 };
  }
  if (sourceGroupId) {
    const parentPcbGroup = inflatorContext.injectionDb.pcb_group.getWhere({
      source_group_id: sourceGroupId
    });
    if (parentPcbGroup?.anchor_position) {
      return {
        pcbX: pcbComponent.center.x - parentPcbGroup.anchor_position.x,
        pcbY: pcbComponent.center.y - parentPcbGroup.anchor_position.y
      };
    }
  }
  return {
    pcbX: pcbComponent.center.x,
    pcbY: pcbComponent.center.y
  };
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceCapacitor.ts
function inflateSourceCapacitor(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const cadElm = injectionDb.cad_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const capacitor = new Capacitor({
    name: sourceElm.name,
    capacitance: sourceElm.capacitance,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: capacitor
    });
    if (footprint) {
      capacitor.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(capacitor);
  } else {
    subcircuit.add(capacitor);
  }
}

// lib/components/normal-components/Chip.ts
import { chipProps } from "@tscircuit/props";
import { normalizeDegrees as normalizeDegrees3 } from "@tscircuit/math-utils";
import { pcb_component_invalid_layer_error as pcb_component_invalid_layer_error2 } from "circuit-json";
var Chip = class extends NormalComponent3 {
  schematicBoxDimensions = null;
  constructor(props) {
    super(props);
  }
  get config() {
    return {
      componentName: "Chip",
      zodProps: chipProps,
      shouldRenderAsSchematicBox: true
    };
  }
  initPorts(opts = {}) {
    super.initPorts(opts);
    const { _parsedProps: props } = this;
    if (props.externallyConnectedPins) {
      const requiredPorts = /* @__PURE__ */ new Set();
      for (const [pin1, pin2] of props.externallyConnectedPins) {
        requiredPorts.add(pin1);
        requiredPorts.add(pin2);
      }
      for (const pinIdentifier of requiredPorts) {
        const existingPort = this.children.find(
          (child) => child instanceof Port && child.isMatchingAnyOf([pinIdentifier])
        );
        if (!existingPort) {
          const pinMatch = pinIdentifier.match(/^pin(\d+)$/);
          if (pinMatch) {
            const pinNumber = parseInt(pinMatch[1]);
            this.add(
              new Port({
                pinNumber,
                aliases: [pinIdentifier]
              })
            );
          } else {
            this.add(
              new Port({
                name: pinIdentifier,
                aliases: [pinIdentifier]
              })
            );
          }
        }
      }
    }
  }
  doInitialSchematicComponentRender() {
    const { _parsedProps: props } = this;
    if (props?.noSchematicRepresentation === true) return;
    super.doInitialSchematicComponentRender();
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_chip",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber,
      supplier_part_numbers: props.supplierPartNumbers,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { pcbX, pcbY } = this.getResolvedPcbPositionProp();
    const footprint = props.footprint ?? this._getImpliedFootprintString();
    const hasFootprintChild = this.children.some(
      (c) => c.componentName === "Footprint"
    );
    if (!footprint && !hasFootprintChild && !this._hasStartedFootprintUrlLoad) {
      const footprint_error = db.pcb_missing_footprint_error.insert({
        message: `No footprint specified for component: ${this.getString()}`,
        source_component_id: `${this.source_component_id}`,
        error_type: "pcb_missing_footprint_error"
      });
      this.pcb_missing_footprint_error_id = footprint_error.pcb_missing_footprint_error_id;
    }
    const componentLayer = this._getPcbComponentLayer();
    if (componentLayer !== "top" && componentLayer !== "bottom") {
      const subcircuit = this.getSubcircuit();
      const error = pcb_component_invalid_layer_error2.parse({
        type: "pcb_component_invalid_layer_error",
        message: `Component cannot be placed on layer '${componentLayer}'. Components can only be placed on 'top' or 'bottom' layers.`,
        source_component_id: this.source_component_id,
        layer: componentLayer,
        subcircuit_id: subcircuit.subcircuit_id ?? void 0
      });
      db.pcb_component_invalid_layer_error.insert(error);
    }
    const globalTransformRotation = this.getGlobalTransformRotation();
    const pcb_component = db.pcb_component.insert({
      center: { x: pcbX, y: pcbY },
      width: 2,
      // Default width, adjust as needed
      height: 3,
      // Default height, adjust as needed
      layer: componentLayer === "top" || componentLayer === "bottom" ? componentLayer : "top",
      rotation: normalizeDegrees3(globalTransformRotation),
      insertion_direction: this._getPcbComponentInsertionDirection(
        componentLayer,
        globalTransformRotation
      ),
      source_component_id: this.source_component_id,
      subcircuit_id: this.getSubcircuit().subcircuit_id ?? void 0,
      do_not_place: props.doNotPlace ?? false,
      obstructs_within_bounds: props.obstructsWithinBounds ?? true,
      is_allowed_to_be_off_board: props.allowOffBoard ?? false,
      metadata: props.kicadFootprintMetadata ? { kicad_footprint: props.kicadFootprintMetadata } : void 0
    });
    this.pcb_component_id = pcb_component.pcb_component_id;
  }
  doInitialCreateTracesFromProps() {
    const { _parsedProps: props } = this;
    if (props.externallyConnectedPins) {
      for (const [pin1, pin2] of props.externallyConnectedPins) {
        this.add(
          new Trace3({
            from: `${this.getSubcircuitSelector()} > port.${pin1}`,
            to: `${this.getSubcircuitSelector()} > port.${pin2}`
          })
        );
      }
    }
    this._createTracesFromConnectionsProp();
  }
  doInitialSimulationRender() {
    const { db } = this.root;
    const { pinAttributes } = this.props;
    if (!pinAttributes) return;
    let powerPort = null;
    let groundPort = null;
    let voltage;
    const ports = this.selectAll("port");
    for (const port of ports) {
      for (const alias of port.getNameAndAliases()) {
        if (pinAttributes[alias]) {
          const attributes = pinAttributes[alias];
          if (attributes.providesPower) {
            powerPort = port;
            voltage = attributes.providesVoltage;
          }
          if (attributes.providesGround) {
            groundPort = port;
          }
        }
      }
    }
    if (!powerPort || !groundPort || voltage === void 0) {
      return;
    }
    const powerSourcePort = db.source_port.get(powerPort.source_port_id);
    if (!powerSourcePort?.subcircuit_connectivity_map_key) return;
    const groundSourcePort = db.source_port.get(groundPort.source_port_id);
    if (!groundSourcePort?.subcircuit_connectivity_map_key) return;
    const powerNet = db.source_net.getWhere({
      subcircuit_connectivity_map_key: powerSourcePort.subcircuit_connectivity_map_key
    });
    const groundNet = db.source_net.getWhere({
      subcircuit_connectivity_map_key: groundSourcePort.subcircuit_connectivity_map_key
    });
    if (!powerNet || !groundNet) {
      return;
    }
    ;
    db.simulation_voltage_source.insert({
      type: "simulation_voltage_source",
      positive_source_port_id: powerPort.source_port_id,
      positive_source_net_id: powerNet.source_net_id,
      negative_source_port_id: groundPort.source_port_id,
      negative_source_net_id: groundNet.source_net_id,
      voltage
    });
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceChip.ts
var mapInternallyConnectedSourcePortIdsToPinLabels = (sourcePortIds, inflatorContext) => {
  if (!sourcePortIds || sourcePortIds.length === 0) return void 0;
  const { injectionDb } = inflatorContext;
  const mapped = sourcePortIds.map(
    (group) => group.map((sourcePortId) => {
      const port = injectionDb.source_port.get(
        sourcePortId
      );
      if (!port) return null;
      if (port.pin_number !== void 0 && port.pin_number !== null) {
        return `pin${port.pin_number}`;
      }
      return port.name;
    }).filter((value) => value !== null)
  ).filter((group) => group.length > 0);
  return mapped.length > 0 ? mapped : void 0;
};
var inflateSourceChip = (sourceElm, inflatorContext) => {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const schematicElm = injectionDb.schematic_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const cadElm = injectionDb.cad_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const internallyConnectedPins = mapInternallyConnectedSourcePortIdsToPinLabels(
    sourceElm.internally_connected_source_port_ids,
    inflatorContext
  );
  const footprinterString = cadElm?.footprinter_string ?? null;
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const chip = new Chip({
    name: sourceElm.name,
    manufacturerPartNumber: sourceElm.manufacturer_part_number,
    supplierPartNumbers: sourceElm.supplier_part_numbers ?? void 0,
    pinLabels: schematicElm?.port_labels ?? void 0,
    schWidth: schematicElm?.size?.width,
    schHeight: schematicElm?.size?.height,
    schPinSpacing: schematicElm?.pin_spacing,
    schX: schematicElm?.center?.x,
    schY: schematicElm?.center?.y,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds,
    internallyConnectedPins
  });
  if (footprinterString) {
    Object.assign(chip.props, { footprint: footprinterString });
    Object.assign(chip._parsedProps, { footprint: footprinterString });
  }
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: chip
    });
    if (footprint) {
      chip.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(chip);
  } else {
    subcircuit.add(chip);
  }
};

// lib/components/normal-components/Connector.ts
import { guessCableInsertCenter } from "@tscircuit/infer-cable-insertion-point";
import {
  connectorProps
} from "@tscircuit/props";
import { source_part_not_found_warning as source_part_not_found_warning3 } from "circuit-json";

// lib/utils/connectors/usb-c-canonical-pin-definitions.ts
var STANDARD_USB_C_PIN_LABELS = [
  { label: "GND1", aliases: [] },
  { label: "VBUS1", aliases: [] },
  { label: "CC1", aliases: [] },
  { label: "DP1", aliases: [] },
  { label: "DM1", aliases: ["DN1"] },
  { label: "SBU1", aliases: [] },
  { label: "SBU2", aliases: [] },
  { label: "DM2", aliases: ["DN2"] },
  { label: "DP2", aliases: [] },
  { label: "CC2", aliases: [] },
  { label: "VBUS2", aliases: [] },
  { label: "GND2", aliases: [] },
  { label: "SHELL1", aliases: ["MH1", "EH1", "MOUNT1"] },
  { label: "SHELL2", aliases: ["MH2", "EH2", "MOUNT2"] },
  { label: "SHELL3", aliases: ["MH3", "EH3", "MOUNT3"] },
  { label: "SHELL4", aliases: ["MH4", "EH4", "MOUNT4"] }
];

// lib/utils/connectors/convertCircuitJsonToUsbCStandardCircuitJson.ts
var PIN_NUMBER_HINT_PATTERN = /^(?:pin)?(\d+)$/i;
var dedupeHintsPreservingOrder = (hints) => Array.from(new Set(hints));
var convertCircuitJsonToUsbCStandardCircuitJson = (partCircuitJson) => {
  const unassignedPorts = [];
  for (const elm of partCircuitJson) {
    if (elm.type !== "source_port") continue;
    const pinNumber = elm.pin_number;
    if (typeof pinNumber !== "number") continue;
    const upperCaseHints = /* @__PURE__ */ new Set();
    for (const hint of elm.port_hints ?? []) {
      upperCaseHints.add(hint.trim().toUpperCase());
    }
    unassignedPorts.push({ pinKey: `pin${pinNumber}`, upperCaseHints });
  }
  const canonicalHintsByPin = {};
  for (const { label, aliases } of STANDARD_USB_C_PIN_LABELS) {
    const canonicalAndAliasHintsUpper = [label, ...aliases].map(
      (s) => s.toUpperCase()
    );
    const matchIndex = unassignedPorts.findIndex(
      (port) => canonicalAndAliasHintsUpper.some((hint) => port.upperCaseHints.has(hint))
    );
    if (matchIndex === -1) continue;
    const { pinKey } = unassignedPorts[matchIndex];
    canonicalHintsByPin[pinKey] = [label];
    unassignedPorts.splice(matchIndex, 1);
  }
  if (Object.keys(canonicalHintsByPin).length === 0) return partCircuitJson;
  return partCircuitJson.map((elm) => {
    if (!("port_hints" in elm) || !Array.isArray(elm.port_hints)) return elm;
    const originalHints = elm.port_hints.filter((h) => typeof h === "string").map((h) => h.trim()).filter((h) => h.length > 0);
    if (originalHints.length === 0) return elm;
    const pinKeys = /* @__PURE__ */ new Set();
    if (elm.type === "source_port" && typeof elm.pin_number === "number") {
      pinKeys.add(`pin${elm.pin_number}`);
    }
    for (const hint of originalHints) {
      const matchedPin = hint.match(PIN_NUMBER_HINT_PATTERN);
      if (!matchedPin) continue;
      pinKeys.add(`pin${Number.parseInt(matchedPin[1], 10)}`);
    }
    const canonicalHintsToAdd = [];
    for (const pinKey of pinKeys) {
      canonicalHintsToAdd.push(...canonicalHintsByPin[pinKey] ?? []);
    }
    if (canonicalHintsToAdd.length === 0) return elm;
    return {
      ...elm,
      port_hints: dedupeHintsPreservingOrder([
        ...originalHints,
        ...canonicalHintsToAdd
      ])
    };
  });
};

// lib/components/normal-components/Connector.ts
import { symbols as symbols3 } from "schematic-symbols";

// lib/components/normal-components/Connector_insertInnerSymbolInSchematicBox.ts
var INNER_SYMBOL_SCALE_FACTOR = 0.5;
var INNER_SYMBOL_LEFT_SHIFT_FACTOR = 0.15;
function insertInnerSymbolInSchematicBox(connector, symbol) {
  if (!connector.schematic_component_id || !connector.root) return;
  const { db } = connector.root;
  const schematicComponent = db.schematic_component.get(
    connector.schematic_component_id
  );
  if (!schematicComponent) return;
  if (schematicComponent.symbol_name) return;
  const targetWidth = schematicComponent.size.width * INNER_SYMBOL_SCALE_FACTOR;
  const targetHeight = schematicComponent.size.height * INNER_SYMBOL_SCALE_FACTOR;
  const scaleFactor = Math.min(
    targetWidth / symbol.size.width,
    targetHeight / symbol.size.height
  );
  if (!Number.isFinite(scaleFactor) || scaleFactor <= 0) return;
  const subcircuit_id = connector.getSubcircuit()?.subcircuit_id ?? void 0;
  const center = schematicComponent.center;
  const symbolCenter = symbol.center;
  const centerOffsetX = -schematicComponent.size.width * INNER_SYMBOL_LEFT_SHIFT_FACTOR;
  const transformPoint = (point6) => ({
    x: center.x + centerOffsetX + (point6.x - symbolCenter.x) * scaleFactor,
    y: center.y + (point6.y - symbolCenter.y) * scaleFactor
  });
  for (const primitive of symbol.primitives) {
    if (primitive.type === "path") {
      const points = primitive.points.map(transformPoint);
      if (primitive.closed && points.length > 1) {
        const first = points[0];
        const last = points[points.length - 1];
        if (first.x !== last.x || first.y !== last.y) {
          points.push(first);
        }
      }
      db.schematic_path.insert({
        schematic_component_id: connector.schematic_component_id,
        points,
        is_filled: primitive.fill ?? false,
        is_dashed: false,
        fill_color: primitive.fill ? SCHEMATIC_COMPONENT_OUTLINE_COLOR : void 0,
        stroke_width: 0.02,
        subcircuit_id,
        schematic_sheet_id: connector._resolveSchematicSheetId()
      });
    } else if (primitive.type === "circle") {
      db.schematic_circle.insert({
        schematic_component_id: connector.schematic_component_id,
        center: transformPoint({ x: primitive.x, y: primitive.y }),
        radius: primitive.radius * scaleFactor,
        stroke_width: 0.02,
        color: SCHEMATIC_COMPONENT_OUTLINE_COLOR,
        is_filled: primitive.fill,
        fill_color: primitive.fill ? SCHEMATIC_COMPONENT_OUTLINE_COLOR : void 0,
        is_dashed: false,
        subcircuit_id,
        schematic_sheet_id: connector._resolveSchematicSheetId()
      });
    } else if (primitive.type === "box") {
      const topLeft = transformPoint({ x: primitive.x, y: primitive.y });
      const bottomRight = transformPoint({
        x: primitive.x + primitive.width,
        y: primitive.y + primitive.height
      });
      db.schematic_rect.insert({
        schematic_component_id: connector.schematic_component_id,
        center: {
          x: (topLeft.x + bottomRight.x) / 2,
          y: (topLeft.y + bottomRight.y) / 2
        },
        width: Math.abs(bottomRight.x - topLeft.x),
        height: Math.abs(bottomRight.y - topLeft.y),
        stroke_width: 0.02,
        color: SCHEMATIC_COMPONENT_OUTLINE_COLOR,
        is_filled: false,
        is_dashed: false,
        rotation: 0,
        subcircuit_id,
        schematic_sheet_id: connector._resolveSchematicSheetId()
      });
    }
  }
}

// lib/components/normal-components/Connector.ts
var USB_C_SIGNAL_LABELS_IN_ORDER = [
  "VBUS1",
  "VBUS2",
  "CC1",
  "CC2",
  "DP1",
  "DP2",
  "DM1",
  "DM2",
  "SBU1",
  "SBU2",
  "GND1",
  "GND2"
];
var USB_C_SHELL_LABELS_IN_ORDER = [
  "SHELL1",
  "SHELL2",
  "SHELL3",
  "SHELL4"
];
var USB_C_CANONICAL_LABELS_IN_ORDER = [
  ...USB_C_SIGNAL_LABELS_IN_ORDER,
  ...USB_C_SHELL_LABELS_IN_ORDER
];
var USB_C_CANONICAL_LABELS = /* @__PURE__ */ new Set([
  ...USB_C_CANONICAL_LABELS_IN_ORDER
]);
var USB_C_DEFAULT_SCH_PIN_STYLE_BY_LABEL = [
  // Group spacing on right side
  ["CC1", { marginTop: 0.15 }],
  ["DP1", { marginTop: 0.15 }],
  ["SBU1", { marginTop: 0.15 }],
  ["GND1", { marginTop: 0.15 }],
  // Bottom-side label spacing uses horizontal margins.
  ["SHELL4", { marginRight: 0.15 }]
];
var Connector = class extends Chip {
  _getConnectorProps() {
    return this._parsedProps;
  }
  _hasExplicitFootprint() {
    const props = this._getConnectorProps();
    return props.footprint !== void 0 || this.children.some((child) => child.componentName === "Footprint");
  }
  _shouldUseStandardPartsEngineCircuitJsonFlow() {
    const props = this._getConnectorProps();
    if (!props.standard) return false;
    if (this._hasExplicitFootprint()) return false;
    if (this.getInheritedProperty("partsEngineDisabled")) return false;
    return true;
  }
  _addUsbCCanonicalFallbackPorts() {
    if (this.selectAll("port").length > 0) return;
    this.addAll(
      STANDARD_USB_C_PIN_LABELS.map(
        ({ label, aliases }, pinIndex) => new Port({
          pinNumber: pinIndex + 1,
          name: label,
          aliases: [...aliases]
        })
      )
    );
    this._markDirty("InitializePortsFromChildren");
  }
  _handleStandardConnectorCircuitJsonFailure(standard, message) {
    const { db } = this.root;
    const warning = source_part_not_found_warning3.parse({
      type: "source_part_not_found_warning",
      message: `Failed to fetch circuit JSON for ${this.getString()} (standard="${standard}"): ${message}`,
      source_component_id: this.source_component_id ?? void 0,
      subcircuit_id: this.getSubcircuit()?.subcircuit_id ?? void 0,
      manufacturer_part_number: this._getConnectorProps().manufacturerPartNumber ?? this._getConnectorProps().mfn ?? void 0,
      part_name: this.name
    });
    db.source_part_not_found_warning.insert(warning);
    if (standard === "usb_c") {
      this._addUsbCCanonicalFallbackPorts();
    }
  }
  _getSupplierPartNumbersToTry(supplierPartNumbers) {
    const partNumbers = [];
    for (const supplier of Object.keys(supplierPartNumbers ?? {})) {
      const nums = supplierPartNumbers?.[supplier];
      if (Array.isArray(nums) && nums.length > 0) {
        partNumbers.push(nums[0]);
      }
    }
    return partNumbers;
  }
  async _tryFetchPartCircuitJson(fetchPartCircuitJson, params) {
    const maybeCircuitJson = await Promise.resolve(fetchPartCircuitJson(params)) ?? null;
    if (Array.isArray(maybeCircuitJson) && maybeCircuitJson.length > 0) {
      return maybeCircuitJson;
    }
    return null;
  }
  async _fetchStandardConnectorCircuitJson(fetchPartCircuitJson, supplierPartNumbers, manufacturerPartNumber) {
    for (const supplierPartNumber of this._getSupplierPartNumbersToTry(
      supplierPartNumbers
    )) {
      const circuitJson = await this._tryFetchPartCircuitJson(
        fetchPartCircuitJson,
        { supplierPartNumber }
      );
      if (circuitJson) return circuitJson;
    }
    if (!manufacturerPartNumber) return null;
    return this._tryFetchPartCircuitJson(fetchPartCircuitJson, {
      manufacturerPartNumber
    });
  }
  _addConnectorFootprintFromCircuitJson(standard, circuitJson) {
    const props = this._getConnectorProps();
    const standardizedCircuitJson = standard === "usb_c" ? convertCircuitJsonToUsbCStandardCircuitJson(circuitJson) : circuitJson;
    const fpComponents = createComponentsFromCircuitJson(
      {
        componentName: this.name,
        componentRotation: String(props.pcbRotation ?? 0),
        footprinterString: `standard:${standard}`,
        pinLabels: props.pinLabels,
        pcbPinLabels: props.pcbPinLabels
      },
      standardizedCircuitJson
    );
    const fetchedCadModel = extractCadModelFromCircuitJson(
      standardizedCircuitJson
    );
    if (fetchedCadModel) {
      this._asyncFootprintCadModel = fetchedCadModel;
    }
    this.addAll(fpComponents);
    this._markDirty("InitializePortsFromChildren");
  }
  _getUsbCCanonicalLabelToPinNumberMap() {
    const labelToPinNumber = /* @__PURE__ */ new Map();
    const ports = this.selectAll("port");
    for (const port of ports) {
      const pinNumber = port.props.pinNumber;
      if (typeof pinNumber !== "number") continue;
      for (const alias of port.getNameAndAliases()) {
        const normalizedAlias = alias.trim().toUpperCase();
        if (!USB_C_CANONICAL_LABELS.has(normalizedAlias)) continue;
        const label = normalizedAlias;
        if (!labelToPinNumber.has(label)) {
          labelToPinNumber.set(label, pinNumber);
        }
      }
    }
    return labelToPinNumber;
  }
  _getSchematicPortArrangement() {
    const arrangement = super._getSchematicPortArrangement();
    if (arrangement && Object.keys(arrangement).length > 0) return arrangement;
    if (this._getConnectorProps().standard !== "usb_c") return arrangement;
    const labelToPinNumber = this._getUsbCCanonicalLabelToPinNumberMap();
    const rightPins = USB_C_SIGNAL_LABELS_IN_ORDER.map(
      (label) => labelToPinNumber.get(label)
    ).filter((pin) => typeof pin === "number");
    const bottomPins = USB_C_SHELL_LABELS_IN_ORDER.map(
      (label) => labelToPinNumber.get(label)
    ).filter((pin) => typeof pin === "number");
    if (rightPins.length === 0 && bottomPins.length === 0) return arrangement;
    const canonicalArrangement = {};
    if (rightPins.length > 0) {
      canonicalArrangement.rightSide = {
        pins: rightPins,
        direction: "top-to-bottom"
      };
    }
    if (bottomPins.length > 0) {
      canonicalArrangement.bottomSide = {
        pins: bottomPins,
        direction: "left-to-right"
      };
    }
    return canonicalArrangement;
  }
  _getSchematicBoxDimensions() {
    if (this._getConnectorProps().standard !== "usb_c") {
      return super._getSchematicBoxDimensions();
    }
    if (this.getSchematicSymbol()) return null;
    if (!this.config.shouldRenderAsSchematicBox) return null;
    const { _parsedProps: props } = this;
    const pinCount = this._getPrimaryPinCount();
    const pinSpacing = 0.2;
    const pinLabelsFromPorts = this._getPinLabelsFromPorts();
    const allPinLabels = {
      ...pinLabelsFromPorts
    };
    if (props.pinLabels) {
      for (const [k, v] of Object.entries(props.pinLabels)) {
        if (typeof v === "string") allPinLabels[k] = v;
      }
    }
    const labelToPinNumber = this._getUsbCCanonicalLabelToPinNumberMap();
    const resolvedDefaultSchPinStyle = {};
    for (const [label, style] of USB_C_DEFAULT_SCH_PIN_STYLE_BY_LABEL) {
      const pinNumber = labelToPinNumber.get(label);
      if (typeof pinNumber !== "number") continue;
      resolvedDefaultSchPinStyle[`pin${pinNumber}`] = style;
    }
    const mergedSchPinStyle = {
      ...resolvedDefaultSchPinStyle,
      ...props.schPinStyle ?? {}
    };
    const schPortArrangement = this._getSchematicPortArrangement();
    return getAllDimensionsForSchematicBox({
      schWidth: props.schWidth,
      schHeight: props.schHeight,
      schPinSpacing: pinSpacing,
      numericSchPinStyle: getNumericSchPinStyle(
        mergedSchPinStyle,
        allPinLabels
      ),
      pinCount,
      schPortArrangement: schPortArrangement ?? void 0,
      pinLabels: allPinLabels
    });
  }
  get config() {
    return {
      componentName: "Connector",
      zodProps: connectorProps,
      shouldRenderAsSchematicBox: true
    };
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const props = this._getConnectorProps();
    const manufacturerPartNumber = props.manufacturerPartNumber ?? props.mfn;
    const source_component = db.source_component.insert({
      ftype: "simple_connector",
      name: this.name,
      manufacturer_part_number: manufacturerPartNumber,
      supplier_part_numbers: props.supplierPartNumbers,
      display_name: props.displayName,
      standard: props.standard
    });
    this.source_component_id = source_component.source_component_id;
    if (props.standard && !manufacturerPartNumber) {
      db.source_missing_manufacturer_part_number_warning.insert({
        source_component_id: this.source_component_id,
        standard: props.standard,
        subcircuit_id: this.getSubcircuit()?.subcircuit_id ?? void 0,
        warning_type: "source_missing_manufacturer_part_number_warning",
        message: `${this.getString()} has standard="${props.standard}" but no manufacturerPartNumber (mfn). Add mfn if you do not want the USB-C part to change in future.`
      });
    }
  }
  _isUsingStandardPartsEngineCircuitJsonFlow() {
    if (!this._shouldUseStandardPartsEngineCircuitJsonFlow()) return false;
    const partsEngine = this.getInheritedProperty("partsEngine");
    return Boolean(partsEngine?.fetchPartCircuitJson);
  }
  doInitialFetchPartFootprint() {
    const props = this._getConnectorProps();
    const standard = props.standard;
    if (!standard) return;
    if (!this._shouldUseStandardPartsEngineCircuitJsonFlow()) return;
    if (this._hasStartedFootprintUrlLoad) return;
    const partsEngine = this.getInheritedProperty("partsEngine");
    if (partsEngine && !partsEngine.fetchPartCircuitJson) {
      this._handleStandardConnectorCircuitJsonFailure(
        standard,
        "partsEngine.fetchPartCircuitJson is not configured"
      );
      return;
    }
    const fetchPartCircuitJson = partsEngine?.fetchPartCircuitJson;
    if (!fetchPartCircuitJson) return;
    this._hasStartedFootprintUrlLoad = true;
    const sourceComponentForQuery = {
      type: "source_component",
      ftype: "simple_connector",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      standard
    };
    this._queueAsyncEffect("load-standard-connector-circuit-json", async () => {
      const { db } = this.root;
      try {
        const supplierPartNumbers = await this._getSupplierPartNumbers(
          partsEngine,
          sourceComponentForQuery,
          `standard:${standard}`
        );
        if (this.source_component_id) {
          db.source_component.update(this.source_component_id, {
            supplier_part_numbers: supplierPartNumbers
          });
        }
        const circuitJson = await this._fetchStandardConnectorCircuitJson(
          fetchPartCircuitJson,
          supplierPartNumbers,
          sourceComponentForQuery.manufacturer_part_number
        );
        if (!circuitJson) {
          this._handleStandardConnectorCircuitJsonFailure(
            standard,
            "part circuit JSON was not found"
          );
          return;
        }
        this._addConnectorFootprintFromCircuitJson(standard, circuitJson);
      } catch (error) {
        if (this.source_component_id) {
          db.source_component.update(this.source_component_id, {
            supplier_part_numbers: {}
          });
        }
        this._handleStandardConnectorCircuitJsonFailure(standard, error.message);
      }
    });
  }
  doInitialPartsEngineRender() {
    if (this._isUsingStandardPartsEngineCircuitJsonFlow()) return;
    super.doInitialPartsEngineRender();
  }
  updatePartsEngineRender() {
    if (this._isUsingStandardPartsEngineCircuitJsonFlow()) return;
    super.updatePartsEngineRender();
  }
  doInitialSchematicComponentRender() {
    super.doInitialSchematicComponentRender();
    if (!this.root?.schematicDisabled && this.schematic_component_id && this._getConnectorProps().standard === "usb_c") {
      const usbcSymbol = symbols3.usbc;
      if (usbcSymbol) {
        insertInnerSymbolInSchematicBox(this, usbcSymbol);
      }
    }
  }
  doInitialPcbComponentSizeCalculation() {
    super.doInitialPcbComponentSizeCalculation();
    if (this.root?.pcbDisabled) return;
    if (!this.pcb_component_id || !this.source_component_id) return;
    const { db } = this.root;
    const connectorCircuitJson = db.toArray().filter((elm) => {
      if (elm.type === "pcb_component") {
        return elm.pcb_component_id === this.pcb_component_id;
      }
      if (elm.type === "source_component") {
        return elm.source_component_id === this.source_component_id;
      }
      if ("pcb_component_id" in elm && elm.pcb_component_id === this.pcb_component_id) {
        return true;
      }
      if ("source_component_id" in elm && elm.source_component_id === this.source_component_id) {
        return true;
      }
      return false;
    });
    if (connectorCircuitJson.length === 0) return;
    const inferredInsertionCenter = guessCableInsertCenter(
      connectorCircuitJson
    );
    db.pcb_component.update(this.pcb_component_id, {
      cable_insertion_center: {
        x: inferredInsertionCenter.x,
        y: inferredInsertionCenter.y
      }
    });
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceConnector.ts
var getImportedSchPortArrangement = (schematicElm) => {
  const arrangement = schematicElm?.port_arrangement;
  if (!arrangement) return void 0;
  if ("left_size" in arrangement) {
    return {
      leftPinCount: arrangement.left_size,
      rightPinCount: arrangement.right_size,
      topPinCount: arrangement.top_size,
      bottomPinCount: arrangement.bottom_size
    };
  }
  const importedArrangement = {};
  if (arrangement.left_side) {
    importedArrangement.leftSide = {
      pins: arrangement.left_side.pins,
      direction: arrangement.left_side.direction ?? "top-to-bottom"
    };
  }
  if (arrangement.right_side) {
    importedArrangement.rightSide = {
      pins: arrangement.right_side.pins,
      direction: arrangement.right_side.direction ?? "top-to-bottom"
    };
  }
  if (arrangement.top_side) {
    importedArrangement.topSide = {
      pins: arrangement.top_side.pins,
      direction: arrangement.top_side.direction ?? "left-to-right"
    };
  }
  if (arrangement.bottom_side) {
    importedArrangement.bottomSide = {
      pins: arrangement.bottom_side.pins,
      direction: arrangement.bottom_side.direction ?? "left-to-right"
    };
  }
  return importedArrangement;
};
var getImportedConnectorPinLabels = (sourceElm, inflatorContext) => {
  const sourcePorts = inflatorContext.injectionDb.source_port.list().filter(
    (port) => port.source_component_id === sourceElm.source_component_id
  );
  const pinLabels = {};
  for (const sourcePort of sourcePorts) {
    const pinNumber = sourcePort.pin_number;
    if (pinNumber === void 0 || pinNumber === null) continue;
    const labels = Array.from(
      new Set(
        [sourcePort.name, ...sourcePort.port_hints ?? []].filter(
          (label) => typeof label === "string" && label.length > 0
        )
      )
    );
    if (labels.length > 0) pinLabels[`pin${pinNumber}`] = labels;
  }
  if (Object.keys(pinLabels).length === 0) return void 0;
  return pinLabels;
};
function inflateSourceConnector(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const schematicElm = injectionDb.schematic_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const cadElm = injectionDb.cad_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const connectorProps2 = {
    name: sourceElm.name,
    standard: sourceElm.standard,
    manufacturerPartNumber: sourceElm.manufacturer_part_number,
    supplierPartNumbers: sourceElm.supplier_part_numbers ?? void 0,
    pinLabels: getImportedConnectorPinLabels(sourceElm, inflatorContext) ?? schematicElm?.port_labels ?? void 0,
    schPortArrangement: getImportedSchPortArrangement(schematicElm),
    schWidth: schematicElm?.size?.width,
    schHeight: schematicElm?.size?.height,
    schPinSpacing: schematicElm?.pin_spacing,
    schX: schematicElm?.center?.x,
    schY: schematicElm?.center?.y,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  };
  const connector = new Connector(connectorProps2);
  if (cadElm?.footprinter_string) {
    Object.assign(connector.props, { footprint: cadElm.footprinter_string });
    Object.assign(connector._parsedProps, {
      footprint: cadElm.footprinter_string
    });
  }
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: connector
    });
    if (footprint) connector.add(footprint);
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    groupsMap.get(sourceElm.source_group_id).add(connector);
  } else {
    subcircuit.add(connector);
  }
}

// lib/components/normal-components/Diode.ts
import { diodeProps } from "@tscircuit/props";

// lib/components/base-components/NormalComponent/utils/isFootprinterString.ts
var isFootprinterString = (footprint) => typeof footprint === "string" && !parseLibraryFootprintRef(footprint) && !isHttpUrl(footprint) && !isBlobUrl(footprint) && !isStaticAssetPath(footprint);

// lib/components/normal-components/Diode.ts
var Diode = class extends NormalComponent3 {
  get config() {
    const symbolMap = {
      schottky: "schottky_diode",
      avalanche: "avalanche_diode",
      zener: "zener_diode",
      photodiode: "photodiode"
    };
    const variantSymbol = this.props.schottky ? "schottky" : this.props.avalanche ? "avalanche" : this.props.zener ? "zener" : this.props.photo ? "photodiode" : null;
    return {
      schematicSymbolName: variantSymbol ? symbolMap[variantSymbol] : this.props.symbolName ?? "diode",
      componentName: "Diode",
      zodProps: diodeProps,
      sourceFtype: "simple_diode"
    };
  }
  initPorts() {
    const hasFootprintChild = this.children.some(
      (child) => child.componentName === "Footprint"
    );
    const footprint = this.resolveFootprint();
    const hasPinLabels = Boolean(this._resolvePinLabels());
    const shouldAddDefaultAliases = !hasPinLabels && !hasFootprintChild && (!footprint || isFootprinterString(footprint));
    super.initPorts({
      pinCount: 2,
      ignoreSymbolPorts: !hasPinLabels && !shouldAddDefaultAliases,
      additionalAliases: {
        pin1: shouldAddDefaultAliases ? ["anode", "pos", "left"] : [],
        pin2: shouldAddDefaultAliases ? ["cathode", "neg", "right"] : []
      }
    });
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_diode",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      are_pins_interchangeable: false,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  getRefDesPrefixes() {
    return ["D"];
  }
  get pos() {
    return this.portMap.pos;
  }
  get anode() {
    return this.portMap.anode;
  }
  get neg() {
    return this.portMap.neg;
  }
  get cathode() {
    return this.portMap.cathode;
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceDiode.ts
var getLabels = (sourcePort) => Array.from(
  new Set(
    [sourcePort?.name, ...sourcePort?.port_hints ?? []].filter(
      (label) => typeof label === "string" && label.length > 0
    )
  )
);
var getImportedDiodePinLabels = (sourceElm, inflatorContext) => {
  const sourcePorts = inflatorContext.injectionDb.source_port.list().filter(
    (port) => port.source_component_id === sourceElm.source_component_id
  );
  const pinLabels = {};
  for (const sourcePort of sourcePorts) {
    if (sourcePort.pin_number === void 0 || sourcePort.pin_number === null) {
      continue;
    }
    const labels = getLabels(sourcePort);
    if (labels.length === 0) continue;
    pinLabels[`pin${sourcePort.pin_number}`] = labels;
  }
  return Object.keys(pinLabels).length > 0 ? pinLabels : void 0;
};
function inflateSourceDiode(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const importedPinLabels = getImportedDiodePinLabels(
    sourceElm,
    inflatorContext
  );
  const diodeProps2 = {
    name: sourceElm.name,
    manufacturerPartNumber: sourceElm.manufacturer_part_number,
    supplierPartNumbers: sourceElm.supplier_part_numbers ?? void 0,
    pinLabels: importedPinLabels,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  };
  const diode = new Diode(diodeProps2);
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: diode
    });
    if (footprint) {
      diode.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(diode);
  } else {
    subcircuit.add(diode);
  }
}

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceFiducial.ts
import { fiducialProps } from "@tscircuit/props";
var InflatedFiducial = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "Fiducial",
      zodProps: fiducialProps,
      sourceFtype: "simple_fiducial"
    };
  }
  doInitialSchematicComponentRender() {
  }
};
function inflateSourceFiducial(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const pcbSmtPad = injectionDb.pcb_smtpad.list({ pcb_component_id: pcbElm?.pcb_component_id }).find((elm) => elm.shape === "circle");
  const padDiameter = pcbSmtPad && "radius" in pcbSmtPad ? pcbSmtPad.radius * 2 : "1mm";
  const fiducial = new InflatedFiducial({
    name: sourceElm.name,
    padDiameter,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: fiducial
    });
    if (footprint) {
      fiducial.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(fiducial);
  } else {
    subcircuit.add(fiducial);
  }
}

// lib/components/primitive-components/Group/Group.ts
import {
  convertSrjToGraphicsObject as convertSrjToGraphicsObject2,
  getRerouteSimpleRouteJson,
  reconnectReroutedSimpleRouteJsonRegion
} from "@tscircuit/capacity-autorouter";
import { getBoundsFromPoints as getBoundsFromPoints6 } from "@tscircuit/math-utils";
import {
  groupProps
} from "@tscircuit/props";
import {
  distance as distance12
} from "circuit-json";
import Debug15 from "debug";

// lib/utils/autorouting/get-pcb-component-names-by-id.ts
var getPcbComponentNamesById = (db) => {
  const namesById = /* @__PURE__ */ new Map();
  for (const pcbComponent of db.pcb_component.list()) {
    const sourceComponent = db.source_component.get(
      pcbComponent.source_component_id
    );
    if (sourceComponent?.name) {
      namesById.set(pcbComponent.pcb_component_id, sourceComponent.name);
    }
  }
  return namesById;
};

// node_modules/@tscircuit/fanout-solver/lib/fanout-solver.ts
import {
  convertSrjToGraphicsObject
} from "@tscircuit/capacity-autorouter";
import { BaseSolver } from "@tscircuit/solver-utils";

// node_modules/@tscircuit/fanout-solver/lib/build-output.ts
function createViaObstacle(plan, layerNames) {
  if (!plan.via) return null;
  const zLayers = plan.via.spanLayers.map((layer) => {
    const layerIndex = layerNames.indexOf(layer);
    if (layerIndex < 0) {
      throw new Error(
        `FanoutSolver: via for "${plan.connectionName}" uses unknown layer "${layer}"`
      );
    }
    return layerIndex;
  });
  return {
    obstacleId: `fanout-via:${plan.connectionName}`,
    type: "rect",
    center: plan.via.center,
    width: plan.via.diameter,
    height: plan.via.diameter,
    layers: plan.via.spanLayers,
    zLayers,
    __zLayers: zLayers,
    connectedTo: [plan.connectionName, plan.trace.pcb_trace_id]
  };
}
function buildOutputSimpleRouteJson(params) {
  const { inputSrj, plans, layerNames } = params;
  const outputConnections = inputSrj.connections.map((connection) => ({
    ...connection,
    pointsToConnect: connection.pointsToConnect.map((point6) => ({ ...point6 }))
  }));
  const viaObstacles = [];
  const planeTerminatedConnectionNames = /* @__PURE__ */ new Set();
  for (const plan of plans) {
    const connection = outputConnections[plan.connectionIndex];
    if (!connection) {
      throw new Error(
        `FanoutSolver: output connection index ${plan.connectionIndex} is missing`
      );
    }
    connection.pointsToConnect[plan.sourcePointIndex] = {
      x: plan.exitPoint.x,
      y: plan.exitPoint.y,
      layer: plan.targetLayer,
      pointId: plan.termination.type === "plane" ? `fanout-plane:${plan.connectionName}` : `fanout-exit:${plan.connectionName}`
    };
    if (plan.termination.type === "plane") {
      planeTerminatedConnectionNames.add(plan.connectionName);
    }
    const viaObstacle = createViaObstacle(plan, layerNames);
    if (viaObstacle) viaObstacles.push(viaObstacle);
  }
  const coordinateRoutePoints = plans.flatMap(
    (plan) => plan.trace.route.filter(
      (routePoint) => "x" in routePoint && "y" in routePoint
    )
  );
  const boundsMargin = Math.max(
    inputSrj.defaultObstacleMargin ?? 0,
    inputSrj.minTraceWidth
  );
  const outputBounds = coordinateRoutePoints.length === 0 ? { ...inputSrj.bounds } : {
    minX: Math.min(
      inputSrj.bounds.minX,
      ...coordinateRoutePoints.map(
        (routePoint) => routePoint.x - boundsMargin
      )
    ),
    maxX: Math.max(
      inputSrj.bounds.maxX,
      ...coordinateRoutePoints.map(
        (routePoint) => routePoint.x + boundsMargin
      )
    ),
    minY: Math.min(
      inputSrj.bounds.minY,
      ...coordinateRoutePoints.map(
        (routePoint) => routePoint.y - boundsMargin
      )
    ),
    maxY: Math.max(
      inputSrj.bounds.maxY,
      ...coordinateRoutePoints.map(
        (routePoint) => routePoint.y + boundsMargin
      )
    )
  };
  return {
    ...inputSrj,
    bounds: outputBounds,
    connections: outputConnections.filter(
      (connection) => !planeTerminatedConnectionNames.has(connection.name)
    ),
    buses: inputSrj.buses?.map((bus) => ({
      ...bus,
      connectionNames: bus.connectionNames.filter(
        (connectionName) => !planeTerminatedConnectionNames.has(connectionName)
      )
    })).filter((bus) => bus.connectionNames.length > 0),
    obstacles: [
      ...inputSrj.obstacles.map((obstacle) => ({
        ...obstacle,
        center: { ...obstacle.center },
        layers: [...obstacle.layers],
        connectedTo: [...obstacle.connectedTo]
      })),
      ...viaObstacles
    ],
    traces: [
      ...(inputSrj.traces ?? []).map((trace) => ({
        ...trace,
        route: trace.route.map((routePoint) => ({ ...routePoint }))
      })),
      ...plans.map((plan) => plan.trace)
    ]
  };
}

// node_modules/@tscircuit/fanout-solver/lib/layer-colors.ts
var COPPER_LAYER_COLORS = [
  "#ef4444",
  "#2563eb",
  "#16a34a",
  "#9333ea",
  "#f59e0b",
  "#0891b2",
  "#db2777",
  "#65a30d",
  "#4f46e5",
  "#ea580c"
];
function getCopperLayerColor(layerIndex) {
  if (!Number.isInteger(layerIndex) || layerIndex < 0) {
    throw new Error(
      `FanoutSolver: copper layer index must be a non-negative integer, received ${layerIndex}`
    );
  }
  return COPPER_LAYER_COLORS[layerIndex % COPPER_LAYER_COLORS.length];
}

// node_modules/@tscircuit/fanout-solver/lib/layer-names.ts
function getCopperLayerNames(layerCount) {
  if (!Number.isInteger(layerCount) || layerCount < 1) {
    throw new Error(
      `FanoutSolver: layerCount must be a positive integer, received ${layerCount}`
    );
  }
  if (layerCount === 1) return ["top"];
  if (layerCount === 2) return ["top", "bottom"];
  return [
    "top",
    ...Array.from(
      { length: layerCount - 2 },
      (_, index) => `inner${index + 1}`
    ),
    "bottom"
  ];
}
function getLayerSpan(fromLayer, toLayer, layerNames) {
  const fromIndex = layerNames.indexOf(fromLayer);
  const toIndex = layerNames.indexOf(toLayer);
  if (fromIndex < 0 || toIndex < 0) {
    throw new Error(
      `FanoutSolver: cannot build via span from "${fromLayer}" to "${toLayer}"`
    );
  }
  const firstIndex = Math.min(fromIndex, toIndex);
  const lastIndex = Math.max(fromIndex, toIndex);
  return layerNames.slice(firstIndex, lastIndex + 1);
}
function generateLayerAssignments(params) {
  const { busIds, layers, layersByBusId, maxAssignments } = params;
  if (layers.length === 0) {
    throw new Error("FanoutSolver: no escape layers are available");
  }
  if (!Number.isInteger(maxAssignments) || maxAssignments < 1) {
    throw new Error(
      `FanoutSolver: maxLayerCombinations must be positive, received ${maxAssignments}`
    );
  }
  const availableLayersByBus = busIds.map(
    (busId) => layersByBusId?.[busId] ?? layers
  );
  const busWithoutLayersIndex = availableLayersByBus.findIndex(
    (availableLayers) => availableLayers.length === 0
  );
  if (busWithoutLayersIndex >= 0) {
    throw new Error(
      `FanoutSolver: no escape layers are available for bus "${busIds[busWithoutLayersIndex]}"`
    );
  }
  const rawCombinationCount = availableLayersByBus.reduce(
    (count, availableLayers) => count * availableLayers.length,
    1
  );
  const combinationCount = Math.min(
    maxAssignments,
    Number.isFinite(rawCombinationCount) ? rawCombinationCount : maxAssignments
  );
  const assignments = [];
  const seenAssignments = /* @__PURE__ */ new Set();
  function addAssignment(layerIndexes) {
    const assignment = {};
    for (let busIndex = 0; busIndex < busIds.length; busIndex++) {
      assignment[busIds[busIndex]] = availableLayersByBus[busIndex][layerIndexes[busIndex]];
    }
    const key = JSON.stringify(assignment);
    if (seenAssignments.has(key)) return;
    seenAssignments.add(key);
    assignments.push(assignment);
  }
  if (rawCombinationCount <= maxAssignments) {
    for (let ordinal = 0; ordinal < combinationCount; ordinal++) {
      const layerIndexes = [];
      let remaining = ordinal;
      for (let busIndex = 0; busIndex < busIds.length; busIndex++) {
        const layerCount = availableLayersByBus[busIndex].length;
        const digit = remaining % layerCount;
        remaining = Math.floor(remaining / layerCount);
        layerIndexes.push((digit + busIndex) % layerCount);
      }
      addAssignment(layerIndexes);
    }
    return assignments;
  }
  function mix32(value) {
    let mixed = value | 0;
    mixed = Math.imul(mixed ^ mixed >>> 16, 569420461);
    mixed = Math.imul(mixed ^ mixed >>> 15, 1935289751);
    return (mixed ^ mixed >>> 15) >>> 0;
  }
  const balancedLayerIndexes = busIds.map(
    (_, busIndex) => busIndex % availableLayersByBus[busIndex].length
  );
  addAssignment(balancedLayerIndexes);
  const maximumAvailableLayerCount = Math.max(
    ...availableLayersByBus.map((availableLayers) => availableLayers.length)
  );
  for (let globalShift = 1; globalShift < maximumAvailableLayerCount && assignments.length < combinationCount; globalShift++) {
    addAssignment(
      balancedLayerIndexes.map(
        (layerIndex, busIndex) => (layerIndex + globalShift) % availableLayersByBus[busIndex].length
      )
    );
  }
  for (let busIndex = 0; busIndex < busIds.length && assignments.length < combinationCount; busIndex++) {
    for (let shift = 1; shift < availableLayersByBus[busIndex].length && assignments.length < combinationCount; shift++) {
      const layerIndexes = [...balancedLayerIndexes];
      layerIndexes[busIndex] = (balancedLayerIndexes[busIndex] + shift) % availableLayersByBus[busIndex].length;
      addAssignment(layerIndexes);
    }
  }
  for (let seed = 1; assignments.length < combinationCount && seed < combinationCount * 20; seed++) {
    addAssignment(
      busIds.map(
        (_, busIndex) => mix32(seed * 2654435761 + busIndex * 2246822507) % availableLayersByBus[busIndex].length
      )
    );
  }
  return assignments;
}

// node_modules/@tscircuit/fanout-solver/lib/geometry.ts
var EPSILON = 1e-9;
function obstacleIsCircular(obstacle) {
  return obstacle.shape === "circle";
}
function toObstacleLocalPoint(point6, obstacle) {
  const rotationRadians = -(obstacle.ccwRotationDegrees ?? 0) * Math.PI / 180;
  const dx = point6.x - obstacle.center.x;
  const dy = point6.y - obstacle.center.y;
  return {
    x: dx * Math.cos(rotationRadians) - dy * Math.sin(rotationRadians),
    y: dx * Math.sin(rotationRadians) + dy * Math.cos(rotationRadians)
  };
}
function distance9(a, b) {
  return Math.hypot(a.x - b.x, a.y - b.y);
}
function distancePointToSegment(point6, start, end) {
  const dx = end.x - start.x;
  const dy = end.y - start.y;
  const lengthSquared = dx * dx + dy * dy;
  const rawT = lengthSquared < EPSILON ? 0 : ((point6.x - start.x) * dx + (point6.y - start.y) * dy) / lengthSquared;
  const t = Math.max(0, Math.min(1, rawT));
  return Math.hypot(point6.x - (start.x + t * dx), point6.y - (start.y + t * dy));
}
function cross(origin, a, b) {
  return (a.x - origin.x) * (b.y - origin.y) - (a.y - origin.y) * (b.x - origin.x);
}
function segmentsProperlyCross(a, b, c, d) {
  const d1 = cross(c, d, a);
  const d2 = cross(c, d, b);
  const d3 = cross(a, b, c);
  const d4 = cross(a, b, d);
  return (d1 > 0 && d2 < 0 || d1 < 0 && d2 > 0) && (d3 > 0 && d4 < 0 || d3 < 0 && d4 > 0);
}
function distanceSegmentToSegment(firstStart, firstEnd, secondStart, secondEnd) {
  if (segmentsProperlyCross(firstStart, firstEnd, secondStart, secondEnd)) {
    return 0;
  }
  return Math.min(
    distancePointToSegment(firstStart, secondStart, secondEnd),
    distancePointToSegment(firstEnd, secondStart, secondEnd),
    distancePointToSegment(secondStart, firstStart, firstEnd),
    distancePointToSegment(secondEnd, firstStart, firstEnd)
  );
}
function pointIsInsideObstacle(point6, obstacle, tolerance = EPSILON) {
  if (obstacleIsCircular(obstacle)) {
    return distance9(point6, obstacle.center) <= obstacle.width / 2 + tolerance;
  }
  const localPoint = toObstacleLocalPoint(point6, obstacle);
  return Math.abs(localPoint.x) <= obstacle.width / 2 + tolerance && Math.abs(localPoint.y) <= obstacle.height / 2 + tolerance;
}
function distancePointToObstacle(point6, obstacle) {
  if (obstacleIsCircular(obstacle)) {
    return Math.max(0, distance9(point6, obstacle.center) - obstacle.width / 2);
  }
  const localPoint = toObstacleLocalPoint(point6, obstacle);
  const dx = Math.max(Math.abs(localPoint.x) - obstacle.width / 2, 0);
  const dy = Math.max(Math.abs(localPoint.y) - obstacle.height / 2, 0);
  return Math.hypot(dx, dy);
}
function distanceSegmentToObstacle(segment2, obstacle) {
  if (obstacleIsCircular(obstacle)) {
    return Math.max(
      0,
      distancePointToSegment(obstacle.center, segment2.start, segment2.end) - obstacle.width / 2
    );
  }
  const localStart = toObstacleLocalPoint(segment2.start, obstacle);
  const localEnd = toObstacleLocalPoint(segment2.end, obstacle);
  if (Math.abs(localStart.x) <= obstacle.width / 2 + EPSILON && Math.abs(localStart.y) <= obstacle.height / 2 + EPSILON) {
    return 0;
  }
  if (Math.abs(localEnd.x) <= obstacle.width / 2 + EPSILON && Math.abs(localEnd.y) <= obstacle.height / 2 + EPSILON) {
    return 0;
  }
  const minX = -obstacle.width / 2;
  const maxX = obstacle.width / 2;
  const minY = -obstacle.height / 2;
  const maxY = obstacle.height / 2;
  const corners = [
    { x: minX, y: minY },
    { x: maxX, y: minY },
    { x: maxX, y: maxY },
    { x: minX, y: maxY }
  ];
  let minimumDistance = Number.POSITIVE_INFINITY;
  for (let index = 0; index < corners.length; index++) {
    minimumDistance = Math.min(
      minimumDistance,
      distanceSegmentToSegment(
        localStart,
        localEnd,
        corners[index],
        corners[(index + 1) % corners.length]
      )
    );
  }
  return minimumDistance;
}
function segmentsAreClear(first, second, clearance) {
  if (first.layer !== second.layer) return true;
  const requiredDistance = (first.width + second.width) / 2 + clearance;
  return distanceSegmentToSegment(
    first.start,
    first.end,
    second.start,
    second.end
  ) >= requiredDistance - EPSILON;
}

// node_modules/@tscircuit/fanout-solver/lib/prepare-buses.ts
var FANOUT_BORDER_TARGETS = /* @__PURE__ */ new Set([
  "left",
  "right",
  "top",
  "bottom",
  "top-left",
  "top-right",
  "bottom-left",
  "bottom-right"
]);
var AVAILABLE_BOUNDARY_REGIONS = {
  top_left: {
    direction: "up",
    preferredExit: "top-left"
  },
  top_middle: {
    direction: "up",
    preferredExit: "top"
  },
  top_right: {
    direction: "up",
    preferredExit: "top-right"
  },
  right_top: {
    direction: "right",
    preferredExit: "top-right"
  },
  right_middle: {
    direction: "right",
    preferredExit: "right"
  },
  right_bottom: {
    direction: "right",
    preferredExit: "bottom-right"
  },
  bottom_right: {
    direction: "down",
    preferredExit: "bottom-right"
  },
  bottom_middle: {
    direction: "down",
    preferredExit: "bottom"
  },
  bottom_left: {
    direction: "down",
    preferredExit: "bottom-left"
  },
  left_bottom: {
    direction: "left",
    preferredExit: "bottom-left"
  },
  left_middle: {
    direction: "left",
    preferredExit: "left"
  },
  left_top: {
    direction: "left",
    preferredExit: "top-left"
  },
  top: {
    direction: "up",
    preferredExit: "top"
  },
  right: {
    direction: "right",
    preferredExit: "right"
  },
  bottom: {
    direction: "down",
    preferredExit: "bottom"
  },
  left: {
    direction: "left",
    preferredExit: "left"
  }
};
function uniqueSorted(values) {
  const sortedValues = [...values].sort((a, b) => a - b);
  const result = [];
  for (const value of sortedValues) {
    if (result.length === 0 || Math.abs(result[result.length - 1] - value) > 1e-6) {
      result.push(value);
    }
  }
  return result;
}
function getPitch(coordinates) {
  let pitch = Number.POSITIVE_INFINITY;
  for (let index = 1; index < coordinates.length; index++) {
    const difference = coordinates[index] - coordinates[index - 1];
    if (difference > 1e-6) pitch = Math.min(pitch, difference);
  }
  return pitch;
}
function getAlignedPitch(obstacles, axis) {
  const perpendicularAxis = axis === "x" ? "y" : "x";
  let pitch = Number.POSITIVE_INFINITY;
  for (let firstIndex = 0; firstIndex < obstacles.length; firstIndex++) {
    const first = obstacles[firstIndex];
    for (let secondIndex = firstIndex + 1; secondIndex < obstacles.length; secondIndex++) {
      const second = obstacles[secondIndex];
      if (Math.abs(
        first.center[perpendicularAxis] - second.center[perpendicularAxis]
      ) > 1e-6) {
        continue;
      }
      const separation = Math.abs(first.center[axis] - second.center[axis]);
      if (separation > 1e-6) pitch = Math.min(pitch, separation);
    }
  }
  return pitch;
}
function getComponentBounds(obstacles) {
  return {
    minX: Math.min(
      ...obstacles.map((obstacle) => obstacle.center.x - obstacle.width / 2)
    ),
    maxX: Math.max(
      ...obstacles.map((obstacle) => obstacle.center.x + obstacle.width / 2)
    ),
    minY: Math.min(
      ...obstacles.map((obstacle) => obstacle.center.y - obstacle.height / 2)
    ),
    maxY: Math.max(
      ...obstacles.map((obstacle) => obstacle.center.y + obstacle.height / 2)
    )
  };
}
function resolveComponentBounds(grid, options) {
  const requestedBounds = options.componentBounds?.[grid.componentId];
  if (!requestedBounds) {
    const inferredMarginX = grid.pitchX * 2.25;
    const inferredMarginY = grid.pitchY * 2.25;
    return {
      minX: grid.bounds.minX - inferredMarginX,
      maxX: grid.bounds.maxX + inferredMarginX,
      minY: grid.bounds.minY - inferredMarginY,
      maxY: grid.bounds.maxY + inferredMarginY
    };
  }
  const values = [
    requestedBounds.minX,
    requestedBounds.maxX,
    requestedBounds.minY,
    requestedBounds.maxY
  ];
  if (values.some((value) => !Number.isFinite(value)) || requestedBounds.minX >= requestedBounds.maxX || requestedBounds.minY >= requestedBounds.maxY) {
    throw new Error(
      `FanoutSolver: componentBounds for "${grid.componentId}" must contain finite, increasing bounds`
    );
  }
  if (requestedBounds.minX > grid.bounds.minX + 1e-6 || requestedBounds.maxX < grid.bounds.maxX - 1e-6 || requestedBounds.minY > grid.bounds.minY + 1e-6 || requestedBounds.maxY < grid.bounds.maxY - 1e-6) {
    throw new Error(
      `FanoutSolver: componentBounds for "${grid.componentId}" must contain every component pad`
    );
  }
  return { ...requestedBounds };
}
function validateSharedBoundary(boundary, componentGrids) {
  const values = [boundary.minX, boundary.maxX, boundary.minY, boundary.maxY];
  if (values.some((value) => !Number.isFinite(value)) || boundary.minX >= boundary.maxX || boundary.minY >= boundary.maxY) {
    throw new Error(
      "FanoutSolver: sharedBoundary must contain finite, increasing bounds"
    );
  }
  for (const grid of componentGrids) {
    if (boundary.minX > grid.bounds.minX + 1e-6 || boundary.maxX < grid.bounds.maxX - 1e-6 || boundary.minY > grid.bounds.minY + 1e-6 || boundary.maxY < grid.bounds.maxY - 1e-6) {
      throw new Error(
        `FanoutSolver: sharedBoundary must contain every pad of component "${grid.componentId}"`
      );
    }
  }
  return { ...boundary };
}
function resolveSharedBoundary(componentGrids, options) {
  if (options.sharedBoundary) {
    return validateSharedBoundary(options.sharedBoundary, componentGrids);
  }
  const componentBounds = componentGrids.map(
    (grid) => resolveComponentBounds(grid, options)
  );
  const maximumPitch = Math.max(
    ...componentGrids.flatMap((grid) => [grid.pitchX, grid.pitchY])
  );
  const inferredMargin = maximumPitch * 2.25;
  return validateSharedBoundary(
    {
      minX: Math.min(...componentBounds.map((bounds) => bounds.minX)) - inferredMargin,
      maxX: Math.max(...componentBounds.map((bounds) => bounds.maxX)) + inferredMargin,
      minY: Math.min(...componentBounds.map((bounds) => bounds.minY)) - inferredMargin,
      maxY: Math.max(...componentBounds.map((bounds) => bounds.maxY)) + inferredMargin
    },
    componentGrids
  );
}
function findComponentGrids(obstacles) {
  const obstaclesByComponent = /* @__PURE__ */ new Map();
  for (const obstacle of obstacles) {
    if (!obstacle.componentId || obstacle.isCopperPour) continue;
    const componentObstacles = obstaclesByComponent.get(obstacle.componentId) ?? [];
    componentObstacles.push(obstacle);
    obstaclesByComponent.set(obstacle.componentId, componentObstacles);
  }
  const grids = [];
  for (const [componentId, componentObstacles] of obstaclesByComponent) {
    const xCoordinates = uniqueSorted(
      componentObstacles.map((obstacle) => obstacle.center.x)
    );
    const yCoordinates = uniqueSorted(
      componentObstacles.map((obstacle) => obstacle.center.y)
    );
    const alignedPitchX = getAlignedPitch(componentObstacles, "x");
    const alignedPitchY = getAlignedPitch(componentObstacles, "y");
    const coordinatePitchX = getPitch(xCoordinates);
    const coordinatePitchY = getPitch(yCoordinates);
    const fallbackPitch = Math.min(
      ...[
        alignedPitchX,
        alignedPitchY,
        coordinatePitchX,
        coordinatePitchY
      ].filter(Number.isFinite)
    );
    const padSizeFallback = Math.max(
      ...componentObstacles.flatMap((obstacle) => [
        obstacle.width,
        obstacle.height
      ])
    );
    const resolvedFallback = Number.isFinite(fallbackPitch) ? fallbackPitch : padSizeFallback;
    const pitchX = Number.isFinite(alignedPitchX) ? alignedPitchX : Number.isFinite(coordinatePitchX) ? coordinatePitchX : resolvedFallback;
    const pitchY = Number.isFinite(alignedPitchY) ? alignedPitchY : Number.isFinite(coordinatePitchY) ? coordinatePitchY : resolvedFallback;
    grids.push({
      componentId,
      obstacles: componentObstacles,
      xCoordinates,
      yCoordinates,
      pitchX,
      pitchY,
      bounds: getComponentBounds(componentObstacles)
    });
  }
  return grids;
}
function getPointLayers(point6) {
  return "layer" in point6 ? [point6.layer] : point6.layers;
}
function findPointObstacleMatches(params) {
  const { point: point6, connection, componentGrids } = params;
  const pointLayers = getPointLayers(point6);
  const matches = [];
  for (const grid of componentGrids) {
    const candidateObstacles = grid.obstacles.filter(
      (obstacle) => obstacle.layers.some((layer) => pointLayers.includes(layer))
    ).filter((obstacle) => pointIsInsideObstacle(point6, obstacle, 1e-5)).sort((a, b) => {
      const aDirect = a.connectedTo.includes(connection.name) || a.connectedTo.includes(point6.pointId ?? "") || a.connectedTo.includes(point6.pcb_port_id ?? "");
      const bDirect = b.connectedTo.includes(connection.name) || b.connectedTo.includes(point6.pointId ?? "") || b.connectedTo.includes(point6.pcb_port_id ?? "");
      if (aDirect !== bDirect) return aDirect ? -1 : 1;
      return a.width * a.height - b.width * b.height;
    });
    if (candidateObstacles[0]) {
      matches.push({ grid, obstacle: candidateObstacles[0] });
    }
  }
  return matches;
}
function inferBusId(connection) {
  for (const point6 of connection.pointsToConnect) {
    if ("layers" in point6 && point6.busId) return point6.busId;
  }
  const nameMatch = /^BUS[_:-]([^_:-]+)(?:[_:-]\d+)?$/i.exec(connection.name);
  return nameMatch?.[1] ?? null;
}
function resolvePreferredExit(busId, value) {
  if (value === void 0) return void 0;
  if (!FANOUT_BORDER_TARGETS.has(value)) {
    throw new Error(
      `FanoutSolver: bus "${busId}" has invalid preferredExit "${value}"`
    );
  }
  return value;
}
function resolveAvailableBoundaryRegions(value) {
  if (value === void 0) return void 0;
  if (value.length === 0) {
    throw new Error(
      "FanoutSolver: availableCornersAndSides must contain at least one boundary region"
    );
  }
  const regions = [];
  const seen = /* @__PURE__ */ new Set();
  for (const input of value) {
    const region = AVAILABLE_BOUNDARY_REGIONS[input];
    if (!region) {
      throw new Error(
        `FanoutSolver: invalid availableCornersAndSides value "${input}"`
      );
    }
    const key = `${region.direction}:${region.preferredExit}`;
    if (seen.has(key)) continue;
    seen.add(key);
    regions.push(region);
  }
  return regions;
}
function resolveTermination(busId, value) {
  if (value === void 0 || value.type === "boundary") {
    return { type: "boundary" };
  }
  if (value.type !== "plane" || typeof value.layer !== "string" || value.layer.length === 0) {
    throw new Error(
      `FanoutSolver: bus "${busId}" has an invalid termination target`
    );
  }
  return { type: "plane", layer: value.layer };
}
function resolveBusSpecs(srj, options) {
  const requestedBuses = options.buses ?? srj.buses;
  const specsById = /* @__PURE__ */ new Map();
  const claimedConnectionNames = /* @__PURE__ */ new Set();
  const knownConnectionNames = new Set(
    srj.connections.map((connection) => connection.name)
  );
  for (const requestedBus of requestedBuses ?? []) {
    if (specsById.has(requestedBus.busId)) {
      throw new Error(`FanoutSolver: duplicate bus id "${requestedBus.busId}"`);
    }
    for (const connectionName of requestedBus.connectionNames) {
      if (!knownConnectionNames.has(connectionName)) {
        throw new Error(
          `FanoutSolver: bus "${requestedBus.busId}" references unknown connection "${connectionName}"`
        );
      }
      if (claimedConnectionNames.has(connectionName)) {
        throw new Error(
          `FanoutSolver: connection "${connectionName}" belongs to more than one bus`
        );
      }
      claimedConnectionNames.add(connectionName);
    }
    const termination = resolveTermination(
      requestedBus.busId,
      requestedBus.termination
    );
    const preferredExit = resolvePreferredExit(
      requestedBus.busId,
      options.busExitPreferences?.[requestedBus.busId] ?? requestedBus.preferredExit ?? options.defaultPreferredExit
    );
    if (termination.type === "plane" && preferredExit !== void 0) {
      throw new Error(
        `FanoutSolver: plane-terminated bus "${requestedBus.busId}" cannot also specify preferredExit`
      );
    }
    specsById.set(requestedBus.busId, {
      ...requestedBus,
      sourceComponentId: requestedBus.sourceComponentId ?? options.sourceComponentId,
      direction: options.busDirections?.[requestedBus.busId] ?? requestedBus.direction ?? options.defaultDirection,
      preferredExit,
      termination
    });
  }
  for (const connection of srj.connections) {
    if (claimedConnectionNames.has(connection.name)) continue;
    const inferredBusId = inferBusId(connection);
    if (inferredBusId) {
      const existing = specsById.get(inferredBusId);
      specsById.set(inferredBusId, {
        busId: inferredBusId,
        connectionNames: [
          ...existing?.connectionNames ?? [],
          connection.name
        ],
        direction: options.busDirections?.[inferredBusId] ?? existing?.direction ?? options.defaultDirection,
        sourceComponentId: existing?.sourceComponentId ?? options.sourceComponentId,
        preferredExit: resolvePreferredExit(
          inferredBusId,
          options.busExitPreferences?.[inferredBusId] ?? existing?.preferredExit ?? options.defaultPreferredExit
        ),
        termination: existing?.termination ?? { type: "boundary" }
      });
    } else {
      const singletonBusId = `connection:${connection.name}`;
      specsById.set(singletonBusId, {
        busId: singletonBusId,
        connectionNames: [connection.name],
        sourceComponentId: options.sourceComponentId,
        direction: options.busDirections?.[singletonBusId] ?? options.defaultDirection,
        preferredExit: resolvePreferredExit(
          singletonBusId,
          options.busExitPreferences?.[singletonBusId] ?? options.defaultPreferredExit
        ),
        termination: { type: "boundary" }
      });
    }
  }
  return [...specsById.values()];
}
function chooseSourceGrid(params) {
  const { busSpec, connections, componentGrids } = params;
  const matchCountByComponent = /* @__PURE__ */ new Map();
  for (const connection of connections) {
    const matchedComponents = /* @__PURE__ */ new Set();
    for (const point6 of connection.pointsToConnect) {
      for (const match of findPointObstacleMatches({
        point: point6,
        connection,
        componentGrids
      })) {
        matchedComponents.add(match.grid.componentId);
      }
    }
    for (const componentId of matchedComponents) {
      matchCountByComponent.set(
        componentId,
        (matchCountByComponent.get(componentId) ?? 0) + 1
      );
    }
  }
  const selectedGrid = [...componentGrids].sort((a, b) => {
    const countDifference = (matchCountByComponent.get(b.componentId) ?? 0) - (matchCountByComponent.get(a.componentId) ?? 0);
    if (countDifference !== 0) return countDifference;
    return b.obstacles.length - a.obstacles.length;
  })[0];
  const requestedGrid = busSpec.sourceComponentId ? componentGrids.find(
    (grid) => grid.componentId === busSpec.sourceComponentId
  ) : void 0;
  if (busSpec.sourceComponentId && !requestedGrid) {
    throw new Error(
      `FanoutSolver: source component "${busSpec.sourceComponentId}" for bus "${busSpec.busId}" was not found`
    );
  }
  const sourceGrid = requestedGrid ?? selectedGrid;
  const sourceMatchCount = sourceGrid ? matchCountByComponent.get(sourceGrid.componentId) ?? 0 : 0;
  if (!sourceGrid || sourceMatchCount !== connections.length) {
    throw new Error(
      busSpec.sourceComponentId ? `FanoutSolver: source component "${busSpec.sourceComponentId}" is not an endpoint on every connection in bus "${busSpec.busId}"` : `FanoutSolver: bus "${busSpec.busId}" does not have one component endpoint on every connection`
    );
  }
  return sourceGrid;
}
function chooseTargetPoint(sourcePoint, connection, sourcePointIndex, termination) {
  const targetCandidates = connection.pointsToConnect.filter(
    (_, pointIndex) => pointIndex !== sourcePointIndex
  );
  const targetPoint = targetCandidates.sort(
    (a, b) => distance9(sourcePoint, b) - distance9(sourcePoint, a)
  )[0];
  if (!targetPoint && termination.type === "plane") {
    return sourcePoint;
  }
  if (!targetPoint) {
    throw new Error(
      `FanoutSolver: connection "${connection.name}" has no target beyond its BGA pad`
    );
  }
  return targetPoint;
}
function prepareConnection(params) {
  const {
    connection,
    connectionIndex,
    sourceGrid,
    componentGrids,
    termination
  } = params;
  for (let sourcePointIndex = 0; sourcePointIndex < connection.pointsToConnect.length; sourcePointIndex++) {
    const sourcePoint = connection.pointsToConnect[sourcePointIndex];
    const sourceMatch = findPointObstacleMatches({
      point: sourcePoint,
      connection,
      componentGrids
    }).find((match) => match.grid.componentId === sourceGrid.componentId);
    if (!sourceMatch) continue;
    const sourceLayer = getPointLayers(sourcePoint).find(
      (layer) => sourceMatch.obstacle.layers.includes(layer)
    );
    if (!sourceLayer) {
      throw new Error(
        `FanoutSolver: connection "${connection.name}" has no source layer shared with its BGA pad`
      );
    }
    return {
      connection,
      connectionIndex,
      sourcePoint,
      sourcePointIndex,
      sourceLayer,
      sourceObstacle: sourceMatch.obstacle,
      targetPoint: chooseTargetPoint(
        sourcePoint,
        connection,
        sourcePointIndex,
        termination
      )
    };
  }
  throw new Error(
    `FanoutSolver: connection "${connection.name}" does not touch component "${sourceGrid.componentId}"`
  );
}
function inferDirection(busId, connections) {
  let dx = 0;
  let dy = 0;
  for (const preparedConnection of connections) {
    dx += preparedConnection.targetPoint.x - preparedConnection.sourcePoint.x;
    dy += preparedConnection.targetPoint.y - preparedConnection.sourcePoint.y;
  }
  if (Math.abs(dx) < 1e-9 && Math.abs(dy) < 1e-9) {
    throw new Error(
      `FanoutSolver: cannot infer an escape direction for bus "${busId}"`
    );
  }
  if (Math.abs(dx) >= Math.abs(dy)) return dx >= 0 ? "right" : "left";
  return dy >= 0 ? "up" : "down";
}
function getDirectionsForBorderTarget(target) {
  switch (target) {
    case "left":
      return ["left"];
    case "right":
      return ["right"];
    case "top":
      return ["up"];
    case "bottom":
      return ["down"];
    case "top-left":
      return ["up", "left"];
    case "top-right":
      return ["up", "right"];
    case "bottom-left":
      return ["down", "left"];
    case "bottom-right":
      return ["down", "right"];
  }
}
function getAverageSourcePoint(connections) {
  return {
    x: connections.reduce(
      (sum, connection) => sum + connection.sourcePoint.x,
      0
    ) / connections.length,
    y: connections.reduce(
      (sum, connection) => sum + connection.sourcePoint.y,
      0
    ) / connections.length
  };
}
function getDistanceToBoundary(source, direction, boundary) {
  switch (direction) {
    case "left":
      return source.x - boundary.minX;
    case "right":
      return boundary.maxX - source.x;
    case "up":
      return boundary.maxY - source.y;
    case "down":
      return source.y - boundary.minY;
  }
}
function getRegionAnchor(region, boundary) {
  if (region.direction === "up" || region.direction === "down") {
    if (region.preferredExit.endsWith("left")) return boundary.minX;
    if (region.preferredExit.endsWith("right")) return boundary.maxX;
    return (boundary.minX + boundary.maxX) / 2;
  }
  if (region.preferredExit.startsWith("top")) return boundary.maxY;
  if (region.preferredExit.startsWith("bottom")) return boundary.minY;
  return (boundary.minY + boundary.maxY) / 2;
}
function getRegionSourceCoordinate(source, direction) {
  return direction === "up" || direction === "down" ? source.x : source.y;
}
function tryInferDirection(busId, connections) {
  try {
    return inferDirection(busId, connections);
  } catch {
    return void 0;
  }
}
function resolveAvailableBusExit(params) {
  const {
    busId,
    explicitDirection,
    preferredExit,
    connections,
    sharedBoundary,
    availableRegions
  } = params;
  const compatibleRegions = availableRegions.filter(
    (region) => (explicitDirection === void 0 || region.direction === explicitDirection) && (preferredExit === void 0 || region.preferredExit === preferredExit)
  );
  if (compatibleRegions.length === 0) {
    throw new Error(
      `FanoutSolver: bus "${busId}" cannot use its requested exit with availableCornersAndSides`
    );
  }
  const inferredDirection = explicitDirection ? void 0 : tryInferDirection(busId, connections);
  const preferredDirectionRegions = inferredDirection ? compatibleRegions.filter(
    (region) => region.direction === inferredDirection
  ) : [];
  const candidates = preferredDirectionRegions.length > 0 ? preferredDirectionRegions : compatibleRegions;
  const averageSource = getAverageSourcePoint(connections);
  return [...candidates].toSorted(
    (first, second) => getDistanceToBoundary(averageSource, first.direction, sharedBoundary) - getDistanceToBoundary(
      averageSource,
      second.direction,
      sharedBoundary
    ) || Math.abs(
      getRegionSourceCoordinate(averageSource, first.direction) - getRegionAnchor(first, sharedBoundary)
    ) - Math.abs(
      getRegionSourceCoordinate(averageSource, second.direction) - getRegionAnchor(second, sharedBoundary)
    ) || first.preferredExit.localeCompare(second.preferredExit)
  )[0];
}
function resolveBusDirection(params) {
  const {
    busId,
    explicitDirection,
    preferredExit,
    connections,
    sharedBoundary,
    availableRegions
  } = params;
  if (availableRegions) {
    return resolveAvailableBusExit({
      busId,
      explicitDirection,
      preferredExit,
      connections,
      sharedBoundary,
      availableRegions
    });
  }
  if (!preferredExit) {
    return {
      direction: explicitDirection ?? inferDirection(busId, connections)
    };
  }
  const compatibleDirections = getDirectionsForBorderTarget(preferredExit);
  if (explicitDirection) {
    if (!compatibleDirections.includes(explicitDirection)) {
      throw new Error(
        `FanoutSolver: bus "${busId}" direction "${explicitDirection}" is incompatible with preferredExit "${preferredExit}"`
      );
    }
    return { direction: explicitDirection, preferredExit };
  }
  if (compatibleDirections.length === 1) {
    return { direction: compatibleDirections[0], preferredExit };
  }
  let inferredDirection;
  try {
    inferredDirection = inferDirection(busId, connections);
  } catch {
    inferredDirection = void 0;
  }
  if (inferredDirection && compatibleDirections.includes(inferredDirection)) {
    return { direction: inferredDirection, preferredExit };
  }
  const averageSource = getAverageSourcePoint(connections);
  return {
    direction: compatibleDirections.toSorted(
      (first, second) => getDistanceToBoundary(averageSource, first, sharedBoundary) - getDistanceToBoundary(averageSource, second, sharedBoundary) || first.localeCompare(second)
    )[0],
    preferredExit
  };
}
function prepareFanoutBuses(srj, options) {
  const componentGrids = findComponentGrids(srj.obstacles);
  if (componentGrids.length === 0 && srj.connections.length > 0) {
    throw new Error(
      "FanoutSolver: no componentId-tagged pad footprint was found"
    );
  }
  const connectionIndexByName = new Map(
    srj.connections.map((connection, index) => [connection.name, index])
  );
  const resolvedBusInputs = resolveBusSpecs(srj, options).map((busSpec) => {
    const connections = busSpec.connectionNames.map((connectionName) => {
      const connectionIndex = connectionIndexByName.get(connectionName);
      if (connectionIndex === void 0) {
        throw new Error(
          `FanoutSolver: connection "${connectionName}" is missing from the input`
        );
      }
      return srj.connections[connectionIndex];
    });
    const sourceGrid = chooseSourceGrid({
      busSpec,
      connections,
      componentGrids
    });
    const preparedConnections = connections.map(
      (connection) => prepareConnection({
        connection,
        connectionIndex: connectionIndexByName.get(connection.name),
        sourceGrid,
        componentGrids,
        termination: busSpec.termination ?? { type: "boundary" }
      })
    );
    return { busSpec, sourceGrid, preparedConnections };
  });
  const sourceGrids = [
    ...new Map(
      resolvedBusInputs.map(({ sourceGrid }) => [
        sourceGrid.componentId,
        sourceGrid
      ])
    ).values()
  ];
  const sharedBoundary = resolveSharedBoundary(sourceGrids, options);
  const availableRegions = resolveAvailableBoundaryRegions(
    options.availableCornersAndSides
  );
  const buses = [];
  for (const {
    busSpec,
    sourceGrid,
    preparedConnections
  } of resolvedBusInputs) {
    const resolvedExit = resolveBusDirection({
      busId: busSpec.busId,
      explicitDirection: busSpec.direction ?? options.busDirections?.[busSpec.busId],
      preferredExit: busSpec.preferredExit,
      connections: preparedConnections,
      sharedBoundary,
      availableRegions: busSpec.termination?.type === "plane" ? void 0 : availableRegions
    });
    buses.push({
      busId: busSpec.busId,
      direction: resolvedExit.direction,
      preferredExit: resolvedExit.preferredExit,
      termination: busSpec.termination ?? { type: "boundary" },
      connections: preparedConnections,
      componentId: sourceGrid.componentId,
      componentObstacles: sourceGrid.obstacles,
      componentBounds: resolveComponentBounds(sourceGrid, options),
      sharedBoundary,
      xCoordinates: [...sourceGrid.xCoordinates],
      yCoordinates: [...sourceGrid.yCoordinates],
      pitchX: sourceGrid.pitchX,
      pitchY: sourceGrid.pitchY
    });
  }
  return buses;
}

// node_modules/@tscircuit/fanout-solver/lib/net-identity.ts
var identityCache = /* @__PURE__ */ new WeakMap();
function getConnectionNetKey(connection) {
  return connection.netConnectionName ?? connection.rootConnectionName ?? connection.name;
}
function addTokenNet(tokenNetKeys, token, netKey) {
  if (!token) return false;
  const keys = tokenNetKeys.get(token) ?? /* @__PURE__ */ new Set();
  const sizeBefore = keys.size;
  keys.add(netKey);
  tokenNetKeys.set(token, keys);
  return keys.size !== sizeBefore;
}
function getKnownNetKeys(tokenNetKeys, tokens) {
  const keys = /* @__PURE__ */ new Set();
  for (const token of tokens) {
    for (const key of tokenNetKeys.get(token) ?? []) keys.add(key);
  }
  return keys;
}
function createElectricalNetIdentity(srj) {
  const connectionNetKeys = /* @__PURE__ */ new Map();
  const tokenNetKeys = /* @__PURE__ */ new Map();
  for (const connection of srj.connections) {
    const netKey = getConnectionNetKey(connection);
    connectionNetKeys.set(connection.name, netKey);
    addTokenNet(tokenNetKeys, connection.name, netKey);
    addTokenNet(tokenNetKeys, connection.rootConnectionName, netKey);
    addTokenNet(tokenNetKeys, connection.netConnectionName, netKey);
    for (const point6 of connection.pointsToConnect) {
      addTokenNet(tokenNetKeys, point6.pointId, netKey);
      addTokenNet(tokenNetKeys, point6.pcb_port_id, netKey);
    }
  }
  for (const trace of srj.traces ?? []) {
    const netKey = trace.connection_name ? connectionNetKeys.get(trace.connection_name) : void 0;
    if (!netKey) continue;
    addTokenNet(tokenNetKeys, trace.pcb_trace_id, netKey);
    for (const token of trace.connectsTo ?? []) {
      addTokenNet(tokenNetKeys, token, netKey);
    }
  }
  for (let pass = 0; pass < 2; pass++) {
    let changed = false;
    for (const obstacle of srj.obstacles) {
      const netKeys = getKnownNetKeys(tokenNetKeys, obstacle.connectedTo);
      if (netKeys.size !== 1) continue;
      const netKey = [...netKeys][0];
      for (const token of obstacle.connectedTo) {
        changed = addTokenNet(tokenNetKeys, token, netKey) || changed;
      }
    }
    if (!changed) break;
  }
  const parentByNetKey = /* @__PURE__ */ new Map();
  const findRoot = (netKey) => {
    const parent = parentByNetKey.get(netKey) ?? netKey;
    parentByNetKey.set(netKey, parent);
    if (parent === netKey) return netKey;
    const root = findRoot(parent);
    parentByNetKey.set(netKey, root);
    return root;
  };
  const union = (first, second) => {
    const firstRoot = findRoot(first);
    const secondRoot = findRoot(second);
    if (firstRoot !== secondRoot) parentByNetKey.set(secondRoot, firstRoot);
  };
  for (const netKeys of tokenNetKeys.values()) {
    const [firstNetKey, ...otherNetKeys] = [...netKeys];
    if (!firstNetKey) continue;
    for (const otherNetKey of otherNetKeys) union(firstNetKey, otherNetKey);
  }
  for (const connectedTokens of [
    ...srj.obstacles.map((obstacle) => obstacle.connectedTo),
    ...(srj.traces ?? []).map((trace) => trace.connectsTo ?? [])
  ]) {
    const [firstNetKey, ...otherNetKeys] = [
      ...getKnownNetKeys(tokenNetKeys, connectedTokens)
    ];
    if (!firstNetKey) continue;
    for (const otherNetKey of otherNetKeys) union(firstNetKey, otherNetKey);
  }
  for (const [connectionName, netKey] of connectionNetKeys) {
    connectionNetKeys.set(connectionName, findRoot(netKey));
  }
  for (const [token, netKeys] of tokenNetKeys) {
    tokenNetKeys.set(
      token,
      new Set([...netKeys].map((netKey) => findRoot(netKey)))
    );
  }
  return { connectionNetKeys, tokenNetKeys };
}
function getElectricalNetIdentity(srj) {
  const cached = identityCache.get(srj);
  if (cached) return cached;
  const identity9 = createElectricalNetIdentity(srj);
  identityCache.set(srj, identity9);
  return identity9;
}
function connectionsShareElectricalNet(srj, firstConnectionName, secondConnectionName) {
  const identity9 = getElectricalNetIdentity(srj);
  const firstNet = identity9.connectionNetKeys.get(firstConnectionName);
  const secondNet = identity9.connectionNetKeys.get(secondConnectionName);
  return firstNet !== void 0 && firstNet === secondNet;
}
function obstacleSharesElectricalNet(srj, obstacle, connectionName) {
  const identity9 = getElectricalNetIdentity(srj);
  const connectionNet = identity9.connectionNetKeys.get(connectionName);
  if (!connectionNet) return false;
  return obstacle.connectedTo.some(
    (token) => identity9.tokenNetKeys.get(token)?.has(connectionNet)
  );
}

// node_modules/@tscircuit/fanout-solver/lib/route-bus.ts
function isHorizontal(direction) {
  return direction === "left" || direction === "right";
}
function directionSign(direction) {
  return direction === "right" || direction === "up" ? 1 : -1;
}
function getAxis(point6, direction) {
  return isHorizontal(direction) ? point6.x : point6.y;
}
function getPerpendicularAxis(point6, direction) {
  return isHorizontal(direction) ? point6.y : point6.x;
}
function makePoint(axis, perpendicularAxis, direction) {
  return isHorizontal(direction) ? { x: axis, y: perpendicularAxis } : { x: perpendicularAxis, y: axis };
}
function getExitAxis(bus) {
  switch (bus.direction) {
    case "right":
      return bus.sharedBoundary.maxX;
    case "left":
      return bus.sharedBoundary.minX;
    case "up":
      return bus.sharedBoundary.maxY;
    case "down":
      return bus.sharedBoundary.minY;
  }
}
function getDirectionalPitch(bus) {
  return isHorizontal(bus.direction) ? bus.pitchX : bus.pitchY;
}
function getPerpendicularPitch(bus) {
  return isHorizontal(bus.direction) ? bus.pitchY : bus.pitchX;
}
function getDepthInRows(bus) {
  const directionalCoordinates = (isHorizontal(bus.direction) ? bus.xCoordinates : bus.yCoordinates).toSorted((a, b) => a - b);
  const averageDirectionalSource = bus.connections.reduce(
    (sum, candidate) => sum + getAxis(candidate.sourcePoint, bus.direction),
    0
  ) / bus.connections.length;
  const outwardCoordinate = directionSign(bus.direction) > 0 ? directionalCoordinates.at(-1) : directionalCoordinates[0];
  return Math.abs(outwardCoordinate - averageDirectionalSource) / getDirectionalPitch(bus);
}
function busIsOnOutwardComponentEdge(bus) {
  const directionalCoordinates = isHorizontal(bus.direction) ? bus.xCoordinates : bus.yCoordinates;
  const averageDirectionalSource = bus.connections.reduce(
    (sum, connection) => sum + getAxis(connection.sourcePoint, bus.direction),
    0
  ) / bus.connections.length;
  const outwardCoordinate = directionSign(bus.direction) > 0 ? Math.max(...directionalCoordinates) : Math.min(...directionalCoordinates);
  return Math.abs(averageDirectionalSource - outwardCoordinate) < 1e-6;
}
function getConnectionRank(bus, connection) {
  const connectionRank = [...bus.connections].sort(
    (a, b) => getPerpendicularAxis(a.sourcePoint, bus.direction) - getPerpendicularAxis(b.sourcePoint, bus.direction)
  ).findIndex(
    (candidate) => candidate.connectionIndex === connection.connectionIndex
  );
  if (connectionRank < 0) {
    throw new Error(
      `FanoutSolver: connection "${connection.connection.name}" is missing from bus "${bus.busId}"`
    );
  }
  return connectionRank;
}
function getRoutableBounds(srjBounds, sharedBoundary) {
  return {
    minX: Math.min(srjBounds.minX, sharedBoundary.minX),
    maxX: Math.max(srjBounds.maxX, sharedBoundary.maxX),
    minY: Math.min(srjBounds.minY, sharedBoundary.minY),
    maxY: Math.max(srjBounds.maxY, sharedBoundary.maxY)
  };
}
function pointIsInsideBounds(point6, bounds) {
  return point6.x >= bounds.minX - 1e-6 && point6.x <= bounds.maxX + 1e-6 && point6.y >= bounds.minY - 1e-6 && point6.y <= bounds.maxY + 1e-6;
}
function getTracksInSpan(minimum, maximum, traceWidth, clearance, kind) {
  const freeWidth = maximum - minimum;
  const trackCount = Math.floor(
    (freeWidth - clearance) / (traceWidth + clearance) + 1e-9
  );
  if (trackCount < 1) return [];
  const usedWidth = trackCount * traceWidth + (trackCount - 1) * clearance;
  const firstTrack = minimum + (freeWidth - usedWidth) / 2 + traceWidth / 2;
  return Array.from({ length: trackCount }, (_, index) => ({
    value: firstTrack + index * (traceWidth + clearance),
    kind
  }));
}
function getTrackCandidates(params) {
  const { bus, connection, preferredTrack, traceWidth, clearance } = params;
  const coordinates = (isHorizontal(bus.direction) ? bus.yCoordinates : bus.xCoordinates).toSorted((a, b) => a - b);
  const obstacleHalfSize = Math.max(
    ...bus.componentObstacles.map(
      (obstacle) => isHorizontal(bus.direction) ? obstacle.height / 2 : obstacle.width / 2
    )
  );
  const boundaryMinimum = isHorizontal(bus.direction) ? bus.sharedBoundary.minY : bus.sharedBoundary.minX;
  const boundaryMaximum = isHorizontal(bus.direction) ? bus.sharedBoundary.maxY : bus.sharedBoundary.maxX;
  const maximumJog = boundaryMaximum - boundaryMinimum;
  const ladderMinimum = boundaryMinimum;
  const ladderMaximum = boundaryMaximum;
  const tracks = [
    { value: preferredTrack, kind: "preferred" },
    ...getTracksInSpan(
      ladderMinimum,
      coordinates[0] - obstacleHalfSize,
      traceWidth,
      clearance,
      "margin"
    )
  ];
  for (let index = 0; index < coordinates.length; index++) {
    tracks.push({ value: coordinates[index], kind: "corridor" });
    if (index < coordinates.length - 1) {
      tracks.push(
        ...getTracksInSpan(
          coordinates[index] + obstacleHalfSize,
          coordinates[index + 1] - obstacleHalfSize,
          traceWidth,
          clearance,
          "gap"
        )
      );
    }
  }
  tracks.push(
    ...getTracksInSpan(
      coordinates.at(-1) + obstacleHalfSize,
      ladderMaximum,
      traceWidth,
      clearance,
      "margin"
    )
  );
  const sourceTrack = getPerpendicularAxis(
    connection.sourcePoint,
    bus.direction
  );
  const componentCenter = (coordinates[0] + coordinates.at(-1)) / 2;
  return tracks.filter((track) => Math.abs(track.value - sourceTrack) <= maximumJog + 1e-9).filter(
    (track, index, candidates) => candidates.findIndex(
      (candidate) => Math.abs(candidate.value - track.value) < 1e-9
    ) === index
  ).sort(
    (a, b) => Math.abs(a.value - preferredTrack) - Math.abs(b.value - preferredTrack) - (Math.abs(a.value - componentCenter) - Math.abs(b.value - componentCenter)) * 1e-3
  );
}
function getPreferredTrack(params) {
  const {
    bus,
    connection,
    targetUsesVia,
    interstitialEscape,
    compactBusTracks,
    traceWidth,
    viaDiameter,
    clearance
  } = params;
  const perpendicularCoordinates = (isHorizontal(bus.direction) ? bus.yCoordinates : bus.xCoordinates).toSorted((a, b) => a - b);
  const sourceTrack = getPerpendicularAxis(
    connection.sourcePoint,
    bus.direction
  );
  if (compactBusTracks) {
    const connectionRank2 = getConnectionRank(bus, connection);
    const componentCenter = (perpendicularCoordinates[0] + perpendicularCoordinates.at(-1)) / 2;
    return componentCenter + (connectionRank2 - (bus.connections.length - 1) / 2) * (traceWidth + clearance);
  }
  if (!targetUsesVia) return sourceTrack;
  if (!interstitialEscape) return sourceTrack;
  const depthInRows = getDepthInRows(bus);
  const trackPitch = traceWidth + clearance;
  const halfConnectionCount = Math.ceil(bus.connections.length / 2);
  const sideBandWidth = (halfConnectionCount - 1) * trackPitch;
  const bandSeparation = viaDiameter / 2 + traceWidth / 2 + clearance + 1e-3;
  const depthIndex = Math.round(depthInRows) + 1;
  const nearOffset = depthIndex * bandSeparation + (depthIndex - 1) * sideBandWidth;
  const connectionRank = getConnectionRank(bus, connection);
  const componentMinimum = perpendicularCoordinates[0];
  const componentMaximum = perpendicularCoordinates.at(-1);
  const requestedTrack = connectionRank < halfConnectionCount ? componentMinimum - nearOffset - (halfConnectionCount - connectionRank - 1) * trackPitch : componentMaximum + nearOffset + (connectionRank - halfConnectionCount) * trackPitch;
  const boundaryMinimum = isHorizontal(bus.direction) ? bus.sharedBoundary.minY : bus.sharedBoundary.minX;
  const boundaryMaximum = isHorizontal(bus.direction) ? bus.sharedBoundary.maxY : bus.sharedBoundary.maxX;
  return Math.max(
    boundaryMinimum + traceWidth / 2,
    Math.min(boundaryMaximum - traceWidth / 2, requestedTrack)
  );
}
function getConnectionOrders(bus) {
  const sign = directionSign(bus.direction);
  const outwardFirst = [...bus.connections].sort((a, b) => {
    const directionalDifference = sign * (getAxis(b.sourcePoint, bus.direction) - getAxis(a.sourcePoint, bus.direction));
    if (Math.abs(directionalDifference) > 1e-6) {
      return directionalDifference;
    }
    return getPerpendicularAxis(a.sourcePoint, bus.direction) - getPerpendicularAxis(b.sourcePoint, bus.direction);
  });
  const perpendicularFirst = [...bus.connections].sort(
    (a, b) => getPerpendicularAxis(a.sourcePoint, bus.direction) - getPerpendicularAxis(b.sourcePoint, bus.direction) || sign * (getAxis(b.sourcePoint, bus.direction) - getAxis(a.sourcePoint, bus.direction))
  );
  const orders = [
    outwardFirst,
    [...outwardFirst].reverse(),
    perpendicularFirst,
    [...perpendicularFirst].reverse()
  ];
  for (let offset = 1; offset < Math.min(outwardFirst.length, 8); offset++) {
    orders.push([
      ...outwardFirst.slice(offset),
      ...outwardFirst.slice(0, offset)
    ]);
  }
  return orders;
}
function appendSegment(segments, start, end, width, layer) {
  if (distance9(start, end) < 1e-9) return;
  segments.push({ start, end, width, layer });
}
function chamferOrthogonalPolyline(points, requestedChamfer) {
  if (points.length < 3) return points;
  const chamfered = [points[0]];
  for (let index = 1; index < points.length - 1; index++) {
    const previous = points[index - 1];
    const corner = points[index];
    const next = points[index + 1];
    const incomingLength = distance9(previous, corner);
    const outgoingLength = distance9(corner, next);
    if (incomingLength < 1e-9 || outgoingLength < 1e-9) continue;
    const incomingUnit = {
      x: (corner.x - previous.x) / incomingLength,
      y: (corner.y - previous.y) / incomingLength
    };
    const outgoingUnit = {
      x: (next.x - corner.x) / outgoingLength,
      y: (next.y - corner.y) / outgoingLength
    };
    const dot = incomingUnit.x * outgoingUnit.x + incomingUnit.y * outgoingUnit.y;
    if (Math.abs(dot) > 1e-6) {
      chamfered.push(corner);
      continue;
    }
    const chamfer = Math.min(
      requestedChamfer,
      incomingLength / 2,
      outgoingLength / 2
    );
    chamfered.push({
      x: corner.x - incomingUnit.x * chamfer,
      y: corner.y - incomingUnit.y * chamfer
    });
    chamfered.push({
      x: corner.x + outgoingUnit.x * chamfer,
      y: corner.y + outgoingUnit.y * chamfer
    });
  }
  chamfered.push(points.at(-1));
  return chamfered;
}
function buildPlan(params) {
  const {
    preparedConnection,
    bus,
    targetLayer,
    track,
    exitAxis,
    layerNames,
    traceWidth,
    viaDiameter,
    viaHoleDiameter,
    viaHandedness,
    interstitialEscape,
    spreadLaneIndex,
    clearance,
    terminateAtVia
  } = params;
  const sourcePoint = {
    x: preparedConnection.sourcePoint.x,
    y: preparedConnection.sourcePoint.y
  };
  const sign = directionSign(bus.direction);
  const directionalPitch = getDirectionalPitch(bus);
  const perpendicularPitch = getPerpendicularPitch(bus);
  const targetUsesVia = targetLayer !== preparedConnection.sourceLayer;
  const directionalPadSize = isHorizontal(bus.direction) ? preparedConnection.sourceObstacle.width : preparedConnection.sourceObstacle.height;
  const initialEscapeDistance = targetUsesVia && !busIsOnOutwardComponentEdge(bus) ? directionalPadSize >= directionalPitch ? directionalPadSize / 2 + viaDiameter / 2 + clearance + 1e-3 : directionalPitch * 0.5 : !targetUsesVia ? directionalPadSize / 2 + traceWidth / 2 + clearance + 1e-3 : Math.max(
    directionalPitch * 0.5,
    directionalPadSize / 2 + (targetUsesVia ? viaDiameter : traceWidth) / 2 + clearance + 1e-3
  );
  const viaAxis = getAxis(sourcePoint, bus.direction) + sign * initialEscapeDistance;
  const sourcePerpendicularAxis = getPerpendicularAxis(
    sourcePoint,
    bus.direction
  );
  const viaPerpendicularAxis = sourcePerpendicularAxis + viaHandedness * perpendicularPitch * 0.5;
  const viaPoint = makePoint(viaAxis, viaPerpendicularAxis, bus.direction);
  const spreadLaneDistance = viaDiameter / 2 + traceWidth / 2 + clearance + 1e-3 + spreadLaneIndex * (traceWidth + clearance);
  const useNestedSpread = interstitialEscape && directionalPitch >= spreadLaneDistance + viaDiameter / 2 + clearance;
  const spreadPoint = useNestedSpread ? makePoint(
    viaAxis + sign * spreadLaneDistance,
    viaPerpendicularAxis,
    bus.direction
  ) : viaPoint;
  const targetLayerDoglegAxis = useNestedSpread ? getAxis(spreadPoint, bus.direction) : viaAxis + sign * Math.abs(track - viaPerpendicularAxis);
  const doglegPoint = makePoint(targetLayerDoglegAxis, track, bus.direction);
  const exitPoint = terminateAtVia ? viaPoint : makePoint(exitAxis, track, bus.direction);
  const segments = [];
  const route = [];
  route.push({
    route_type: "wire",
    x: sourcePoint.x,
    y: sourcePoint.y,
    width: traceWidth,
    layer: preparedConnection.sourceLayer,
    start_pcb_port_id: preparedConnection.sourcePoint.pcb_port_id
  });
  appendSegment(
    segments,
    sourcePoint,
    viaPoint,
    traceWidth,
    preparedConnection.sourceLayer
  );
  route.push({
    route_type: "wire",
    x: viaPoint.x,
    y: viaPoint.y,
    width: traceWidth,
    layer: preparedConnection.sourceLayer
  });
  let via;
  if (targetLayer !== preparedConnection.sourceLayer) {
    const spanLayers = getLayerSpan(
      preparedConnection.sourceLayer,
      targetLayer,
      layerNames
    );
    via = {
      center: viaPoint,
      diameter: viaDiameter,
      holeDiameter: viaHoleDiameter,
      fromLayer: preparedConnection.sourceLayer,
      toLayer: targetLayer,
      spanLayers
    };
    route.push({
      route_type: "via",
      x: viaPoint.x,
      y: viaPoint.y,
      from_layer: preparedConnection.sourceLayer,
      to_layer: targetLayer,
      via_diameter: viaDiameter,
      via_hole_diameter: viaHoleDiameter
    });
    route.push({
      route_type: "wire",
      x: viaPoint.x,
      y: viaPoint.y,
      width: traceWidth,
      layer: targetLayer
    });
  }
  const targetLayerPoints = terminateAtVia ? [viaPoint] : useNestedSpread ? chamferOrthogonalPolyline(
    [viaPoint, spreadPoint, doglegPoint, exitPoint],
    Math.max(traceWidth + clearance, traceWidth * 2)
  ) : [viaPoint, doglegPoint, exitPoint];
  for (let index = 1; index < targetLayerPoints.length; index++) {
    const previousPoint = targetLayerPoints[index - 1];
    const nextPoint = targetLayerPoints[index];
    appendSegment(segments, previousPoint, nextPoint, traceWidth, targetLayer);
    route.push({
      route_type: "wire",
      x: nextPoint.x,
      y: nextPoint.y,
      width: traceWidth,
      layer: targetLayer
    });
  }
  return {
    busId: bus.busId,
    connectionName: preparedConnection.connection.name,
    connectionIndex: preparedConnection.connectionIndex,
    sourcePointIndex: preparedConnection.sourcePointIndex,
    sourcePoint: preparedConnection.sourcePoint,
    sourceObstacle: preparedConnection.sourceObstacle,
    sourceLayer: preparedConnection.sourceLayer,
    targetLayer,
    termination: bus.termination,
    direction: bus.direction,
    exitPoint,
    trace: {
      type: "pcb_trace",
      pcb_trace_id: `fanout:${preparedConnection.connection.name}`,
      connection_name: preparedConnection.connection.name,
      connectsTo: [
        preparedConnection.connection.name,
        ...preparedConnection.sourcePoint.pointId ? [preparedConnection.sourcePoint.pointId] : [],
        ...preparedConnection.sourcePoint.pcb_port_id ? [preparedConnection.sourcePoint.pcb_port_id] : []
      ],
      route
    },
    segments,
    via,
    length: segments.reduce(
      (total, segment2) => total + distance9(segment2.start, segment2.end),
      0
    )
  };
}
function segmentIsClearOfObstacles(params) {
  const {
    segment: segment2,
    plan,
    segmentIndex,
    srj,
    allowSameNetMerges,
    obstacles,
    clearance
  } = params;
  for (const obstacle of obstacles) {
    if (!obstacle.layers.includes(segment2.layer)) continue;
    if (obstacle.connectedTo.includes(plan.connectionName)) continue;
    if (allowSameNetMerges && obstacleSharesElectricalNet(srj, obstacle, plan.connectionName)) {
      continue;
    }
    if (segmentIndex === 0 && obstacle === plan.sourceObstacle && segment2.layer === plan.sourceLayer) {
      continue;
    }
    if (distanceSegmentToObstacle(segment2, obstacle) < segment2.width / 2 + clearance - 1e-9) {
      return false;
    }
  }
  return true;
}
function planIsStaticallyClear(params) {
  const { plan, srj, sharedBoundary, clearance, allowSameNetMerges } = params;
  const routableBounds = getRoutableBounds(srj.bounds, sharedBoundary);
  if (!pointIsInsideBounds(plan.exitPoint, routableBounds) || plan.segments.some(
    (segment2) => !pointIsInsideBounds(segment2.start, routableBounds) || !pointIsInsideBounds(segment2.end, routableBounds)
  )) {
    return false;
  }
  for (let index = 0; index < plan.segments.length; index++) {
    if (!segmentIsClearOfObstacles({
      segment: plan.segments[index],
      plan,
      segmentIndex: index,
      srj,
      allowSameNetMerges,
      obstacles: srj.obstacles,
      clearance
    })) {
      return false;
    }
  }
  if (plan.via) {
    for (const obstacle of srj.obstacles) {
      if (!obstacle.layers.some((layer) => plan.via.spanLayers.includes(layer))) {
        continue;
      }
      if (allowSameNetMerges && obstacleSharesElectricalNet(srj, obstacle, plan.connectionName)) {
        continue;
      }
      if (distancePointToObstacle(plan.via.center, obstacle) < plan.via.diameter / 2 + clearance - 1e-9) {
        return false;
      }
    }
  }
  return true;
}
function planIsClearOfPlans(params) {
  const {
    plan,
    otherPlans,
    srj,
    allowSameNetMerges,
    clearance,
    blockingBusCounts
  } = params;
  for (const otherPlan of otherPlans) {
    if (allowSameNetMerges && connectionsShareElectricalNet(
      srj,
      plan.connectionName,
      otherPlan.connectionName
    )) {
      continue;
    }
    const plansShareSourcePort = plan.sourcePoint.pcb_port_id && plan.sourcePoint.pcb_port_id === otherPlan.sourcePoint.pcb_port_id || plan.sourcePoint.pointId && plan.sourcePoint.pointId === otherPlan.sourcePoint.pointId;
    if (plansShareSourcePort) continue;
    const recordBlocker = () => {
      if (otherPlan.busId === plan.busId) return;
      blockingBusCounts?.set(
        otherPlan.busId,
        (blockingBusCounts.get(otherPlan.busId) ?? 0) + 1
      );
    };
    for (const segment2 of plan.segments) {
      for (const otherSegment of otherPlan.segments) {
        if (!segmentsAreClear(segment2, otherSegment, clearance)) {
          recordBlocker();
          return false;
        }
      }
      if (otherPlan.via?.spanLayers.includes(segment2.layer) && distancePointToSegment(
        otherPlan.via.center,
        segment2.start,
        segment2.end
      ) < otherPlan.via.diameter / 2 + segment2.width / 2 + clearance - 1e-9) {
        recordBlocker();
        return false;
      }
    }
    if (plan.via) {
      for (const otherSegment of otherPlan.segments) {
        if (plan.via.spanLayers.includes(otherSegment.layer) && distancePointToSegment(
          plan.via.center,
          otherSegment.start,
          otherSegment.end
        ) < plan.via.diameter / 2 + otherSegment.width / 2 + clearance - 1e-9) {
          recordBlocker();
          return false;
        }
      }
      if (otherPlan.via && plan.via.spanLayers.some(
        (layer) => otherPlan.via.spanLayers.includes(layer)
      ) && distance9(plan.via.center, otherPlan.via.center) < (plan.via.diameter + otherPlan.via.diameter) / 2 + clearance - 1e-9) {
        recordBlocker();
        return false;
      }
    }
  }
  return true;
}
function planIsClear(params) {
  const {
    plan,
    otherPlans,
    staticClearanceCache,
    blockingBusCounts,
    cacheKey,
    srj,
    sharedBoundary,
    clearance,
    allowSameNetMerges
  } = params;
  let staticallyClear = staticClearanceCache?.get(cacheKey);
  if (staticallyClear === void 0) {
    staticallyClear = planIsStaticallyClear({
      plan,
      srj,
      sharedBoundary,
      clearance,
      allowSameNetMerges
    });
    staticClearanceCache?.set(cacheKey, staticallyClear);
  }
  return staticallyClear && planIsClearOfPlans({
    plan,
    otherPlans,
    srj,
    allowSameNetMerges,
    clearance,
    blockingBusCounts
  });
}
function routePlaneTerminatedBus(params) {
  const {
    srj,
    bus,
    targetLayer,
    acceptedPlans,
    layerNames,
    traceWidth,
    viaDiameter,
    viaHoleDiameter,
    clearance,
    staticClearanceCache,
    blockingBusCounts,
    allowSameNetMerges = false
  } = params;
  const sourceObstacle = bus.connections[0]?.sourceObstacle;
  if (!sourceObstacle || bus.termination.type !== "plane") return null;
  const sourceLayer = bus.connections[0].sourceLayer;
  if (targetLayer === sourceLayer) return null;
  const directionalPadSize = isHorizontal(bus.direction) ? sourceObstacle.width : sourceObstacle.height;
  const pairChannelFitsVia = getDirectionalPitch(bus) / 2 - directionalPadSize / 2 >= viaDiameter / 2 + clearance - 1e-9;
  const viaHandednesses = pairChannelFitsVia ? [0] : [1, -1];
  for (const viaHandedness of viaHandednesses) {
    for (const connectionOrder of getConnectionOrders(bus)) {
      const candidatePlans = [];
      let orderIsClear = true;
      for (const preparedConnection of connectionOrder) {
        const sourceTrack = getPerpendicularAxis(
          preparedConnection.sourcePoint,
          bus.direction
        );
        const plan = buildPlan({
          preparedConnection,
          bus,
          targetLayer,
          track: sourceTrack,
          exitAxis: getExitAxis(bus),
          layerNames,
          traceWidth,
          viaDiameter,
          viaHoleDiameter,
          viaHandedness,
          interstitialEscape: !pairChannelFitsVia,
          spreadLaneIndex: 0,
          clearance,
          terminateAtVia: true
        });
        if (!planIsClear({
          plan,
          otherPlans: [...acceptedPlans, ...candidatePlans],
          staticClearanceCache,
          blockingBusCounts,
          cacheKey: `plane:${bus.busId}:${targetLayer}:${preparedConnection.connectionIndex}:${viaHandedness}`,
          srj,
          sharedBoundary: bus.sharedBoundary,
          clearance,
          allowSameNetMerges
        })) {
          orderIsClear = false;
          break;
        }
        candidatePlans.push(plan);
      }
      if (orderIsClear) return candidatePlans;
    }
  }
  return null;
}
function routeBusAlternatives(params, maxAlternatives = 1) {
  const {
    srj,
    bus,
    targetLayer,
    acceptedPlans,
    layerNames,
    traceWidth,
    viaDiameter,
    viaHoleDiameter,
    clearance,
    compactBusTracks,
    staticClearanceCache,
    blockingBusCounts,
    allowSameNetMerges = false
  } = params;
  if (!Number.isInteger(maxAlternatives) || maxAlternatives < 1) {
    throw new Error(
      `FanoutSolver: maxAlternatives must be a positive integer, received ${maxAlternatives}`
    );
  }
  if (bus.termination.type === "plane") {
    const plan = routePlaneTerminatedBus(params);
    return plan ? [plan] : [];
  }
  const exitAxis = getExitAxis(bus);
  const sourceObstacle = bus.connections[0]?.sourceObstacle;
  if (!sourceObstacle) return [[]];
  const directionalPadSize = isHorizontal(bus.direction) ? sourceObstacle.width : sourceObstacle.height;
  const sourceLayer = bus.connections[0].sourceLayer;
  const targetUsesVia = targetLayer !== sourceLayer;
  const outwardEdgeBus = busIsOnOutwardComponentEdge(bus);
  const pairChannelFitsVia = getDirectionalPitch(bus) / 2 - directionalPadSize / 2 >= viaDiameter / 2 + clearance - 1e-9;
  const interstitialEscape = targetUsesVia && !outwardEdgeBus && !pairChannelFitsVia;
  const viaHandednesses = targetUsesVia ? pairChannelFitsVia || outwardEdgeBus ? [0] : [1, -1] : [0];
  const alternatives = [];
  const seenAlternativeKeys = /* @__PURE__ */ new Set();
  const addAlternative = (plans) => {
    const key = plans.map(
      (plan) => `${plan.connectionIndex}:${plan.targetLayer}:${plan.exitPoint.x}:${plan.exitPoint.y}:${plan.segments.map((segment2) => `${segment2.start.x},${segment2.start.y},${segment2.end.x},${segment2.end.y},${segment2.layer}`).join(";")}`
    ).join("|");
    if (seenAlternativeKeys.has(key)) return;
    seenAlternativeKeys.add(key);
    alternatives.push(plans);
  };
  const searchConnectionOrder = (connectionOrder, viaHandedness, connectionIndex, candidatePlans) => {
    if (alternatives.length >= maxAlternatives) return;
    if (connectionIndex >= connectionOrder.length) {
      addAlternative(candidatePlans);
      return;
    }
    const preparedConnection = connectionOrder[connectionIndex];
    const connectionRank = getConnectionRank(bus, preparedConnection);
    const trackCandidates = getTrackCandidates({
      bus,
      connection: preparedConnection,
      preferredTrack: getPreferredTrack({
        bus,
        connection: preparedConnection,
        targetUsesVia,
        interstitialEscape,
        compactBusTracks,
        traceWidth,
        viaDiameter,
        clearance
      }),
      traceWidth,
      clearance
    });
    for (let trackIndex = 0; trackIndex < trackCandidates.length; trackIndex++) {
      const track = trackCandidates[trackIndex];
      const plan = buildPlan({
        preparedConnection,
        bus,
        targetLayer,
        track: track.value,
        exitAxis,
        layerNames,
        traceWidth,
        viaDiameter,
        viaHoleDiameter,
        viaHandedness,
        interstitialEscape,
        spreadLaneIndex: Math.min(
          connectionRank,
          bus.connections.length - connectionRank - 1
        ),
        clearance,
        terminateAtVia: false
      });
      if (!planIsClear({
        plan,
        otherPlans: [...acceptedPlans, ...candidatePlans],
        staticClearanceCache,
        blockingBusCounts,
        cacheKey: `boundary:${bus.busId}:${targetLayer}:${preparedConnection.connectionIndex}:${viaHandedness}:${trackIndex}`,
        srj,
        sharedBoundary: bus.sharedBoundary,
        clearance,
        allowSameNetMerges
      })) {
        continue;
      }
      searchConnectionOrder(
        connectionOrder,
        viaHandedness,
        connectionIndex + 1,
        [...candidatePlans, plan]
      );
      if (alternatives.length >= maxAlternatives) return;
      if (maxAlternatives === 1) return;
    }
  };
  for (const viaHandedness of viaHandednesses) {
    for (const connectionOrder of getConnectionOrders(bus)) {
      searchConnectionOrder(connectionOrder, viaHandedness, 0, []);
      if (alternatives.length >= maxAlternatives) return alternatives;
    }
  }
  return alternatives;
}
function routeBus(params) {
  return routeBusAlternatives(params, 1)[0] ?? null;
}

// node_modules/@tscircuit/fanout-solver/lib/route-single-layer-adaptive-exits.ts
var EPSILON2 = 1e-9;
var OBSTACLE_BIN_SIZE = 1;
var FANOUT_FLOW_DEBUG_ENABLED = globalThis.process?.env?.FANOUT_FLOW_DEBUG === "1";
function getNetKey(connection) {
  const simpleRouteConnection = connection.connection;
  const connectivityNet = connection.sourceObstacle.connectedTo.find(
    (connectionName) => connectionName.startsWith("connectivity_net")
  );
  return connectivityNet ?? simpleRouteConnection.netConnectionName ?? connection.connection.name.replace(/::fanout:\d+$/, "");
}
function obstacleBelongsToItem(obstacle, item) {
  return obstacle === item.connection.sourceObstacle || obstacle.connectedTo.includes(item.connection.connection.name) || obstacle.connectedTo.includes(item.netKey);
}
function connectWith45DegreeSegments(start, end) {
  const deltaX = end.x - start.x;
  const deltaY = end.y - start.y;
  const absoluteX = Math.abs(deltaX);
  const absoluteY = Math.abs(deltaY);
  if (absoluteX < EPSILON2 || absoluteY < EPSILON2 || Math.abs(absoluteX - absoluteY) < EPSILON2) {
    return [start, end];
  }
  if (absoluteX > absoluteY) {
    return [
      start,
      {
        x: start.x + Math.sign(deltaX) * absoluteY,
        y: end.y
      },
      end
    ];
  }
  return [
    start,
    {
      x: end.x,
      y: start.y + Math.sign(deltaY) * absoluteX
    },
    end
  ];
}
function enforceStraightOr45DegreeSegments(points) {
  if (points.length < 2) return points;
  const normalized = [points[0]];
  for (const end of points.slice(1)) {
    const start = normalized.at(-1);
    const deltaX = Math.abs(end.x - start.x);
    const deltaY = Math.abs(end.y - start.y);
    if (deltaX < EPSILON2 || deltaY < EPSILON2 || Math.abs(deltaX - deltaY) < EPSILON2) {
      normalized.push(end);
      continue;
    }
    normalized.push(
      ...connectWith45DegreeSegments(end, start).reverse().slice(1)
    );
  }
  return compressPath(normalized);
}
function compressPath(points) {
  if (points.length < 3) return points;
  const compressed = [points[0]];
  for (let index = 1; index < points.length - 1; index++) {
    const previous = compressed.at(-1);
    const current = points[index];
    const next = points[index + 1];
    if (Math.sign(current.x - previous.x) !== Math.sign(next.x - current.x) || Math.sign(current.y - previous.y) !== Math.sign(next.y - current.y)) {
      compressed.push(current);
    }
  }
  compressed.push(points.at(-1));
  return compressed;
}
function chamferOrthogonalPolyline2(points, requestedChamfer) {
  if (points.length < 3) return points;
  const chamfered = [points[0]];
  for (let index = 1; index < points.length - 1; index++) {
    const previous = points[index - 1];
    const corner = points[index];
    const next = points[index + 1];
    const incomingLength = distance9(previous, corner);
    const outgoingLength = distance9(corner, next);
    if (incomingLength < EPSILON2 || outgoingLength < EPSILON2) continue;
    const incoming = {
      x: (corner.x - previous.x) / incomingLength,
      y: (corner.y - previous.y) / incomingLength
    };
    const outgoing = {
      x: (next.x - corner.x) / outgoingLength,
      y: (next.y - corner.y) / outgoingLength
    };
    if (Math.abs(incoming.x * outgoing.x + incoming.y * outgoing.y) > 1e-6) {
      chamfered.push(corner);
      continue;
    }
    const chamfer = Math.min(
      requestedChamfer,
      incomingLength / 2,
      outgoingLength / 2
    );
    chamfered.push({
      x: corner.x - incoming.x * chamfer,
      y: corner.y - incoming.y * chamfer
    });
    chamfered.push({
      x: corner.x + outgoing.x * chamfer,
      y: corner.y + outgoing.y * chamfer
    });
  }
  chamfered.push(points.at(-1));
  return compressPath(chamfered);
}
function getSegments(points, traceWidth) {
  const segments = [];
  for (let index = 1; index < points.length; index++) {
    if (distance9(points[index - 1], points[index]) < EPSILON2) continue;
    segments.push({
      start: points[index - 1],
      end: points[index],
      width: traceWidth,
      layer: "top"
    });
  }
  return segments;
}
var Dinic = class {
  edges;
  levels;
  nextEdges;
  constructor(nodeCount) {
    this.edges = Array.from({ length: nodeCount }, () => []);
    this.levels = new Int32Array(nodeCount);
    this.nextEdges = new Int32Array(nodeCount);
  }
  addEdge(from, to, capacity, metadata = {}) {
    const forward = {
      to,
      reverseIndex: this.edges[to].length,
      capacity,
      initialCapacity: capacity,
      ...metadata
    };
    const reverse = {
      to: from,
      reverseIndex: this.edges[from].length,
      capacity: 0,
      initialCapacity: 0
    };
    this.edges[from].push(forward);
    this.edges[to].push(reverse);
  }
  buildLevels(source, sink) {
    this.levels.fill(-1);
    const queue = new Int32Array(this.edges.length);
    let head = 0;
    let tail = 0;
    queue[tail++] = source;
    this.levels[source] = 0;
    while (head < tail) {
      const node = queue[head++];
      for (const edge of this.edges[node]) {
        if (edge.capacity <= 0 || this.levels[edge.to] >= 0) continue;
        this.levels[edge.to] = this.levels[node] + 1;
        queue[tail++] = edge.to;
      }
    }
    return this.levels[sink] >= 0;
  }
  sendFlow(node, sink) {
    if (node === sink) return 1;
    for (let edgeIndex = this.nextEdges[node]; edgeIndex < this.edges[node].length; edgeIndex++, this.nextEdges[node] = edgeIndex) {
      const edge = this.edges[node][edgeIndex];
      if (edge.capacity <= 0 || this.levels[edge.to] !== this.levels[node] + 1) {
        continue;
      }
      const sent = this.sendFlow(edge.to, sink);
      if (sent === 0) continue;
      edge.capacity -= sent;
      this.edges[edge.to][edge.reverseIndex].capacity += sent;
      return sent;
    }
    return 0;
  }
  maximumFlow(source, sink, limit) {
    let flow = 0;
    while (flow < limit && this.buildLevels(source, sink)) {
      this.nextEdges.fill(0);
      while (flow < limit) {
        const sent = this.sendFlow(source, sink);
        if (sent === 0) break;
        flow += sent;
      }
    }
    return flow;
  }
};
function createFlowGrid(params) {
  const { boundary, obstacles, traceWidth, clearance } = params;
  const step = traceWidth + clearance;
  const columnCount = Math.round((boundary.maxX - boundary.minX) / step) + 1;
  const rowCount = Math.round((boundary.maxY - boundary.minY) / step) + 1;
  const nodeCount = columnCount * rowCount;
  const points = Array.from({ length: nodeCount }, (_, node) => ({
    x: boundary.minX + node % columnCount * step,
    y: boundary.minY + Math.floor(node / columnCount) * step
  }));
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  const obstacleIndexesByBin = /* @__PURE__ */ new Map();
  for (let obstacleIndex = 0; obstacleIndex < obstacles.length; obstacleIndex++) {
    const obstacle = obstacles[obstacleIndex];
    const minBinX = Math.floor(
      (obstacle.center.x - obstacle.width / 2 - requiredObstacleDistance) / OBSTACLE_BIN_SIZE
    );
    const maxBinX = Math.floor(
      (obstacle.center.x + obstacle.width / 2 + requiredObstacleDistance) / OBSTACLE_BIN_SIZE
    );
    const minBinY = Math.floor(
      (obstacle.center.y - obstacle.height / 2 - requiredObstacleDistance) / OBSTACLE_BIN_SIZE
    );
    const maxBinY = Math.floor(
      (obstacle.center.y + obstacle.height / 2 + requiredObstacleDistance) / OBSTACLE_BIN_SIZE
    );
    for (let binX = minBinX; binX <= maxBinX; binX++) {
      for (let binY = minBinY; binY <= maxBinY; binY++) {
        const key = `${binX}:${binY}`;
        const indexes = obstacleIndexesByBin.get(key) ?? [];
        indexes.push(obstacleIndex);
        obstacleIndexesByBin.set(key, indexes);
      }
    }
  }
  const getNearbyObstacles = (first, second = first) => {
    const minBinX = Math.floor(Math.min(first.x, second.x) / OBSTACLE_BIN_SIZE);
    const maxBinX = Math.floor(Math.max(first.x, second.x) / OBSTACLE_BIN_SIZE);
    const minBinY = Math.floor(Math.min(first.y, second.y) / OBSTACLE_BIN_SIZE);
    const maxBinY = Math.floor(Math.max(first.y, second.y) / OBSTACLE_BIN_SIZE);
    const indexes = /* @__PURE__ */ new Set();
    for (let binX = minBinX; binX <= maxBinX; binX++) {
      for (let binY = minBinY; binY <= maxBinY; binY++) {
        for (const obstacleIndex of obstacleIndexesByBin.get(
          `${binX}:${binY}`
        ) ?? []) {
          indexes.add(obstacleIndex);
        }
      }
    }
    return [...indexes].map((index) => obstacles[index]);
  };
  const obstacleFreeNodes = new Uint8Array(nodeCount);
  for (let node = 0; node < nodeCount; node++) {
    const point6 = points[node];
    if (getNearbyObstacles(point6).every(
      (obstacle) => distancePointToObstacle(point6, obstacle) >= requiredObstacleDistance - EPSILON2
    )) {
      obstacleFreeNodes[node] = 1;
    }
  }
  const neighbors = Array.from({ length: nodeCount }, () => []);
  for (let node = 0; node < nodeCount; node++) {
    if (!obstacleFreeNodes[node]) continue;
    const column = node % columnCount;
    const row = Math.floor(node / columnCount);
    for (const [deltaColumn, deltaRow] of [
      [1, 0],
      [0, 1]
    ]) {
      const nextColumn = column + deltaColumn;
      const nextRow = row + deltaRow;
      if (nextColumn >= columnCount || nextRow >= rowCount) continue;
      const nextNode = nextRow * columnCount + nextColumn;
      if (!obstacleFreeNodes[nextNode]) continue;
      const segment2 = {
        start: points[node],
        end: points[nextNode],
        width: traceWidth,
        layer: "top"
      };
      if (getNearbyObstacles(segment2.start, segment2.end).some(
        (obstacle) => distanceSegmentToObstacle(segment2, obstacle) < requiredObstacleDistance - EPSILON2
      )) {
        continue;
      }
      neighbors[node].push(nextNode);
      neighbors[nextNode].push(node);
    }
  }
  return {
    boundary,
    step,
    columnCount,
    rowCount,
    nodeCount,
    points,
    obstacleFreeNodes,
    neighbors
  };
}
function routeDirectionGroup(params) {
  const {
    direction,
    availableDirections,
    items,
    grid,
    obstacles,
    traceWidth,
    clearance,
    occupiedNodes,
    acceptedSegments,
    connectorSelectionOffset = 0
  } = params;
  if (items.length === 0) {
    return { routes: [], usedNodes: [], unmatchedItems: [] };
  }
  const {
    boundary,
    step,
    columnCount,
    rowCount,
    nodeCount: gridNodeCount
  } = grid;
  const pointForNode = (node) => grid.points[node];
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  const requiredRouteDistance = traceWidth + clearance;
  const freeNodes = new Uint8Array(gridNodeCount);
  for (let node = 0; node < gridNodeCount; node++) {
    if (!occupiedNodes[node] && grid.obstacleFreeNodes[node]) {
      freeNodes[node] = 1;
    }
  }
  const connectorIsClear = (points, item) => {
    const segments = getSegments(points, traceWidth);
    return segments.every(
      (segment2) => obstacles.every(
        (obstacle) => obstacleBelongsToItem(obstacle, item) || distanceSegmentToObstacle(segment2, obstacle) >= requiredObstacleDistance - EPSILON2
      )
    ) && segments.every(
      (segment2) => acceptedSegments.every(
        (acceptedSegment) => distanceSegmentToSegment(
          segment2.start,
          segment2.end,
          acceptedSegment.start,
          acceptedSegment.end
        ) >= requiredRouteDistance - EPSILON2
      )
    );
  };
  const equivalentItemsByKey = /* @__PURE__ */ new Map();
  for (const item of items) {
    const key = `${item.source.x.toFixed(6)}:${item.source.y.toFixed(6)}:${item.netKey}`;
    const equivalents = equivalentItemsByKey.get(key) ?? [];
    equivalents.push(item);
    equivalentItemsByKey.set(key, equivalents);
  }
  const offsetCandidates = Array.from({ length: 61 * 61 }, (_, index) => {
    const x = index % 61 - 30;
    const y = Math.floor(index / 61) - 30;
    return { x, y, distanceSquared: x * x + y * y };
  }).sort((first, second) => first.distanceSquared - second.distanceSquared);
  const terminals = [];
  for (const equivalentItems of equivalentItemsByKey.values()) {
    const item = equivalentItems[0];
    const maxConnectorLength = Math.hypot(
      item.connection.sourceObstacle.width / 2,
      item.connection.sourceObstacle.height / 2
    ) + clearance + step * 2;
    const sourceColumn = Math.round((item.source.x - boundary.minX) / step);
    const sourceRow = Math.round((item.source.y - boundary.minY) / step);
    const candidates = [];
    const candidateNodes = /* @__PURE__ */ new Set();
    for (const offset of offsetCandidates) {
      const column = sourceColumn + offset.x;
      const row = sourceRow + offset.y;
      if (column < 0 || column >= columnCount || row < 0 || row >= rowCount) {
        continue;
      }
      const node = row * columnCount + column;
      if (!freeNodes[node] || candidateNodes.has(node)) continue;
      if (distance9(item.source, pointForNode(node)) > maxConnectorLength) {
        continue;
      }
      const connectorPoints = connectWith45DegreeSegments(
        item.source,
        pointForNode(node)
      );
      if (!connectorIsClear(connectorPoints, item)) continue;
      candidateNodes.add(node);
      candidates.push({ node, connectorPoints });
      if (candidates.length >= 12) break;
    }
    if (candidates.length === 0) return null;
    terminals.push({ item, equivalentItems, candidates });
  }
  const selectedConnectorSegments = [];
  for (const terminal of [...terminals].sort(
    (first, second) => first.candidates.length - second.candidates.length
  )) {
    const candidateOffset = terminal.candidates.length === 0 ? 0 : connectorSelectionOffset % terminal.candidates.length;
    const orderedCandidates = [
      ...terminal.candidates.slice(candidateOffset),
      ...terminal.candidates.slice(0, candidateOffset)
    ];
    const candidate = orderedCandidates.find((value) => {
      const segments = getSegments(value.connectorPoints, traceWidth);
      return selectedConnectorSegments.every(
        (selected) => selected.netKey === terminal.item.netKey || segments.every(
          (segment2) => selected.segments.every(
            (otherSegment) => distanceSegmentToSegment(
              segment2.start,
              segment2.end,
              otherSegment.start,
              otherSegment.end
            ) >= requiredRouteDistance - EPSILON2
          )
        )
      );
    });
    if (!candidate) return null;
    terminal.candidates = [candidate];
    selectedConnectorSegments.push({
      netKey: terminal.item.netKey,
      segments: getSegments(candidate.connectorPoints, traceWidth)
    });
  }
  const terminalNodes = new Set(
    terminals.map((terminal) => terminal.candidates[0].node)
  );
  for (const selected of selectedConnectorSegments) {
    for (const segment2 of selected.segments) {
      const minColumn = Math.max(
        0,
        Math.floor(
          (Math.min(segment2.start.x, segment2.end.x) - requiredRouteDistance - boundary.minX) / step
        )
      );
      const maxColumn = Math.min(
        columnCount - 1,
        Math.ceil(
          (Math.max(segment2.start.x, segment2.end.x) + requiredRouteDistance - boundary.minX) / step
        )
      );
      const minRow = Math.max(
        0,
        Math.floor(
          (Math.min(segment2.start.y, segment2.end.y) - requiredRouteDistance - boundary.minY) / step
        )
      );
      const maxRow = Math.min(
        rowCount - 1,
        Math.ceil(
          (Math.max(segment2.start.y, segment2.end.y) + requiredRouteDistance - boundary.minY) / step
        )
      );
      for (let row = minRow; row <= maxRow; row++) {
        for (let column = minColumn; column <= maxColumn; column++) {
          const node = row * columnCount + column;
          if (!freeNodes[node] || terminalNodes.has(node)) continue;
          const point6 = pointForNode(node);
          if (distanceSegmentToSegment(segment2.start, segment2.end, point6, point6) < requiredRouteDistance - EPSILON2) {
            freeNodes[node] = 0;
          }
        }
      }
    }
  }
  const source = 0;
  const terminalStart = 1;
  const gridInStart = terminalStart + terminals.length;
  const gridOutStart = gridInStart + gridNodeCount;
  const sink = gridOutStart + gridNodeCount;
  const flow = new Dinic(sink + 1);
  for (let terminalIndex = 0; terminalIndex < terminals.length; terminalIndex++) {
    const terminalNode = terminalStart + terminalIndex;
    flow.addEdge(source, terminalNode, 1);
    for (const candidate of terminals[terminalIndex].candidates) {
      flow.addEdge(terminalNode, gridInStart + candidate.node, 1, {
        gridNode: candidate.node
      });
    }
  }
  for (let node = 0; node < gridNodeCount; node++) {
    if (!freeNodes[node]) continue;
    flow.addEdge(gridInStart + node, gridOutStart + node, 1);
    const column = node % columnCount;
    const row = Math.floor(node / columnCount);
    for (const nextNode of grid.neighbors[node]) {
      if (!freeNodes[nextNode]) continue;
      flow.addEdge(gridOutStart + node, gridInStart + nextNode, 1, {
        gridNode: nextNode
      });
    }
    const isTarget = direction === "any" && ((!availableDirections || availableDirections.has("left")) && column === 0 || (!availableDirections || availableDirections.has("right")) && column === columnCount - 1 || (!availableDirections || availableDirections.has("down")) && row === 0 || (!availableDirections || availableDirections.has("up")) && row === rowCount - 1) || direction === "left" && column === 0 || direction === "right" && column === columnCount - 1 || direction === "down" && row === 0 || direction === "up" && row === rowCount - 1;
    if (isTarget) {
      flow.addEdge(gridOutStart + node, sink, 1, { isSink: true });
    }
  }
  const achievedFlow = flow.maximumFlow(source, sink, terminals.length);
  const terminalWasMatched = (terminalIndex) => {
    const terminalNode = terminalStart + terminalIndex;
    return flow.edges[source].some(
      (edge) => edge.to === terminalNode && edge.initialCapacity === 1 && edge.capacity === 0
    );
  };
  if (achievedFlow !== terminals.length) {
    if (FANOUT_FLOW_DEBUG_ENABLED) {
      const unmatchedConnections = terminals.flatMap((terminal, index) => {
        const terminalNode = terminalStart + index;
        return terminalWasMatched(index) ? [] : [terminal.item.connection.connection.name];
      });
      console.error("single-layer flow group failed", {
        direction,
        achievedFlow,
        requiredFlow: terminals.length,
        unmatchedConnections
      });
    }
  }
  const routes = [];
  const usedNodes = [];
  const unmatchedItems = [];
  for (let terminalIndex = 0; terminalIndex < terminals.length; terminalIndex++) {
    const terminal = terminals[terminalIndex];
    if (!terminalWasMatched(terminalIndex)) {
      unmatchedItems.push(...terminal.equivalentItems);
      continue;
    }
    const terminalNode = terminalStart + terminalIndex;
    const candidateEdge = flow.edges[terminalNode].find(
      (edge) => edge.initialCapacity === 1 && edge.capacity === 0 && edge.gridNode !== void 0
    );
    if (candidateEdge?.gridNode === void 0) return null;
    const candidate = terminal.candidates.find(
      (value) => value.node === candidateEdge.gridNode
    );
    if (!candidate) return null;
    const gridPoints = [pointForNode(candidate.node)];
    let node = candidate.node;
    const routeNodes = [node];
    for (let guard = 0; guard <= gridNodeCount; guard++) {
      const outNode = gridOutStart + node;
      const nextEdge = flow.edges[outNode].find(
        (edge) => edge.initialCapacity === 1 && edge.capacity === 0 && (edge.gridNode !== void 0 || edge.isSink)
      );
      if (!nextEdge) return null;
      if (nextEdge.isSink) break;
      if (nextEdge.gridNode === void 0) return null;
      node = nextEdge.gridNode;
      routeNodes.push(node);
      gridPoints.push(pointForNode(node));
    }
    usedNodes.push(...routeNodes);
    const unchamferedPoints = compressPath([
      ...candidate.connectorPoints,
      ...gridPoints.slice(1)
    ]);
    const points = enforceStraightOr45DegreeSegments(
      chamferOrthogonalPolyline2(unchamferedPoints, step / 2)
    );
    const segments = getSegments(points, traceWidth);
    for (const item of terminal.equivalentItems) {
      routes.push({ item, points, segments });
    }
  }
  return { routes, usedNodes, unmatchedItems };
}
function buildPlan2(route, traceWidth) {
  const { item, points, segments } = route;
  const traceRoute = points.map(
    (point6, index) => ({
      route_type: "wire",
      x: point6.x,
      y: point6.y,
      width: traceWidth,
      layer: "top",
      ...index === 0 && item.connection.sourcePoint.pcb_port_id ? { start_pcb_port_id: item.connection.sourcePoint.pcb_port_id } : {}
    })
  );
  return {
    busId: item.bus.busId,
    connectionName: item.connection.connection.name,
    connectionIndex: item.connection.connectionIndex,
    sourcePointIndex: item.connection.sourcePointIndex,
    sourcePoint: item.connection.sourcePoint,
    sourceObstacle: item.connection.sourceObstacle,
    sourceLayer: item.connection.sourceLayer,
    targetLayer: "top",
    termination: item.bus.termination,
    direction: item.bus.direction,
    exitPoint: points.at(-1),
    trace: {
      type: "pcb_trace",
      pcb_trace_id: `fanout:${item.connection.connection.name}`,
      connection_name: item.connection.connection.name,
      connectsTo: [
        item.connection.connection.name,
        item.netKey,
        ...item.connection.sourcePoint.pointId ? [item.connection.sourcePoint.pointId] : [],
        ...item.connection.sourcePoint.pcb_port_id ? [item.connection.sourcePoint.pcb_port_id] : []
      ],
      route: traceRoute
    },
    segments,
    length: segments.reduce(
      (total, segment2) => total + distance9(segment2.start, segment2.end),
      0
    )
  };
}
function getConnectorVariants(start, end) {
  const first = connectWith45DegreeSegments(start, end);
  const second = connectWith45DegreeSegments(end, start).reverse();
  return first.length === second.length && first.every((point6, index) => distance9(point6, second[index]) < EPSILON2) ? [first] : [first, second];
}
function plansHaveRequiredClearance(params) {
  const { plans, items, obstacles, traceWidth, clearance } = params;
  const netKeyByConnectionName = new Map(
    items.map((item) => [item.connection.connection.name, item.netKey])
  );
  const uniqueSegmentsByNet = /* @__PURE__ */ new Map();
  const segmentKeysByNet = /* @__PURE__ */ new Map();
  for (const plan of plans) {
    const netKey = netKeyByConnectionName.get(plan.connectionName);
    const segments = uniqueSegmentsByNet.get(netKey) ?? [];
    const segmentKeys = segmentKeysByNet.get(netKey) ?? /* @__PURE__ */ new Set();
    for (const segment2 of plan.segments) {
      const endpoints = [segment2.start, segment2.end].map((point6) => `${point6.x.toFixed(6)}:${point6.y.toFixed(6)}`).sort();
      const key = endpoints.join(":");
      if (segmentKeys.has(key)) continue;
      segmentKeys.add(key);
      segments.push(segment2);
    }
    uniqueSegmentsByNet.set(netKey, segments);
    segmentKeysByNet.set(netKey, segmentKeys);
  }
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  for (const [netKey, segments] of uniqueSegmentsByNet) {
    for (const segment2 of segments) {
      for (const obstacle of obstacles) {
        if (obstacle.connectedTo.includes(netKey) || distanceSegmentToObstacle(segment2, obstacle) >= requiredObstacleDistance - EPSILON2) {
          continue;
        }
        if (FANOUT_FLOW_DEBUG_ENABLED) {
          console.error("single-layer route violates obstacle clearance", {
            netKey,
            segment: segment2,
            obstacleId: obstacle.obstacleId,
            distance: distanceSegmentToObstacle(segment2, obstacle),
            requiredObstacleDistance
          });
        }
        return false;
      }
    }
  }
  const requiredRouteDistance = traceWidth + clearance;
  const entries = [...uniqueSegmentsByNet];
  for (let firstIndex = 0; firstIndex < entries.length; firstIndex++) {
    for (let secondIndex = firstIndex + 1; secondIndex < entries.length; secondIndex++) {
      for (const first of entries[firstIndex][1]) {
        for (const second of entries[secondIndex][1]) {
          if (distanceSegmentToSegment(
            first.start,
            first.end,
            second.start,
            second.end
          ) < requiredRouteDistance - EPSILON2) {
            if (FANOUT_FLOW_DEBUG_ENABLED) {
              console.error("single-layer routes violate copper clearance", {
                firstNetKey: entries[firstIndex][0],
                secondNetKey: entries[secondIndex][0],
                first,
                second,
                distance: distanceSegmentToSegment(
                  first.start,
                  first.end,
                  second.start,
                  second.end
                ),
                requiredRouteDistance
              });
            }
            return false;
          }
        }
      }
    }
  }
  return true;
}
function getDirectionForBoundaryPoint(point6, boundary) {
  if (Math.abs(point6.x - boundary.minX) < EPSILON2) return "left";
  if (Math.abs(point6.x - boundary.maxX) < EPSILON2) return "right";
  if (Math.abs(point6.y - boundary.minY) < EPSILON2) return "down";
  if (Math.abs(point6.y - boundary.maxY) < EPSILON2) return "up";
  return null;
}
function routeWithAdaptiveExits(params) {
  const {
    items,
    grid,
    obstacles,
    traceWidth,
    clearance,
    availableBoundaryRegions
  } = params;
  const availableDirections = availableBoundaryRegions ? new Set(availableBoundaryRegions.map((region) => region.direction)) : void 0;
  const mergeItems = new Set(
    items.filter(
      (item) => item.connection.sourceObstacle.width > 2 && item.connection.sourceObstacle.height > 2
    )
  );
  let unrestricted = null;
  for (let mergeRound = 0; mergeRound < 4; mergeRound++) {
    const directlyRoutedItems = items.filter((item) => !mergeItems.has(item));
    let bestResult = null;
    for (let connectorSelectionOffset = 0; connectorSelectionOffset < 4; connectorSelectionOffset++) {
      const result = routeDirectionGroup({
        direction: "any",
        availableDirections,
        items: directlyRoutedItems,
        grid,
        obstacles,
        traceWidth,
        clearance,
        occupiedNodes: new Uint8Array(grid.nodeCount),
        acceptedSegments: [],
        connectorSelectionOffset
      });
      if (!result) continue;
      if (!bestResult || result.routes.length > bestResult.routes.length) {
        bestResult = result;
      }
      if (result.routes.length === directlyRoutedItems.length) break;
    }
    if (!bestResult) return null;
    if (bestResult.routes.length === directlyRoutedItems.length) {
      unrestricted = bestResult;
      break;
    }
    const directlyRoutedNetCounts = /* @__PURE__ */ new Map();
    for (const item of directlyRoutedItems) {
      directlyRoutedNetCounts.set(
        item.netKey,
        (directlyRoutedNetCounts.get(item.netKey) ?? 0) + 1
      );
    }
    let addedMergeItem = false;
    for (const item of bestResult.unmatchedItems) {
      if ((directlyRoutedNetCounts.get(item.netKey) ?? 0) < 2) continue;
      mergeItems.add(item);
      addedMergeItem = true;
    }
    if (!addedMergeItem) return null;
  }
  if (!unrestricted) return null;
  const routes = [...unrestricted.routes];
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  const requiredRouteDistance = traceWidth + clearance;
  for (const mergeItem of mergeItems) {
    const candidates = routes.filter((route) => route.item.netKey === mergeItem.netKey).flatMap(
      (route) => route.points.map((point6, pointIndex) => ({
        route,
        point: point6,
        pointIndex
      }))
    ).sort(
      (first, second) => Number(
        second.route.item.bus.componentId === mergeItem.bus.componentId
      ) - Number(
        first.route.item.bus.componentId === mergeItem.bus.componentId
      ) || distance9(mergeItem.source, first.point) - distance9(mergeItem.source, second.point)
    );
    let mergedRoute = null;
    for (const candidate of candidates) {
      for (const connectorPoints of getConnectorVariants(
        mergeItem.source,
        candidate.point
      )) {
        const connectorSegments = getSegments(connectorPoints, traceWidth);
        const clearsObstacles = connectorSegments.every(
          (segment2) => obstacles.every(
            (obstacle) => obstacleBelongsToItem(obstacle, mergeItem) || distanceSegmentToObstacle(segment2, obstacle) >= requiredObstacleDistance - EPSILON2
          )
        );
        const clearsRoutes = connectorSegments.every(
          (segment2) => routes.every(
            (route) => route.item.netKey === mergeItem.netKey || route.segments.every(
              (otherSegment) => distanceSegmentToSegment(
                segment2.start,
                segment2.end,
                otherSegment.start,
                otherSegment.end
              ) >= requiredRouteDistance - EPSILON2
            )
          )
        );
        if (!clearsObstacles || !clearsRoutes) continue;
        const points = enforceStraightOr45DegreeSegments(
          compressPath([
            ...connectorPoints,
            ...candidate.route.points.slice(candidate.pointIndex + 1)
          ])
        );
        mergedRoute = {
          item: mergeItem,
          points,
          segments: getSegments(points, traceWidth)
        };
        break;
      }
      if (mergedRoute) break;
    }
    if (!mergedRoute) return null;
    routes.push(mergedRoute);
  }
  const plans = routes.map((route) => buildPlan2(route, traceWidth));
  if (!plansHaveRequiredClearance({
    plans,
    items,
    obstacles,
    traceWidth,
    clearance
  })) {
    if (FANOUT_FLOW_DEBUG_ENABLED) {
      console.error("single-layer adaptive exits failed exact clearance");
    }
    return null;
  }
  for (const plan of plans) {
    const direction = getDirectionForBoundaryPoint(
      plan.exitPoint,
      grid.boundary
    );
    if (!direction || availableDirections && !availableDirections.has(direction)) {
      return null;
    }
    const item = items.find(
      (candidate) => candidate.connection.connection.name === plan.connectionName
    );
    item.bus.direction = direction;
    const compatibleRegions = availableBoundaryRegions?.filter(
      (region) => region.direction === direction
    );
    const exitCoordinate = direction === "up" || direction === "down" ? plan.exitPoint.x : plan.exitPoint.y;
    item.bus.preferredExit = compatibleRegions?.toSorted(
      (first, second) => Math.abs(exitCoordinate - getRegionAnchor(first, grid.boundary)) - Math.abs(exitCoordinate - getRegionAnchor(second, grid.boundary))
    )[0]?.preferredExit ?? (direction === "up" ? "top" : direction === "down" ? "bottom" : direction);
    plan.direction = direction;
  }
  const planByConnectionName = new Map(
    plans.map((plan) => [plan.connectionName, plan])
  );
  return items.map(
    (item) => planByConnectionName.get(item.connection.connection.name)
  );
}
function routeSingleLayerWithAdaptiveExits(params) {
  const { srj, buses, traceWidth, clearance, availableBoundaryRegions } = params;
  if (buses.some((bus) => bus.connections.length !== 1)) return null;
  const items = buses.flatMap(
    (bus) => bus.connections.map(
      (connection) => ({
        bus,
        connection,
        source: {
          x: connection.sourcePoint.x,
          y: connection.sourcePoint.y
        },
        netKey: getNetKey(connection)
      })
    )
  );
  const topObstacles = srj.obstacles.filter(
    (obstacle) => obstacle.layers.includes("top")
  );
  const boundary = buses[0]?.sharedBoundary;
  if (!boundary) return [];
  const grid = createFlowGrid({
    boundary,
    obstacles: topObstacles,
    traceWidth,
    clearance
  });
  if (FANOUT_FLOW_DEBUG_ENABLED) {
    console.error("single-layer adaptive-exit grid ready", {
      nodeCount: grid.nodeCount
    });
  }
  const adaptivePlans = routeWithAdaptiveExits({
    items,
    grid,
    obstacles: topObstacles,
    traceWidth,
    clearance,
    availableBoundaryRegions
  });
  if (adaptivePlans) return adaptivePlans;
  return null;
}

// node_modules/@tscircuit/fanout-solver/lib/route-single-layer-push-shove.ts
function isHorizontal2(direction) {
  return direction === "left" || direction === "right";
}
function directionSign2(direction) {
  return direction === "right" || direction === "up" ? 1 : -1;
}
function getAxis2(point6, direction) {
  return isHorizontal2(direction) ? point6.x : point6.y;
}
function getPerpendicularAxis2(point6, direction) {
  return isHorizontal2(direction) ? point6.y : point6.x;
}
function makePoint2(axis, perpendicularAxis, direction) {
  return isHorizontal2(direction) ? { x: axis, y: perpendicularAxis } : { x: perpendicularAxis, y: axis };
}
function getExitAxis2(bus) {
  switch (bus.direction) {
    case "right":
      return bus.sharedBoundary.maxX;
    case "left":
      return bus.sharedBoundary.minX;
    case "up":
      return bus.sharedBoundary.maxY;
    case "down":
      return bus.sharedBoundary.minY;
  }
}
function compressPath2(points) {
  if (points.length < 3) return points;
  const compressed = [points[0]];
  for (let index = 1; index < points.length - 1; index++) {
    const previous = compressed.at(-1);
    const current = points[index];
    const next = points[index + 1];
    const incoming = {
      x: Math.sign(current.x - previous.x),
      y: Math.sign(current.y - previous.y)
    };
    const outgoing = {
      x: Math.sign(next.x - current.x),
      y: Math.sign(next.y - current.y)
    };
    if (incoming.x !== outgoing.x || incoming.y !== outgoing.y) {
      compressed.push(current);
    }
  }
  compressed.push(points.at(-1));
  return compressed;
}
function getPathSegments(points, traceWidth) {
  const segments = [];
  for (let index = 1; index < points.length; index++) {
    if (distance9(points[index - 1], points[index]) < 1e-9) continue;
    segments.push({
      start: points[index - 1],
      end: points[index],
      width: traceWidth,
      layer: "top"
    });
  }
  return segments;
}
function getCornerChannelPrefixes(params) {
  const { srj, bus, connection, traceWidth, clearance } = params;
  const direction = bus.direction;
  const sign = directionSign2(direction);
  const source = {
    x: connection.sourcePoint.x,
    y: connection.sourcePoint.y
  };
  const sourceAxis = getAxis2(source, direction);
  const sourceTrack = getPerpendicularAxis2(source, direction);
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  const componentExitAxis = (() => {
    switch (direction) {
      case "right":
        return bus.componentBounds.maxX + requiredObstacleDistance;
      case "left":
        return bus.componentBounds.minX - requiredObstacleDistance;
      case "up":
        return bus.componentBounds.maxY + requiredObstacleDistance;
      case "down":
        return bus.componentBounds.minY - requiredObstacleDistance;
    }
  })();
  const obstacleDistanceForSegment = (segment2) => Math.min(
    ...srj.obstacles.filter(
      (obstacle) => obstacle !== connection.sourceObstacle && obstacle.layers.includes("top")
    ).map((obstacle) => distanceSegmentToObstacle(segment2, obstacle))
  );
  const directSegment = {
    start: source,
    end: makePoint2(componentExitAxis, sourceTrack, direction),
    width: traceWidth,
    layer: "top"
  };
  if (obstacleDistanceForSegment(directSegment) >= requiredObstacleDistance - 1e-9) {
    return null;
  }
  const perpendicularBounds = isHorizontal2(direction) ? {
    minimum: bus.componentBounds.minY,
    maximum: bus.componentBounds.maxY
  } : {
    minimum: bus.componentBounds.minX,
    maximum: bus.componentBounds.maxX
  };
  const directionalDistance = sign * (componentExitAxis - sourceAxis);
  const targetTrack = getPerpendicularAxis2(connection.targetPoint, direction);
  const lanePitch = traceWidth + clearance;
  const exitAxis = getExitAxis2(bus);
  const candidates = [-1, 1].map((perpendicularSign) => {
    const perpendicularExit = perpendicularSign < 0 ? perpendicularBounds.minimum - requiredObstacleDistance : perpendicularBounds.maximum + requiredObstacleDistance;
    const perpendicularDistance = perpendicularSign * (perpendicularExit - sourceTrack);
    const diagonalDistance = Math.max(
      directionalDistance,
      perpendicularDistance
    );
    const diagonalEnd = makePoint2(
      sourceAxis + sign * diagonalDistance,
      sourceTrack + perpendicularSign * diagonalDistance,
      direction
    );
    if (sign * (exitAxis - getAxis2(diagonalEnd, direction)) < lanePitch - 1e-9) {
      return null;
    }
    const points = [source, diagonalEnd];
    const withinBoundary = points.every(
      (point6) => point6.x >= bus.sharedBoundary.minX - 1e-9 && point6.x <= bus.sharedBoundary.maxX + 1e-9 && point6.y >= bus.sharedBoundary.minY - 1e-9 && point6.y <= bus.sharedBoundary.maxY + 1e-9
    );
    if (!withinBoundary) return null;
    const obstacleDistance = Math.min(
      ...getPathSegments(points, traceWidth).map(obstacleDistanceForSegment)
    );
    if (obstacleDistance < requiredObstacleDistance - 1e-9) return null;
    return {
      points,
      obstacleDistance,
      targetDistance: Math.abs(
        getPerpendicularAxis2(diagonalEnd, direction) - targetTrack
      )
    };
  }).filter((candidate) => candidate !== null).toSorted(
    (first, second) => second.obstacleDistance - first.obstacleDistance || first.targetDistance - second.targetDistance
  );
  return candidates.length > 0 ? candidates.map((candidate) => candidate.points) : null;
}
function completeCornerChannelRoute(params) {
  const { item, srj, acceptedSegments, traceWidth, clearance } = params;
  const prefixes = item.cornerChannelPrefixes;
  if (!prefixes) return null;
  const direction = item.direction;
  const sign = directionSign2(direction);
  const exitAxis = getExitAxis2(item.bus);
  const lanePitch = traceWidth + clearance;
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  const boundaryMinimum = isHorizontal2(direction) ? item.bus.sharedBoundary.minY : item.bus.sharedBoundary.minX;
  const boundaryMaximum = isHorizontal2(direction) ? item.bus.sharedBoundary.maxY : item.bus.sharedBoundary.maxX;
  const minimumTrack = boundaryMinimum + traceWidth / 2;
  const maximumTrack = boundaryMaximum - traceWidth / 2;
  const targetTrack = getPerpendicularAxis2(
    item.connection.targetPoint,
    direction
  );
  const trackStep = lanePitch / 2;
  const trackCandidates = /* @__PURE__ */ new Set([
    Math.max(minimumTrack, Math.min(maximumTrack, targetTrack))
  ]);
  for (let track = Math.ceil(minimumTrack / trackStep) * trackStep; track <= maximumTrack + 1e-9; track += trackStep) {
    trackCandidates.add(Number(track.toFixed(9)));
  }
  const orderedTracks = [...trackCandidates].toSorted(
    (first, second) => Math.abs(first - targetTrack) - Math.abs(second - targetTrack)
  );
  for (const prefix of prefixes) {
    const diagonalEnd = prefix.at(-1);
    const diagonalTrack = getPerpendicularAxis2(diagonalEnd, direction);
    for (const track of orderedTracks) {
      const shift = Math.abs(track - diagonalTrack);
      const straightEnd = makePoint2(
        getAxis2(diagonalEnd, direction) + sign * lanePitch,
        diagonalTrack,
        direction
      );
      const doglegEnd = makePoint2(
        getAxis2(straightEnd, direction) + sign * shift,
        track,
        direction
      );
      if (sign * (exitAxis - getAxis2(doglegEnd, direction)) < -1e-9) {
        continue;
      }
      const boundaryPoint = makePoint2(exitAxis, track, direction);
      const completionPoints = [
        ...prefix,
        straightEnd,
        ...shift > 1e-9 ? [doglegEnd] : []
      ];
      if (distance9(completionPoints.at(-1), boundaryPoint) > 1e-9) {
        completionPoints.push(boundaryPoint);
      }
      const points = compressPath2(completionPoints);
      const segments = getPathSegments(points, traceWidth);
      const clearsObstacles = segments.every(
        (segment2) => srj.obstacles.every(
          (obstacle) => obstacle === item.connection.sourceObstacle || !obstacle.layers.includes("top") || distanceSegmentToObstacle(segment2, obstacle) >= requiredObstacleDistance - 1e-9
        )
      );
      if (!clearsObstacles) continue;
      const clearsRoutes = segments.every(
        (segment2) => acceptedSegments.every(
          (acceptedSegment) => distanceSegmentToSegment(
            segment2.start,
            segment2.end,
            acceptedSegment.start,
            acceptedSegment.end
          ) >= traceWidth + clearance - 1e-9
        )
      );
      if (!clearsRoutes) continue;
      return { item, points, track };
    }
  }
  return null;
}
function getMaximumObstacleClearDistributionShift(params) {
  const {
    route,
    direction,
    exitAxis,
    signedShift,
    maximumShift,
    obstacles,
    requiredObstacleDistance
  } = params;
  if (maximumShift <= 1e-9 || Math.abs(signedShift) <= 1e-9) return 0;
  const perpendicularSign = Math.sign(signedShift);
  const shiftIsClear = (shift) => {
    if (shift <= 1e-9) return true;
    const segment2 = {
      start: makePoint2(
        exitAxis - directionSign2(direction) * shift,
        route.track,
        direction
      ),
      end: makePoint2(
        exitAxis,
        route.track + perpendicularSign * shift,
        direction
      ),
      width: 0,
      layer: "top"
    };
    return obstacles.every(
      (obstacle) => obstacle === route.item.connection.sourceObstacle || !obstacle.layers.includes("top") || distanceSegmentToObstacle(segment2, obstacle) >= requiredObstacleDistance - 1e-9
    );
  };
  if (shiftIsClear(maximumShift)) return maximumShift;
  let lowerShift = 0;
  let upperShift = maximumShift;
  for (let iteration = 0; iteration < 32; iteration++) {
    const candidateShift = (lowerShift + upperShift) / 2;
    if (shiftIsClear(candidateShift)) lowerShift = candidateShift;
    else upperShift = candidateShift;
  }
  return lowerShift;
}
function selectOrderedTracks(params) {
  const { requestedTracks, currentTracks, candidateTracks, maximumShifts } = params;
  const rowCount = requestedTracks.length + 1;
  const columnCount = candidateTracks.length + 1;
  const costs = Array.from(
    { length: rowCount },
    () => new Float64Array(columnCount).fill(Number.POSITIVE_INFINITY)
  );
  const tookCandidate = Array.from(
    { length: rowCount },
    () => new Uint8Array(columnCount)
  );
  costs[0].fill(0);
  for (let row2 = 1; row2 < rowCount; row2++) {
    for (let column2 = 1; column2 < columnCount; column2++) {
      const skippedCost = costs[row2][column2 - 1];
      const requestedShift = Math.abs(
        requestedTracks[row2 - 1] - candidateTracks[column2 - 1]
      );
      const currentShift = Math.abs(
        currentTracks[row2 - 1] - candidateTracks[column2 - 1]
      );
      const selectedCost = currentShift > maximumShifts[row2 - 1] + 1e-9 ? Number.POSITIVE_INFINITY : costs[row2 - 1][column2 - 1] + requestedShift;
      if (selectedCost < skippedCost) {
        costs[row2][column2] = selectedCost;
        tookCandidate[row2][column2] = 1;
      } else {
        costs[row2][column2] = skippedCost;
      }
    }
  }
  if (!Number.isFinite(costs.at(-1).at(-1))) return null;
  const selectedTracks = [];
  let row = requestedTracks.length;
  let column = candidateTracks.length;
  while (row > 0 && column > 0) {
    if (tookCandidate[row][column]) {
      selectedTracks.push(candidateTracks[column - 1]);
      row--;
    }
    column--;
  }
  if (row > 0) return null;
  return selectedTracks.reverse();
}
function getCandidateTracks(params) {
  const {
    direction,
    activeRoutes,
    obstacles,
    boundaryMinimum,
    boundaryMaximum,
    traceWidth,
    clearance,
    requestedTracks,
    maximumShifts
  } = params;
  const lanePitch = traceWidth + clearance;
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  let selectedTracks = null;
  let selectedCost = Number.POSITIVE_INFINITY;
  for (const phase of Array.from(
    { length: Math.round(lanePitch / traceWidth) },
    (_, index) => index * traceWidth
  )) {
    const candidates = [];
    const firstTrack = Math.ceil((boundaryMinimum - phase) / lanePitch) * lanePitch + phase;
    for (let track = firstTrack; track <= boundaryMaximum + 1e-9; track += lanePitch) {
      const roundedTrack = Math.round(track / traceWidth) * traceWidth;
      if (obstacles.every((obstacle) => {
        const obstacleTrack = getPerpendicularAxis2(obstacle.center, direction);
        const obstacleSize = isHorizontal2(direction) ? obstacle.height : obstacle.width;
        return Math.abs(roundedTrack - obstacleTrack) >= obstacleSize / 2 + requiredObstacleDistance - 1e-9;
      })) {
        candidates.push(roundedTrack);
      }
    }
    const tracks = selectOrderedTracks({
      requestedTracks,
      currentTracks: activeRoutes.map((route) => route.track),
      candidateTracks: candidates,
      maximumShifts
    });
    if (!tracks) continue;
    const cost = tracks.reduce(
      (sum, track, index) => sum + Math.abs(track - requestedTracks[index]),
      0
    );
    if (cost < selectedCost) {
      selectedCost = cost;
      selectedTracks = tracks;
    }
  }
  return selectedTracks;
}
function getCornerSide(corner, direction) {
  switch (direction) {
    case "up":
      if (corner === "top-left") return "minimum";
      if (corner === "top-right") return "maximum";
      return null;
    case "down":
      if (corner === "bottom-left") return "minimum";
      if (corner === "bottom-right") return "maximum";
      return null;
    case "left":
      if (corner === "bottom-left") return "minimum";
      if (corner === "top-left") return "maximum";
      return null;
    case "right":
      if (corner === "bottom-right") return "minimum";
      if (corner === "top-right") return "maximum";
      return null;
  }
}
function getFinalTrackTargets(params) {
  const {
    items,
    direction,
    boundaryMinimum,
    boundaryMaximum,
    traceWidth,
    clearance,
    borderDistribution,
    currentTrackByConnectionName
  } = params;
  const getCurrentTrack = (item) => currentTrackByConnectionName.get(item.connection.connection.name) ?? getPerpendicularAxis2(item.source, direction);
  const orderedItems = [...items].toSorted(
    (first, second) => getCurrentTrack(first) - getCurrentTrack(second) || first.connection.connection.name.localeCompare(
      second.connection.connection.name
    )
  );
  const lanePitch = traceWidth + clearance;
  const minimumLane = boundaryMinimum + traceWidth / 2;
  const maximumLane = boundaryMaximum - traceWidth / 2;
  const availableSpan = maximumLane - minimumLane;
  const requiredSpan = lanePitch * Math.max(orderedItems.length - 1, 0);
  if (availableSpan < requiredSpan - 1e-9) return null;
  const sourceTracks = orderedItems.map(getCurrentTrack);
  let targetTracks = [...sourceTracks];
  let distributedPitch = lanePitch;
  const enforcedConnectionNames = /* @__PURE__ */ new Set();
  if (borderDistribution === "even" && orderedItems.length > 1) {
    const sourceMinimum = sourceTracks[0];
    const sourceMaximum = sourceTracks.at(-1);
    const desiredPitch = Math.max(
      lanePitch,
      (sourceMaximum - sourceMinimum) / (orderedItems.length - 1)
    );
    const buildOutwardTracks = (pitch) => {
      const tracks = [...sourceTracks];
      const upperMiddle = Math.floor(orderedItems.length / 2);
      const lowerMiddle = orderedItems.length % 2 === 0 ? upperMiddle - 1 : upperMiddle;
      if (lowerMiddle !== upperMiddle) {
        const middleGap = tracks[upperMiddle] - tracks[lowerMiddle];
        if (middleGap < pitch) {
          const shove = (pitch - middleGap) / 2;
          tracks[lowerMiddle] = tracks[lowerMiddle] - shove;
          tracks[upperMiddle] = tracks[upperMiddle] + shove;
        }
      }
      for (let index = lowerMiddle - 1; index >= 0; index--) {
        tracks[index] = Math.min(tracks[index], tracks[index + 1] - pitch);
      }
      for (let index = upperMiddle + 1; index < tracks.length; index++) {
        tracks[index] = Math.max(tracks[index], tracks[index - 1] + pitch);
      }
      return tracks;
    };
    const tracksFitBoundary = (tracks) => tracks[0] >= minimumLane - 1e-9 && tracks.at(-1) <= maximumLane + 1e-9;
    targetTracks = buildOutwardTracks(desiredPitch);
    if (tracksFitBoundary(targetTracks)) {
      distributedPitch = desiredPitch;
    } else {
      let lowerPitch = lanePitch;
      let upperPitch = desiredPitch;
      targetTracks = buildOutwardTracks(lowerPitch);
      if (!tracksFitBoundary(targetTracks)) return null;
      for (let iteration = 0; iteration < 32; iteration++) {
        const candidatePitch = (lowerPitch + upperPitch) / 2;
        const candidateTracks = buildOutwardTracks(candidatePitch);
        if (tracksFitBoundary(candidateTracks)) {
          lowerPitch = candidatePitch;
          targetTracks = candidateTracks;
        } else {
          upperPitch = candidatePitch;
        }
      }
      distributedPitch = lowerPitch;
    }
    for (const item of orderedItems) {
      enforcedConnectionNames.add(item.connection.connection.name);
    }
  }
  const minimumCornerIndexes = [];
  const maximumCornerIndexes = [];
  for (let index = 0; index < orderedItems.length; index++) {
    const item = orderedItems[index];
    const preferredExit = item.bus.preferredExit;
    if (!preferredExit?.includes("-")) continue;
    const cornerSide = getCornerSide(preferredExit, direction);
    if (!cornerSide) return null;
    enforcedConnectionNames.add(item.connection.connection.name);
    if (cornerSide === "minimum") minimumCornerIndexes.push(index);
    else maximumCornerIndexes.push(index);
  }
  if (minimumCornerIndexes.some(
    (index, expectedIndex) => index !== expectedIndex
  ) || maximumCornerIndexes.some(
    (index, offset) => index !== orderedItems.length - maximumCornerIndexes.length + offset
  )) {
    return null;
  }
  const cornerInset = lanePitch * 2;
  if (minimumCornerIndexes.length > 0) {
    targetTracks[0] = minimumLane + cornerInset;
    for (let index = 1; index < minimumCornerIndexes.length; index++) {
      const preservedPitch = Math.max(
        distributedPitch,
        sourceTracks[index] - sourceTracks[index - 1]
      );
      targetTracks[index] = targetTracks[index - 1] + preservedPitch;
    }
  }
  if (maximumCornerIndexes.length > 0) {
    const lastIndex = orderedItems.length - 1;
    targetTracks[lastIndex] = maximumLane - cornerInset;
    for (let index = lastIndex - 1; index >= orderedItems.length - maximumCornerIndexes.length; index--) {
      const preservedPitch = Math.max(
        distributedPitch,
        sourceTracks[index + 1] - sourceTracks[index]
      );
      targetTracks[index] = targetTracks[index + 1] - preservedPitch;
    }
  }
  return {
    byConnectionName: new Map(
      orderedItems.map((item, index) => [
        item.connection.connection.name,
        targetTracks[index]
      ])
    ),
    enforcedConnectionNames
  };
}
function buildPlan3(path) {
  const { item, points, segments } = path;
  const route = points.map((point6, index) => ({
    route_type: "wire",
    x: point6.x,
    y: point6.y,
    width: segments[0]?.width ?? 0.1,
    layer: "top",
    ...index === 0 && item.connection.sourcePoint.pcb_port_id ? { start_pcb_port_id: item.connection.sourcePoint.pcb_port_id } : {}
  }));
  return {
    busId: item.bus.busId,
    connectionName: item.connection.connection.name,
    connectionIndex: item.connection.connectionIndex,
    sourcePointIndex: item.connection.sourcePointIndex,
    sourcePoint: item.connection.sourcePoint,
    sourceObstacle: item.connection.sourceObstacle,
    sourceLayer: item.connection.sourceLayer,
    targetLayer: "top",
    termination: item.bus.termination,
    direction: item.direction,
    exitPoint: points.at(-1),
    trace: {
      type: "pcb_trace",
      pcb_trace_id: `fanout:${item.connection.connection.name}`,
      connection_name: item.connection.connection.name,
      connectsTo: [
        item.connection.connection.name,
        ...item.connection.sourcePoint.pointId ? [item.connection.sourcePoint.pointId] : [],
        ...item.connection.sourcePoint.pcb_port_id ? [item.connection.sourcePoint.pcb_port_id] : []
      ],
      route
    },
    segments,
    length: segments.reduce(
      (total, segment2) => total + distance9(segment2.start, segment2.end),
      0
    )
  };
}
function routesAreClear(params) {
  const { paths, obstacles, traceWidth, clearance } = params;
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  const requiredCenterDistance = traceWidth + clearance;
  for (const path of paths) {
    if (Math.abs(
      getAxis2(path.points.at(-1), path.item.direction) - getExitAxis2(path.item.bus)
    ) > 1e-6) {
      return false;
    }
    for (const segment2 of path.segments) {
      for (const obstacle of obstacles) {
        if (obstacle === path.item.connection.sourceObstacle || !obstacle.layers.includes("top")) {
          continue;
        }
        if (distanceSegmentToObstacle(segment2, obstacle) < requiredObstacleDistance - 1e-9) {
          return false;
        }
      }
    }
  }
  for (let firstIndex = 0; firstIndex < paths.length; firstIndex++) {
    for (let secondIndex = firstIndex + 1; secondIndex < paths.length; secondIndex++) {
      for (const firstSegment of paths[firstIndex].segments) {
        for (const secondSegment of paths[secondIndex].segments) {
          if (distanceSegmentToSegment(
            firstSegment.start,
            firstSegment.end,
            secondSegment.start,
            secondSegment.end
          ) < requiredCenterDistance - 1e-9) {
            return false;
          }
        }
      }
    }
  }
  return true;
}
function routeSingleLayerWithPushAndShove(params) {
  const { srj, buses, traceWidth, clearance, borderDistribution } = params;
  const requiredObstacleDistance = traceWidth / 2 + clearance;
  const recordsByConnectionName = /* @__PURE__ */ new Map();
  const enforcedFinalTargets = /* @__PURE__ */ new Map();
  const items = buses.flatMap(
    (bus) => bus.connections.map((connection) => {
      const source = {
        x: connection.sourcePoint.x,
        y: connection.sourcePoint.y
      };
      const cornerChannelPrefixes = getCornerChannelPrefixes({
        srj,
        bus,
        connection,
        traceWidth,
        clearance
      });
      return {
        bus,
        connection,
        direction: bus.direction,
        source,
        ...cornerChannelPrefixes ? { cornerChannelPrefixes } : {}
      };
    })
  );
  for (const direction of [
    "left",
    "right",
    "up",
    "down"
  ]) {
    const sign = directionSign2(direction);
    const directionItems = items.filter(
      (item) => item.direction === direction && !item.cornerChannelPrefixes
    );
    if (directionItems.length === 0) continue;
    const boundaryMinimum = isHorizontal2(direction) ? directionItems[0].bus.sharedBoundary.minY : directionItems[0].bus.sharedBoundary.minX;
    const boundaryMaximum = isHorizontal2(direction) ? directionItems[0].bus.sharedBoundary.maxY : directionItems[0].bus.sharedBoundary.maxX;
    const exitAxis = getExitAxis2(directionItems[0].bus);
    const sourcesByEventKey = /* @__PURE__ */ new Map();
    const obstaclesByEventKey = /* @__PURE__ */ new Map();
    const axisByEventKey = /* @__PURE__ */ new Map();
    for (const item of directionItems) {
      const axis = getAxis2(item.source, direction);
      const key = axis.toFixed(6);
      axisByEventKey.set(key, axis);
      const eventSources = sourcesByEventKey.get(key) ?? [];
      eventSources.push(item);
      sourcesByEventKey.set(key, eventSources);
    }
    for (const [eventKey, eventSources] of sourcesByEventKey) {
      const eventAxis = axisByEventKey.get(eventKey);
      const eventComponentIds = new Set(
        eventSources.map((item) => item.bus.componentId)
      );
      obstaclesByEventKey.set(
        eventKey,
        srj.obstacles.filter(
          (obstacle) => obstacle.layers.includes("top") && !!obstacle.componentId && eventComponentIds.has(obstacle.componentId) && Math.abs(getAxis2(obstacle.center, direction) - eventAxis) < 1e-6
        )
      );
    }
    const eventAxes = [...axisByEventKey.values()].toSorted(
      (a, b) => sign * (a - b)
    );
    const active = /* @__PURE__ */ new Map();
    for (let eventIndex = 0; eventIndex < eventAxes.length; eventIndex++) {
      const eventAxis = eventAxes[eventIndex];
      const eventKey = eventAxis.toFixed(6);
      for (const item of sourcesByEventKey.get(eventKey) ?? []) {
        const directionalPadSize = isHorizontal2(direction) ? item.connection.sourceObstacle.width : item.connection.sourceObstacle.height;
        const sourceEscapeDistance = Math.max(
          item.connection.sourceObstacle.width,
          item.connection.sourceObstacle.height
        ) < 1.5 ? directionalPadSize / 2 + requiredObstacleDistance : 0;
        const sourceEscapePoint = makePoint2(
          eventAxis + sign * sourceEscapeDistance,
          getPerpendicularAxis2(item.source, direction),
          direction
        );
        active.set(item.connection.connection.name, {
          item,
          points: distance9(item.source, sourceEscapePoint) > 1e-9 ? [item.source, sourceEscapePoint] : [item.source],
          track: getPerpendicularAxis2(item.source, direction)
        });
      }
      const nextAxis = eventAxes[eventIndex + 1] ?? exitAxis;
      if (active.size === 0 || sign * (nextAxis - eventAxis) <= 1e-9) {
        continue;
      }
      const orderedRoutes = [...active.values()].toSorted(
        (a, b) => a.track - b.track || a.item.connection.connection.name.localeCompare(
          b.item.connection.connection.name
        )
      );
      const lookaheadObstacles = eventAxes.slice(eventIndex + 1, eventIndex + 3).flatMap((axis) => obstaclesByEventKey.get(axis.toFixed(6)) ?? []);
      const maximumShifts = orderedRoutes.map(
        (route) => Math.abs(nextAxis - getAxis2(route.points.at(-1), direction))
      );
      const selectedTracks = getCandidateTracks({
        direction,
        activeRoutes: orderedRoutes,
        obstacles: lookaheadObstacles,
        boundaryMinimum,
        boundaryMaximum,
        traceWidth,
        clearance,
        requestedTracks: orderedRoutes.map((route) => route.track),
        maximumShifts
      });
      if (!selectedTracks) return null;
      for (let index = 0; index < orderedRoutes.length; index++) {
        const route = orderedRoutes[index];
        const selectedTrack = selectedTracks[index];
        const shift = Math.abs(selectedTrack - route.track);
        const routeStartAxis = getAxis2(route.points.at(-1), direction);
        const diagonalEnd = makePoint2(
          routeStartAxis + sign * shift,
          selectedTrack,
          direction
        );
        if (distance9(route.points.at(-1), diagonalEnd) > 1e-9) {
          route.points.push(diagonalEnd);
        }
        const nextPoint = makePoint2(nextAxis, selectedTrack, direction);
        if (distance9(route.points.at(-1), nextPoint) > 1e-9) {
          route.points.push(nextPoint);
        }
        route.track = selectedTrack;
      }
    }
    const finalTrackTargets = getFinalTrackTargets({
      items: directionItems,
      direction,
      boundaryMinimum,
      boundaryMaximum,
      traceWidth,
      clearance,
      borderDistribution,
      currentTrackByConnectionName: new Map(
        [...active.values()].map((route) => [
          route.item.connection.connection.name,
          route.track
        ])
      )
    });
    if (!finalTrackTargets) return null;
    const orderedActiveRoutes = [...active.values()].toSorted(
      (first, second) => first.track - second.track || first.item.connection.connection.name.localeCompare(
        second.item.connection.connection.name
      )
    );
    const distributionStartOffsetByConnectionName = /* @__PURE__ */ new Map();
    const lanePitch = traceWidth + clearance;
    let nextMinimumStartOffset = lanePitch;
    for (const route of orderedActiveRoutes) {
      const connectionName = route.item.connection.connection.name;
      if (!finalTrackTargets.enforcedConnectionNames.has(connectionName)) {
        continue;
      }
      const targetTrack = finalTrackTargets.byConnectionName.get(connectionName);
      const signedShift = targetTrack - route.track;
      if (signedShift >= -1e-9) continue;
      distributionStartOffsetByConnectionName.set(
        connectionName,
        nextMinimumStartOffset
      );
      nextMinimumStartOffset += lanePitch * 2;
    }
    let nextMaximumStartOffset = lanePitch;
    for (let index = orderedActiveRoutes.length - 1; index >= 0; index--) {
      const route = orderedActiveRoutes[index];
      const connectionName = route.item.connection.connection.name;
      if (!finalTrackTargets.enforcedConnectionNames.has(connectionName)) {
        continue;
      }
      const targetTrack = finalTrackTargets.byConnectionName.get(connectionName);
      const signedShift = targetTrack - route.track;
      if (signedShift <= 1e-9) continue;
      distributionStartOffsetByConnectionName.set(
        connectionName,
        nextMaximumStartOffset
      );
      nextMaximumStartOffset += lanePitch * 2;
    }
    for (const route of orderedActiveRoutes) {
      const connectionName = route.item.connection.connection.name;
      const intendedTargetTrack = finalTrackTargets.byConnectionName.get(connectionName) ?? route.track;
      const startOffset = distributionStartOffsetByConnectionName.get(connectionName) ?? 0;
      const previousPoint = route.points.at(-2);
      const availableDepth = previousPoint ? sign * (exitAxis - getAxis2(previousPoint, direction)) : 0;
      const intendedSignedShift = intendedTargetTrack - route.track;
      const maximumRunwayShift = Math.min(
        Math.abs(intendedSignedShift),
        Math.max(0, availableDepth - startOffset)
      );
      const maximumShift = getMaximumObstacleClearDistributionShift({
        route,
        direction,
        exitAxis,
        signedShift: intendedSignedShift,
        maximumShift: maximumRunwayShift,
        obstacles: srj.obstacles,
        requiredObstacleDistance
      });
      const adjustedShift = Math.sign(intendedSignedShift) * maximumShift;
      finalTrackTargets.byConnectionName.set(
        connectionName,
        route.track + adjustedShift
      );
    }
    for (const connectionName of finalTrackTargets.enforcedConnectionNames) {
      enforcedFinalTargets.set(
        connectionName,
        finalTrackTargets.byConnectionName.get(connectionName)
      );
    }
    for (const route of orderedActiveRoutes) {
      const connectionName = route.item.connection.connection.name;
      const targetTrack = finalTrackTargets.byConnectionName.get(connectionName) ?? route.track;
      const shift = Math.abs(targetTrack - route.track);
      const distributionStartOffset = distributionStartOffsetByConnectionName.get(connectionName) ?? 0;
      const distributionDepth = shift > 1e-9 ? distributionStartOffset + shift : 0;
      const distributionStartAxis = exitAxis - sign * distributionDepth;
      const lastPoint = route.points.at(-1);
      if (Math.abs(getAxis2(lastPoint, direction) - exitAxis) > 1e-6) {
        return null;
      }
      const previousPoint = route.points.at(-2);
      if (previousPoint && (Math.abs(
        getPerpendicularAxis2(previousPoint, direction) - route.track
      ) > 1e-6 || sign * (distributionStartAxis - getAxis2(previousPoint, direction)) < -1e-6)) {
        return null;
      }
      const stagingPoint = makePoint2(
        distributionStartAxis,
        route.track,
        direction
      );
      if (previousPoint && distance9(previousPoint, stagingPoint) < 1e-9) {
        route.points.pop();
      } else {
        route.points[route.points.length - 1] = stagingPoint;
      }
    }
    for (const route of orderedActiveRoutes) {
      const connectionName = route.item.connection.connection.name;
      const targetTrack = finalTrackTargets.byConnectionName.get(connectionName) ?? route.track;
      const shift = Math.abs(targetTrack - route.track);
      const distributionStartOffset = distributionStartOffsetByConnectionName.get(connectionName) ?? 0;
      if (shift > 1e-9 && distributionStartOffset > 1e-9) {
        route.points.push(
          makePoint2(
            getAxis2(route.points.at(-1), direction) + sign * distributionStartOffset,
            route.track,
            direction
          )
        );
      }
      if (shift > 1e-9) {
        route.points.push(
          makePoint2(
            getAxis2(route.points.at(-1), direction) + sign * shift,
            targetTrack,
            direction
          )
        );
        route.track = targetTrack;
      }
      const boundaryPoint = makePoint2(exitAxis, route.track, direction);
      if (distance9(route.points.at(-1), boundaryPoint) > 1e-9) {
        if (sign * (exitAxis - getAxis2(route.points.at(-1), direction)) < -1e-6) {
          return null;
        }
        route.points.push(boundaryPoint);
      }
      recordsByConnectionName.set(connectionName, route);
    }
  }
  const acceptedSegments = [...recordsByConnectionName.values()].flatMap(
    (route) => getPathSegments(compressPath2(route.points), traceWidth)
  );
  for (const item of items) {
    if (!item.cornerChannelPrefixes) continue;
    const route = completeCornerChannelRoute({
      item,
      srj,
      acceptedSegments,
      traceWidth,
      clearance
    });
    if (!route) return null;
    recordsByConnectionName.set(item.connection.connection.name, route);
    acceptedSegments.push(
      ...getPathSegments(compressPath2(route.points), traceWidth)
    );
  }
  if (recordsByConnectionName.size !== items.length) return null;
  const maximumTargetError = (traceWidth + clearance) / 2 + 1e-6;
  for (const [connectionName, targetTrack] of enforcedFinalTargets) {
    const route = recordsByConnectionName.get(connectionName);
    if (!route || Math.abs(route.track - targetTrack) > maximumTargetError) {
      return null;
    }
  }
  const paths = items.map((item) => {
    const activeRoute = recordsByConnectionName.get(
      item.connection.connection.name
    );
    const points = compressPath2(activeRoute.points);
    return {
      item,
      points,
      segments: getPathSegments(points, traceWidth)
    };
  });
  if (!routesAreClear({
    paths,
    obstacles: srj.obstacles,
    traceWidth,
    clearance
  })) {
    return null;
  }
  return paths.map(buildPlan3);
}

// node_modules/@tscircuit/fanout-solver/lib/validate-fanout-solution.ts
var EPSILON3 = 1e-6;
function pointsMatch(first, second) {
  return distance9(first, second) <= EPSILON3;
}
function getPointLayers2(point6) {
  return "layer" in point6 ? [point6.layer] : point6.layers;
}
function connectionPointsMatch(first, second) {
  return pointsMatch(first, second) && getPointLayers2(first).join("\0") === getPointLayers2(second).join("\0") && first.pointId === second.pointId && first.pcb_port_id === second.pcb_port_id;
}
function pointIsOnBoundary(point6, boundary) {
  const inside = point6.x >= boundary.minX - EPSILON3 && point6.x <= boundary.maxX + EPSILON3 && point6.y >= boundary.minY - EPSILON3 && point6.y <= boundary.maxY + EPSILON3;
  const onEdge = Math.abs(point6.x - boundary.minX) <= EPSILON3 || Math.abs(point6.x - boundary.maxX) <= EPSILON3 || Math.abs(point6.y - boundary.minY) <= EPSILON3 || Math.abs(point6.y - boundary.maxY) <= EPSILON3;
  return inside && onEdge;
}
function pointIsInsideBounds2(point6, bounds) {
  return point6.x >= bounds.minX - EPSILON3 && point6.x <= bounds.maxX + EPSILON3 && point6.y >= bounds.minY - EPSILON3 && point6.y <= bounds.maxY + EPSILON3;
}
function addIssue(issues, code, message, plan, otherConnectionName) {
  issues.push({
    code,
    message,
    ...plan ? {
      connectionName: plan.connectionName,
      busId: plan.busId
    } : {},
    ...otherConnectionName ? { otherConnectionName } : {}
  });
}
function extractTraceSegments(params) {
  const { trace, plan, issues } = params;
  const segments = [];
  let previousWire;
  let pendingVia;
  for (const routePoint of trace.route) {
    if (routePoint.route_type === "via") {
      if (!previousWire || !pointsMatch(previousWire, routePoint) || previousWire.layer !== routePoint.from_layer) {
        addIssue(
          issues,
          "disconnected-trace",
          `Trace ${trace.pcb_trace_id} reaches a via without a matching ${routePoint.from_layer} wire endpoint`,
          plan
        );
      }
      pendingVia = routePoint;
      continue;
    }
    if (routePoint.route_type !== "wire") {
      addIssue(
        issues,
        "unsupported-route-point",
        `Trace ${trace.pcb_trace_id} contains unsupported ${routePoint.route_type} geometry`,
        plan
      );
      continue;
    }
    if (pendingVia) {
      if (!pointsMatch(routePoint, pendingVia) || routePoint.layer !== pendingVia.to_layer) {
        addIssue(
          issues,
          "disconnected-trace",
          `Trace ${trace.pcb_trace_id} does not continue from its via on ${pendingVia.to_layer}`,
          plan
        );
      }
      previousWire = routePoint;
      pendingVia = void 0;
      continue;
    }
    if (previousWire) {
      if (previousWire.layer !== routePoint.layer) {
        addIssue(
          issues,
          "disconnected-trace",
          `Trace ${trace.pcb_trace_id} changes from ${previousWire.layer} to ${routePoint.layer} without a via`,
          plan
        );
      } else if (!pointsMatch(previousWire, routePoint)) {
        segments.push({
          start: { x: previousWire.x, y: previousWire.y },
          end: { x: routePoint.x, y: routePoint.y },
          width: routePoint.width,
          layer: routePoint.layer
        });
      }
    }
    previousWire = routePoint;
  }
  if (pendingVia) {
    addIssue(
      issues,
      "disconnected-trace",
      `Trace ${trace.pcb_trace_id} ends at a via without a wire on ${pendingVia.to_layer}`,
      plan
    );
  }
  return segments;
}
function validatePlanStructure(params) {
  const { plan, preparedBus, inputSrj, outputSrj, sharedBoundary, issues } = params;
  const inputConnection = inputSrj.connections[plan.connectionIndex];
  if (!inputConnection || inputConnection.name !== plan.connectionName) {
    addIssue(
      issues,
      "connection-mismatch",
      `Plan index ${plan.connectionIndex} does not identify connection ${plan.connectionName}`,
      plan
    );
    return;
  }
  const preparedConnection = preparedBus?.connections.find(
    (connection) => connection.connectionIndex === plan.connectionIndex
  );
  if (!preparedConnection || preparedConnection.sourcePointIndex !== plan.sourcePointIndex || !connectionPointsMatch(preparedConnection.sourcePoint, plan.sourcePoint) || preparedConnection.sourceObstacle.obstacleId !== plan.sourceObstacle.obstacleId) {
    addIssue(
      issues,
      "source-mismatch",
      `Plan ${plan.connectionName} does not start at its prepared component endpoint`,
      plan
    );
  }
  if (preparedBus?.termination.type !== plan.termination.type) {
    addIssue(
      issues,
      "termination-mismatch",
      `Plan ${plan.connectionName} does not use its bus termination`,
      plan
    );
  }
  if (plan.segments.length === 0 || plan.length <= EPSILON3) {
    addIssue(
      issues,
      "not-broken-out",
      `Plan ${plan.connectionName} has no non-zero escape geometry`,
      plan
    );
  } else {
    const routableBounds = {
      minX: Math.min(inputSrj.bounds.minX, sharedBoundary.minX),
      maxX: Math.max(inputSrj.bounds.maxX, sharedBoundary.maxX),
      minY: Math.min(inputSrj.bounds.minY, sharedBoundary.minY),
      maxY: Math.max(inputSrj.bounds.maxY, sharedBoundary.maxY)
    };
    if (plan.segments.some(
      (segment2) => !pointIsInsideBounds2(segment2.start, routableBounds) || !pointIsInsideBounds2(segment2.end, routableBounds)
    )) {
      addIssue(
        issues,
        "outside-routing-bounds",
        `Plan ${plan.connectionName} leaves the routable SRJ/shared-boundary area`,
        plan
      );
    }
    if (!pointsMatch(plan.segments[0].start, plan.sourcePoint)) {
      addIssue(
        issues,
        "disconnected-trace",
        `Plan ${plan.connectionName} does not start at its source pad`,
        plan
      );
    }
    if (!pointsMatch(plan.segments.at(-1).end, plan.exitPoint)) {
      addIssue(
        issues,
        "disconnected-trace",
        `Plan ${plan.connectionName} does not end at its declared exit`,
        plan
      );
    }
    for (let index = 1; index < plan.segments.length; index++) {
      const previous = plan.segments[index - 1];
      const current = plan.segments[index];
      if (!pointsMatch(previous.end, current.start)) {
        addIssue(
          issues,
          "disconnected-trace",
          `Plan ${plan.connectionName} has a gap between route segments`,
          plan
        );
      }
      if (previous.layer !== current.layer && (!plan.via || !pointsMatch(previous.end, plan.via.center) || !plan.via.spanLayers.includes(previous.layer) || !plan.via.spanLayers.includes(current.layer))) {
        addIssue(
          issues,
          "disconnected-trace",
          `Plan ${plan.connectionName} changes layers without a connecting via`,
          plan
        );
      }
    }
  }
  const traceSegments = extractTraceSegments({
    trace: plan.trace,
    plan,
    issues
  });
  if (traceSegments.length !== plan.segments.length || traceSegments.some((segment2, index) => {
    const declared = plan.segments[index];
    return !declared || segment2.layer !== declared.layer || Math.abs(segment2.width - declared.width) > EPSILON3 || !pointsMatch(segment2.start, declared.start) || !pointsMatch(segment2.end, declared.end);
  })) {
    addIssue(
      issues,
      "trace-plan-mismatch",
      `Trace ${plan.trace.pcb_trace_id} does not encode its declared route segments`,
      plan
    );
  }
  const firstRoutePoint = plan.trace.route.find(
    (routePoint) => "x" in routePoint && "y" in routePoint
  );
  const lastRoutePoint = [...plan.trace.route].reverse().find(
    (routePoint) => "x" in routePoint && "y" in routePoint
  );
  if (!firstRoutePoint || !lastRoutePoint || !pointsMatch(firstRoutePoint, plan.sourcePoint) || !pointsMatch(lastRoutePoint, plan.exitPoint)) {
    addIssue(
      issues,
      "disconnected-trace",
      `Trace ${plan.trace.pcb_trace_id} does not span its source and exit`,
      plan
    );
  }
  const outputConnection = outputSrj.connections.find(
    (connection) => connection.name === plan.connectionName
  );
  if (plan.termination.type === "boundary") {
    if (!outputConnection) {
      addIssue(
        issues,
        "output-connection-missing",
        `Boundary connection ${plan.connectionName} was removed from the output`,
        plan
      );
    } else {
      const outputSource = outputConnection.pointsToConnect[plan.sourcePointIndex];
      if (!outputSource || !pointsMatch(outputSource, plan.exitPoint) || !("layer" in outputSource) || outputSource.layer !== plan.targetLayer) {
        addIssue(
          issues,
          "output-exit-mismatch",
          `Output connection ${plan.connectionName} is not attached to its fanout exit`,
          plan
        );
      }
      for (let index = 0; index < inputConnection.pointsToConnect.length; index++) {
        if (index === plan.sourcePointIndex) continue;
        const inputPoint = inputConnection.pointsToConnect[index];
        const outputPoint = outputConnection.pointsToConnect[index];
        if (!inputPoint || !outputPoint || !connectionPointsMatch(inputPoint, outputPoint)) {
          addIssue(
            issues,
            "downstream-endpoint-lost",
            `Output connection ${plan.connectionName} did not retain downstream endpoint ${index}`,
            plan
          );
        }
      }
    }
  } else if (outputConnection) {
    addIssue(
      issues,
      "plane-connection-retained",
      `Plane-terminated connection ${plan.connectionName} remains in the output`,
      plan
    );
  }
}
function plansHaveConnectedCopper(first, second) {
  for (const firstSegment of first.segments) {
    for (const secondSegment of second.segments) {
      if (firstSegment.layer === secondSegment.layer && distanceSegmentToSegment(
        firstSegment.start,
        firstSegment.end,
        secondSegment.start,
        secondSegment.end
      ) <= (firstSegment.width + secondSegment.width) / 2 + EPSILON3) {
        return true;
      }
    }
    if (second.via?.spanLayers.includes(firstSegment.layer) && distancePointToSegment(
      second.via.center,
      firstSegment.start,
      firstSegment.end
    ) <= second.via.diameter / 2 + firstSegment.width / 2 + EPSILON3) {
      return true;
    }
  }
  if (first.via) {
    for (const secondSegment of second.segments) {
      if (first.via.spanLayers.includes(secondSegment.layer) && distancePointToSegment(
        first.via.center,
        secondSegment.start,
        secondSegment.end
      ) <= first.via.diameter / 2 + secondSegment.width / 2 + EPSILON3) {
        return true;
      }
    }
    if (second.via && first.via.spanLayers.some(
      (layer) => second.via.spanLayers.includes(layer)
    ) && distance9(first.via.center, second.via.center) <= (first.via.diameter + second.via.diameter) / 2 + EPSILON3) {
      return true;
    }
  }
  return false;
}
function validateBreakoutConnectivity(params) {
  const { plans, inputSrj, sharedBoundary, issues } = params;
  const connectedPlans = /* @__PURE__ */ new Set();
  const neighboringPlans = /* @__PURE__ */ new Map();
  for (const plan of plans) neighboringPlans.set(plan, []);
  for (let firstIndex = 0; firstIndex < plans.length; firstIndex++) {
    const first = plans[firstIndex];
    if (first.termination.type === "plane" ? Boolean(first.via) : first.segments.some(
      (segment2) => pointIsOnBoundary(segment2.start, sharedBoundary) || pointIsOnBoundary(segment2.end, sharedBoundary)
    )) {
      connectedPlans.add(first);
    }
    for (let secondIndex = firstIndex + 1; secondIndex < plans.length; secondIndex++) {
      const second = plans[secondIndex];
      if (!connectionsShareElectricalNet(
        inputSrj,
        first.connectionName,
        second.connectionName
      ) || !plansHaveConnectedCopper(first, second)) {
        continue;
      }
      neighboringPlans.get(first).push(second);
      neighboringPlans.get(second).push(first);
    }
  }
  const queue = [...connectedPlans];
  while (queue.length > 0) {
    const plan = queue.shift();
    for (const neighbor of neighboringPlans.get(plan) ?? []) {
      if (connectedPlans.has(neighbor)) continue;
      connectedPlans.add(neighbor);
      queue.push(neighbor);
    }
  }
  for (const plan of plans) {
    if (connectedPlans.has(plan)) continue;
    addIssue(
      issues,
      "not-broken-out",
      plan.termination.type === "boundary" ? `Connection ${plan.connectionName} has no continuous same-net copper path to the shared boundary` : `Plane connection ${plan.connectionName} has no terminating via`,
      plan
    );
  }
  return connectedPlans;
}
function validateClearances(params) {
  const { plans, inputSrj, clearance, issues } = params;
  for (const plan of plans) {
    for (let segmentIndex = 0; segmentIndex < plan.segments.length; segmentIndex++) {
      const segment2 = plan.segments[segmentIndex];
      for (const obstacle of inputSrj.obstacles) {
        if (!obstacle.layers.includes(segment2.layer)) continue;
        if (obstacleSharesElectricalNet(inputSrj, obstacle, plan.connectionName)) {
          continue;
        }
        if (segmentIndex === 0 && obstacle.obstacleId === plan.sourceObstacle.obstacleId && segment2.layer === plan.sourceLayer) {
          continue;
        }
        const actual = distanceSegmentToObstacle(segment2, obstacle);
        const required = segment2.width / 2 + clearance;
        if (actual < required - 1e-9) {
          addIssue(
            issues,
            "obstacle-clearance",
            `Trace ${plan.connectionName} on ${segment2.layer} is ${actual.toFixed(4)}mm from different-net obstacle ${obstacle.obstacleId}; ${required.toFixed(4)}mm is required`,
            plan
          );
        }
      }
    }
    if (plan.via) {
      for (const obstacle of inputSrj.obstacles) {
        if (!obstacle.layers.some(
          (layer) => plan.via.spanLayers.includes(layer)
        ) || obstacleSharesElectricalNet(inputSrj, obstacle, plan.connectionName)) {
          continue;
        }
        const actual = distancePointToObstacle(plan.via.center, obstacle);
        const required = plan.via.diameter / 2 + clearance;
        if (actual < required - 1e-9) {
          addIssue(
            issues,
            "via-obstacle-clearance",
            `Via ${plan.connectionName} is ${actual.toFixed(4)}mm from different-net obstacle ${obstacle.obstacleId} on its layer span; ${required.toFixed(4)}mm is required`,
            plan
          );
        }
      }
    }
  }
  for (let firstIndex = 0; firstIndex < plans.length; firstIndex++) {
    const first = plans[firstIndex];
    for (let secondIndex = firstIndex + 1; secondIndex < plans.length; secondIndex++) {
      const second = plans[secondIndex];
      if (connectionsShareElectricalNet(
        inputSrj,
        first.connectionName,
        second.connectionName
      )) {
        continue;
      }
      for (const firstSegment of first.segments) {
        for (const secondSegment of second.segments) {
          if (!segmentsAreClear(firstSegment, secondSegment, clearance)) {
            addIssue(
              issues,
              "different-net-trace-clearance",
              `Different-net traces ${first.connectionName} and ${second.connectionName} intersect or violate clearance on ${firstSegment.layer}`,
              first,
              second.connectionName
            );
          }
        }
        if (second.via?.spanLayers.includes(firstSegment.layer) && distancePointToSegment(
          second.via.center,
          firstSegment.start,
          firstSegment.end
        ) < second.via.diameter / 2 + firstSegment.width / 2 + clearance - 1e-9) {
          addIssue(
            issues,
            "different-net-trace-via-clearance",
            `Trace ${first.connectionName} violates via clearance to ${second.connectionName} on ${firstSegment.layer}`,
            first,
            second.connectionName
          );
        }
      }
      if (first.via) {
        for (const secondSegment of second.segments) {
          if (first.via.spanLayers.includes(secondSegment.layer) && distancePointToSegment(
            first.via.center,
            secondSegment.start,
            secondSegment.end
          ) < first.via.diameter / 2 + secondSegment.width / 2 + clearance - 1e-9) {
            addIssue(
              issues,
              "different-net-trace-via-clearance",
              `Via ${first.connectionName} violates trace clearance to ${second.connectionName} on ${secondSegment.layer}`,
              first,
              second.connectionName
            );
          }
        }
        if (second.via && first.via.spanLayers.some(
          (layer) => second.via.spanLayers.includes(layer)
        ) && distance9(first.via.center, second.via.center) < (first.via.diameter + second.via.diameter) / 2 + clearance - 1e-9) {
          addIssue(
            issues,
            "different-net-via-clearance",
            `Vias ${first.connectionName} and ${second.connectionName} violate clearance on an overlapping layer span`,
            first,
            second.connectionName
          );
        }
      }
    }
  }
}
function validateFanoutSolution(params) {
  const {
    inputSrj,
    outputSrj,
    plans,
    preparedBuses,
    sharedBoundary,
    clearance
  } = params;
  const issues = [];
  const plansByConnection = /* @__PURE__ */ new Map();
  const preparedBusById = new Map(preparedBuses.map((bus) => [bus.busId, bus]));
  for (const plan of plans) {
    const connectionPlans = plansByConnection.get(plan.connectionName) ?? [];
    connectionPlans.push(plan);
    plansByConnection.set(plan.connectionName, connectionPlans);
  }
  for (const connection of inputSrj.connections) {
    const connectionPlans = plansByConnection.get(connection.name) ?? [];
    if (connectionPlans.length === 0) {
      addIssue(
        issues,
        "missing-plan",
        `Connection ${connection.name} has no fanout plan`
      );
    } else if (connectionPlans.length > 1) {
      addIssue(
        issues,
        "duplicate-plan",
        `Connection ${connection.name} has ${connectionPlans.length} fanout plans`,
        connectionPlans[0]
      );
    }
  }
  for (const plan of plans) {
    if (!inputSrj.connections.some(
      (connection) => connection.name === plan.connectionName
    )) {
      addIssue(
        issues,
        "unknown-plan",
        `Plan ${plan.connectionName} is not an input connection`,
        plan
      );
      continue;
    }
    validatePlanStructure({
      plan,
      preparedBus: preparedBusById.get(plan.busId),
      inputSrj,
      outputSrj,
      sharedBoundary,
      issues
    });
  }
  const connectedPlans = validateBreakoutConnectivity({
    plans,
    inputSrj,
    sharedBoundary,
    issues
  });
  validateClearances({ plans, inputSrj, clearance, issues });
  return {
    valid: issues.length === 0,
    checkedConnectionCount: inputSrj.connections.length,
    brokenOutConnectionCount: new Set(
      [...connectedPlans].map((plan) => plan.connectionName)
    ).size,
    issues
  };
}

// node_modules/@tscircuit/fanout-solver/lib/fanout-solver.ts
function resolvePositiveNumber(label, value) {
  if (!Number.isFinite(value) || value <= 0) {
    throw new Error(
      `FanoutSolver: ${label} must be a positive number, received ${value}`
    );
  }
  return value;
}
function resolveConfig(srj, options) {
  const traceWidth = resolvePositiveNumber(
    "traceWidth",
    options.traceWidth ?? srj.nominalTraceWidth ?? srj.minTraceWidth
  );
  const viaDiameter = resolvePositiveNumber(
    "viaDiameter",
    options.viaDiameter ?? srj.minViaPadDiameter ?? srj.min_via_pad_diameter ?? srj.minViaDiameter ?? Math.max(traceWidth * 2, 0.3)
  );
  const viaHoleDiameter = resolvePositiveNumber(
    "viaHoleDiameter",
    options.viaHoleDiameter ?? srj.minViaHoleDiameter ?? srj.min_via_hole_diameter ?? viaDiameter * 0.5
  );
  if (viaHoleDiameter >= viaDiameter) {
    throw new Error(
      `FanoutSolver: viaHoleDiameter ${viaHoleDiameter} must be smaller than viaDiameter ${viaDiameter}`
    );
  }
  const clearance = resolvePositiveNumber(
    "clearance",
    options.clearance ?? srj.minViaEdgeToPadEdgeClearance ?? srj.minTraceToPadEdgeClearance ?? srj.defaultObstacleMargin ?? srj.minTraceWidth
  );
  const layerNames = getCopperLayerNames(srj.layerCount);
  const escapeLayers = options.escapeLayers ?? layerNames;
  for (const layer of escapeLayers) {
    if (!layerNames.includes(layer)) {
      throw new Error(
        `FanoutSolver: escape layer "${layer}" is not available in a ${srj.layerCount}-layer SimpleRouteJson`
      );
    }
  }
  if (new Set(escapeLayers).size !== escapeLayers.length) {
    throw new Error("FanoutSolver: escapeLayers contains duplicates");
  }
  const borderDistribution = options.borderDistribution ?? "preserve";
  if (borderDistribution !== "preserve" && borderDistribution !== "even") {
    throw new Error(
      `FanoutSolver: borderDistribution must be "preserve" or "even", received "${borderDistribution}"`
    );
  }
  return {
    traceWidth,
    viaDiameter,
    viaHoleDiameter,
    clearance,
    compactBusTracks: options.compactBusTracks ?? false,
    allowSameNetMerges: options.allowSameNetMerges ?? false,
    singleLayerPushAndShove: options.singleLayerPushAndShove ?? false,
    singleLayerAdaptiveExits: options.singleLayerAdaptiveExits ?? false,
    borderDistribution,
    layerNames,
    escapeLayers,
    maxLayerCombinations: options.maxLayerCombinations === void 0 ? 256 : resolvePositiveNumber(
      "maxLayerCombinations",
      options.maxLayerCombinations
    )
  };
}
function assignmentLoadPenalty(assignment) {
  const loadByLayer = /* @__PURE__ */ new Map();
  for (const layer of Object.values(assignment)) {
    loadByLayer.set(layer, (loadByLayer.get(layer) ?? 0) + 1);
  }
  return [...loadByLayer.values()].reduce(
    (penalty, load) => penalty + load * load,
    0
  );
}
function getBusDistanceToBoundary(bus) {
  const averageSource = bus.connections.reduce((sum, connection) => {
    const sourceAxis = bus.direction === "left" || bus.direction === "right" ? connection.sourcePoint.x : connection.sourcePoint.y;
    return sum + sourceAxis;
  }, 0) / bus.connections.length;
  switch (bus.direction) {
    case "right":
      return bus.sharedBoundary.maxX - averageSource;
    case "left":
      return averageSource - bus.sharedBoundary.minX;
    case "up":
      return bus.sharedBoundary.maxY - averageSource;
    case "down":
      return averageSource - bus.sharedBoundary.minY;
  }
}
function busIsOnOutwardComponentEdge2(bus) {
  const isHorizontal3 = bus.direction === "left" || bus.direction === "right";
  const directionalCoordinates = isHorizontal3 ? bus.xCoordinates : bus.yCoordinates;
  const averageSource = bus.connections.reduce(
    (sum, connection) => sum + (isHorizontal3 ? connection.sourcePoint.x : connection.sourcePoint.y),
    0
  ) / bus.connections.length;
  const outwardCoordinate = bus.direction === "right" || bus.direction === "up" ? Math.max(...directionalCoordinates) : Math.min(...directionalCoordinates);
  return Math.abs(averageSource - outwardCoordinate) < 1e-6;
}
function getBusDepthInRows(bus) {
  const isHorizontal3 = bus.direction === "left" || bus.direction === "right";
  const directionalCoordinates = isHorizontal3 ? bus.xCoordinates : bus.yCoordinates;
  const averageSource = bus.connections.reduce(
    (sum, connection) => sum + (isHorizontal3 ? connection.sourcePoint.x : connection.sourcePoint.y),
    0
  ) / bus.connections.length;
  const outwardCoordinate = bus.direction === "right" || bus.direction === "up" ? Math.max(...directionalCoordinates) : Math.min(...directionalCoordinates);
  const directionalPitch = isHorizontal3 ? bus.pitchX : bus.pitchY;
  return Math.round(
    Math.abs(averageSource - outwardCoordinate) / directionalPitch
  );
}
function createPreferredLayerAssignment(params) {
  const { buses, escapeLayers, escapeLayersByBusId } = params;
  const assignment = {};
  const directionsByComponent = /* @__PURE__ */ new Map();
  let nextViaLayerIndex = 0;
  for (const bus of buses) {
    const directions = directionsByComponent.get(bus.componentId) ?? /* @__PURE__ */ new Set();
    directions.add(bus.direction);
    directionsByComponent.set(bus.componentId, directions);
  }
  for (const bus of buses) {
    const sourceLayer = bus.connections[0]?.sourceLayer;
    if (!sourceLayer) {
      throw new Error(`FanoutSolver: bus "${bus.busId}" has no connections`);
    }
    if (bus.termination.type === "plane") {
      assignment[bus.busId] = bus.termination.layer;
      continue;
    }
    const routableEscapeLayers = escapeLayersByBusId[bus.busId] ?? escapeLayers;
    const viaLayers = routableEscapeLayers.filter(
      (layer) => layer !== sourceLayer
    );
    if (routableEscapeLayers.includes(sourceLayer) && busIsOnOutwardComponentEdge2(bus)) {
      assignment[bus.busId] = sourceLayer;
    } else if (viaLayers.length > 0) {
      const componentDirections = directionsByComponent.get(bus.componentId);
      const hasOpposingDirection = componentDirections.has("left") && componentDirections.has("right") || componentDirections.has("up") && componentDirections.has("down");
      if (hasOpposingDirection) {
        const depthInRows = getBusDepthInRows(bus);
        assignment[bus.busId] = viaLayers[Math.max(depthInRows - 1, 0) % viaLayers.length];
      } else {
        assignment[bus.busId] = viaLayers[nextViaLayerIndex % viaLayers.length];
        nextViaLayerIndex++;
      }
    } else {
      assignment[bus.busId] = sourceLayer;
    }
  }
  return assignment;
}
function prioritizeLayerAssignment(params) {
  const { preferredAssignment, generatedAssignments, maxAssignments } = params;
  const preferredKey = JSON.stringify(preferredAssignment);
  return [
    preferredAssignment,
    ...generatedAssignments.filter(
      (assignment) => JSON.stringify(assignment) !== preferredKey
    )
  ].slice(0, maxAssignments);
}
function getCandidateEscapeLayersForBus(params) {
  const { bus, srj, config, staticClearanceCache } = params;
  const individuallyRoutableLayers = config.escapeLayers.filter(
    (targetLayer) => routeBus({
      srj,
      bus,
      targetLayer,
      acceptedPlans: [],
      layerNames: config.layerNames,
      traceWidth: config.traceWidth,
      viaDiameter: config.viaDiameter,
      viaHoleDiameter: config.viaHoleDiameter,
      clearance: config.clearance,
      compactBusTracks: config.compactBusTracks,
      allowSameNetMerges: config.allowSameNetMerges,
      staticClearanceCache
    }) !== null
  );
  return individuallyRoutableLayers.length > 0 ? individuallyRoutableLayers : config.escapeLayers;
}
var FanoutSolver = class extends BaseSolver {
  constructor(inputSrj, options = {}) {
    super();
    this.inputSrj = inputSrj;
    this.options = options;
    this.config = resolveConfig(inputSrj, options);
    this.preparedBuses = prepareFanoutBuses(inputSrj, options);
    for (const bus of this.preparedBuses) {
      if (bus.termination.type !== "plane") continue;
      const planeLayer = bus.termination.layer;
      if (!this.config.layerNames.includes(planeLayer)) {
        throw new Error(
          `FanoutSolver: plane-terminated bus "${bus.busId}" targets unavailable layer "${planeLayer}"`
        );
      }
      if (bus.connections.some(
        (connection) => connection.sourceLayer === planeLayer
      )) {
        throw new Error(
          `FanoutSolver: plane-terminated bus "${bus.busId}" must target a layer below its source pad`
        );
      }
    }
    const boundaryBusIds = this.preparedBuses.filter((bus) => bus.termination.type === "boundary").map((bus) => bus.busId);
    const fixedPlaneAssignments = Object.fromEntries(
      this.preparedBuses.flatMap(
        (bus) => bus.termination.type === "plane" ? [[bus.busId, bus.termination.layer]] : []
      )
    );
    const escapeLayersByBusId = Object.fromEntries(
      this.preparedBuses.flatMap((bus) => {
        if (bus.termination.type === "plane") return [];
        return [
          [
            bus.busId,
            getCandidateEscapeLayersForBus({
              bus,
              srj: inputSrj,
              config: this.config,
              staticClearanceCache: this.routeStaticClearanceCache
            })
          ]
        ];
      })
    );
    this.escapeLayersByBusId = escapeLayersByBusId;
    const generatedAssignments = generateLayerAssignments({
      busIds: boundaryBusIds,
      layers: this.config.escapeLayers,
      layersByBusId: escapeLayersByBusId,
      maxAssignments: this.config.maxLayerCombinations
    }).map((assignment) => ({
      ...assignment,
      ...fixedPlaneAssignments
    }));
    this.layerAssignments = prioritizeLayerAssignment({
      preferredAssignment: createPreferredLayerAssignment({
        buses: this.preparedBuses,
        escapeLayers: this.config.escapeLayers,
        escapeLayersByBusId
      }),
      generatedAssignments,
      maxAssignments: this.config.maxLayerCombinations
    });
    this.MAX_ITERATIONS = this.config.maxLayerCombinations + 2;
  }
  inputSrj;
  options;
  preparedBuses;
  attempts = [];
  layerAssignments;
  config;
  escapeLayersByBusId;
  evaluatedAssignmentKeys = /* @__PURE__ */ new Set();
  queuedAssignmentKeys = /* @__PURE__ */ new Set();
  assignmentRepairDepthByKey = /* @__PURE__ */ new Map();
  pendingRepairAssignments = [];
  routeStaticClearanceCache = /* @__PURE__ */ new Map();
  routingPrefixCache = /* @__PURE__ */ new Map();
  groupedBeamEvaluated = false;
  nextAssignmentIndex = 0;
  nextGeneratedAssignmentIndex = 0;
  bestAttempt = null;
  getSolverName() {
    return "FanoutSolver";
  }
  getValidationBoundary() {
    if (this.options.sharedBoundary) return this.options.sharedBoundary;
    const firstBoundary = this.preparedBuses[0]?.sharedBoundary;
    if (!firstBoundary) return this.inputSrj.bounds;
    return this.preparedBuses.slice(1).reduce(
      (boundary, bus) => ({
        minX: Math.min(boundary.minX, bus.sharedBoundary.minX),
        maxX: Math.max(boundary.maxX, bus.sharedBoundary.maxX),
        minY: Math.min(boundary.minY, bus.sharedBoundary.minY),
        maxY: Math.max(boundary.maxY, bus.sharedBoundary.maxY)
      }),
      { ...firstBoundary }
    );
  }
  validateCompletePlans(plans, outputSrj) {
    return validateFanoutSolution({
      inputSrj: this.inputSrj,
      outputSrj,
      plans,
      preparedBuses: this.preparedBuses,
      sharedBoundary: this.getValidationBoundary(),
      clearance: this.config.clearance
    });
  }
  evaluateAssignmentWithStrategy(assignmentIndex, busLayerAssignments, routingStrategy) {
    let plans = [];
    let failedBusIds = [];
    let blockingBusCounts = /* @__PURE__ */ new Map();
    const isSingleLayerFanout = this.config.escapeLayers.length === 1;
    if (isSingleLayerFanout && this.config.singleLayerPushAndShove) {
      const singleLayerParams = {
        srj: this.inputSrj,
        buses: this.preparedBuses,
        traceWidth: this.config.traceWidth,
        clearance: this.config.clearance,
        borderDistribution: this.config.borderDistribution
      };
      const singleLayerPlans = routeSingleLayerWithPushAndShove(singleLayerParams) ?? (this.config.singleLayerAdaptiveExits ? routeSingleLayerWithAdaptiveExits({
        ...singleLayerParams,
        availableBoundaryRegions: resolveAvailableBoundaryRegions(
          this.options.availableCornersAndSides
        )
      }) : null);
      if (singleLayerPlans) {
        plans.push(...singleLayerPlans);
      } else {
        failedBusIds.push(...this.preparedBuses.map((bus) => bus.busId));
      }
    }
    const busesInRoutingOrder = [...this.preparedBuses].sort(
      (a, b) => Number(a.termination.type === "plane") - Number(b.termination.type === "plane") || (routingStrategy === "group-by-layer" ? (busLayerAssignments[a.busId] ?? "").localeCompare(
        busLayerAssignments[b.busId] ?? ""
      ) : 0) || b.componentObstacles.length - a.componentObstacles.length || (isSingleLayerFanout ? getBusDistanceToBoundary(b) - getBusDistanceToBoundary(a) : b.connections.length - a.connections.length || (routingStrategy === "deep-first" ? getBusDistanceToBoundary(b) - getBusDistanceToBoundary(a) : getBusDistanceToBoundary(a) - getBusDistanceToBoundary(b)))
    );
    let routingPrefixKey = `${routingStrategy}|`;
    for (const bus of isSingleLayerFanout && this.config.singleLayerPushAndShove ? [] : busesInRoutingOrder) {
      const targetLayer = busLayerAssignments[bus.busId];
      if (!targetLayer) {
        throw new Error(
          `FanoutSolver: assignment ${assignmentIndex} has no layer for bus "${bus.busId}"`
        );
      }
      routingPrefixKey += `${bus.busId.length}:${bus.busId};${targetLayer.length}:${targetLayer};`;
      const cachedPrefix = this.routingPrefixCache.get(routingPrefixKey);
      if (cachedPrefix) {
        plans = [...cachedPrefix.plans];
        failedBusIds = [...cachedPrefix.failedBusIds];
        blockingBusCounts = new Map(cachedPrefix.blockingBusCounts);
        continue;
      }
      const currentBusBlockingCounts = /* @__PURE__ */ new Map();
      const busPlans = routeBus({
        srj: this.inputSrj,
        bus,
        targetLayer,
        acceptedPlans: plans,
        layerNames: this.config.layerNames,
        traceWidth: this.config.traceWidth,
        viaDiameter: this.config.viaDiameter,
        viaHoleDiameter: this.config.viaHoleDiameter,
        clearance: this.config.clearance,
        compactBusTracks: this.config.compactBusTracks,
        allowSameNetMerges: this.config.allowSameNetMerges,
        staticClearanceCache: this.routeStaticClearanceCache,
        blockingBusCounts: currentBusBlockingCounts
      });
      if (!busPlans) {
        failedBusIds.push(bus.busId);
        for (const [blockingBusId, count] of currentBusBlockingCounts) {
          blockingBusCounts.set(
            blockingBusId,
            (blockingBusCounts.get(blockingBusId) ?? 0) + count
          );
        }
      } else {
        plans.push(...busPlans);
      }
      this.routingPrefixCache.set(routingPrefixKey, {
        plans: [...plans],
        failedBusIds: [...failedBusIds],
        blockingBusCounts: new Map(blockingBusCounts)
      });
    }
    let validationIssues;
    let outputSrj = buildOutputSimpleRouteJson({
      inputSrj: this.inputSrj,
      plans,
      layerNames: this.config.layerNames
    });
    const validation = plans.length === this.inputSrj.connections.length ? this.validateCompletePlans(plans, outputSrj) : null;
    if (validation && !validation.valid) {
      validationIssues = validation.issues;
      plans = [];
      failedBusIds = this.preparedBuses.map((bus) => bus.busId);
      blockingBusCounts.clear();
      outputSrj = buildOutputSimpleRouteJson({
        inputSrj: this.inputSrj,
        plans,
        layerNames: this.config.layerNames
      });
    }
    const routedBusCount = this.preparedBuses.length - failedBusIds.length;
    const routeLength = plans.reduce((total, plan) => total + plan.length, 0);
    const unroutedConnectionCount = this.inputSrj.connections.length - plans.length;
    const score = unroutedConnectionCount * 1e6 + failedBusIds.length * 1e5 + routeLength + plans.filter((plan) => plan.via).length * 0.1 + assignmentLoadPenalty(busLayerAssignments) * 0.01;
    const summary = {
      assignmentIndex,
      busLayerAssignments,
      routedBusCount,
      routedConnectionCount: plans.length,
      failedBusIds,
      score,
      ...validationIssues ? { validationIssues } : {}
    };
    return {
      summary,
      plans,
      blockingBusIds: [...blockingBusCounts.entries()].toSorted(([, firstCount], [, secondCount]) => secondCount - firstCount).map(([busId]) => busId),
      outputSrj
    };
  }
  evaluateAssignment(assignmentIndex, busLayerAssignments) {
    let bestAttempt = this.evaluateAssignmentWithStrategy(
      assignmentIndex,
      busLayerAssignments,
      "default"
    );
    if (bestAttempt.summary.routedConnectionCount === this.inputSrj.connections.length) {
      return bestAttempt;
    }
    for (const routingStrategy of ["group-by-layer", "deep-first"]) {
      const attempt = this.evaluateAssignmentWithStrategy(
        assignmentIndex,
        busLayerAssignments,
        routingStrategy
      );
      if (attempt.summary.score < bestAttempt.summary.score) {
        bestAttempt = attempt;
      }
      if (bestAttempt.summary.routedConnectionCount === this.inputSrj.connections.length) {
        return bestAttempt;
      }
    }
    return bestAttempt;
  }
  /**
   * Search layer assignments and track alternatives together. The regular
   * assignment loop commits to one route per bus before the next bus is
   * considered, so a locally-valid track can still starve a later bus. A
   * bounded beam keeps several grouped-layer route prefixes alive. It also
   * evaluates multi-connection buses atomically, so one promising route for a
   * power or signal lane cannot starve a later bus before the solver explores
   * an alternate layer/track combination.
   */
  evaluateGroupedBeam(assignmentIndex, groupByDirection = false) {
    if (this.config.escapeLayers.length < 2) return null;
    if (this.preparedBuses.length > 56) return null;
    const totalConnections = this.inputSrj.connections.length;
    if (totalConnections > 64) return null;
    if (new Set(this.preparedBuses.map((bus) => bus.componentId)).size !== 1) {
      return null;
    }
    const busesInSearchOrder = [...this.preparedBuses].sort((a, b) => {
      const aLayerCount = a.termination.type === "plane" ? 1 : this.escapeLayersByBusId[a.busId]?.length ?? this.config.escapeLayers.length;
      const bLayerCount = b.termination.type === "plane" ? 1 : this.escapeLayersByBusId[b.busId]?.length ?? this.config.escapeLayers.length;
      return Number(a.termination.type === "plane") - Number(b.termination.type === "plane") || (groupByDirection ? a.direction.localeCompare(b.direction) : 0) || aLayerCount - bLayerCount || b.componentObstacles.length - a.componentObstacles.length || b.connections.length - a.connections.length || getBusDepthInRows(b) - getBusDepthInRows(a) || a.busId.localeCompare(b.busId);
    });
    const isSmallProblem = totalConnections <= 24;
    const hasMultiConnectionBus = this.preparedBuses.some(
      (bus) => bus.connections.length > 1
    );
    const beamWidth = isSmallProblem ? 48 : totalConnections <= 32 ? 24 : 12;
    const alternativesPerLayer = isSmallProblem && !hasMultiConnectionBus ? 4 : 1;
    let states = [{ assignment: {}, plans: [] }];
    const getStateScore = (state) => {
      const routeLength = state.plans.reduce(
        (total, plan) => total + plan.length,
        0
      );
      const viaCount = state.plans.filter((plan) => plan.via).length;
      return routeLength + viaCount * 0.1 + assignmentLoadPenalty(state.assignment) * 0.01;
    };
    for (const bus of busesInSearchOrder) {
      const nextStates = [];
      for (const state of states) {
        const candidateLayers = bus.termination.type === "plane" ? [bus.termination.layer] : this.escapeLayersByBusId[bus.busId] ?? this.config.escapeLayers;
        const layerLoads = /* @__PURE__ */ new Map();
        for (const layer of Object.values(state.assignment)) {
          layerLoads.set(layer, (layerLoads.get(layer) ?? 0) + 1);
        }
        const sourceLayer = bus.connections[0]?.sourceLayer;
        const orderedLayers = candidateLayers.toSorted(
          (first, second) => (layerLoads.get(first) ?? 0) - (layerLoads.get(second) ?? 0) || Number(first === sourceLayer) - Number(second === sourceLayer) || first.localeCompare(second)
        );
        for (const targetLayer of orderedLayers) {
          const busAlternatives = routeBusAlternatives(
            {
              srj: this.inputSrj,
              bus,
              targetLayer,
              acceptedPlans: state.plans,
              layerNames: this.config.layerNames,
              traceWidth: this.config.traceWidth,
              viaDiameter: this.config.viaDiameter,
              viaHoleDiameter: this.config.viaHoleDiameter,
              clearance: this.config.clearance,
              compactBusTracks: this.config.compactBusTracks,
              allowSameNetMerges: this.config.allowSameNetMerges,
              staticClearanceCache: this.routeStaticClearanceCache
            },
            alternativesPerLayer
          );
          for (const busPlans of busAlternatives) {
            nextStates.push({
              assignment: {
                ...state.assignment,
                [bus.busId]: targetLayer
              },
              plans: [...state.plans, ...busPlans]
            });
          }
        }
      }
      if (nextStates.length === 0) return null;
      nextStates.sort((first, second) => {
        const scoreDifference = getStateScore(first) - getStateScore(second);
        if (Math.abs(scoreDifference) > 1e-9) return scoreDifference;
        return JSON.stringify(first.assignment).localeCompare(
          JSON.stringify(second.assignment)
        );
      });
      const statesByAssignment = /* @__PURE__ */ new Map();
      states = [];
      for (const state of nextStates) {
        const key = JSON.stringify(state.assignment);
        const sameAssignmentCount = statesByAssignment.get(key) ?? 0;
        if (sameAssignmentCount >= 2) continue;
        statesByAssignment.set(key, sameAssignmentCount + 1);
        states.push(state);
        if (states.length >= beamWidth) break;
      }
    }
    const bestState = states[0];
    if (!bestState) return null;
    const outputSrj = buildOutputSimpleRouteJson({
      inputSrj: this.inputSrj,
      plans: bestState.plans,
      layerNames: this.config.layerNames
    });
    if (bestState.plans.length === this.inputSrj.connections.length && !this.validateCompletePlans(bestState.plans, outputSrj).valid) {
      return null;
    }
    const score = bestState.plans.length === this.inputSrj.connections.length ? bestState.plans.reduce((total, plan) => total + plan.length, 0) + bestState.plans.filter((plan) => plan.via).length * 0.1 + assignmentLoadPenalty(bestState.assignment) * 0.01 : Number.POSITIVE_INFINITY;
    if (!Number.isFinite(score)) return null;
    const summary = {
      assignmentIndex,
      busLayerAssignments: bestState.assignment,
      routedBusCount: this.preparedBuses.length,
      routedConnectionCount: bestState.plans.length,
      failedBusIds: [],
      score
    };
    return {
      summary,
      plans: bestState.plans,
      blockingBusIds: [],
      outputSrj
    };
  }
  prioritizeFailedBusRepairs(assignment, failedBusIds, blockingBusIds) {
    const assignmentKey = JSON.stringify(assignment);
    const repairDepth = this.assignmentRepairDepthByKey.get(assignmentKey) ?? 0;
    if (repairDepth >= 2) return;
    const maximumRepairs = 8;
    const repairs = [];
    const repairKeys = /* @__PURE__ */ new Set();
    const addRepair = (repair) => {
      const key = JSON.stringify(repair);
      if (repairKeys.has(key) || this.evaluatedAssignmentKeys.has(key) || this.queuedAssignmentKeys.has(key)) {
        return;
      }
      repairKeys.add(key);
      this.queuedAssignmentKeys.add(key);
      this.assignmentRepairDepthByKey.set(key, repairDepth + 1);
      repairs.push(repair);
    };
    const repairBusIds = [];
    for (let index = 0; index < Math.max(failedBusIds.length, blockingBusIds.length); index++) {
      const failedBusId = failedBusIds[index];
      const blockingBusId = blockingBusIds[index];
      if (failedBusId && !repairBusIds.includes(failedBusId)) {
        repairBusIds.push(failedBusId);
      }
      if (blockingBusId && !repairBusIds.includes(blockingBusId)) {
        repairBusIds.push(blockingBusId);
      }
    }
    for (const failedBusId of failedBusIds) {
      const failedLayer = assignment[failedBusId];
      const failedCandidateLayers = this.escapeLayersByBusId[failedBusId];
      if (!failedLayer || !failedCandidateLayers) continue;
      for (const blockingBusId of blockingBusIds.slice(0, 4)) {
        const blockingLayer = assignment[blockingBusId];
        const blockingCandidateLayers = this.escapeLayersByBusId[blockingBusId];
        if (!blockingLayer || !blockingCandidateLayers || !failedCandidateLayers.includes(blockingLayer) || !blockingCandidateLayers.includes(failedLayer)) {
          continue;
        }
        addRepair({
          ...assignment,
          [failedBusId]: blockingLayer,
          [blockingBusId]: failedLayer
        });
        if (repairs.length >= maximumRepairs) break;
      }
      if (repairs.length >= maximumRepairs) break;
    }
    for (const busId of repairBusIds) {
      const currentLayer = assignment[busId];
      const candidateLayers = this.escapeLayersByBusId[busId];
      if (!currentLayer || !candidateLayers) continue;
      const currentLayerIndex = candidateLayers.indexOf(currentLayer);
      for (let shift = 1; shift < candidateLayers.length; shift++) {
        const candidateLayer = candidateLayers[(Math.max(currentLayerIndex, 0) + shift) % candidateLayers.length];
        if (candidateLayer === currentLayer) continue;
        addRepair({ ...assignment, [busId]: candidateLayer });
        if (repairs.length >= maximumRepairs) break;
      }
      if (repairs.length >= maximumRepairs) break;
    }
    this.pendingRepairAssignments.push(...repairs);
  }
  _step() {
    if (!this.groupedBeamEvaluated) {
      this.groupedBeamEvaluated = true;
      let beamAttempt = this.evaluateGroupedBeam(-1);
      if (!beamAttempt) {
        beamAttempt = this.evaluateGroupedBeam(-1, true);
      }
      if (beamAttempt) {
        this.attempts.push(beamAttempt.summary);
        this.bestAttempt = beamAttempt;
        this.stats = {
          assignment: 0,
          assignmentCount: this.config.maxLayerCombinations,
          routedBuses: `${beamAttempt.summary.routedBusCount}/${this.preparedBuses.length}`,
          routedConnections: `${beamAttempt.summary.routedConnectionCount}/${this.inputSrj.connections.length}`,
          failedBuses: "none",
          bestScore: beamAttempt.summary.score
        };
        this.solved = true;
        return;
      }
    }
    let assignment;
    while (!assignment && this.nextAssignmentIndex < this.config.maxLayerCombinations) {
      const preferGeneratedAssignment = this.nextAssignmentIndex % 3 === 0;
      let candidate;
      let candidateCameFromRepairQueue = false;
      if (preferGeneratedAssignment) {
        candidate = this.layerAssignments[this.nextGeneratedAssignmentIndex++];
      } else {
        candidate = this.pendingRepairAssignments.pop();
        candidateCameFromRepairQueue = candidate !== void 0;
      }
      if (!candidate) {
        candidate = preferGeneratedAssignment ? this.pendingRepairAssignments.pop() : this.layerAssignments[this.nextGeneratedAssignmentIndex++];
        candidateCameFromRepairQueue = preferGeneratedAssignment && candidate !== void 0;
      }
      if (!candidate) break;
      const candidateKey = JSON.stringify(candidate);
      if (candidateCameFromRepairQueue) {
        this.queuedAssignmentKeys.delete(candidateKey);
      }
      if (this.evaluatedAssignmentKeys.has(candidateKey)) continue;
      assignment = candidate;
    }
    if (!assignment) {
      if (this.bestAttempt && this.bestAttempt.summary.routedConnectionCount === this.inputSrj.connections.length) {
        this.solved = true;
      } else {
        this.failed = true;
        this.error = this.bestAttempt ? `FanoutSolver: best layer assignment routed ${this.bestAttempt.summary.routedConnectionCount}/${this.inputSrj.connections.length} connections` : "FanoutSolver: no layer assignment could be evaluated";
      }
      return;
    }
    const attempt = this.evaluateAssignment(
      this.nextAssignmentIndex,
      assignment
    );
    this.nextAssignmentIndex++;
    this.evaluatedAssignmentKeys.add(JSON.stringify(assignment));
    if (!this.bestAttempt || attempt.summary.routedConnectionCount >= this.bestAttempt.summary.routedConnectionCount) {
      this.prioritizeFailedBusRepairs(
        assignment,
        attempt.summary.failedBusIds,
        attempt.blockingBusIds
      );
    }
    this.attempts.push(attempt.summary);
    if (!this.bestAttempt || attempt.summary.score < this.bestAttempt.summary.score) {
      this.bestAttempt = attempt;
    }
    this.stats = {
      assignment: attempt.summary.assignmentIndex + 1,
      assignmentCount: this.config.maxLayerCombinations,
      routedBuses: `${attempt.summary.routedBusCount}/${this.preparedBuses.length}`,
      routedConnections: `${attempt.summary.routedConnectionCount}/${this.inputSrj.connections.length}`,
      failedBuses: attempt.summary.failedBusIds.join(", ") || "none",
      bestScore: this.bestAttempt.summary.score
    };
    if (attempt.summary.routedConnectionCount === this.inputSrj.connections.length) {
      this.solved = true;
    }
  }
  computeProgress() {
    if (this.solved || this.failed) return 1;
    return this.nextAssignmentIndex / this.config.maxLayerCombinations;
  }
  getConstructorParams() {
    return [this.inputSrj, this.options];
  }
  getOutput() {
    if (!this.solved || !this.bestAttempt) {
      throw new Error(
        "FanoutSolver: getOutput() called before a complete fanout was solved"
      );
    }
    const validation = this.validateCompletePlans(
      this.bestAttempt.plans,
      this.bestAttempt.outputSrj
    );
    if (!validation.valid) {
      throw new Error(
        `FanoutSolver: completed output failed validation: ${validation.issues[0]?.message ?? "unknown validation error"}`
      );
    }
    return {
      simpleRouteJson: this.bestAttempt.outputSrj,
      fanoutTraces: this.bestAttempt.plans.map((plan) => plan.trace),
      planeTerminations: this.bestAttempt.plans.flatMap(
        (plan) => plan.termination.type === "plane" && plan.via ? [
          {
            busId: plan.busId,
            connectionName: plan.connectionName,
            layer: plan.termination.layer,
            via: plan.via
          }
        ] : []
      ),
      busLayerAssignments: this.bestAttempt.summary.busLayerAssignments,
      busDirections: Object.fromEntries(
        this.preparedBuses.map((bus) => [bus.busId, bus.direction])
      ),
      attempts: [...this.attempts],
      validation
    };
  }
  getOutputSimpleRouteJson() {
    return this.getOutput().simpleRouteJson;
  }
  visualize() {
    const visualizedSrj = this.bestAttempt?.outputSrj ?? this.inputSrj;
    const graphics = convertSrjToGraphicsObject(visualizedSrj);
    const circularPadKeys = new Set(
      visualizedSrj.obstacles.filter(
        (obstacle) => obstacle.shape === "circle"
      ).map(
        (obstacle) => `${obstacle.center.x}:${obstacle.center.y}:${obstacle.width}:${obstacle.height}`
      )
    );
    const circularPadGraphics = [];
    const rects = graphics.rects?.filter((rect) => {
      const key = `${rect.center.x}:${rect.center.y}:${rect.width}:${rect.height}`;
      if (!circularPadKeys.has(key)) return true;
      circularPadGraphics.push({
        center: rect.center,
        radius: Math.min(rect.width, rect.height) / 2,
        fill: rect.fill,
        stroke: rect.stroke,
        layer: rect.layer,
        label: rect.label
      });
      return false;
    });
    return {
      ...graphics,
      rects,
      circles: [...graphics.circles ?? [], ...circularPadGraphics],
      lines: graphics.lines?.map((line) => {
        const layerMatch = /^z(\d+)$/.exec(line.layer ?? "");
        if (!layerMatch) return line;
        const { strokeDash: _strokeDash, ...solidLine } = line;
        return {
          ...solidLine,
          strokeColor: getCopperLayerColor(Number(layerMatch[1]))
        };
      })
    };
  }
};

// lib/utils/autorouting/get-fanout-shared-boundary.ts
import { distance as distance10 } from "circuit-json";
var getDirectionalPadding = (padding) => {
  if (typeof padding !== "object") {
    const parsedPadding = distance10.parse(padding);
    return {
      top: parsedPadding,
      right: parsedPadding,
      bottom: parsedPadding,
      left: parsedPadding
    };
  }
  return {
    top: padding.top === void 0 ? 0 : distance10.parse(padding.top),
    right: padding.right === void 0 ? 0 : distance10.parse(padding.right),
    bottom: padding.bottom === void 0 ? 0 : distance10.parse(padding.bottom),
    left: padding.left === void 0 ? 0 : distance10.parse(padding.left)
  };
};
var getObstacleHalfExtents = (obstacle) => {
  const rotationRadians = (obstacle.ccwRotationDegrees ?? 0) * Math.PI / 180;
  const absoluteCosine = Math.abs(Math.cos(rotationRadians));
  const absoluteSine = Math.abs(Math.sin(rotationRadians));
  return {
    x: (absoluteCosine * obstacle.width + absoluteSine * obstacle.height) / 2,
    y: (absoluteSine * obstacle.width + absoluteCosine * obstacle.height) / 2
  };
};
var getFanoutSharedBoundary = ({
  preparedBuses,
  padding
}) => {
  if (padding === void 0) return void 0;
  const sourcePadsByComponentId = /* @__PURE__ */ new Map();
  for (const bus of preparedBuses) {
    if (sourcePadsByComponentId.has(bus.componentId)) continue;
    sourcePadsByComponentId.set(
      bus.componentId,
      bus.componentObstacles.filter(
        (obstacle) => obstacle.connectedTo.length > 0
      )
    );
  }
  const sourcePadObstacles = [...sourcePadsByComponentId.values()].flat();
  if (sourcePadObstacles.length === 0) {
    throw new Error(
      "Cannot apply fanout boundary padding without source pad obstacles"
    );
  }
  const obstacleBounds = sourcePadObstacles.map((obstacle) => {
    const halfExtents = getObstacleHalfExtents(obstacle);
    return {
      minX: obstacle.center.x - halfExtents.x,
      maxX: obstacle.center.x + halfExtents.x,
      minY: obstacle.center.y - halfExtents.y,
      maxY: obstacle.center.y + halfExtents.y
    };
  });
  const directionalPadding = getDirectionalPadding(padding);
  return {
    minX: Math.min(...obstacleBounds.map((bounds) => bounds.minX)) - directionalPadding.left,
    maxX: Math.max(...obstacleBounds.map((bounds) => bounds.maxX)) + directionalPadding.right,
    minY: Math.min(...obstacleBounds.map((bounds) => bounds.minY)) - directionalPadding.bottom,
    maxY: Math.max(...obstacleBounds.map((bounds) => bounds.maxY)) + directionalPadding.top
  };
};

// lib/utils/autorouting/get-fanout-space-error-message.ts
var roundMm = (value) => Math.round(value * 1e3) / 1e3;
var getPadBoundsForComponents = (input, componentIds) => {
  let bounds;
  for (const obstacle of input.obstacles) {
    if (!obstacle.componentId || !componentIds.has(obstacle.componentId)) {
      continue;
    }
    const minX = obstacle.center.x - obstacle.width / 2;
    const maxX = obstacle.center.x + obstacle.width / 2;
    const minY = obstacle.center.y - obstacle.height / 2;
    const maxY = obstacle.center.y + obstacle.height / 2;
    bounds = bounds ? {
      minX: Math.min(bounds.minX, minX),
      maxX: Math.max(bounds.maxX, maxX),
      minY: Math.min(bounds.minY, minY),
      maxY: Math.max(bounds.maxY, maxY)
    } : { minX, maxX, minY, maxY };
  }
  return bounds;
};
var getNarrowestGap = (padBounds, boundary) => Math.min(
  padBounds.minX - boundary.minX,
  boundary.maxX - padBounds.maxX,
  padBounds.minY - boundary.minY,
  boundary.maxY - padBounds.maxY
);
var getFanoutSpaceErrorMessage = ({
  input,
  solverError,
  componentIds,
  sharedBoundary,
  componentNamesById
}) => {
  const routedMatch = solverError?.match(/routed (\d+)\/(\d+) connections/);
  const routedCount = routedMatch ? Number(routedMatch[1]) : void 0;
  const connectionCount = routedMatch ? Number(routedMatch[2]) : input.connections.length;
  const componentLabels = [...componentIds].map((componentId) => componentNamesById?.get(componentId) ?? componentId).sort();
  const componentSummary = componentLabels.length > 0 ? ` for ${componentLabels.join(", ")}` : "";
  const lines = [
    routedCount === void 0 ? `Fanout failed${componentSummary}: ${solverError ?? "the fanout solver did not report a reason"}.` : `Fanout failed${componentSummary}: only ${routedCount} of ${connectionCount} connections could escape to the breakout boundary.`
  ];
  const padBounds = getPadBoundsForComponents(input, componentIds);
  if (padBounds && sharedBoundary) {
    const gap = getNarrowestGap(padBounds, sharedBoundary);
    lines.push(
      `The breakout boundary is ${roundMm(gap)}mm from the pads at its narrowest point, which all ${connectionCount} escapes have to share.`
    );
  }
  lines.push(
    "Give the fanout more room by increasing the breakout's padding, or by extending the breakout to include the parts crowding it (decoupling capacitors, series resistors) so their pads sit inside the boundary instead of against it."
  );
  return lines.join(" ");
};

// lib/utils/autorouting/FanoutAutorouter.ts
var boundsDiffer = (first, second) => Math.abs(first.minX - second.minX) > 1e-6 || Math.abs(first.maxX - second.maxX) > 1e-6 || Math.abs(first.minY - second.minY) > 1e-6 || Math.abs(first.maxY - second.maxY) > 1e-6;
var expandBoundsToIncludePoints = (bounds, points) => {
  if (!bounds && points.length === 0) return void 0;
  return {
    minX: Math.min(
      bounds?.minX ?? Number.POSITIVE_INFINITY,
      ...points.map((p) => p.x)
    ),
    maxX: Math.max(
      bounds?.maxX ?? Number.NEGATIVE_INFINITY,
      ...points.map((p) => p.x)
    ),
    minY: Math.min(
      bounds?.minY ?? Number.POSITIVE_INFINITY,
      ...points.map((p) => p.y)
    ),
    maxY: Math.max(
      bounds?.maxY ?? Number.NEGATIVE_INFINITY,
      ...points.map((p) => p.y)
    )
  };
};
var getNinePointAnchor = (fanoutDirection) => typeof fanoutDirection === "string" ? fanoutDirection : fanoutDirection.direction;
var getFanoutBorderTarget = (anchor) => {
  switch (anchor) {
    case "top_left":
      return "top-left";
    case "top_center":
      return "top";
    case "top_right":
      return "top-right";
    case "center_left":
      return "left";
    case "center":
      return void 0;
    case "center_right":
      return "right";
    case "bottom_left":
      return "bottom-left";
    case "bottom_center":
      return "bottom";
    case "bottom_right":
      return "bottom-right";
  }
};
var getPlaneFanoutDirection = (anchor) => {
  switch (anchor) {
    case "top_left":
    case "top_center":
    case "top_right":
      return "up";
    case "center_left":
      return "left";
    case "center":
      return void 0;
    case "center_right":
      return "right";
    case "bottom_left":
    case "bottom_center":
    case "bottom_right":
      return "down";
  }
};
var getSourceComponentIdForPoint = (input, point6) => {
  const matchingObstacle = input.obstacles.find(
    (obstacle) => obstacle.componentId && obstacle.layers.includes(point6.layer) && (point6.pointId && obstacle.connectedTo.includes(point6.pointId) || point6.x >= obstacle.center.x - obstacle.width / 2 && point6.x <= obstacle.center.x + obstacle.width / 2 && point6.y >= obstacle.center.y - obstacle.height / 2 && point6.y <= obstacle.center.y + obstacle.height / 2)
  );
  return matchingObstacle?.componentId;
};
var inferPlaneBusDirection = (input, bus) => {
  const connectionNames = new Set(bus.connectionNames);
  const sourcePointsByComponentId = /* @__PURE__ */ new Map();
  for (const connection of input.connections) {
    if (!connectionNames.has(connection.name)) continue;
    for (const point6 of connection.pointsToConnect) {
      const componentId = getSourceComponentIdForPoint(input, point6);
      if (!componentId) continue;
      const sourcePoints2 = sourcePointsByComponentId.get(componentId) ?? [];
      sourcePoints2.push(point6);
      sourcePointsByComponentId.set(componentId, sourcePoints2);
    }
  }
  const sourceComponent = [...sourcePointsByComponentId.entries()].sort(
    ([firstComponentId, firstPoints], [secondComponentId, secondPoints]) => secondPoints.length - firstPoints.length || firstComponentId.localeCompare(secondComponentId)
  )[0];
  if (!sourceComponent) return void 0;
  const [sourceComponentId, sourcePoints] = sourceComponent;
  const componentObstacles = input.obstacles.filter(
    (obstacle) => obstacle.componentId === sourceComponentId
  );
  if (componentObstacles.length === 0) return void 0;
  const componentXCoordinates = componentObstacles.map(
    (obstacle) => obstacle.center.x
  );
  const componentYCoordinates = componentObstacles.map(
    (obstacle) => obstacle.center.y
  );
  const minComponentX = Math.min(...componentXCoordinates);
  const maxComponentX = Math.max(...componentXCoordinates);
  const minComponentY = Math.min(...componentYCoordinates);
  const maxComponentY = Math.max(...componentYCoordinates);
  const componentCenter = {
    x: (minComponentX + maxComponentX) / 2,
    y: (minComponentY + maxComponentY) / 2
  };
  const componentHalfSpan = {
    x: Math.max((maxComponentX - minComponentX) / 2, 1e-6),
    y: Math.max((maxComponentY - minComponentY) / 2, 1e-6)
  };
  const averageSourcePoint = {
    x: sourcePoints.reduce((sum, point6) => sum + point6.x, 0) / sourcePoints.length,
    y: sourcePoints.reduce((sum, point6) => sum + point6.y, 0) / sourcePoints.length
  };
  const normalizedOffset = {
    x: (averageSourcePoint.x - componentCenter.x) / componentHalfSpan.x,
    y: (averageSourcePoint.y - componentCenter.y) / componentHalfSpan.y
  };
  if (Math.abs(normalizedOffset.x) > Math.abs(normalizedOffset.y)) {
    return normalizedOffset.x >= 0 ? "right" : "left";
  }
  if (Math.abs(normalizedOffset.y) > 1e-9) {
    return normalizedOffset.y >= 0 ? "up" : "down";
  }
  return "right";
};
var createDownstreamSimpleRouteJson = ({
  fanoutSimpleRouteJson,
  sourceComponentIds
}) => {
  const sourceObstaclesByComponentId = /* @__PURE__ */ new Map();
  for (const obstacle of fanoutSimpleRouteJson.obstacles) {
    if (!obstacle.componentId || !sourceComponentIds.has(obstacle.componentId)) {
      continue;
    }
    const sourceObstacles = sourceObstaclesByComponentId.get(obstacle.componentId) ?? [];
    sourceObstacles.push(obstacle);
    sourceObstaclesByComponentId.set(obstacle.componentId, sourceObstacles);
  }
  const sourceFootprintKeepouts = [];
  for (const [componentId, sourceObstacles] of sourceObstaclesByComponentId) {
    const minX = Math.min(
      ...sourceObstacles.map(
        (obstacle) => obstacle.center.x - obstacle.width / 2
      )
    );
    const maxX = Math.max(
      ...sourceObstacles.map(
        (obstacle) => obstacle.center.x + obstacle.width / 2
      )
    );
    const minY = Math.min(
      ...sourceObstacles.map(
        (obstacle) => obstacle.center.y - obstacle.height / 2
      )
    );
    const maxY = Math.max(
      ...sourceObstacles.map(
        (obstacle) => obstacle.center.y + obstacle.height / 2
      )
    );
    sourceFootprintKeepouts.push({
      obstacleId: `fanout-source-keepout:${componentId}`,
      type: "rect",
      layers: getViaBoardLayers(fanoutSimpleRouteJson.layerCount),
      center: { x: (minX + maxX) / 2, y: (minY + maxY) / 2 },
      width: maxX - minX,
      height: maxY - minY,
      connectedTo: []
    });
  }
  return {
    ...fanoutSimpleRouteJson,
    obstacles: [
      ...fanoutSimpleRouteJson.obstacles.filter(
        (obstacle) => !obstacle.componentId || !sourceComponentIds.has(obstacle.componentId)
      ),
      ...sourceFootprintKeepouts
    ]
  };
};
var FanoutAutorouter = class _FanoutAutorouter {
  constructor(input, options) {
    this.input = input;
    this.options = options;
  }
  input;
  options;
  isRouting = false;
  outputSimpleRouteJson;
  startTimeoutId;
  eventHandlers = {
    complete: [],
    error: [],
    progress: []
  };
  getBusExitPreferences() {
    if (!this.options.busFanoutDirections) return void 0;
    const knownBusIds = new Set(this.input.buses?.map((bus) => bus.busId) ?? []);
    const planeBusIds = new Set(
      this.input.buses?.filter((bus) => bus.termination?.type === "plane").map((bus) => bus.busId) ?? []
    );
    const preferences = {};
    for (const [busId, fanoutDirection] of Object.entries(
      this.options.busFanoutDirections
    )) {
      if (!knownBusIds.has(busId)) {
        throw new Error(
          `Fanout direction references unknown bus "${busId}" in this autorouting phase`
        );
      }
      if (planeBusIds.has(busId)) continue;
      const target = getFanoutBorderTarget(getNinePointAnchor(fanoutDirection));
      if (target) preferences[busId] = target;
    }
    return Object.keys(preferences).length > 0 ? preferences : void 0;
  }
  getPlaneBusDirections() {
    const planeBuses = this.input.buses?.filter((bus) => bus.termination?.type === "plane") ?? [];
    const planeBusIds = new Set(planeBuses.map((bus) => bus.busId));
    const directions = {};
    for (const [busId, fanoutDirection] of Object.entries(
      this.options.busFanoutDirections ?? {}
    )) {
      if (!planeBusIds.has(busId)) continue;
      const direction = getPlaneFanoutDirection(
        getNinePointAnchor(fanoutDirection)
      );
      if (direction) directions[busId] = direction;
    }
    for (const planeBus of planeBuses) {
      if (directions[planeBus.busId]) continue;
      const direction = inferPlaneBusDirection(this.input, planeBus);
      if (direction) directions[planeBus.busId] = direction;
    }
    return Object.keys(directions).length > 0 ? directions : void 0;
  }
  getFanoutSolverOptions() {
    const commonOptions = {
      borderDistribution: "even",
      compactBusTracks: true,
      busDirections: this.getPlaneBusDirections(),
      busExitPreferences: this.getBusExitPreferences(),
      escapeLayers: this.options.fanoutRoutingLayers
    };
    if (this.options.mode === "single_layer_fanout") {
      return {
        ...commonOptions,
        escapeLayers: ["top"],
        singleLayerPushAndShove: true
      };
    }
    return commonOptions;
  }
  static resolveFanoutBounds(input, options) {
    let paddingBounds;
    if (options.fanoutBoundaryPadding !== void 0) {
      const boundsResolver = new _FanoutAutorouter(input, options);
      const fanoutSolver = new FanoutSolver(
        input,
        boundsResolver.getFanoutSolverOptions()
      );
      paddingBounds = getFanoutSharedBoundary({
        preparedBuses: fanoutSolver.preparedBuses,
        padding: options.fanoutBoundaryPadding
      });
    }
    if (options.fanoutBounds && paddingBounds && boundsDiffer(options.fanoutBounds, paddingBounds)) {
      options.onFanoutBoundsConflict?.();
    }
    return expandBoundsToIncludePoints(
      options.fanoutBounds ?? paddingBounds,
      options.breakoutPoints ?? []
    );
  }
  solveFanout() {
    const fanoutSolverOptions = this.getFanoutSolverOptions();
    const fanoutSolver = new FanoutSolver(
      this.input,
      {
        ...fanoutSolverOptions,
        ...this.options.fanoutBounds ? { sharedBoundary: this.options.fanoutBounds } : {}
      }
    );
    fanoutSolver.solve();
    if (fanoutSolver.failed) {
      throw new Error(
        getFanoutSpaceErrorMessage({
          input: this.input,
          solverError: fanoutSolver.error,
          componentIds: new Set(
            fanoutSolver.preparedBuses.map((bus) => bus.componentId)
          ),
          sharedBoundary: this.options.fanoutBounds ?? fanoutSolverOptions.sharedBoundary,
          componentNamesById: this.options.componentNamesById
        })
      );
    }
    const output = fanoutSolver.getOutput();
    const fanoutSimpleRouteJson = output.simpleRouteJson;
    const fanoutTraces = output.fanoutTraces;
    return {
      downstreamSimpleRouteJson: createDownstreamSimpleRouteJson({
        fanoutSimpleRouteJson,
        sourceComponentIds: new Set(
          fanoutSolver.preparedBuses.map((bus) => bus.componentId)
        )
      }),
      fanoutTraces,
      debugGraphics: fanoutSolver.visualize()
    };
  }
  startFanout() {
    try {
      const { downstreamSimpleRouteJson, fanoutTraces, debugGraphics } = this.solveFanout();
      if (!this.isRouting) return;
      this.outputSimpleRouteJson = downstreamSimpleRouteJson;
      this.emitEvent({
        type: "progress",
        steps: 1,
        progress: 1,
        phase: this.options.mode,
        debugGraphics
      });
      this.isRouting = false;
      this.emitEvent({
        type: "complete",
        traces: fanoutTraces
      });
    } catch (caughtError) {
      this.isRouting = false;
      this.emitEvent({
        type: "error",
        error: caughtError instanceof Error ? caughtError : new Error(String(caughtError))
      });
    }
  }
  start() {
    if (this.isRouting) return;
    this.isRouting = true;
    this.startTimeoutId = setTimeout(() => {
      this.startTimeoutId = void 0;
      this.startFanout();
    }, 0);
  }
  stop() {
    if (this.startTimeoutId !== void 0) {
      clearTimeout(this.startTimeoutId);
      this.startTimeoutId = void 0;
    }
    this.isRouting = false;
  }
  on(event, callback) {
    if (event === "complete") {
      this.eventHandlers.complete.push(
        callback
      );
    } else if (event === "error") {
      this.eventHandlers.error.push(
        callback
      );
    } else {
      this.eventHandlers.progress.push(
        callback
      );
    }
  }
  emitEvent(event) {
    if (event.type === "complete") {
      for (const handler of this.eventHandlers.complete) handler(event);
    } else if (event.type === "error") {
      for (const handler of this.eventHandlers.error) handler(event);
    } else {
      for (const handler of this.eventHandlers.progress) handler(event);
    }
  }
  solveSync() {
    const { downstreamSimpleRouteJson, fanoutTraces } = this.solveFanout();
    this.outputSimpleRouteJson = downstreamSimpleRouteJson;
    return fanoutTraces;
  }
  getOutputSimpleRouteJson() {
    return this.outputSimpleRouteJson;
  }
};

// lib/utils/autorouting/createSourceTracesFromOffboardConnections.ts
var createSourceTracesFromOffboardConnections = ({
  db,
  connectedOffboardObstacles,
  simpleRouteJson,
  subcircuit_id
}) => {
  if (Object.keys(connectedOffboardObstacles).length === 0) return;
  const pcbElementIdToSourcePortId = /* @__PURE__ */ new Map();
  for (const pcbPort of db.pcb_port.list()) {
    if (pcbPort.source_port_id) {
      const smtpad = db.pcb_smtpad.getWhere({
        pcb_port_id: pcbPort.pcb_port_id
      });
      if (smtpad) {
        pcbElementIdToSourcePortId.set(
          smtpad.pcb_smtpad_id,
          pcbPort.source_port_id
        );
      }
      const platedHole = db.pcb_plated_hole.getWhere({
        pcb_port_id: pcbPort.pcb_port_id
      });
      if (platedHole) {
        pcbElementIdToSourcePortId.set(
          platedHole.pcb_plated_hole_id,
          pcbPort.source_port_id
        );
      }
    }
  }
  const obstacleById = /* @__PURE__ */ new Map();
  for (const obstacle of simpleRouteJson.obstacles) {
    if (obstacle.obstacleId) {
      obstacleById.set(obstacle.obstacleId, obstacle);
    }
  }
  const connectionGroups = /* @__PURE__ */ new Map();
  for (const [obstacleId, rootConnectionName] of Object.entries(
    connectedOffboardObstacles
  )) {
    if (!connectionGroups.has(rootConnectionName)) {
      connectionGroups.set(rootConnectionName, []);
    }
    connectionGroups.get(rootConnectionName).push(obstacleId);
  }
  for (const [rootConnectionName, obstacleIds] of connectionGroups) {
    const sourcePortIds = /* @__PURE__ */ new Set();
    for (const obstacleId of obstacleIds) {
      const obstacle = obstacleById.get(obstacleId);
      if (!obstacle) continue;
      for (const connectedId of obstacle.connectedTo) {
        const sourcePortId = pcbElementIdToSourcePortId.get(connectedId);
        if (sourcePortId) {
          sourcePortIds.add(sourcePortId);
        }
      }
    }
    if (sourcePortIds.size < 2) continue;
    const sourcePortIdArray = Array.from(sourcePortIds);
    const existingTraces = db.source_trace.list();
    const alreadyConnected = existingTraces.some((trace) => {
      const tracePortIds = new Set(trace.connected_source_port_ids);
      return sourcePortIdArray.every((id) => tracePortIds.has(id));
    });
    if (alreadyConnected) continue;
    db.source_trace.insert({
      connected_source_port_ids: sourcePortIdArray,
      connected_source_net_ids: [],
      subcircuit_id: subcircuit_id ?? void 0,
      display_name: `offboard_${rootConnectionName}`
    });
  }
};

// lib/utils/autorouting/getPresetAutoroutingConfig.ts
var normalizeAutorouterName = (value) => value?.replace(/-/g, "_");
function getLegacyAutorouterPreset(autorouterConfig) {
  if (!autorouterConfig) return null;
  if (typeof autorouterConfig === "string") {
    const normalizedAutorouterName = normalizeAutorouterName(autorouterConfig);
    return normalizedAutorouterName === "sequential_trace" || normalizedAutorouterName === "auto_cloud" ? normalizedAutorouterName : null;
  }
  const normalizedPreset = normalizeAutorouterName(autorouterConfig.preset);
  if (normalizedPreset === "sequential_trace" || normalizedPreset === "auto_cloud") {
    return normalizedPreset;
  }
  return normalizeAutorouterName(autorouterConfig.groupMode) === "sequential_trace" ? "sequential_trace" : null;
}
function getPresetAutoroutingConfig(autorouterConfig, platformConfig) {
  const defaults = {
    serverUrl: "https://registry-api.tscircuit.com",
    serverMode: "job",
    serverCacheEnabled: true
  };
  if (typeof autorouterConfig === "object" && !autorouterConfig.preset) {
    return {
      local: !(autorouterConfig.serverUrl || autorouterConfig.serverMode || autorouterConfig.serverCacheEnabled),
      ...defaults,
      ...autorouterConfig
    };
  }
  const preset = typeof autorouterConfig === "object" ? autorouterConfig.preset : autorouterConfig;
  const providedConfig = typeof autorouterConfig === "object" ? autorouterConfig : {};
  const normalizedPreset = typeof preset === "string" ? preset.replace(/_/g, "-") : preset;
  const platformAutorouter = normalizedPreset && typeof normalizedPreset === "string" ? platformConfig?.autorouterMap?.[normalizedPreset] : void 0;
  if (platformAutorouter) {
    return {
      local: true,
      groupMode: "subcircuit",
      algorithmFn: async (simpleRouteJson) => platformAutorouter.createAutorouter(simpleRouteJson)
    };
  }
  switch (normalizedPreset) {
    case "default":
    case "auto":
    case "auto-local":
      return {
        local: true,
        groupMode: "subcircuit"
      };
    case "sequential-trace":
      return {
        local: true,
        groupMode: "sequential-trace"
      };
    case "subcircuit":
      return {
        local: true,
        groupMode: "subcircuit"
      };
    case "beta-pipeline9":
      return {
        local: true,
        groupMode: "subcircuit",
        autorouterVersion: "beta_pipeline9"
      };
    case "single-layer-fanout":
      return {
        local: true,
        groupMode: "subcircuit",
        preset: "single_layer_fanout"
      };
    case "fanout":
      return {
        local: true,
        groupMode: "subcircuit",
        preset: "fanout"
      };
    case "auto-cloud": {
      const {
        preset: _preset,
        local: _local,
        groupMode: _groupMode,
        ...rest
      } = providedConfig;
      return {
        local: false,
        groupMode: "subcircuit",
        ...defaults,
        ...rest
      };
    }
    case "laser-prefab": {
      const {
        preset: _preset,
        local: _local,
        groupMode: _groupMode,
        ...rest
      } = providedConfig;
      return {
        local: true,
        groupMode: "subcircuit",
        preset: "laser_prefab",
        ...rest
      };
    }
    case "auto-jumper": {
      const {
        preset: _preset,
        local: _local,
        groupMode: _groupMode,
        ...rest
      } = providedConfig;
      return {
        local: true,
        groupMode: "subcircuit",
        preset: "auto_jumper",
        ...rest
      };
    }
    default:
      return {
        local: true,
        groupMode: "subcircuit"
      };
  }
}

// lib/utils/autorouting/CapacityMeshAutorouter.ts
import "@tscircuit/capacity-autorouter";

// lib/solvers.ts
import {
  AssignableAutoroutingPipeline2,
  AssignableAutoroutingPipeline3,
  AutoroutingPipeline1_OriginalUnravel,
  AutoroutingPipelineSolver,
  AutoroutingPipelineSolver3_HgPortPointPathing,
  AutoroutingPipelineSolver4,
  AutoroutingPipelineSolver5,
  AutoroutingPipelineSolver7_MultiGraph,
  AutoroutingPipelineSolver8,
  AutoroutingPipelineSolver9_PreloadedTraceGraph
} from "@tscircuit/capacity-autorouter";
import { CopperPourPipelineSolver } from "@tscircuit/copper-pour-solver";
import { CreateFdmEnclosureSolver } from "@tscircuit/create-fdm-enclosure";
import { LayoutPipelineSolver } from "@tscircuit/matchpack";
import { SchematicTracePipelineSolver } from "@tscircuit/schematic-trace-solver";
import { PackSolver2 } from "calculate-packing";
var SOLVERS = {
  PackSolver2,
  LayoutPipelineSolver,
  AutoroutingPipelineSolver,
  AssignableAutoroutingPipeline2,
  AssignableAutoroutingPipeline3,
  AutoroutingPipeline1_OriginalUnravel,
  AutoroutingPipelineSolver3_HgPortPointPathing,
  AutoroutingPipelineSolver4,
  AutoroutingPipelineSolver5,
  AutoroutingPipelineSolver7_MultiGraph,
  AutoroutingPipelineSolver8,
  AutoroutingPipelineSolver9_PreloadedTraceGraph,
  CopperPourPipelineSolver,
  CreateFdmEnclosureSolver,
  SchematicTracePipelineSolver
};

// lib/utils/autorouting/LocalCacheEngineCacheProvider.ts
var AUTOROUTER_CACHE_KEY_PREFIX = "capacity-autorouter:";
var cacheProvidersByLocalCacheEngine = /* @__PURE__ */ new WeakMap();
function isPromiseLike(value) {
  return typeof value === "object" && value !== null && "then" in value && typeof value.then === "function";
}
function isAsyncFunction(fn) {
  return fn.constructor.name === "AsyncFunction";
}
var LocalCacheEngineCacheProvider = class {
  constructor(localCacheEngine) {
    this.localCacheEngine = localCacheEngine;
    this.isSyncCache = !isAsyncFunction(localCacheEngine.getItem) && !isAsyncFunction(localCacheEngine.setItem);
  }
  localCacheEngine;
  isSyncCache;
  cacheHits = 0;
  cacheMisses = 0;
  cacheHitsByPrefix = {};
  cacheMissesByPrefix = {};
  knownCacheKeys = /* @__PURE__ */ new Set();
  getCachedSolutionSync(cacheKey) {
    if (!this.isSyncCache) return void 0;
    this.knownCacheKeys.add(cacheKey);
    try {
      const cachedItem = this.localCacheEngine.getItem(
        this.getLocalCacheEngineKey(cacheKey)
      );
      if (isPromiseLike(cachedItem)) {
        this.isSyncCache = false;
        void Promise.resolve(cachedItem).catch(() => {
        });
        this.recordCacheMiss(cacheKey);
        return void 0;
      }
      return this.parseCachedItem(cacheKey, cachedItem);
    } catch {
      this.recordCacheMiss(cacheKey);
      return void 0;
    }
  }
  async getCachedSolution(cacheKey) {
    this.knownCacheKeys.add(cacheKey);
    try {
      const cachedItem = await this.localCacheEngine.getItem(
        this.getLocalCacheEngineKey(cacheKey)
      );
      return this.parseCachedItem(cacheKey, cachedItem);
    } catch {
      this.recordCacheMiss(cacheKey);
      return void 0;
    }
  }
  setCachedSolutionSync(cacheKey, cachedSolution) {
    if (!this.isSyncCache) return;
    this.knownCacheKeys.add(cacheKey);
    try {
      const serializedSolution = JSON.stringify(cachedSolution);
      if (serializedSolution === void 0) return;
      const writeResult = this.localCacheEngine.setItem(
        this.getLocalCacheEngineKey(cacheKey),
        serializedSolution
      );
      if (isPromiseLike(writeResult)) {
        this.isSyncCache = false;
        void Promise.resolve(writeResult).catch(() => {
        });
      }
    } catch {
    }
  }
  async setCachedSolution(cacheKey, cachedSolution) {
    this.knownCacheKeys.add(cacheKey);
    try {
      const serializedSolution = JSON.stringify(cachedSolution);
      if (serializedSolution === void 0) return;
      await this.localCacheEngine.setItem(
        this.getLocalCacheEngineKey(cacheKey),
        serializedSolution
      );
    } catch {
    }
  }
  getAllCacheKeys() {
    return [...this.knownCacheKeys];
  }
  clearCache() {
    for (const cacheKey of this.knownCacheKeys) {
      const localCacheEngineKey = this.getLocalCacheEngineKey(cacheKey);
      try {
        const clearResult = this.localCacheEngine.removeItem ? this.localCacheEngine.removeItem(localCacheEngineKey) : this.localCacheEngine.setItem(localCacheEngineKey, "null");
        if (isPromiseLike(clearResult)) {
          void Promise.resolve(clearResult).catch(() => {
          });
        }
      } catch {
      }
    }
    this.knownCacheKeys.clear();
    this.cacheHits = 0;
    this.cacheMisses = 0;
    this.cacheHitsByPrefix = {};
    this.cacheMissesByPrefix = {};
  }
  getLocalCacheEngineKey(cacheKey) {
    return `${AUTOROUTER_CACHE_KEY_PREFIX}${cacheKey}`;
  }
  parseCachedItem(cacheKey, cachedItem) {
    if (cachedItem === null) {
      this.recordCacheMiss(cacheKey);
      return void 0;
    }
    try {
      const cachedSolution = JSON.parse(cachedItem);
      if (cachedSolution === null) {
        this.recordCacheMiss(cacheKey);
        return void 0;
      }
      this.recordCacheHit(cacheKey);
      return cachedSolution;
    } catch {
      this.recordCacheMiss(cacheKey);
      return void 0;
    }
  }
  recordCacheHit(cacheKey) {
    this.cacheHits++;
    const prefix = cacheKey.split(":")[0];
    this.cacheHitsByPrefix[prefix] = (this.cacheHitsByPrefix[prefix] ?? 0) + 1;
  }
  recordCacheMiss(cacheKey) {
    this.cacheMisses++;
    const prefix = cacheKey.split(":")[0];
    this.cacheMissesByPrefix[prefix] = (this.cacheMissesByPrefix[prefix] ?? 0) + 1;
  }
};
function getCacheProviderForLocalCacheEngine(localCacheEngine) {
  let cacheProvider = cacheProvidersByLocalCacheEngine.get(localCacheEngine);
  if (!cacheProvider) {
    cacheProvider = new LocalCacheEngineCacheProvider(localCacheEngine);
    cacheProvidersByLocalCacheEngine.set(localCacheEngine, cacheProvider);
  }
  return cacheProvider;
}

// lib/utils/autorouting/CapacityMeshAutorouter.ts
function getCapacityAutorouterCacheProvider(platformConfig) {
  if (!platformConfig?.localCacheEngine) return null;
  return getCacheProviderForLocalCacheEngine(platformConfig.localCacheEngine);
}
var TscircuitAutorouter = class {
  input;
  isRouting = false;
  solver;
  eventHandlers = {
    complete: [],
    error: [],
    progress: []
  };
  cycleCount = 0;
  stepDelay;
  timeoutId;
  constructor(input, options = {}) {
    this.input = input;
    const {
      capacityDepth,
      targetMinCapacity,
      stepDelay = 0,
      useAssignableSolver = false,
      useAutoJumperSolver = false,
      autorouterVersion: autorouterVersion2,
      useLaserPrefabSolver = false,
      effort,
      platformConfig,
      onSolverStarted
    } = options;
    let solverName;
    if (autorouterVersion2 === "beta_pipeline1") {
      solverName = "AutoroutingPipeline1_OriginalUnravel";
    } else if (autorouterVersion2 === "beta_pipeline3") {
      solverName = "AutoroutingPipelineSolver3_HgPortPointPathing";
    } else if (autorouterVersion2 === "beta_pipeline4") {
      solverName = "AutoroutingPipelineSolver4";
    } else if (autorouterVersion2 === "beta_pipeline5") {
      solverName = "AutoroutingPipelineSolver5";
    } else if (autorouterVersion2 === "beta_pipeline7" || autorouterVersion2 === "latest") {
      solverName = "AutoroutingPipelineSolver7_MultiGraph";
    } else if (autorouterVersion2 === "beta_pipeline9") {
      solverName = "AutoroutingPipelineSolver9_PreloadedTraceGraph";
    } else if (useLaserPrefabSolver) {
      solverName = "AutoroutingPipelineSolver8";
    } else if (useAutoJumperSolver) {
      solverName = "AssignableAutoroutingPipeline3";
    } else if (useAssignableSolver) {
      solverName = "AssignableAutoroutingPipeline2";
    } else {
      solverName = "AutoroutingPipelineSolver7_MultiGraph";
    }
    const SolverClass = SOLVERS[solverName];
    const solverCacheProvider = getCapacityAutorouterCacheProvider(platformConfig);
    this.solver = new SolverClass(input, {
      capacityDepth,
      targetMinCapacity,
      cacheProvider: solverCacheProvider,
      effort
    });
    onSolverStarted?.({
      solverName,
      solverParams: {
        input,
        options: {
          capacityDepth,
          targetMinCapacity,
          cacheProvider: solverCacheProvider,
          effort
        }
      }
    });
    this.stepDelay = stepDelay;
  }
  /**
   * Start the autorouting process asynchronously
   * This will emit progress events during routing and a complete event when done
   */
  start() {
    if (this.isRouting) return;
    this.isRouting = true;
    this.cycleCount = 0;
    void this.runCycleAndQueueNextCycle();
  }
  async stepSolver() {
    if ("stepAsync" in this.solver && typeof this.solver.stepAsync === "function") {
      await this.solver.stepAsync();
      return;
    }
    this.solver.step();
  }
  /**
   * Execute the next routing step and schedule the following one if needed
   */
  async runCycleAndQueueNextCycle() {
    if (!this.isRouting) return;
    try {
      if (this.solver.solved || this.solver.failed) {
        if (this.solver.failed) {
          this.emitEvent({
            type: "error",
            error: new AutorouterError(this.solver.error || "Routing failed")
          });
        } else {
          this.emitEvent({
            type: "complete",
            traces: this.solver.getOutputSimpleRouteJson().traces || []
          });
        }
        this.isRouting = false;
        return;
      }
      const startTime = Date.now();
      const startIterations = this.solver.iterations;
      while (Date.now() - startTime < 250 && !this.solver.failed && !this.solver.solved) {
        await this.stepSolver();
      }
      const iterationsPerSecond = (this.solver.iterations - startIterations) / (Date.now() - startTime) * 1e3;
      this.cycleCount++;
      const debugGraphics = this.solver?.preview() || void 0;
      const progress = this.solver.progress;
      this.emitEvent({
        type: "progress",
        steps: this.cycleCount,
        iterationsPerSecond,
        progress,
        phase: this.solver.getCurrentPhase(),
        debugGraphics
      });
      if (this.stepDelay > 0) {
        this.timeoutId = setTimeout(
          () => void this.runCycleAndQueueNextCycle(),
          this.stepDelay
        );
      } else {
        this.timeoutId = setTimeout(
          () => void this.runCycleAndQueueNextCycle(),
          0
        );
      }
    } catch (error) {
      this.emitEvent({
        type: "error",
        error: error instanceof Error ? new AutorouterError(error.message) : new AutorouterError(String(error))
      });
      this.isRouting = false;
    }
  }
  /**
   * Stop the routing process if it's in progress
   */
  stop() {
    if (!this.isRouting) return;
    this.isRouting = false;
    if (this.timeoutId !== void 0) {
      clearTimeout(this.timeoutId);
      this.timeoutId = void 0;
    }
  }
  on(event, callback) {
    if (event === "complete") {
      this.eventHandlers.complete.push(
        callback
      );
    } else if (event === "error") {
      this.eventHandlers.error.push(
        callback
      );
    } else if (event === "progress") {
      this.eventHandlers.progress.push(
        callback
      );
    }
  }
  /**
   * Emit an event to all registered handlers
   */
  emitEvent(event) {
    if (event.type === "complete") {
      for (const handler of this.eventHandlers.complete) {
        handler(event);
      }
    } else if (event.type === "error") {
      for (const handler of this.eventHandlers.error) {
        handler(event);
      }
    } else if (event.type === "progress") {
      for (const handler of this.eventHandlers.progress) {
        handler(event);
      }
    }
  }
  /**
   * Solve the routing problem synchronously
   * @returns Array of routed traces
   */
  solveSync() {
    this.solver.solve();
    if (this.solver.failed) {
      throw new AutorouterError(this.solver.error || "Routing failed");
    }
    return this.solver.getOutputSimpleRouteJson().traces || [];
  }
  /**
   * Get the mapping of obstacle IDs to root connection names that were
   * connected via off-board paths (e.g., interconnects).
   * Only available when using AssignableAutoroutingPipeline2.
   */
  getConnectedOffboardObstacles() {
    if ("getConnectedOffboardObstacles" in this.solver) {
      return this.solver.getConnectedOffboardObstacles();
    }
    return {};
  }
};

// lib/utils/autorouting/localAutorouterStrategies.ts
var defaultLocalAutorouterStrategy = {
  cacheable: true,
  create: ({ simpleRouteJson, commonAutorouterOptions }) => new TscircuitAutorouter(simpleRouteJson, commonAutorouterOptions)
};
var createFanoutAutorouterStrategy = (mode) => ({
  cacheable: false,
  followUpAutorouter: "default",
  create: ({
    simpleRouteJson,
    busFanoutDirections,
    fanoutBounds,
    fanoutRoutingLayers,
    componentNamesById
  }) => new FanoutAutorouter(simpleRouteJson, {
    mode,
    busFanoutDirections,
    fanoutBounds,
    fanoutRoutingLayers,
    componentNamesById
  })
});
var localAutorouterStrategies = /* @__PURE__ */ new Map([
  [
    "single_layer_fanout",
    createFanoutAutorouterStrategy("single_layer_fanout")
  ],
  ["fanout", createFanoutAutorouterStrategy("fanout")]
]);
var getLocalAutorouterStrategy = (preset) => localAutorouterStrategies.get(preset ?? "") ?? defaultLocalAutorouterStrategy;
var getLocalAutoroutingStages = (autorouterConfig, platformConfig) => {
  const strategy = getLocalAutorouterStrategy(autorouterConfig.preset);
  const stages = [
    {
      autorouterConfig,
      strategy,
      usesPreviousStageOutput: false
    }
  ];
  if (strategy.followUpAutorouter) {
    const followUpAutorouterConfig = getPresetAutoroutingConfig(
      strategy.followUpAutorouter,
      platformConfig
    );
    stages.push({
      autorouterConfig: followUpAutorouterConfig,
      strategy: getLocalAutorouterStrategy(followUpAutorouterConfig.preset),
      usesPreviousStageOutput: true
    });
  }
  return stages;
};

// lib/utils/edit-events/apply-pcb-edit-events-to-manual-edits-file.ts
import "zod";
import { su } from "@tscircuit/circuit-json-util";
var applyPcbEditEventsToManualEditsFile = ({
  circuitJson,
  editEvents,
  manualEditsFile
}) => {
  const updatedManualEditsFile = {
    ...manualEditsFile,
    pcb_placements: [...manualEditsFile.pcb_placements ?? []]
  };
  for (const editEvent of editEvents) {
    if (editEvent.edit_event_type === "edit_pcb_component_location") {
      const { pcb_component_id, new_center } = editEvent;
      const pcb_component = su(circuitJson).pcb_component.get(pcb_component_id);
      if (!pcb_component) continue;
      const source_component = su(circuitJson).source_component.get(
        pcb_component.source_component_id
      );
      if (!source_component) continue;
      const existingPlacementIndex = updatedManualEditsFile.pcb_placements?.findIndex(
        (p) => p.selector === source_component.name
      );
      const newPlacement = {
        selector: source_component.name,
        center: new_center,
        relative_to: "group_center"
      };
      if (existingPlacementIndex >= 0) {
        updatedManualEditsFile.pcb_placements[existingPlacementIndex] = newPlacement;
      } else {
        updatedManualEditsFile.pcb_placements.push(newPlacement);
      }
    }
  }
  return updatedManualEditsFile;
};

// lib/utils/edit-events/apply-schematic-edit-events-to-manual-edits-file.ts
import { su as su2 } from "@tscircuit/circuit-json-util";
import "zod";
var applySchematicEditEventsToManualEditsFile = ({
  circuitJson,
  editEvents,
  manualEditsFile
}) => {
  const updatedManualEditsFile = {
    ...manualEditsFile,
    schematic_placements: [...manualEditsFile.schematic_placements ?? []]
  };
  for (const editEvent of editEvents) {
    if (editEvent.edit_event_type === "edit_schematic_component_location") {
      const { schematic_component_id, new_center } = editEvent;
      const schematic_component2 = su2(circuitJson).schematic_component.get(
        schematic_component_id
      );
      if (!schematic_component2) continue;
      if (!schematic_component2.source_component_id) continue;
      const source_component = su2(circuitJson).source_component.get(
        schematic_component2.source_component_id
      );
      if (!source_component) continue;
      const existingPlacementIndex = updatedManualEditsFile.schematic_placements?.findIndex(
        (p) => p.selector === source_component.name
      );
      const newPlacement = {
        selector: source_component.name,
        center: new_center,
        relative_to: "group_center"
      };
      if (existingPlacementIndex >= 0) {
        updatedManualEditsFile.schematic_placements[existingPlacementIndex] = newPlacement;
      } else {
        updatedManualEditsFile.schematic_placements.push(newPlacement);
      }
    }
  }
  return updatedManualEditsFile;
};

// lib/utils/edit-events/apply-edit-events-to-manual-edits-file.ts
var applyEditEventsToManualEditsFile = ({
  circuitJson,
  editEvents,
  manualEditsFile
}) => {
  const schematicEditEvents = editEvents.filter(
    (event) => event.edit_event_type === "edit_schematic_component_location"
  );
  const pcbEditEvents = editEvents.filter(
    (event) => event.edit_event_type === "edit_pcb_component_location"
  );
  let updatedManualEditsFile = manualEditsFile;
  if (schematicEditEvents.length > 0) {
    updatedManualEditsFile = applySchematicEditEventsToManualEditsFile({
      circuitJson,
      editEvents: schematicEditEvents,
      manualEditsFile: updatedManualEditsFile
    });
  }
  if (pcbEditEvents.length > 0) {
    updatedManualEditsFile = applyPcbEditEventsToManualEditsFile({
      circuitJson,
      editEvents: pcbEditEvents,
      manualEditsFile: updatedManualEditsFile
    });
  }
  return updatedManualEditsFile;
};

// lib/utils/edit-events/apply-edit-events-to-circuit-json.ts
import { transformPCBElement } from "@tscircuit/circuit-json-util";
import { translate as translate5 } from "transformation-matrix";

// lib/utils/edit-events/apply-trace-hint-edit-event.ts
import { su as su3 } from "@tscircuit/circuit-json-util";
var applyTraceHintEditEvent = (circuitJson, edit_event) => {
  const existingTraceHint = su3(circuitJson).pcb_trace_hint.get(
    edit_event.pcb_trace_hint_id
  );
  if (existingTraceHint) {
    circuitJson = circuitJson.map(
      (e) => e.pcb_trace_hint_id === edit_event.pcb_trace_hint_id ? {
        ...e,
        route: edit_event.route
      } : e
    );
  } else {
    const pcbPort = su3(circuitJson).pcb_port.get(edit_event.pcb_port_id);
    circuitJson = circuitJson.filter(
      (e) => !(e.type === "pcb_trace_hint" && e.pcb_port_id === edit_event.pcb_port_id)
    ).concat([
      {
        type: "pcb_trace_hint",
        pcb_trace_hint_id: edit_event.pcb_trace_hint_id,
        route: edit_event.route,
        pcb_port_id: edit_event.pcb_port_id,
        pcb_component_id: pcbPort?.pcb_component_id
      }
    ]);
  }
  return circuitJson;
};

// lib/utils/edit-events/apply-edit-events-to-circuit-json.ts
var applyEditEvents = ({
  circuitJson,
  editEvents
}) => {
  circuitJson = JSON.parse(JSON.stringify(circuitJson));
  for (const editEvent of editEvents) {
    if (editEvent.edit_event_type === "edit_pcb_component_location") {
      const component = circuitJson.find(
        (e) => e.type === "pcb_component" && e.pcb_component_id === editEvent.pcb_component_id
      );
      const needsMovement = !component || component.center.x !== editEvent.new_center.x || component.center.y !== editEvent.new_center.y;
      if (needsMovement && editEvent.original_center) {
        const mat = translate5(
          editEvent.new_center.x - editEvent.original_center.x,
          editEvent.new_center.y - editEvent.original_center.y
        );
        circuitJson = circuitJson.map(
          (e) => e.pcb_component_id !== editEvent.pcb_component_id ? e : transformPCBElement(e, mat)
        );
      }
    } else if (editEvent.edit_event_type === "edit_schematic_component_location") {
      circuitJson = circuitJson.map((e) => {
        if (e.type === "schematic_component" && e.schematic_component_id === editEvent.schematic_component_id) {
          return {
            ...e,
            center: editEvent.new_center
          };
        }
        return e;
      });
    } else if (editEvent.edit_event_type === "edit_pcb_trace_hint") {
      circuitJson = applyTraceHintEditEvent(circuitJson, editEvent);
    }
  }
  return circuitJson;
};

// lib/utils/autorouting/getSimpleRouteJsonFromCircuitJson.ts
import { su as su4 } from "@tscircuit/circuit-json-util";
import {
  getFullConnectivityMapFromCircuitJson as getFullConnectivityMapFromCircuitJson3
} from "circuit-json-to-connectivity-map";

// lib/components/primitive-components/Bus.ts
import { busProps } from "@tscircuit/props";
var Bus = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "Bus",
      zodProps: busProps
    };
  }
};

// lib/components/primitive-components/DifferentialPair.ts
import { differentialPairProps } from "@tscircuit/props";

// lib/components/primitive-components/DifferentialPair_doInitialSourceDesignRuleChecks.ts
var resolvePointToPointConnection = (differentialPair, connectionSelector) => {
  const { db } = differentialPair.root;
  const subcircuit = differentialPair.getSubcircuit();
  const subcircuitSourceTraces = db.source_trace.list().filter(
    (sourceTrace) => sourceTrace.subcircuit_id === subcircuit.subcircuit_id
  );
  const matchingSourceTraces = subcircuitSourceTraces.filter(
    (sourceTrace) => sourceTrace.name === connectionSelector
  );
  const connectivityMapKeys = /* @__PURE__ */ new Set();
  if (matchingSourceTraces.length > 0) {
    for (const sourceTrace of matchingSourceTraces) {
      if (sourceTrace.subcircuit_connectivity_map_key) {
        connectivityMapKeys.add(sourceTrace.subcircuit_connectivity_map_key);
      }
    }
  } else {
    const selectedPort = subcircuit.selectOne(connectionSelector, {
      type: "port"
    });
    if (selectedPort?.source_port_id) {
      const sourcePort = db.source_port.get(selectedPort.source_port_id);
      if (sourcePort?.subcircuit_connectivity_map_key) {
        connectivityMapKeys.add(sourcePort.subcircuit_connectivity_map_key);
      }
    }
  }
  if (connectivityMapKeys.size !== 1) return void 0;
  const connectivityMapKey = connectivityMapKeys.values().next().value;
  if (!connectivityMapKey) return void 0;
  const sourcePorts = db.source_port.list().filter(
    (sourcePort) => sourcePort.subcircuit_connectivity_map_key === connectivityMapKey
  );
  return {
    sourcePorts,
    sourceTraceName: matchingSourceTraces.length > 0 ? connectionSelector : void 0
  };
};
var formatTerminalPinList = (terminalPinSelectors) => {
  if (terminalPinSelectors.length <= 1) return terminalPinSelectors.join("");
  if (terminalPinSelectors.length === 2) {
    return `${terminalPinSelectors[0]} and ${terminalPinSelectors[1]}`;
  }
  return `${terminalPinSelectors.slice(0, -1).join(", ")}, and ${terminalPinSelectors.at(-1)}`;
};
var getTerminalPinSelector = (sourcePort, sourceComponentsById) => {
  let sourceComponent;
  if (sourcePort.source_component_id) {
    sourceComponent = sourceComponentsById.get(sourcePort.source_component_id);
  }
  if (!sourceComponent?.name) return sourcePort.source_port_id;
  const portName = sourcePort.most_frequently_referenced_by_name ?? sourcePort.name;
  return `.${sourceComponent.name} > .${portName}`;
};
var getPointToPointWarningMessage = ({
  differentialPairName,
  connectionPolarity,
  connectionSelector,
  sourceTraceName,
  terminalPinSelectors
}) => {
  const terminalList = formatTerminalPinList(terminalPinSelectors);
  const terminalListDescription = terminalList ? `: ${terminalList}` : "";
  if (sourceTraceName && terminalPinSelectors.length > 2) {
    return `Differential pair "${differentialPairName}" ${connectionPolarity}Connection uses the ambiguous trace name "${sourceTraceName}": its connection has more than 2 terminal pins${terminalListDescription}.`;
  }
  return `Differential pair "${differentialPairName}" ${connectionPolarity}Connection="${connectionSelector}" is not point-to-point: expected exactly 2 terminal pins, found ${terminalPinSelectors.length}${terminalListDescription}.`;
};
var DifferentialPair_doInitialSourceDesignRuleChecks = (differentialPair) => {
  const { db } = differentialPair.root;
  const sourceComponentsById = /* @__PURE__ */ new Map();
  for (const sourceComponent of db.source_component.list()) {
    sourceComponentsById.set(sourceComponent.source_component_id, {
      name: sourceComponent.name
    });
  }
  for (const connectionPolarity of ["positive", "negative"]) {
    let connectionSelector = differentialPair._parsedProps.negativeConnection;
    if (connectionPolarity === "positive") {
      connectionSelector = differentialPair._parsedProps.positiveConnection;
    }
    const resolvedConnection = resolvePointToPointConnection(
      differentialPair,
      connectionSelector
    );
    if (!resolvedConnection) continue;
    const terminalSourcePorts = resolvedConnection.sourcePorts;
    if (terminalSourcePorts.length === 2) continue;
    let warningSourceComponentId = "";
    const firstTerminalSourcePort = terminalSourcePorts[0];
    if (firstTerminalSourcePort) {
      if (!firstTerminalSourcePort.source_component_id) {
        throw new Error(
          `Differential pair "${differentialPair.name}" resolved terminal port "${firstTerminalSourcePort.source_port_id}" without a source_component_id`
        );
      }
      warningSourceComponentId = firstTerminalSourcePort.source_component_id;
    }
    const terminalPinSelectors = terminalSourcePorts.map(
      (sourcePort) => getTerminalPinSelector(sourcePort, sourceComponentsById)
    ).sort((selectorA, selectorB) => selectorA.localeCompare(selectorB));
    db.source_property_ignored_warning.insert({
      source_component_id: warningSourceComponentId,
      property_name: `${connectionPolarity}Connection`,
      error_type: "source_property_ignored_warning",
      message: getPointToPointWarningMessage({
        differentialPairName: differentialPair.name,
        connectionPolarity,
        connectionSelector,
        sourceTraceName: resolvedConnection.sourceTraceName,
        terminalPinSelectors
      }),
      subcircuit_id: differentialPair.getSubcircuit().subcircuit_id ?? void 0
    });
  }
};

// lib/components/primitive-components/DifferentialPair.ts
var DifferentialPair = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "DifferentialPair",
      zodProps: differentialPairProps
    };
  }
  doInitialSourceDesignRuleChecks() {
    DifferentialPair_doInitialSourceDesignRuleChecks(this);
  }
};

// lib/utils/autorouting/expand-srj-bounds-to-include-connection-points.ts
var expandSrjBoundsToIncludeConnectionPoints = (input) => {
  const expandedBounds = { ...input.bounds };
  for (const connection of input.connections) {
    if (connection.isOffBoard) continue;
    for (const point6 of connection.pointsToConnect) {
      expandedBounds.minX = Math.min(expandedBounds.minX, point6.x);
      expandedBounds.maxX = Math.max(expandedBounds.maxX, point6.x);
      expandedBounds.minY = Math.min(expandedBounds.minY, point6.y);
      expandedBounds.maxY = Math.max(expandedBounds.maxY, point6.y);
    }
  }
  return expandedBounds;
};

// lib/utils/autorouting/getBusesForSimpleRouteJson.ts
var getBusSourceTraceSubcircuitConnectivityMapKeyOrThrow = ({
  bus,
  busSourceTraces,
  traceNameOrPortSelector
}) => {
  const sourceTracesWithMatchingName = busSourceTraces.filter(
    (sourceTrace2) => sourceTrace2.name === traceNameOrPortSelector
  );
  const selectedPort = sourceTracesWithMatchingName.length === 0 ? bus.getSubcircuit().selectOne(traceNameOrPortSelector, {
    type: "port"
  }) : null;
  const selectedSourcePortId = selectedPort?.source_port_id ?? void 0;
  const matchingSourceTraces = selectedSourcePortId ? busSourceTraces.filter(
    (sourceTrace2) => sourceTrace2.connected_source_port_ids.includes(selectedSourcePortId)
  ) : sourceTracesWithMatchingName;
  if (matchingSourceTraces.length === 0) {
    throw new Error(
      `Could not find source trace for trace name or port selector "${traceNameOrPortSelector}" in bus "${bus.name}"`
    );
  }
  if (matchingSourceTraces.length > 1) {
    throw new Error(
      `Trace name or port selector "${traceNameOrPortSelector}" matches multiple source traces in bus "${bus.name}"`
    );
  }
  const sourceTrace = matchingSourceTraces[0];
  if (!sourceTrace?.subcircuit_connectivity_map_key) {
    throw new Error(
      `Source trace for "${traceNameOrPortSelector}" does not have a subcircuit connectivity map key in bus "${bus.name}"`
    );
  }
  return sourceTrace.subcircuit_connectivity_map_key;
};
var getBusSrjConnectionNamesOrThrow = ({
  srjConnections,
  bus,
  busSourceTraces,
  traceSubcircuitConnectivityMapKey,
  traceNameOrPortSelector
}) => {
  const sourceTraceIds = busSourceTraces.filter(
    (sourceTrace) => sourceTrace.subcircuit_connectivity_map_key === traceSubcircuitConnectivityMapKey
  ).map((sourceTrace) => sourceTrace.source_trace_id);
  const matchingSrjConnections = srjConnections.filter(
    (srjConnection) => srjConnection.source_trace_id && sourceTraceIds.includes(srjConnection.source_trace_id)
  );
  if (matchingSrjConnections.length === 0) {
    throw new Error(
      `Could not find an SRJ connection for "${traceNameOrPortSelector}" in bus "${bus.name}"`
    );
  }
  return matchingSrjConnections.map((srjConnection) => srjConnection.name);
};
var normalizeNetName = (netNameOrSelector) => netNameOrSelector.trim().replace(/^net\./, "").replace(/^\./, "");
var getPlaneTerminatedSourceTraceLayers = ({
  fanoutPourNetMap,
  sourceNets,
  sourceTraces,
  subcircuitId
}) => {
  const traceLayers = /* @__PURE__ */ new Map();
  if (!fanoutPourNetMap) return traceLayers;
  const planeLayerBySourceNetId = /* @__PURE__ */ new Map();
  for (const [layer, netOrNets] of Object.entries(fanoutPourNetMap)) {
    const netNames = Array.isArray(netOrNets) ? netOrNets : [netOrNets];
    for (const netNameOrSelector of netNames) {
      const netName = normalizeNetName(netNameOrSelector);
      for (const sourceNet of sourceNets) {
        if (sourceNet.name !== netName) continue;
        const previousLayer = planeLayerBySourceNetId.get(
          sourceNet.source_net_id
        );
        if (previousLayer && previousLayer !== layer) {
          throw new Error(
            `Fanout plane net "${netName}" maps to multiple layers ("${previousLayer}" and "${layer}"); use fanoutPourNetMap to select one`
          );
        }
        planeLayerBySourceNetId.set(sourceNet.source_net_id, layer);
      }
    }
  }
  for (const sourceTrace of sourceTraces) {
    if (subcircuitId && sourceTrace.subcircuit_id !== subcircuitId) continue;
    if (sourceTrace.connected_source_port_ids.length !== 1) continue;
    const mappedLayers = new Set(
      (sourceTrace.connected_source_net_ids ?? []).map((sourceNetId) => planeLayerBySourceNetId.get(sourceNetId)).filter((layer2) => layer2 !== void 0)
    );
    if (mappedLayers.size > 1) {
      throw new Error(
        `Source trace "${sourceTrace.name ?? sourceTrace.source_trace_id}" connects to fanout planes on multiple layers`
      );
    }
    const layer = mappedLayers.values().next().value;
    if (layer) traceLayers.set(sourceTrace.source_trace_id, layer);
  }
  return traceLayers;
};
var getBusesForSimpleRouteJson = ({
  srjConnections,
  buses,
  sourceTraces,
  subcircuitId,
  planeTerminatedSourceTraceLayers
}) => {
  const declaredSrjBuses = [];
  for (const bus of buses) {
    const busSubcircuitId = bus.getSubcircuit().subcircuit_id;
    if (subcircuitId && busSubcircuitId !== subcircuitId) continue;
    const busSourceTraces = sourceTraces.filter(
      (sourceTrace) => sourceTrace.subcircuit_id === busSubcircuitId
    );
    const connectionNames = bus._parsedProps.connections.flatMap(
      (traceNameOrPortSelector) => {
        const traceSubcircuitConnectivityMapKey = getBusSourceTraceSubcircuitConnectivityMapKeyOrThrow({
          bus,
          busSourceTraces,
          traceNameOrPortSelector
        });
        return getBusSrjConnectionNamesOrThrow({
          srjConnections,
          bus,
          busSourceTraces,
          traceSubcircuitConnectivityMapKey,
          traceNameOrPortSelector
        });
      }
    );
    if (new Set(connectionNames).size !== connectionNames.length) {
      throw new Error(
        `Bus "${bus.name}" resolves multiple entries to one trace`
      );
    }
    declaredSrjBuses.push({
      busId: bus.name,
      name: bus.name,
      connectionNames,
      ...bus._parsedProps.maxLengthSkew !== void 0 ? { maxLengthSkew: bus._parsedProps.maxLengthSkew } : {},
      ...bus._parsedProps.pcbTraceWidth !== void 0 ? { traceWidth: bus._parsedProps.pcbTraceWidth } : {},
      ...bus._parsedProps.pcbAllowedLayers !== void 0 ? { allowedLayers: bus._parsedProps.pcbAllowedLayers } : {}
    });
  }
  const planeSrjBuses = [];
  for (const [sourceTraceId, layer] of planeTerminatedSourceTraceLayers ?? []) {
    const sourceTrace = sourceTraces.find(
      (candidate) => candidate.source_trace_id === sourceTraceId
    );
    const srjConnection = srjConnections.find(
      (connection) => connection.source_trace_id === sourceTraceId
    );
    if (!sourceTrace || !srjConnection) continue;
    const busId = sourceTrace.name ?? sourceTrace.source_trace_id;
    if ([...declaredSrjBuses, ...planeSrjBuses].some((bus) => bus.busId === busId)) {
      throw new Error(
        `Fanout plane trace "${busId}" conflicts with an existing bus name`
      );
    }
    planeSrjBuses.push({
      busId,
      name: busId,
      connectionNames: [srjConnection.name],
      termination: { type: "plane", layer }
    });
  }
  const srjBuses = [...planeSrjBuses, ...declaredSrjBuses];
  return srjBuses.length > 0 ? srjBuses : void 0;
};

// lib/utils/autorouting/getDifferentialPairsForSimpleRouteJson.ts
var getDifferentialPairSourceTracesByTraceName = (differentialPairSourceTraces, traceName) => differentialPairSourceTraces.filter(
  (sourceTrace) => sourceTrace.name === traceName
);
var getDifferentialPairSourceTracesByPortId = (differentialPairSourceTraces, sourcePortId) => differentialPairSourceTraces.filter(
  (sourceTrace) => sourceTrace.connected_source_port_ids.includes(sourcePortId)
);
var getDifferentialPairTraceSubcircuitConnectivityMapKeyOrThrow = ({
  differentialPair,
  differentialPairSourceTraces,
  traceNameOrPortSelector
}) => {
  const differentialPairSubcircuit = differentialPair.getSubcircuit();
  const sourceTracesWithMatchingName = getDifferentialPairSourceTracesByTraceName(
    differentialPairSourceTraces,
    traceNameOrPortSelector
  );
  const selectedPort = sourceTracesWithMatchingName.length === 0 ? differentialPairSubcircuit.selectOne(traceNameOrPortSelector, {
    type: "port"
  }) : null;
  const selectedSourcePortId = selectedPort?.source_port_id ?? void 0;
  const matchingSourceTraces = selectedSourcePortId ? getDifferentialPairSourceTracesByPortId(
    differentialPairSourceTraces,
    selectedSourcePortId
  ) : sourceTracesWithMatchingName;
  if (matchingSourceTraces.length === 0) {
    throw new Error(
      `Could not find source trace for trace name or port selector "${traceNameOrPortSelector}" in differential pair "${differentialPair.name}"`
    );
  }
  if (matchingSourceTraces.length > 1) {
    throw new Error(
      `Trace name or port selector "${traceNameOrPortSelector}" matches multiple source traces for differential pair "${differentialPair.name}"`
    );
  }
  const sourceTrace = matchingSourceTraces[0];
  if (!sourceTrace) {
    throw new Error(
      `Expected one source trace for trace name or port selector "${traceNameOrPortSelector}" in differential pair "${differentialPair.name}"`
    );
  }
  const subcircuitConnectivityMapKey = sourceTrace.subcircuit_connectivity_map_key;
  if (!subcircuitConnectivityMapKey) {
    throw new Error(
      `Source trace "${sourceTrace.source_trace_id}" does not have a subcircuit connectivity map key for differential pair "${differentialPair.name}"`
    );
  }
  return subcircuitConnectivityMapKey;
};
var getDifferentialPairSrjConnectionNameOrThrow = ({
  srjConnections,
  differentialPairName,
  differentialPairSourceTraces,
  traceSubcircuitConnectivityMapKey,
  traceNameOrPortSelector
}) => {
  const differentialPairSourceTraceIds = [];
  for (const sourceTrace of differentialPairSourceTraces) {
    if (sourceTrace.subcircuit_connectivity_map_key === traceSubcircuitConnectivityMapKey) {
      differentialPairSourceTraceIds.push(sourceTrace.source_trace_id);
    }
  }
  const matchingSrjConnections = [];
  for (const srjConnection2 of srjConnections) {
    if (srjConnection2.source_trace_id && differentialPairSourceTraceIds.includes(srjConnection2.source_trace_id)) {
      matchingSrjConnections.push(srjConnection2);
    }
  }
  if (matchingSrjConnections.length === 0) {
    throw new Error(
      `Could not find an SRJ connection for trace name or port selector "${traceNameOrPortSelector}" in differential pair "${differentialPairName}"`
    );
  }
  if (matchingSrjConnections.length > 1) {
    throw new Error(
      `Subcircuit connectivity map key "${traceSubcircuitConnectivityMapKey}" matches multiple SRJ connections for differential pair "${differentialPairName}"`
    );
  }
  const srjConnection = matchingSrjConnections[0];
  if (!srjConnection) {
    throw new Error(
      `Expected one SRJ connection for subcircuit connectivity map key "${traceSubcircuitConnectivityMapKey}"`
    );
  }
  return srjConnection.name;
};
var getDifferentialPairsForSimpleRouteJson = ({
  srjConnections,
  differentialPairs,
  sourceTraces,
  subcircuitId
}) => {
  const srjDifferentialPairs = [];
  for (const differentialPair of differentialPairs) {
    const differentialPairSubcircuitId = differentialPair.getSubcircuit().subcircuit_id;
    if (subcircuitId && differentialPairSubcircuitId !== subcircuitId) {
      continue;
    }
    const differentialPairSourceTraces = sourceTraces.filter(
      (sourceTrace) => sourceTrace.subcircuit_id === differentialPairSubcircuitId
    );
    const positiveTraceNameOrPortSelector = differentialPair._parsedProps.positiveConnection;
    const negativeTraceNameOrPortSelector = differentialPair._parsedProps.negativeConnection;
    const positiveSubcircuitConnectivityMapKey = getDifferentialPairTraceSubcircuitConnectivityMapKeyOrThrow({
      differentialPair,
      differentialPairSourceTraces,
      traceNameOrPortSelector: positiveTraceNameOrPortSelector
    });
    const negativeSubcircuitConnectivityMapKey = getDifferentialPairTraceSubcircuitConnectivityMapKeyOrThrow({
      differentialPair,
      differentialPairSourceTraces,
      traceNameOrPortSelector: negativeTraceNameOrPortSelector
    });
    const positiveSrjConnectionName = getDifferentialPairSrjConnectionNameOrThrow({
      srjConnections,
      differentialPairName: differentialPair.name,
      differentialPairSourceTraces,
      traceSubcircuitConnectivityMapKey: positiveSubcircuitConnectivityMapKey,
      traceNameOrPortSelector: positiveTraceNameOrPortSelector
    });
    const negativeSrjConnectionName = getDifferentialPairSrjConnectionNameOrThrow({
      srjConnections,
      differentialPairName: differentialPair.name,
      differentialPairSourceTraces,
      traceSubcircuitConnectivityMapKey: negativeSubcircuitConnectivityMapKey,
      traceNameOrPortSelector: negativeTraceNameOrPortSelector
    });
    const lengthTolerance = differentialPair._parsedProps.maxLengthSkew ?? 0.1;
    srjDifferentialPairs.push({
      connectionNames: [positiveSrjConnectionName, negativeSrjConnectionName],
      lengthTolerance,
      ...differentialPair._parsedProps.pcbTraceGap !== void 0 ? { traceGap: differentialPair._parsedProps.pcbTraceGap } : {},
      ...differentialPair._parsedProps.maxUncoupledLength !== void 0 ? {
        maxUncoupledLength: differentialPair._parsedProps.maxUncoupledLength
      } : {}
    });
  }
  return srjDifferentialPairs.length > 0 ? srjDifferentialPairs : void 0;
};

// lib/utils/autorouting/getPreservedRoutedSubcircuitTraces.ts
var getLayerName = (layer) => typeof layer === "string" ? layer : layer.name;
var getPreservedTraceConnectionName = (trace) => trace.source_trace_id ?? trace.connection_name ?? trace.pcb_trace_id;
var getPhysicalConnectionIdsForPreservedTrace = (trace) => {
  const physicallyConnectedIds = new Set(trace.connectsTo ?? []);
  for (const routePoint of trace.route) {
    if (routePoint.route_type !== "wire") continue;
    for (const pcbPortId of [
      routePoint.start_pcb_port_id,
      routePoint.end_pcb_port_id
    ]) {
      if (pcbPortId) physicallyConnectedIds.add(pcbPortId);
    }
  }
  return Array.from(physicallyConnectedIds);
};
var getSimpleRouteForPreservedTrace = (trace) => trace.route.map((routePoint) => {
  if (routePoint.route_type === "wire") {
    return {
      route_type: "wire",
      x: routePoint.x,
      y: routePoint.y,
      width: routePoint.width,
      layer: getLayerName(routePoint.layer)
    };
  }
  if (routePoint.route_type === "via") {
    return {
      route_type: "via",
      x: routePoint.x,
      y: routePoint.y,
      from_layer: getLayerName(routePoint.from_layer),
      to_layer: getLayerName(routePoint.to_layer),
      ...routePoint.outer_diameter !== void 0 ? { via_diameter: routePoint.outer_diameter } : {},
      ...routePoint.hole_diameter !== void 0 ? { via_hole_diameter: routePoint.hole_diameter } : {}
    };
  }
  return {
    route_type: "through_obstacle",
    start: routePoint.start,
    end: routePoint.end,
    from_layer: getLayerName(routePoint.start_layer),
    to_layer: getLayerName(routePoint.end_layer),
    width: routePoint.width
  };
});
var getPreservedRoutedSubcircuitTraces = ({
  scopedDb,
  relevantSubcircuitIds
}) => scopedDb.pcb_trace.list().filter((trace) => {
  if (!trace.subcircuit_id) return false;
  return relevantSubcircuitIds === null || relevantSubcircuitIds.has(trace.subcircuit_id);
}).map((trace) => {
  const preservedTrace = trace;
  const connectionName = getPreservedTraceConnectionName(preservedTrace);
  return {
    type: "pcb_trace",
    pcb_trace_id: trace.pcb_trace_id,
    source_trace_id: trace.source_trace_id,
    connection_name: connectionName,
    connectsTo: getPhysicalConnectionIdsForPreservedTrace(preservedTrace),
    route: getSimpleRouteForPreservedTrace(preservedTrace)
  };
}).filter((trace) => trace.route.length >= 2);

// lib/utils/autorouting/getUnbrokenCopperPourObstacles.ts
var COPPER_POUR_RECT_HEIGHT = 0.5;
var dedupeStrings = (values) => Array.from(new Set(values.filter((value) => Boolean(value))));
var getLayerName2 = (layer) => typeof layer === "string" ? layer : layer.name;
var createCopperPourObstacle = ({
  layer,
  connectedTo,
  center,
  width,
  height
}) => ({
  type: "rect",
  layers: [layer],
  center,
  width,
  height,
  connectedTo,
  isCopperPour: true
});
var getCopperPourBoundary = ({
  outline,
  group,
  board
}) => {
  if (outline && outline.length >= 3) return outline;
  if (group?.outline && group.outline.length >= 3) return group.outline;
  if (board?.outline && board.outline.length >= 3) return board.outline;
  return null;
};
var getCopperPourBoundsRect = ({
  group,
  board
}) => {
  if (group?.width && group.height) {
    return {
      center: group.center,
      width: group.width,
      height: group.height
    };
  }
  if (board?.width && board.height) {
    return {
      center: board.center,
      width: board.width,
      height: board.height
    };
  }
  return null;
};
var getCopperPourConnectedTo = (net, connMap) => dedupeStrings([
  net.source_net_id,
  net.subcircuit_connectivity_map_key,
  net.source_net_id ? connMap.getNetConnectedToId(net.source_net_id) : null
]);
var getUnbrokenCopperPourObstacles = ({
  connMap,
  subcircuitComponent,
  board,
  group
}) => {
  if (!subcircuitComponent) return [];
  const obstacles = [];
  const copperPours = subcircuitComponent.selectAll(
    "copperpour"
  );
  for (const copperPour of copperPours) {
    const props = copperPour._parsedProps;
    if (!props.unbroken) continue;
    const net = copperPour.getSubcircuit().selectOne(props.connectsTo);
    if (!net?.source_net_id) continue;
    const layer = getLayerName2(props.layer);
    const connectedTo = getCopperPourConnectedTo(net, connMap);
    if (connectedTo.length === 0) continue;
    const boundary = getCopperPourBoundary({
      outline: props.outline,
      group,
      board
    });
    if (boundary) {
      const rects = fillPolygonWithRects(boundary, {
        rectHeight: COPPER_POUR_RECT_HEIGHT
      });
      if (rects.length > 0) {
        obstacles.push(
          ...rects.map(
            (rect) => createCopperPourObstacle({
              layer,
              connectedTo,
              center: rect.center,
              width: rect.width,
              height: rect.height
            })
          )
        );
        continue;
      }
    }
    const boundsRect = getCopperPourBoundsRect({ group, board });
    if (!boundsRect) continue;
    obstacles.push(
      createCopperPourObstacle({
        layer,
        connectedTo,
        center: boundsRect.center,
        width: boundsRect.width,
        height: boundsRect.height
      })
    );
  }
  return obstacles;
};

// lib/utils/autorouting/getSimpleRouteJsonFromCircuitJson.ts
var getSimpleRouteJsonFromCircuitJson = ({
  db,
  circuitJson,
  subcircuit_id,
  minTraceWidth,
  minTraceToPadEdgeClearance,
  minViaEdgeToPadEdgeClearance,
  minViaHoleEdgeToViaHoleEdgeClearance,
  minPlatedHoleDrillEdgeToDrillEdgeClearance,
  minPadEdgeToPadEdgeClearance,
  minBoardEdgeClearance,
  minViaHoleDiameter,
  minViaPadDiameter,
  nominalTraceWidth,
  subcircuitComponent,
  fanoutPourNetMap,
  ignoreExistingTopLevelPcbRouteState = false
}) => {
  if (!db && circuitJson) {
    db = su4(circuitJson);
  }
  if (!db) {
    throw new Error("db or circuitJson is required");
  }
  const traceHints = db.pcb_trace_hint.list();
  const relevantSubcircuitIds = subcircuit_id ? /* @__PURE__ */ new Set([subcircuit_id]) : null;
  if (subcircuit_id) {
    const descendantSubcircuitIds = getDescendantSubcircuitIds(
      db,
      subcircuit_id
    );
    for (const id of descendantSubcircuitIds) {
      relevantSubcircuitIds.add(id);
    }
  }
  const subcircuitElements = (circuitJson ?? db.toArray()).filter(
    (e) => !subcircuit_id || "subcircuit_id" in e && relevantSubcircuitIds.has(e.subcircuit_id)
  );
  let board = null;
  let subcircuitIsBoard = false;
  if (subcircuit_id) {
    const source_group_id = subcircuit_id.replace(/^subcircuit_/, "");
    const source_board = db.source_board.getWhere({ source_group_id });
    if (source_board) {
      board = db.pcb_board.getWhere({
        source_board_id: source_board.source_board_id
      });
      if (board) subcircuitIsBoard = true;
    }
  }
  if (!board) {
    board = db.pcb_board.list()[0];
  }
  db = su4(subcircuitElements);
  const getPortSelector = (sourcePort) => {
    if (!sourcePort?.source_component_id) return void 0;
    const sourceComponent = db.source_component.get(
      sourcePort.source_component_id
    );
    if (!sourceComponent?.name) return void 0;
    return `${sourceComponent.name}.${sourcePort.name}`;
  };
  const pcbGroup = subcircuit_id ? db.pcb_group.getWhere({ subcircuit_id }) : void 0;
  const activeRoutingPcbGroupId = subcircuitComponent?.pcb_group_id ?? (!subcircuitIsBoard ? pcbGroup?.pcb_group_id : void 0);
  const sharedConnMap = getFullConnectivityMapFromCircuitJson3(subcircuitElements);
  const breakoutPoints = db.pcb_breakout_point.list().filter(
    (bp) => !subcircuit_id || relevantSubcircuitIds?.has(bp.subcircuit_id)
  );
  const breakoutPointIsInActiveRoutingGroup = (breakoutPoint) => breakoutPoint.subcircuit_id === subcircuit_id && (activeRoutingPcbGroupId ? breakoutPoint.pcb_group_id === activeRoutingPcbGroupId : !subcircuitIsBoard);
  const obstacles = getObstaclesFromCircuitJson(
    [
      ...board ? [board] : [],
      ...db.source_component.list(),
      ...db.source_port.list(),
      ...db.pcb_port.list(),
      ...db.pcb_component.list(),
      ...db.pcb_smtpad.list(),
      ...db.pcb_plated_hole.list(),
      ...db.pcb_hole.list(),
      // Footprint copper primitives such as solder-jumper bridges are fixed.
      ...db.pcb_trace.list().filter((trace) => !trace.source_trace_id),
      ...db.pcb_via.list().filter(
        (via) => !ignoreExistingTopLevelPcbRouteState || Boolean(via.subcircuit_id)
      ),
      ...db.pcb_keepout.list(),
      ...db.pcb_cutout.list()
    ].filter(
      (e) => e.type === "pcb_board" || !subcircuit_id || relevantSubcircuitIds?.has(e.subcircuit_id)
    ),
    sharedConnMap
  );
  obstacles.push(
    ...getUnbrokenCopperPourObstacles({
      connMap: sharedConnMap,
      subcircuitComponent,
      board,
      group: pcbGroup
    })
  );
  const preservedRoutedSubcircuitTraces = getPreservedRoutedSubcircuitTraces({
    scopedDb: db,
    relevantSubcircuitIds
  });
  for (const obstacle of obstacles) {
    const additionalIds = obstacle.connectedTo.flatMap(
      (id) => sharedConnMap.getIdsConnectedToNet(id)
    );
    obstacle.connectedTo.push(...additionalIds);
  }
  const internalConnections = db.source_component_internal_connection.list();
  const sourcePortIdToInternalConnectionId = /* @__PURE__ */ new Map();
  for (const ic of internalConnections) {
    for (const sourcePortId of ic.source_port_ids) {
      sourcePortIdToInternalConnectionId.set(
        sourcePortId,
        ic.source_component_internal_connection_id
      );
    }
  }
  const pcbElementIdToSourcePortId = /* @__PURE__ */ new Map();
  for (const pcbPort of db.pcb_port.list()) {
    if (pcbPort.source_port_id) {
      const smtpad = db.pcb_smtpad.getWhere({
        pcb_port_id: pcbPort.pcb_port_id
      });
      if (smtpad) {
        pcbElementIdToSourcePortId.set(
          smtpad.pcb_smtpad_id,
          pcbPort.source_port_id
        );
      }
      const platedHole = db.pcb_plated_hole.getWhere({
        pcb_port_id: pcbPort.pcb_port_id
      });
      if (platedHole) {
        pcbElementIdToSourcePortId.set(
          platedHole.pcb_plated_hole_id,
          pcbPort.source_port_id
        );
      }
    }
  }
  for (const obstacle of obstacles) {
    for (const connectedId of obstacle.connectedTo) {
      const sourcePortId = pcbElementIdToSourcePortId.get(connectedId);
      if (sourcePortId) {
        const internalConnectionId = sourcePortIdToInternalConnectionId.get(sourcePortId);
        if (internalConnectionId) {
          obstacle.offBoardConnectsTo = [internalConnectionId];
          obstacle.netIsAssignable = true;
          break;
        }
      }
    }
  }
  const allPoints = obstacles.flatMap((o) => [
    {
      x: o.center.x - o.width / 2,
      y: o.center.y - o.height / 2
    },
    {
      x: o.center.x + o.width / 2,
      y: o.center.y + o.height / 2
    }
  ]).concat(board?.outline ?? []);
  let bounds;
  const useGroupBoundsAsSrjBounds = !!(pcbGroup?.width && pcbGroup.height && subcircuit_id && !subcircuitIsBoard);
  if (useGroupBoundsAsSrjBounds) {
    bounds = {
      minX: pcbGroup.center.x - pcbGroup.width / 2,
      maxX: pcbGroup.center.x + pcbGroup.width / 2,
      minY: pcbGroup.center.y - pcbGroup.height / 2,
      maxY: pcbGroup.center.y + pcbGroup.height / 2
    };
  } else if (board && !board.outline) {
    bounds = {
      minX: board.center.x - board.width / 2,
      maxX: board.center.x + board.width / 2,
      minY: board.center.y - board.height / 2,
      maxY: board.center.y + board.height / 2
    };
  } else {
    bounds = {
      minX: Math.min(...allPoints.map((p) => p.x)) - 1,
      maxX: Math.max(...allPoints.map((p) => p.x)) + 1,
      minY: Math.min(...allPoints.map((p) => p.y)) - 1,
      maxY: Math.max(...allPoints.map((p) => p.y)) + 1
    };
  }
  if (pcbGroup?.width && pcbGroup.height && !useGroupBoundsAsSrjBounds) {
    const groupBounds = {
      minX: pcbGroup.center.x - pcbGroup.width / 2,
      maxX: pcbGroup.center.x + pcbGroup.width / 2,
      minY: pcbGroup.center.y - pcbGroup.height / 2,
      maxY: pcbGroup.center.y + pcbGroup.height / 2
    };
    bounds = {
      minX: Math.min(bounds.minX, groupBounds.minX),
      maxX: Math.max(bounds.maxX, groupBounds.maxX),
      minY: Math.min(bounds.minY, groupBounds.minY),
      maxY: Math.max(bounds.maxY, groupBounds.maxY)
    };
  }
  const sourceTraceIdsAlreadyPreservedAsSrjTraces = new Set(
    db.pcb_trace.list().filter((t) => {
      if (!t.source_trace_id) return false;
      if (subcircuit_id) return t.subcircuit_id === subcircuit_id;
      if (!t.subcircuit_id) return false;
      const sourceTrace = db.source_trace.get(t.source_trace_id);
      return sourceTrace?.subcircuit_id === t.subcircuit_id;
    }).map((t) => t.source_trace_id).filter((id) => Boolean(id))
  );
  const sourcePortIdToBreakoutPoint = /* @__PURE__ */ new Map();
  for (const bp of breakoutPoints) {
    const spId = bp.source_port_id;
    if (spId) sourcePortIdToBreakoutPoint.set(spId, bp);
  }
  const sourceTraces = db.source_trace.list();
  const planeTerminatedSourceTraceLayers = getPlaneTerminatedSourceTraceLayers({
    fanoutPourNetMap,
    sourceNets: db.source_net.list(),
    sourceTraces,
    subcircuitId: subcircuit_id
  });
  const directTraceConnections = sourceTraces.filter(
    (trace) => !sourceTraceIdsAlreadyPreservedAsSrjTraces.has(trace.source_trace_id)
  ).filter(
    (trace) => !subcircuit_id || trace.subcircuit_id === subcircuit_id
  ).map((trace) => {
    const connectedPorts = trace.connected_source_port_ids.map((id) => {
      const source_port = db.source_port.get(id);
      const pcb_port = db.pcb_port.getWhere({ source_port_id: id });
      return {
        ...source_port,
        ...pcb_port
      };
    });
    const isPlaneTerminatedSourceTrace = planeTerminatedSourceTraceLayers.has(
      trace.source_trace_id
    );
    if (connectedPorts.length < 2 && !(isPlaneTerminatedSourceTrace && connectedPorts.length === 1)) {
      return null;
    }
    for (const connectedPort of connectedPorts) {
      if (connectedPort.x === void 0 || connectedPort.y === void 0) {
        console.error(
          `(source_port_id: ${connectedPort.source_port_id}) for trace ${trace.source_trace_id} does not have x/y coordinates. Skipping this trace.`
        );
        return null;
      }
    }
    const connectedPcbPortIds = new Set(
      connectedPorts.map((port) => port.pcb_port_id)
    );
    const matchingHints = traceHints.filter(
      (hint) => connectedPcbPortIds.has(hint.pcb_port_id)
    );
    const hintPoints = [];
    for (const hint of matchingHints) {
      const port = db.pcb_port.get(hint.pcb_port_id);
      const layer = port?.layers?.[0] ?? "top";
      for (const pt of hint.route) {
        hintPoints.push({
          x: pt.x,
          y: pt.y,
          layer
        });
      }
    }
    const getPortOrBreakoutPoint = (port, layer, sourcePortId) => {
      const bp = sourcePortIdToBreakoutPoint.get(sourcePortId);
      const portSelector = getPortSelector(db.source_port.get(sourcePortId));
      if (bp && !breakoutPointIsInActiveRoutingGroup(bp)) {
        return {
          x: bp.x,
          y: bp.y,
          layer,
          pointId: bp.pcb_breakout_point_id,
          port_selector: portSelector
        };
      }
      return {
        x: port.x,
        y: port.y,
        layer,
        pointId: port.pcb_port_id,
        pcb_port_id: port.pcb_port_id,
        port_selector: portSelector
      };
    };
    const connectedPortRoutePoints = connectedPorts.map(
      (port, index) => getPortOrBreakoutPoint(
        port,
        port.layers?.[0] ?? "top",
        trace.connected_source_port_ids[index]
      )
    );
    const [firstConnectedPortRoutePoint, ...remainingPortRoutePoints] = connectedPortRoutePoints;
    return {
      name: trace.source_trace_id ?? sharedConnMap.getNetConnectedToId(trace.source_trace_id) ?? "",
      source_trace_id: trace.source_trace_id,
      nominalTraceWidth: trace.min_trace_thickness,
      width: trace.min_trace_thickness,
      // Simple Route JSON connections are multi-terminal, so retain every
      // source trace endpoint in the autorouter input.
      pointsToConnect: [
        firstConnectedPortRoutePoint,
        ...hintPoints,
        ...remainingPortRoutePoints
      ]
    };
  }).filter((c) => c !== null);
  const source_nets = db.source_net.list().filter((e) => !subcircuit_id || e.subcircuit_id === subcircuit_id);
  const connectionsFromNets = [];
  const connectionFromNetId = /* @__PURE__ */ new Map();
  const handledNetConnectivityKeys = /* @__PURE__ */ new Set();
  const sourceNetIds = new Set(source_nets.map((net) => net.source_net_id));
  const currentSubcircuitSourceTraces = db.source_trace.list().filter((trace) => !subcircuit_id || trace.subcircuit_id === subcircuit_id);
  const exposedBridgeSourceTraceIds = new Set(
    (subcircuitComponent?.selectAll("trace") ?? []).flatMap((trace) => {
      const candidate = trace;
      return candidate._exposesSubcircuitConnection && candidate.source_trace_id ? [candidate.source_trace_id] : [];
    })
  );
  const exposedDescendantSourceNetIds = /* @__PURE__ */ new Set();
  for (const trace of currentSubcircuitSourceTraces) {
    if (!exposedBridgeSourceTraceIds.has(trace.source_trace_id)) {
      continue;
    }
    for (const sourceNetId of trace.connected_source_net_ids ?? []) {
      if (!sourceNetIds.has(sourceNetId)) {
        exposedDescendantSourceNetIds.add(sourceNetId);
      }
    }
  }
  const routedDescendantSourceTraceIds = new Set(
    subcircuit_id ? db.pcb_trace.list().filter(
      (trace) => trace.subcircuit_id && trace.subcircuit_id !== subcircuit_id && relevantSubcircuitIds?.has(trace.subcircuit_id)
    ).map((trace) => trace.source_trace_id).filter((id) => Boolean(id)) : []
  );
  const sourceTracesEligibleForNetConnections = db.source_trace.list().filter(
    (trace) => !planeTerminatedSourceTraceLayers.has(trace.source_trace_id)
  ).filter(
    (trace) => (
      // Existing copper must still contribute endpoint connectivity when it
      // belongs to the scope currently being routed. Descendant copper is
      // preserved as static geometry only, preventing it from being routed
      // a second time by its parent.
      !sourceTraceIdsAlreadyPreservedAsSrjTraces.has(trace.source_trace_id) || subcircuit_id != null && trace.subcircuit_id === subcircuit_id
    )
  ).filter(
    (trace) => !subcircuit_id || trace.subcircuit_id === subcircuit_id || (trace.connected_source_net_ids?.some((id) => sourceNetIds.has(id)) ?? false) || (trace.connected_source_net_ids?.some(
      (id) => exposedDescendantSourceNetIds.has(id)
    ) ?? false) && !routedDescendantSourceTraceIds.has(trace.source_trace_id)
  );
  const getSourceConnectivityKey = (id) => id ? sharedConnMap.getNetConnectedToId(id) ?? id : null;
  for (const net of source_nets) {
    const netConnectivityKey = getSourceConnectivityKey(net.source_net_id);
    if (!netConnectivityKey || handledNetConnectivityKeys.has(netConnectivityKey)) {
      continue;
    }
    handledNetConnectivityKeys.add(netConnectivityKey);
    const connectedSourceNetIds = source_nets.filter(
      (sourceNet) => getSourceConnectivityKey(sourceNet.source_net_id) === netConnectivityKey
    ).map((sourceNet) => sourceNet.source_net_id);
    const connectedSourceTraces = sourceTracesEligibleForNetConnections.filter(
      (st) => [st.source_trace_id, ...st.connected_source_net_ids ?? []].some(
        (id) => getSourceConnectivityKey(id) === netConnectivityKey
      )
    );
    let nominalTraceWidthFromConnectedTraces;
    for (const sourceTrace of connectedSourceTraces) {
      if (sourceTrace.min_trace_thickness === void 0) continue;
      nominalTraceWidthFromConnectedTraces = Math.max(
        nominalTraceWidthFromConnectedTraces ?? 0,
        sourceTrace.min_trace_thickness
      );
    }
    const pointsToConnect = [];
    const addedPointIds = /* @__PURE__ */ new Set();
    for (const st of connectedSourceTraces) {
      const pcb_ports = db.pcb_port.list().filter((p) => st.connected_source_port_ids.includes(p.source_port_id));
      for (const p of pcb_ports) {
        if (addedPointIds.has(p.pcb_port_id)) continue;
        addedPointIds.add(p.pcb_port_id);
        pointsToConnect.push({
          x: p.x,
          y: p.y,
          layer: p.layers?.[0] ?? "top",
          pointId: p.pcb_port_id,
          pcb_port_id: p.pcb_port_id,
          port_selector: getPortSelector(db.source_port.get(p.source_port_id))
        });
      }
    }
    const connection = {
      name: net.source_net_id ?? sharedConnMap.getNetConnectedToId(net.source_net_id),
      nominalTraceWidth: nominalTraceWidthFromConnectedTraces,
      width: nominalTraceWidthFromConnectedTraces,
      pointsToConnect
    };
    if (pointsToConnect.length === 0) continue;
    connectionsFromNets.push(connection);
    for (const sourceNetId of connectedSourceNetIds) {
      connectionFromNetId.set(sourceNetId, connection);
    }
  }
  const connectionsFromBreakoutPoints = [];
  for (const bp of breakoutPoints) {
    const bpSourcePortId = bp.source_port_id;
    const pt = {
      x: bp.x,
      y: bp.y,
      layer: "top",
      pointId: bp.pcb_breakout_point_id
    };
    if (bpSourcePortId) {
      const pcb_port = db.pcb_port.getWhere({
        source_port_id: bpSourcePortId
      });
      if (!pcb_port) continue;
      const portPt = {
        x: pcb_port.x,
        y: pcb_port.y,
        layer: pcb_port.layers?.[0] ?? "top",
        pointId: pcb_port.pcb_port_id,
        pcb_port_id: pcb_port.pcb_port_id,
        port_selector: getPortSelector(
          db.source_port.get(pcb_port.source_port_id)
        )
      };
      const isInActiveRoutingGroup = breakoutPointIsInActiveRoutingGroup(bp);
      const isInTransparentChildRoutingGroup = !isInActiveRoutingGroup && bp.subcircuit_id === subcircuit_id;
      if (isInActiveRoutingGroup || isInTransparentChildRoutingGroup) {
        connectionsFromBreakoutPoints.push({
          name: isInTransparentChildRoutingGroup ? `breakout:${bp.pcb_breakout_point_id}` : bpSourcePortId,
          source_trace_id: bp.source_trace_id,
          routingPcbGroupId: isInTransparentChildRoutingGroup ? bp.pcb_group_id : void 0,
          pointsToConnect: [portPt, pt]
        });
        continue;
      }
      if (!bp.source_trace_id) {
        connectionsFromBreakoutPoints.push({
          name: bpSourcePortId,
          pointsToConnect: [portPt, pt]
        });
      }
      continue;
    }
    if (bp.source_net_id) {
      const conn = connectionFromNetId.get(bp.source_net_id);
      if (conn) {
        conn.pointsToConnect.push(pt);
      } else {
        connectionsFromBreakoutPoints.push({
          name: bp.source_net_id,
          pointsToConnect: [pt]
        });
      }
    }
  }
  const allConns = [
    ...directTraceConnections,
    ...connectionsFromNets,
    ...connectionsFromBreakoutPoints
  ];
  const defaultTraceWidth = minTraceWidth ?? board?.min_trace_width ?? 0.1;
  for (const conn of allConns) {
    conn.nominalTraceWidth ??= nominalTraceWidth ?? defaultTraceWidth;
    conn.width ??= nominalTraceWidth ?? defaultTraceWidth;
  }
  bounds = expandSrjBoundsToIncludeConnectionPoints({
    bounds,
    connections: allConns
  });
  const differentialPairs = subcircuitComponent?.selectAll("differentialpair") ?? [];
  const buses = subcircuitComponent?.selectAll("bus") ?? [];
  const srjDifferentialPairs = getDifferentialPairsForSimpleRouteJson({
    srjConnections: allConns,
    differentialPairs,
    sourceTraces: db.source_trace.list(),
    subcircuitId: subcircuit_id
  });
  const srjBuses = getBusesForSimpleRouteJson({
    srjConnections: allConns,
    buses,
    sourceTraces: db.source_trace.list(),
    subcircuitId: subcircuit_id,
    planeTerminatedSourceTraceLayers
  });
  if (subcircuit_id) {
    const pointIdToConn = /* @__PURE__ */ new Map();
    for (const conn of allConns) {
      for (const pt of conn.pointsToConnect) {
        if (pt.pointId) pointIdToConn.set(pt.pointId, conn);
      }
    }
    const existingTraces = db.pcb_trace.list().filter((t) => {
      return relevantSubcircuitIds?.has(t.subcircuit_id);
    });
    for (const tr of existingTraces) {
      const tracePortIds = /* @__PURE__ */ new Set();
      for (const seg of tr.route) {
        if (seg.start_pcb_port_id) tracePortIds.add(seg.start_pcb_port_id);
        if (seg.end_pcb_port_id) tracePortIds.add(seg.end_pcb_port_id);
      }
      if (tracePortIds.size < 2) continue;
      const firstId = tracePortIds.values().next().value;
      if (!firstId) continue;
      const conn = pointIdToConn.get(firstId);
      if (!conn) continue;
      if (![...tracePortIds].every((pid) => pointIdToConn.get(pid) === conn)) {
        continue;
      }
      conn.externallyConnectedPointIds ??= [];
      conn.externallyConnectedPointIds.push([...tracePortIds]);
    }
  }
  const resolvedMinViaHoleDiameter = minViaHoleDiameter ?? board?.min_via_hole_diameter;
  const resolvedMinViaPadDiameter = minViaPadDiameter ?? board?.min_via_pad_diameter;
  const resolvedMinTraceToPadEdgeClearance = minTraceToPadEdgeClearance ?? board?.min_trace_to_pad_edge_clearance;
  const resolvedMinViaEdgeToPadEdgeClearance = minViaEdgeToPadEdgeClearance ?? board?.min_via_edge_to_pad_edge_clearance;
  const resolvedMinViaHoleEdgeToViaHoleEdgeClearance = minViaHoleEdgeToViaHoleEdgeClearance ?? board?.min_via_hole_edge_to_via_hole_edge_clearance;
  const resolvedMinPlatedHoleDrillEdgeToDrillEdgeClearance = minPlatedHoleDrillEdgeToDrillEdgeClearance ?? board?.min_plated_hole_drill_edge_to_drill_edge_clearance;
  const resolvedMinPadEdgeToPadEdgeClearance = minPadEdgeToPadEdgeClearance ?? board?.min_pad_edge_to_pad_edge_clearance;
  const resolvedMinBoardEdgeClearance = minBoardEdgeClearance ?? board?.min_board_edge_clearance;
  return {
    simpleRouteJson: {
      bounds,
      obstacles,
      connections: allConns,
      differentialPairs: srjDifferentialPairs,
      ...srjBuses ? { buses: srjBuses } : {},
      traces: preservedRoutedSubcircuitTraces.length > 0 ? preservedRoutedSubcircuitTraces : void 0,
      layerCount: board?.num_layers ?? 2,
      minTraceWidth: Math.min(
        defaultTraceWidth,
        ...allConns.map((c) => c.width)
      ),
      minViaDiameter: resolvedMinViaPadDiameter,
      minViaHoleDiameter: resolvedMinViaHoleDiameter,
      minViaPadDiameter: resolvedMinViaPadDiameter,
      min_via_hole_diameter: resolvedMinViaHoleDiameter,
      min_via_pad_diameter: resolvedMinViaPadDiameter,
      minTraceToPadEdgeClearance: resolvedMinTraceToPadEdgeClearance,
      minViaEdgeToPadEdgeClearance: resolvedMinViaEdgeToPadEdgeClearance,
      minViaHoleEdgeToViaHoleEdgeClearance: resolvedMinViaHoleEdgeToViaHoleEdgeClearance,
      minPlatedHoleDrillEdgeToDrillEdgeClearance: resolvedMinPlatedHoleDrillEdgeToDrillEdgeClearance,
      minPadEdgeToPadEdgeClearance: resolvedMinPadEdgeToPadEdgeClearance,
      minBoardEdgeClearance: resolvedMinBoardEdgeClearance,
      nominalTraceWidth,
      outline: board?.outline?.map((point6) => ({ ...point6 }))
    },
    connMap: sharedConnMap
  };
};

// lib/utils/autorouting/unrouteCircuitJson.ts
var unroutedElementTypes = /* @__PURE__ */ new Set([
  "pcb_trace",
  "pcb_trace_error"
]);
var unrouteCircuitJson = (circuitJson) => {
  return circuitJson.filter(
    (element) => !unroutedElementTypes.has(element.type)
  );
};

// lib/utils/render-events/getPhaseTimingsFromRenderEvents.ts
var getPhaseTimingsFromRenderEvents = (renderEvents) => {
  const phaseTimings = {};
  if (!renderEvents) return phaseTimings;
  for (const renderPhase of orderedRenderPhases) {
    phaseTimings[renderPhase] = 0;
  }
  const startEvents = /* @__PURE__ */ new Map();
  for (const event of renderEvents) {
    const [, , phase, eventType] = event.type.split(":");
    if (eventType === "start") {
      startEvents.set(`${phase}:${event.renderId}`, event);
      continue;
    }
    if (eventType === "end") {
      const startEvent = startEvents.get(`${phase}:${event.renderId}`);
      if (startEvent) {
        const duration = event.createdAt - startEvent.createdAt;
        phaseTimings[phase] = (phaseTimings[phase] || 0) + duration;
      }
    }
  }
  return phaseTimings;
};

// lib/utils/schematic/normalizePinLabels.ts
var normalizePinLabels = (inputPinLabels) => {
  const unqInputPinLabels = inputPinLabels.map((labels) => [...new Set(labels)]);
  const result = unqInputPinLabels.map(() => []);
  const desiredNumbers = unqInputPinLabels.map(() => null);
  for (let i = 0; i < unqInputPinLabels.length; i++) {
    for (const label of unqInputPinLabels[i]) {
      if (/^\d+$/.test(label)) {
        desiredNumbers[i] = Number.parseInt(label);
        break;
      }
    }
  }
  let highestPinNumber = 0;
  const alreadyAcceptedDesiredNumbers = /* @__PURE__ */ new Set();
  for (let i = 0; i < desiredNumbers.length; i++) {
    const desiredNumber = desiredNumbers[i];
    if (desiredNumber === null || desiredNumber < 1) {
      continue;
    }
    if (!alreadyAcceptedDesiredNumbers.has(desiredNumber)) {
      alreadyAcceptedDesiredNumbers.add(desiredNumber);
      result[i].push(`pin${desiredNumber}`);
      highestPinNumber = Math.max(highestPinNumber, desiredNumber);
      continue;
    }
    let existingAltsForPin = 0;
    for (const label of result[i]) {
      if (label.startsWith(`pin${desiredNumber}_alt`)) {
        existingAltsForPin++;
      }
    }
    result[i].push(`pin${desiredNumber}_alt${existingAltsForPin + 1}`);
  }
  for (let i = 0; i < result.length; i++) {
    const firstLabel = result[i][0];
    if (firstLabel?.includes("_alt")) {
      highestPinNumber++;
      result[i].unshift(`pin${highestPinNumber}`);
    }
  }
  for (let i = 0; i < result.length; i++) {
    if (result[i].length === 0) {
      highestPinNumber++;
      result[i].push(`pin${highestPinNumber}`);
    }
  }
  const totalLabelCounts = {};
  for (const inputLabels of unqInputPinLabels) {
    for (const label of inputLabels) {
      if (/^\d+$/.test(label)) {
        continue;
      }
      totalLabelCounts[label] = (totalLabelCounts[label] ?? 0) + 1;
    }
  }
  const incrementalLabelCounts = {};
  for (let i = 0; i < unqInputPinLabels.length; i++) {
    const inputLabels = unqInputPinLabels[i];
    for (const label of inputLabels) {
      if (/^\d+$/.test(label)) {
        continue;
      }
      if (totalLabelCounts[label] === 1) {
        result[i].push(label);
      } else {
        incrementalLabelCounts[label] = (incrementalLabelCounts[label] ?? 0) + 1;
        result[i].push(`${label}${incrementalLabelCounts[label]}`);
      }
    }
  }
  return result;
};

// lib/components/primitive-components/Group/Group.ts
import "zod";

// lib/components/primitive-components/TraceHint.ts
import { traceHintProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint24 } from "transformation-matrix";
var TraceHint = class extends PrimitiveComponent2 {
  matchedPort = null;
  get config() {
    return {
      componentName: "TraceHint",
      zodProps: traceHintProps
    };
  }
  doInitialPortMatching() {
    const { db } = this.root;
    const { _parsedProps: props, parent } = this;
    if (!parent) return;
    if (parent.componentName === "Trace") {
      this.renderError(
        `Port inference inside trace is not yet supported (${this})`
      );
      return;
    }
    if (!parent) throw new Error("TraceHint has no parent");
    if (!props.for) {
      this.renderError(`TraceHint has no for property (${this})`);
      return;
    }
    const port = parent.selectOne(props.for, { type: "port" });
    if (!port) {
      this.renderError(
        `${this} could not find port for selector "${props.for}"`
      );
    }
    this.matchedPort = port;
    port.registerMatch(this);
  }
  getPcbRouteHints() {
    const { _parsedProps: props } = this;
    const offsets = props.offset ? [props.offset] : props.offsets;
    if (!offsets) return [];
    const globalTransform = this._computePcbGlobalTransformBeforeLayout();
    return offsets.map(
      (offset) => ({
        ...applyToPoint24(globalTransform, offset),
        via: offset.via,
        to_layer: offset.to_layer,
        trace_width: offset.trace_width
      })
    );
  }
  doInitialPcbTraceHintRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    db.pcb_trace_hint.insert({
      pcb_component_id: this.matchedPort?.pcb_component_id,
      pcb_port_id: this.matchedPort?.pcb_port_id,
      route: this.getPcbRouteHints()
    });
  }
};

// lib/components/primitive-components/SchematicBox/SchematicBox.ts
import { schematicBoxProps } from "@tscircuit/props";

// lib/components/primitive-components/getTitleAnchorAndPosition.ts
function getTitleAnchorAndPosition({
  anchor,
  x,
  y,
  width,
  height,
  isInside
}) {
  switch (anchor) {
    case "top_left":
      return {
        x,
        y: y + height,
        textAnchor: isInside ? "top_left" : "bottom_left"
      };
    case "top_center":
      return {
        x: x + width / 2,
        y: y + height,
        textAnchor: isInside ? "top_center" : "bottom_center"
      };
    case "top_right":
      return {
        x: x + width,
        y: y + height,
        textAnchor: isInside ? "top_right" : "bottom_right"
      };
    case "center_left":
      return {
        x,
        y: y + height / 2,
        textAnchor: isInside ? "center_left" : "center_right"
      };
    case "center":
      return {
        x: x + width / 2,
        y: y + height / 2,
        textAnchor: "center"
      };
    case "center_right":
      return {
        x: x + width,
        y: y + height / 2,
        textAnchor: isInside ? "center_right" : "center_left"
      };
    case "bottom_left":
      return {
        x,
        y,
        textAnchor: isInside ? "bottom_left" : "top_left"
      };
    case "bottom_center":
      return {
        x: x + width / 2,
        y,
        textAnchor: isInside ? "bottom_center" : "top_center"
      };
    case "bottom_right":
      return {
        x: x + width,
        y,
        textAnchor: isInside ? "bottom_right" : "top_right"
      };
    default:
      return {
        x: x + width / 2,
        y: y + height,
        textAnchor: "center"
      };
  }
}

// lib/components/primitive-components/SchematicBox/SchematicBox_doInitialSchematicComponentRender.ts
var getSchematicBoxPinLabels = (pinLabels) => {
  if (!pinLabels) return [];
  return Object.entries(pinLabels).map(([pinKey, pinLabel]) => {
    const pinNumber = getPinNumberFromPinLabelsKey(pinKey);
    if (pinNumber === null) {
      throw new Error(
        `Invalid schematicbox pinLabels key "${pinKey}". Expected pin<number>.`
      );
    }
    const pinAliases = typeof pinLabel === "string" ? [pinLabel] : [...pinLabel];
    return {
      pinNumber,
      displayPinLabel: pinAliases[0] ?? pinKey,
      pinAliases
    };
  });
};
var SchematicBox_doInitialSchematicComponentRender = (schematicBox) => {
  if (schematicBox.root?.schematicDisabled) return;
  const { db } = schematicBox.root;
  const props = schematicBox._parsedProps;
  if (!props.chipRef) return;
  const referencedChip = schematicBox.getSubcircuit().selectOne(props.chipRef);
  if (!referencedChip?.source_component_id) {
    throw new Error(
      `Could not resolve chipRef "${props.chipRef}" for ${schematicBox.getString()}`
    );
  }
  const schematicBoxPinLabels = getSchematicBoxPinLabels(props.pinLabels);
  const pinLabelsByPinKey = Object.fromEntries(
    schematicBoxPinLabels.map(({ pinNumber, displayPinLabel }) => [
      `pin${pinNumber}`,
      displayPinLabel
    ])
  );
  const sourcePorts = db.source_port.list({
    source_component_id: referencedChip.source_component_id
  });
  const dimensions = getAllDimensionsForSchematicBox({
    schWidth: props.width,
    schHeight: props.height,
    schPinSpacing: 0.2,
    pinCount: schematicBoxPinLabels.length,
    schPortArrangement: props.schPinArrangement,
    pinLabels: pinLabelsByPinKey
  });
  const center = schematicBox._getGlobalSchematicPositionBeforeLayout();
  const size = dimensions.getSize();
  const schematicSheetId = schematicBox._resolveSchematicSheetId();
  const portLabels = Object.fromEntries(
    schematicBoxPinLabels.map(({ pinNumber, displayPinLabel }) => [
      String(pinNumber),
      displayPinLabel
    ])
  );
  const schematicComponent = db.schematic_component.insert({
    center,
    size,
    source_component_id: referencedChip.source_component_id,
    is_box_with_pins: true,
    port_arrangement: underscorifyPortArrangement(props.schPinArrangement),
    port_labels: portLabels,
    pin_spacing: 0.2,
    schematic_sheet_id: schematicSheetId
  });
  schematicBox.schematic_component_id = schematicComponent.schematic_component_id;
  for (const {
    pinNumber,
    displayPinLabel,
    pinAliases
  } of schematicBoxPinLabels) {
    const referencedSourcePort = sourcePorts.find(
      (sourcePort) => pinAliases.some(
        (pinAlias) => sourcePort.name === pinAlias || sourcePort.port_hints?.includes(pinAlias)
      )
    );
    if (!referencedSourcePort) {
      throw new Error(
        `Could not find pin "${displayPinLabel}" on chipRef "${props.chipRef}"`
      );
    }
    const portPosition = dimensions.getPortPositionByPinNumber(pinNumber);
    if (!portPosition) {
      throw new Error(
        `Could not determine schematic position for ${schematicBox.getString()} pin${pinNumber}`
      );
    }
    const facingDirection = {
      left: "left",
      right: "right",
      top: "up",
      bottom: "down"
    }[portPosition.side];
    db.schematic_port.insert({
      schematic_component_id: schematicComponent.schematic_component_id,
      center: {
        x: center.x + portPosition.x,
        y: center.y + portPosition.y
      },
      source_port_id: referencedSourcePort.source_port_id,
      facing_direction: facingDirection,
      distance_from_component_edge: 0.4,
      side_of_component: portPosition.side,
      pin_number: pinNumber,
      true_ccw_index: portPosition.trueIndex,
      display_pin_label: displayPinLabel,
      is_connected: false,
      schematic_sheet_id: schematicSheetId
    });
  }
  const sectionName = props.name ?? props.chipRef;
  db.schematic_text.insert({
    text: sectionName,
    schematic_component_id: schematicComponent.schematic_component_id,
    anchor: "left",
    rotation: 0,
    position: {
      x: center.x - size.width / 2,
      y: center.y + size.height / 2 + 0.13
    },
    color: "#006464",
    font_size: 0.18,
    schematic_sheet_id: schematicSheetId
  });
};

// lib/components/primitive-components/SchematicBox/SchematicBox.ts
var SchematicBox = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  get config() {
    return {
      componentName: "SchematicBox",
      zodProps: schematicBoxProps,
      shouldRenderAsSchematicBox: true
    };
  }
  doInitialSchematicComponentRender() {
    SchematicBox_doInitialSchematicComponentRender(this);
  }
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    if (props.chipRef) return;
    const basePadding = 0.6;
    const generalPadding = typeof props.padding === "number" ? props.padding : 0;
    const paddingTop = typeof props.paddingTop === "number" ? props.paddingTop : generalPadding;
    const paddingBottom = typeof props.paddingBottom === "number" ? props.paddingBottom : generalPadding;
    const paddingLeft = typeof props.paddingLeft === "number" ? props.paddingLeft : generalPadding;
    const paddingRight = typeof props.paddingRight === "number" ? props.paddingRight : generalPadding;
    const hasOverlay = props.overlay && props.overlay.length > 0;
    const hasFixedSize = typeof props.width === "number" && typeof props.height === "number";
    let width;
    let height;
    let x;
    let y;
    let centerX;
    let centerY;
    if (hasOverlay) {
      const portsWithSelectors = props.overlay.map((selector) => ({
        selector,
        port: this.getSubcircuit().selectOne(selector, {
          type: "port"
        })
      })).filter(({ port }) => port != null);
      const portsWithPosition = portsWithSelectors.map(({ port }) => ({
        position: port._getGlobalSchematicPositionAfterLayout()
      }));
      if (portsWithPosition.length === 0) return;
      const xs = portsWithPosition.map((p) => p.position.x);
      const ys = portsWithPosition.map((p) => p.position.y);
      const minX = Math.min(...xs);
      const maxX = Math.max(...xs);
      const minY = Math.min(...ys);
      const maxY = Math.max(...ys);
      const rawWidth = maxX - minX;
      const rawHeight = maxY - minY;
      const defaultHorizontalPadding = rawWidth === 0 ? basePadding : 0;
      const defaultVerticalPadding = rawHeight === 0 ? basePadding : 0;
      const finalPaddingLeft = paddingLeft + defaultHorizontalPadding / 2;
      const finalPaddingRight = paddingRight + defaultHorizontalPadding / 2;
      const finalPaddingTop = paddingTop + defaultVerticalPadding / 2;
      const finalPaddingBottom = paddingBottom + defaultVerticalPadding / 2;
      const left = minX - finalPaddingLeft;
      const right = maxX + finalPaddingRight;
      const top = minY - finalPaddingBottom;
      const bottom = maxY + finalPaddingTop;
      width = right - left;
      height = bottom - top;
      x = left + (props.schX ?? 0);
      y = top + (props.schY ?? 0);
      centerX = x + width / 2;
      centerY = y + height / 2;
    } else if (hasFixedSize) {
      width = props.width;
      height = props.height;
      const center = this._getGlobalSchematicPositionBeforeLayout();
      centerX = center.x;
      centerY = center.y;
      x = centerX - width / 2;
      y = centerY - height / 2;
    } else {
      return;
    }
    db.schematic_box.insert({
      height,
      width,
      x,
      y,
      is_dashed: props.strokeStyle === "dashed",
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    if (props.title) {
      const isInside = props.titleInside;
      const TITLE_PADDING = 0.1;
      const anchor = props.titleAlignment;
      const anchorPos = getTitleAnchorAndPosition({
        anchor,
        x,
        y,
        width,
        height,
        isInside
      });
      let titleOffsetY;
      let titleOffsetX;
      const textAnchor = anchorPos.textAnchor;
      if (isInside) {
        titleOffsetY = anchor.includes("top") ? -TITLE_PADDING : anchor.includes("bottom") ? TITLE_PADDING : 0;
        titleOffsetX = anchor.includes("left") ? TITLE_PADDING : anchor.includes("right") ? -TITLE_PADDING : 0;
      } else {
        titleOffsetY = anchor.includes("top") ? TITLE_PADDING : anchor.includes("bottom") ? -TITLE_PADDING : 0;
        titleOffsetX = anchor.includes("center_left") ? -TITLE_PADDING : anchor.includes("center_right") ? TITLE_PADDING : 0;
      }
      const titleX = anchorPos.x + titleOffsetX;
      const titleY = anchorPos.y + titleOffsetY;
      db.schematic_text.insert({
        anchor: textAnchor,
        text: props.title,
        font_size: props.titleFontSize ?? 0.18,
        color: props.titleColor ?? "#000000",
        position: {
          x: titleX,
          y: titleY
        },
        rotation: 0,
        schematic_sheet_id: this._resolveSchematicSheetId()
      });
    }
  }
};

// lib/components/primitive-components/Group/Group_doInitialAssignSchematicSheetToConnectedComponents.ts
var Group_doInitialAssignSchematicSheetToConnectedComponents = (group) => {
  if (!group.isSubcircuit || group.root?.schematicDisabled) return;
  const { db } = group.root;
  const schematicSheetIdsBySubcircuitConnectivityMapKey = /* @__PURE__ */ new Map();
  const schematicBoxReferencedSourceComponentIds = /* @__PURE__ */ new Set();
  const schematicBoxes = group.getDescendants().filter(
    (component) => component instanceof SchematicBox && component.getSubcircuit() === group
  );
  for (const schematicBox of schematicBoxes) {
    const { chipRef, pinLabels } = schematicBox._parsedProps;
    const schematicSheetId = schematicBox._resolveSchematicSheetId();
    if (!chipRef || !schematicSheetId) continue;
    const referencedComponent = schematicBox.getSubcircuit().selectOne(chipRef);
    if (!(referencedComponent instanceof NormalComponent3)) continue;
    if (!referencedComponent.source_component_id) continue;
    schematicBoxReferencedSourceComponentIds.add(
      referencedComponent.source_component_id
    );
    const referencedSourcePorts = db.source_port.list({
      source_component_id: referencedComponent.source_component_id
    });
    for (const { pinAliases } of getSchematicBoxPinLabels(pinLabels)) {
      const referencedSourcePort = referencedSourcePorts.find(
        (sourcePort) => pinAliases.some(
          (pinAlias) => sourcePort.name === pinAlias || sourcePort.port_hints?.includes(pinAlias)
        )
      );
      const subcircuitConnectivityMapKey = referencedSourcePort?.subcircuit_connectivity_map_key;
      if (!subcircuitConnectivityMapKey) continue;
      const schematicSheetIds = schematicSheetIdsBySubcircuitConnectivityMapKey.get(
        subcircuitConnectivityMapKey
      ) ?? /* @__PURE__ */ new Set();
      schematicSheetIds.add(schematicSheetId);
      schematicSheetIdsBySubcircuitConnectivityMapKey.set(
        subcircuitConnectivityMapKey,
        schematicSheetIds
      );
    }
  }
  const normalComponents = group.getDescendants().filter(
    (component) => component instanceof NormalComponent3 && component.getSubcircuit() === group
  );
  for (const normalComponent of normalComponents) {
    if (!normalComponent.source_component_id) continue;
    if (schematicBoxReferencedSourceComponentIds.has(
      normalComponent.source_component_id
    )) {
      continue;
    }
    if (normalComponent._resolveSchematicSheetId()) continue;
    const connectedSchematicSheetIds = /* @__PURE__ */ new Set();
    const sourcePorts = db.source_port.list({
      source_component_id: normalComponent.source_component_id
    });
    for (const sourcePort of sourcePorts) {
      const subcircuitConnectivityMapKey = sourcePort.subcircuit_connectivity_map_key;
      if (!subcircuitConnectivityMapKey) continue;
      const schematicSheetIds = schematicSheetIdsBySubcircuitConnectivityMapKey.get(
        subcircuitConnectivityMapKey
      );
      if (!schematicSheetIds) continue;
      for (const schematicSheetId of schematicSheetIds) {
        connectedSchematicSheetIds.add(schematicSheetId);
      }
    }
    if (connectedSchematicSheetIds.size !== 1) continue;
    const [inferredSchematicSheetId] = connectedSchematicSheetIds;
    if (!inferredSchematicSheetId) continue;
    normalComponent.schematic_sheet_id = inferredSchematicSheetId;
  }
};

// lib/components/primitive-components/Group/Group_doInitialPcbCalcPlacementResolution.ts
import { getBoundsOfPcbElements as getBoundsOfPcbElements2 } from "@tscircuit/circuit-json-util";
var SUPPORTED_COMPONENT_FIELDS = /* @__PURE__ */ new Set([
  "x",
  "y",
  "minx",
  "maxx",
  "miny",
  "maxy",
  "width",
  "height"
]);
function Group_doInitialPcbCalcPlacementResolution(group) {
  if (group.root?.pcbDisabled) return;
  if (!group.isSubcircuit) return;
  const { db } = group.root;
  const normalComponentNameMap = group.getNormalComponentNameMap?.() ?? /* @__PURE__ */ new Map();
  const allNormalComponents = collectNormalComponentsInSubcircuit(group);
  const namedComponentVars = {};
  for (const component of allNormalComponents) {
    updateVarsForNamedComponent(component, namedComponentVars);
  }
  const candidatesByName = /* @__PURE__ */ new Map();
  const inDegree = /* @__PURE__ */ new Map();
  const dependents = /* @__PURE__ */ new Map();
  for (const component of allNormalComponents) {
    if (!shouldResolvePlacementInCalcPhase(component)) continue;
    const refs = getComponentRefsForCalcPlacement(component);
    if (!component.name) {
      throw new Error(
        `Invalid pcb position expression for ${component.getString()}: component-relative calc requires the component to have a name`
      );
    }
    candidatesByName.set(component.name, { component, refs });
    inDegree.set(component.name, 0);
  }
  for (const [candidateName, candidate] of candidatesByName.entries()) {
    const referencedCandidateNames = /* @__PURE__ */ new Set();
    for (const token of candidate.refs) {
      const { referencePath, field } = parseComponentReferenceToken(token);
      if (!SUPPORTED_COMPONENT_FIELDS.has(field)) {
        throw new Error(
          `Invalid pcb position expression for ${candidate.component.getString()}: unsupported component field "${field}" in "${token}"`
        );
      }
      const referencedComponentName = resolveReferencedComponentName({
        token,
        referencePath,
        candidate,
        normalComponentNameMap
      });
      if (candidatesByName.has(referencedComponentName) && referencedComponentName !== candidateName) {
        referencedCandidateNames.add(referencedComponentName);
      }
    }
    for (const referencedComponentName of referencedCandidateNames) {
      inDegree.set(candidateName, (inDegree.get(candidateName) ?? 0) + 1);
      if (!dependents.has(referencedComponentName)) {
        dependents.set(referencedComponentName, /* @__PURE__ */ new Set());
      }
      dependents.get(referencedComponentName).add(candidateName);
    }
  }
  const queue = [...inDegree.entries()].filter(([, degree]) => degree === 0).map(([name]) => name);
  let resolvedCount = 0;
  while (queue.length > 0) {
    const name = queue.shift();
    const candidate = candidatesByName.get(name);
    if (!candidate) continue;
    resolveCandidatePlacement(candidate.component, namedComponentVars);
    resolvedCount++;
    for (const dependentName of dependents.get(name) ?? []) {
      const nextDegree = (inDegree.get(dependentName) ?? 0) - 1;
      inDegree.set(dependentName, nextDegree);
      if (nextDegree === 0) queue.push(dependentName);
    }
  }
  if (resolvedCount < candidatesByName.size) {
    const unresolvedNames = [...candidatesByName.keys()].filter(
      (name) => (inDegree.get(name) ?? 0) > 0
    );
    throw new Error(
      `Circular pcb position calc references detected among: ${unresolvedNames.join(", ")}`
    );
  }
  function resolveCandidatePlacement(component, componentVars) {
    if (!component.pcb_component_id) return;
    const pcbComponent = db.pcb_component.get(component.pcb_component_id);
    if (!pcbComponent) return;
    const rawPcbX = component._parsedProps.pcbX;
    const rawPcbY = component._parsedProps.pcbY;
    const rawComponentProps = component.props;
    const rawPcbLeftEdgeX = component._parsedProps.pcbLeftEdgeX ?? rawComponentProps.pcbLeftEdgeX;
    const rawPcbRightEdgeX = component._parsedProps.pcbRightEdgeX ?? rawComponentProps.pcbRightEdgeX;
    const rawPcbTopEdgeY = component._parsedProps.pcbTopEdgeY ?? rawComponentProps.pcbTopEdgeY;
    const rawPcbBottomEdgeY = component._parsedProps.pcbBottomEdgeY ?? rawComponentProps.pcbBottomEdgeY;
    if (rawPcbLeftEdgeX !== void 0 && rawPcbRightEdgeX !== void 0) {
      throw new Error(
        `${component.componentName} cannot set both pcbLeftEdgeX and pcbRightEdgeX`
      );
    }
    if (rawPcbTopEdgeY !== void 0 && rawPcbBottomEdgeY !== void 0) {
      throw new Error(
        `${component.componentName} cannot set both pcbTopEdgeY and pcbBottomEdgeY`
      );
    }
    const componentWidth = pcbComponent.width ?? 0;
    const componentHeight = pcbComponent.height ?? 0;
    const nextCenter = {
      x: pcbComponent.center.x,
      y: pcbComponent.center.y
    };
    if (rawPcbX !== void 0) {
      const resolvedPcbX = component.resolvePcbCoordinate({
        rawValue: rawPcbX,
        axis: "pcbX",
        allowComponentVariables: true,
        componentVariables: componentVars
      });
      component._resolvedPcbCalcOffsetX = resolvedPcbX;
      nextCenter.x = resolvedPcbX;
    } else if (rawPcbLeftEdgeX !== void 0) {
      const resolvedPcbLeftEdgeX = component.resolvePcbCoordinate({
        rawValue: rawPcbLeftEdgeX,
        axis: "pcbX",
        allowComponentVariables: true,
        componentVariables: componentVars,
        propertyName: "pcbLeftEdgeX"
      });
      const resolvedPcbX = resolvedPcbLeftEdgeX + componentWidth / 2;
      component._resolvedPcbCalcOffsetX = resolvedPcbX;
      nextCenter.x = resolvedPcbX;
    } else if (rawPcbRightEdgeX !== void 0) {
      const resolvedPcbRightEdgeX = component.resolvePcbCoordinate({
        rawValue: rawPcbRightEdgeX,
        axis: "pcbX",
        allowComponentVariables: true,
        componentVariables: componentVars,
        propertyName: "pcbRightEdgeX"
      });
      const resolvedPcbX = resolvedPcbRightEdgeX - componentWidth / 2;
      component._resolvedPcbCalcOffsetX = resolvedPcbX;
      nextCenter.x = resolvedPcbX;
    }
    if (rawPcbY !== void 0) {
      const resolvedPcbY = component.resolvePcbCoordinate({
        rawValue: rawPcbY,
        axis: "pcbY",
        allowComponentVariables: true,
        componentVariables: componentVars
      });
      component._resolvedPcbCalcOffsetY = resolvedPcbY;
      nextCenter.y = resolvedPcbY;
    } else if (rawPcbTopEdgeY !== void 0) {
      const resolvedPcbTopEdgeY = component.resolvePcbCoordinate({
        rawValue: rawPcbTopEdgeY,
        axis: "pcbY",
        allowComponentVariables: true,
        componentVariables: componentVars,
        propertyName: "pcbTopEdgeY"
      });
      const resolvedPcbY = resolvedPcbTopEdgeY - componentHeight / 2;
      component._resolvedPcbCalcOffsetY = resolvedPcbY;
      nextCenter.y = resolvedPcbY;
    } else if (rawPcbBottomEdgeY !== void 0) {
      const resolvedPcbBottomEdgeY = component.resolvePcbCoordinate({
        rawValue: rawPcbBottomEdgeY,
        axis: "pcbY",
        allowComponentVariables: true,
        componentVariables: componentVars,
        propertyName: "pcbBottomEdgeY"
      });
      const resolvedPcbY = resolvedPcbBottomEdgeY + componentHeight / 2;
      component._resolvedPcbCalcOffsetY = resolvedPcbY;
      nextCenter.y = resolvedPcbY;
    }
    component._repositionOnPcb(nextCenter);
    updateVarsForNamedComponent(component, componentVars);
  }
}
function shouldResolvePlacementInCalcPhase(component) {
  const parsedProps = component._parsedProps;
  const rawProps = component.props;
  const pcbX = parsedProps.pcbX;
  const pcbY = parsedProps.pcbY;
  const pcbLeftEdgeX = parsedProps.pcbLeftEdgeX ?? rawProps.pcbLeftEdgeX;
  const pcbRightEdgeX = parsedProps.pcbRightEdgeX ?? rawProps.pcbRightEdgeX;
  const pcbTopEdgeY = parsedProps.pcbTopEdgeY ?? rawProps.pcbTopEdgeY;
  const pcbBottomEdgeY = parsedProps.pcbBottomEdgeY ?? rawProps.pcbBottomEdgeY;
  if (pcbLeftEdgeX !== void 0 || pcbRightEdgeX !== void 0 || pcbTopEdgeY !== void 0 || pcbBottomEdgeY !== void 0) {
    return true;
  }
  return typeof pcbX === "string" || typeof pcbY === "string";
}
function collectNormalComponentsInSubcircuit(group) {
  const components = [];
  const walk = (node) => {
    if (node._isNormalComponent) {
      components.push(node);
    }
    for (const child of node.children) {
      if (!child.isSubcircuit) walk(child);
    }
  };
  for (const child of group.children) {
    if (!child.isSubcircuit) walk(child);
  }
  return components;
}
function getComponentRefsForCalcPlacement(component) {
  const refs = /* @__PURE__ */ new Set();
  const rawPcbX = component._parsedProps.pcbX;
  const rawPcbY = component._parsedProps.pcbY;
  const rawComponentProps = component.props;
  const rawPcbLeftEdgeX = component._parsedProps.pcbLeftEdgeX ?? rawComponentProps.pcbLeftEdgeX;
  const rawPcbRightEdgeX = component._parsedProps.pcbRightEdgeX ?? rawComponentProps.pcbRightEdgeX;
  const rawPcbTopEdgeY = component._parsedProps.pcbTopEdgeY ?? rawComponentProps.pcbTopEdgeY;
  const rawPcbBottomEdgeY = component._parsedProps.pcbBottomEdgeY ?? rawComponentProps.pcbBottomEdgeY;
  const addRefs = (rawValue) => {
    if (typeof rawValue !== "string") return;
    const identifiers = extractCalcIdentifiers(rawValue);
    for (const identifier of identifiers) {
      if (!identifier.startsWith("board.")) {
        refs.add(identifier);
      }
    }
  };
  addRefs(rawPcbX);
  addRefs(rawPcbY);
  addRefs(rawPcbLeftEdgeX);
  addRefs(rawPcbRightEdgeX);
  addRefs(rawPcbTopEdgeY);
  addRefs(rawPcbBottomEdgeY);
  return refs;
}
function parseComponentReferenceToken(token) {
  const dotIndex = token.lastIndexOf(".");
  if (dotIndex <= 0 || dotIndex === token.length - 1) {
    throw new Error(`Invalid component reference token: "${token}"`);
  }
  return {
    referencePath: token.slice(0, dotIndex),
    field: token.slice(dotIndex + 1).toLowerCase()
  };
}
function resolveReferencedComponentName(params) {
  const { token, referencePath, candidate, normalComponentNameMap } = params;
  const directComponents = normalComponentNameMap.get(referencePath);
  if (directComponents && directComponents.length > 0) {
    if (directComponents.length > 1) {
      throw new Error(
        `Invalid pcb position expression for ${candidate.component.getString()}: ambiguous component reference "${referencePath}" in "${token}"`
      );
    }
    return referencePath;
  }
  let bestMatch = null;
  for (const componentName of normalComponentNameMap.keys()) {
    if (!referencePath.startsWith(`${componentName}.`)) continue;
    if (!bestMatch || componentName.length > bestMatch.length) {
      bestMatch = componentName;
    }
  }
  if (!bestMatch) {
    throw new Error(
      `Invalid pcb position expression for ${candidate.component.getString()}: unknown component reference "${referencePath}" in "${token}"`
    );
  }
  return bestMatch;
}
function updateVarsForNamedComponent(component, vars) {
  if (!component.name || !component.pcb_component_id || !component.root) return;
  const pcbComponent = component.root.db.pcb_component.get(
    component.pcb_component_id
  );
  if (!pcbComponent) return;
  const width = pcbComponent.width ?? 0;
  const height = pcbComponent.height ?? 0;
  const x = pcbComponent.center.x;
  const y = pcbComponent.center.y;
  setSubcircuitPcbComponentCalcVariables({
    vars,
    componentName: component.name,
    position: { x, y },
    size: { width, height }
  });
  const padElementsByReference = collectPadElementsByReference(component);
  for (const [referencePath, elements] of padElementsByReference.entries()) {
    const bounds = getBoundsOfPcbElements2(elements);
    const minX = bounds.minX;
    const maxX = bounds.maxX;
    const minY = bounds.minY;
    const maxY = bounds.maxY;
    vars[`${referencePath}.x`] = (minX + maxX) / 2;
    vars[`${referencePath}.y`] = (minY + maxY) / 2;
    vars[`${referencePath}.width`] = maxX - minX;
    vars[`${referencePath}.height`] = maxY - minY;
    vars[`${referencePath}.minX`] = minX;
    vars[`${referencePath}.maxX`] = maxX;
    vars[`${referencePath}.minY`] = minY;
    vars[`${referencePath}.maxY`] = maxY;
  }
}
function collectPadElementsByReference(component) {
  const refsToElements = /* @__PURE__ */ new Map();
  if (!component.name || !component.pcb_component_id || !component.root) {
    return refsToElements;
  }
  const { db } = component.root;
  const padElements = [
    ...db.pcb_smtpad.list({ pcb_component_id: component.pcb_component_id }),
    ...db.pcb_plated_hole.list({
      pcb_component_id: component.pcb_component_id
    })
  ];
  for (const pad of padElements) {
    const aliases = /* @__PURE__ */ new Set();
    for (const hint of pad.port_hints ?? []) {
      if (typeof hint === "string" && hint.length > 0) aliases.add(hint);
    }
    const pcbPortId = pad.pcb_port_id;
    if (pcbPortId) {
      const pcbPort = db.pcb_port.get(pcbPortId);
      const sourcePort = pcbPort?.source_port_id ? db.source_port.get(pcbPort.source_port_id) : null;
      if (sourcePort?.name) aliases.add(sourcePort.name);
      if (sourcePort?.pin_number != null) {
        aliases.add(`pin${sourcePort.pin_number}`);
      }
    }
    for (const alias of aliases) {
      const referencePath = `${component.name}.${alias}`;
      if (!refsToElements.has(referencePath)) {
        refsToElements.set(referencePath, []);
      }
      refsToElements.get(referencePath).push(pad);
    }
  }
  return refsToElements;
}

// lib/components/primitive-components/Group/Group_doInitialPcbComponentAnchorAlignment.ts
import { getBoundsFromPoints as getBoundsFromPoints3 } from "@tscircuit/math-utils";
function Group_doInitialPcbComponentAnchorAlignment(group) {
  if (group.root?.pcbDisabled) return;
  if (!group.pcb_group_id) return;
  const pcbPositionAnchor = group._parsedProps?.pcbPositionAnchor;
  if (!pcbPositionAnchor) return;
  const targetPosition = group._getGlobalPcbPositionBeforeLayout();
  const { pcbX, pcbY } = group._parsedProps;
  if (pcbX === void 0 && pcbY === void 0) return;
  const { db } = group.root;
  const pcbGroup = db.pcb_group.get(group.pcb_group_id);
  if (!pcbGroup) return;
  let width = pcbGroup.width;
  let height = pcbGroup.height;
  const { center } = pcbGroup;
  if (pcbGroup.outline && pcbGroup.outline.length > 0) {
    const bounds2 = getBoundsFromPoints3(pcbGroup.outline);
    if (bounds2) {
      width = bounds2.maxX - bounds2.minX;
      height = bounds2.maxY - bounds2.minY;
    }
  }
  if (!width || !height) return;
  const bounds = {
    left: center.x - width / 2,
    right: center.x + width / 2,
    top: center.y + height / 2,
    // Y-up: top is at higher Y
    bottom: center.y - height / 2
    // Y-up: bottom is at lower Y
  };
  const currentCenter = { ...center };
  let anchorPos = null;
  const ninePointAnchors = /* @__PURE__ */ new Set([
    "center",
    "top_left",
    "top_center",
    "top_right",
    "center_left",
    "center_right",
    "bottom_left",
    "bottom_center",
    "bottom_right"
  ]);
  if (ninePointAnchors.has(pcbPositionAnchor)) {
    switch (pcbPositionAnchor) {
      case "center":
        anchorPos = currentCenter;
        break;
      case "top_left":
        anchorPos = { x: bounds.left, y: bounds.top };
        break;
      case "top_center":
        anchorPos = { x: currentCenter.x, y: bounds.top };
        break;
      case "top_right":
        anchorPos = { x: bounds.right, y: bounds.top };
        break;
      case "center_left":
        anchorPos = { x: bounds.left, y: currentCenter.y };
        break;
      case "center_right":
        anchorPos = { x: bounds.right, y: currentCenter.y };
        break;
      case "bottom_left":
        anchorPos = { x: bounds.left, y: bounds.bottom };
        break;
      case "bottom_center":
        anchorPos = { x: currentCenter.x, y: bounds.bottom };
        break;
      case "bottom_right":
        anchorPos = { x: bounds.right, y: bounds.bottom };
        break;
    }
  }
  if (!anchorPos) return;
  const newCenter = { ...currentCenter };
  if (targetPosition.x !== void 0)
    newCenter.x += targetPosition.x - anchorPos.x;
  if (targetPosition.y !== void 0)
    newCenter.y += targetPosition.y - anchorPos.y;
  if (Math.abs(newCenter.x - currentCenter.x) > 1e-6 || Math.abs(newCenter.y - currentCenter.y) > 1e-6) {
    group._repositionOnPcb(newCenter);
    db.pcb_group.update(group.pcb_group_id, {
      center: newCenter
    });
  }
  db.pcb_group.update(group.pcb_group_id, {
    anchor_position: targetPosition,
    anchor_alignment: pcbPositionAnchor,
    display_offset_x: pcbX,
    display_offset_y: pcbY
  });
}

// lib/components/primitive-components/Group/Group_doInitialPcbLayoutFlex.ts
import { getMinimumFlexContainer } from "@tscircuit/circuit-json-util";
import { RootFlexBox } from "@tscircuit/miniflex";
import { length } from "circuit-json";
var Group_doInitialPcbLayoutFlex = (group) => {
  const { db } = group.root;
  const { _parsedProps: props } = group;
  const pcbChildren = group.children.filter(
    (c) => c.pcb_component_id || c.pcb_group_id
  );
  const anyChildHasExplicitPcbPosition = pcbChildren.some((child) => {
    const childProps = child._parsedProps;
    return childProps?.pcbX !== void 0 || childProps?.pcbY !== void 0;
  });
  if (anyChildHasExplicitPcbPosition) {
    return;
  }
  const rawJustify = props.pcbJustifyContent ?? props.justifyContent;
  const rawAlign = props.pcbAlignItems ?? props.alignItems;
  const rawGap = props.pcbFlexGap ?? props.pcbGap ?? props.gap;
  const direction = props.pcbFlexDirection ?? "row";
  const justifyContent = {
    start: "flex-start",
    end: "flex-end",
    "flex-start": "flex-start",
    "flex-end": "flex-end",
    stretch: "space-between",
    "space-between": "space-between",
    "space-around": "space-around",
    "space-evenly": "space-evenly",
    center: "center"
  }[rawJustify ?? "space-between"];
  const alignItems = {
    start: "flex-start",
    end: "flex-end",
    "flex-start": "flex-start",
    "flex-end": "flex-end",
    stretch: "stretch",
    center: "center"
  }[rawAlign ?? "center"];
  if (!justifyContent) {
    throw new Error(`Invalid justifyContent value: "${rawJustify}"`);
  }
  if (!alignItems) {
    throw new Error(`Invalid alignItems value: "${rawAlign}"`);
  }
  let rowGap = 0;
  let columnGap = 0;
  if (typeof rawGap === "object") {
    rowGap = rawGap.y ?? 0;
    columnGap = rawGap.x ?? 0;
  } else if (typeof rawGap === "number") {
    rowGap = rawGap;
    columnGap = rawGap;
  } else if (typeof rawGap === "string") {
    rowGap = length.parse(rawGap);
    columnGap = length.parse(rawGap);
  }
  let minFlexContainer;
  let width = props.width ?? props.pcbWidth ?? void 0;
  let height = props.height ?? props.pcbHeight ?? void 0;
  const isInline = Boolean(width === void 0 || height === void 0);
  if (isInline) {
    minFlexContainer = getMinimumFlexContainer(
      pcbChildren.map((child) => child._getMinimumFlexContainerSize()).filter((size) => size !== null),
      {
        alignItems,
        justifyContent,
        direction,
        rowGap,
        columnGap
      }
    );
    width = minFlexContainer.width;
    height = minFlexContainer.height;
  }
  const flexBox = new RootFlexBox(width, height, {
    alignItems,
    justifyContent,
    direction,
    rowGap,
    columnGap
  });
  for (const child of pcbChildren) {
    const size = child._getMinimumFlexContainerSize();
    flexBox.addChild({
      metadata: child,
      // TODO these should be minWidth/minHeight
      width: size?.width ?? 0,
      height: size?.height ?? 0,
      // TODO allow overriding flexBasis
      flexBasis: !size ? void 0 : direction === "row" ? size.width : size.height
      // TODO alignSelf, flexGrow, flexShrink etc.
    });
  }
  flexBox.build();
  const bounds = {
    minX: Infinity,
    minY: Infinity,
    maxX: -Infinity,
    maxY: -Infinity,
    width: 0,
    height: 0
  };
  for (const child of flexBox.children) {
    bounds.minX = Math.min(bounds.minX, child.position.x);
    bounds.minY = Math.min(bounds.minY, child.position.y);
    bounds.maxX = Math.max(bounds.maxX, child.position.x + child.size.width);
    bounds.maxY = Math.max(bounds.maxY, child.position.y + child.size.height);
  }
  bounds.width = bounds.maxX - bounds.minX;
  bounds.height = bounds.maxY - bounds.minY;
  const groupCenter = group._getGlobalPcbPositionBeforeLayout();
  const offset = {
    x: groupCenter.x - (bounds.maxX + bounds.minX) / 2,
    y: groupCenter.y - (bounds.maxY + bounds.minY) / 2
  };
  for (const child of flexBox.children) {
    const childMetadata = child.metadata;
    childMetadata._repositionOnPcb({
      x: child.position.x + child.size.width / 2 + offset.x,
      y: child.position.y + child.size.height / 2 + offset.y
    });
  }
  db.pcb_group.update(group.pcb_group_id, {
    width: bounds.width,
    height: bounds.height,
    center: groupCenter
  });
};

// lib/components/primitive-components/Group/Group_doInitialPcbLayoutGrid.ts
import {
  repositionPcbComponentTo as repositionPcbComponentTo2,
  repositionPcbGroupTo as repositionPcbGroupTo2
} from "@tscircuit/circuit-json-util";
import { length as length2 } from "circuit-json";
import { CssGrid } from "minicssgrid";
var MIN_GAP = 1;
function Group_doInitialPcbLayoutGrid(group) {
  const { db } = group.root;
  const props = group._parsedProps;
  const pcbChildren = getPcbChildren(group);
  if (pcbChildren.length === 0) return;
  const childDimensions = calculateChildDimensions({ db, pcbChildren });
  const gridConfig = parseGridConfiguration(props);
  const gridLayout = createGridLayout({
    props,
    pcbChildren,
    childDimensions,
    gridConfig
  });
  const cssGrid = createCssGrid({
    pcbChildren,
    childDimensions,
    gridLayout,
    gridConfig
  });
  const { itemCoordinates } = cssGrid.layout();
  positionChildren({ db, group, pcbChildren, itemCoordinates, gridLayout });
  updateGroupDimensions({ db, group, props, gridLayout });
}
function getPcbChildren(group) {
  return group.children.filter(
    (child) => child.pcb_component_id || child.pcb_group_id
  );
}
function calculateChildDimensions(params) {
  const { db, pcbChildren } = params;
  let maxWidth = 0;
  let maxHeight = 0;
  for (const child of pcbChildren) {
    let width = 0;
    let height = 0;
    if (child.pcb_group_id) {
      const pcbGroup = db.pcb_group.get(child.pcb_group_id);
      width = pcbGroup?.width ?? 0;
      height = pcbGroup?.height ?? 0;
    } else if (child.pcb_component_id) {
      const pcbComp = db.pcb_component.get(child.pcb_component_id);
      width = pcbComp?.width ?? 0;
      height = pcbComp?.height ?? 0;
    }
    maxWidth = Math.max(maxWidth, width);
    maxHeight = Math.max(maxHeight, height);
  }
  return {
    width: maxWidth,
    height: maxHeight
  };
}
function parseGridConfiguration(props) {
  const cols = props.pcbGridCols ?? props.gridCols ?? props.pcbLayout?.grid?.cols;
  const rows = props.pcbGridRows ?? props.pcbLayout?.grid?.rows;
  const templateColumns = props.pcbGridTemplateColumns;
  const templateRows = props.pcbGridTemplateRows;
  const parseGap = (gapValue) => {
    if (gapValue === void 0) return MIN_GAP;
    return typeof gapValue === "number" ? gapValue : length2.parse(gapValue);
  };
  const gridGapOption = props.pcbGridGap ?? props.gridGap ?? props.pcbLayout?.gridGap;
  const rowGapOption = props.pcbGridRowGap ?? props.gridRowGap ?? props.pcbLayout?.gridRowGap;
  const colGapOption = props.pcbGridColumnGap ?? props.gridColumnGap ?? props.pcbLayout?.gridColumnGap;
  let gapX = MIN_GAP;
  let gapY = MIN_GAP;
  if (rowGapOption !== void 0 || colGapOption !== void 0) {
    const fallbackX = typeof gridGapOption === "object" ? gridGapOption?.x : gridGapOption;
    const fallbackY = typeof gridGapOption === "object" ? gridGapOption?.y : gridGapOption;
    gapX = parseGap(colGapOption ?? fallbackX);
    gapY = parseGap(rowGapOption ?? fallbackY);
  } else if (typeof gridGapOption === "object" && gridGapOption !== null) {
    gapX = parseGap(gridGapOption.x);
    gapY = parseGap(gridGapOption.y);
  } else {
    const gap = parseGap(gridGapOption);
    gapX = gap;
    gapY = gap;
  }
  return { cols, rows, gapX, gapY, templateColumns, templateRows };
}
function createGridLayout(params) {
  const { props, pcbChildren, childDimensions, gridConfig } = params;
  if (props.pcbGridTemplateColumns || props.pcbGridTemplateRows) {
    return createTemplateBasedLayout({
      props,
      gridConfig,
      pcbChildren,
      childDimensions
    });
  }
  return createDefaultLayout({ gridConfig, pcbChildren, childDimensions });
}
function createTemplateBasedLayout(params) {
  const { props, gridConfig, pcbChildren, childDimensions } = params;
  const gridTemplateColumns = props.pcbGridTemplateColumns ?? "";
  const gridTemplateRows = props.pcbGridTemplateRows ?? "";
  const extractRepeatCount = (template) => {
    const match = template.match(/repeat\((\d+),/);
    return match ? parseInt(match[1]) : Math.ceil(Math.sqrt(pcbChildren.length));
  };
  const numCols = props.pcbGridTemplateColumns ? extractRepeatCount(gridTemplateColumns) : Math.ceil(Math.sqrt(pcbChildren.length));
  const numRows = props.pcbGridTemplateRows ? extractRepeatCount(gridTemplateRows) : Math.ceil(pcbChildren.length / numCols);
  const containerWidth = numCols * childDimensions.width + Math.max(0, numCols - 1) * gridConfig.gapX;
  const containerHeight = numRows * childDimensions.height + Math.max(0, numRows - 1) * gridConfig.gapY;
  return {
    gridTemplateColumns,
    gridTemplateRows,
    containerWidth,
    containerHeight
  };
}
function createDefaultLayout(params) {
  const { gridConfig, pcbChildren, childDimensions } = params;
  let numCols;
  let numRows;
  if (gridConfig.cols !== void 0 && gridConfig.rows !== void 0) {
    numCols = gridConfig.cols;
    numRows = gridConfig.rows;
  } else if (gridConfig.cols !== void 0) {
    numCols = gridConfig.cols;
    numRows = Math.ceil(pcbChildren.length / numCols);
  } else if (gridConfig.rows !== void 0) {
    numRows = gridConfig.rows;
    numCols = Math.ceil(pcbChildren.length / numRows);
  } else {
    numCols = Math.ceil(Math.sqrt(pcbChildren.length));
    numRows = Math.ceil(pcbChildren.length / numCols);
  }
  numCols = Math.max(1, numCols);
  numRows = Math.max(1, numRows);
  const containerWidth = numCols * childDimensions.width + Math.max(0, numCols - 1) * gridConfig.gapX;
  const containerHeight = numRows * childDimensions.height + Math.max(0, numRows - 1) * gridConfig.gapY;
  const gridTemplateColumns = `repeat(${numCols}, ${childDimensions.width}px)`;
  const gridTemplateRows = `repeat(${numRows}, ${childDimensions.height}px)`;
  return {
    gridTemplateColumns,
    gridTemplateRows,
    containerWidth,
    containerHeight
  };
}
function createCssGrid(params) {
  const { pcbChildren, childDimensions, gridLayout, gridConfig } = params;
  const gridChildren = pcbChildren.map((child, index) => ({
    key: child.getString() || `child-${index}`,
    contentWidth: childDimensions.width,
    contentHeight: childDimensions.height
  }));
  return new CssGrid({
    containerWidth: gridLayout.containerWidth,
    containerHeight: gridLayout.containerHeight,
    gridTemplateColumns: gridLayout.gridTemplateColumns,
    gridTemplateRows: gridLayout.gridTemplateRows,
    gap: [gridConfig.gapY, gridConfig.gapX],
    // [rowGap, columnGap]
    children: gridChildren
  });
}
function positionChildren(params) {
  const { db, group, pcbChildren, itemCoordinates, gridLayout } = params;
  const groupCenter = group._getGlobalPcbPositionBeforeLayout();
  const allCircuitJson = db.toArray();
  for (let i = 0; i < pcbChildren.length; i++) {
    const child = pcbChildren[i];
    const childKey = child.getString() || `child-${i}`;
    const coordinates = itemCoordinates[childKey];
    if (!coordinates) {
      console.warn(
        `PCB grid layout: No coordinates found for child ${childKey}`
      );
      continue;
    }
    const targetX = groupCenter.x - gridLayout.containerWidth / 2 + coordinates.x + coordinates.width / 2;
    const targetY = groupCenter.y + gridLayout.containerHeight / 2 - coordinates.y - coordinates.height / 2;
    if (child.pcb_component_id) {
      repositionPcbComponentTo2(allCircuitJson, child.pcb_component_id, {
        x: targetX,
        y: targetY
      });
    } else {
      const groupChild = child;
      if (groupChild.pcb_group_id && groupChild.source_group_id) {
        repositionPcbGroupTo2(allCircuitJson, groupChild.source_group_id, {
          x: targetX,
          y: targetY
        });
      }
    }
  }
}
function updateGroupDimensions(params) {
  const { db, group, props, gridLayout } = params;
  if (group.pcb_group_id) {
    const groupCenter = group._getGlobalPcbPositionBeforeLayout();
    db.pcb_group.update(group.pcb_group_id, {
      width: props.width ?? gridLayout.containerWidth,
      height: props.height ?? gridLayout.containerHeight,
      center: groupCenter
    });
  }
}

// lib/components/primitive-components/Group/Group_doInitialPcbLayoutPack/Group_doInitialPcbLayoutPack.ts
import {
  PackSolver2 as PackSolver22,
  convertCircuitJsonToPackOutput,
  convertPackOutputToPackInput,
  getGraphicsFromPackOutput
} from "calculate-packing";
import { length as length3 } from "circuit-json";
import Debug6 from "debug";

// lib/utils/packing/solvePackSolverWithTimeout.ts
var solvePackSolverWithTimeout = (solver, timeoutMs, getCurrentTime = () => performance.now()) => {
  if (timeoutMs === void 0 || !Number.isFinite(timeoutMs) || timeoutMs <= 0) {
    solver.solve();
    return { elapsedMs: 0, timedOut: false };
  }
  const startedAt = getCurrentTime();
  let elapsedMs = 0;
  while (!solver.solved && !solver.failed) {
    solver.step();
    if (solver.solved || solver.failed) break;
    elapsedMs = getCurrentTime() - startedAt;
    if (elapsedMs >= timeoutMs) {
      return { elapsedMs, timedOut: true };
    }
  }
  return { elapsedMs, timedOut: false };
};

// lib/components/primitive-components/Group/Group_doInitialPcbLayoutPack/applyComponentConstraintClusters.ts
import * as kiwi2 from "@lume/kiwi";
var applyComponentConstraintClusters = (group, packInput) => {
  const constraints = group.children.filter(
    (c) => c.componentName === "Constraint" && c._parsedProps.pcb
  );
  const clusterByRoot = /* @__PURE__ */ new Map();
  const parent = {};
  const find = (x) => {
    if (parent[x] !== x) parent[x] = find(parent[x]);
    return parent[x];
  };
  const union = (a, b) => {
    const ra = find(a);
    const rb = find(b);
    if (ra !== rb) parent[rb] = ra;
  };
  const makeSet = (x) => {
    if (!(x in parent)) parent[x] = x;
  };
  const getIdFromSelector = (sel2) => {
    const name = sel2.startsWith(".") ? sel2.slice(1) : sel2;
    const child = group.children.find((c) => c.name === name);
    return child?.pcb_component_id ?? void 0;
  };
  for (const constraint of constraints) {
    const props = constraint._parsedProps;
    if ("left" in props && "right" in props) {
      const a = getIdFromSelector(props.left);
      const b = getIdFromSelector(props.right);
      if (a && b) {
        makeSet(a);
        makeSet(b);
        union(a, b);
      }
    } else if ("top" in props && "bottom" in props) {
      const a = getIdFromSelector(props.top);
      const b = getIdFromSelector(props.bottom);
      if (a && b) {
        makeSet(a);
        makeSet(b);
        union(a, b);
      }
    } else if ("for" in props && Array.isArray(props.for)) {
      const ids = props.for.map((s) => getIdFromSelector(s)).filter((s) => !!s);
      for (const id of ids) makeSet(id);
      for (let i = 1; i < ids.length; i++) union(ids[0], ids[i]);
    }
  }
  for (const id of Object.keys(parent)) {
    const rootId = find(id);
    if (!clusterByRoot.has(rootId))
      clusterByRoot.set(rootId, { componentIds: [], constraints: [] });
    clusterByRoot.get(rootId).componentIds.push(id);
  }
  for (const constraint of constraints) {
    const props = constraint._parsedProps;
    let compId;
    if ("left" in props) compId = getIdFromSelector(props.left);
    else if ("top" in props) compId = getIdFromSelector(props.top);
    else if ("for" in props) compId = getIdFromSelector(props.for[0]);
    if (!compId) continue;
    const root = find(compId);
    clusterByRoot.get(root)?.constraints.push(constraint);
  }
  const clusterMap = {};
  const packCompById = Object.fromEntries(
    packInput.components.map((c) => [c.componentId, c])
  );
  for (const [rootId, info] of clusterByRoot.entries()) {
    if (info.componentIds.length <= 1) continue;
    const solver = new kiwi2.Solver();
    const kVars = {};
    const getVar = (id, axis) => {
      const key = `${id}_${axis}`;
      if (!kVars[key]) kVars[key] = new kiwi2.Variable(key);
      return kVars[key];
    };
    const anchor = info.componentIds[0];
    solver.addConstraint(
      new kiwi2.Constraint(
        getVar(anchor, "x"),
        kiwi2.Operator.Eq,
        0,
        kiwi2.Strength.required
      )
    );
    solver.addConstraint(
      new kiwi2.Constraint(
        getVar(anchor, "y"),
        kiwi2.Operator.Eq,
        0,
        kiwi2.Strength.required
      )
    );
    for (const constraint of info.constraints) {
      const props = constraint._parsedProps;
      if ("xDist" in props) {
        const left = getIdFromSelector(props.left);
        const right = getIdFromSelector(props.right);
        if (left && right) {
          solver.addConstraint(
            new kiwi2.Constraint(
              new kiwi2.Expression(getVar(right, "x"), [-1, getVar(left, "x")]),
              kiwi2.Operator.Eq,
              props.xDist,
              kiwi2.Strength.required
            )
          );
        }
      } else if ("yDist" in props) {
        const top = getIdFromSelector(props.top);
        const bottom = getIdFromSelector(props.bottom);
        if (top && bottom) {
          solver.addConstraint(
            new kiwi2.Constraint(
              new kiwi2.Expression(getVar(top, "y"), [-1, getVar(bottom, "y")]),
              kiwi2.Operator.Eq,
              props.yDist,
              kiwi2.Strength.required
            )
          );
        }
      } else if ("sameX" in props && Array.isArray(props.for)) {
        const ids = props.for.map((s) => getIdFromSelector(s)).filter((s) => !!s);
        if (ids.length > 1) {
          const base = getVar(ids[0], "x");
          for (let i = 1; i < ids.length; i++) {
            solver.addConstraint(
              new kiwi2.Constraint(
                new kiwi2.Expression(getVar(ids[i], "x"), [-1, base]),
                kiwi2.Operator.Eq,
                0,
                kiwi2.Strength.required
              )
            );
          }
        }
      } else if ("sameY" in props && Array.isArray(props.for)) {
        const ids = props.for.map((s) => getIdFromSelector(s)).filter((s) => !!s);
        if (ids.length > 1) {
          const base = getVar(ids[0], "y");
          for (let i = 1; i < ids.length; i++) {
            solver.addConstraint(
              new kiwi2.Constraint(
                new kiwi2.Expression(getVar(ids[i], "y"), [-1, base]),
                kiwi2.Operator.Eq,
                0,
                kiwi2.Strength.required
              )
            );
          }
        }
      }
    }
    solver.updateVariables();
    const positions = {};
    for (const id of info.componentIds) {
      positions[id] = {
        x: getVar(id, "x").value(),
        y: getVar(id, "y").value()
      };
    }
    let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
    for (const id of info.componentIds) {
      const comp = packCompById[id];
      const pos = positions[id];
      if (!comp) continue;
      for (const pad of comp.pads) {
        const ax = pos.x + pad.offset.x;
        const ay = pos.y + pad.offset.y;
        minX = Math.min(minX, ax - pad.size.x / 2);
        maxX = Math.max(maxX, ax + pad.size.x / 2);
        minY = Math.min(minY, ay - pad.size.y / 2);
        maxY = Math.max(maxY, ay + pad.size.y / 2);
      }
    }
    const clusterCenter = { x: (minX + maxX) / 2, y: (minY + maxY) / 2 };
    const mergedPads = [];
    const relCenters = {};
    for (const id of info.componentIds) {
      const comp = packCompById[id];
      const pos = positions[id];
      if (!comp) continue;
      relCenters[id] = {
        x: pos.x - clusterCenter.x,
        y: pos.y - clusterCenter.y
      };
      for (const pad of comp.pads) {
        mergedPads.push({
          padId: pad.padId,
          networkId: pad.networkId,
          type: pad.type,
          size: pad.size,
          offset: {
            x: pos.x + pad.offset.x - clusterCenter.x,
            y: pos.y + pad.offset.y - clusterCenter.y
          }
        });
      }
    }
    packInput.components = packInput.components.filter(
      (c) => !info.componentIds.includes(c.componentId)
    );
    packInput.components.push({
      componentId: info.componentIds[0],
      pads: mergedPads,
      availableRotationDegrees: [0]
    });
    info.relativeCenters = relCenters;
    clusterMap[info.componentIds[0]] = info;
  }
  return clusterMap;
};

// lib/components/primitive-components/Group/Group_doInitialPcbLayoutPack/applyPackOutput.ts
import { translate as translate6, rotate as rotate4, compose as compose7 } from "transformation-matrix";
import {
  findBoundsAndCenter,
  transformPCBElements as transformPCBElements2
} from "@tscircuit/circuit-json-util";
import { normalizeDegrees as normalizeDegrees4 } from "@tscircuit/math-utils";
var updateCadRotation = ({
  db,
  pcbComponentId,
  rotationDegrees,
  layer
}) => {
  if (rotationDegrees == null) return;
  if (!db?.cad_component?.list) return;
  const cadComponent = db.cad_component.getWhere({
    pcb_component_id: pcbComponentId
  });
  if (!cadComponent) return;
  const delta = layer?.toLowerCase?.() === "bottom" ? -rotationDegrees : rotationDegrees;
  const currentRotationZ = cadComponent.rotation?.z ?? 0;
  const nextRotation = {
    ...cadComponent.rotation ?? { x: 0, y: 0, z: 0 },
    z: normalizeDegrees4(currentRotationZ + delta)
  };
  db.cad_component.update(cadComponent.cad_component_id, {
    rotation: nextRotation
  });
  cadComponent.rotation = nextRotation;
};
var isDescendantGroup = (db, groupId, ancestorId) => {
  if (groupId === ancestorId) return true;
  const group = db.source_group.get(groupId);
  if (!group || !group.parent_source_group_id) return false;
  return isDescendantGroup(db, group.parent_source_group_id, ancestorId);
};
var applyPackOutput = (group, packOutput, clusterMap, initialPackOutput) => {
  const { db } = group.root;
  for (const packedComponent of packOutput.components) {
    const { center, componentId, ccwRotationOffset, ccwRotationDegrees } = packedComponent;
    const cluster = clusterMap[componentId];
    if (cluster) {
      const rotationDegrees2 = ccwRotationDegrees ?? ccwRotationOffset ?? 0;
      const angleRad = rotationDegrees2 * Math.PI / 180;
      for (const memberId of cluster.componentIds) {
        const rel = cluster.relativeCenters[memberId];
        if (!rel) continue;
        db.pcb_component.update(memberId, {
          position_mode: "packed"
        });
        const rotatedRel = {
          x: rel.x * Math.cos(angleRad) - rel.y * Math.sin(angleRad),
          y: rel.x * Math.sin(angleRad) + rel.y * Math.cos(angleRad)
        };
        const member = db.pcb_component.get(memberId);
        if (!member) continue;
        const originalCenter2 = member.center;
        const transformMatrix2 = compose7(
          group._computePcbGlobalTransformBeforeLayout(),
          translate6(center.x + rotatedRel.x, center.y + rotatedRel.y),
          rotate4(angleRad),
          translate6(-originalCenter2.x, -originalCenter2.y)
        );
        const related = db.toArray().filter(
          (elm) => "pcb_component_id" in elm && elm.pcb_component_id === memberId
        );
        transformPCBElements2(related, transformMatrix2);
        updateCadRotation({
          db,
          pcbComponentId: memberId,
          rotationDegrees: rotationDegrees2,
          layer: member.layer
        });
      }
      continue;
    }
    const pcbComponent = db.pcb_component.get(componentId);
    if (pcbComponent) {
      db.pcb_component.update(componentId, {
        position_mode: "packed"
      });
      const currentGroupId = group.source_group_id;
      const sourceComponent = db.source_component.get(
        pcbComponent.source_component_id
      );
      const componentGroupId = sourceComponent?.source_group_id;
      if (componentGroupId !== void 0 && !isDescendantGroup(db, componentGroupId, currentGroupId)) {
        continue;
      }
      const originalCenter2 = pcbComponent.center;
      const rotationDegrees2 = ccwRotationDegrees ?? ccwRotationOffset ?? 0;
      const transformMatrix2 = compose7(
        group._computePcbGlobalTransformBeforeLayout(),
        translate6(center.x, center.y),
        rotate4(rotationDegrees2 * Math.PI / 180),
        translate6(-originalCenter2.x, -originalCenter2.y)
      );
      const related = db.toArray().filter(
        (elm) => "pcb_component_id" in elm && elm.pcb_component_id === componentId
      );
      transformPCBElements2(related, transformMatrix2);
      updateCadRotation({
        db,
        pcbComponentId: componentId,
        rotationDegrees: rotationDegrees2,
        layer: pcbComponent.layer
      });
      continue;
    }
    const pcbGroup = db.pcb_group.list().find((g) => g.source_group_id === componentId);
    if (!pcbGroup) continue;
    const initialPackedComponent = initialPackOutput.components.find(
      (component) => component.componentId === componentId
    );
    const originalCenter = initialPackedComponent?.center ?? pcbGroup.center;
    const rotationDegrees = ccwRotationDegrees ?? ccwRotationOffset ?? 0;
    const transformMatrix = compose7(
      group._computePcbGlobalTransformBeforeLayout(),
      translate6(center.x, center.y),
      rotate4(rotationDegrees * Math.PI / 180),
      translate6(-originalCenter.x, -originalCenter.y)
    );
    const relatedElements = db.toArray().filter((elm) => {
      if ("source_group_id" in elm && elm.source_group_id) {
        if (elm.source_group_id === componentId) {
          return true;
        }
        if (isDescendantGroup(db, elm.source_group_id, componentId)) {
          return true;
        }
      }
      if ("source_component_id" in elm && elm.source_component_id) {
        const sourceComponent = db.source_component.get(elm.source_component_id);
        if (sourceComponent?.source_group_id) {
          if (sourceComponent.source_group_id === componentId) {
            return true;
          }
          if (isDescendantGroup(db, sourceComponent.source_group_id, componentId)) {
            return true;
          }
        }
      }
      if ("pcb_component_id" in elm && elm.pcb_component_id) {
        const pcbComp = db.pcb_component.get(elm.pcb_component_id);
        if (pcbComp?.source_component_id) {
          const sourceComp = db.source_component.get(
            pcbComp.source_component_id
          );
          if (sourceComp?.source_group_id) {
            if (sourceComp.source_group_id === componentId) {
              return true;
            }
            if (isDescendantGroup(db, sourceComp.source_group_id, componentId)) {
              return true;
            }
          }
        }
      }
      return false;
    });
    for (const elm of relatedElements) {
      if (elm.type === "pcb_component") {
        db.pcb_component.update(elm.pcb_component_id, {
          position_mode: "packed"
        });
      }
    }
    transformPCBElements2(relatedElements, transformMatrix);
    db.pcb_group.update(pcbGroup.pcb_group_id, { center });
  }
  if (group.pcb_group_id) {
    const groupPcbComponents = db.pcb_component.list({
      pcb_group_id: group.pcb_group_id
    });
    group.calculatePcbGroupBounds(findBoundsAndCenter(groupPcbComponents));
  }
};

// lib/components/primitive-components/Group/Group_doInitialPcbLayoutPack/getPackInputsByPcbLayer.ts
var normalizeLayer = (layer) => (layer ?? "top").toLowerCase();
var getComponentLayer = (component, db) => {
  const layers = /* @__PURE__ */ new Set();
  const directPcbComponent = db.pcb_component.get(component.componentId);
  if (directPcbComponent) {
    layers.add(normalizeLayer(directPcbComponent.layer));
  }
  for (const pad of component.pads) {
    const via = db.pcb_via.get(pad.padId);
    const pcbComponentId = db.pcb_smtpad.get(pad.padId)?.pcb_component_id ?? db.pcb_plated_hole.get(pad.padId)?.pcb_component_id ?? via?.pcb_component_id;
    if (!pcbComponentId) continue;
    const pcbComponent = db.pcb_component.get(pcbComponentId);
    if (pcbComponent) layers.add(normalizeLayer(pcbComponent.layer));
  }
  return layers.size === 1 ? layers.values().next().value : void 0;
};
var getPackInputsByPcbLayer = (packInput, db) => {
  const componentsByLayer = /* @__PURE__ */ new Map();
  for (const component of packInput.components) {
    const layer = getComponentLayer(component, db);
    if (!layer) return [packInput];
    const components = componentsByLayer.get(layer) ?? [];
    components.push(component);
    componentsByLayer.set(layer, components);
  }
  if (componentsByLayer.size <= 1) return [packInput];
  const layerPriority = (layer) => {
    if (layer === "top") return 0;
    if (layer === "bottom") return 1;
    return 2;
  };
  return Array.from(componentsByLayer.entries()).sort(([layerA], [layerB]) => {
    const priorityDifference = layerPriority(layerA) - layerPriority(layerB);
    return priorityDifference || layerA.localeCompare(layerB);
  }).map(([, components]) => ({
    ...packInput,
    components
  }));
};

// lib/components/primitive-components/Group/Group_doInitialPcbLayoutPack/Group_doInitialPcbLayoutPack.ts
var DEFAULT_MIN_GAP = "1mm";
var debug4 = Debug6("Group_doInitialPcbLayoutPack");
var Group_doInitialPcbLayoutPack = (group) => {
  const { db } = group.root;
  const { _parsedProps: props } = group;
  group.root?.emit("packing:start", {
    subcircuit_id: group.subcircuit_id,
    componentDisplayName: group.getString()
  });
  const {
    packOrderStrategy,
    packPlacementStrategy,
    gap: gapProp,
    pcbGap,
    pcbPackGap
  } = props;
  const gap = pcbPackGap ?? pcbGap ?? gapProp;
  const gapMm = length3.parse(gap ?? DEFAULT_MIN_GAP);
  const chipMarginsMap = {};
  const staticPcbComponentIds = /* @__PURE__ */ new Set();
  const collectMargins = (comp) => {
    if (comp?.pcb_component_id && comp?._parsedProps) {
      const props2 = comp._parsedProps;
      const left = length3.parse(props2.pcbMarginLeft ?? props2.pcbMarginX ?? 0);
      const right = length3.parse(props2.pcbMarginRight ?? props2.pcbMarginX ?? 0);
      const top = length3.parse(props2.pcbMarginTop ?? props2.pcbMarginY ?? 0);
      const bottom = length3.parse(
        props2.pcbMarginBottom ?? props2.pcbMarginY ?? 0
      );
      if (left || right || top || bottom) {
        chipMarginsMap[comp.pcb_component_id] = { left, right, top, bottom };
      }
    }
    if (comp?.children) comp.children.forEach(collectMargins);
  };
  collectMargins(group);
  const excludedPcbGroupIds = /* @__PURE__ */ new Set();
  for (const child of group.children) {
    const childIsGroupOrNormalComponent = child;
    if (childIsGroupOrNormalComponent._isNormalComponent && childIsGroupOrNormalComponent.isRelativelyPositioned?.()) {
      if (childIsGroupOrNormalComponent.pcb_component_id) {
        staticPcbComponentIds.add(
          childIsGroupOrNormalComponent.pcb_component_id
        );
      }
      if (childIsGroupOrNormalComponent.pcb_group_id) {
        excludedPcbGroupIds.add(
          childIsGroupOrNormalComponent.pcb_group_id
        );
      }
    }
  }
  const isDescendantGroup2 = (db2, groupId, ancestorId) => {
    if (groupId === ancestorId) return true;
    const group2 = db2.source_group.get(groupId);
    if (!group2 || !group2.parent_source_group_id) return false;
    return isDescendantGroup2(db2, group2.parent_source_group_id, ancestorId);
  };
  if (excludedPcbGroupIds.size > 0) {
    for (const element of db.toArray()) {
      if (element.type !== "pcb_component") continue;
      const sourceComponent = db.source_component.get(
        element.source_component_id
      );
      if (!sourceComponent?.source_group_id) continue;
      for (const groupId of excludedPcbGroupIds) {
        if (isDescendantGroup2(db, sourceComponent.source_group_id, groupId)) {
          staticPcbComponentIds.add(element.pcb_component_id);
        }
      }
    }
  }
  const filteredCircuitJson = db.toArray();
  let bounds;
  if (props.width !== void 0 && props.height !== void 0) {
    const widthMm = length3.parse(props.width);
    const heightMm = length3.parse(props.height);
    bounds = {
      minX: -widthMm / 2,
      maxX: widthMm / 2,
      minY: -heightMm / 2,
      maxY: heightMm / 2
    };
  }
  const initialPackOutput = convertCircuitJsonToPackOutput(
    filteredCircuitJson,
    {
      source_group_id: group.source_group_id,
      // shouldAddInnerObstacles: true,
      chipMarginsMap,
      staticPcbComponentIds: Array.from(staticPcbComponentIds)
    }
  );
  const packInput = {
    ...convertPackOutputToPackInput(initialPackOutput),
    // @ts-expect-error calculate-packing is missing some supported strategies
    packOrderStrategy: packOrderStrategy ?? "largest_to_smallest",
    packPlacementStrategy: packPlacementStrategy ?? "minimum_sum_squared_distance_to_network",
    minGap: gapMm,
    bounds
  };
  const clusterMap = applyComponentConstraintClusters(group, packInput);
  if (debug4.enabled) {
    group.root?.emit("debug:logOutput", {
      type: "debug:logOutput",
      name: `packInput-circuitjson-${group.name}`,
      content: JSON.stringify(db.toArray())
    });
    group.root?.emit("debug:logOutput", {
      type: "debug:logOutput",
      name: `packInput-${group.name}`,
      content: packInput
    });
  }
  const packInputs = getPackInputsByPcbLayer(packInput, db);
  const packOutput = {
    ...packInput,
    components: []
  };
  let packingFailed = false;
  const reportPackingError = (solverErrorMessage) => {
    const message = `Unable to pack all PCB components within the layout bounds: ${solverErrorMessage}`;
    db.pcb_packing_error.insert({
      error_type: "pcb_packing_error",
      message,
      pcb_group_id: group.pcb_group_id ?? void 0,
      subcircuit_id: group.subcircuit_id ?? void 0
    });
    group.root?.emit("packing:error", {
      subcircuit_id: group.subcircuit_id,
      componentDisplayName: group.getString(),
      error: { message }
    });
  };
  try {
    for (const layerPackInput of packInputs) {
      const componentIdsForLayer = new Set(
        layerPackInput.components.map((component) => component.componentId)
      );
      const networkReferenceComponents = packOutput.components.map(
        (component) => ({
          ...component,
          componentId: `network_reference_${component.componentId}`,
          isStatic: true,
          // Keep opposite-layer pads available to the placement objective while
          // reducing their collision geometry to a point at the component center.
          courtyard: {
            offsetFromCenter: { x: 0, y: 0 },
            width: Number.EPSILON,
            height: Number.EPSILON
          }
        })
      );
      const solverInput = {
        ...layerPackInput,
        components: [
          ...networkReferenceComponents,
          ...layerPackInput.components
        ]
      };
      const solver = new PackSolver22(solverInput);
      const solverParams = solver.getConstructorParams();
      group.root?.emit("solver:started", {
        type: "solver:started",
        solverName: "PackSolver2",
        solverParams,
        solverConstructorArgs: [solverParams],
        componentName: group.getString()
      });
      const pcbPackSolverTimeoutMs = group.root?.platform?.pcbPackSolverTimeoutMs;
      const { timedOut } = solvePackSolverWithTimeout(
        solver,
        pcbPackSolverTimeoutMs
      );
      if (timedOut) {
        packingFailed = true;
        reportPackingError(
          `PackSolver2 timed out after ${pcbPackSolverTimeoutMs}ms`
        );
      }
      if (solver.failed) {
        packingFailed = true;
        const solverErrorMessage = solver.error ?? solver.activeSubSolver?.error ?? "No valid packing solution found";
        reportPackingError(solverErrorMessage);
      }
      packOutput.components.push(
        ...solver.packedComponents.filter(
          (component) => componentIdsForLayer.has(component.componentId)
        )
      );
      if (timedOut) break;
    }
  } catch (error) {
    reportPackingError(error instanceof Error ? error.message : String(error));
    throw error;
  }
  if (debug4.enabled && global?.debugGraphics) {
    const graphics = getGraphicsFromPackOutput(packOutput);
    graphics.title = `packOutput-${group.name}`;
    global.debugGraphics?.push(graphics);
  }
  applyPackOutput(group, packOutput, clusterMap, initialPackOutput);
  if (packingFailed) return;
  group.root?.emit("packing:end", {
    subcircuit_id: group.subcircuit_id,
    componentDisplayName: group.getString()
  });
};

// lib/components/primitive-components/Group/Group_doInitialSchematicBoxComponentRender.ts
var parsePinNumberFromPortName = (name) => {
  if (!name) return null;
  const match = name.match(/^pin(\d+)$/i) ?? name.match(/^(\d+)$/);
  if (!match) return null;
  const pinNumber = Number(match[1]);
  return Number.isFinite(pinNumber) && pinNumber > 0 ? pinNumber : null;
};
var getDirectGroupPorts = (group) => {
  return group.children.filter((child) => {
    return child.componentName === "Port";
  });
};
var normalizeGroupSchematicBoxPorts = (group) => {
  const { db } = group.root;
  const ports = getDirectGroupPorts(group);
  for (const port of ports) {
    if (port._parsedProps.pinNumber !== void 0) continue;
    const pinNumber = parsePinNumberFromPortName(port._parsedProps.name);
    if (pinNumber === null) continue;
    port._parsedProps.pinNumber = pinNumber;
  }
  const usedPinNumbers = new Set(
    ports.map((port) => port._parsedProps.pinNumber).filter((pinNumber) => pinNumber !== void 0)
  );
  let nextPinNumber = 1;
  for (const port of ports) {
    if (port._parsedProps.pinNumber === void 0) {
      while (usedPinNumbers.has(nextPinNumber)) nextPinNumber++;
      port._parsedProps.pinNumber = nextPinNumber;
      usedPinNumbers.add(nextPinNumber);
    }
    if (port.source_port_id) {
      db.source_port.update(port.source_port_id, {
        pin_number: port._parsedProps.pinNumber,
        port_hints: port.getNameAndAliases()
      });
    }
  }
};
var getGroupSchematicBoxPinLabels = (group) => {
  const pinLabels = {};
  for (const port of getDirectGroupPorts(group)) {
    const pinNumber = port._parsedProps.pinNumber;
    const name = port._parsedProps.name;
    if (pinNumber === void 0 || !name) continue;
    if (name === `pin${pinNumber}` || name === String(pinNumber)) continue;
    pinLabels[`pin${pinNumber}`] = name;
  }
  return pinLabels;
};
var Group_doInitialSchematicBoxComponentRender = (group) => {
  if (group.root?.schematicDisabled) return;
  const { db } = group.root;
  const props = group._parsedProps;
  if (props.schPinSpacing !== void 0) {
    db.source_property_ignored_warning.insert({
      source_component_id: group.source_group_id ?? "",
      property_name: "schPinSpacing",
      error_type: "source_property_ignored_warning",
      message: "schPinSpacing is deprecated and will be ignored. Pin spacing is always 0.2."
    });
  }
  normalizeGroupSchematicBoxPorts(group);
  const dimensions = group._getSchematicBoxDimensions();
  if (!dimensions) return;
  const center = group._getGlobalSchematicPositionBeforeLayout();
  const size = dimensions.getSize();
  const portLabels = group._getPinLabelsFromPorts();
  const schPortArrangement = group._getSchematicPortArrangement();
  const schematicComponent = db.schematic_component.insert({
    center,
    size,
    source_group_id: group.source_group_id,
    schematic_group_id: group.schematic_group_id ?? void 0,
    subcircuit_id: group.subcircuit_id ?? group.getSubcircuit()?.subcircuit_id ?? void 0,
    is_schematic_group: true,
    is_box_with_pins: true,
    port_arrangement: underscorifyPortArrangement(schPortArrangement),
    pin_spacing: 0.2,
    pin_styles: underscorifyPinStyles(
      props.schPinStyle,
      portLabels
    ),
    port_labels: portLabels,
    schematic_sheet_id: group._resolveSchematicSheetId()
  });
  group.schematic_component_id = schematicComponent.schematic_component_id;
  if (group.schematic_group_id) {
    db.schematic_group.update(group.schematic_group_id, {
      center,
      width: size.width,
      height: size.height,
      schematic_component_ids: [schematicComponent.schematic_component_id]
    });
  }
  const title = props.schTitle ?? props.name;
  if (title) {
    db.schematic_text.insert({
      text: title,
      schematic_component_id: schematicComponent.schematic_component_id,
      anchor: "left",
      rotation: 0,
      position: {
        x: center.x - size.width / 2,
        y: center.y + size.height / 2 + 0.13
      },
      color: "#006464",
      font_size: 0.18,
      schematic_sheet_id: group._resolveSchematicSheetId()
    });
  }
};

// lib/components/primitive-components/Group/Group_doInitialSchematicLayoutFlex.ts
import {
  getCircuitJsonTree,
  getMinimumFlexContainer as getMinimumFlexContainer2,
  repositionSchematicComponentTo,
  repositionSchematicGroupTo
} from "@tscircuit/circuit-json-util";
import { RootFlexBox as RootFlexBox2 } from "@tscircuit/miniflex";
import { length as length4 } from "circuit-json";
var getSizeOfTreeNodeChild = (db, child) => {
  const { sourceComponent, sourceGroup } = child;
  if (child.nodeType === "component") {
    const schComponent = db.schematic_component.getWhere({
      source_component_id: sourceComponent?.source_component_id
    });
    if (!schComponent?.size) return null;
    return {
      width: schComponent.size.width,
      height: schComponent.size.height
    };
  }
  if (child.nodeType === "group") {
    const schGroup = db.schematic_group.getWhere({
      source_group_id: sourceGroup?.source_group_id
    });
    if (schGroup?.width && schGroup?.height) {
      return {
        width: schGroup.width,
        height: schGroup.height
      };
    }
    const groupComponents = db.schematic_component.list({
      schematic_group_id: schGroup?.schematic_group_id
    });
    let minX = Infinity;
    let maxX = -Infinity;
    let minY = Infinity;
    let maxY = -Infinity;
    for (const comp of groupComponents) {
      if (comp.center && comp.size) {
        const halfWidth = comp.size.width / 2;
        const halfHeight = comp.size.height / 2;
        minX = Math.min(minX, comp.center.x - halfWidth);
        maxX = Math.max(maxX, comp.center.x + halfWidth);
        minY = Math.min(minY, comp.center.y - halfHeight);
        maxY = Math.max(maxY, comp.center.y + halfHeight);
      }
    }
    const groupWidth = maxX - minX;
    const groupHeight = maxY - minY;
    return {
      width: groupWidth,
      height: groupHeight
    };
  }
  return null;
};
var Group_doInitialSchematicLayoutFlex = (group) => {
  const { db } = group.root;
  const props = group._parsedProps;
  const tree = getCircuitJsonTree(db.toArray(), {
    source_group_id: group.source_group_id
  });
  const rawJustify = props.schJustifyContent ?? props.justifyContent;
  const rawAlign = props.schAlignItems ?? props.alignItems;
  const rawGap = props.schFlexGap ?? props.schGap ?? props.gap;
  const direction = props.schFlexDirection ?? "row";
  const justifyContent = {
    start: "flex-start",
    end: "flex-end",
    "flex-start": "flex-start",
    "flex-end": "flex-end",
    stretch: "space-between",
    "space-between": "space-between",
    "space-around": "space-around",
    "space-evenly": "space-evenly",
    center: "center"
  }[rawJustify ?? "space-between"];
  const alignItems = {
    start: "flex-start",
    end: "flex-end",
    "flex-start": "flex-start",
    "flex-end": "flex-end",
    stretch: "stretch",
    center: "center"
  }[rawAlign ?? "center"];
  if (!justifyContent) {
    throw new Error(`Invalid justifyContent value: "${rawJustify}"`);
  }
  if (!alignItems) {
    throw new Error(`Invalid alignItems value: "${rawAlign}"`);
  }
  let rowGap = 0;
  let columnGap = 0;
  if (typeof rawGap === "object") {
    rowGap = rawGap.y ?? 0;
    columnGap = rawGap.x ?? 0;
  } else if (typeof rawGap === "number") {
    rowGap = rawGap;
    columnGap = rawGap;
  } else if (typeof rawGap === "string") {
    rowGap = length4.parse(rawGap);
    columnGap = length4.parse(rawGap);
  }
  let minFlexContainer;
  let width = props.width ?? props.schWidth ?? void 0;
  let height = props.height ?? props.schHeight ?? void 0;
  const isInline = Boolean(width === void 0 || height === void 0);
  if (isInline) {
    minFlexContainer = getMinimumFlexContainer2(
      tree.childNodes.map((child) => getSizeOfTreeNodeChild(db, child)).filter((size) => size !== null),
      {
        alignItems,
        justifyContent,
        direction,
        rowGap,
        columnGap
      }
    );
    width = minFlexContainer.width;
    height = minFlexContainer.height;
  }
  const flexBox = new RootFlexBox2(width, height, {
    alignItems,
    justifyContent,
    direction,
    rowGap,
    columnGap
  });
  for (const child of tree.childNodes) {
    const size = getSizeOfTreeNodeChild(db, child);
    flexBox.addChild({
      metadata: child,
      width: size?.width ?? 0,
      height: size?.height ?? 0,
      flexBasis: !size ? void 0 : direction === "row" ? size.width : size.height
    });
  }
  flexBox.build();
  const bounds = {
    minX: Infinity,
    minY: Infinity,
    maxX: -Infinity,
    maxY: -Infinity,
    width: 0,
    height: 0
  };
  for (const child of flexBox.children) {
    bounds.minX = Math.min(bounds.minX, child.position.x);
    bounds.minY = Math.min(bounds.minY, child.position.y);
    bounds.maxX = Math.max(bounds.maxX, child.position.x + child.size.width);
    bounds.maxY = Math.max(bounds.maxY, child.position.y + child.size.height);
  }
  bounds.width = bounds.maxX - bounds.minX;
  bounds.height = bounds.maxY - bounds.minY;
  const offset = {
    x: -(bounds.maxX + bounds.minX) / 2,
    y: -(bounds.maxY + bounds.minY) / 2
  };
  const allCircuitJson = db.toArray();
  for (const child of flexBox.children) {
    const { sourceComponent, sourceGroup } = child.metadata;
    if (sourceComponent) {
      const schComponent = db.schematic_component.getWhere({
        source_component_id: sourceComponent.source_component_id
      });
      if (!schComponent) continue;
      repositionSchematicComponentTo(
        allCircuitJson,
        schComponent.schematic_component_id,
        {
          x: child.position.x + child.size.width / 2 + offset.x,
          y: child.position.y + child.size.height / 2 + offset.y
        }
      );
    }
    if (sourceGroup) {
      const schGroup = db.schematic_group.getWhere({
        source_group_id: sourceGroup.source_group_id
      });
      if (!schGroup) continue;
      repositionSchematicGroupTo(allCircuitJson, sourceGroup.source_group_id, {
        x: child.position.x + child.size.width / 2 + offset.x,
        y: child.position.y + child.size.height / 2 + offset.y
      });
    }
  }
  if (group.schematic_group_id) {
    db.schematic_group.update(group.schematic_group_id, {
      width: bounds.width,
      height: bounds.height
    });
  }
};

// lib/components/primitive-components/Group/Group_doInitialSchematicLayoutGrid.ts
import { length as length5 } from "circuit-json";

// lib/components/primitive-components/Group/utils/updateSchematicPrimitivesForLayoutShift.ts
function updateSchematicPrimitivesForLayoutShift({
  db,
  schematicComponentId,
  deltaX,
  deltaY
}) {
  const rects = db.schematic_rect.list({
    schematic_component_id: schematicComponentId
  });
  for (const rect of rects) {
    rect.center.x += deltaX;
    rect.center.y += deltaY;
  }
  const lines = db.schematic_line.list({
    schematic_component_id: schematicComponentId
  });
  for (const line of lines) {
    line.x1 += deltaX;
    line.y1 += deltaY;
    line.x2 += deltaX;
    line.y2 += deltaY;
  }
  const circles = db.schematic_circle.list({
    schematic_component_id: schematicComponentId
  });
  for (const circle of circles) {
    circle.center.x += deltaX;
    circle.center.y += deltaY;
  }
  const arcs = db.schematic_arc.list({
    schematic_component_id: schematicComponentId
  });
  for (const arc of arcs) {
    arc.center.x += deltaX;
    arc.center.y += deltaY;
  }
  const paths = db.schematic_path.list({
    schematic_component_id: schematicComponentId
  });
  for (const path of paths) {
    for (const point6 of path.points) {
      point6.x += deltaX;
      point6.y += deltaY;
    }
  }
}

// lib/components/primitive-components/Group/Group_doInitialSchematicLayoutGrid.ts
function Group_doInitialSchematicLayoutGrid(group) {
  const { db } = group.root;
  const props = group._parsedProps;
  const schematicChildren = group.children.filter((child) => {
    const isExplicitlyPositioned = child._parsedProps?.schX !== void 0 || child._parsedProps?.schY !== void 0;
    return child.schematic_component_id && !isExplicitlyPositioned;
  });
  if (schematicChildren.length === 0) return;
  let maxCellWidth = 0;
  let maxCellHeight = 0;
  for (const child of schematicChildren) {
    const schComp = db.schematic_component.get(child.schematic_component_id);
    if (schComp?.size) {
      maxCellWidth = Math.max(maxCellWidth, schComp.size.width);
      maxCellHeight = Math.max(maxCellHeight, schComp.size.height);
    }
  }
  if (maxCellWidth === 0 && schematicChildren.length > 0) maxCellWidth = 1;
  if (maxCellHeight === 0 && schematicChildren.length > 0) maxCellHeight = 1;
  let gridColsOption = props.gridCols;
  let gridRowsOption = void 0;
  let gridGapOption = props.gridGap;
  let gridRowGapOption = props.gridRowGap;
  let gridColumnGapOption = props.gridColumnGap;
  if (props.schLayout?.grid) {
    gridColsOption = props.schLayout.grid.cols ?? gridColsOption;
    gridRowsOption = props.schLayout.grid.rows;
    gridGapOption = props.schLayout.gridGap ?? gridGapOption;
    gridRowGapOption = props.schLayout.gridRowGap ?? gridRowGapOption;
    gridColumnGapOption = props.schLayout.gridColumnGap ?? gridColumnGapOption;
  }
  let numCols;
  let numRows;
  if (gridColsOption !== void 0 && gridRowsOption !== void 0) {
    numCols = gridColsOption;
    numRows = gridRowsOption;
  } else if (gridColsOption !== void 0) {
    numCols = gridColsOption;
    numRows = Math.ceil(schematicChildren.length / numCols);
  } else if (gridRowsOption !== void 0) {
    numRows = gridRowsOption;
    numCols = Math.ceil(schematicChildren.length / numRows);
  } else {
    numCols = Math.ceil(Math.sqrt(schematicChildren.length));
    numRows = Math.ceil(schematicChildren.length / numCols);
  }
  if (numCols === 0 && schematicChildren.length > 0) numCols = 1;
  if (numRows === 0 && schematicChildren.length > 0)
    numRows = schematicChildren.length;
  let gridGapX;
  let gridGapY;
  const parseGap = (val) => {
    if (val === void 0) return void 0;
    return typeof val === "number" ? val : length5.parse(val);
  };
  if (gridRowGapOption !== void 0 || gridColumnGapOption !== void 0) {
    const fallbackX = typeof gridGapOption === "object" && gridGapOption !== null ? gridGapOption.x : gridGapOption;
    const fallbackY = typeof gridGapOption === "object" && gridGapOption !== null ? gridGapOption.y : gridGapOption;
    gridGapX = parseGap(gridColumnGapOption ?? fallbackX) ?? 1;
    gridGapY = parseGap(gridRowGapOption ?? fallbackY) ?? 1;
  } else if (typeof gridGapOption === "number") {
    gridGapX = gridGapOption;
    gridGapY = gridGapOption;
  } else if (typeof gridGapOption === "string") {
    const parsed = length5.parse(gridGapOption);
    gridGapX = parsed;
    gridGapY = parsed;
  } else if (typeof gridGapOption === "object" && gridGapOption !== null) {
    const xRaw = gridGapOption.x;
    const yRaw = gridGapOption.y;
    gridGapX = typeof xRaw === "number" ? xRaw : length5.parse(xRaw ?? "0mm");
    gridGapY = typeof yRaw === "number" ? yRaw : length5.parse(yRaw ?? "0mm");
  } else {
    gridGapX = 1;
    gridGapY = 1;
  }
  const totalGridWidth = numCols * maxCellWidth + Math.max(0, numCols - 1) * gridGapX;
  const totalGridHeight = numRows * maxCellHeight + Math.max(0, numRows - 1) * gridGapY;
  const groupCenter = group._getGlobalSchematicPositionBeforeLayout();
  const firstCellCenterX = groupCenter.x - totalGridWidth / 2 + maxCellWidth / 2;
  const firstCellCenterY = groupCenter.y + totalGridHeight / 2 - maxCellHeight / 2;
  for (let i = 0; i < schematicChildren.length; i++) {
    const child = schematicChildren[i];
    if (!child.schematic_component_id) continue;
    const row = Math.floor(i / numCols);
    const col = i % numCols;
    if (row >= numRows || col >= numCols) {
      console.warn(
        `Schematic grid layout: Child ${child.getString()} at index ${i} (row ${row}, col ${col}) exceeds specified grid dimensions (${numRows}x${numCols}). Skipping placement.`
      );
      continue;
    }
    const targetCellCenterX = firstCellCenterX + col * (maxCellWidth + gridGapX);
    const targetCellCenterY = firstCellCenterY - row * (maxCellHeight + gridGapY);
    const schComp = db.schematic_component.get(child.schematic_component_id);
    if (schComp) {
      const oldChildCenter = schComp.center;
      const newChildCenter = { x: targetCellCenterX, y: targetCellCenterY };
      db.schematic_component.update(child.schematic_component_id, {
        center: newChildCenter
      });
      const deltaX = newChildCenter.x - oldChildCenter.x;
      const deltaY = newChildCenter.y - oldChildCenter.y;
      const schPorts = db.schematic_port.list({
        schematic_component_id: child.schematic_component_id
      });
      for (const port of schPorts) {
        db.schematic_port.update(port.schematic_port_id, {
          center: { x: port.center.x + deltaX, y: port.center.y + deltaY }
        });
      }
      const schTexts = db.schematic_text.list({
        schematic_component_id: child.schematic_component_id
      });
      for (const text of schTexts) {
        db.schematic_text.update(text.schematic_text_id, {
          position: {
            x: text.position.x + deltaX,
            y: text.position.y + deltaY
          }
        });
      }
      updateSchematicPrimitivesForLayoutShift({
        db,
        schematicComponentId: child.schematic_component_id,
        deltaX,
        deltaY
      });
    }
  }
  if (group.schematic_group_id) {
    db.schematic_group.update(group.schematic_group_id, {
      width: totalGridWidth,
      height: totalGridHeight,
      center: groupCenter
    });
  }
}

// lib/components/primitive-components/Group/Group_doInitialSchematicLayoutMatchAdapt.ts
import { convertCircuitJsonToBpc } from "circuit-json-to-bpc";
import { getGraphicsForBpcGraph, layoutSchematicGraphVariants } from "bpc-graph";
import Debug7 from "debug";
import { buildSubtree } from "@tscircuit/circuit-json-util";
var debug5 = Debug7("Group_doInitialSchematicLayoutMatchAdapt");
function Group_doInitialSchematicLayoutMatchAdapt(group) {
  const { db } = group.root;
  const subtreeCircuitJson = buildSubtree(db.toArray(), {
    source_group_id: group.source_group_id
  });
  const shouldSkip = subtreeCircuitJson.filter((item) => item.type === "source_component").length <= 1;
  if (shouldSkip) return;
  const floatingGraph = convertCircuitJsonToBpc(subtreeCircuitJson);
  if (debug5.enabled && global?.debugGraphics) {
    global.debugGraphics?.push(
      getGraphicsForBpcGraph(floatingGraph, {
        title: `floatingBpcGraph-${group.name}`
      })
    );
  }
  const floatingGraphNoNotConnected = {
    boxes: floatingGraph.boxes,
    pins: floatingGraph.pins.map((p) => ({
      ...p,
      color: p.color.replace("not_connected", "normal")
    }))
  };
  const { result: laidOutBpcGraph } = layoutSchematicGraphVariants(
    [
      { variantName: "default", floatingGraph },
      {
        variantName: "noNotConnected",
        floatingGraph: floatingGraphNoNotConnected
      }
    ],
    {
      singletonKeys: ["vcc/2", "gnd/2"],
      centerPinColors: ["netlabel_center", "component_center"],
      floatingBoxIdsWithMutablePinOffsets: new Set(
        floatingGraph.boxes.filter((box) => {
          const boxPins = floatingGraph.pins.filter(
            (p) => p.boxId === box.boxId
          );
          const nonCenterBoxPins = boxPins.filter(
            (bp) => !bp.color.includes("center")
          );
          if (nonCenterBoxPins.length <= 2) {
            return true;
          }
          return false;
        }).map((b) => b.boxId)
      ),
      corpus: {}
    }
  );
  if (debug5.enabled && global?.debugGraphics) {
    global.debugGraphics?.push(
      getGraphicsForBpcGraph(laidOutBpcGraph, {
        title: `laidOutBpcGraph-${group.name}`
      })
    );
  }
  const groupOffset = group._getGlobalSchematicPositionBeforeLayout();
  for (const box of laidOutBpcGraph.boxes) {
    if (!box.center) continue;
    const schematic_component2 = db.schematic_component.get(box.boxId);
    if (schematic_component2) {
      const newCenter = {
        x: box.center.x + groupOffset.x,
        y: box.center.y + groupOffset.y
      };
      const ports = db.schematic_port.list({
        schematic_component_id: schematic_component2.schematic_component_id
      });
      const texts = db.schematic_text.list({
        schematic_component_id: schematic_component2.schematic_component_id
      });
      const positionDelta = {
        x: newCenter.x - schematic_component2.center.x,
        y: newCenter.y - schematic_component2.center.y
      };
      for (const port of ports) {
        port.center.x += positionDelta.x;
        port.center.y += positionDelta.y;
      }
      for (const text of texts) {
        text.position.x += positionDelta.x;
        text.position.y += positionDelta.y;
      }
      updateSchematicPrimitivesForLayoutShift({
        db,
        schematicComponentId: schematic_component2.schematic_component_id,
        deltaX: positionDelta.x,
        deltaY: positionDelta.y
      });
      schematic_component2.center = newCenter;
      continue;
    }
    const schematic_net_label = db.schematic_net_label.get(box.boxId);
    if (schematic_net_label) {
      const pin = laidOutBpcGraph.pins.find(
        (p) => p.boxId === box.boxId && p.color === "netlabel_center"
      );
      if (!pin) {
        throw new Error(`No pin found for net label: ${box.boxId}`);
      }
      const finalCenter = {
        x: box.center.x + groupOffset.x,
        y: box.center.y + groupOffset.y
      };
      schematic_net_label.center = finalCenter;
      schematic_net_label.anchor_position = {
        x: finalCenter.x + pin.offset.x,
        y: finalCenter.y + pin.offset.y
      };
      continue;
    }
    console.error(
      `No schematic element found for box: ${box.boxId}. This is a bug in the matchAdapt binding with @tscircuit/core`
    );
  }
}

// lib/components/primitive-components/Group/applySchematicMatchPackLayoutToTree.ts
import "@tscircuit/circuit-json-util";
import { LayoutPipelineSolver as LayoutPipelineSolver2 } from "@tscircuit/matchpack";
import { getBoundFromCenteredRect as getBoundFromCenteredRect2 } from "@tscircuit/math-utils";
import Debug8 from "debug";

// lib/utils/schematic/getSchematicComponentWithTextBounds.ts
import { getBoundFromCenteredRect } from "@tscircuit/math-utils";
import { symbols as symbols4 } from "schematic-symbols";

// lib/utils/schematic/getSchematicTextWidth.ts
function getSchematicTextWidth({
  text,
  fontSize
}) {
  const fontScale = fontSize / 0.18;
  const charWidth = 0.12 * fontScale;
  return text.length * charWidth;
}

// lib/utils/schematic/getSchematicComponentWithTextBounds.ts
var SYMBOL_TEXT_FONT_SIZE = 0.18;
var TEXT_BOX_ENABLED_FTYPES = /* @__PURE__ */ new Set([
  "simple_resistor",
  "simple_capacitor",
  "simple_inductor",
  "simple_crystal",
  "simple_resonator",
  "simple_potentiometer",
  "simple_fuse",
  "simple_diode",
  "simple_led",
  "simple_transistor",
  "simple_mosfet",
  "simple_op_amp",
  "simple_voltage_source",
  "simple_test_point"
]);
function getTextBounds({
  text,
  position,
  anchor,
  fontSize
}) {
  const width = getSchematicTextWidth({ text, fontSize });
  const height = fontSize;
  let relMinX;
  let relMaxX;
  if (anchor.includes("left")) {
    relMinX = 0;
    relMaxX = width;
  } else if (anchor.includes("right")) {
    relMinX = -width;
    relMaxX = 0;
  } else {
    relMinX = -width / 2;
    relMaxX = width / 2;
  }
  let relMinY;
  let relMaxY;
  if (anchor.includes("top")) {
    relMinY = -height;
    relMaxY = 0;
  } else if (anchor.includes("bottom")) {
    relMinY = 0;
    relMaxY = height;
  } else {
    relMinY = -height / 2;
    relMaxY = height / 2;
  }
  return {
    minX: position.x + relMinX,
    maxX: position.x + relMaxX,
    minY: position.y + relMinY,
    maxY: position.y + relMaxY
  };
}
function getSymbolTextBounds({
  schematicComponent,
  sourceComponent
}) {
  if (!schematicComponent.symbol_name) return [];
  const symbol = symbols4[schematicComponent.symbol_name];
  if (!symbol?.primitives || !symbol.center) return [];
  const textBounds = [];
  for (const primitive of symbol.primitives) {
    if (primitive.type !== "text") continue;
    let value;
    if (primitive.text === "{REF}") {
      value = sourceComponent?.display_name ?? sourceComponent?.name ?? "";
    } else if (primitive.text === "{VAL}") {
      value = schematicComponent.symbol_display_value ?? "";
    } else {
      value = primitive.text ?? "";
    }
    if (!value) continue;
    textBounds.push(
      getTextBounds({
        text: value,
        position: {
          x: primitive.x - symbol.center.x + schematicComponent.center.x,
          y: primitive.y - symbol.center.y + schematicComponent.center.y
        },
        anchor: primitive.anchor ?? "center",
        fontSize: SYMBOL_TEXT_FONT_SIZE
      })
    );
  }
  return textBounds;
}
function getSymbolBoxBounds(schematicComponent) {
  return getBoundFromCenteredRect({
    center: schematicComponent.center,
    width: schematicComponent.size.width,
    height: schematicComponent.size.height
  });
}
function getSchematicComponentTextInclusiveBounds(db, schematicComponent) {
  if (!schematicComponent.center || !schematicComponent.size) return null;
  const sourceComponent = schematicComponent.source_component_id ? db.source_component.get(schematicComponent.source_component_id) : void 0;
  if (!sourceComponent || !TEXT_BOX_ENABLED_FTYPES.has(sourceComponent.ftype)) {
    return null;
  }
  const textBounds = getSymbolTextBounds({
    schematicComponent,
    sourceComponent
  });
  if (textBounds.length === 0) return null;
  const boxBounds = getSymbolBoxBounds(schematicComponent);
  const bounds = { ...boxBounds };
  for (const textBound of textBounds) {
    bounds.minX = Math.min(bounds.minX, textBound.minX);
    bounds.maxX = Math.max(bounds.maxX, textBound.maxX);
    bounds.minY = Math.min(bounds.minY, textBound.minY);
    bounds.maxY = Math.max(bounds.maxY, textBound.maxY);
  }
  if (bounds.minX === boxBounds.minX && bounds.maxX === boxBounds.maxX && bounds.minY === boxBounds.minY && bounds.maxY === boxBounds.maxY) {
    return null;
  }
  return bounds;
}
function getOrientedSchematicComponent(schematicComponent, ccwRotationDegrees) {
  let size = schematicComponent.size;
  if (size) size = getRotatedSize(size, ccwRotationDegrees);
  return {
    ...schematicComponent,
    symbol_name: getRotatedSymbolName(
      schematicComponent.symbol_name,
      ccwRotationDegrees
    ),
    size
  };
}
function getSchematicComponentWithTextBounds({
  db,
  schematicComponent,
  ccwRotationDegrees = 0
}) {
  const orientedComponent = getOrientedSchematicComponent(
    schematicComponent,
    ccwRotationDegrees
  );
  const textBounds = getSchematicComponentTextInclusiveBounds(
    db,
    orientedComponent
  );
  if (!textBounds) return null;
  const boxBounds = getSymbolBoxBounds(orientedComponent);
  const isVertical = orientedComponent.size.height > orientedComponent.size.width;
  if (isVertical) return textBounds;
  const padLeft = Math.max(0, boxBounds.minX - textBounds.minX);
  const padRight = Math.max(0, textBounds.maxX - boxBounds.maxX);
  const padTop = Math.max(0, textBounds.maxY - boxBounds.maxY);
  const padBottom = Math.max(0, boxBounds.minY - textBounds.minY);
  let left = padLeft;
  let right = padRight;
  if (padLeft > 0 && padRight > 0) {
    left = Math.max(padLeft, padRight);
    right = left;
  }
  let top = padTop;
  let bottom = padBottom;
  if (padTop > 0 && padBottom > 0) {
    top = Math.max(padTop, padBottom);
    bottom = top;
  }
  return {
    minX: boxBounds.minX - left,
    maxX: boxBounds.maxX + right,
    minY: boxBounds.minY - bottom,
    maxY: boxBounds.maxY + top
  };
}

// lib/components/primitive-components/Group/applySchematicMatchPackLayoutToTree.ts
var debug6 = Debug8("Group_doInitialSchematicLayoutMatchpack");
var DEFAULT_AVAILABLE_ROTATIONS = [0, 90, 180, 270];
var ROTATION_TO_PLACE_SIDE_ON_TOP = {
  "y+": 0,
  "x+": 90,
  "y-": 180,
  "x-": 270
};
function facingDirectionToSide(facingDirection) {
  switch (facingDirection) {
    case "up":
      return "y+";
    case "down":
      return "y-";
    case "left":
      return "x-";
    case "right":
      return "x+";
    default:
      return "y+";
  }
}
function getPowerGroundForcedRotation(db, ports) {
  if (ports.length !== 2) return null;
  const powerGroundRotations = /* @__PURE__ */ new Set();
  for (const port of ports) {
    const sourcePort = db.source_port.get(port.source_port_id);
    const connectivityKey = sourcePort?.subcircuit_connectivity_map_key;
    if (!connectivityKey) continue;
    const net = db.source_net.getWhere({
      subcircuit_connectivity_map_key: connectivityKey
    });
    if (!net) continue;
    const isGround = net.is_ground ?? false;
    const isPositiveVoltageSource = net.is_power === true || net.is_positive_voltage_source === true;
    if (isPositiveVoltageSource === isGround) continue;
    const rotationToPlacePinOnTop = ROTATION_TO_PLACE_SIDE_ON_TOP[facingDirectionToSide(port.facing_direction)];
    const rotation4 = isPositiveVoltageSource ? rotationToPlacePinOnTop : (rotationToPlacePinOnTop + 180) % 360;
    powerGroundRotations.add(rotation4);
  }
  return powerGroundRotations.size === 1 ? powerGroundRotations.values().next().value : null;
}
function getChipSize({
  db,
  schematicComponent,
  component,
  ccwRotationDegrees
}) {
  let orientedSymbolSize = schematicComponent.size;
  if (orientedSymbolSize) {
    orientedSymbolSize = getRotatedSize(orientedSymbolSize, ccwRotationDegrees);
  }
  const componentWithTextBounds = getSchematicComponentWithTextBounds({
    db,
    schematicComponent,
    ccwRotationDegrees
  });
  let textPadLeft = 0;
  let textPadRight = 0;
  let textPadTop = 0;
  let textPadBottom = 0;
  if (componentWithTextBounds && schematicComponent.center) {
    const halfWidth = (orientedSymbolSize?.width ?? 0) / 2;
    const halfHeight = (orientedSymbolSize?.height ?? 0) / 2;
    textPadLeft = schematicComponent.center.x - halfWidth - componentWithTextBounds.minX;
    textPadRight = componentWithTextBounds.maxX - (schematicComponent.center.x + halfWidth);
    textPadTop = componentWithTextBounds.maxY - (schematicComponent.center.y + halfHeight);
    textPadBottom = schematicComponent.center.y - halfHeight - componentWithTextBounds.minY;
  }
  const marginLeft = (component?._parsedProps?.schMarginLeft ?? component?._parsedProps?.schMarginX ?? 0) + textPadLeft;
  const marginRight = (component?._parsedProps?.schMarginRight ?? component?._parsedProps?.schMarginX ?? 0) + textPadRight;
  let marginTop = (component?._parsedProps?.schMarginTop ?? component?._parsedProps?.schMarginY ?? 0) + textPadTop;
  let marginBottom = (component?._parsedProps?.schMarginBottom ?? component?._parsedProps?.schMarginY ?? 0) + textPadBottom;
  if (component?.config.shouldRenderAsSchematicBox) {
    marginTop += 0.4;
    marginBottom += 0.4;
  }
  const orientedFootprint = {
    width: (orientedSymbolSize?.width || 1) + marginLeft + marginRight,
    height: (orientedSymbolSize?.height || 1) + marginTop + marginBottom
  };
  const chipSize = getRotatedSize(orientedFootprint, ccwRotationDegrees);
  return { x: chipSize.width, y: chipSize.height };
}
function getReservedChipSize({
  db,
  schematicComponent,
  component,
  ccwRotationDegrees,
  chipGap
}) {
  const unrotatedSize = getChipSize({
    db,
    schematicComponent,
    component,
    ccwRotationDegrees: 0
  });
  if (!shouldSwapOrientation(ccwRotationDegrees)) return unrotatedSize;
  const rotatedSize = getChipSize({
    db,
    schematicComponent,
    component,
    ccwRotationDegrees
  });
  const growsPastGap = rotatedSize.x - unrotatedSize.x >= chipGap || rotatedSize.y - unrotatedSize.y >= chipGap;
  return growsPastGap ? rotatedSize : unrotatedSize;
}
function getNestedGroupMatchpackProxy({
  db,
  schematicGroup,
  groupComponents,
  groupInstance
}) {
  let minX = Infinity;
  let maxX = -Infinity;
  let minY = Infinity;
  let maxY = -Infinity;
  let hasValidBounds = false;
  for (const component of groupComponents) {
    if (!component.center || !component.size) continue;
    hasValidBounds = true;
    const componentBounds = getSchematicComponentWithTextBounds({
      db,
      schematicComponent: component
    }) ?? getBoundFromCenteredRect2({
      center: component.center,
      width: component.size.width,
      height: component.size.height
    });
    minX = Math.min(minX, componentBounds.minX);
    maxX = Math.max(maxX, componentBounds.maxX);
    minY = Math.min(minY, componentBounds.minY);
    maxY = Math.max(maxY, componentBounds.maxY);
  }
  const marginLeft = groupInstance?._parsedProps?.schMarginLeft ?? groupInstance?._parsedProps?.schMarginX ?? 0;
  const marginRight = groupInstance?._parsedProps?.schMarginRight ?? groupInstance?._parsedProps?.schMarginX ?? 0;
  const marginTop = groupInstance?._parsedProps?.schMarginTop ?? groupInstance?._parsedProps?.schMarginY ?? 0;
  const marginBottom = groupInstance?._parsedProps?.schMarginBottom ?? groupInstance?._parsedProps?.schMarginY ?? 0;
  const contentCenter = hasValidBounds ? { x: (minX + maxX) / 2, y: (minY + maxY) / 2 } : schematicGroup.center ?? { x: 0, y: 0 };
  return {
    center: {
      x: contentCenter.x + (marginRight - marginLeft) / 2,
      y: contentCenter.y + (marginTop - marginBottom) / 2
    },
    size: {
      x: (hasValidBounds ? maxX - minX : 2) + marginLeft + marginRight,
      y: (hasValidBounds ? maxY - minY : 2) + marginTop + marginBottom
    }
  };
}
function isTreeChildExplicitlyPositioned(treeChild, group) {
  if (treeChild.nodeType === "component" && treeChild.sourceComponent) {
    const component = group.children.find(
      (groupChild) => groupChild.source_component_id === treeChild.sourceComponent?.source_component_id
    );
    return component?._parsedProps?.schX !== void 0 || component?._parsedProps?.schY !== void 0;
  }
  if (treeChild.nodeType === "group" && treeChild.sourceGroup) {
    const nestedGroup = group.children.find(
      (groupChild) => groupChild.source_group_id === treeChild.sourceGroup?.source_group_id
    );
    return nestedGroup?._parsedProps?.schX !== void 0 || nestedGroup?._parsedProps?.schY !== void 0;
  }
  return false;
}
function getTreeChildChipId(child, index) {
  if (child.nodeType === "component" && child.sourceComponent) {
    return child.sourceComponent.name || `chip_${index}`;
  }
  if (child.nodeType === "group" && child.sourceGroup) {
    return child.sourceGroup.name || `group_${index}`;
  }
  return null;
}
var getSchematicComponentForSourceComponent = ({
  db,
  sourceComponentId,
  schematicSheetId
}) => db.schematic_component.getWhere({
  source_component_id: sourceComponentId,
  ...schematicSheetId ? { schematic_sheet_id: schematicSheetId } : {}
});
function convertTreeToMatchPackInputProblem(tree, db, group, opts = {}) {
  const problem = {
    chipMap: {},
    chipPinMap: {},
    netMap: {},
    pinStrongConnMap: {},
    netConnMap: {},
    chipGap: 0.6,
    decouplingCapsGap: 0.4,
    partitionGap: 1.2
  };
  const nestedGroupMatchpackProxyMap = {};
  const groupOffset = group._getGlobalSchematicPositionBeforeLayout();
  debug6(
    `[${group.name}] Processing ${tree.childNodes.length} child nodes for input problem`
  );
  tree.childNodes.forEach((child, index) => {
    const explicitlyPositioned = isTreeChildExplicitlyPositioned(child, group);
    if (explicitlyPositioned) {
      debug6(
        `[${group.name}] Child ${index} explicitly positioned, including as fixed chip`
      );
    } else {
      debug6(
        `[${group.name}] Processing child ${index}: nodeType=${child.nodeType}`
      );
    }
    if (child.nodeType === "component") {
      debug6(`[${group.name}] - Component: ${child.sourceComponent?.name}`);
    } else if (child.nodeType === "group") {
      debug6(`[${group.name}] - Group: ${child.sourceGroup?.name}`);
    }
    if (child.nodeType === "component" && child.sourceComponent) {
      const chipId = getTreeChildChipId(child, index);
      const schematicComponent = getSchematicComponentForSourceComponent({
        db,
        sourceComponentId: child.sourceComponent.source_component_id,
        schematicSheetId: opts.schematicSheetId
      });
      if (!schematicComponent) return;
      const component = group.children.find(
        (groupChild) => groupChild.source_component_id === child.sourceComponent?.source_component_id
      );
      const ports = db.schematic_port.list({
        schematic_component_id: schematicComponent.schematic_component_id
      });
      let availableRotations = [
        ...DEFAULT_AVAILABLE_ROTATIONS
      ];
      if (component?._parsedProps?.schOrientation) {
        availableRotations = [0];
      }
      if (component?._parsedProps?.schRotation !== void 0) {
        availableRotations = [0];
      }
      if (component?._parsedProps?.facingDirection) {
        availableRotations = [0];
      }
      if (component?._parsedProps?.schFacingDirection) {
        availableRotations = [0];
      }
      if (component?.componentName === "Chip") {
        availableRotations = [0];
      }
      if (availableRotations.length === DEFAULT_AVAILABLE_ROTATIONS.length) {
        const forcedRotation = getPowerGroundForcedRotation(db, ports);
        if (forcedRotation !== null) availableRotations = [forcedRotation];
      }
      const ccwRotationDegrees = availableRotations.length === 1 ? availableRotations[0] : 0;
      problem.chipMap[chipId] = {
        chipId,
        pins: [],
        size: getReservedChipSize({
          db,
          schematicComponent,
          component,
          ccwRotationDegrees,
          chipGap: problem.chipGap
        }),
        isCapacitor: child.sourceComponent.ftype === "simple_capacitor",
        isCrystal: child.sourceComponent.ftype === "simple_crystal",
        isResistor: child.sourceComponent.ftype === "simple_resistor",
        isTestPoint: child.sourceComponent.ftype === "simple_test_point",
        availableRotations,
        ...explicitlyPositioned && {
          fixedPosition: {
            x: schematicComponent.center.x - groupOffset.x,
            y: schematicComponent.center.y - groupOffset.y
          }
        }
      };
      for (const port of ports) {
        const sourcePort = db.source_port.get(port.source_port_id);
        if (!sourcePort) continue;
        const pinId = `${chipId}.${sourcePort.pin_number || sourcePort.name || port.schematic_port_id}`;
        problem.chipMap[chipId].pins.push(pinId);
        const side = facingDirectionToSide(port.facing_direction);
        problem.chipPinMap[pinId] = {
          pinId,
          offset: {
            x: (port.center?.x || 0) - (schematicComponent.center.x || 0),
            y: (port.center?.y || 0) - (schematicComponent.center.y || 0)
          },
          side
        };
      }
    } else if (child.nodeType === "group" && child.sourceGroup) {
      const groupId = getTreeChildChipId(child, index);
      debug6(`[${group.name}] Processing nested group: ${groupId}`);
      const schematicGroup = db.schematic_group?.getWhere?.({
        source_group_id: child.sourceGroup.source_group_id
      });
      const groupInstance = group.children.find(
        (groupChild) => groupChild.source_group_id === child.sourceGroup?.source_group_id
      );
      debug6(
        `[${group.name}] Found schematic_group for ${groupId}:`,
        schematicGroup
      );
      if (schematicGroup) {
        debug6(`[${group.name}] Treating group ${groupId} as composite chip`);
        const groupComponents = db.schematic_component.list({
          schematic_group_id: schematicGroup.schematic_group_id
        });
        debug6(
          `[${group.name}] Group ${groupId} has ${groupComponents.length} components:`,
          groupComponents.map((c) => c.source_component_id)
        );
        const matchpackProxy = getNestedGroupMatchpackProxy({
          db,
          schematicGroup,
          groupComponents,
          groupInstance
        });
        nestedGroupMatchpackProxyMap[groupId] = matchpackProxy;
        debug6(
          `[${group.name}] Group ${groupId} computed proxy: ${matchpackProxy.size.x} x ${matchpackProxy.size.y} centered at (${matchpackProxy.center.x}, ${matchpackProxy.center.y})`
        );
        const groupPins = [];
        for (const comp of groupComponents) {
          const ports = db.schematic_port.list({
            schematic_component_id: comp.schematic_component_id
          });
          for (const port of ports) {
            const sourcePort = db.source_port.get(port.source_port_id);
            if (!sourcePort) continue;
            const pinId = `${groupId}.${sourcePort.pin_number || sourcePort.name || port.schematic_port_id}`;
            groupPins.push(pinId);
            const side = facingDirectionToSide(port.facing_direction);
            problem.chipPinMap[pinId] = {
              pinId,
              offset: {
                x: (port.center?.x || 0) - matchpackProxy.center.x,
                y: (port.center?.y || 0) - matchpackProxy.center.y
              },
              side
            };
          }
        }
        debug6(
          `[${group.name}] Group ${groupId} has ${groupPins.length} pins:`,
          groupPins
        );
        problem.chipMap[groupId] = {
          chipId: groupId,
          pins: groupPins,
          size: matchpackProxy.size
        };
        debug6(`[${group.name}] Added group ${groupId} to chipMap`);
      } else {
        debug6(
          `[${group.name}] Warning: No schematic_group found for group ${groupId}`
        );
      }
    }
  });
  debug6(`[${group.name}] Creating connections using connectivity keys`);
  const connectivityGroups = /* @__PURE__ */ new Map();
  for (const [chipId, chip] of Object.entries(problem.chipMap)) {
    for (const pinId of chip.pins) {
      const pinNumber = pinId.split(".").pop();
      const treeNode = tree.childNodes.find((child) => {
        return getTreeChildChipId(child, tree.childNodes.indexOf(child)) === chipId;
      });
      if (treeNode?.nodeType === "group" && treeNode.sourceGroup) {
        const schematicGroup = db.schematic_group?.getWhere?.({
          source_group_id: treeNode.sourceGroup.source_group_id
        });
        if (schematicGroup) {
          const groupComponents = db.schematic_component.list({
            schematic_group_id: schematicGroup.schematic_group_id
          });
          for (const comp of groupComponents) {
            const sourcePorts = db.source_port.list({
              source_component_id: comp.source_component_id
            });
            for (const sourcePort of sourcePorts) {
              const portNumber = sourcePort.pin_number || sourcePort.name;
              if (String(portNumber) === String(pinNumber)) {
                if (sourcePort.subcircuit_connectivity_map_key) {
                  const connectivityKey = sourcePort.subcircuit_connectivity_map_key;
                  if (!connectivityGroups.has(connectivityKey)) {
                    connectivityGroups.set(connectivityKey, []);
                  }
                  connectivityGroups.get(connectivityKey).push(pinId);
                  debug6(
                    `[${group.name}] \u2713 Pin ${pinId} has connectivity key: ${connectivityKey}`
                  );
                } else {
                  debug6(`[${group.name}] Pin ${pinId} has no connectivity key`);
                }
              }
            }
          }
        }
      } else if (treeNode?.nodeType === "component" && treeNode.sourceComponent) {
        const sourcePorts = db.source_port.list({
          source_component_id: treeNode.sourceComponent.source_component_id
        });
        for (const sourcePort of sourcePorts) {
          const portNumber = sourcePort.pin_number || sourcePort.name;
          if (String(portNumber) === String(pinNumber) && sourcePort.subcircuit_connectivity_map_key) {
            const connectivityKey = sourcePort.subcircuit_connectivity_map_key;
            if (!connectivityGroups.has(connectivityKey)) {
              connectivityGroups.set(connectivityKey, []);
            }
            connectivityGroups.get(connectivityKey).push(pinId);
            debug6(
              `[${group.name}] Pin ${pinId} has connectivity key: ${connectivityKey}`
            );
          }
        }
      }
    }
  }
  debug6(
    `[${group.name}] Found ${connectivityGroups.size} connectivity groups:`,
    Array.from(connectivityGroups.entries()).map(([key, pins]) => ({
      key,
      pins
    }))
  );
  for (const [connectivityKey, pins] of connectivityGroups) {
    const tracesWithThisKey = db.source_trace.list().filter(
      (trace) => trace.subcircuit_connectivity_map_key === connectivityKey
    );
    const hasNetConnections = tracesWithThisKey.some(
      (trace) => trace.connected_source_net_ids && trace.connected_source_net_ids.length > 0
    );
    const hasDirectConnections = tracesWithThisKey.some(
      (trace) => trace.connected_source_port_ids && trace.connected_source_port_ids.length >= 2
    );
    debug6(
      `[${group.name}] Connectivity ${connectivityKey}: hasNetConnections=${hasNetConnections}, hasDirectConnections=${hasDirectConnections}`
    );
    if (pins.length >= 2) {
      if (hasDirectConnections) {
        for (const trace of tracesWithThisKey) {
          if (trace.connected_source_port_ids && trace.connected_source_port_ids.length >= 2) {
            const directlyConnectedPins = [];
            for (const portId of trace.connected_source_port_ids) {
              for (const pinId of pins) {
                const pinNumber = pinId.split(".").pop();
                const sourcePort = db.source_port.get(portId);
                if (sourcePort && String(sourcePort.pin_number || sourcePort.name) === String(pinNumber)) {
                  const chipId = pinId.split(".")[0];
                  const treeNode = tree.childNodes.find((child) => {
                    return getTreeChildChipId(
                      child,
                      tree.childNodes.indexOf(child)
                    ) === chipId;
                  });
                  if (treeNode?.nodeType === "component" && treeNode.sourceComponent) {
                    const portBelongsToComponent = db.source_port.list({
                      source_component_id: treeNode.sourceComponent.source_component_id
                    }).some((p) => p.source_port_id === portId);
                    if (portBelongsToComponent) {
                      directlyConnectedPins.push(pinId);
                    }
                  }
                }
              }
            }
            for (let i = 0; i < directlyConnectedPins.length; i++) {
              for (let j = i + 1; j < directlyConnectedPins.length; j++) {
                const pin1 = directlyConnectedPins[i];
                const pin2 = directlyConnectedPins[j];
                problem.pinStrongConnMap[`${pin1}-${pin2}`] = true;
                problem.pinStrongConnMap[`${pin2}-${pin1}`] = true;
                debug6(
                  `[${group.name}] Created strong connection: ${pin1} <-> ${pin2}`
                );
              }
            }
          }
        }
      }
    }
    if (hasNetConnections) {
      const source_net = db.source_net.getWhere({
        subcircuit_connectivity_map_key: connectivityKey
      });
      const isGround = source_net?.is_ground ?? false;
      const isPositiveVoltageSource = source_net?.is_power === true || source_net?.is_positive_voltage_source === true;
      problem.netMap[connectivityKey] = {
        netId: connectivityKey,
        isGround,
        isPositiveVoltageSource
      };
      for (const pinId of pins) {
        problem.netConnMap[`${pinId}-${connectivityKey}`] = true;
      }
      debug6(
        `[${group.name}] Created net ${connectivityKey} with ${pins.length} pins:`,
        pins
      );
    }
  }
  return { inputProblem: problem, nestedGroupMatchpackProxyMap };
}
function applySchematicMatchPackLayoutToTree(group, tree, opts = {}) {
  const { db } = group.root;
  debug6(
    `[${group.name}] Starting matchpack layout with ${tree.childNodes.length} children`
  );
  debug6(`[${group.name}] Tree structure:`, JSON.stringify(tree, null, 2));
  if (tree.childNodes.length <= 1) {
    debug6(
      `[${group.name}] Only ${tree.childNodes.length} children, skipping layout`
    );
    return;
  }
  debug6("Converting circuit tree to InputProblem...");
  const { inputProblem, nestedGroupMatchpackProxyMap } = convertTreeToMatchPackInputProblem(tree, db, group, opts);
  if (debug6.enabled) {
    group.root?.emit("debug:logOutput", {
      type: "debug:logOutput",
      name: `matchpack-input-problem-${group.name}`,
      content: JSON.stringify(inputProblem, null, 2)
    });
  }
  const solver = new LayoutPipelineSolver2(inputProblem);
  group.root?.emit("solver:started", {
    type: "solver:started",
    solverName: "LayoutPipelineSolver",
    solverParams: inputProblem,
    solverConstructorArgs: [inputProblem],
    componentName: group.getString()
  });
  debug6("Starting LayoutPipelineSolver...");
  if (debug6.enabled && global?.debugGraphics) {
    const initialViz = solver.visualize();
    global.debugGraphics.push({
      ...initialViz,
      title: `matchpack-initial-${group.name}`
    });
  }
  solver.solve();
  group.root?.emit("solver:ended", {
    type: "solver:ended",
    solverName: "LayoutPipelineSolver",
    componentName: group.getString(),
    solved: solver.solved,
    failed: solver.failed,
    iterations: solver.iterations,
    error: solver.error
  });
  debug6(`Solver completed in ${solver.iterations} iterations`);
  debug6(`Solved: ${solver.solved}, Failed: ${solver.failed}`);
  if (solver.failed) {
    const solverError = solver.error ?? "Unknown Matchpack failure";
    debug6(`Solver failed with error: ${solverError}`);
    db.schematic_layout_error.insert({
      error_type: "schematic_layout_error",
      message: `Matchpack layout solver failed: ${solverError}`,
      is_fatal: false,
      source_group_id: group.source_group_id,
      schematic_group_id: group.schematic_group_id,
      subcircuit_id: group.subcircuit_id ?? void 0
    });
    return;
  }
  const outputLayout = solver.getOutputLayout();
  debug6("OutputLayout:", JSON.stringify(outputLayout, null, 2));
  debug6("Solver completed successfully:", !solver.failed);
  if (debug6.enabled && global?.debugGraphics) {
    const finalViz = solver.visualize();
    global.debugGraphics.push({
      ...finalViz,
      title: `matchpack-final-${group.name}`
    });
  }
  const overlaps = solver.checkForOverlaps(outputLayout);
  if (overlaps.length > 0) {
    debug6(`Warning: Found ${overlaps.length} overlapping components:`);
    for (const overlap of overlaps) {
      debug6(
        `  ${overlap.chip1} overlaps ${overlap.chip2} (area: ${overlap.overlapArea})`
      );
    }
  }
  const groupOffset = group._getGlobalSchematicPositionBeforeLayout();
  debug6(`Group offset: x=${groupOffset.x}, y=${groupOffset.y}`);
  debug6(
    `Applying layout results for ${Object.keys(outputLayout.chipPlacements).length} chip placements`
  );
  for (const [chipId, placement] of Object.entries(
    outputLayout.chipPlacements
  )) {
    debug6(
      `Processing placement for chip: ${chipId} at (${placement.x}, ${placement.y})`
    );
    const treeNode = tree.childNodes.find((child) => {
      const expectedChipId = getTreeChildChipId(
        child,
        tree.childNodes.indexOf(child)
      );
      const matches = expectedChipId === chipId;
      debug6(`  Checking child ${expectedChipId}: matches=${matches}`);
      return matches;
    });
    if (!treeNode) {
      debug6(`Warning: No tree node found for chip: ${chipId}`);
      debug6(
        "Available tree nodes:",
        tree.childNodes.map((child, idx) => ({
          type: child.nodeType,
          name: child.nodeType === "component" ? child.sourceComponent?.name : child.sourceGroup?.name,
          expectedChipId: getTreeChildChipId(child, idx)
        }))
      );
      continue;
    }
    const newCenter = {
      x: placement.x + groupOffset.x,
      y: placement.y + groupOffset.y
    };
    if (treeNode.nodeType === "component" && treeNode.sourceComponent) {
      const groupChild = group.children.find(
        (c) => c.source_component_id === treeNode.sourceComponent?.source_component_id
      );
      if (groupChild?._parsedProps?.schX !== void 0 || groupChild?._parsedProps?.schY !== void 0) {
        debug6(`Skipping position update for fixed chip ${chipId}`);
        continue;
      }
      const schematicComponent = getSchematicComponentForSourceComponent({
        db,
        sourceComponentId: treeNode.sourceComponent.source_component_id,
        schematicSheetId: opts.schematicSheetId
      });
      if (schematicComponent) {
        debug6(`Moving component ${chipId} to (${newCenter.x}, ${newCenter.y})`);
        const ports = db.schematic_port.list({
          schematic_component_id: schematicComponent.schematic_component_id
        });
        const texts = db.schematic_text.list({
          schematic_component_id: schematicComponent.schematic_component_id
        });
        const positionDelta = {
          x: newCenter.x - schematicComponent.center.x,
          y: newCenter.y - schematicComponent.center.y
        };
        for (const port of ports) {
          port.center.x += positionDelta.x;
          port.center.y += positionDelta.y;
        }
        for (const text of texts) {
          text.position.x += positionDelta.x;
          text.position.y += positionDelta.y;
        }
        updateSchematicPrimitivesForLayoutShift({
          db,
          schematicComponentId: schematicComponent.schematic_component_id,
          deltaX: positionDelta.x,
          deltaY: positionDelta.y
        });
        schematicComponent.center = newCenter;
        if (placement.ccwRotationDegrees !== 0) {
          debug6(
            `Component ${chipId} has rotation: ${placement.ccwRotationDegrees}\xB0`
          );
          const normalizedRotation = (Math.round(placement.ccwRotationDegrees) % 360 + 360) % 360;
          if ((normalizedRotation === 90 || normalizedRotation === 270) && schematicComponent.size) {
            const prevWidth = schematicComponent.size.width;
            schematicComponent.size.width = schematicComponent.size.height;
            schematicComponent.size.height = prevWidth;
          }
          const angleRad = placement.ccwRotationDegrees * Math.PI / 180;
          const cos = Math.cos(angleRad);
          const sin = Math.sin(angleRad);
          for (const port of ports) {
            const dx = port.center.x - newCenter.x;
            const dy = port.center.y - newCenter.y;
            const rotatedDx = dx * cos - dy * sin;
            const rotatedDy = dx * sin + dy * cos;
            port.center.x = newCenter.x + rotatedDx;
            port.center.y = newCenter.y + rotatedDy;
            const originalDirection = port.facing_direction || "right";
            port.facing_direction = rotateDirection(
              originalDirection,
              placement.ccwRotationDegrees
            );
            port.side_of_component = (port.facing_direction === "up" ? "top" : port.facing_direction === "down" ? "bottom" : port.facing_direction) || port.side_of_component;
          }
          for (const text of texts) {
            const dx = text.position.x - newCenter.x;
            const dy = text.position.y - newCenter.y;
            const rotatedDx = dx * cos - dy * sin;
            const rotatedDy = dx * sin + dy * cos;
            text.position.x = newCenter.x + rotatedDx;
            text.position.y = newCenter.y + rotatedDy;
          }
          if (schematicComponent.symbol_name) {
            schematicComponent.symbol_name = getRotatedSymbolName(
              schematicComponent.symbol_name,
              placement.ccwRotationDegrees
            );
          }
        }
      }
    } else if (treeNode.nodeType === "group" && treeNode.sourceGroup) {
      const schematicGroup = db.schematic_group?.getWhere?.({
        source_group_id: treeNode.sourceGroup.source_group_id
      });
      if (schematicGroup) {
        debug6(
          `Moving group ${chipId} to (${newCenter.x}, ${newCenter.y}) from (${schematicGroup.center?.x}, ${schematicGroup.center?.y})`
        );
        const groupComponents = db.schematic_component.list({
          schematic_group_id: schematicGroup.schematic_group_id
        });
        debug6(
          `Group ${chipId} has ${groupComponents.length} components to move`
        );
        const matchpackProxy = nestedGroupMatchpackProxyMap[chipId];
        const oldCenter = matchpackProxy?.center ?? schematicGroup.center ?? { x: 0, y: 0 };
        const positionDelta = {
          x: newCenter.x - oldCenter.x,
          y: newCenter.y - oldCenter.y
        };
        debug6(
          `Position delta for group ${chipId}: (${positionDelta.x}, ${positionDelta.y})`
        );
        for (const component of groupComponents) {
          if (component.center) {
            const oldComponentCenter = { ...component.center };
            component.center.x += positionDelta.x;
            component.center.y += positionDelta.y;
            debug6(
              `Moved component ${component.source_component_id} from (${oldComponentCenter.x}, ${oldComponentCenter.y}) to (${component.center.x}, ${component.center.y})`
            );
            const ports = db.schematic_port.list({
              schematic_component_id: component.schematic_component_id
            });
            const texts = db.schematic_text.list({
              schematic_component_id: component.schematic_component_id
            });
            for (const port of ports) {
              if (port.center) {
                port.center.x += positionDelta.x;
                port.center.y += positionDelta.y;
              }
            }
            for (const text of texts) {
              if (text.position) {
                text.position.x += positionDelta.x;
                text.position.y += positionDelta.y;
              }
            }
            updateSchematicPrimitivesForLayoutShift({
              db,
              schematicComponentId: component.schematic_component_id,
              deltaX: positionDelta.x,
              deltaY: positionDelta.y
            });
          }
        }
        schematicGroup.center = newCenter;
        if (matchpackProxy) {
          schematicGroup.width = matchpackProxy.size.x;
          schematicGroup.height = matchpackProxy.size.y;
        }
        debug6(
          `Updated group ${chipId} center to (${newCenter.x}, ${newCenter.y})`
        );
      }
    }
  }
  debug6("Matchpack layout completed successfully");
}

// lib/components/primitive-components/Group/getSchematicCircuitJsonTrees.ts
import {
  getCircuitJsonTree as getCircuitJsonTree2
} from "@tscircuit/circuit-json-util";
function getSchematicSheetIdsBySourceGroupId({
  circuitJson,
  schematicSheetIdsBySourceComponentId
}) {
  const parentBySourceGroupId = /* @__PURE__ */ new Map();
  for (const element of circuitJson) {
    if (element.type === "source_group") {
      parentBySourceGroupId.set(
        element.source_group_id,
        element.parent_source_group_id
      );
    }
  }
  const schematicSheetIdsBySourceGroupId = /* @__PURE__ */ new Map();
  for (const element of circuitJson) {
    if (element.type !== "source_component") continue;
    if (!element.source_group_id) continue;
    const schematicSheetIds = schematicSheetIdsBySourceComponentId.get(
      element.source_component_id
    );
    if (schematicSheetIds === void 0) continue;
    for (const schematicSheetId of schematicSheetIds) {
      let sourceGroupId = element.source_group_id;
      const visitedSourceGroupIds = /* @__PURE__ */ new Set();
      while (sourceGroupId && !visitedSourceGroupIds.has(sourceGroupId)) {
        visitedSourceGroupIds.add(sourceGroupId);
        const groupSchematicSheetIds = schematicSheetIdsBySourceGroupId.get(sourceGroupId) ?? /* @__PURE__ */ new Set();
        groupSchematicSheetIds.add(schematicSheetId);
        schematicSheetIdsBySourceGroupId.set(
          sourceGroupId,
          groupSchematicSheetIds
        );
        sourceGroupId = parentBySourceGroupId.get(sourceGroupId);
      }
    }
  }
  return schematicSheetIdsBySourceGroupId;
}
function getCircuitJsonScopedToSchematicSheet({
  circuitJson,
  schematicSheetId,
  rootSourceGroupId,
  schematicSheetIdsBySourceComponentId,
  schematicSheetIdsBySourceGroupId
}) {
  const isGroupOnSheet = (sourceGroupId) => sourceGroupId === rootSourceGroupId || schematicSheetIdsBySourceGroupId.get(sourceGroupId)?.has(schematicSheetId) === true;
  return circuitJson.filter((element) => {
    if (element.type === "source_group") {
      return isGroupOnSheet(element.source_group_id);
    }
    if (element.type === "source_component") {
      if (schematicSheetIdsBySourceComponentId.get(element.source_component_id)?.has(schematicSheetId) !== true) {
        return false;
      }
      return element.source_group_id ? isGroupOnSheet(element.source_group_id) : true;
    }
    return true;
  });
}
function getSchematicCircuitJsonTrees(circuitJson, opts) {
  const { source_group_id } = opts;
  const schematicSheetIdsBySourceComponentId = /* @__PURE__ */ new Map();
  for (const element of circuitJson) {
    if (element.type !== "schematic_component") continue;
    if (!element.source_component_id) continue;
    if (!element.schematic_sheet_id) continue;
    const schematicSheetIds2 = schematicSheetIdsBySourceComponentId.get(element.source_component_id) ?? /* @__PURE__ */ new Set();
    schematicSheetIds2.add(element.schematic_sheet_id);
    schematicSheetIdsBySourceComponentId.set(
      element.source_component_id,
      schematicSheetIds2
    );
  }
  const schematicSheetIds = /* @__PURE__ */ new Set();
  for (const componentSchematicSheetIds of schematicSheetIdsBySourceComponentId.values()) {
    for (const schematicSheetId of componentSchematicSheetIds) {
      schematicSheetIds.add(schematicSheetId);
    }
  }
  if (schematicSheetIds.size <= 1) {
    return [
      {
        tree: getCircuitJsonTree2(circuitJson, { source_group_id }),
        schematicSheetId: schematicSheetIds.values().next().value
      }
    ];
  }
  const schematicSheetIdsBySourceGroupId = getSchematicSheetIdsBySourceGroupId({
    circuitJson,
    schematicSheetIdsBySourceComponentId
  });
  return Array.from(schematicSheetIds).map((schematicSheetId) => ({
    tree: getCircuitJsonTree2(
      getCircuitJsonScopedToSchematicSheet({
        circuitJson,
        schematicSheetId,
        rootSourceGroupId: source_group_id,
        schematicSheetIdsBySourceComponentId,
        schematicSheetIdsBySourceGroupId
      }),
      { source_group_id }
    ),
    schematicSheetId
  }));
}

// lib/components/primitive-components/Group/Group_doInitialSchematicLayoutMatchPack.ts
function Group_doInitialSchematicLayoutMatchPack(group) {
  const { db } = group.root;
  const trees = getSchematicCircuitJsonTrees(db.toArray(), {
    source_group_id: group.source_group_id
  });
  for (const { tree, schematicSheetId } of trees) {
    applySchematicMatchPackLayoutToTree(group, tree, { schematicSheetId });
  }
}

// lib/components/primitive-components/Group/Group_doInitialSchematicLayoutSections.ts
import { getCircuitJsonTree as getCircuitJsonTree3 } from "@tscircuit/circuit-json-util";
import Debug9 from "debug";

// lib/components/primitive-components/Group/computeSchematicSectionLayoutUsingRows.ts
var SECTION_GAP = 1;
var MARGIN = 1.5;
function computeSchematicSectionLayoutUsingRows({
  sectionBlocks,
  groupSchPositionBeforeLayout: groupOffset
}) {
  const totalArea = sectionBlocks.reduce((sum, block) => {
    const paddedWidth = Math.max(block.size.x, 0.5) + MARGIN * 2;
    const paddedHeight = Math.max(block.size.y, 0.5) + MARGIN * 2;
    return sum + paddedWidth * paddedHeight;
  }, 0);
  const targetRowWidth = Math.sqrt(totalArea) * 2;
  const computedSectionsMatrix = [];
  let currentSectionRow = [];
  let currentRowSectionWidth = 0;
  for (const section of sectionBlocks) {
    const paddedWidth = Math.max(section.size.x, 0.5) + MARGIN * 2;
    const paddedHeight = Math.max(section.size.y, 0.5) + MARGIN * 2;
    const neededWidth = currentRowSectionWidth > 0 ? currentRowSectionWidth + SECTION_GAP + paddedWidth : paddedWidth;
    if (currentSectionRow.length > 0 && neededWidth > targetRowWidth) {
      computedSectionsMatrix.push(currentSectionRow);
      currentSectionRow = [];
      currentRowSectionWidth = 0;
    }
    const xOffsetInRow = currentRowSectionWidth > 0 ? currentRowSectionWidth + SECTION_GAP : 0;
    currentSectionRow.push({
      sectionName: section.sectionName,
      rowOffset: xOffsetInRow,
      width: paddedWidth,
      height: paddedHeight
    });
    currentRowSectionWidth = xOffsetInRow + paddedWidth;
  }
  if (currentSectionRow.length > 0)
    computedSectionsMatrix.push(currentSectionRow);
  const sectionPlacements = /* @__PURE__ */ new Map();
  let rowY = 0;
  for (const sectionRow of computedSectionsMatrix) {
    const rowHeight = Math.max(...sectionRow.map((rowEntry) => rowEntry.height));
    const rowWidth = sectionRow[sectionRow.length - 1].rowOffset + sectionRow[sectionRow.length - 1].width;
    const rowCenteringOffsetX = -rowWidth / 2;
    for (const section of sectionRow) {
      sectionPlacements.set(section.sectionName, {
        x: rowCenteringOffsetX + section.rowOffset + section.width / 2 + groupOffset.x,
        y: rowY - rowHeight / 2 + groupOffset.y
      });
    }
    rowY -= rowHeight + SECTION_GAP;
  }
  return sectionPlacements;
}

// lib/components/primitive-components/Group/Group_doInitialSchematicLayoutSections.ts
var debug7 = Debug9("Group_doInitialSchematicLayoutSections");
function Group_doInitialSchematicLayoutSections(group) {
  const { db } = group.root;
  const sectionNamesUsedByChildren = /* @__PURE__ */ new Set();
  let hasChildrenWithoutSection = false;
  for (const child of group.children) {
    if (!child.source_component_id) continue;
    const sectionName = child.getSchematicSectionName();
    if (sectionName !== null) {
      sectionNamesUsedByChildren.add(sectionName);
    } else {
      hasChildrenWithoutSection = true;
    }
  }
  const sectionNamesToLayout = Array.from(
    sectionNamesUsedByChildren
  );
  if (hasChildrenWithoutSection) sectionNamesToLayout.push(null);
  if (sectionNamesToLayout.length === 0) return;
  for (const sectionName of sectionNamesToLayout) {
    const sectionTree = getCircuitJsonTree3(db.toArray(), {
      source_group_id: group.source_group_id
    });
    sectionTree.childNodes = sectionTree.childNodes.filter((child) => {
      if (child.nodeType !== "component" || !child.sourceComponent) return false;
      const component = group.children.find(
        (c) => c.source_component_id === child.sourceComponent?.source_component_id
      );
      const compSection = component?.getSchematicSectionName() ?? null;
      return compSection === sectionName;
    });
    applySchematicMatchPackLayoutToTree(group, sectionTree);
  }
  if (sectionNamesToLayout.length <= 1) return;
  const sectionNameToBoundsWithChildren = /* @__PURE__ */ new Map();
  for (const sectionName of sectionNamesToLayout) {
    let minX = Infinity;
    let maxX = -Infinity;
    let minY = Infinity;
    let maxY = -Infinity;
    const sourceCompIds = /* @__PURE__ */ new Set();
    for (const child of group.children) {
      const sourceComponentId = child.source_component_id;
      if (!sourceComponentId) continue;
      const compSectionName = child.getSchematicSectionName();
      if (compSectionName !== sectionName) continue;
      const schComp = db.schematic_component.getWhere({
        source_component_id: sourceComponentId
      });
      if (!schComp) continue;
      sourceCompIds.add(sourceComponentId);
      const hw = schComp.size.width / 2;
      const hh = schComp.size.height / 2;
      minX = Math.min(minX, schComp.center.x - hw);
      maxX = Math.max(maxX, schComp.center.x + hw);
      minY = Math.min(minY, schComp.center.y - hh);
      maxY = Math.max(maxY, schComp.center.y + hh);
    }
    if (!Number.isFinite(minX) || sourceCompIds.size === 0) continue;
    sectionNameToBoundsWithChildren.set(sectionName, {
      sectionName,
      center: { x: (minX + maxX) / 2, y: (minY + maxY) / 2 },
      size: { x: maxX - minX, y: maxY - minY },
      sourceCompIds
    });
  }
  if (sectionNameToBoundsWithChildren.size <= 1) return;
  const anchoredSections = /* @__PURE__ */ new Set();
  for (const child of group.children) {
    if (!child.source_component_id) continue;
    const props = child._parsedProps;
    if (props?.schX !== void 0 || props?.schY !== void 0) {
      anchoredSections.add(child.getSchematicSectionName());
    }
  }
  const groupSchPositionBeforeLayout = group._getGlobalSchematicPositionBeforeLayout();
  const sectionPlacements = computeSchematicSectionLayoutUsingRows({
    sectionBlocks: Array.from(sectionNameToBoundsWithChildren.values()).map(
      (bounds) => ({
        sectionName: bounds.sectionName,
        size: bounds.size
      })
    ),
    groupSchPositionBeforeLayout
  });
  for (const [sectionName, placement] of sectionPlacements) {
    const boundsWithChildren = sectionNameToBoundsWithChildren.get(sectionName);
    if (!boundsWithChildren) continue;
    if (anchoredSections.has(sectionName)) {
      debug7(
        `[${group.name}] Section "${sectionName}" anchored by manual position, skipping shift`
      );
      continue;
    }
    const delta = {
      x: placement.x - boundsWithChildren.center.x,
      y: placement.y - boundsWithChildren.center.y
    };
    for (const sourceComponentId of boundsWithChildren.sourceCompIds) {
      const schComp = db.schematic_component.getWhere({
        source_component_id: sourceComponentId
      });
      if (!schComp) continue;
      const ports = db.schematic_port.list({
        schematic_component_id: schComp.schematic_component_id
      });
      const texts = db.schematic_text.list({
        schematic_component_id: schComp.schematic_component_id
      });
      for (const port of ports) {
        port.center.x += delta.x;
        port.center.y += delta.y;
      }
      for (const text of texts) {
        text.position.x += delta.x;
        text.position.y += delta.y;
      }
      updateSchematicPrimitivesForLayoutShift({
        db,
        schematicComponentId: schComp.schematic_component_id,
        deltaX: delta.x,
        deltaY: delta.y
      });
      schComp.center = {
        x: schComp.center.x + delta.x,
        y: schComp.center.y + delta.y
      };
    }
  }
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/Group_doInitialSchematicTraceRender.ts
import { SchematicTracePipelineSolver as SchematicTracePipelineSolver4 } from "@tscircuit/schematic-trace-solver";
import Debug13 from "debug";

// lib/components/primitive-components/NetLabel.ts
import { netLabelProps } from "@tscircuit/props";
import {
  applyToPoint as applyToPoint25,
  identity as identity6,
  translate as translate7
} from "transformation-matrix";
import { calculateElbow as calculateElbow2 } from "calculate-elbow";
var NetLabel = class extends PrimitiveComponent2 {
  source_net_label_id;
  schematic_net_label_id;
  get config() {
    return {
      componentName: "NetLabel",
      zodProps: netLabelProps
    };
  }
  _getAnchorSide() {
    const { _parsedProps: props } = this;
    if (props.anchorSide) return props.anchorSide;
    const connectsTo = this._resolveConnectsTo();
    if (!connectsTo) return "right";
    const net = this.getSubcircuit().selectOne(
      `net.${this._getNetName()}`
    );
    let sourceNet;
    if (net?.source_net_id) {
      sourceNet = this.root?.db.source_net.get(net.source_net_id);
    }
    if (sourceNet?.is_ground) return "top";
    if (sourceNet?.is_power) return "bottom";
    const anchorPos = this._getGlobalSchematicPositionBeforeLayout();
    const connectedPorts = this._getConnectedPorts();
    if (connectedPorts.length === 0) return "right";
    const port = connectedPorts[0];
    if (port.facingDirection) {
      return getEnteringEdgeFromDirection(port.facingDirection);
    }
    const connectedPortPosition = port._getGlobalSchematicPositionBeforeLayout();
    const dx = connectedPortPosition.x - anchorPos.x;
    const dy = connectedPortPosition.y - anchorPos.y;
    if (Math.abs(dx) > Math.abs(dy)) {
      if (dx > 0) return "right";
      if (dx < 0) return "left";
    } else {
      if (dy > 0) return "top";
      if (dy < 0) return "bottom";
    }
    return "right";
  }
  _getConnectedPorts() {
    const connectsTo = this._resolveConnectsTo();
    if (!connectsTo) return [];
    const connectedPorts = [];
    for (const connection of connectsTo) {
      const port = this.getSubcircuit().selectOne(connection);
      if (port) {
        connectedPorts.push(port);
      }
    }
    return connectedPorts;
  }
  computeSchematicPropsTransform() {
    const { _parsedProps: props } = this;
    if (props.schX === void 0 && props.schY === void 0) {
      const connectedPorts = this._getConnectedPorts();
      if (connectedPorts.length > 0) {
        const portPos = connectedPorts[0]._getGlobalSchematicPositionBeforeLayout();
        const parentCenter = applyToPoint25(
          this.parent?.computeSchematicGlobalTransform?.() ?? identity6(),
          { x: 0, y: 0 }
        );
        return translate7(portPos.x - parentCenter.x, portPos.y - parentCenter.y);
      }
    }
    return super.computeSchematicPropsTransform();
  }
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const anchorPos = this._getGlobalSchematicPositionBeforeLayout();
    const net = this.getSubcircuit().selectOne(
      `net.${this._getNetName()}`
    );
    const anchorSide = this._getAnchorSide();
    const center = computeSchematicNetLabelCenter({
      anchor_position: anchorPos,
      anchor_side: anchorSide,
      text: props.net
    });
    const netLabel = db.schematic_net_label.insert({
      text: props.net,
      source_net_id: net.source_net_id,
      anchor_position: anchorPos,
      center,
      anchor_side: anchorSide,
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.source_net_label_id = netLabel.source_net_id;
    this.schematic_net_label_id = netLabel.schematic_net_label_id;
  }
  _resolveConnectsTo() {
    const { _parsedProps: props } = this;
    const connectsTo = props.connectsTo ?? props.connection;
    if (Array.isArray(connectsTo)) {
      return connectsTo;
    }
    if (typeof connectsTo === "string") {
      return [connectsTo];
    }
    return void 0;
  }
  _getNetName() {
    const { _parsedProps: props } = this;
    return props.net;
  }
  doInitialCreateNetsFromProps() {
    const { _parsedProps: props } = this;
    if (props.net) {
      createNetsFromProps(this, [`net.${props.net}`]);
    }
  }
  doInitialCreateTracesFromNetLabels() {
    const connectsTo = this._resolveConnectsTo();
    if (!connectsTo) return;
    for (const connection of connectsTo) {
      this.add(
        new Trace3({
          from: connection,
          to: `net.${this._getNetName()}`
        })
      );
    }
  }
  doInitialSchematicTraceRender() {
    if (!this.root?._featureMspSchematicTraceRouting) return;
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { schX, schY } = this._parsedProps;
    if (schX === void 0 && schY === void 0) return;
    const { db } = this.root;
    const connectsTo = this._resolveConnectsTo();
    if (!connectsTo || connectsTo.length === 0) return;
    const anchorPos = this._getGlobalSchematicPositionBeforeLayout();
    const anchorSide = this._getAnchorSide();
    const sideToAxisDir = {
      left: "x-",
      right: "x+",
      top: "y+",
      bottom: "y-"
    };
    const anchorFacing = sideToAxisDir[anchorSide];
    const net = this.getSubcircuit().selectOne(
      `net.${this._getNetName()}`
    );
    for (const connection of connectsTo) {
      const port = this.getSubcircuit().selectOne(connection, {
        type: "port"
      });
      if (!port || !port.schematic_port_id) continue;
      let existingTraceForThisConnection = false;
      if (net?.source_net_id) {
        const candidateSourceTrace = db.source_trace.list().find(
          (st) => st.connected_source_net_ids?.includes(net.source_net_id) && st.connected_source_port_ids?.includes(port.source_port_id ?? "")
        );
        if (candidateSourceTrace) {
          existingTraceForThisConnection = db.schematic_trace.list().some(
            (t) => t.source_trace_id === candidateSourceTrace.source_trace_id
          );
        }
        if (existingTraceForThisConnection) continue;
      }
      const portPos = port._getGlobalSchematicPositionAfterLayout();
      const portFacing = convertFacingDirectionToElbowDirection(
        port.facingDirection ?? "right"
      ) ?? "x+";
      const path = calculateElbow2(
        {
          x: portPos.x,
          y: portPos.y,
          facingDirection: portFacing
        },
        {
          x: anchorPos.x,
          y: anchorPos.y,
          facingDirection: anchorFacing
        }
      );
      if (!Array.isArray(path) || path.length < 2) continue;
      const edges = [];
      for (let i = 0; i < path.length - 1; i++) {
        edges.push({
          from: { x: path[i].x, y: path[i].y },
          to: { x: path[i + 1].x, y: path[i + 1].y }
        });
      }
      let source_trace_id;
      let subcircuit_connectivity_map_key;
      if (net?.source_net_id && port.source_port_id) {
        const st = db.source_trace.list().find(
          (s) => s.connected_source_net_ids?.includes(net.source_net_id) && s.connected_source_port_ids?.includes(port.source_port_id)
        );
        source_trace_id = st?.source_trace_id;
        subcircuit_connectivity_map_key = st?.subcircuit_connectivity_map_key || db.source_net.get(net.source_net_id)?.subcircuit_connectivity_map_key;
      }
      db.schematic_trace.insert({
        source_trace_id,
        edges,
        junctions: [],
        subcircuit_connectivity_map_key,
        schematic_sheet_id: this._resolveSchematicSheetId()
      });
      db.schematic_port.update(port.schematic_port_id, { is_connected: true });
    }
  }
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/applyInlineNetLabelPlacements.ts
import Debug10 from "debug";

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/getNetNameFromPorts.ts
var getNetNameFromPorts = (ports) => {
  const directlyConnectedTraces = [
    ...new Set(ports.flatMap((port) => port._getDirectlyConnectedTraces()))
  ];
  const displayLabel = directlyConnectedTraces.find((trace) => trace._parsedProps.schDisplayLabel)?._parsedProps.schDisplayLabel ?? directlyConnectedTraces.find((trace) => trace._parsedProps.displayName)?._parsedProps.displayName ?? directlyConnectedTraces.find((trace) => trace._parsedProps.name)?._parsedProps.name;
  if (displayLabel) {
    return { name: displayLabel, wasAssignedDisplayLabel: true };
  }
  const db = ports.find((port) => port.root)?.root?.db;
  const sourcePortIds = ports.map((port) => port.source_port_id).filter((sourcePortId) => Boolean(sourcePortId));
  const netName = db ? getNetNameFromSourcePorts(db, sourcePortIds) : ports.map((port) => port._getNetLabelText()).filter((name) => Boolean(name)).sort(
    (a, b) => a.localeCompare(b, void 0, { numeric: true, sensitivity: "base" })
  )[0];
  return { name: netName ?? "", wasAssignedDisplayLabel: false };
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/createCanonicalSchematicNetLabelTextResolver.ts
var appendToMap = (map, key, value) => {
  const values = map.get(key);
  if (values) {
    values.push(value);
  } else {
    map.set(key, [value]);
  }
};
var createCanonicalSchematicNetLabelTextResolver = (group) => {
  const { db } = group.root;
  const sourceNetByConnectionKey = /* @__PURE__ */ new Map();
  const portsByConnectionKey = /* @__PURE__ */ new Map();
  const sourcePortIdsByConnectionKey = /* @__PURE__ */ new Map();
  const resolvedTextByConnectionKey = /* @__PURE__ */ new Map();
  for (const sourceNet of db.source_net.list()) {
    if (sourceNet.subcircuit_connectivity_map_key) {
      sourceNetByConnectionKey.set(
        sourceNet.subcircuit_connectivity_map_key,
        sourceNet
      );
    }
  }
  for (const port of group.selectAll("port")) {
    const connectionKey = port._getSubcircuitConnectivityKey();
    if (connectionKey) {
      appendToMap(portsByConnectionKey, connectionKey, port);
    }
  }
  for (const sourcePort of db.source_port.list()) {
    if (sourcePort.subcircuit_connectivity_map_key) {
      appendToMap(
        sourcePortIdsByConnectionKey,
        sourcePort.subcircuit_connectivity_map_key,
        sourcePort.source_port_id
      );
    }
  }
  return ({
    subcircuitConnectivityMapKey
  }) => {
    const cached = resolvedTextByConnectionKey.get(subcircuitConnectivityMapKey);
    if (cached) return cached;
    const resolvedSourceNet = sourceNetByConnectionKey.get(
      subcircuitConnectivityMapKey
    );
    if (resolvedSourceNet?.name) {
      const result2 = {
        name: resolvedSourceNet.name,
        wasAssignedDisplayLabel: true
      };
      resolvedTextByConnectionKey.set(subcircuitConnectivityMapKey, result2);
      return result2;
    }
    const portLabel = getNetNameFromPorts(
      portsByConnectionKey.get(subcircuitConnectivityMapKey) ?? []
    );
    if (portLabel.name) {
      resolvedTextByConnectionKey.set(subcircuitConnectivityMapKey, portLabel);
      return portLabel;
    }
    const result = {
      name: getNetNameFromSourcePorts(
        db,
        sourcePortIdsByConnectionKey.get(subcircuitConnectivityMapKey) ?? []
      ) ?? "",
      wasAssignedDisplayLabel: false
    };
    resolvedTextByConnectionKey.set(subcircuitConnectivityMapKey, result);
    return result;
  };
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/port-id-types.ts
var asSchematicPortId = (id) => id;
var asSourcePortId = (id) => id;

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/applyInlineNetLabelPlacements.ts
var debug8 = Debug10("Group_doInitialSchematicTraceRender");
var INLINE_NET_LABEL_COLOR = "rgb(132, 0, 0)";
var VERTICAL_INLINE_NET_LABEL_ROTATION = -90;
function applyInlineNetLabelPlacements(args) {
  const { group, solver, userNetIdToConnKey, sourceTraceIdByPinPairKey } = args;
  const { db } = group.root;
  const inlineNetLabelPlacements = solver.inlineNetLabelSolver?.getOutput().inlineNetLabelPlacements ?? [];
  if (inlineNetLabelPlacements.length === 0) return;
  const resolveCanonicalNetLabelText = createCanonicalSchematicNetLabelTextResolver(group);
  const globalConnMap = solver.mspConnectionPairSolver.globalConnMap;
  for (const placement of inlineNetLabelPlacements) {
    const schematicPortIds = placement.pinIds.map(asSchematicPortId);
    const placementUserNetId = globalConnMap.getIdsConnectedToNet(placement.globalConnNetId).find((id) => userNetIdToConnKey.has(id));
    const connKey = placementUserNetId ? userNetIdToConnKey.get(placementUserNetId) : void 0;
    const text = connKey ? resolveCanonicalNetLabelText({ subcircuitConnectivityMapKey: connKey }).name : placement.netId ?? placement.globalConnNetId;
    if (!text) {
      debug8(
        `skipping inline net label for "${placement.netId}" REASON:no resolvable text`
      );
      continue;
    }
    let schematicSheetId = group._resolveSchematicSheetId();
    const schematicPort = db.schematic_port.get(schematicPortIds[0]);
    if (schematicPort?.schematic_sheet_id) {
      schematicSheetId = schematicPort.schematic_sheet_id;
    }
    db.schematic_text.insert({
      text,
      // Links the label back to the trace it names, so a consumer can tell an
      // inline net label apart from free-standing schematic text.
      source_trace_id: sourceTraceIdByPinPairKey.get(
        [...schematicPortIds].sort().join("::")
      ),
      anchor: "center",
      position: placement.center,
      rotation: placement.axis === "y" ? VERTICAL_INLINE_NET_LABEL_ROTATION : 0,
      font_size: placement.height,
      color: INLINE_NET_LABEL_COLOR,
      schematic_sheet_id: schematicSheetId
    });
  }
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/applyNetLabelPlacements.ts
import { doBoundsOverlap as doBoundsOverlap2 } from "@tscircuit/math-utils";
import "@tscircuit/schematic-trace-solver";
import Debug11 from "debug";

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/getNetLabelTextBounds.ts
var NET_LABEL_TEXT_HEIGHT = 0.18;
var getNetLabelTextBounds = ({
  center,
  text
}) => {
  const width = getSchematicNetLabelTextWidth({ text });
  const halfWidth = width / 2;
  const halfHeight = NET_LABEL_TEXT_HEIGHT / 2;
  return {
    minX: center.x - halfWidth,
    maxX: center.x + halfWidth,
    minY: center.y - halfHeight,
    maxY: center.y + halfHeight
  };
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/oppositeSide.ts
var oppositeSide = (input) => {
  switch (input) {
    case "x+":
      return "left";
    case "x-":
      return "right";
    case "y+":
      return "bottom";
    case "y-":
      return "top";
    case "left":
      return "right";
    case "top":
      return "bottom";
    case "right":
      return "left";
    case "bottom":
      return "top";
  }
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/applyNetLabelPlacements.ts
var debug9 = Debug11("Group_doInitialSchematicTraceRender");
var isSameNet = (label, sourceNet) => label.schematicNetLabel.source_net_id != null && label.schematicNetLabel.source_net_id === sourceNet.source_net_id;
var isUserDefinedNetLabelRedundantWithPlacement = (label, solverPlacement) => {
  if (!isSameNet(label, solverPlacement.sourceNet)) {
    return false;
  }
  if (label.schematicPortIds.some(
    (id) => solverPlacement.schematicPortIds.has(id)
  )) {
    return true;
  }
  if (!label.schematicNetLabel.center) return false;
  return doBoundsOverlap2(
    getNetLabelTextBounds({
      center: label.schematicNetLabel.center,
      text: label.schematicNetLabel.text
    }),
    solverPlacement.bounds
  );
};
function applyNetLabelPlacements(args) {
  const {
    group,
    solver,
    connKeyToSourceNet,
    userNetIdToConnKey,
    crossSubcircuitTraceLabelTextBySchematicPortId,
    connKeysWithExplicitPortNetTraces,
    schematicPortIdsWithPreExistingNetLabels,
    schematicPortIdsWithRoutedTraces,
    netLabels
  } = args;
  const { db } = group.root;
  const resolveCanonicalNetLabelText = createCanonicalSchematicNetLabelTextResolver(group);
  const netLabelPlacements = solver.inlineNetLabelSolver?.getOutput().netLabelPlacements ?? solver.netLabelNetLabelCollisionSolver?.getOutput().netLabelPlacements ?? solver.netLabelTraceCollisionSolver?.getOutput().netLabelPlacements ?? solver.netLabelPlacementSolver?.netLabelPlacements ?? solver.traceLabelOverlapAvoidanceSolver?.getOutput().netLabelPlacements ?? [];
  const dedupedNetLabelPlacements = [];
  const netLabelPlacementKeys = /* @__PURE__ */ new Set();
  for (const placement of netLabelPlacements) {
    const pinIds = [...placement.pinIds ?? []].sort();
    const key = `${placement.globalConnNetId}|${pinIds.join("::")}|${placement.netId ?? ""}`;
    if (netLabelPlacementKeys.has(key)) {
      debug9(
        `skipping duplicate placement for "${placement.netId}" REASON:identical net label placement`
      );
      continue;
    }
    netLabelPlacementKeys.add(key);
    dedupedNetLabelPlacements.push(placement);
  }
  const netLabelPlacementCountByGlobalNetId = /* @__PURE__ */ new Map();
  const routedPairKeysByGlobalNetId = /* @__PURE__ */ new Map();
  const globalNetIdsWithPortOnlyPlacements = /* @__PURE__ */ new Set();
  for (const placement of dedupedNetLabelPlacements) {
    netLabelPlacementCountByGlobalNetId.set(
      placement.globalConnNetId,
      (netLabelPlacementCountByGlobalNetId.get(placement.globalConnNetId) ?? 0) + 1
    );
    if ((placement.pinIds?.length ?? 0) > 1) {
      if (!routedPairKeysByGlobalNetId.has(placement.globalConnNetId)) {
        routedPairKeysByGlobalNetId.set(placement.globalConnNetId, /* @__PURE__ */ new Set());
      }
      routedPairKeysByGlobalNetId.get(placement.globalConnNetId).add(placement.pinIds.slice().sort().join("::"));
    }
    if ((placement.pinIds?.length ?? 0) <= 1) {
      globalNetIdsWithPortOnlyPlacements.add(placement.globalConnNetId);
    }
  }
  const globalConnMap = solver.mspConnectionPairSolver.globalConnMap;
  const userDefinedNetLabels = netLabels.map((label) => {
    if (!label.schematic_net_label_id) return null;
    const dbLabel = db.schematic_net_label.get(label.schematic_net_label_id);
    if (!dbLabel) return null;
    return {
      schematicNetLabel: dbLabel,
      schematicPortIds: label._getConnectedPorts().map((port) => port.schematic_port_id).filter((id) => Boolean(id)),
      shouldUseSchematicTraceSolver: label._parsedProps.schX === void 0 && label._parsedProps.schY === void 0
    };
  }).filter((label) => label !== null);
  for (const placement of dedupedNetLabelPlacements) {
    debug9(`processing placement: ${placement.netId}`);
    const placementUserNetId = globalConnMap.getIdsConnectedToNet(placement.globalConnNetId).find((id) => userNetIdToConnKey.get(id));
    const placementConnKey = userNetIdToConnKey.get(placementUserNetId);
    const orientation = placement.orientation;
    const anchor_side = oppositeSide(orientation);
    let sourceNet;
    if (placementConnKey) {
      sourceNet = connKeyToSourceNet.get(placementConnKey);
    }
    const crossSubcircuitTraceLabelText = placement.pinIds.length === 1 ? crossSubcircuitTraceLabelTextBySchematicPortId.get(
      asSchematicPortId(placement.pinIds[0])
    ) : void 0;
    const canonicalNetLabel = placementConnKey ? crossSubcircuitTraceLabelText ? {
      name: crossSubcircuitTraceLabelText,
      wasAssignedDisplayLabel: false
    } : resolveCanonicalNetLabelText({
      subcircuitConnectivityMapKey: placementConnKey
    }) : {
      name: placement.netId ?? placement.globalConnNetId,
      wasAssignedDisplayLabel: false
    };
    const schPortIds = placement.pinIds.map(asSchematicPortId);
    let schematicSheetId = group._resolveSchematicSheetId();
    const schematicPort = db.schematic_port.get(schPortIds[0]);
    if (schematicPort?.schematic_sheet_id) {
      schematicSheetId = schematicPort.schematic_sheet_id;
    }
    let anchor_position = placement.anchorPoint;
    if (schPortIds.length === 1 && schPortIds[0]) {
      const anchorSchPort = db.schematic_port.get(schPortIds[0]);
      if (anchorSchPort?.center && !anchorSchPort.is_connected) {
        anchor_position = anchorSchPort.center;
      }
    }
    if (schPortIds.some(
      (schPortId) => schematicPortIdsWithPreExistingNetLabels.has(schPortId)
    )) {
      debug9(
        `skipping net label placement for "${placement.netId}" REASON:schematic port has pre-existing net label`
      );
      continue;
    }
    if (sourceNet) {
      const isPowerOrGroundNet = sourceNet.is_ground || sourceNet.is_power;
      const hasExplicitPortNetTrace = connKeysWithExplicitPortNetTraces.has(
        placementConnKey
      );
      const hasRoutedTraceForPlacementPort = schPortIds.some(
        (id) => schematicPortIdsWithRoutedTraces.has(id)
      );
      const hasSingleLabelPlacement = (netLabelPlacementCountByGlobalNetId.get(placement.globalConnNetId) ?? 0) <= 1;
      const shouldSkipImplicitSignalNetLabel = !hasExplicitPortNetTrace && !isPowerOrGroundNet && hasRoutedTraceForPlacementPort && hasSingleLabelPlacement;
      if (shouldSkipImplicitSignalNetLabel) {
        debug9(
          `skipping net label placement for "${placement.netId}" REASON:schematic port has routed trace`
        );
        continue;
      }
      const text2 = canonicalNetLabel.name;
      const center2 = computeSchematicNetLabelCenter({
        anchor_position,
        anchor_side,
        text: text2
      });
      const solverManagedUserNetLabel = userDefinedNetLabels.find(
        (label) => label.shouldUseSchematicTraceSolver && isSameNet(label, sourceNet) && label.schematicPortIds.some((id) => schPortIds.includes(id))
      );
      if (solverManagedUserNetLabel) {
        db.schematic_net_label.update(
          solverManagedUserNetLabel.schematicNetLabel.schematic_net_label_id,
          {
            anchor_position,
            center: center2,
            anchor_side,
            schematic_sheet_id: schematicSheetId
          }
        );
        continue;
      }
      if (!isPowerOrGroundNet) {
        const solverPlacement = {
          sourceNet,
          text: text2,
          schematicPortIds: new Set(schPortIds),
          bounds: getNetLabelTextBounds({ center: center2, text: text2 })
        };
        if (userDefinedNetLabels.some(
          (label) => isUserDefinedNetLabelRedundantWithPlacement(label, solverPlacement)
        )) {
          continue;
        }
      }
      const netLabel2 = {
        text: text2,
        source_net_id: sourceNet.source_net_id,
        anchor_position,
        center: center2,
        anchor_side,
        schematic_sheet_id: schematicSheetId
      };
      db.schematic_net_label.insert(netLabel2);
      continue;
    }
    const { name: text, wasAssignedDisplayLabel } = canonicalNetLabel;
    const isRoutedPairPlacement = (placement.pinIds?.length ?? 0) > 1;
    const shouldKeepRoutedPairLabel = isRoutedPairPlacement && (globalNetIdsWithPortOnlyPlacements.has(placement.globalConnNetId) || (routedPairKeysByGlobalNetId.get(placement.globalConnNetId)?.size ?? 0) > 1);
    if (!wasAssignedDisplayLabel && !shouldKeepRoutedPairLabel && schPortIds.some(
      (schPortId) => schematicPortIdsWithRoutedTraces.has(schPortId)
    )) {
      debug9(
        `skipping net label placement for "${placement.netId}" REASON:schematic port has routed traces and no display label`
      );
      continue;
    }
    const center = computeSchematicNetLabelCenter({
      anchor_position,
      anchor_side,
      text
    });
    const netLabel = {
      text,
      source_net_id: placementConnKey ?? placement.netId ?? placement.globalConnNetId,
      anchor_position,
      center,
      anchor_side,
      schematic_sheet_id: schematicSheetId
    };
    db.schematic_net_label.insert(netLabel);
  }
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/applyTracesFromSolverOutput.ts
import { pointToBoundsDistance } from "@tscircuit/math-utils";
import "@tscircuit/schematic-trace-solver";
import Debug12 from "debug";

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/segment-intersection.ts
var cross2 = (ax, ay, bx, by) => {
  return ax * by - ay * bx;
};
function segmentIntersection(p1, p2, q1, q2, tol) {
  const r = { x: p2.x - p1.x, y: p2.y - p1.y };
  const s = { x: q2.x - q1.x, y: q2.y - q1.y };
  const rxs = cross2(r.x, r.y, s.x, s.y);
  const q_p = { x: q1.x - p1.x, y: q1.y - p1.y };
  const q_pxr = cross2(q_p.x, q_p.y, r.x, r.y);
  if (Math.abs(rxs) < tol && Math.abs(q_pxr) < tol) return null;
  if (Math.abs(rxs) < tol && Math.abs(q_pxr) >= tol) return null;
  const t = cross2(q_p.x, q_p.y, s.x, s.y) / rxs;
  const u = cross2(q_p.x, q_p.y, r.x, r.y) / rxs;
  if (t < -tol || t > 1 + tol || u < -tol || u > 1 + tol) return null;
  return { x: p1.x + t * r.x, y: p1.y + t * r.y };
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/compute-crossings.ts
var TOL = 1e-6;
function isHorizontalEdge(edge) {
  const dx = Math.abs(edge.to.x - edge.from.x);
  const dy = Math.abs(edge.to.y - edge.from.y);
  return dx >= dy;
}
function length6(a, b) {
  return Math.hypot(b.x - a.x, b.y - a.y);
}
function pointAt(a, b, t) {
  return { x: a.x + (b.x - a.x) * t, y: a.y + (b.y - a.y) * t };
}
function paramAlong(a, b, p) {
  const L = length6(a, b);
  if (L < TOL) return 0;
  const t = ((p.x - a.x) * (b.x - a.x) + (p.y - a.y) * (b.y - a.y)) / ((b.x - a.x) * (b.x - a.x) + (b.y - a.y) * (b.y - a.y));
  return Math.max(0, Math.min(1, t)) * L;
}
function mergeIntervals(intervals, tol = TOL) {
  if (intervals.length === 0) return intervals;
  intervals.sort((a, b) => a.start - b.start);
  const merged = [];
  let cur = { ...intervals[0] };
  for (let i = 1; i < intervals.length; i++) {
    const nxt = intervals[i];
    if (nxt.start <= cur.end + tol) {
      cur.end = Math.max(cur.end, nxt.end);
    } else {
      merged.push(cur);
      cur = { ...nxt };
    }
  }
  merged.push(cur);
  return merged;
}
function splitEdgeByCrossings(edge, crossingDistances, crossLen) {
  const L = length6(edge.from, edge.to);
  if (L < TOL || crossingDistances.length === 0) return [edge];
  const half = crossLen / 2;
  const rawIntervals = crossingDistances.map((d) => ({
    start: Math.max(0, d - half),
    end: Math.min(L, d + half)
  })).filter((iv) => iv.end - iv.start > TOL);
  const intervals = mergeIntervals(rawIntervals);
  const result = [];
  let cursor = 0;
  const dir = { x: edge.to.x - edge.from.x, y: edge.to.y - edge.from.y };
  const addSeg = (d0, d1, isCrossing) => {
    if (d1 - d0 <= TOL) return;
    const t0 = d0 / L;
    const t1 = d1 / L;
    result.push({
      from: pointAt(edge.from, edge.to, t0),
      to: pointAt(edge.from, edge.to, t1),
      ...isCrossing ? { is_crossing: true } : {}
    });
  };
  for (const iv of intervals) {
    if (iv.start - cursor > TOL) {
      addSeg(cursor, iv.start, false);
    }
    addSeg(iv.start, iv.end, true);
    cursor = iv.end;
  }
  if (L - cursor > TOL) {
    addSeg(cursor, L, false);
  }
  return result.length > 0 ? result : [edge];
}
function computeCrossings(traces, opts = {}) {
  const crossLen = opts.crossSegmentLength ?? 0.075;
  const tol = opts.tolerance ?? TOL;
  const crossingsByEdge = /* @__PURE__ */ new Map();
  const keyOf = (ref) => `${ref.traceIdx}:${ref.edgeIdx}`;
  const getEdge = (ref) => traces[ref.traceIdx].edges[ref.edgeIdx];
  for (let ti = 0; ti < traces.length; ti++) {
    const A = traces[ti];
    for (let ei = 0; ei < A.edges.length; ei++) {
      const eA = A.edges[ei];
      for (let tj = ti; tj < traces.length; tj++) {
        const B = traces[tj];
        for (let ej = tj === ti ? ei + 1 : 0; ej < B.edges.length; ej++) {
          const eB = B.edges[ej];
          if (A.connectivity_key !== void 0 && A.connectivity_key === B.connectivity_key) {
            continue;
          }
          const P = segmentIntersection(eA.from, eA.to, eB.from, eB.to, tol);
          if (!P) continue;
          const LA = length6(eA.from, eA.to);
          const LB = length6(eB.from, eB.to);
          if (LA < tol || LB < tol) continue;
          const dA = paramAlong(eA.from, eA.to, P);
          const dB = paramAlong(eB.from, eB.to, P);
          const nearEndpointA = dA <= tol || Math.abs(LA - dA) <= tol || Number.isNaN(dA);
          const nearEndpointB = dB <= tol || Math.abs(LB - dB) <= tol || Number.isNaN(dB);
          if (!nearEndpointA && !nearEndpointB) {
            const aIsHorizontal = isHorizontalEdge(eA);
            const bIsHorizontal = isHorizontalEdge(eB);
            let assignToA;
            if (aIsHorizontal !== bIsHorizontal) {
              assignToA = aIsHorizontal;
            } else {
              const ax = Math.abs(eA.to.x - eA.from.x);
              const ay = Math.abs(eA.to.y - eA.from.y);
              const bx = Math.abs(eB.to.x - eB.from.x);
              const by = Math.abs(eB.to.y - eB.from.y);
              const aScore = ax - ay;
              const bScore = bx - by;
              assignToA = aScore === bScore ? true : aScore > bScore;
            }
            const chosenKey = keyOf({
              traceIdx: assignToA ? ti : tj,
              edgeIdx: assignToA ? ei : ej
            });
            const chosenList = crossingsByEdge.get(chosenKey) ?? [];
            chosenList.push(assignToA ? dA : dB);
            crossingsByEdge.set(chosenKey, chosenList);
          }
        }
      }
    }
  }
  const out = traces.map((t) => ({
    source_trace_id: t.source_trace_id,
    connectivity_key: t.connectivity_key,
    edges: []
  }));
  for (let ti = 0; ti < traces.length; ti++) {
    const trace = traces[ti];
    for (let ei = 0; ei < trace.edges.length; ei++) {
      const eRefKey = keyOf({ traceIdx: ti, edgeIdx: ei });
      const splittingDistances = crossingsByEdge.get(eRefKey) ?? [];
      if (splittingDistances.length === 0) {
        out[ti].edges.push(trace.edges[ei]);
        continue;
      }
      const uniqueSorted2 = Array.from(
        new Set(splittingDistances.map((d) => Number(d.toFixed(6))))
      ).sort((a, b) => a - b);
      const split = splitEdgeByCrossings(
        trace.edges[ei],
        uniqueSorted2,
        crossLen
      );
      out[ti].edges.push(...split);
    }
  }
  return out;
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/compute-junctions.ts
var TOL2 = 1e-6;
function nearlyEqual(a, b, tol = TOL2) {
  return Math.abs(a - b) <= tol;
}
function pointEq(a, b, tol = TOL2) {
  return nearlyEqual(a.x, b.x, tol) && nearlyEqual(a.y, b.y, tol);
}
function onSegment(p, a, b, tol = TOL2) {
  const minX = Math.min(a.x, b.x) - tol;
  const maxX = Math.max(a.x, b.x) + tol;
  const minY = Math.min(a.y, b.y) - tol;
  const maxY = Math.max(a.y, b.y) + tol;
  if (p.x < minX || p.x > maxX || p.y < minY || p.y > maxY) return false;
  const area = Math.abs((b.x - a.x) * (p.y - a.y) - (b.y - a.y) * (p.x - a.x));
  return area <= tol;
}
function dedupePoints(points, tol = TOL2) {
  const map = /* @__PURE__ */ new Map();
  for (const p of points) {
    const key = `${p.x.toFixed(6)},${p.y.toFixed(6)}`;
    if (!map.has(key)) map.set(key, p);
  }
  return Array.from(map.values());
}
function edgeVec(e) {
  return { x: e.to.x - e.from.x, y: e.to.y - e.from.y };
}
function isEndpointOfEdge(p, e, tol = TOL2) {
  return pointEq(p, e.from, tol) || pointEq(p, e.to, tol);
}
function isParallel(e1, e2, tol = TOL2) {
  const v1 = edgeVec(e1);
  const v2 = edgeVec(e2);
  const L1 = Math.hypot(v1.x, v1.y);
  const L2 = Math.hypot(v2.x, v2.y);
  if (L1 < tol || L2 < tol) return true;
  const cross3 = v1.x * v2.y - v1.y * v2.x;
  return Math.abs(cross3) <= tol * L1 * L2;
}
function incidentEdgesAtPoint(trace, p, tol = TOL2) {
  return trace.edges.filter(
    (e) => pointEq(e.from, p, tol) || pointEq(e.to, p, tol)
  );
}
function nearestEndpointOnTrace(trace, p, tol = TOL2) {
  for (const e of trace.edges) {
    if (pointEq(e.from, p, tol)) return e.from;
    if (pointEq(e.to, p, tol)) return e.to;
  }
  return null;
}
function edgeDirectionFromPoint(e, p, tol = TOL2) {
  const other = pointEq(e.from, p, tol) || nearlyEqual(e.from.x, p.x, tol) && nearlyEqual(e.from.y, p.y, tol) ? e.to : e.from;
  const dx = other.x - p.x;
  const dy = other.y - p.y;
  if (Math.abs(dx) < tol && Math.abs(dy) < tol) return null;
  if (Math.abs(dx) >= Math.abs(dy)) {
    return dx >= 0 ? "right" : "left";
  }
  return dy >= 0 ? "up" : "down";
}
function getCornerOrientationAtPoint(trace, p, tol = TOL2) {
  const incident = incidentEdgesAtPoint(trace, p, tol);
  if (incident.length < 2) return null;
  const dirs = incident.map((e) => edgeDirectionFromPoint(e, p, tol));
  const hasUp = dirs.includes("up");
  const hasDown = dirs.includes("down");
  const hasLeft = dirs.includes("left");
  const hasRight = dirs.includes("right");
  const vertical = hasUp ? "up" : hasDown ? "down" : null;
  const horizontal = hasRight ? "right" : hasLeft ? "left" : null;
  if (vertical && horizontal) {
    return `${vertical}-${horizontal}`;
  }
  return null;
}
function computeJunctions(traces, opts = {}) {
  const tol = opts.tolerance ?? TOL2;
  const result = {};
  for (const t of traces) result[t.source_trace_id] = [];
  const endpointsByTrace = traces.map((t) => {
    const pts = [];
    for (const e of t.edges) {
      pts.push(e.from, e.to);
    }
    return dedupePoints(pts, tol);
  });
  for (let i = 0; i < traces.length; i++) {
    const A = traces[i];
    const AEnds = endpointsByTrace[i];
    for (let j = i + 1; j < traces.length; j++) {
      const B = traces[j];
      const BEnds = endpointsByTrace[j];
      const isSameNet2 = A.connectivity_key !== void 0 && A.connectivity_key === B.connectivity_key;
      if (!isSameNet2) continue;
      for (const eA of A.edges) {
        for (const eB of B.edges) {
          const p = segmentIntersection(eA.from, eA.to, eB.from, eB.to, tol);
          if (!p) continue;
          if (isEndpointOfEdge(p, eA, tol) || isEndpointOfEdge(p, eB, tol)) {
            continue;
          }
          result[A.source_trace_id].push(p);
          if (A.source_trace_id !== B.source_trace_id) {
            result[B.source_trace_id].push(p);
          }
        }
      }
      for (const pa of AEnds) {
        for (const pb of BEnds) {
          if (pointEq(pa, pb, tol)) {
            const aEdgesAtP = incidentEdgesAtPoint(A, pa, tol);
            const bEdgesAtP = incidentEdgesAtPoint(B, pb, tol);
            const hasCorner = aEdgesAtP.some(
              (eA) => bEdgesAtP.some((eB) => !isParallel(eA, eB, tol))
            );
            const aCorner = getCornerOrientationAtPoint(A, pa, tol);
            const bCorner = getCornerOrientationAtPoint(B, pb, tol);
            const sameCornerOrientation = aCorner !== null && bCorner !== null && aCorner === bCorner;
            if (hasCorner && !sameCornerOrientation) {
              result[A.source_trace_id].push(pa);
              if (A.source_trace_id !== B.source_trace_id)
                result[B.source_trace_id].push(pb);
            }
          }
        }
      }
      for (const pa of AEnds) {
        for (const eB of B.edges) {
          if (onSegment(pa, eB.from, eB.to, tol)) {
            const aEdgesAtP = incidentEdgesAtPoint(A, pa, tol);
            const hasCorner = aEdgesAtP.some((eA) => !isParallel(eA, eB, tol));
            const aCorner = getCornerOrientationAtPoint(A, pa, tol);
            const bEndpointNearPa = nearestEndpointOnTrace(B, pa, tol * 1e3);
            const bCorner = bEndpointNearPa ? getCornerOrientationAtPoint(B, bEndpointNearPa, tol) : null;
            const sameCornerOrientation = aCorner !== null && bCorner !== null && aCorner === bCorner;
            if (hasCorner && !sameCornerOrientation) {
              result[A.source_trace_id].push(pa);
              if (A.source_trace_id !== B.source_trace_id)
                result[B.source_trace_id].push(pa);
            }
          }
        }
      }
      for (const pb of BEnds) {
        for (const eA of A.edges) {
          if (onSegment(pb, eA.from, eA.to, tol)) {
            const bEdgesAtP = incidentEdgesAtPoint(B, pb, tol);
            const hasCorner = bEdgesAtP.some((eB) => !isParallel(eA, eB, tol));
            const bCorner = getCornerOrientationAtPoint(B, pb, tol);
            const aEndpointNearPb = nearestEndpointOnTrace(A, pb, tol * 1e3);
            const aCorner = aEndpointNearPb ? getCornerOrientationAtPoint(A, aEndpointNearPb, tol) : null;
            const sameCornerOrientation = aCorner !== null && bCorner !== null && aCorner === bCorner;
            if (hasCorner && !sameCornerOrientation) {
              result[B.source_trace_id].push(pb);
              if (A.source_trace_id !== B.source_trace_id)
                result[A.source_trace_id].push(pb);
            }
          }
        }
      }
    }
  }
  for (const id of Object.keys(result)) {
    result[id] = dedupePoints(result[id], tol);
  }
  return result;
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/getPortForSchematicSymbolPort.ts
var getPortForSchematicSymbolPort = (port) => {
  const { schX, schY } = port._parsedProps;
  if (schX !== void 0 && schY !== void 0) return port;
  if (!port.getParentNormalComponent()?.getSchematicSymbol()) return port;
  const internallyConnectedPorts = port._getPortsInternallyConnectedToThisPort();
  const schematicSymbolPortDef = port.schematicSymbolPortDef ?? internallyConnectedPorts.find(
    (connectedPort) => connectedPort.schematicSymbolPortDef
  )?.schematicSymbolPortDef;
  if (!schematicSymbolPortDef) return port;
  return internallyConnectedPorts.find(
    (connectedPort) => connectedPort.schematicSymbolPortDef === schematicSymbolPortDef
  ) ?? port;
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/remove-overlapping-same-net-crossing-segments.ts
var TOL3 = 1e-6;
function isCollinear(a, b, tol = TOL3) {
  const adx = a.to.x - a.from.x;
  const ady = a.to.y - a.from.y;
  const bdx = b.to.x - b.from.x;
  const bdy = b.to.y - b.from.y;
  const aLen = Math.hypot(adx, ady);
  const bLen = Math.hypot(bdx, bdy);
  if (aLen <= tol || bLen <= tol) return false;
  const directionCross = adx * bdy - ady * bdx;
  if (Math.abs(directionCross) > tol * aLen * bLen) return false;
  const offsetCross = adx * (b.from.y - a.from.y) - ady * (b.from.x - a.from.x);
  return Math.abs(offsetCross) <= tol * aLen;
}
function splitEdgeAroundIntervals(edge, intervals) {
  const merged = mergeIntervals(intervals);
  if (merged.length === 0) return [edge];
  const edgeLength = length6(edge.from, edge.to);
  if (edgeLength <= TOL3) return [edge];
  const result = [];
  let cursor = 0;
  for (const interval of merged) {
    if (interval.start - cursor > TOL3) {
      result.push({
        from: pointAt(edge.from, edge.to, cursor / edgeLength),
        to: pointAt(edge.from, edge.to, interval.start / edgeLength)
      });
    }
    if (interval.end - interval.start > TOL3) {
      result.push({
        from: pointAt(edge.from, edge.to, interval.start / edgeLength),
        to: pointAt(edge.from, edge.to, interval.end / edgeLength),
        is_crossing: true
      });
    }
    cursor = Math.max(cursor, interval.end);
  }
  if (edgeLength - cursor > TOL3) {
    result.push({
      from: pointAt(edge.from, edge.to, cursor / edgeLength),
      to: edge.to
    });
  }
  return result;
}
function removeOverlappingSameNetCrossingSegments(traces) {
  const intervalsToRemove = /* @__PURE__ */ new Map();
  for (let crossingTraceIndex = 0; crossingTraceIndex < traces.length; crossingTraceIndex++) {
    const crossingTrace = traces[crossingTraceIndex];
    if (!crossingTrace.connectivity_key) continue;
    for (const crossingEdge of crossingTrace.edges) {
      if (!crossingEdge.is_crossing || length6(crossingEdge.from, crossingEdge.to) <= TOL3)
        continue;
      for (let traceIndex = 0; traceIndex < traces.length; traceIndex++) {
        const candidateTrace = traces[traceIndex];
        if (traceIndex === crossingTraceIndex || candidateTrace.connectivity_key !== crossingTrace.connectivity_key) {
          continue;
        }
        for (let edgeIndex = 0; edgeIndex < candidateTrace.edges.length; edgeIndex++) {
          const candidateEdge = candidateTrace.edges[edgeIndex];
          const candidateLength = length6(candidateEdge.from, candidateEdge.to);
          if (candidateEdge.is_crossing || candidateLength <= TOL3) continue;
          if (!isCollinear(candidateEdge, crossingEdge)) continue;
          const start = Math.max(
            0,
            Math.min(
              paramAlong(
                candidateEdge.from,
                candidateEdge.to,
                crossingEdge.from
              ),
              paramAlong(candidateEdge.from, candidateEdge.to, crossingEdge.to)
            )
          );
          const end = Math.min(
            candidateLength,
            Math.max(
              paramAlong(
                candidateEdge.from,
                candidateEdge.to,
                crossingEdge.from
              ),
              paramAlong(candidateEdge.from, candidateEdge.to, crossingEdge.to)
            )
          );
          if (end - start <= TOL3) continue;
          const key = `${traceIndex}:${edgeIndex}`;
          const intervals = intervalsToRemove.get(key) ?? [];
          intervals.push({ start, end });
          intervalsToRemove.set(key, intervals);
        }
      }
    }
  }
  if (intervalsToRemove.size === 0) return traces;
  return traces.map((trace, traceIndex) => ({
    ...trace,
    edges: trace.edges.flatMap((edge, edgeIndex) => {
      const intervals = intervalsToRemove.get(`${traceIndex}:${edgeIndex}`);
      return intervals ? splitEdgeAroundIntervals(edge, intervals) : [edge];
    })
  }));
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/applyTracesFromSolverOutput.ts
var debug10 = Debug12("Group_doInitialSchematicTraceRender");
var MAX_PIN_SNAP_GAP = 1.5;
function extendTraceEndpointsToReachPinsInsideExpandedBoundingBox(params, db) {
  const { points, schematicPortIds, expandedBoundsByPortId } = params;
  const centers = schematicPortIds.map((id) => {
    const center = db.schematic_port.get(id)?.center;
    const bounds = expandedBoundsByPortId.get(id);
    if (!center || !bounds) return null;
    return { center, bounds };
  }).filter(
    (port) => Boolean(port)
  );
  if (centers.length === 0) return points;
  const result = points.map((p) => ({ x: p.x, y: p.y }));
  const d2 = (a, b) => (a.x - b.x) ** 2 + (a.y - b.y) ** 2;
  const usedCenters = /* @__PURE__ */ new Set();
  for (let i = 0; i < centers.length; i++) {
    if (result.some((p) => d2(centers[i].center, p) <= 1e-12)) {
      usedCenters.add(i);
    }
  }
  const ALIGN_EPS = 1e-3;
  const endpoints = ["start", "end"];
  const candidates = [];
  for (const endpoint of endpoints) {
    const endpointPoint = endpoint === "start" ? result[0] : result[result.length - 1];
    for (let i = 0; i < centers.length; i++) {
      if (usedCenters.has(i)) continue;
      const { center, bounds } = centers[i];
      const dist = d2(center, endpointPoint);
      if (Math.abs(center.x - endpointPoint.x) > ALIGN_EPS && Math.abs(center.y - endpointPoint.y) > ALIGN_EPS) {
        continue;
      }
      if (dist > MAX_PIN_SNAP_GAP ** 2 && pointToBoundsDistance(endpointPoint, bounds) > ALIGN_EPS) {
        continue;
      }
      candidates.push({ endpoint, centerIndex: i, dist });
    }
  }
  candidates.sort((a, b) => a.dist - b.dist);
  const usedEndpoints = /* @__PURE__ */ new Set();
  for (const { endpoint, centerIndex, dist } of candidates) {
    if (usedEndpoints.has(endpoint) || usedCenters.has(centerIndex)) continue;
    usedCenters.add(centerIndex);
    usedEndpoints.add(endpoint);
    if (dist <= 1e-12) continue;
    const center = centers[centerIndex].center;
    if (endpoint === "start") result.unshift({ x: center.x, y: center.y });
    else result.push({ x: center.x, y: center.y });
  }
  return result;
}
function applyTracesFromSolverOutput(args) {
  const {
    group,
    solver,
    userNetIdToConnKey,
    schematicPortIdsWithPreExistingNetLabels
  } = args;
  const { db } = group.root;
  const schematicPortIdsByRoutedSchematicPortId = /* @__PURE__ */ new Map();
  for (const port of group.selectAll("port")) {
    const routedSchematicPortIdValue = getPortForSchematicSymbolPort(port).schematic_port_id;
    const schematicPortIdValue = port.schematic_port_id;
    if (!routedSchematicPortIdValue || !schematicPortIdValue) continue;
    const routedSchematicPortId = asSchematicPortId(routedSchematicPortIdValue);
    const schematicPortId = asSchematicPortId(schematicPortIdValue);
    const schematicPortIds = schematicPortIdsByRoutedSchematicPortId.get(routedSchematicPortId) ?? [];
    schematicPortIds.push(schematicPortId);
    schematicPortIdsByRoutedSchematicPortId.set(
      routedSchematicPortId,
      schematicPortIds
    );
  }
  const expandedBoundsByPortId = /* @__PURE__ */ new Map();
  for (const schematicComponent of db.schematic_component.list()) {
    const bounds = getSchematicComponentWithTextBounds({
      db,
      schematicComponent
    });
    if (!bounds) continue;
    for (const port of db.schematic_port.list({
      schematic_component_id: schematicComponent.schematic_component_id
    })) {
      expandedBoundsByPortId.set(
        asSchematicPortId(port.schematic_port_id),
        bounds
      );
    }
  }
  const traces = solver.sameNetJunctionAlignmentSolver?.getOutput().traces ?? solver.netLabelTraceCollisionSolver?.getOutput().traces ?? solver.traceCleanupSolver?.getOutput().traces ?? solver.traceLabelOverlapAvoidanceSolver?.getOutput().traces ?? solver.schematicTraceLinesSolver?.solvedTracePaths;
  const pendingTraces = [];
  debug10(`Traces inside SchematicTraceSolver output: ${(traces ?? []).length}`);
  for (const solvedTracePath of traces ?? []) {
    const uniquePinIds = Array.from(new Set(solvedTracePath.pinIds ?? []));
    const solvedTraceSchematicPortIds = uniquePinIds.map(asSchematicPortId);
    const isNetLabelCoveredTrace = solvedTraceSchematicPortIds.length > 0 && solvedTraceSchematicPortIds.every(
      (id) => schematicPortIdsWithPreExistingNetLabels.has(id)
    );
    if (isNetLabelCoveredTrace) {
      debug10(
        `Skipping solver netlabel-covered trace ${solvedTracePath?.mspPairId} because all schematic ports already have netlabels`
      );
      continue;
    }
    const points = solvedTracePath?.tracePath;
    if (!Array.isArray(points) || points.length < 2) {
      debug10(
        `Skipping trace ${solvedTracePath?.pinIds.join(",")} because it has less than 2 points`
      );
      continue;
    }
    const snappedPoints = extendTraceEndpointsToReachPinsInsideExpandedBoundingBox(
      {
        points,
        schematicPortIds: solvedTraceSchematicPortIds,
        expandedBoundsByPortId
      },
      db
    );
    const edges = [];
    for (let i = 0; i < snappedPoints.length - 1; i++) {
      edges.push({
        from: { x: snappedPoints[i].x, y: snappedPoints[i].y },
        to: { x: snappedPoints[i + 1].x, y: snappedPoints[i + 1].y }
      });
    }
    const source_trace_id = String(solvedTracePath?.mspPairId);
    let subcircuit_connectivity_map_key;
    if (Array.isArray(solvedTracePath?.pins) && solvedTracePath.pins.length === 2) {
      const firstPinId = solvedTracePath.pins[0]?.pinId;
      const secondPinId = solvedTracePath.pins[1]?.pinId;
      const pA = firstPinId ? asSchematicPortId(firstPinId) : void 0;
      const pB = secondPinId ? asSchematicPortId(secondPinId) : void 0;
      if (pA && pB) {
        const routedSchematicPortIds = /* @__PURE__ */ new Set([pA, pB]);
        for (const routedSchematicPortId of routedSchematicPortIds) {
          db.schematic_port.update(routedSchematicPortId, {
            is_connected: true
          });
          for (const schematicPortId of schematicPortIdsByRoutedSchematicPortId.get(
            routedSchematicPortId
          ) ?? []) {
            db.schematic_port.update(schematicPortId, {
              is_connected: true
            });
          }
        }
        subcircuit_connectivity_map_key = userNetIdToConnKey.get(
          String(solvedTracePath.userNetId)
        );
      }
    }
    if (!subcircuit_connectivity_map_key) {
      subcircuit_connectivity_map_key = userNetIdToConnKey.get(
        String(solvedTracePath.userNetId)
      );
    }
    if (!subcircuit_connectivity_map_key) {
      const sourcePortConnKeys = solvedTraceSchematicPortIds.map((schematicPortId) => {
        const schematicPort = db.schematic_port.get(schematicPortId);
        const sourcePortId = schematicPort?.source_port_id;
        if (!sourcePortId) return void 0;
        return db.source_port.get(sourcePortId)?.subcircuit_connectivity_map_key;
      }).filter((key) => Boolean(key));
      const uniqueSourcePortConnKeys = new Set(sourcePortConnKeys);
      if (uniqueSourcePortConnKeys.size === 1) {
        subcircuit_connectivity_map_key = sourcePortConnKeys[0];
      }
    }
    const endpointSchematicSheetIds = new Set(
      solvedTraceSchematicPortIds.map(
        (schematicPortId) => db.schematic_port.get(schematicPortId)?.schematic_sheet_id
      ).filter((sheetId) => Boolean(sheetId))
    );
    let schematicSheetId2;
    if (endpointSchematicSheetIds.size === 1) {
      schematicSheetId2 = endpointSchematicSheetIds.values().next().value;
    } else if (endpointSchematicSheetIds.size === 0) {
      schematicSheetId2 = group._resolveSchematicSheetId();
    }
    pendingTraces.push({
      source_trace_id,
      edges,
      subcircuit_connectivity_map_key,
      schematic_sheet_id: schematicSheetId2
    });
  }
  debug10(
    `Applying ${pendingTraces.length} traces from SchematicTraceSolver output`
  );
  const withCrossings = computeCrossings(
    pendingTraces.map((t) => ({
      source_trace_id: t.source_trace_id,
      edges: t.edges,
      connectivity_key: t.subcircuit_connectivity_map_key
    }))
  );
  const existingTracesForJunctions = [];
  const schematicSheetId = group._resolveSchematicSheetId();
  for (const t of db.schematic_trace.list()) {
    if (t.edges.length === 0) continue;
    if (t.schematic_sheet_id !== schematicSheetId) continue;
    const sourceTrace = t.source_trace_id ? db.source_trace.get(t.source_trace_id) : void 0;
    existingTracesForJunctions.push({
      schematic_trace_id: t.schematic_trace_id,
      source_trace_id: t.source_trace_id ?? t.schematic_trace_id,
      edges: t.edges,
      connectivity_key: t.subcircuit_connectivity_map_key ?? sourceTrace?.subcircuit_connectivity_map_key
    });
  }
  const tracesWithTrimmedCrossingOverlaps = removeOverlappingSameNetCrossingSegments([
    ...withCrossings,
    ...existingTracesForJunctions
  ]);
  const visibleTraces = tracesWithTrimmedCrossingOverlaps.slice(
    0,
    withCrossings.length
  );
  const visibleExistingTraces = tracesWithTrimmedCrossingOverlaps.slice(
    withCrossings.length
  );
  for (const trace of visibleExistingTraces) {
    if (!trace.schematic_trace_id) continue;
    db.schematic_trace.update(trace.schematic_trace_id, {
      edges: trace.edges
    });
  }
  const junctionsById = computeJunctions([
    ...visibleTraces,
    ...visibleExistingTraces
  ]);
  for (const t of visibleTraces) {
    const pendingTrace = pendingTraces.find(
      (pendingTrace2) => pendingTrace2.source_trace_id === t.source_trace_id
    );
    let traceSchematicSheetId = schematicSheetId;
    if (pendingTrace?.schematic_sheet_id) {
      traceSchematicSheetId = pendingTrace.schematic_sheet_id;
    }
    db.schematic_trace.insert({
      source_trace_id: t.source_trace_id,
      edges: t.edges,
      junctions: junctionsById[t.source_trace_id] ?? [],
      subcircuit_connectivity_map_key: pendingTrace?.subcircuit_connectivity_map_key,
      schematic_sheet_id: traceSchematicSheetId
    });
  }
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/createSchematicTraceSolverInputProblem.ts
import {
  getBoundFromCenteredRect as getBoundFromCenteredRect4,
  getBoundsCenter as getBoundsCenter2
} from "@tscircuit/math-utils";
import "@tscircuit/schematic-trace-solver";

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/applyInlineNetLabelEligibility.ts
var applyInlineNetLabelEligibility = ({
  directConnections,
  connKeyToSchematicPortIds,
  connKeyToSourceNet,
  resolveCanonicalNetLabelText
}) => {
  for (const directConnection of directConnections) {
    const { connKey } = directConnection;
    if (!connKey) continue;
    const portsOnNet = connKeyToSchematicPortIds.get(connKey);
    if (portsOnNet?.length !== 2) continue;
    const sourceNet = connKeyToSourceNet.get(connKey);
    if (sourceNet?.is_power || sourceNet?.is_ground) continue;
    const { name, wasAssignedDisplayLabel } = resolveCanonicalNetLabelText({
      subcircuitConnectivityMapKey: connKey
    });
    if (!name || !wasAssignedDisplayLabel) continue;
    directConnection.allowInlineNetLabel = true;
    directConnection.inlineNetLabelWidth = Number(
      getSchematicNetLabelTextWidth({ text: name }).toFixed(2)
    );
  }
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/resolveNetLabelForPortMissingTrace.ts
var getDirectCrossSubcircuitConnectedSourcePortId = (db, sourcePortId) => {
  const sourcePort = db.source_port.get(sourcePortId);
  if (!sourcePort) return void 0;
  for (const sourceTrace of db.source_trace.list()) {
    const connectedSourcePortIds = sourceTrace.connected_source_port_ids ?? [];
    if (connectedSourcePortIds.length !== 2) continue;
    if ((sourceTrace.connected_source_net_ids ?? []).length > 0) continue;
    if (!connectedSourcePortIds.includes(sourcePortId)) continue;
    const otherSourcePortId = connectedSourcePortIds.find(
      (portId) => portId !== sourcePortId
    );
    if (!otherSourcePortId) continue;
    const otherSourcePort = db.source_port.get(otherSourcePortId);
    if (!otherSourcePort) continue;
    if (otherSourcePort.subcircuit_id === sourcePort.subcircuit_id) continue;
    return asSourcePortId(otherSourcePortId);
  }
};
var resolveNetLabelForPortMissingTrace = ({
  group,
  sourcePortId,
  connectedSourcePortIdsForKey,
  connKey,
  sourceNet
}) => {
  const { db } = group.root;
  const connectedPortsForKey = group.selectAll("port").filter((port) => port._getSubcircuitConnectivityKey() === connKey);
  const { name: resolvedPortNetLabelText, wasAssignedDisplayLabel } = getNetNameFromPorts(connectedPortsForKey);
  const assignedPortNetLabelText = wasAssignedDisplayLabel ? resolvedPortNetLabelText : void 0;
  const fallbackPortNetLabelText = wasAssignedDisplayLabel ? void 0 : resolvedPortNetLabelText || void 0;
  const implicitPortLabelText = getNetNameFromSourcePorts(
    db,
    connectedSourcePortIdsForKey
  );
  const directCrossSubcircuitConnectedSourcePortId = getDirectCrossSubcircuitConnectedSourcePortId(db, sourcePortId);
  const directCrossSubcircuitConnectionLabelText = directCrossSubcircuitConnectedSourcePortId ? getSourcePortNetLabelText(
    db,
    directCrossSubcircuitConnectedSourcePortId
  ) : void 0;
  return {
    text: sourceNet?.name || sourceNet?.source_net_id || assignedPortNetLabelText || directCrossSubcircuitConnectionLabelText || fallbackPortNetLabelText || implicitPortLabelText || connKey,
    wasAssignedDisplayLabel,
    directCrossSubcircuitConnectedSourcePortId
  };
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/schematicTextToTextBounds.ts
import {
  getBoundFromCenteredRect as getBoundFromCenteredRect3,
  getBoundsCenter
} from "@tscircuit/math-utils";

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/getSchematicTextBounds.ts
import { getBoundsFromPoints as getBoundsFromPoints4 } from "@tscircuit/math-utils";
var DEFAULT_SCHEMATIC_TEXT_FONT_SIZE = 0.18;
function getSchematicTextBounds(text) {
  const fontSize = text.font_size ?? DEFAULT_SCHEMATIC_TEXT_FONT_SIZE;
  const width = getSchematicTextWidth({
    text: text.text ?? "",
    fontSize
  });
  const height = fontSize;
  const anchor = text.anchor ?? "center";
  let minX;
  let maxX;
  if (anchor.includes("left")) {
    minX = 0;
    maxX = width;
  } else if (anchor.includes("right")) {
    minX = -width;
    maxX = 0;
  } else {
    minX = -width / 2;
    maxX = width / 2;
  }
  let minY;
  let maxY;
  if (anchor.includes("top")) {
    minY = -height;
    maxY = 0;
  } else if (anchor.includes("bottom")) {
    minY = 0;
    maxY = height;
  } else {
    minY = -height / 2;
    maxY = height / 2;
  }
  const rotation4 = (text.rotation ?? 0) * Math.PI / 180;
  const cos = Math.cos(rotation4);
  const sin = Math.sin(rotation4);
  const corners = [
    { x: minX, y: minY },
    { x: minX, y: maxY },
    { x: maxX, y: minY },
    { x: maxX, y: maxY }
  ].map((corner) => ({
    x: text.position.x + corner.x * cos - corner.y * sin,
    y: text.position.y + corner.x * sin + corner.y * cos
  }));
  return getBoundsFromPoints4(corners);
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/schematicTextToTextBounds.ts
var REFDES_TEXT_BOX_CHIP_OVERLAP = 0.01;
var REFDES_TEXT_BOX_CLEARANCE = 0.06;
function isReferenceDesignatorText(text, sourceComponent) {
  if (!sourceComponent) return false;
  return text.text === sourceComponent.display_name || text.text === sourceComponent.name;
}
function schematicTextToTextBox(text, options = {}) {
  if (!text.text) return null;
  const bounds = getSchematicTextBounds(text);
  if (isReferenceDesignatorText(text, options.sourceComponent) && options.schematicComponent?.center && options.schematicComponent?.size) {
    const componentBounds = getBoundFromCenteredRect3({
      center: options.schematicComponent.center,
      width: options.schematicComponent.size.width,
      height: options.schematicComponent.size.height
    });
    const textHeight = bounds.maxY - bounds.minY;
    if (bounds.minY > componentBounds.maxY) {
      bounds.minX -= REFDES_TEXT_BOX_CLEARANCE;
      bounds.maxX += REFDES_TEXT_BOX_CLEARANCE;
      bounds.minY = componentBounds.maxY - REFDES_TEXT_BOX_CHIP_OVERLAP;
      bounds.maxY = componentBounds.maxY + textHeight + REFDES_TEXT_BOX_CLEARANCE;
    }
  }
  return {
    chipId: text.schematic_component_id,
    center: getBoundsCenter(bounds),
    width: bounds.maxX - bounds.minX,
    height: bounds.maxY - bounds.minY,
    text: text.text
  };
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/createSchematicTraceSolverInputProblem.ts
var DEFAULT_MAX_MSP_PAIR_DISTANCE = 2.4;
var SCHEMATIC_RAIL_NET_LABEL_HEIGHT = 0.42;
function createSchematicTraceSolverInputProblem(group, opts = {}) {
  const { db } = group.root;
  const connKeyToSourceNet = /* @__PURE__ */ new Map();
  const childGroups = [];
  const componentsToVisit = [...group.children].reverse();
  while (componentsToVisit.length > 0) {
    const child = componentsToVisit.pop();
    if (child instanceof Group5) {
      if (child.isSubcircuit) {
        if (child._parsedProps.showAsSchematicBox) childGroups.push(child);
        continue;
      }
      childGroups.push(child);
    }
    componentsToVisit.push(...[...child.children].reverse());
  }
  const allSchematicGroupIds = [
    group.schematic_group_id,
    ...childGroups.map((a) => a.schematic_group_id)
  ];
  const schematicComponents = db.schematic_component.list().filter(
    (component) => allSchematicGroupIds.includes(component.schematic_group_id)
  ).filter(
    (component) => opts.schematicSheetId === void 0 || component.schematic_sheet_id === opts.schematicSheetId
  );
  const schematicComponentIds = new Set(
    schematicComponents.map((component) => component.schematic_component_id)
  );
  const schematicComponentsById = new Map(
    schematicComponents.map((component) => [
      component.schematic_component_id,
      component
    ])
  );
  const textBoxes = db.schematic_text.list().filter(
    (text) => text.schematic_component_id && schematicComponentIds.has(text.schematic_component_id)
  ).map((text) => {
    const schematicComponent = schematicComponentsById.get(
      text.schematic_component_id
    );
    if (!schematicComponent) return;
    const sourceComponent = db.source_component.get(
      schematicComponent.source_component_id
    );
    return schematicTextToTextBox(text, {
      schematicComponent,
      sourceComponent
    });
  }).filter((textBox) => Boolean(textBox));
  const sectionIdBySchematicComponentId = /* @__PURE__ */ new Map();
  for (const component of group.getDescendants()) {
    const schematicComponentId = component.schematic_component_id;
    const sectionId = component.getSchematicSectionName();
    if (schematicComponentId && sectionId) {
      sectionIdBySchematicComponentId.set(schematicComponentId, sectionId);
    }
  }
  const chips = [];
  for (const schematicComponent of schematicComponents) {
    const chipId = schematicComponent.schematic_component_id;
    const pins = [];
    const schematicPorts = db.schematic_port.list({
      schematic_component_id: schematicComponent.schematic_component_id
    });
    for (const schematicPort of schematicPorts) {
      const schematicPortId = asSchematicPortId(schematicPort.schematic_port_id);
      pins.push({
        pinId: schematicPortId,
        x: schematicPort.center.x,
        y: schematicPort.center.y,
        // Pass the port's true facing direction (known from the schematic
        // symbol). The chip box handed to the solver is text-inclusive, so for
        // small parts with a large reference designator the pins sit inside the
        // box. The solver snaps such pins to the box edge along this facing
        // direction (rather than guessing from geometry, which would pick the
        // wrong edge for a resistor whose ref text widened the box).
        _facingDirection: convertFacingDirectionToElbowDirection(
          schematicPort.facing_direction ?? null
        )
      });
    }
    const sectionId = sectionIdBySchematicComponentId.get(
      schematicComponent.schematic_component_id
    );
    const layoutBounds = getSchematicComponentWithTextBounds({ db, schematicComponent }) ?? getBoundFromCenteredRect4({
      center: schematicComponent.center,
      width: schematicComponent.size.width,
      height: schematicComponent.size.height
    });
    chips.push({
      chipId,
      center: getBoundsCenter2(layoutBounds),
      width: layoutBounds.maxX - layoutBounds.minX,
      height: layoutBounds.maxY - layoutBounds.minY,
      pins,
      sectionId
    });
  }
  const schematicPortIdsInScope = /* @__PURE__ */ new Set();
  const schPortIdToSourcePortId = /* @__PURE__ */ new Map();
  const sourcePortIdToSchPortId = /* @__PURE__ */ new Map();
  const userNetIdToConnKey = /* @__PURE__ */ new Map();
  const componentPortBySchematicPortId = new Map(
    group.selectAll("port").filter((port) => port.schematic_port_id).map((port) => [asSchematicPortId(port.schematic_port_id), port])
  );
  const resolveCanonicalNetLabelText = createCanonicalSchematicNetLabelTextResolver(group);
  for (const sc of schematicComponents) {
    const ports = db.schematic_port.list({
      schematic_component_id: sc.schematic_component_id
    });
    for (const sp of ports) {
      const schematicPortId = asSchematicPortId(sp.schematic_port_id);
      schematicPortIdsInScope.add(schematicPortId);
      if (sp.source_port_id) {
        const sourcePortId = asSourcePortId(sp.source_port_id);
        schPortIdToSourcePortId.set(schematicPortId, sourcePortId);
        sourcePortIdToSchPortId.set(sourcePortId, schematicPortId);
      }
    }
  }
  const netLabelsInScope = (opts.netLabels ?? []).filter(
    (netLabel) => netLabel._getConnectedPorts().some((port) => {
      if (!port.schematic_port_id) return false;
      return schematicPortIdsInScope.has(
        asSchematicPortId(port.schematic_port_id)
      );
    })
  );
  const solverManagedNetLabelSchematicPortIds = new Set(
    netLabelsInScope.filter(
      (netLabel) => netLabel._parsedProps.schX === void 0 && netLabel._parsedProps.schY === void 0
    ).flatMap((netLabel) => netLabel._getConnectedPorts()).map((port) => port.schematic_port_id).filter(
      (schematicPortId) => schematicPortId !== null && schematicPortId !== void 0
    ).map(asSchematicPortId)
  );
  const allowedSubcircuitIds = /* @__PURE__ */ new Set();
  if (group.subcircuit_id) allowedSubcircuitIds.add(group.subcircuit_id);
  for (const cg of childGroups) {
    if (cg.subcircuit_id) allowedSubcircuitIds.add(cg.subcircuit_id);
  }
  const tracesInScope = db.source_trace.list().filter((st) => {
    if (st.subcircuit_id === group.subcircuit_id) return true;
    for (const source_port_id of st.connected_source_port_ids) {
      if (sourcePortIdToSchPortId.has(asSourcePortId(source_port_id))) {
        return true;
      }
    }
    return false;
  });
  const schematicPortIdsWithExternallyRoutedRepresentations = /* @__PURE__ */ new Set();
  for (const sourceTrace of tracesInScope) {
    if (sourceTrace.subcircuit_id === group.subcircuit_id) continue;
    if (sourceTrace.connected_source_net_ids.length === 0) continue;
    for (const sourcePortId of sourceTrace.connected_source_port_ids) {
      const typedSourcePortId = asSourcePortId(sourcePortId);
      const schematicPortId = sourcePortIdToSchPortId.get(typedSourcePortId);
      if (!schematicPortId) continue;
      if (solverManagedNetLabelSchematicPortIds.has(schematicPortId)) continue;
      const hasAnotherRepresentationOnSheet = db.schematic_port.list({ source_port_id: sourcePortId }).some((port) => {
        if (port.schematic_port_id === schematicPortId) return false;
        const component = port.schematic_component_id ? db.schematic_component.get(port.schematic_component_id) : void 0;
        return component?.schematic_sheet_id === opts.schematicSheetId;
      });
      if (!hasAnotherRepresentationOnSheet) continue;
      schematicPortIdsWithExternallyRoutedRepresentations.add(schematicPortId);
    }
  }
  const externalNetIds = tracesInScope.flatMap(
    (st) => st.connected_source_net_ids
  );
  for (const netId of externalNetIds) {
    const net = db.source_net.get(netId);
    if (net?.subcircuit_id) {
      allowedSubcircuitIds.add(net.subcircuit_id);
    }
  }
  for (const net of db.source_net.list().filter(
    (sourceNet) => !sourceNet.subcircuit_id || allowedSubcircuitIds.has(sourceNet.subcircuit_id)
  )) {
    if (net.subcircuit_connectivity_map_key) {
      connKeyToSourceNet.set(net.subcircuit_connectivity_map_key, net);
    }
  }
  const directConnections = [];
  const singlePortTraceNetConnections = [];
  const connectedPairKeys = /* @__PURE__ */ new Set();
  const crossSubcircuitTraceLabelTextBySchematicPortId = /* @__PURE__ */ new Map();
  const sourceTraceIdByPinPairKey = /* @__PURE__ */ new Map();
  const connKeysWithExplicitPortNetTraces = /* @__PURE__ */ new Set();
  for (const sourceTrace of tracesInScope) {
    if (sourceTrace?.subcircuit_connectivity_map_key && (sourceTrace.connected_source_port_ids?.length ?? 0) > 0 && (sourceTrace.connected_source_net_ids?.length ?? 0) > 0) {
      connKeysWithExplicitPortNetTraces.add(
        sourceTrace.subcircuit_connectivity_map_key
      );
    }
  }
  for (const st of tracesInScope) {
    const connected = (st.connected_source_port_ids ?? []).map(
      (sourcePortId) => sourcePortIdToSchPortId.get(asSourcePortId(sourcePortId))
    ).filter(
      (schematicPortId) => Boolean(schematicPortId) && schematicPortIdsInScope.has(schematicPortId)
    );
    const [firstSourcePort, secondSourcePort] = st.connected_source_port_ids.map(
      (sourcePortId) => db.source_port.get(sourcePortId)
    );
    const crossesSubcircuitBoundary = Boolean(
      firstSourcePort && secondSourcePort && firstSourcePort.subcircuit_id !== secondSourcePort.subcircuit_id
    );
    if (connected.length === 1 && st.connected_source_port_ids.length === 2 && crossesSubcircuitBoundary && st.connected_source_net_ids.length === 0 && st.subcircuit_connectivity_map_key) {
      const schematicPortId = connected[0];
      const sourcePortId = schPortIdToSourcePortId.get(schematicPortId);
      if (sourcePortId) {
        const connKey = st.subcircuit_connectivity_map_key;
        const connectedSourcePortIdsForKey = Array.from(schematicPortIdsInScope).map((portId) => schPortIdToSourcePortId.get(portId)).filter((sourcePortId2) => {
          if (!sourcePortId2) return false;
          return db.source_port.get(sourcePortId2)?.subcircuit_connectivity_map_key === connKey;
        });
        const { text } = resolveNetLabelForPortMissingTrace({
          group,
          sourcePortId,
          connectedSourcePortIdsForKey,
          connKey,
          sourceNet: connKeyToSourceNet.get(connKey)
        });
        if (text && !crossSubcircuitTraceLabelTextBySchematicPortId.has(schematicPortId)) {
          crossSubcircuitTraceLabelTextBySchematicPortId.set(
            schematicPortId,
            text
          );
          userNetIdToConnKey.set(text, connKey);
          singlePortTraceNetConnections.push({
            netId: text,
            schematicPortIds: [schematicPortId],
            netLabelWidth: Number(
              getSchematicNetLabelTextWidth({ text }).toFixed(2)
            )
          });
        }
      }
    } else if (connected.length >= 2) {
      const traceLabel = st.name ?? st.display_name;
      const userNetId = traceLabel ?? st.source_trace_id;
      if (st.subcircuit_connectivity_map_key) {
        userNetIdToConnKey.set(userNetId, st.subcircuit_connectivity_map_key);
      }
      const maxMspDist = group._parsedProps.schMaxTraceDistance ?? DEFAULT_MAX_MSP_PAIR_DISTANCE;
      const portsForConnection = connected.map(
        (schematicPortId) => componentPortBySchematicPortId.get(schematicPortId)
      ).filter((port) => Boolean(port));
      const renderedNetLabelText = st.subcircuit_connectivity_map_key ? resolveCanonicalNetLabelText({
        subcircuitConnectivityMapKey: st.subcircuit_connectivity_map_key
      }).name : getNetNameFromPorts(portsForConnection).name || traceLabel;
      const shouldRenderNetLabels = connected.slice(1).some((b, index) => {
        const a = connected[index];
        const portA = db.schematic_port.get(a);
        const portB = db.schematic_port.get(b);
        if (!portA || !portB) return false;
        return Math.sqrt(
          (portA.center.x - portB.center.x) ** 2 + (portA.center.y - portB.center.y) ** 2
        ) > maxMspDist;
      });
      const netLabelWidth = renderedNetLabelText && shouldRenderNetLabels ? Number(
        getSchematicNetLabelTextWidth({
          text: renderedNetLabelText
        }).toFixed(2)
      ) : void 0;
      for (let i = 0; i < connected.length - 1; i++) {
        const a = connected[i];
        const b = connected[i + 1];
        const pairKey = [a, b].sort().join("::");
        if (connectedPairKeys.has(pairKey)) continue;
        connectedPairKeys.add(pairKey);
        sourceTraceIdByPinPairKey.set(pairKey, st.source_trace_id);
        directConnections.push({
          schematicPortIds: [a, b],
          netId: userNetId,
          netLabelWidth,
          connKey: st.subcircuit_connectivity_map_key
        });
      }
    }
  }
  const netConnections = [...singlePortTraceNetConnections];
  const connKeyToSchematicPortIds = /* @__PURE__ */ new Map();
  for (const [schId, srcPortId] of schPortIdToSourcePortId) {
    if (schematicPortIdsWithExternallyRoutedRepresentations.has(schId)) continue;
    const sp = db.source_port.get(srcPortId);
    if (!sp?.subcircuit_connectivity_map_key) continue;
    const connKey = sp.subcircuit_connectivity_map_key;
    if (!connKeyToSchematicPortIds.has(connKey)) {
      connKeyToSchematicPortIds.set(connKey, []);
    }
    connKeyToSchematicPortIds.get(connKey).push(schId);
  }
  applyInlineNetLabelEligibility({
    directConnections,
    connKeyToSchematicPortIds,
    connKeyToSourceNet,
    resolveCanonicalNetLabelText
  });
  for (const [connKey, schematicPortIds] of connKeyToSchematicPortIds) {
    const sourceNet = connKeyToSourceNet.get(connKey);
    if (sourceNet && schematicPortIds.length >= 1) {
      const seenRoutedSchematicPortIds = /* @__PURE__ */ new Set();
      const uniqueSchematicPortIds = schematicPortIds.filter(
        (schematicPortId) => {
          const componentPort = componentPortBySchematicPortId.get(schematicPortId);
          const routedSchematicPortIdValue = componentPort ? getPortForSchematicSymbolPort(componentPort).schematic_port_id : null;
          const routedSchematicPortId = routedSchematicPortIdValue ? asSchematicPortId(routedSchematicPortIdValue) : schematicPortId;
          if (seenRoutedSchematicPortIds.has(routedSchematicPortId)) {
            return false;
          }
          seenRoutedSchematicPortIds.add(routedSchematicPortId);
          return true;
        }
      );
      const userNetId = String(
        sourceNet.name || sourceNet.source_net_id || connKey
      );
      userNetIdToConnKey.set(userNetId, connKey);
      const netLabelTextWidth = Number(
        getSchematicNetLabelTextWidth({ text: String(userNetId) }).toFixed(2)
      );
      let netLabelWidth = netLabelTextWidth;
      let netLabelHeight;
      if (sourceNet.is_ground || sourceNet.is_power) {
        netLabelWidth = SCHEMATIC_RAIL_NET_LABEL_HEIGHT;
        netLabelHeight = netLabelTextWidth;
      }
      netConnections.push({
        netId: userNetId,
        schematicPortIds: uniqueSchematicPortIds,
        netLabelWidth,
        netLabelHeight
      });
    }
  }
  const availableNetLabelOrientations = (() => {
    const netToAllowedOrientations = {};
    const presentNetIds = new Set(netConnections.map((nc) => nc.netId));
    for (const net of db.source_net.list().filter(
      (n) => !n.subcircuit_id || allowedSubcircuitIds.has(n.subcircuit_id)
    )) {
      if (!net.name) continue;
      if (!presentNetIds.has(net.name)) continue;
      if (net.is_ground) {
        netToAllowedOrientations[net.name] = ["y-"];
      } else if (net.is_power) {
        netToAllowedOrientations[net.name] = ["y+"];
      } else {
        netToAllowedOrientations[net.name] = ["x-", "x+"];
      }
    }
    for (const { netId } of singlePortTraceNetConnections) {
      netToAllowedOrientations[netId] ??= ["x-", "x+"];
    }
    return netToAllowedOrientations;
  })();
  const inputProblem = {
    chips,
    directConnections: directConnections.map(
      ({ schematicPortIds, connKey, ...connection }) => ({
        ...connection,
        pinIds: schematicPortIds
      })
    ),
    netConnections: netConnections.map(
      ({ schematicPortIds, ...connection }) => ({
        ...connection,
        pinIds: schematicPortIds
      })
    ),
    textBoxes,
    availableNetLabelOrientations,
    maxMspPairDistance: group._parsedProps.schMaxTraceDistance ?? DEFAULT_MAX_MSP_PAIR_DISTANCE
  };
  return {
    inputProblem,
    connKeyToSourceNet,
    userNetIdToConnKey,
    crossSubcircuitTraceLabelTextBySchematicPortId,
    connKeysWithExplicitPortNetTraces,
    schematicPortIdsInScope,
    schematicPortIdsWithExternallyRoutedRepresentations,
    schPortIdToSourcePortId,
    netLabelsInScope,
    sourceTraceIdByPinPairKey
  };
}

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/getSchematicPortIdsWithAssignedNetLabels.ts
var getSchematicPortIdsWithAssignedNetLabels = (netLabels) => {
  const schematicPortIdsWithNetLabels = /* @__PURE__ */ new Set();
  for (const netLabel of netLabels) {
    const { schX, schY } = netLabel._parsedProps;
    if (schX === void 0 && schY === void 0) continue;
    const netLabelPorts = netLabel._getConnectedPorts();
    for (const port of netLabelPorts) {
      if (!port.schematic_port_id) continue;
      schematicPortIdsWithNetLabels.add(
        asSchematicPortId(port.schematic_port_id)
      );
    }
  }
  return schematicPortIdsWithNetLabels;
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/getSchematicPortIdsWithRoutedTraces.ts
var getSchematicPortIdsWithRoutedTraces = ({
  solver
}) => {
  const solvedTraces = solver.traceCleanupSolver?.getOutput().traces ?? solver.traceLabelOverlapAvoidanceSolver?.getOutput().traces ?? solver.schematicTraceLinesSolver?.solvedTracePaths ?? [];
  const schematicPortIdsWithRoutedTraces = /* @__PURE__ */ new Set();
  for (const solvedTrace of solvedTraces) {
    const points = solvedTrace?.tracePath;
    if (!Array.isArray(points) || points.length < 2) continue;
    const pinIds = Array.isArray(solvedTrace.pins) ? solvedTrace.pins.map((pin) => pin.pinId) : solvedTrace.pinIds;
    for (const pinId of pinIds) {
      const schPortId = asSchematicPortId(pinId);
      if (schPortId) {
        schematicPortIdsWithRoutedTraces.add(schPortId);
      }
    }
  }
  return schematicPortIdsWithRoutedTraces;
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/insertNetLabelsForPortsMissingTrace.ts
var NEAR_EXISTING_NET_LABEL_DISTANCE = 0.5;
var SAME_ANCHOR_POSITION_DISTANCE = 0.1;
var doesSchematicNetLabelRepresentCurrentSourceConnection = (args) => {
  const { nl, connKey, sourceNet, text } = args;
  if (sourceNet?.source_net_id && nl.source_net_id) {
    return nl.source_net_id === sourceNet.source_net_id;
  }
  if (nl.source_net_id) {
    return nl.source_net_id === connKey;
  }
  return nl.text === text;
};
var insertNetLabelsForPortsMissingTrace = ({
  schematicPortIdsInScope,
  schematicPortIdsWithExternallyRoutedRepresentations,
  group,
  schPortIdToSourcePortId,
  connKeyToSourceNet
}) => {
  const { db } = group.root;
  for (const schematicPortId of schematicPortIdsInScope) {
    if (schematicPortIdsWithExternallyRoutedRepresentations.has(schematicPortId))
      continue;
    const schPort = db.schematic_port.get(schematicPortId);
    if (!schPort) continue;
    if (schPort.is_connected) continue;
    const srcPortId = schPortIdToSourcePortId.get(schematicPortId);
    if (!srcPortId) continue;
    const sourcePort = db.source_port.get(srcPortId);
    if (group._parsedProps.showAsSchematicBox && sourcePort?.subcircuit_id === group.subcircuit_id && !sourcePort.source_component_id && getDirectCrossSubcircuitConnectedSourcePortId(db, srcPortId)) {
      continue;
    }
    const connKey = sourcePort?.subcircuit_connectivity_map_key;
    if (!connKey) continue;
    const sourceNet = connKeyToSourceNet.get(connKey);
    const schComponent = schPort.schematic_component_id ? db.schematic_component.get(schPort.schematic_component_id) : void 0;
    const portSchematicSheetId = schComponent?.schematic_sheet_id ?? group._resolveSchematicSheetId();
    const connectedSourcePortIdsForKey = Array.from(schematicPortIdsInScope).map((portId) => schPortIdToSourcePortId.get(portId)).filter((sourcePortId) => {
      if (!sourcePortId) return false;
      return db.source_port.get(sourcePortId)?.subcircuit_connectivity_map_key === connKey;
    });
    const {
      text,
      wasAssignedDisplayLabel,
      directCrossSubcircuitConnectedSourcePortId
    } = resolveNetLabelForPortMissingTrace({
      group,
      sourcePortId: srcPortId,
      connectedSourcePortIdsForKey,
      connKey,
      sourceNet
    });
    const directCrossSubcircuitPortAlsoNeedsNetLabel = directCrossSubcircuitConnectedSourcePortId ? db.schematic_port.list({
      source_port_id: directCrossSubcircuitConnectedSourcePortId
    }).some((port) => !port.is_connected) : false;
    const shouldPreserveDirectCrossSubcircuitEndpointLabels = directCrossSubcircuitPortAlsoNeedsNetLabel && !wasAssignedDisplayLabel;
    const isCollapsedBoxPort = schComponent?.is_box_with_pins && sourcePort && !sourcePort.source_component_id;
    const hasExternalTrace = db.source_trace.list().some(
      (trace) => trace.connected_source_port_ids.includes(srcPortId) && trace.subcircuit_id !== sourcePort?.subcircuit_id
    );
    const hasExplicitLabel = Boolean(sourceNet || wasAssignedDisplayLabel);
    if (isCollapsedBoxPort && !hasExternalTrace && !hasExplicitLabel) continue;
    const connectedPortCountForKey = Array.from(schematicPortIdsInScope).filter(
      (portId) => {
        const sourcePortId = schPortIdToSourcePortId.get(portId);
        if (!sourcePortId) return false;
        return db.source_port.get(sourcePortId)?.subcircuit_connectivity_map_key === connKey;
      }
    ).length;
    const isGndNet = sourceNet?.is_ground ?? false;
    const isPowerNet = !isGndNet && (sourceNet?.is_power ?? false);
    let side;
    if (isGndNet) {
      side = "top";
    } else if (isPowerNet) {
      side = "bottom";
    } else {
      side = getEnteringEdgeFromDirection(
        schPort.facing_direction || "right"
      ) || "right";
    }
    const center = computeSchematicNetLabelCenter({
      anchor_position: schPort.center,
      anchor_side: side,
      text
    });
    const existingNetLabelForCurrentSourceConnection = db.schematic_net_label.list().find((nl) => {
      if (nl.schematic_sheet_id !== portSchematicSheetId) return false;
      return doesSchematicNetLabelRepresentCurrentSourceConnection({
        nl,
        connKey,
        sourceNet,
        text
      });
    });
    if (existingNetLabelForCurrentSourceConnection && connectedPortCountForKey <= 1 && !shouldPreserveDirectCrossSubcircuitEndpointLabels) {
      db.schematic_net_label.update(
        existingNetLabelForCurrentSourceConnection.schematic_net_label_id,
        {
          text,
          anchor_position: schPort.center,
          center,
          anchor_side: side
        }
      );
      continue;
    }
    if (existingNetLabelForCurrentSourceConnection) {
      const dx = existingNetLabelForCurrentSourceConnection.anchor_position.x - schPort.center.x;
      const dy = existingNetLabelForCurrentSourceConnection.anchor_position.y - schPort.center.y;
      const labelIsNearPort = dx * dx + dy * dy < NEAR_EXISTING_NET_LABEL_DISTANCE * NEAR_EXISTING_NET_LABEL_DISTANCE;
      if (labelIsNearPort) {
        if (isGndNet) {
          const anchor_side = existingNetLabelForCurrentSourceConnection.anchor_side ?? side;
          db.schematic_net_label.update(
            existingNetLabelForCurrentSourceConnection.schematic_net_label_id,
            {
              text,
              anchor_position: schPort.center,
              center: computeSchematicNetLabelCenter({
                anchor_position: schPort.center,
                anchor_side,
                text
              }),
              anchor_side
            }
          );
        } else if (!isPowerNet && existingNetLabelForCurrentSourceConnection.text === text) {
          const anchor_side = existingNetLabelForCurrentSourceConnection.anchor_side ?? side;
          db.schematic_net_label.update(
            existingNetLabelForCurrentSourceConnection.schematic_net_label_id,
            {
              text,
              anchor_position: schPort.center,
              center: computeSchematicNetLabelCenter({
                anchor_position: schPort.center,
                anchor_side,
                text
              }),
              anchor_side
            }
          );
        }
        continue;
      }
      const existingAtPort = db.schematic_net_label.list().some((nl) => {
        if (nl.schematic_sheet_id !== portSchematicSheetId) return false;
        const dx2 = nl.anchor_position.x - schPort.center.x;
        const dy2 = nl.anchor_position.y - schPort.center.y;
        if (dx2 * dx2 + dy2 * dy2 >= SAME_ANCHOR_POSITION_DISTANCE * SAME_ANCHOR_POSITION_DISTANCE) {
          return false;
        }
        return doesSchematicNetLabelRepresentCurrentSourceConnection({
          nl,
          connKey,
          sourceNet,
          text
        });
      });
      if (existingAtPort) continue;
    }
    if (!sourceNet && !shouldPreserveDirectCrossSubcircuitEndpointLabels) {
      for (const nl of db.schematic_net_label.list()) {
        if (nl.source_net_id !== connKey) continue;
        if (nl.schematic_sheet_id !== portSchematicSheetId) continue;
        const isAttachedToConnectedPort = connectedSourcePortIdsForKey.some(
          (sourcePortId) => {
            const schPortId = Array.from(
              schPortIdToSourcePortId.entries()
            ).find(([, id]) => id === sourcePortId)?.[0];
            let connectedSchPort;
            if (schPortId) {
              connectedSchPort = db.schematic_port.get(schPortId);
            }
            if (!connectedSchPort?.center || !nl.anchor_position) return false;
            const dx = nl.anchor_position.x - connectedSchPort.center.x;
            const dy = nl.anchor_position.y - connectedSchPort.center.y;
            return dx * dx + dy * dy < SAME_ANCHOR_POSITION_DISTANCE * SAME_ANCHOR_POSITION_DISTANCE;
          }
        );
        if (!isAttachedToConnectedPort) {
          db.schematic_net_label.delete(nl.schematic_net_label_id);
        }
      }
    }
    const netLabel = {
      text,
      source_net_id: sourceNet?.source_net_id ?? connKey,
      anchor_position: schPort.center,
      center,
      anchor_side: side,
      schematic_sheet_id: portSchematicSheetId
    };
    db.schematic_net_label.insert(netLabel);
  }
};

// lib/components/primitive-components/Group/Group_doInitialSchematicTraceRender/Group_doInitialSchematicTraceRender.ts
var debug11 = Debug13("Group_doInitialSchematicTraceRender");
var renderSchematicTracesForSheet = ({
  group,
  schematicSheetId,
  netLabels
}) => {
  const {
    inputProblem,
    connKeyToSourceNet,
    schematicPortIdsInScope,
    schematicPortIdsWithExternallyRoutedRepresentations,
    schPortIdToSourcePortId,
    userNetIdToConnKey,
    crossSubcircuitTraceLabelTextBySchematicPortId,
    connKeysWithExplicitPortNetTraces,
    netLabelsInScope,
    sourceTraceIdByPinPairKey
  } = createSchematicTraceSolverInputProblem(group, {
    schematicSheetId,
    netLabels
  });
  if (inputProblem.chips.length === 0) return;
  const schematicPortIdsWithPreExistingNetLabels = getSchematicPortIdsWithAssignedNetLabels(netLabelsInScope);
  const hasRouteableSchematicConnections = inputProblem.directConnections.length > 0 || inputProblem.netConnections.length > 0;
  if (!hasRouteableSchematicConnections) {
    insertNetLabelsForPortsMissingTrace({
      group,
      schematicPortIdsInScope,
      schematicPortIdsWithExternallyRoutedRepresentations,
      schPortIdToSourcePortId,
      connKeyToSourceNet
    });
    return;
  }
  if (debug11.enabled) {
    group.root?.emit("debug:logOutput", {
      type: "debug:logOutput",
      name: "group-trace-render-input-problem",
      content: JSON.stringify(inputProblem, null, 2)
    });
  }
  const solver = new SchematicTracePipelineSolver4(inputProblem);
  const solverConstructorArgs = solver.getConstructorParams();
  group.root?.emit("solver:started", {
    type: "solver:started",
    solverName: "SchematicTracePipelineSolver",
    // Keep the legacy field as the input problem while also exposing the exact
    // [inputProblem, opts?] tuple needed to reconstruct the solver.
    solverParams: solverConstructorArgs[0],
    solverConstructorArgs,
    componentName: group.getString()
  });
  solver.solve();
  const schematicPortIdsWithRoutedTraces = getSchematicPortIdsWithRoutedTraces({
    solver
  });
  applyTracesFromSolverOutput({
    group,
    solver,
    userNetIdToConnKey,
    schematicPortIdsWithPreExistingNetLabels
  });
  applyNetLabelPlacements({
    group,
    solver,
    connKeyToSourceNet,
    userNetIdToConnKey,
    crossSubcircuitTraceLabelTextBySchematicPortId,
    connKeysWithExplicitPortNetTraces,
    schematicPortIdsWithPreExistingNetLabels,
    schematicPortIdsWithRoutedTraces,
    netLabels: netLabelsInScope
  });
  applyInlineNetLabelPlacements({
    group,
    solver,
    userNetIdToConnKey,
    sourceTraceIdByPinPairKey
  });
  insertNetLabelsForPortsMissingTrace({
    group,
    schematicPortIdsInScope,
    schematicPortIdsWithExternallyRoutedRepresentations,
    schPortIdToSourcePortId,
    connKeyToSourceNet
  });
};
var Group_doInitialSchematicTraceRender = (group) => {
  if (!group.root?._featureMspSchematicTraceRouting) return;
  if (!group.isSubcircuit) return;
  if (group.root?.schematicDisabled) return;
  const netLabels = /* @__PURE__ */ new Set();
  const board = group._getBoard();
  let schematicScope = group;
  while (schematicScope) {
    if (schematicScope instanceof Group5) {
      for (const netLabel of schematicScope.selectAll("netlabel")) {
        netLabels.add(netLabel);
      }
    }
    if (schematicScope === board) break;
    schematicScope = schematicScope.parent;
  }
  const netLabelComponents = [...netLabels];
  const schematicGroupIds = new Set(
    [
      group,
      ...group.getDescendants().filter(
        (component) => component instanceof Group5
      )
    ].map((schematicGroup) => schematicGroup.schematic_group_id).filter(
      (schematicGroupId) => Boolean(schematicGroupId)
    )
  );
  const schematicSheetIds = new Set(
    group.root.db.schematic_component.list().filter(
      (schematicComponent) => schematicGroupIds.has(schematicComponent.schematic_group_id)
    ).map((schematicComponent) => schematicComponent.schematic_sheet_id).filter(
      (schematicSheetId) => Boolean(schematicSheetId)
    )
  );
  if (schematicSheetIds.size === 0) {
    renderSchematicTracesForSheet({ group, netLabels: netLabelComponents });
    return;
  }
  for (const schematicSheetId of schematicSheetIds) {
    renderSchematicTracesForSheet({
      group,
      schematicSheetId,
      netLabels: netLabelComponents
    });
  }
};

// lib/components/primitive-components/Group/Group_doInitialSimulationSpiceEngineRender.ts
import Debug14 from "debug";

// lib/utils/simulation/get-transient-voltage-graph-names-from-spice-netlist.ts
function getTransientVoltageGraphNamesFromSpiceNetlist(spiceNetlist) {
  const graphNames = [];
  for (const printStatement of spiceNetlist.printStatements) {
    const match = printStatement.match(/^\.PRINT\s+TRAN\s+(.+)$/i);
    if (!match) continue;
    for (const voltageExpression of match[1].match(/V\(([^)]+)\)/g) ?? []) {
      const nodeList = voltageExpression.slice(2, -1);
      graphNames.push(nodeList.replace(",", "-"));
    }
  }
  return graphNames;
}

// lib/utils/simulation/getSimulationColorForId.ts
var SIMULATION_COLOR_PALETTE = [
  // Start with the colors users most commonly expect from circuit sims.
  "rgb(0, 150, 0)",
  "rgb(0, 0, 220)",
  "rgb(220, 0, 0)",
  "rgb(0, 160, 160)",
  "rgb(180, 0, 180)",
  "rgb(170, 140, 0)",
  "rgb(220, 120, 0)",
  "rgb(128, 128, 128)",
  "rgb(100, 0, 180)",
  "rgb(140, 70, 0)",
  "rgb(220, 80, 120)",
  "rgb(0, 120, 220)",
  "rgb(0, 120, 120)",
  "rgb(120, 0, 0)",
  "rgb(0, 0, 132)",
  "rgb(80, 80, 80)"
];
var idToColorMap = /* @__PURE__ */ new Map();
var colorIndex = 0;
function getSimulationColorForId(id) {
  if (idToColorMap.has(id)) {
    const color2 = idToColorMap.get(id);
    return color2;
  }
  const color = SIMULATION_COLOR_PALETTE[colorIndex];
  colorIndex = (colorIndex + 1) % SIMULATION_COLOR_PALETTE.length;
  idToColorMap.set(id, color);
  return color;
}
function resetSimulationColorState() {
  idToColorMap.clear();
  colorIndex = 0;
}

// lib/spice/get-spicey-engine.ts
import { simulate, spiceyTranToVGraphs } from "spicey";
var getSpiceyEngine = () => {
  return {
    async simulate(spiceString) {
      const simulation_experiment_id = "spice-experiment-1";
      const { circuit: parsedCircuit, tran } = simulate(spiceString);
      const voltageGraphs = spiceyTranToVGraphs(
        tran,
        parsedCircuit,
        simulation_experiment_id
      );
      return {
        simulationResultCircuitJson: voltageGraphs
      };
    }
  };
};

// lib/components/primitive-components/Group/isCurrentGraph.ts
var isCurrentGraph = (element) => element.type === "simulation_transient_current_graph";

// lib/components/primitive-components/Group/isSimulationCurrentResult.ts
var isSimulationCurrentResult = (circuitElement) => circuitElement.type === "simulation_dc_operating_point_current" || circuitElement.type === "simulation_dc_sweep_current_graph" || circuitElement.type === "simulation_ac_sweep_current_graph";

// lib/components/primitive-components/Group/isSimulationVoltageResult.ts
var isSimulationVoltageResult = (circuitElement) => circuitElement.type === "simulation_dc_operating_point_voltage" || circuitElement.type === "simulation_dc_sweep_voltage_graph" || circuitElement.type === "simulation_ac_sweep_voltage_graph";

// lib/components/primitive-components/Group/isVoltageGraph.ts
var isVoltageGraph = (element) => element.type === "simulation_transient_voltage_graph";

// lib/components/primitive-components/Group/isSimulationAnalysisResult.ts
var isSimulationAnalysisResult = (circuitElement) => isVoltageGraph(circuitElement) || isCurrentGraph(circuitElement) || isSimulationVoltageResult(circuitElement) || isSimulationCurrentResult(circuitElement);

// lib/components/primitive-components/Group/attachSweepCoordinateToSimulationResult.ts
var attachSweepCoordinateToSimulationResult = ({
  simulationResult,
  simulationParameterSweepCoordinate
}) => {
  if (!simulationParameterSweepCoordinate || !isSimulationAnalysisResult(simulationResult)) {
    return simulationResult;
  }
  const simulationResultWithSweepCoordinate = { ...simulationResult };
  const simulationResultIdKey = `${simulationResultWithSweepCoordinate.type}_id`;
  Reflect.deleteProperty(
    simulationResultWithSweepCoordinate,
    simulationResultIdKey
  );
  simulationResultWithSweepCoordinate.simulation_parameter_sweep_coordinate = simulationParameterSweepCoordinate;
  return simulationResultWithSweepCoordinate;
};

// lib/components/primitive-components/Group/createSimulationRuns.ts
import { circuitJsonToSpice } from "circuit-json-to-spice";

// lib/components/primitive-components/Group/applyParameterSweepCoordinate.ts
var getSourceComponentOrThrow = ({
  circuitJson,
  sourceComponentId,
  isMatchingSourceComponent
}) => {
  const sourceComponent = circuitJson.find(
    (circuitElement) => isMatchingSourceComponent(circuitElement) && circuitElement.source_component_id === sourceComponentId
  );
  if (!sourceComponent) {
    throw new Error(`Parameter sweep target ${sourceComponentId} was not found`);
  }
  return sourceComponent;
};
var applyParameterSweepCoordinate = ({
  circuitJson,
  parameterSweep,
  simulationParameterSweepCoordinate
}) => {
  const sweptCircuitJson = structuredClone(circuitJson);
  const parameterValue = simulationParameterSweepCoordinate.parameter_value;
  if (parameterSweep.parameter_type === "resistance") {
    const resistor = getSourceComponentOrThrow({
      circuitJson: sweptCircuitJson,
      sourceComponentId: parameterSweep.resistor_source_component_id,
      isMatchingSourceComponent: (circuitElement) => circuitElement.type === "source_component" && circuitElement.ftype === "simple_resistor"
    });
    resistor.resistance = parameterValue;
    return sweptCircuitJson;
  }
  if (parameterSweep.parameter_type === "capacitance") {
    const capacitor = getSourceComponentOrThrow({
      circuitJson: sweptCircuitJson,
      sourceComponentId: parameterSweep.capacitor_source_component_id,
      isMatchingSourceComponent: (circuitElement) => circuitElement.type === "source_component" && circuitElement.ftype === "simple_capacitor"
    });
    capacitor.capacitance = parameterValue;
    return sweptCircuitJson;
  }
  if (parameterSweep.parameter_type === "inductance") {
    const inductor = getSourceComponentOrThrow({
      circuitJson: sweptCircuitJson,
      sourceComponentId: parameterSweep.inductor_source_component_id,
      isMatchingSourceComponent: (circuitElement) => circuitElement.type === "source_component" && circuitElement.ftype === "simple_inductor"
    });
    inductor.inductance = parameterValue;
    return sweptCircuitJson;
  }
  if (parameterSweep.parameter_type === "voltage") {
    const groundNet = sweptCircuitJson.find(
      (circuitElement) => circuitElement.type === "source_net" && circuitElement.name.toUpperCase() === "GND"
    );
    if (!groundNet || groundNet.type !== "source_net") {
      throw new Error("A voltage parameter sweep requires a GND net");
    }
    const sweepVoltageSource = {
      type: "simulation_voltage_source",
      simulation_voltage_source_id: `simulation_voltage_source_${simulationParameterSweepCoordinate.simulation_parameter_sweep_id}_${simulationParameterSweepCoordinate.sweep_index}`,
      is_dc_source: true,
      positive_source_net_id: parameterSweep.source_net_id,
      negative_source_net_id: groundNet.source_net_id,
      voltage: parameterValue
    };
    sweptCircuitJson.push(sweepVoltageSource);
    return sweptCircuitJson;
  }
  if (parameterSweep.parameter_type === "current") {
    const currentSourcePortIds = new Set(
      sweptCircuitJson.filter(
        (circuitElement) => circuitElement.type === "source_port" && circuitElement.source_component_id === parameterSweep.current_source_component_id
      ).map((sourcePort) => sourcePort.source_port_id)
    );
    const currentSource = sweptCircuitJson.find(
      (circuitElement) => circuitElement.type === "simulation_current_source" && (circuitElement.is_dc_source ? Boolean(
        circuitElement.positive_source_port_id && currentSourcePortIds.has(
          circuitElement.positive_source_port_id
        )
      ) : Boolean(
        circuitElement.terminal1_source_port_id && currentSourcePortIds.has(
          circuitElement.terminal1_source_port_id
        )
      ))
    );
    if (!currentSource) {
      throw new Error(
        `Current sweep target ${parameterSweep.current_source_component_id} has no simulation source`
      );
    }
    currentSource.current = parameterValue;
    return sweptCircuitJson;
  }
  throw new Error("Parameter sweep target is incomplete");
};

// lib/components/primitive-components/Group/createSimulationRuns.ts
var createSimulationRun = ({
  circuitJson,
  simulationParameterSweepCoordinate
}) => {
  const spiceNetlist = circuitJsonToSpice(circuitJson);
  return {
    spiceNetlist,
    spiceString: spiceNetlist.toSpiceString(),
    simulationParameterSweepCoordinate
  };
};
var createSimulationRuns = ({
  circuitJson,
  simulationExperimentId
}) => {
  const parameterSweep = circuitJson.find(
    (circuitElement) => circuitElement.type === "simulation_parameter_sweep" && circuitElement.simulation_experiment_id === simulationExperimentId
  );
  if (!parameterSweep) {
    return [createSimulationRun({ circuitJson })];
  }
  return parameterSweep.parameter_values.map((parameterValue, sweepIndex) => {
    const simulationParameterSweepCoordinate = {
      simulation_parameter_sweep_id: parameterSweep.simulation_parameter_sweep_id,
      sweep_index: sweepIndex,
      parameter_value: parameterValue,
      parameter_unit: parameterSweep.parameter_unit
    };
    return createSimulationRun({
      circuitJson: applyParameterSweepCoordinate({
        circuitJson,
        parameterSweep,
        simulationParameterSweepCoordinate
      }),
      simulationParameterSweepCoordinate
    });
  });
};

// lib/utils/simulation/parseSimulationGraphValue.ts
var SI_PREFIX_MULTIPLIERS = {
  p: 1e-12,
  n: 1e-9,
  u: 1e-6,
  \u00B5: 1e-6,
  m: 1e-3,
  "": 1,
  k: 1e3,
  K: 1e3,
  M: 1e6,
  G: 1e9
};
var parseSimulationGraphValue = (value) => {
  if (value === void 0) return void 0;
  if (typeof value === "number") return value;
  const trimmed = value.trim();
  const match = trimmed.match(
    /^([+-]?(?:\d+(?:\.\d*)?|\.\d+))\s*([pnumµkKMG]?)/
  );
  if (!match) return void 0;
  const numericValue = Number(match[1]);
  if (!Number.isFinite(numericValue)) return void 0;
  return numericValue * (SI_PREFIX_MULTIPLIERS[match[2] ?? ""] ?? 1);
};

// lib/components/primitive-components/Group/getGraphDisplayOverrides.ts
var getVoltageProbeGraphDisplayOverrides = (probe) => {
  const {
    graphDisplayName,
    graphCenter,
    graphVerticalOffset,
    graphVoltagePerDiv
  } = probe._parsedProps;
  return {
    displayName: graphDisplayName,
    color: probe.color ?? void 0,
    center: graphCenter,
    verticalOffset: parseSimulationGraphValue(graphVerticalOffset),
    valuePerDiv: parseSimulationGraphValue(graphVoltagePerDiv)
  };
};
var getAmmeterGraphDisplayOverrides = (ammeter) => {
  const {
    color,
    graphDisplayName,
    graphCenter,
    graphVerticalOffset,
    graphCurrentPerDiv
  } = ammeter._parsedProps;
  return {
    displayName: graphDisplayName,
    color,
    center: graphCenter,
    verticalOffset: parseSimulationGraphValue(graphVerticalOffset),
    valuePerDiv: parseSimulationGraphValue(graphCurrentPerDiv)
  };
};

// lib/components/primitive-components/Group/getFiniteBounds.ts
var getFiniteBounds = (values) => {
  let min = Number.POSITIVE_INFINITY;
  let max = Number.NEGATIVE_INFINITY;
  for (const value of values) {
    if (!Number.isFinite(value)) continue;
    min = Math.min(min, value);
    max = Math.max(max, value);
  }
  if (min === Number.POSITIVE_INFINITY || max === Number.NEGATIVE_INFINITY) {
    return null;
  }
  return { min, max };
};

// lib/components/primitive-components/Group/getGraphLevels.ts
var getGraphLevels = (graph) => graph.type === "simulation_transient_current_graph" ? graph.current_levels : graph.voltage_levels;

// lib/components/primitive-components/Group/getAutoDisplayScale.ts
var DISPLAY_SECTION_HEIGHT_DIVS = 2;
var DISPLAY_SECTION_DATA_FILL = 0.8;
var getAutoDisplayScale = ({
  graph,
  index,
  graphCount
}) => {
  const bounds = getFiniteBounds(getGraphLevels(graph));
  if (!bounds) return null;
  const center = (bounds.min + bounds.max) / 2;
  const span = bounds.max - bounds.min;
  const valuePerDiv = span > 0 ? span / (DISPLAY_SECTION_HEIGHT_DIVS * DISPLAY_SECTION_DATA_FILL) : Math.max(Math.abs(center) / 10, Number.EPSILON);
  return {
    display_center_value: center,
    display_center_offset_divs: ((graphCount - 1) / 2 - index) * DISPLAY_SECTION_HEIGHT_DIVS,
    value_per_div: valuePerDiv
  };
};

// lib/components/primitive-components/Group/insertIndependentAxisScopeTraces.ts
var isDefinedString = (value) => value !== void 0;
var getTraceByVoltageProbeId = (traces) => {
  const traceByVoltageProbeId = /* @__PURE__ */ new Map();
  for (const trace of traces) {
    if (trace.simulation_voltage_probe_id) {
      traceByVoltageProbeId.set(trace.simulation_voltage_probe_id, trace);
    }
  }
  return traceByVoltageProbeId;
};
var getTraceByCurrentProbeId = (traces) => {
  const traceByCurrentProbeId = /* @__PURE__ */ new Map();
  for (const trace of traces) {
    if (trace.simulation_current_probe_id) {
      traceByCurrentProbeId.set(trace.simulation_current_probe_id, trace);
    }
  }
  return traceByCurrentProbeId;
};
var insertIndependentAxisScopeTraces = ({
  db,
  graphs,
  graphDisplayOverridesByProbeId
}) => {
  if (graphs.length === 0) return;
  const existingScopeTraces = db.simulation_oscilloscope_trace.list();
  const traceByVoltageProbeId = getTraceByVoltageProbeId(existingScopeTraces);
  const traceByCurrentProbeId = getTraceByCurrentProbeId(existingScopeTraces);
  const graphIdsWithTrace = new Set(
    existingScopeTraces.flatMap((trace) => [
      trace.simulation_transient_voltage_graph_id,
      trace.simulation_transient_current_graph_id
    ]).filter(isDefinedString)
  );
  for (const [index, { type, graph }] of graphs.entries()) {
    const scale2 = getAutoDisplayScale({
      graph,
      index,
      graphCount: graphs.length
    });
    if (!scale2) continue;
    const graphId = type === "voltage" ? graph.simulation_transient_voltage_graph_id : graph.simulation_transient_current_graph_id;
    if (graphIdsWithTrace.has(graphId)) continue;
    const sourceProbeId = graph.source_probe_id ?? void 0;
    const probeTrace = sourceProbeId === void 0 ? void 0 : type === "voltage" ? traceByVoltageProbeId.get(sourceProbeId) : traceByCurrentProbeId.get(sourceProbeId);
    const overrides = sourceProbeId === void 0 ? void 0 : graphDisplayOverridesByProbeId?.get(sourceProbeId);
    const valuePerDiv = overrides?.valuePerDiv ?? scale2.value_per_div;
    const displayCenter = overrides?.center ?? scale2.display_center_value;
    const displayCenterOffsetDivs = overrides?.verticalOffset !== void 0 && valuePerDiv !== 0 ? overrides.verticalOffset / valuePerDiv : probeTrace?.display_center_offset_divs ?? scale2.display_center_offset_divs;
    db.simulation_oscilloscope_trace.insert({
      ...type === "voltage" ? {
        simulation_transient_voltage_graph_id: graph.simulation_transient_voltage_graph_id,
        volts_per_div: valuePerDiv
      } : {
        simulation_transient_current_graph_id: graph.simulation_transient_current_graph_id,
        amps_per_div: valuePerDiv
      },
      display_name: overrides?.displayName ?? probeTrace?.display_name,
      color: overrides?.color ?? probeTrace?.color ?? graph.color,
      display_center_value: displayCenter,
      display_center_offset_divs: displayCenterOffsetDivs
    });
  }
};

// lib/components/primitive-components/Group/isCircuitElementInput.ts
var isCircuitElementInput = (element) => Boolean(element && typeof element === "object" && "type" in element);

// lib/components/primitive-components/Group/Group_doInitialSimulationSpiceEngineRender.ts
var debug12 = Debug14("tscircuit:core:Group_doInitialSimulationSpiceEngineRender");
var getCircuitJsonForAnalogSimulation = ({
  circuitJson,
  simulationExperimentId,
  voltageProbes,
  ammeters
}) => {
  const voltageProbeIds = new Set(
    voltageProbes.map((voltageProbe) => voltageProbe.simulation_voltage_probe_id).filter(
      (simulationVoltageProbeId) => Boolean(simulationVoltageProbeId)
    )
  );
  const currentProbeIds = new Set(
    ammeters.map((ammeter) => ammeter.simulation_current_probe_id).filter(
      (simulationCurrentProbeId) => Boolean(simulationCurrentProbeId)
    )
  );
  return circuitJson.filter((element) => {
    if (element.type === "simulation_experiment") {
      return element.simulation_experiment_id === simulationExperimentId;
    }
    if (element.type === "simulation_voltage_probe") {
      return voltageProbeIds.has(element.simulation_voltage_probe_id);
    }
    if (element.type === "simulation_current_probe") {
      return currentProbeIds.has(element.simulation_current_probe_id);
    }
    if (element.type === "simulation_parameter_sweep") {
      return element.simulation_experiment_id === simulationExperimentId;
    }
    if (element.type === "simulation_oscilloscope_trace") {
      if (element.simulation_voltage_probe_id) {
        return voltageProbeIds.has(element.simulation_voltage_probe_id);
      }
      if (element.simulation_current_probe_id) {
        return currentProbeIds.has(element.simulation_current_probe_id);
      }
    }
    if (element.type === "simulation_transient_voltage_graph" || element.type === "simulation_transient_current_graph" || element.type === "simulation_dc_operating_point_voltage" || element.type === "simulation_dc_operating_point_current" || element.type === "simulation_dc_sweep_voltage_graph" || element.type === "simulation_dc_sweep_current_graph" || element.type === "simulation_ac_sweep_voltage_graph" || element.type === "simulation_ac_sweep_current_graph" || element.type === "simulation_unknown_experiment_error") {
      return false;
    }
    return true;
  });
};
function Group_doInitialSimulationSpiceEngineRender(group) {
  if (!group.isSubcircuit) return;
  const { root } = group;
  if (!root) return;
  const analogSimulations = [
    ...group.selectAll("analogsimulation"),
    ...group.selectAll("analogtransientsimulation"),
    ...group.selectAll(
      "analogdcoperatingpointsimulation"
    ),
    ...group.selectAll("analogdcsweepsimulation"),
    ...group.selectAll("analogacsweepsimulation")
  ];
  if (analogSimulations.length === 0) return;
  resetSimulationColorState();
  const spiceEngineMap = { ...root.platform?.spiceEngineMap };
  if (!spiceEngineMap.spicey) {
    spiceEngineMap.spicey = getSpiceyEngine();
  }
  for (const analogSimulation of analogSimulations) {
    const simulationExperimentId = analogSimulation.simulation_experiment_id;
    if (!simulationExperimentId) {
      debug12("No simulation experiment id found, skipping simulation");
      continue;
    }
    const simulationExperiment = root.db.simulation_experiment.get(
      simulationExperimentId
    );
    if (!simulationExperiment) {
      debug12("No simulation experiment found, skipping simulation");
      continue;
    }
    const simulationScope = analogSimulation.getGroup() ?? group;
    const voltageProbes = simulationScope.selectAll("voltageprobe");
    const ammeters = simulationScope.selectAll("ammeter");
    const circuitJson = getCircuitJsonForAnalogSimulation({
      circuitJson: root.db.toArray(),
      simulationExperimentId,
      voltageProbes,
      ammeters
    });
    let simulationRuns;
    try {
      simulationRuns = createSimulationRuns({
        circuitJson,
        simulationExperimentId
      });
      debug12(
        `Generated ${simulationRuns.length} SPICE run(s):
${simulationRuns.map((simulationRun) => simulationRun.spiceString).join("\n")}`
      );
    } catch (error) {
      debug12(`Failed to convert circuit JSON to SPICE: ${error}`);
      root.db.simulation_unknown_experiment_error.insert({
        simulation_experiment_id: simulationExperimentId,
        error_type: "simulation_unknown_experiment_error",
        message: error instanceof Error ? error.message : String(error)
      });
      continue;
    }
    const voltageProbeByGraphName = /* @__PURE__ */ new Map();
    const voltageProbeById = /* @__PURE__ */ new Map();
    const graphDisplayOverridesByProbeId = /* @__PURE__ */ new Map();
    for (const probe of voltageProbes) {
      if (probe.finalProbeName) {
        voltageProbeByGraphName.set(probe.finalProbeName, probe);
      }
      if (probe.simulation_voltage_probe_id) {
        voltageProbeById.set(probe.simulation_voltage_probe_id, probe);
        graphDisplayOverridesByProbeId.set(
          probe.simulation_voltage_probe_id,
          getVoltageProbeGraphDisplayOverrides(probe)
        );
      }
    }
    for (const ammeter of ammeters) {
      if (ammeter.simulation_current_probe_id) {
        graphDisplayOverridesByProbeId.set(
          ammeter.simulation_current_probe_id,
          getAmmeterGraphDisplayOverrides(ammeter)
        );
      }
    }
    const scopedCurrentProbeIds = new Set(
      ammeters.map((ammeter) => ammeter.simulation_current_probe_id).filter(
        (simulationCurrentProbeId) => Boolean(simulationCurrentProbeId)
      )
    );
    const currentProbeById = /* @__PURE__ */ new Map();
    const currentProbeByName = /* @__PURE__ */ new Map();
    for (const probe of root.db.simulation_current_probe.list().filter(
      (probe2) => scopedCurrentProbeIds.has(probe2.simulation_current_probe_id)
    )) {
      currentProbeById.set(probe.simulation_current_probe_id, probe);
      if (probe.name !== void 0) {
        currentProbeByName.set(probe.name, probe);
      }
    }
    const orderedSimulationProbes = root.db.simulation_voltage_probe.list().filter(
      (probe) => voltageProbeById.has(probe.simulation_voltage_probe_id)
    );
    const firstSpiceNetlist = simulationRuns[0]?.spiceNetlist;
    const graphNamesFromNetlist = firstSpiceNetlist ? getTransientVoltageGraphNamesFromSpiceNetlist(firstSpiceNetlist) : [];
    if (graphNamesFromNetlist.length === orderedSimulationProbes.length) {
      for (const [
        simulationProbeIndex,
        simulationProbe
      ] of orderedSimulationProbes.entries()) {
        const probe = voltageProbeById.get(
          simulationProbe.simulation_voltage_probe_id
        );
        const graphName = graphNamesFromNetlist[simulationProbeIndex];
        if (probe && graphName) {
          voltageProbeByGraphName.set(graphName, probe);
        }
      }
    } else {
      debug12(
        `Skipping probe-to-graph order mapping because counts differ: probes=${orderedSimulationProbes.length} graphNames=${graphNamesFromNetlist.length}`
      );
    }
    const engineName = analogSimulation.getSpiceEngineName() ?? "spicey";
    const spiceEngine = spiceEngineMap[engineName];
    if (!spiceEngine) {
      throw new Error(
        `SPICE engine "${engineName}" not found in platform config. Available engines: ${JSON.stringify(
          Object.keys(spiceEngineMap).filter(
            (spiceEngineName) => spiceEngineName !== "spicey"
          )
        )}`
      );
    }
    const effectId = `spice-simulation-${engineName}-${simulationExperimentId}`;
    debug12(
      `Queueing simulation for spice engine: ${engineName} (id: ${effectId})`
    );
    group._queueAsyncEffect(effectId, async () => {
      try {
        const insertedVoltageGraphs = [];
        const insertedCurrentGraphs = [];
        for (const simulationRun of simulationRuns) {
          debug12(`Running simulation with engine: ${engineName}`);
          const simulationResult = await spiceEngine.simulate(
            simulationRun.spiceString
          );
          debug12(
            `Simulation completed, received ${simulationResult.simulationResultCircuitJson.length} elements`
          );
          for (const simulationResultCircuitElement of simulationResult.simulationResultCircuitJson) {
            if (!isCircuitElementInput(simulationResultCircuitElement)) {
              debug12("Skipping invalid simulation result element");
              continue;
            }
            const simulationResultWithSweepCoordinate = attachSweepCoordinateToSimulationResult({
              simulationResult: simulationResultCircuitElement,
              simulationParameterSweepCoordinate: simulationRun.simulationParameterSweepCoordinate
            });
            if (isVoltageGraph(simulationResultWithSweepCoordinate)) {
              simulationResultWithSweepCoordinate.simulation_experiment_id = simulationExperiment.simulation_experiment_id;
              const probeMatch = simulationResultWithSweepCoordinate.name ? voltageProbeByGraphName.get(
                simulationResultWithSweepCoordinate.name
              ) : void 0;
              if (probeMatch) {
                simulationResultWithSweepCoordinate.color = probeMatch.color ?? void 0;
                simulationResultWithSweepCoordinate.source_probe_id = probeMatch.simulation_voltage_probe_id ?? void 0;
              }
            } else if (isSimulationVoltageResult(simulationResultWithSweepCoordinate)) {
              simulationResultWithSweepCoordinate.simulation_experiment_id = simulationExperiment.simulation_experiment_id;
              const probeMatch = voltageProbeById.get(
                simulationResultWithSweepCoordinate.simulation_voltage_probe_id
              ) ?? (simulationResultWithSweepCoordinate.name ? voltageProbeByGraphName.get(
                simulationResultWithSweepCoordinate.name
              ) : void 0);
              if (probeMatch?.simulation_voltage_probe_id) {
                simulationResultWithSweepCoordinate.color = probeMatch.color ?? void 0;
                simulationResultWithSweepCoordinate.simulation_voltage_probe_id = probeMatch.simulation_voltage_probe_id;
              }
            }
            if (isCurrentGraph(simulationResultWithSweepCoordinate)) {
              simulationResultWithSweepCoordinate.simulation_experiment_id = simulationExperiment.simulation_experiment_id;
              const probeMatch = (simulationResultWithSweepCoordinate.source_probe_id ? currentProbeById.get(
                simulationResultWithSweepCoordinate.source_probe_id
              ) : void 0) ?? (simulationResultWithSweepCoordinate.name ? currentProbeByName.get(
                simulationResultWithSweepCoordinate.name
              ) : void 0);
              if (probeMatch) {
                simulationResultWithSweepCoordinate.color = probeMatch.color;
                simulationResultWithSweepCoordinate.source_probe_id = probeMatch.simulation_current_probe_id;
              }
            } else if (isSimulationCurrentResult(simulationResultWithSweepCoordinate)) {
              simulationResultWithSweepCoordinate.simulation_experiment_id = simulationExperiment.simulation_experiment_id;
              const probeMatch = currentProbeById.get(
                simulationResultWithSweepCoordinate.simulation_current_probe_id
              ) ?? (simulationResultWithSweepCoordinate.name ? currentProbeByName.get(
                simulationResultWithSweepCoordinate.name
              ) : void 0);
              if (probeMatch) {
                simulationResultWithSweepCoordinate.color = probeMatch.color;
                simulationResultWithSweepCoordinate.simulation_current_probe_id = probeMatch.simulation_current_probe_id;
              }
            }
            const insertedSimulationResult = root.db.insert(
              simulationResultWithSweepCoordinate
            );
            if (isVoltageGraph(insertedSimulationResult)) {
              insertedVoltageGraphs.push({
                type: "voltage",
                graph: insertedSimulationResult
              });
            }
            if (isCurrentGraph(insertedSimulationResult)) {
              insertedCurrentGraphs.push({
                type: "current",
                graph: insertedSimulationResult
              });
            }
            debug12(
              `Inserted ${simulationResultWithSweepCoordinate.type} into database`
            );
          }
        }
        if (analogSimulation.usesIndependentGraphAxes()) {
          insertIndependentAxisScopeTraces({
            db: root.db,
            graphs: [...insertedVoltageGraphs, ...insertedCurrentGraphs],
            graphDisplayOverridesByProbeId
          });
        }
        group._markDirty("SimulationSpiceEngineRender");
      } catch (error) {
        debug12(`Simulation failed for engine ${engineName}: ${error}`);
        root.db.simulation_unknown_experiment_error.insert({
          simulation_experiment_id: simulationExperimentId,
          error_type: "simulation_unknown_experiment_error",
          message: error instanceof Error ? error.message : String(error)
        });
      }
    });
  }
}

// lib/components/primitive-components/Group/Group_doInitialSourceAddConnectivityMapKey.ts
import { ConnectivityMap as ConnectivityMap3 } from "circuit-json-to-connectivity-map";
function Group_doInitialSourceAddConnectivityMapKey(group) {
  if (!group.isSubcircuit) return;
  const { db } = group.root;
  const traces = group.selectAll("trace");
  const vias = group.selectAll("via");
  const nets = group.selectAll("net");
  const connMap = new ConnectivityMap3({});
  connMap.addConnections(
    traces.map((t) => {
      const source_trace = db.source_trace.get(
        t.source_trace_id
      );
      if (!source_trace) return null;
      return [
        source_trace.source_trace_id,
        ...source_trace.connected_source_port_ids,
        ...source_trace.connected_source_net_ids
      ];
    }).filter((c) => c !== null)
  );
  const sourceNets = db.source_net.list().filter((net) => net.subcircuit_id === group.subcircuit_id);
  for (const sourceNet of sourceNets) {
    connMap.addConnections([[sourceNet.source_net_id]]);
  }
  const { name: subcircuitName } = group._parsedProps;
  for (const trace of traces) {
    if (!trace.source_trace_id) continue;
    const connNetId = connMap.getNetConnectedToId(trace.source_trace_id);
    if (!connNetId) continue;
    trace.subcircuit_connectivity_map_key = `${subcircuitName ?? `unnamedsubcircuit${group.subcircuit_id ?? group.source_group_id ?? group._renderId}`}_${connNetId}`;
    db.source_trace.update(trace.source_trace_id, {
      subcircuit_connectivity_map_key: trace.subcircuit_connectivity_map_key
    });
  }
  const allSourcePortIds = /* @__PURE__ */ new Set();
  for (const trace of traces) {
    if (!trace.source_trace_id) continue;
    const source_trace = db.source_trace.get(
      trace.source_trace_id
    );
    if (!source_trace) continue;
    for (const id of source_trace.connected_source_port_ids) {
      allSourcePortIds.add(id);
    }
  }
  for (const portId of allSourcePortIds) {
    const connNetId = connMap.getNetConnectedToId(portId);
    if (!connNetId) continue;
    const connectivityMapKey = `${subcircuitName ?? `unnamedsubcircuit${group.subcircuit_id ?? group.source_group_id ?? group._renderId}`}_${connNetId}`;
    db.source_port.update(portId, {
      subcircuit_connectivity_map_key: connectivityMapKey
    });
  }
  const allSourceNetIds = /* @__PURE__ */ new Set();
  for (const trace of traces) {
    if (!trace.source_trace_id) continue;
    const source_trace = db.source_trace.get(
      trace.source_trace_id
    );
    if (!source_trace) continue;
    for (const source_net_id of source_trace.connected_source_net_ids) {
      allSourceNetIds.add(source_net_id);
    }
  }
  for (const sourceNet of sourceNets) {
    allSourceNetIds.add(sourceNet.source_net_id);
  }
  for (const netId of allSourceNetIds) {
    const connNetId = connMap.getNetConnectedToId(netId);
    if (!connNetId) continue;
    const connectivityMapKey = `${subcircuitName ?? `unnamedsubcircuit${group.subcircuit_id ?? group.source_group_id ?? group._renderId}`}_${connNetId}`;
    db.source_net.update(netId, {
      subcircuit_connectivity_map_key: connectivityMapKey
    });
    const netInstance = nets.find((n) => n.source_net_id === netId);
    if (netInstance) {
      netInstance.subcircuit_connectivity_map_key = connectivityMapKey;
    }
  }
  for (const via of vias) {
    const connectedNetOrTrace = via._getConnectedNetOrTrace();
    if (!connectedNetOrTrace) continue;
    if (connectedNetOrTrace.subcircuit_connectivity_map_key) {
      via.subcircuit_connectivity_map_key = connectedNetOrTrace.subcircuit_connectivity_map_key;
    }
  }
}

// lib/components/primitive-components/Group/Group_getFanoutPourNetMap.ts
var addPourNetMap = (target, pourNetMap) => {
  for (const [layer, netOrNets] of Object.entries(pourNetMap)) {
    if (!netOrNets) continue;
    const nets = Array.isArray(netOrNets) ? netOrNets : [netOrNets];
    const layerNets = target.get(layer) ?? /* @__PURE__ */ new Set();
    for (const net of nets) layerNets.add(net);
    target.set(layer, layerNets);
  }
};
var toFanoutPourNetMap = (netsByLayer) => Object.fromEntries(
  Array.from(netsByLayer, ([layer, nets]) => [
    layer,
    nets.size === 1 ? Array.from(nets)[0] : Array.from(nets)
  ])
);
var Group_getFanoutPourNetMap = (group, routingPhasePlans) => {
  const explicitMaps = routingPhasePlans.map((plan) => plan.fanoutPourNetMap).filter((map) => map !== void 0);
  const netsByLayer = /* @__PURE__ */ new Map();
  if (explicitMaps.length > 0) {
    for (const explicitMap of explicitMaps) {
      addPourNetMap(netsByLayer, explicitMap);
    }
    return toFanoutPourNetMap(netsByLayer);
  }
  let scope = group;
  while (scope) {
    if (scope.isSubcircuit) {
      const subcircuit = scope;
      const copperPours = subcircuit.selectAll("copperpour");
      for (const copperPour of copperPours) {
        if (copperPour.getSubcircuit() !== subcircuit) continue;
        const { layer, connectsTo } = copperPour._parsedProps;
        addPourNetMap(netsByLayer, {
          [layer]: connectsTo
        });
      }
    }
    scope = scope.parent;
  }
  return netsByLayer.size > 0 ? toFanoutPourNetMap(netsByLayer) : void 0;
};

// lib/components/primitive-components/Group/Group_getRoutingPhasePlans.ts
import { getBoundsFromPoints as getBoundsFromPoints5 } from "@tscircuit/math-utils";

// lib/components/primitive-components/BreakoutPoint.ts
import { breakoutPointProps } from "@tscircuit/props";

// lib/components/primitive-components/BaseBreakoutPoint.ts
import { pcbLayoutProps } from "@tscircuit/props";
var baseBreakoutPointProps = pcbLayoutProps.omit({
  pcbRotation: true,
  layer: true
});
var BaseBreakoutPoint = class extends PrimitiveComponent2 {
  pcb_breakout_point_id = null;
  matchedPort = null;
  matchedNet = null;
  matchedSourceTraceId = null;
  isPcbPrimitive = true;
  _getSourceTraceIdForPort(port) {
    const { db } = this.root;
    const trace = db.source_trace.list().find((st) => st.connected_source_port_ids.includes(port.source_port_id));
    return trace?.source_trace_id;
  }
  _getSourceNetIdForPort(port) {
    const { db } = this.root;
    const trace = db.source_trace.list().find((st) => st.connected_source_port_ids.includes(port.source_port_id));
    return trace?.connected_source_net_ids[0];
  }
  _renderPcbBreakoutPoint() {
    if (this.pcb_breakout_point_id) return;
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const group = this.parent?.getGroup();
    const subcircuit = this.getSubcircuit();
    if (!group || !group.pcb_group_id) return;
    const pcb_breakout_point = db.pcb_breakout_point.insert({
      pcb_group_id: group.pcb_group_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      source_port_id: this.matchedPort?.source_port_id ?? void 0,
      source_trace_id: this.matchedPort ? this.matchedSourceTraceId ?? this._getSourceTraceIdForPort(this.matchedPort) : void 0,
      source_net_id: this.matchedNet ? this.matchedNet.source_net_id : this.matchedPort ? this._getSourceNetIdForPort(this.matchedPort) : void 0,
      x: position.x,
      y: position.y
    });
    this.pcb_breakout_point_id = pcb_breakout_point.pcb_breakout_point_id;
  }
  _getPcbCircuitJsonBounds() {
    const position = this._getGlobalPcbPositionBeforeLayout();
    return {
      center: { x: position.x, y: position.y },
      bounds: {
        left: position.x,
        top: position.y,
        right: position.x,
        bottom: position.y
      },
      width: 0,
      height: 0
    };
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    if (!this.pcb_breakout_point_id) return;
    const updatedProperties = {
      x: newCenter.x,
      y: newCenter.y
    };
    if (this.matchedSourceTraceId) {
      updatedProperties.source_trace_id = this.matchedSourceTraceId;
    }
    db.pcb_breakout_point.update(this.pcb_breakout_point_id, updatedProperties);
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_breakout_point_id) return;
    const point6 = db.pcb_breakout_point.get(this.pcb_breakout_point_id);
    if (point6) {
      db.pcb_breakout_point.update(this.pcb_breakout_point_id, {
        x: point6.x + deltaX,
        y: point6.y + deltaY
      });
    }
  }
  getPcbSize() {
    return { width: 0, height: 0 };
  }
};

// lib/components/primitive-components/BreakoutPoint.ts
var BreakoutPoint = class extends BaseBreakoutPoint {
  get config() {
    return {
      componentName: "BreakoutPoint",
      zodProps: breakoutPointProps
    };
  }
  _matchConnection() {
    if (this.matchedPort) return;
    const { connection } = this._parsedProps;
    const subcircuit = this.getSubcircuit();
    if (!subcircuit) return;
    this.matchedPort = subcircuit.selectOne(connection, {
      type: "port"
    });
    if (!this.matchedPort) {
      this.matchedNet = subcircuit.selectOne(connection, { type: "net" });
    }
    if (!this.matchedPort && !this.matchedNet) {
      this.renderError(`Could not find connection target "${connection}"`);
    }
  }
  doInitialPcbPrimitiveRender() {
    this._matchConnection();
    this._renderPcbBreakoutPoint();
  }
};

// lib/components/primitive-components/Group/Group_getRoutingPhasePlans.ts
function getPhaseSortValue(routingPhaseIndex) {
  return routingPhaseIndex === null ? Number.POSITIVE_INFINITY : routingPhaseIndex;
}
function compareRoutingPhasePlans(a, b) {
  return getPhaseSortValue(a.routingPhaseIndex) - getPhaseSortValue(b.routingPhaseIndex);
}
function getOrCreateRoutingPhasePlan(plansByPhaseIndex, routingPhaseIndex) {
  let plan = plansByPhaseIndex.get(routingPhaseIndex);
  if (!plan) {
    plan = { routingPhaseIndex, nets: [], traces: [] };
    plansByPhaseIndex.set(routingPhaseIndex, plan);
  }
  return plan;
}
function getNetRoutingPhaseIndex(net) {
  return net.props.routingPhaseIndex ?? null;
}
function getTraceRoutingPhaseIndex(trace, buses = []) {
  const busRoutingPhaseIndexes = /* @__PURE__ */ new Set();
  for (const bus of buses) {
    if (bus._parsedProps.routingPhaseIndex === void 0) continue;
    const traceIsInBus = bus._parsedProps.connections.some(
      (connection) => connection === trace.name || traceHasEndpointMatchingConnectionSelector(
        trace,
        convertPortSelectorToEndpointKey(connection)
      )
    );
    if (traceIsInBus) {
      busRoutingPhaseIndexes.add(bus._parsedProps.routingPhaseIndex);
    }
  }
  if (busRoutingPhaseIndexes.size > 1) {
    throw new Error(
      `Trace "${trace.name}" belongs to buses with different routing phases`
    );
  }
  const busRoutingPhaseIndex = busRoutingPhaseIndexes.values().next().value;
  if (busRoutingPhaseIndex !== void 0) {
    return busRoutingPhaseIndex;
  }
  const traceRoutingPhaseIndex = trace.props.routingPhaseIndex;
  if (traceRoutingPhaseIndex !== void 0) return traceRoutingPhaseIndex;
  let routingPhaseIndex = null;
  const connectedNets = trace._findConnectedNets().nets;
  for (const net of connectedNets) {
    const netRoutingPhaseIndex = getNetRoutingPhaseIndex(net);
    if (typeof netRoutingPhaseIndex === "number") {
      if (routingPhaseIndex === null || netRoutingPhaseIndex < routingPhaseIndex) {
        routingPhaseIndex = netRoutingPhaseIndex;
      }
    }
  }
  return routingPhaseIndex;
}
function convertPortSelectorToEndpointKey(selector) {
  return selector.trim().replace(/\s*>\s*/g, ".").replace(/\s+/g, ".").replace(/^\./, "").replace(/\.\./g, ".");
}
function getConnectionSelectorsFromAutoroutingPhaseProps(phaseProps) {
  return [
    ...phaseProps.connection ? [phaseProps.connection] : [],
    ...phaseProps.connections ?? []
  ];
}
function traceHasEndpointMatchingConnectionSelector(trace, connectionSelectorEndpointKey) {
  return trace.getTracePortPathSelectors().some(
    (selector) => convertPortSelectorToEndpointKey(selector) === connectionSelectorEndpointKey
  );
}
function getAutoroutersByPhaseIndex(group) {
  const autoroutingPhases = group.selectAll(
    "autoroutingphase"
  );
  const autoroutersByPhaseIndex = /* @__PURE__ */ new Map();
  for (const autoroutingPhase of autoroutingPhases) {
    const { phaseIndex, autorouter } = autoroutingPhase._parsedProps;
    if (autorouter === void 0) continue;
    autoroutersByPhaseIndex.set(phaseIndex ?? null, autorouter);
  }
  return autoroutersByPhaseIndex;
}
function getAutoroutingPhasePropsByPhaseIndex(group) {
  const autoroutingPhases = group.selectAll(
    "autoroutingphase"
  );
  const propsByPhaseIndex = /* @__PURE__ */ new Map();
  for (const autoroutingPhase of autoroutingPhases) {
    const props = autoroutingPhase._parsedProps;
    propsByPhaseIndex.set(props.phaseIndex ?? null, props);
  }
  return propsByPhaseIndex;
}
function toParsedDistance(value) {
  if (value === void 0) return void 0;
  return Number(value);
}
function getDrcTolerancesFromAutoroutingPhaseProps(phaseProps) {
  const {
    minTraceWidth,
    minViaHoleEdgeToViaHoleEdgeClearance,
    minPlatedHoleDrillEdgeToDrillEdgeClearance,
    minTraceToPadEdgeClearance,
    minPadEdgeToPadEdgeClearance,
    minBoardEdgeClearance,
    minViaEdgeToPadEdgeClearance,
    minViaHoleDiameter,
    minViaPadDiameter
  } = phaseProps;
  if (minTraceWidth === void 0 && minViaHoleEdgeToViaHoleEdgeClearance === void 0 && minPlatedHoleDrillEdgeToDrillEdgeClearance === void 0 && minTraceToPadEdgeClearance === void 0 && minPadEdgeToPadEdgeClearance === void 0 && minBoardEdgeClearance === void 0 && minViaEdgeToPadEdgeClearance === void 0 && minViaHoleDiameter === void 0 && minViaPadDiameter === void 0) {
    return void 0;
  }
  return {
    minTraceWidth: toParsedDistance(minTraceWidth),
    minViaHoleEdgeToViaHoleEdgeClearance: toParsedDistance(
      minViaHoleEdgeToViaHoleEdgeClearance
    ),
    minPlatedHoleDrillEdgeToDrillEdgeClearance: toParsedDistance(
      minPlatedHoleDrillEdgeToDrillEdgeClearance
    ),
    minTraceToPadEdgeClearance: toParsedDistance(minTraceToPadEdgeClearance),
    minPadEdgeToPadEdgeClearance: toParsedDistance(
      minPadEdgeToPadEdgeClearance
    ),
    minBoardEdgeClearance: toParsedDistance(minBoardEdgeClearance),
    minViaEdgeToPadEdgeClearance: toParsedDistance(
      minViaEdgeToPadEdgeClearance
    ),
    minViaHoleDiameter: toParsedDistance(minViaHoleDiameter),
    minViaPadDiameter: toParsedDistance(minViaPadDiameter)
  };
}
function Group_getRoutingPhasePlans(group) {
  const traces = group.selectAll("trace");
  const nets = group.selectAll("net");
  const buses = group.selectAll("bus");
  const breakouts = group.selectAll("group").filter(
    (candidate) => candidate.isRoutingDirective
  );
  const breakoutByTrace = /* @__PURE__ */ new Map();
  const breakoutByNet = /* @__PURE__ */ new Map();
  for (const trace of traces) {
    let ancestor = trace.parent;
    while (ancestor && ancestor !== group) {
      if (ancestor.isRoutingDirective) {
        breakoutByTrace.set(trace, ancestor);
        break;
      }
      ancestor = ancestor.parent;
    }
  }
  for (const net of nets) {
    if (net.parent?.isRoutingDirective && breakouts.includes(net.parent)) {
      breakoutByNet.set(net, net.parent);
    }
  }
  const plansByPhaseIndex = /* @__PURE__ */ new Map();
  const autoroutersByPhaseIndex = getAutoroutersByPhaseIndex(group);
  const phasePropsByPhaseIndex = getAutoroutingPhasePropsByPhaseIndex(group);
  const groupFanoutProps = group._parsedProps;
  const hasDirectRoutingTargets = traces.length > 0 || nets.length > 0;
  const hasReroutePhase = Array.from(phasePropsByPhaseIndex.values()).some(
    (phaseProps) => phaseProps.reroute
  );
  const hasConnectionTargetedPhase = Array.from(
    phasePropsByPhaseIndex.values()
  ).some(
    (phaseProps) => getConnectionSelectorsFromAutoroutingPhaseProps(phaseProps).length > 0
  );
  if (!hasDirectRoutingTargets && !hasReroutePhase && !hasConnectionTargetedPhase && breakouts.length === 0)
    return [];
  for (const net of nets) {
    if (breakoutByNet.has(net)) continue;
    const routingPhaseIndex = getNetRoutingPhaseIndex(net);
    getOrCreateRoutingPhasePlan(plansByPhaseIndex, routingPhaseIndex).nets.push(
      net
    );
  }
  for (const trace of traces) {
    if (breakoutByTrace.has(trace)) continue;
    const routingPhaseIndex = getTraceRoutingPhaseIndex(trace, buses);
    getOrCreateRoutingPhasePlan(
      plansByPhaseIndex,
      routingPhaseIndex
    ).traces.push(trace);
  }
  for (const [phaseIndex, phaseProps] of phasePropsByPhaseIndex) {
    const connectionSelectors = getConnectionSelectorsFromAutoroutingPhaseProps(phaseProps);
    if (connectionSelectors.length > 0) {
      const plan = getOrCreateRoutingPhasePlan(plansByPhaseIndex, phaseIndex);
      const connectionSelectorEndpointKeys = connectionSelectors.map(
        convertPortSelectorToEndpointKey
      );
      plan.connectionSelectors = connectionSelectors;
      for (const trace of traces) {
        if (breakoutByTrace.has(trace)) continue;
        if (connectionSelectorEndpointKeys.some(
          (endpointKey) => traceHasEndpointMatchingConnectionSelector(trace, endpointKey)
        )) {
          if (!phaseProps.reroute) {
            for (const existingPlan of plansByPhaseIndex.values()) {
              if (existingPlan === plan) continue;
              existingPlan.traces = existingPlan.traces.filter(
                (existingTrace) => existingTrace !== trace
              );
            }
          }
          if (plan.traces.includes(trace)) continue;
          plan.traces.push(trace);
        }
      }
    }
    if (phaseProps.reroute) {
      getOrCreateRoutingPhasePlan(plansByPhaseIndex, phaseIndex);
    }
  }
  const plans = Array.from(plansByPhaseIndex.values()).sort(
    compareRoutingPhasePlans
  );
  for (const plan of plans) {
    plan.autorouter = autoroutersByPhaseIndex.get(plan.routingPhaseIndex);
    const phaseProps = phasePropsByPhaseIndex.get(plan.routingPhaseIndex);
    plan.phaseName = phaseProps?.name;
    plan.reroute = phaseProps?.reroute;
    plan.region = phaseProps?.region;
    plan.connectionSelectors = phaseProps ? getConnectionSelectorsFromAutoroutingPhaseProps(phaseProps) : void 0;
    plan.busFanoutDirections = phaseProps?.busFanoutDirections ?? groupFanoutProps.busFanoutDirections;
    plan.fanoutBoundaryPadding = phaseProps?.fanoutBoundaryPadding ?? groupFanoutProps.fanoutBoundaryPadding;
    const fanoutRoutingLayers = phaseProps?.fanoutRoutingLayers ?? groupFanoutProps.fanoutRoutingLayers;
    plan.fanoutRoutingLayers = fanoutRoutingLayers?.map(
      (layer) => typeof layer === "string" ? layer : layer.name
    );
    plan.fanoutPourNetMap = phaseProps?.fanoutPourNetMap ?? groupFanoutProps.fanoutPourNetMap;
    plan.drcTolerances = phaseProps ? getDrcTolerancesFromAutoroutingPhaseProps(phaseProps) : void 0;
  }
  const breakoutPlans = [];
  for (const breakout of breakouts) {
    const breakoutProps2 = breakout._parsedProps;
    const hasManualBreakoutPoints = breakout.children.some(
      (child) => child instanceof BreakoutPoint
    );
    const shouldUseDefaultAutorouterForBreakoutPoints = hasManualBreakoutPoints && breakout.props.autorouter === void 0;
    const breakoutTraces = traces.filter(
      (trace) => breakoutByTrace.get(trace) === breakout
    );
    const unroutedBreakoutTraces = breakoutTraces.filter(
      (trace) => !trace.pcb_trace_id
    );
    const pcbGroup = breakout.pcb_group_id ? breakout.root?.db.pcb_group.get(breakout.pcb_group_id) : null;
    const outlineBounds = pcbGroup?.outline ? getBoundsFromPoints5(pcbGroup.outline) : null;
    const routingWidth = pcbGroup?.width ?? (outlineBounds ? outlineBounds.maxX - outlineBounds.minX : void 0);
    const routingHeight = pcbGroup?.height ?? (outlineBounds ? outlineBounds.maxY - outlineBounds.minY : void 0);
    const routingBounds = pcbGroup && routingWidth && routingHeight ? {
      minX: pcbGroup.center.x - routingWidth / 2,
      maxX: pcbGroup.center.x + routingWidth / 2,
      minY: pcbGroup.center.y - routingHeight / 2,
      maxY: pcbGroup.center.y + routingHeight / 2
    } : void 0;
    const rawBreakoutProps = breakout.props;
    const hasExplicitGeometry = rawBreakoutProps.width !== void 0 || rawBreakoutProps.height !== void 0 || Boolean(rawBreakoutProps.outline?.length);
    const hasExplicitFanoutBoundaryPadding = rawBreakoutProps.fanoutBoundaryPadding !== void 0;
    const breakoutPlan = {
      routingPhaseIndex: null,
      routingPcbGroupId: breakout.pcb_group_id ?? void 0,
      routingBounds,
      fanoutBounds: hasExplicitGeometry ? routingBounds : hasExplicitFanoutBoundaryPadding ? void 0 : routingBounds,
      autorouter: breakoutProps2.autorouter ?? "fanout",
      busFanoutDirections: breakoutProps2.busFanoutDirections,
      fanoutBoundaryPadding: breakoutProps2.fanoutBoundaryPadding,
      fanoutRoutingLayers: breakoutProps2.fanoutRoutingLayers?.map(
        (layer) => typeof layer === "string" ? layer : layer.name
      ),
      fanoutPourNetMap: breakoutProps2.fanoutPourNetMap,
      nets: nets.filter((net) => breakoutByNet.get(net) === breakout),
      traces: breakoutTraces
    };
    if (!shouldUseDefaultAutorouterForBreakoutPoints) {
      breakoutPlans.push(breakoutPlan);
      continue;
    }
    breakoutPlans.push({
      ...breakoutPlan,
      autorouter: "default",
      traces: []
    });
    if (unroutedBreakoutTraces.length > 0) {
      const automaticFanoutBusIds = new Set(
        buses.filter(
          (bus) => unroutedBreakoutTraces.some(
            (trace) => bus._parsedProps.connections.some(
              (connection) => connection === trace.name || traceHasEndpointMatchingConnectionSelector(
                trace,
                convertPortSelectorToEndpointKey(connection)
              )
            )
          )
        ).map((bus) => bus.name)
      );
      for (const trace of unroutedBreakoutTraces) {
        if (trace.name) automaticFanoutBusIds.add(trace.name);
      }
      const automaticBusFanoutDirections = breakoutProps2.busFanoutDirections ? Object.fromEntries(
        Object.entries(breakoutProps2.busFanoutDirections).filter(
          ([busId]) => automaticFanoutBusIds.has(busId)
        )
      ) : void 0;
      breakoutPlans.push({
        ...breakoutPlan,
        routingPcbGroupId: void 0,
        busFanoutDirections: automaticBusFanoutDirections,
        traces: unroutedBreakoutTraces
      });
    }
  }
  const defaultPhaseProps = phasePropsByPhaseIndex.get(null);
  if (hasDirectRoutingTargets && phasePropsByPhaseIndex.size === 1 && defaultPhaseProps?.reroute && plans.length === 1 && plans[0]?.routingPhaseIndex === null) {
    const reroutePlan = plans[0];
    return [
      ...breakoutPlans,
      {
        routingPhaseIndex: null,
        nets: [...reroutePlan.nets],
        traces: [...reroutePlan.traces]
      },
      reroutePlan
    ];
  }
  return [...breakoutPlans, ...plans];
}

// package.json
var package_default = {
  name: "@tscircuit/core",
  type: "module",
  version: "0.0.1653",
  types: "dist/index.d.ts",
  main: "dist/index.js",
  module: "dist/index.js",
  exports: {
    ".": {
      import: "./dist/index.js",
      types: "./dist/index.d.ts"
    }
  },
  files: [
    "dist"
  ],
  repository: {
    type: "git",
    url: "https://github.com/tscircuit/core"
  },
  scripts: {
    build: "tsup-node",
    format: "biome format . --write",
    "measure-bundle": "howfat -r table .",
    "pkg-pr-new-release": "bunx pkg-pr-new publish --comment=off --peerDeps",
    "smoke-test:dist": "bun run scripts/smoke-tests/test-dist-simple-circuit.tsx",
    "build:benchmarking": "bun build --experimental-html ./benchmarking/website/index.html --outdir ./benchmarking-dist",
    "build:benchmarking:watch": `chokidar "./{benchmarking,lib}/**/*.{ts,tsx}" -c 'bun build --experimental-html ./benchmarking/website/index.html --outdir ./benchmarking-dist'`,
    "start:benchmarking": 'concurrently "bun run build:benchmarking:watch" "live-server ./benchmarking-dist"',
    "generate-test-plan": "bun run scripts/generate-test-plan.ts"
  },
  devDependencies: {
    "@biomejs/biome": "^1.8.3",
    "@resvg/resvg-js": "^2.6.2",
    "@tsci/tscircuit.ti": "github:tscircuit/ti#57e314be4b2b06bc546d4def7453e619604d1157",
    "@tscircuit/alphabet": "0.0.25",
    "@tscircuit/breakout-point-solver": "github:tscircuit/breakout-point-solver#bac9629",
    "@tscircuit/capacity-autorouter": "^0.0.790",
    "@tscircuit/checks": "0.0.154",
    "@tscircuit/circuit-json-util": "^0.0.106",
    "@tscircuit/common": "^0.0.20",
    "@tscircuit/copper-pour-solver": "0.0.44",
    "@tscircuit/create-fdm-enclosure": "0.0.3",
    "@tscircuit/fanout-solver": "0.0.20",
    "@tscircuit/footprinter": "^0.0.409",
    "@tscircuit/image-utils": "^0.0.8",
    "@tscircuit/infer-cable-insertion-point": "^0.0.3",
    "@tscircuit/infgrid-ijump-astar": "^0.0.35",
    "@tscircuit/jlcpcb-manufacturing-specs": "git+https://github.com/tscircuit/jlcpcb-manufacturing-specs#e08af159db01a37db007e33f0a7268d0e4a279a5",
    "@tscircuit/krt-wasm": "^0.1.1",
    "@tscircuit/log-soup": "^1.0.2",
    "@tscircuit/matchpack": "^0.0.77",
    "@tscircuit/math-utils": "^0.0.36",
    "@tscircuit/miniflex": "^0.0.4",
    "@tscircuit/ngspice-spice-engine": "^0.0.20",
    "@tscircuit/props": "^0.0.618",
    "@tscircuit/schematic-match-adapt": "^0.0.18",
    "@tscircuit/schematic-trace-solver": "^0.0.129",
    "@tscircuit/solver-utils": "^0.0.16",
    "@tscircuit/soup-util": "^0.0.41",
    "@types/bun": "^1.2.16",
    "@types/debug": "^4.1.12",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@types/react-reconciler": "^0.28.9",
    "bpc-graph": "^0.0.57",
    "bun-match-svg": "0.0.12",
    "calculate-elbow": "^0.0.12",
    "chokidar-cli": "^3.0.0",
    "circuit-json": "^0.0.465",
    "circuit-json-to-bpc": "^0.0.13",
    "circuit-json-to-gltf": "^0.0.111",
    "circuit-json-to-connectivity-map": "^0.0.27",
    "circuit-json-to-spice": "^0.0.45",
    "circuit-to-svg": "^0.0.400",
    concurrently: "^9.1.2",
    "connectivity-map": "^1.0.0",
    debug: "^4.3.6",
    flatbush: "^4.5.0",
    "graphics-debug": "^0.0.95",
    howfat: "^0.3.8",
    "kicad-to-circuit-json": "^0.0.117",
    kicadts: "^0.0.53",
    "live-server": "^1.2.2",
    minicssgrid: "^0.0.9",
    "pkg-pr-new": "^0.0.37",
    poppygl: "^0.0.26",
    react: "^19.1.0",
    "react-dom": "^19.1.0",
    "schematic-symbols": "^0.0.238",
    spicets: "^0.0.4",
    spicey: "^0.0.14",
    "stack-svgs": "^0.0.1",
    "ts-expect": "^1.3.0",
    tsup: "^8.2.4"
  },
  peerDependencies: {
    "@tscircuit/capacity-autorouter": "*",
    "@tscircuit/checks": "*",
    "@tscircuit/circuit-json-util": "*",
    "@tscircuit/create-fdm-enclosure": "*",
    "@tscircuit/footprinter": "*",
    "@tscircuit/infgrid-ijump-astar": "*",
    "@tscircuit/math-utils": "*",
    "@tscircuit/props": "*",
    "@tscircuit/schematic-match-adapt": "*",
    "circuit-json-to-bpc": "*",
    "bpc-graph": "*",
    "@tscircuit/matchpack": "*",
    "circuit-json": "*",
    "circuit-json-to-connectivity-map": "*",
    "circuit-json-to-spice": "*",
    "schematic-symbols": "*",
    spicets: "*",
    typescript: "^5.0.0"
  },
  dependencies: {
    "@flatten-js/core": "^1.6.2",
    "@lume/kiwi": "^0.4.3",
    "calculate-cell-boundaries": "^0.0.13",
    "calculate-packing": "^0.0.86",
    "css-select": "5.1.0",
    "format-si-unit": "^0.0.7",
    nanoid: "^5.0.7",
    "performance-now": "^2.1.0",
    "react-reconciler": "^0.32.0",
    "svg-path-commander": "^2.1.11",
    "transformation-matrix": "^2.16.1",
    zod: "^3.25.67"
  },
  overrides: {
    "@tscircuit/circuit-json-util": "^0.0.106",
    "@tscircuit/props": "^0.0.618",
    "circuit-json": "^0.0.465"
  }
};

// lib/components/primitive-components/Group/Group_localAutoroutingCache.ts
var getFnv1aHash = (value) => {
  let hash = 2166136261;
  for (let i = 0; i < value.length; i++) {
    hash ^= value.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }
  return hash >>> 0;
};
var getSrjHash = (simpleRouteJson) => {
  const serializedSrj = JSON.stringify(simpleRouteJson);
  const hash1 = getFnv1aHash(serializedSrj);
  const hash2 = getFnv1aHash(`${serializedSrj}${hash1}`);
  return `${hash1.toString(16).padStart(8, "0")}${hash2.toString(16).padStart(8, "0")}`;
};
var getLocalAutoroutingCacheKey = (simpleRouteJson) => `routes:core@${package_default.version}:srj:${getSrjHash(simpleRouteJson)}`;
var getCachedLocalAutoroutingPhaseResult = async ({
  cacheEngine,
  cacheKey
}) => {
  if (!cacheEngine) return null;
  try {
    const cachedResult = await cacheEngine.getItem(cacheKey);
    if (!cachedResult) return null;
    const parsedResult = JSON.parse(cachedResult);
    if (!parsedResult || !Array.isArray(parsedResult.traces)) return null;
    return parsedResult;
  } catch {
    return null;
  }
};
var cacheLocalAutoroutingPhaseResult = async ({
  cacheEngine,
  cacheKey,
  result
}) => {
  if (!cacheEngine) return;
  try {
    await cacheEngine.setItem(cacheKey, JSON.stringify(result));
  } catch {
  }
};

// lib/components/primitive-components/Group/Group_phasedAutoroutingUtils.ts
function connectionIsInRoutingPhase(connection, phasePlan) {
  if (connection.routingPcbGroupId) {
    return connection.routingPcbGroupId === phasePlan.routingPcbGroupId;
  }
  for (const trace of phasePlan.traces) {
    if (!trace.source_trace_id) continue;
    if (connection.source_trace_id === trace.source_trace_id) return true;
    if (connection.name === trace.source_trace_id) return true;
    if (connection.rootConnectionName === trace.source_trace_id) return true;
    if (connection.mergedConnectionNames?.includes(trace.source_trace_id)) {
      return true;
    }
  }
  for (const net of phasePlan.nets) {
    if (!net.source_net_id) continue;
    if (connection.name === net.source_net_id) return true;
  }
  return false;
}
function Group_hasPhasedAutorouting(routingPhasePlans) {
  for (const plan of routingPhasePlans) {
    if (plan.routingPcbGroupId) return true;
    if (plan.routingPhaseIndex !== null) return true;
  }
  return false;
}
function Group_filterSimpleRouteJsonForPhase(simpleRouteJson, phasePlan) {
  const connections = [];
  for (const connection of simpleRouteJson.connections) {
    if (connectionIsInRoutingPhase(connection, phasePlan)) {
      connections.push(connection);
    }
  }
  const includedConnectionNames = /* @__PURE__ */ new Set();
  for (const connection of connections) {
    includedConnectionNames.add(connection.name);
  }
  const differentialPairs = [];
  for (const differentialPair of simpleRouteJson.differentialPairs ?? []) {
    const positiveConnectionIncluded = includedConnectionNames.has(
      differentialPair.connectionNames[0]
    );
    const negativeConnectionIncluded = includedConnectionNames.has(
      differentialPair.connectionNames[1]
    );
    if (positiveConnectionIncluded !== negativeConnectionIncluded) {
      throw new Error(
        `Differential pair "${differentialPair.connectionNames.join("/")}" cannot be split across autorouting phases`
      );
    }
    if (positiveConnectionIncluded) differentialPairs.push(differentialPair);
  }
  const buses = (simpleRouteJson.buses ?? []).map((bus) => ({
    ...bus,
    connectionNames: bus.connectionNames.filter(
      (connectionName) => includedConnectionNames.has(connectionName)
    )
  })).filter((bus) => bus.connectionNames.length > 0);
  const bounds = expandSrjBoundsToIncludeConnectionPoints({
    bounds: phasePlan.routingBounds ?? simpleRouteJson.bounds,
    connections
  });
  return {
    ...simpleRouteJson,
    bounds,
    connections,
    differentialPairs: differentialPairs.length > 0 ? differentialPairs : void 0,
    buses: buses.length > 0 ? buses : void 0
  };
}
function hasDrcTolerances(drcTolerances) {
  if (!drcTolerances) return false;
  return Object.values(drcTolerances).some((value) => value !== void 0);
}
function applyMinTraceWidthToConnections(connections, minTraceWidth) {
  if (minTraceWidth === void 0) return connections;
  return connections.map((connection) => ({
    ...connection,
    nominalTraceWidth: Math.max(
      connection.nominalTraceWidth ?? minTraceWidth,
      minTraceWidth
    ),
    width: Math.max(connection.width ?? minTraceWidth, minTraceWidth)
  }));
}
function Group_applyDrcTolerancesToSimpleRouteJson(simpleRouteJson, drcTolerances) {
  if (!hasDrcTolerances(drcTolerances)) return simpleRouteJson;
  const minTraceWidth = drcTolerances.minTraceWidth ?? simpleRouteJson.minTraceWidth;
  const minViaHoleDiameter = drcTolerances.minViaHoleDiameter ?? simpleRouteJson.minViaHoleDiameter;
  const minViaPadDiameter = drcTolerances.minViaPadDiameter ?? simpleRouteJson.minViaPadDiameter;
  const minTraceToPadEdgeClearance = drcTolerances.minTraceToPadEdgeClearance ?? simpleRouteJson.minTraceToPadEdgeClearance;
  const minViaEdgeToPadEdgeClearance = drcTolerances.minViaEdgeToPadEdgeClearance ?? simpleRouteJson.minViaEdgeToPadEdgeClearance;
  const minViaHoleEdgeToViaHoleEdgeClearance = drcTolerances.minViaHoleEdgeToViaHoleEdgeClearance ?? simpleRouteJson.minViaHoleEdgeToViaHoleEdgeClearance;
  const minPlatedHoleDrillEdgeToDrillEdgeClearance = drcTolerances.minPlatedHoleDrillEdgeToDrillEdgeClearance ?? simpleRouteJson.minPlatedHoleDrillEdgeToDrillEdgeClearance;
  const minPadEdgeToPadEdgeClearance = drcTolerances.minPadEdgeToPadEdgeClearance ?? simpleRouteJson.minPadEdgeToPadEdgeClearance;
  const minBoardEdgeClearance = drcTolerances.minBoardEdgeClearance ?? simpleRouteJson.minBoardEdgeClearance;
  return {
    ...simpleRouteJson,
    connections: applyMinTraceWidthToConnections(
      simpleRouteJson.connections,
      drcTolerances.minTraceWidth
    ),
    minTraceWidth,
    minViaDiameter: minViaPadDiameter,
    minViaHoleDiameter,
    minViaPadDiameter,
    min_via_hole_diameter: minViaHoleDiameter,
    min_via_pad_diameter: minViaPadDiameter,
    minTraceToPadEdgeClearance,
    minViaEdgeToPadEdgeClearance,
    minViaHoleEdgeToViaHoleEdgeClearance,
    minPlatedHoleDrillEdgeToDrillEdgeClearance,
    minPadEdgeToPadEdgeClearance,
    minBoardEdgeClearance
  };
}

// lib/components/primitive-components/Group/Group_syncFanoutExitsWithGlobalConnections.ts
var POINT_MATCH_TOLERANCE = 1e-6;
var pointsMatch2 = (first, second) => Math.abs(first.x - second.x) <= POINT_MATCH_TOLERANCE && Math.abs(first.y - second.y) <= POINT_MATCH_TOLERANCE && first.layer === second.layer;
var removeCompletedFanoutConnections = (simpleRouteJson, routingPcbGroupId) => {
  const connections = simpleRouteJson.connections.filter(
    (connection) => connection.routingPcbGroupId !== routingPcbGroupId
  );
  const includedConnectionNames = new Set(
    connections.map((connection) => connection.name)
  );
  const differentialPairs = [];
  for (const differentialPair of simpleRouteJson.differentialPairs ?? []) {
    const positiveConnectionIncluded = includedConnectionNames.has(
      differentialPair.connectionNames[0]
    );
    const negativeConnectionIncluded = includedConnectionNames.has(
      differentialPair.connectionNames[1]
    );
    if (positiveConnectionIncluded !== negativeConnectionIncluded) {
      throw new Error(
        `Differential pair "${differentialPair.connectionNames.join("/")}" cannot be split at a breakout boundary`
      );
    }
    if (positiveConnectionIncluded) differentialPairs.push(differentialPair);
  }
  const buses = (simpleRouteJson.buses ?? []).map((bus) => ({
    ...bus,
    connectionNames: bus.connectionNames.filter(
      (connectionName) => includedConnectionNames.has(connectionName)
    )
  })).filter((bus) => bus.connectionNames.length > 0);
  return {
    ...simpleRouteJson,
    connections,
    differentialPairs: differentialPairs.length > 0 ? differentialPairs : void 0,
    buses: buses.length > 0 ? buses : void 0
  };
};
function Group_syncFanoutExitsWithGlobalConnections({
  fanoutInputSimpleRouteJson,
  fanoutOutputSimpleRouteJson,
  baseSimpleRouteJson,
  routingPhasePlan
}) {
  const routingPcbGroupId = routingPhasePlan.routingPcbGroupId;
  if (!routingPcbGroupId) {
    return {
      downstreamSimpleRouteJson: fanoutOutputSimpleRouteJson,
      baseSimpleRouteJson,
      synchronizedBreakoutPoints: []
    };
  }
  const outputConnectionByName = new Map(
    fanoutOutputSimpleRouteJson.connections.map((connection) => [
      connection.name,
      connection
    ])
  );
  const baseConnections = baseSimpleRouteJson.connections.map((connection) => ({
    ...connection,
    pointsToConnect: connection.pointsToConnect.map((point6) => ({
      ...point6
    }))
  }));
  const synchronizedBreakoutPoints = [];
  for (const inputConnection of fanoutInputSimpleRouteJson.connections) {
    if (inputConnection.routingPcbGroupId !== routingPcbGroupId || !inputConnection.source_trace_id) {
      continue;
    }
    const outputConnection = outputConnectionByName.get(inputConnection.name);
    if (!outputConnection) continue;
    const globalConnection = baseConnections.find(
      (connection) => connection.routingPcbGroupId !== routingPcbGroupId && connection.source_trace_id === inputConnection.source_trace_id && connection.pointsToConnect.some(
        (globalPoint) => inputConnection.pointsToConnect.some(
          (inputPoint) => pointsMatch2(globalPoint, inputPoint)
        )
      )
    );
    if (!globalConnection) continue;
    const previousGlobalPointIndex = globalConnection.pointsToConnect.findIndex(
      (globalPoint) => inputConnection.pointsToConnect.some(
        (inputPoint) => pointsMatch2(globalPoint, inputPoint)
      )
    );
    if (previousGlobalPointIndex < 0) continue;
    const previousPoint = globalConnection.pointsToConnect[previousGlobalPointIndex];
    const changedPointIndex = outputConnection.pointsToConnect.findIndex(
      (outputPoint, pointIndex) => {
        const inputPoint = inputConnection.pointsToConnect[pointIndex];
        return inputPoint !== void 0 && !pointsMatch2(outputPoint, inputPoint);
      }
    );
    if (changedPointIndex < 0) continue;
    const fanoutExitPoint = outputConnection.pointsToConnect[changedPointIndex];
    const {
      layers: _previousLayers,
      terminalVia: _previousTerminalVia,
      ...previousPointWithoutLayerOverrides
    } = previousPoint;
    const synchronizedGlobalPoint = {
      ...previousPointWithoutLayerOverrides,
      x: fanoutExitPoint.x,
      y: fanoutExitPoint.y,
      layer: fanoutExitPoint.layer,
      ...fanoutExitPoint.layers ? { layers: [...fanoutExitPoint.layers] } : {},
      ...fanoutExitPoint.terminalVia ? { terminalVia: { ...fanoutExitPoint.terminalVia } } : {}
    };
    globalConnection.pointsToConnect[previousGlobalPointIndex] = synchronizedGlobalPoint;
    synchronizedBreakoutPoints.push({
      sourceTraceId: inputConnection.source_trace_id,
      routingPcbGroupId,
      previousPoint,
      fanoutExitPoint
    });
  }
  return {
    downstreamSimpleRouteJson: removeCompletedFanoutConnections(
      fanoutOutputSimpleRouteJson,
      routingPcbGroupId
    ),
    baseSimpleRouteJson: {
      ...baseSimpleRouteJson,
      connections: baseConnections
    },
    synchronizedBreakoutPoints
  };
}

// lib/components/primitive-components/Group/add-port-ids-to-traces-at-jumper-pads.ts
function getJumperPadInfos(db) {
  const padInfos = [];
  const pcbSmtpads = db.pcb_smtpad.list();
  const jumperSmtpads = pcbSmtpads.filter((pad) => {
    const component = db.pcb_component.get(pad.pcb_component_id);
    if (!component) return false;
    const sourceComponent = db.source_component.get(
      component.source_component_id
    );
    return sourceComponent?.name?.startsWith("__autoplaced_jumper");
  });
  for (const smtpad of jumperSmtpads) {
    if (smtpad.shape === "polygon") continue;
    if (!smtpad.pcb_port_id) continue;
    if (smtpad.shape === "rect" || smtpad.shape === "rotated_rect") {
      const halfWidth = smtpad.width / 2;
      const halfHeight = smtpad.height / 2;
      padInfos.push({
        pcb_port_id: smtpad.pcb_port_id,
        x: smtpad.x,
        y: smtpad.y,
        minX: smtpad.x - halfWidth,
        maxX: smtpad.x + halfWidth,
        minY: smtpad.y - halfHeight,
        maxY: smtpad.y + halfHeight
      });
    } else if (smtpad.shape === "circle") {
      const radius = smtpad.radius;
      padInfos.push({
        pcb_port_id: smtpad.pcb_port_id,
        x: smtpad.x,
        y: smtpad.y,
        minX: smtpad.x - radius,
        maxX: smtpad.x + radius,
        minY: smtpad.y - radius,
        maxY: smtpad.y + radius
      });
    }
  }
  return padInfos;
}
function findJumperPortAtPosition(padInfos, x, y, tolerance = 0.01) {
  for (const pad of padInfos) {
    if (Math.abs(pad.x - x) < tolerance && Math.abs(pad.y - y) < tolerance) {
      return pad.pcb_port_id;
    }
  }
  return void 0;
}
function findJumperPortContainingPoint(padInfos, x, y) {
  for (const pad of padInfos) {
    if (x >= pad.minX && x <= pad.maxX && y >= pad.minY && y <= pad.maxY) {
      return pad;
    }
  }
  return void 0;
}
function splitRouteAtJumperPads(route, padInfos) {
  if (route.length === 0 || padInfos.length === 0) return [route];
  const segments = [];
  let currentSegment = [];
  for (let i = 0; i < route.length; i++) {
    const point6 = route[i];
    currentSegment.push(point6);
    if (point6.route_type === "wire" && i > 0 && i < route.length - 1) {
      const padInfo = findJumperPortContainingPoint(padInfos, point6.x, point6.y);
      if (padInfo) {
        if (!point6.end_pcb_port_id) {
          point6.end_pcb_port_id = padInfo.pcb_port_id;
        }
        segments.push(currentSegment);
        const newStartPoint = { ...point6 };
        delete newStartPoint.end_pcb_port_id;
        if (!newStartPoint.start_pcb_port_id) {
          newStartPoint.start_pcb_port_id = padInfo.pcb_port_id;
        }
        currentSegment = [newStartPoint];
      }
    }
  }
  if (currentSegment.length > 0) {
    segments.push(currentSegment);
  }
  return segments;
}
function addPortIdsToTracesAtJumperPads(segments, db) {
  const padInfos = getJumperPadInfos(db);
  if (padInfos.length === 0) return segments;
  const result = [];
  for (const segment2 of segments) {
    const subSegments = splitRouteAtJumperPads(segment2, padInfos);
    for (const subSegment of subSegments) {
      if (subSegment.length > 0) {
        const firstPoint = subSegment[0];
        const lastPoint = subSegment[subSegment.length - 1];
        if (firstPoint.route_type === "wire" && !firstPoint.start_pcb_port_id) {
          const portId = findJumperPortAtPosition(
            padInfos,
            firstPoint.x,
            firstPoint.y
          );
          if (portId) {
            firstPoint.start_pcb_port_id = portId;
          }
        }
        if (lastPoint.route_type === "wire" && !lastPoint.end_pcb_port_id) {
          const portId = findJumperPortAtPosition(
            padInfos,
            lastPoint.x,
            lastPoint.y
          );
          if (portId) {
            lastPoint.end_pcb_port_id = portId;
          }
        }
        result.push(subSegment);
      }
    }
  }
  return result;
}

// lib/components/primitive-components/Group/get-accumulated-pcb-traces-with-stage-output-replacements.ts
function getAccumulatedPcbTracesWithStageOutputReplacements({
  accumulatedPcbTraces,
  stageOutputPcbTraces
}) {
  const stageOutputPcbTraceIds = new Set(
    stageOutputPcbTraces.map((trace) => trace.pcb_trace_id)
  );
  return [
    ...accumulatedPcbTraces.filter(
      (trace) => !stageOutputPcbTraceIds.has(trace.pcb_trace_id)
    ),
    ...stageOutputPcbTraces
  ];
}

// lib/components/primitive-components/Group/get-source-trace-id-for-routed-trace.ts
import { distance as distance11, pointToSegmentDistance } from "@tscircuit/math-utils";
import { ConnectivityMap as ConnectivityMap4 } from "circuit-json-to-connectivity-map";
var POINT_EPSILON = 1e-6;
function isPointOnWireSegment({
  point: point6,
  segmentStart,
  segmentEnd
}) {
  if (point6.layer && (segmentStart.layer !== point6.layer || segmentEnd.layer !== point6.layer)) {
    return false;
  }
  return pointToSegmentDistance(point6, segmentStart, segmentEnd) <= POINT_EPSILON;
}
function isPointInSmtPad({
  point: point6,
  pad,
  traceWidth = 0
}) {
  if (pad.shape === "rect" || pad.shape === "rotated_rect") {
    return Math.abs(point6.x - pad.x) <= pad.width / 2 + traceWidth / 2 && Math.abs(point6.y - pad.y) <= pad.height / 2 + traceWidth / 2;
  }
  if (pad.shape === "circle") {
    return distance11(point6, pad) <= pad.radius + traceWidth / 2;
  }
  return false;
}
function isPointInPlatedHole({
  point: point6,
  hole,
  traceWidth = 0
}) {
  return distance11(point6, hole) <= (hole.outer_diameter ?? hole.hole_diameter ?? 0) / 2 + traceWidth / 2;
}
function getEndpointSourcePortIdsFromGeometry(db, trace) {
  const sourcePortIds = /* @__PURE__ */ new Set();
  const route = trace.route;
  const endpoints = [route[0], route[route.length - 1]].filter(
    (point6) => Boolean(point6) && point6.route_type === "wire" && point6.x !== void 0 && point6.y !== void 0
  );
  for (const endpoint of endpoints) {
    for (const pcbPort of db.pcb_port.list()) {
      if (!pcbPort.source_port_id) continue;
      if (endpoint.layer && pcbPort.layers && !pcbPort.layers.includes(endpoint.layer))
        continue;
      if (distance11(endpoint, pcbPort) <= 0.01) {
        sourcePortIds.add(pcbPort.source_port_id);
      }
    }
    for (const smtpad of db.pcb_smtpad.list()) {
      if (!smtpad.pcb_port_id) continue;
      if (endpoint.layer && smtpad.layer !== endpoint.layer) continue;
      if (!isPointInSmtPad({
        point: endpoint,
        pad: smtpad,
        traceWidth: endpoint.width
      }))
        continue;
      const sourcePortId = db.pcb_port.get(smtpad.pcb_port_id)?.source_port_id;
      if (sourcePortId) sourcePortIds.add(sourcePortId);
    }
    for (const hole of db.pcb_plated_hole.list()) {
      if (!hole.pcb_port_id) continue;
      if (!isPointInPlatedHole({
        point: endpoint,
        hole,
        traceWidth: endpoint.width
      }))
        continue;
      const sourcePortId = db.pcb_port.get(hole.pcb_port_id)?.source_port_id;
      if (sourcePortId) sourcePortIds.add(sourcePortId);
    }
  }
  return [...sourcePortIds];
}
function getWireRouteEndpoints(trace) {
  const route = trace.route.filter(
    (point6) => point6.route_type === "wire" && point6.x !== void 0 && point6.y !== void 0
  );
  return [route[0], route[route.length - 1]].filter(
    (point6) => Boolean(point6)
  );
}
function getSourceIdsFromConnectedPcbTraces(db, trace) {
  const sourceIds = /* @__PURE__ */ new Set();
  const endpoints = getWireRouteEndpoints(trace);
  if (endpoints.length === 0) return [];
  for (const existingTrace of db.pcb_trace.list()) {
    if (!existingTrace.source_trace_id) continue;
    if (existingTrace.pcb_trace_id === trace.pcb_trace_id) continue;
    if (!db.source_trace.get(existingTrace.source_trace_id) && !db.source_net.get(existingTrace.source_trace_id)) {
      continue;
    }
    const existingRoute = existingTrace.route.filter(
      (point6) => point6.route_type === "wire"
    );
    for (let i = 0; i < existingRoute.length - 1; i++) {
      const segmentStart = existingRoute[i];
      const segmentEnd = existingRoute[i + 1];
      if (!segmentStart || !segmentEnd) continue;
      for (const endpoint of endpoints) {
        if (isPointOnWireSegment({
          point: endpoint,
          segmentStart,
          segmentEnd
        })) {
          sourceIds.add(existingTrace.source_trace_id);
        }
      }
    }
  }
  return [...sourceIds];
}
function getSourcePortIdsFromRoutedTrace(db, trace) {
  const sourcePortIds = /* @__PURE__ */ new Set();
  for (const point6 of trace.route) {
    for (const pcbPortId of [point6.start_pcb_port_id, point6.end_pcb_port_id]) {
      if (!pcbPortId) continue;
      const sourcePortId = db.pcb_port.get(pcbPortId)?.source_port_id;
      if (sourcePortId) sourcePortIds.add(sourcePortId);
    }
  }
  if (sourcePortIds.size > 0) return [...sourcePortIds];
  return getEndpointSourcePortIdsFromGeometry(db, trace);
}
function buildSourceConnectivityMap(sourceTraces) {
  const connMap = new ConnectivityMap4({});
  connMap.addConnections(
    sourceTraces.map((trace) => [
      trace.source_trace_id,
      ...trace.connected_source_port_ids,
      ...trace.connected_source_net_ids
    ])
  );
  return connMap;
}
function getSourceTraceIdForRoutedTrace({
  db,
  trace,
  subcircuit_id
}) {
  if (trace.source_trace_id && (db.source_trace.get(trace.source_trace_id) ?? db.source_net.get(trace.source_trace_id))) {
    return trace.source_trace_id;
  }
  const sourcePortIds = getSourcePortIdsFromRoutedTrace(db, trace);
  if (sourcePortIds.length === 0) {
    return getSourceIdsFromConnectedPcbTraces(db, trace)[0];
  }
  const sourceTraces = db.source_trace.list();
  const connMap = buildSourceConnectivityMap(sourceTraces);
  const endpointNetIds = sourcePortIds.map((sourcePortId) => connMap.getNetConnectedToId(sourcePortId)).filter((netId) => Boolean(netId));
  const exactSourceTrace = sourcePortIds.length >= 2 ? sourceTraces.find(
    (sourceTrace) => sourcePortIds.every(
      (sourcePortId) => sourceTrace.connected_source_port_ids.includes(sourcePortId)
    )
  ) : void 0;
  if (exactSourceTrace) return exactSourceTrace.source_trace_id;
  const sourceTracesInEndpointNets = sourceTraces.filter((sourceTrace) => {
    const sourceTraceNetId = connMap.getNetConnectedToId(
      sourceTrace.source_trace_id
    );
    return endpointNetIds.some((netId) => netId === sourceTraceNetId);
  });
  const connectedToEndpoint = sourceTracesInEndpointNets.filter(
    (sourceTrace) => sourceTrace.connected_source_port_ids.some(
      (sourcePortId) => sourcePortIds.includes(sourcePortId)
    )
  );
  const candidates = connectedToEndpoint.length > 0 ? connectedToEndpoint : sourceTracesInEndpointNets;
  if (candidates.length === 0) return void 0;
  return (candidates.find(
    (sourceTrace) => sourceTrace.subcircuit_id === subcircuit_id
  ) ?? candidates[0])?.source_trace_id;
}

// lib/components/primitive-components/Group/insert-autoplaced-jumpers.ts
function groupPadsByInternalConnection(pads, orientation) {
  const tolerance = 0.01;
  const groups = [];
  for (const pad of pads) {
    const key = orientation === "vertical" ? pad.center.y : pad.center.x;
    let foundGroup = false;
    for (const group of groups) {
      const groupKey = orientation === "vertical" ? group[0].center.y : group[0].center.x;
      if (Math.abs(key - groupKey) < tolerance) {
        group.push(pad);
        foundGroup = true;
        break;
      }
    }
    if (!foundGroup) {
      groups.push([pad]);
    }
  }
  return groups.filter((g) => g.length >= 2);
}
function insertAutoplacedJumpers(params) {
  const { db, output_jumpers, subcircuit_id } = params;
  for (let jumperIndex = 0; jumperIndex < output_jumpers.length; jumperIndex++) {
    const jumper = output_jumpers[jumperIndex];
    const sourceComponent = db.source_component.insert({
      ftype: "simple_chip",
      // Use internal naming convention to avoid conflicts with user-specified names
      name: `__autoplaced_jumper_${jumperIndex}`,
      supplier_part_numbers: {}
    });
    const rotation4 = jumper.orientation === "horizontal" ? 0 : 90;
    const firstPadLayer = jumper.pads[0]?.layer || jumper.pads[0]?.layers?.[0] || "top";
    const pcbComponent = db.pcb_component.insert({
      source_component_id: sourceComponent.source_component_id,
      center: jumper.center,
      rotation: rotation4,
      layer: firstPadLayer,
      width: jumper.width || 0,
      height: jumper.height || 0,
      obstructs_within_bounds: false
    });
    const padData = [];
    for (let padIndex = 0; padIndex < jumper.pads.length; padIndex++) {
      const pad = jumper.pads[padIndex];
      const pinNumber = padIndex + 1;
      const sourcePort = db.source_port.insert({
        source_component_id: sourceComponent.source_component_id,
        name: `pin${pinNumber}`,
        pin_number: pinNumber
      });
      const padLayer = pad.layer || pad.layers?.[0] || "top";
      const pcbPort = db.pcb_port.insert({
        pcb_component_id: pcbComponent.pcb_component_id,
        source_port_id: sourcePort.source_port_id,
        x: pad.center.x,
        y: pad.center.y,
        layers: [padLayer]
      });
      const pcbSmtpad = db.pcb_smtpad.insert({
        pcb_component_id: pcbComponent.pcb_component_id,
        pcb_port_id: pcbPort.pcb_port_id,
        shape: "rect",
        x: pad.center.x,
        y: pad.center.y,
        width: pad.width,
        height: pad.height,
        layer: padLayer
      });
      padData.push({
        pad,
        sourcePortId: sourcePort.source_port_id,
        pcbSmtpadId: pcbSmtpad.pcb_smtpad_id
      });
    }
    const internalGroups = groupPadsByInternalConnection(
      jumper.pads,
      jumper.orientation
    );
    for (const group of internalGroups) {
      const sourcePortIds = [];
      for (const groupPad of group) {
        const padInfo = padData.find(
          (p) => Math.abs(p.pad.center.x - groupPad.center.x) < 0.01 && Math.abs(p.pad.center.y - groupPad.center.y) < 0.01
        );
        if (padInfo) {
          sourcePortIds.push(padInfo.sourcePortId);
        }
      }
      if (sourcePortIds.length >= 2) {
        db.source_component_internal_connection.insert({
          source_component_id: sourceComponent.source_component_id,
          subcircuit_id: subcircuit_id ?? void 0,
          source_port_ids: sourcePortIds
        });
      }
    }
  }
}

// lib/components/primitive-components/Group/region-replacement/convert-pcb-trace-to-simplified-pcb-trace.ts
function convertPcbTraceToSimplifiedPcbTrace(trace) {
  return {
    ...trace,
    type: "pcb_trace",
    pcb_trace_id: trace.pcb_trace_id,
    connection_name: trace.connection_name ?? trace.source_trace_id ?? trace.pcb_trace_id,
    route: trace.route.map((point6) => ({ ...point6 }))
  };
}

// lib/components/primitive-components/Group/region-replacement/get-relevant-subcircuit-ids-for-reroute.ts
function getRelevantSubcircuitIdsForReroute(group) {
  const relevantSubcircuitIds = /* @__PURE__ */ new Set();
  if (!group.subcircuit_id) return relevantSubcircuitIds;
  relevantSubcircuitIds.add(group.subcircuit_id);
  const db = group.root?.db;
  if (!db) return relevantSubcircuitIds;
  for (const subcircuitId of getDescendantSubcircuitIds(
    db,
    group.subcircuit_id
  )) {
    relevantSubcircuitIds.add(subcircuitId);
  }
  return relevantSubcircuitIds;
}

// lib/components/primitive-components/Group/region-replacement/get-existing-pcb-traces-for-reroute.ts
function getExistingPcbTracesForReroute(group) {
  const db = group.root?.db;
  if (!db) return [];
  const relevantSubcircuitIds = getRelevantSubcircuitIdsForReroute(group);
  return db.pcb_trace.list().filter((trace) => {
    if (!trace.route || trace.route.length < 2) return false;
    if (!group.subcircuit_id) return true;
    return Boolean(
      trace.subcircuit_id && relevantSubcircuitIds.has(trace.subcircuit_id)
    );
  });
}

// lib/components/primitive-components/Group/region-replacement/get-source-trace-ids-from-reroute-name.ts
function getSourceTraceIdsFromRerouteName(value) {
  if (typeof value !== "string" || value.length === 0) return [];
  return value.split("__").flatMap((connectionNamePart) => {
    if (connectionNamePart.length === 0) return [];
    const sourceTraceIds = [connectionNamePart];
    const rerouteSuffixIndex = connectionNamePart.indexOf("_reroute_");
    if (rerouteSuffixIndex > 0) {
      sourceTraceIds.push(connectionNamePart.slice(0, rerouteSuffixIndex));
    }
    return sourceTraceIds;
  });
}

// lib/components/primitive-components/Group/region-replacement/delete-existing-pcb-traces-replaced-by.ts
function addPossibleReplacementSourceTraceId(sourceTraceIds, value) {
  if (typeof value !== "string" || value.length === 0) return;
  sourceTraceIds.add(value);
  for (const sourceTraceId of getSourceTraceIdsFromRerouteName(value)) {
    sourceTraceIds.add(sourceTraceId);
  }
}
function deleteExistingPcbTracesReplacedBy({
  group,
  outputPcbTraces,
  pcbTraceIdsToReplace = []
}) {
  const db = group.root?.db;
  if (!db) return;
  const replacementPcbTraceIds = new Set(pcbTraceIdsToReplace);
  const replacementSourceTraceIds = /* @__PURE__ */ new Set();
  for (const trace of outputPcbTraces) {
    if (trace.type !== "pcb_trace") continue;
    const replacementTrace = trace;
    replacementPcbTraceIds.add(replacementTrace.pcb_trace_id);
    addPossibleReplacementSourceTraceId(
      replacementSourceTraceIds,
      replacementTrace.source_trace_id
    );
    addPossibleReplacementSourceTraceId(
      replacementSourceTraceIds,
      replacementTrace.connection_name
    );
    addPossibleReplacementSourceTraceId(
      replacementSourceTraceIds,
      replacementTrace.rootConnectionName
    );
  }
  if (replacementPcbTraceIds.size === 0 && replacementSourceTraceIds.size === 0) {
    return;
  }
  const tracesToDelete = getExistingPcbTracesForReroute(group).filter(
    (trace) => replacementPcbTraceIds.has(trace.pcb_trace_id) || Boolean(
      trace.source_trace_id && trace.subcircuit_id === group.subcircuit_id && replacementSourceTraceIds.has(trace.source_trace_id)
    )
  );
  if (tracesToDelete.length === 0) return;
  const deletedPcbTraceIds = new Set(
    tracesToDelete.map((trace) => trace.pcb_trace_id)
  );
  for (const via of db.pcb_via.list()) {
    if (via.pcb_trace_id && deletedPcbTraceIds.has(via.pcb_trace_id)) {
      db.pcb_via.delete(via.pcb_via_id);
    }
  }
  for (const trace of tracesToDelete) {
    db.pcb_trace.delete(trace.pcb_trace_id);
  }
}

// lib/components/primitive-components/Group/region-replacement/get-existing-simplified-pcb-traces-for-reroute.ts
function getExistingSimplifiedPcbTracesForReroute(group) {
  return getExistingPcbTracesForReroute(group).map(
    convertPcbTraceToSimplifiedPcbTrace
  );
}

// lib/components/primitive-components/Group/split-pcb-traces-on-jumper-segments.ts
function splitPcbTracesOnJumperSegments(route) {
  const jumperRoutes = route.filter(
    (p) => p.route_type === "jumper"
  );
  const wireAndViaRoutes = route.filter((p) => p.route_type !== "jumper");
  if (jumperRoutes.length === 0) {
    return null;
  }
  const splitRanges = [];
  for (const jumperRoute of jumperRoutes) {
    const jumperStart = jumperRoute.start;
    const jumperEnd = jumperRoute.end;
    let startIdx = -1;
    let endIdx = -1;
    let minStartDist = Infinity;
    let minEndDist = Infinity;
    for (let i = 0; i < wireAndViaRoutes.length; i++) {
      const p = wireAndViaRoutes[i];
      if (p.route_type !== "wire") continue;
      const distToStart = Math.hypot(p.x - jumperStart.x, p.y - jumperStart.y);
      const distToEnd = Math.hypot(p.x - jumperEnd.x, p.y - jumperEnd.y);
      if (distToStart < minStartDist) {
        minStartDist = distToStart;
        startIdx = i;
      }
      if (distToEnd < minEndDist) {
        minEndDist = distToEnd;
        endIdx = i;
      }
    }
    if (startIdx > endIdx) {
      ;
      [startIdx, endIdx] = [endIdx, startIdx];
    }
    if (startIdx >= 0 && endIdx >= 0 && startIdx !== endIdx) {
      splitRanges.push({ startIdx, endIdx });
    }
  }
  splitRanges.sort((a, b) => a.startIdx - b.startIdx);
  const segments = [];
  let currentStart = 0;
  for (const range of splitRanges) {
    if (currentStart < range.startIdx) {
      segments.push(wireAndViaRoutes.slice(currentStart, range.startIdx + 1));
    }
    currentStart = range.endIdx;
  }
  if (currentStart < wireAndViaRoutes.length) {
    segments.push(wireAndViaRoutes.slice(currentStart));
  }
  return segments;
}

// lib/components/primitive-components/Group/utils/computeCenterFromAnchorPosition.ts
function computeCenterFromAnchorPosition(anchorPosition, ctx) {
  const { width, height, pcbAnchorAlignment } = ctx;
  if (!pcbAnchorAlignment) return anchorPosition;
  const alignment = pcbAnchorAlignment;
  if (typeof width !== "number" || typeof height !== "number") {
    return anchorPosition;
  }
  const ax = anchorPosition.x;
  const ay = anchorPosition.y;
  switch (alignment) {
    case "top_left":
      return { x: ax + width / 2, y: ay - height / 2 };
    case "top_center":
      return { x: ax, y: ay - height / 2 };
    case "top_right":
      return { x: ax - width / 2, y: ay - height / 2 };
    case "center_left":
      return { x: ax + width / 2, y: ay };
    case "center_right":
      return { x: ax - width / 2, y: ay };
    case "bottom_left":
      return { x: ax + width / 2, y: ay + height / 2 };
    case "bottom_center":
      return { x: ax, y: ay + height / 2 };
    case "bottom_right":
      return { x: ax - width / 2, y: ay + height / 2 };
    default:
      return anchorPosition;
  }
}

// lib/components/primitive-components/Group/Group.ts
var getDistanceToPoint = (routePoint, targetPoint) => {
  const position = getRoutePointPosition(routePoint);
  return Math.hypot(position.x - targetPoint.x, position.y - targetPoint.y);
};
var platformAllowsLegacyAutorouters = (group) => {
  const platform = group.root?.platform;
  return platform !== void 0 && "allowLegacyAutorouters" in platform && platform.allowLegacyAutorouters === true;
};
var getDisabledLegacyGroupAutorouterPreset = (group) => {
  if (platformAllowsLegacyAutorouters(group)) return null;
  const resolvedGroupAutorouter = group._parsedProps.autorouter ?? group.getInheritedProperty("autorouter");
  return getLegacyAutorouterPreset(resolvedGroupAutorouter);
};
var getDisabledLegacyAutorouterPreset = (group) => {
  if (platformAllowsLegacyAutorouters(group)) return null;
  const legacyGroupAutorouterPreset = getDisabledLegacyGroupAutorouterPreset(group);
  if (legacyGroupAutorouterPreset) return legacyGroupAutorouterPreset;
  const routingPhaseAutorouters = group.selectAll("autoroutingphase").map((autoroutingPhase) => autoroutingPhase._parsedProps.autorouter);
  for (const autorouter of routingPhaseAutorouters) {
    const legacyAutorouterPreset = getLegacyAutorouterPreset(autorouter);
    if (legacyAutorouterPreset) return legacyAutorouterPreset;
  }
  return null;
};
var reversePcbTraceRoute = (route) => route.slice().reverse().map((point6) => {
  if (point6.route_type === "via") {
    return { ...point6 };
  }
  if (point6.route_type === "through_pad") {
    return {
      ...point6,
      start: point6.end,
      end: point6.start,
      start_layer: point6.end_layer,
      end_layer: point6.start_layer
    };
  }
  return { ...point6 };
});
var ensureRouteStartsAtSourceTraceStart = ({
  db,
  route,
  sourceTraceId
}) => {
  if (!sourceTraceId || route.length < 2) return route;
  const sourceTrace = db.source_trace.get(sourceTraceId);
  const firstSourcePortId = sourceTrace?.connected_source_port_ids[0];
  if (!firstSourcePortId) return route;
  const firstPcbPort = db.pcb_port.list().find((port) => port.source_port_id === firstSourcePortId);
  if (!firstPcbPort) return route;
  const firstPoint = route[0];
  const lastPoint = route[route.length - 1];
  return getDistanceToPoint(lastPoint, firstPcbPort) < getDistanceToPoint(firstPoint, firstPcbPort) ? reversePcbTraceRoute(route) : route;
};
var Group5 = class extends NormalComponent3 {
  pcb_group_id = null;
  schematic_group_id = null;
  subcircuit_id = null;
  _hasStartedAsyncAutorouting = false;
  _pcbPlacementDrcErrorCount = null;
  _pcbPlacementDrcCheckError = null;
  _pcbPlacementDrcChecksPending = false;
  _isInflatedFromCircuitJson = false;
  _isolatedCircuitJson = null;
  get _isIsolatedSubcircuit() {
    return Boolean(this.getInheritedProperty("_subcircuitCachingEnabled"));
  }
  _normalComponentNameMap = null;
  /**
   * Returns a cached map of component names to NormalComponent instances within this subcircuit.
   * The map is built lazily on first access and cached for subsequent calls.
   */
  getNormalComponentNameMap() {
    if (this._normalComponentNameMap) {
      return this._normalComponentNameMap;
    }
    const nameMap = /* @__PURE__ */ new Map();
    const collectNamedComponents = (component) => {
      if (component._isNormalComponent && component.name) {
        const componentsWithSameName = nameMap.get(component.name);
        if (componentsWithSameName) {
          componentsWithSameName.push(component);
        } else {
          nameMap.set(component.name, [component]);
        }
      }
      for (const child of component.children) {
        if (!child.isSubcircuit) collectNamedComponents(child);
      }
    };
    for (const child of this.children) {
      if (!child.isSubcircuit) collectNamedComponents(child);
    }
    this._normalComponentNameMap = nameMap;
    return nameMap;
  }
  _asyncAutoroutingResult = null;
  get config() {
    return {
      zodProps: groupProps,
      componentName: "Group"
    };
  }
  initPorts(opts = {}) {
    const schPinArrangement = this._getSchematicPortArrangement();
    const hasUnresolvedNamedPins = this._parsedProps?.showAsSchematicBox && !this._parsedProps.pinLabels && schPinArrangement && getPinsFromPortArrangement(schPinArrangement).some(
      (pin) => typeof pin === "string"
    );
    if (hasUnresolvedNamedPins) return;
    super.initPorts(opts);
  }
  _getSchematicPortArrangementFromPortDirections() {
    if (!this._parsedProps?.showAsSchematicBox) return null;
    const sidePins = {
      leftSide: [],
      rightSide: [],
      topSide: [],
      bottomSide: []
    };
    for (const port of this.children) {
      if (port.componentName !== "Port") continue;
      const direction = port._parsedProps.direction;
      if (!direction) continue;
      const pin = port._parsedProps.pinNumber ?? port._parsedProps.name ?? void 0;
      if (pin === void 0) continue;
      const side = direction === "left" ? "leftSide" : direction === "right" ? "rightSide" : direction === "up" ? "topSide" : "bottomSide";
      sidePins[side].push(pin);
    }
    if (sidePins.leftSide.length === 0 && sidePins.rightSide.length === 0 && sidePins.topSide.length === 0 && sidePins.bottomSide.length === 0) {
      return null;
    }
    const arrangement = {};
    if (sidePins.leftSide.length > 0) {
      arrangement.leftSide = {
        pins: sidePins.leftSide,
        direction: "top-to-bottom"
      };
    }
    if (sidePins.rightSide.length > 0) {
      arrangement.rightSide = {
        pins: sidePins.rightSide,
        direction: "top-to-bottom"
      };
    }
    if (sidePins.topSide.length > 0) {
      arrangement.topSide = {
        pins: sidePins.topSide,
        direction: "left-to-right"
      };
    }
    if (sidePins.bottomSide.length > 0) {
      arrangement.bottomSide = {
        pins: sidePins.bottomSide,
        direction: "left-to-right"
      };
    }
    return arrangement;
  }
  _getSchematicPortArrangement() {
    return super._getSchematicPortArrangement() ?? this._getSchematicPortArrangementFromPortDirections();
  }
  _ensureSchematicBoxPortsFromConnections() {
    if (!this._parsedProps?.showAsSchematicBox) return;
    if (!this._parsedProps?.connections) return;
    for (const [pinName, target] of Object.entries(
      this._parsedProps.connections
    )) {
      const existingPort = this.children.find(
        (child) => child.componentName === "Port" && child.isMatchingAnyOf([pinName])
      );
      if (existingPort) continue;
      const pinNumberMatch = pinName.match(/^pin(\d+)$/i) ?? pinName.match(/^(\d+)$/);
      const pinNumber = pinNumberMatch ? Number(pinNumberMatch[1]) : void 0;
      this.add(
        new Port({
          name: pinName,
          pinNumber,
          connectsTo: target
        })
      );
    }
  }
  doInitialInitializePortsFromChildren() {
    super.doInitialInitializePortsFromChildren();
    this._ensureSchematicBoxPortsFromConnections();
  }
  updateInitializePortsFromChildren() {
    super.updateInitializePortsFromChildren();
    this._ensureSchematicBoxPortsFromConnections();
  }
  _getPrimaryPinCount() {
    const superPinCount = super._getPrimaryPinCount();
    if (!this._parsedProps?.showAsSchematicBox) return superPinCount;
    const directPortPinNumbers = this.children.filter((child) => child.componentName === "Port").map((port) => port._parsedProps.pinNumber).filter((pinNumber) => pinNumber !== void 0);
    if (directPortPinNumbers.length === 0) return superPinCount;
    return Math.max(superPinCount, ...directPortPinNumbers);
  }
  _getPinLabelsFromPorts() {
    if (!this._parsedProps?.showAsSchematicBox) {
      return super._getPinLabelsFromPorts();
    }
    return getGroupSchematicBoxPinLabels(this);
  }
  doInitialSourceGroupRender() {
    const { db } = this.root;
    const hasExplicitName = typeof this._parsedProps.name === "string" && this._parsedProps.name.length > 0;
    const source_group = db.source_group.insert({
      name: this.name,
      is_subcircuit: this.isSubcircuit,
      was_automatically_named: !hasExplicitName
    });
    this.source_group_id = source_group.source_group_id;
    if (this.isSubcircuit) {
      this.subcircuit_id = `subcircuit_${source_group.source_group_id}`;
      db.source_group.update(source_group.source_group_id, {
        subcircuit_id: this.subcircuit_id
      });
    }
  }
  doInitialSourceRender() {
    const { db } = this.root;
    for (const child of this.getDescendants()) {
      if (child.getGroup()?.source_group_id !== this.source_group_id) continue;
      db.source_component.update(child.source_component_id, {
        source_group_id: this.source_group_id
      });
    }
    if (!this.isSubcircuit) return;
    const disabledLegacyAutorouterPreset = getDisabledLegacyAutorouterPreset(this);
    if (!disabledLegacyAutorouterPreset) return;
    db.source_property_ignored_warning.insert({
      source_component_id: this.source_component_id ?? "",
      property_name: "autorouter",
      subcircuit_id: this.subcircuit_id ?? void 0,
      error_type: "source_property_ignored_warning",
      message: `The "${disabledLegacyAutorouterPreset}" autorouter is deprecated, so autorouting has been disabled for this subcircuit. Use the default autorouter and introduce <autoroutingphase /> or <fanout /> elements as needed.`
    });
  }
  doInitialSourceParentAttachment() {
    const { db } = this.root;
    const parentGroup = this.parent?.getGroup?.();
    if (parentGroup?.source_group_id) {
      db.source_group.update(this.source_group_id, {
        parent_source_group_id: parentGroup.source_group_id
      });
    }
    if (!this.isSubcircuit) return;
    const parent_subcircuit_id = this.parent?.getSubcircuit?.()?.subcircuit_id;
    if (!parent_subcircuit_id) return;
    db.source_group.update(this.source_group_id, {
      parent_subcircuit_id
    });
  }
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const groupProps2 = props;
    const hasOutline = groupProps2.outline && groupProps2.outline.length > 0;
    const numericOutline = hasOutline ? groupProps2.outline.map((point6) => ({
      x: distance12.parse(point6.x),
      y: distance12.parse(point6.y)
    })) : void 0;
    const ctx = this.props;
    const anchorPosition = this._getGlobalPcbPositionBeforeLayout();
    const center = computeCenterFromAnchorPosition(anchorPosition, ctx);
    const pcb_group = db.pcb_group.insert({
      is_subcircuit: this.isSubcircuit,
      subcircuit_id: this.subcircuit_id ?? this.getSubcircuit()?.subcircuit_id,
      name: this.name,
      anchor_position: anchorPosition,
      center,
      ...hasOutline ? { outline: numericOutline } : { width: 0, height: 0 },
      pcb_component_ids: [],
      source_group_id: this.source_group_id,
      autorouter_configuration: props.autorouter ? {
        trace_clearance: props.autorouter.traceClearance
      } : void 0,
      anchor_alignment: props.pcbAnchorAlignment ?? null
    });
    this.pcb_group_id = pcb_group.pcb_group_id;
    for (const child of this.children) {
      db.pcb_component.update(child.pcb_component_id, {
        pcb_group_id: pcb_group.pcb_group_id
      });
    }
  }
  doInitialPcbPrimitiveRender() {
    this.calculatePcbGroupBounds();
  }
  calculatePcbGroupBounds(pcbContentBounds) {
    if (!this.pcb_group_id) return;
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const props = this._parsedProps;
    const hasOutline = props.outline && props.outline.length > 0;
    const hasExplicitPositioning = this._parsedProps.pcbX !== void 0 || this._parsedProps.pcbY !== void 0;
    if (hasOutline) {
      const numericOutline = props.outline.map((point6) => ({
        x: distance12.parse(point6.x),
        y: distance12.parse(point6.y)
      }));
      const outlineBounds = getBoundsFromPoints6(numericOutline);
      if (!outlineBounds) return;
      const centerX2 = (outlineBounds.minX + outlineBounds.maxX) / 2;
      const centerY2 = (outlineBounds.minY + outlineBounds.maxY) / 2;
      const center2 = hasExplicitPositioning ? db.pcb_group.get(this.pcb_group_id)?.center ?? {
        x: centerX2,
        y: centerY2
      } : { x: centerX2, y: centerY2 };
      db.pcb_group.update(this.pcb_group_id, {
        center: center2
      });
      return;
    }
    let contentBounds = pcbContentBounds;
    if (!contentBounds) {
      const bounds = getBoundsOfPcbComponents(this.children);
      contentBounds = {
        center: {
          x: (bounds.minX + bounds.maxX) / 2,
          y: (bounds.minY + bounds.maxY) / 2
        },
        width: bounds.width,
        height: bounds.height
      };
    }
    let { width, height } = contentBounds;
    let { x: centerX, y: centerY } = contentBounds.center;
    if (this.isSubcircuit || this.isRoutingDirective) {
      const { padLeft, padRight, padTop, padBottom } = this._resolvePcbPadding();
      width += padLeft + padRight;
      height += padTop + padBottom;
      centerX += (padRight - padLeft) / 2;
      centerY += (padTop - padBottom) / 2;
    }
    const resolvedWidth = Number(props.width ?? width);
    const resolvedHeight = Number(props.height ?? height);
    const existingPcbGroup = db.pcb_group.get(this.pcb_group_id);
    const existingCenter = existingPcbGroup?.center ?? {
      x: centerX,
      y: centerY
    };
    const shouldUsePcbContentCenter = this.isSubcircuit || pcbContentBounds !== void 0;
    let center = hasExplicitPositioning ? shouldUsePcbContentCenter ? {
      x: props.width === void 0 ? centerX : existingCenter.x,
      y: props.height === void 0 ? centerY : existingCenter.y
    } : existingCenter : { x: centerX, y: centerY };
    if (hasExplicitPositioning && props.pcbAnchorAlignment) {
      const anchorPosition = this._getGlobalPcbPositionBeforeLayout();
      const anchorAlignedCenter = computeCenterFromAnchorPosition(
        anchorPosition,
        {
          ...this.props,
          width: resolvedWidth,
          height: resolvedHeight
        }
      );
      if (Math.abs(anchorAlignedCenter.x - center.x) > 1e-6 || Math.abs(anchorAlignedCenter.y - center.y) > 1e-6) {
        this._repositionOnPcb(anchorAlignedCenter);
      }
      center = anchorAlignedCenter;
    }
    db.pcb_group.update(this.pcb_group_id, {
      width: resolvedWidth,
      height: resolvedHeight,
      center
    });
  }
  updatePcbPrimitiveRender() {
    this.calculatePcbGroupBounds();
  }
  unnamedElementCounter = {};
  getNextAvailableName(elm) {
    this.unnamedElementCounter[elm.lowercaseComponentName] ??= 1;
    return `unnamed_${elm.lowercaseComponentName}${this.unnamedElementCounter[elm.lowercaseComponentName]++}`;
  }
  _resolvePcbPadding() {
    const props = this._parsedProps;
    const layout = props.pcbLayout;
    const getPaddingValue = (key) => {
      const layoutValue = layout?.[key];
      const propsValue = props[key];
      if (typeof layoutValue === "number") return layoutValue;
      if (typeof propsValue === "number") return propsValue;
      return void 0;
    };
    const generalPadding = getPaddingValue("padding") ?? 0;
    const paddingX = getPaddingValue("paddingX");
    const paddingY = getPaddingValue("paddingY");
    const padLeft = getPaddingValue("paddingLeft") ?? paddingX ?? generalPadding;
    const padRight = getPaddingValue("paddingRight") ?? paddingX ?? generalPadding;
    const padTop = getPaddingValue("paddingTop") ?? paddingY ?? generalPadding;
    const padBottom = getPaddingValue("paddingBottom") ?? paddingY ?? generalPadding;
    return { padLeft, padRight, padTop, padBottom };
  }
  doInitialCreateTraceHintsFromProps() {
    const { _parsedProps: props } = this;
    const { db } = this.root;
    const groupProps2 = props;
    if (!this.isSubcircuit) return;
    const manualTraceHints = groupProps2.manualEdits?.manual_trace_hints;
    if (!manualTraceHints) return;
    for (const manualTraceHint of manualTraceHints) {
      this.add(
        new TraceHint({
          for: manualTraceHint.pcb_port_selector,
          offsets: manualTraceHint.offsets
        })
      );
    }
  }
  doInitialSourceAddConnectivityMapKey() {
    Group_doInitialSourceAddConnectivityMapKey(this);
    Group_doInitialAssignSchematicSheetToConnectedComponents(this);
  }
  _areChildSubcircuitsRouted() {
    const subcircuitChildren = this._findDirectChildSubcircuits();
    for (const subcircuitChild of subcircuitChildren) {
      if (subcircuitChild._isInflatedFromCircuitJson) continue;
      if (!subcircuitChild._hasTracesToRoute()) continue;
      if (subcircuitChild._shouldRouteAsync() && !subcircuitChild._asyncAutoroutingResult) {
        return false;
      }
    }
    return true;
  }
  /**
   * Find subcircuit groups that are direct children of this subcircuit in
   * the component tree. Unlike selectAll("group"), this walks the children
   * array directly because the css-select adapter used by selectAll
   * intentionally skips subcircuit boundaries.
   */
  _findDirectChildSubcircuits() {
    const result = [];
    const visit = (children) => {
      for (const child of children) {
        if (child.isSubcircuit && child !== this) {
          result.push(child);
        } else {
          visit(child.children);
        }
      }
    };
    visit(this.children);
    return result;
  }
  _shouldRouteAsync() {
    if (this._isLegacyAutorouterDisabled()) return false;
    const autorouter = this._getAutorouterConfig();
    if (autorouter.groupMode === "sequential-trace") return false;
    if (autorouter.local && autorouter.groupMode === "subcircuit") return true;
    if (!autorouter.local) return true;
    return false;
  }
  _getRoutingPhasePlans() {
    return Group_getRoutingPhasePlans(this);
  }
  _hasTracesToRoute() {
    const debug13 = Debug15("tscircuit:core:_hasTracesToRoute");
    const routingPhasePlans = this._getRoutingPhasePlans();
    let traceCount = 0;
    let hasReroutePhaseWithRegion = false;
    for (const routingPhasePlan of routingPhasePlans) {
      traceCount += routingPhasePlan.traces.length;
      hasReroutePhaseWithRegion ||= Boolean(
        routingPhasePlan.reroute && routingPhasePlan.region
      );
    }
    debug13(`[${this.getString()}] has ${traceCount} traces to route`);
    if (traceCount > 0) return true;
    if (hasReroutePhaseWithRegion) {
      const existingTraceCount = getExistingPcbTracesForReroute(this).length;
      debug13(
        `[${this.getString()}] has ${existingTraceCount} existing pcb traces available for reroute`
      );
      return existingTraceCount > 0;
    }
    return false;
  }
  async _runEffectMakeHttpAutoroutingRequest() {
    const { db } = this.root;
    const debug13 = Debug15("tscircuit:core:_runEffectMakeHttpAutoroutingRequest");
    const props = this._parsedProps;
    const autorouterConfig = this._getAutorouterConfig();
    const serverUrl = autorouterConfig.serverUrl;
    const serverMode = autorouterConfig.serverMode;
    const fetchWithDebug = (url, options) => {
      debug13("fetching", url);
      if (options.headers) {
        options.headers["Tscircuit-Core-Version"] = this.root?.getCoreVersion();
      }
      return fetch(url, options);
    };
    const pcbAndSourceCircuitJson = this.root.db.toArray().filter(
      (element) => {
        return element.type.startsWith("source_") || element.type.startsWith("pcb_");
      }
    );
    if (serverMode === "solve-endpoint") {
      if (this.props.autorouter?.inputFormat === "simplified") {
        const preferredTraceWidth = props.defaultTraceWidth ?? props.nominalTraceWidth;
        const { simpleRouteJson } = getSimpleRouteJsonFromCircuitJson({
          db,
          minTraceWidth: Number(props.minTraceWidth ?? 0.15),
          nominalTraceWidth: preferredTraceWidth != null ? Number(preferredTraceWidth) : void 0,
          subcircuit_id: this.subcircuit_id,
          subcircuitComponent: this
        });
        simpleRouteJson.allowViaInPad = autorouterConfig.allowViaInPad;
        const { autorouting_result: autorouting_result2 } = await fetchWithDebug(
          `${serverUrl}/autorouting/solve`,
          {
            method: "POST",
            body: JSON.stringify({
              input_simple_route_json: simpleRouteJson,
              subcircuit_id: this.subcircuit_id
            }),
            headers: {
              "Content-Type": "application/json"
            }
          }
        ).then((r) => r.json());
        this._asyncAutoroutingResult = autorouting_result2;
        this._markDirty("PcbTraceRender");
        return;
      }
      const { autorouting_result } = await fetchWithDebug(
        `${serverUrl}/autorouting/solve`,
        {
          method: "POST",
          body: JSON.stringify({
            input_circuit_json: pcbAndSourceCircuitJson,
            subcircuit_id: this.subcircuit_id
          }),
          headers: {
            "Content-Type": "application/json"
          }
        }
      ).then((r) => r.json());
      this._asyncAutoroutingResult = autorouting_result;
      this._markDirty("PcbTraceRender");
      return;
    }
    const { autorouting_job } = await fetchWithDebug(
      `${serverUrl}/autorouting/jobs/create`,
      {
        method: "POST",
        body: JSON.stringify({
          input_circuit_json: pcbAndSourceCircuitJson,
          provider: "freerouting",
          autostart: true,
          display_name: this.root?.name,
          subcircuit_id: this.subcircuit_id,
          server_cache_enabled: autorouterConfig.serverCacheEnabled
        }),
        headers: {
          "Content-Type": "application/json"
        }
      }
    ).then((r) => r.json());
    while (true) {
      const { autorouting_job: job } = await fetchWithDebug(
        `${serverUrl}/autorouting/jobs/get`,
        {
          method: "POST",
          body: JSON.stringify({
            autorouting_job_id: autorouting_job.autorouting_job_id
          }),
          headers: { "Content-Type": "application/json" }
        }
      ).then((r) => r.json());
      if (job.is_finished) {
        const { autorouting_job_output } = await fetchWithDebug(
          `${serverUrl}/autorouting/jobs/get_output`,
          {
            method: "POST",
            body: JSON.stringify({
              autorouting_job_id: autorouting_job.autorouting_job_id
            }),
            headers: { "Content-Type": "application/json" }
          }
        ).then((r) => r.json());
        this._asyncAutoroutingResult = {
          output_pcb_traces: autorouting_job_output.output_pcb_traces
        };
        this._markDirty("PcbTraceRender");
        break;
      }
      if (job.has_error) {
        const err = new AutorouterError(
          `Autorouting job failed: ${JSON.stringify(job.error)}`
        );
        db.pcb_autorouting_error.insert({
          pcb_error_id: autorouting_job.autorouting_job_id,
          error_type: "pcb_autorouting_error",
          message: err.message
        });
        throw err;
      }
      await new Promise((resolve) => setTimeout(resolve, 100));
    }
  }
  /**
   * Run local autorouting using the CapacityMeshAutorouter
   */
  async _runLocalAutorouting() {
    const { db } = this.root;
    const props = this._parsedProps;
    const debug13 = Debug15("tscircuit:core:_runLocalAutorouting");
    debug13(`[${this.getString()}] starting local autorouting`);
    const autorouterConfig = this._getAutorouterConfig();
    const isLaserPrefabPreset = this._isLaserPrefabAutorouter(autorouterConfig);
    const isAutoJumperPreset = this._isAutoJumperAutorouter(autorouterConfig);
    const isSingleLayerBoard = this._getSubcircuitLayerCount() === 1;
    const minTraceWidth = Number(props.minTraceWidth ?? 0.15);
    const nominalTraceWidth = Number(
      props.defaultTraceWidth ?? props.nominalTraceWidth ?? minTraceWidth
    );
    const routingPhasePlans = this._getRoutingPhasePlans();
    const hasPhasedAutorouting = Group_hasPhasedAutorouting(routingPhasePlans);
    const routingStages = routingPhasePlans.flatMap((routingPhasePlan) => {
      const phaseAutorouterConfig = routingPhasePlan.autorouter ? getPresetAutoroutingConfig(
        routingPhasePlan.autorouter,
        this.root?.platform
      ) : autorouterConfig;
      const stages = getLocalAutoroutingStages(
        phaseAutorouterConfig,
        this.root?.platform
      );
      return stages.map((stage, phaseStageIndex) => ({
        ...stage,
        routingPhasePlan,
        phaseStageIndex,
        phaseStageCount: stages.length
      }));
    });
    const hasFanoutStage = routingStages.some(
      ({ autorouterConfig: autorouterConfig2 }) => ["fanout", "single_layer_fanout"].includes(autorouterConfig2.preset ?? "")
    );
    const fanoutPourNetMap = hasFanoutStage ? Group_getFanoutPourNetMap(this, routingPhasePlans) : void 0;
    let { simpleRouteJson: baseSimpleRouteJson } = getSimpleRouteJsonFromCircuitJson({
      db,
      minTraceWidth,
      nominalTraceWidth,
      subcircuit_id: this.subcircuit_id,
      subcircuitComponent: this,
      fanoutPourNetMap
    });
    const outputTraces = [];
    const outputJumpers = [];
    const pcbTraceIdsToDelete = /* @__PURE__ */ new Set();
    const existingRerouteSeedTraces = getExistingSimplifiedPcbTracesForReroute(this);
    const traceMatchesRoutingPhase = (trace, routingPhasePlan) => {
      const connectionName = trace.connection_name ?? trace.pcb_trace_id;
      const sourceTraceIds = /* @__PURE__ */ new Set([
        connectionName,
        trace.pcb_trace_id,
        ...getSourceTraceIdsFromRerouteName(connectionName),
        ...getSourceTraceIdsFromRerouteName(trace.pcb_trace_id)
      ]);
      return baseSimpleRouteJson.connections.some((connection) => {
        if (!connectionIsInRoutingPhase(connection, routingPhasePlan)) {
          return false;
        }
        return sourceTraceIds.has(connection.name) || (connection.source_trace_id ? sourceTraceIds.has(connection.source_trace_id) : false) || (connection.rootConnectionName ? sourceTraceIds.has(connection.rootConnectionName) : false) || connection.mergedConnectionNames?.some(
          (name) => sourceTraceIds.has(name)
        );
      });
    };
    let previousStageOutputSimpleRouteJson;
    for (const {
      routingPhasePlan,
      autorouterConfig: phaseAutorouterConfig,
      strategy: localAutorouterStrategy,
      usesPreviousStageOutput,
      phaseStageIndex,
      phaseStageCount
    } of routingStages) {
      if (!usesPreviousStageOutput) {
        previousStageOutputSimpleRouteJson = void 0;
      }
      if (usesPreviousStageOutput && previousStageOutputSimpleRouteJson === void 0) {
        throw new Error(
          "Autorouting follow-up stage is missing the preceding stage output"
        );
      }
      let simpleRouteJson = previousStageOutputSimpleRouteJson ?? baseSimpleRouteJson;
      const isRegionReroutePhase = Boolean(
        routingPhasePlan.reroute && routingPhasePlan.region
      );
      const isConnectionReroutePhase = Boolean(
        routingPhasePlan.reroute && !routingPhasePlan.region && routingPhasePlan.traces.length > 0
      );
      const isReroutePhase = isRegionReroutePhase || isConnectionReroutePhase;
      const rerouteOriginalSrj = isRegionReroutePhase ? {
        ...baseSimpleRouteJson,
        traces: [...existingRerouteSeedTraces, ...outputTraces]
      } : null;
      if (!usesPreviousStageOutput && isRegionReroutePhase && rerouteOriginalSrj) {
        simpleRouteJson = getRerouteSimpleRouteJson(
          rerouteOriginalSrj,
          {
            shape: "rect",
            ...routingPhasePlan.region
          }
        );
      } else if (!usesPreviousStageOutput && isConnectionReroutePhase) {
        const phaseInput = Group_filterSimpleRouteJsonForPhase(
          baseSimpleRouteJson,
          routingPhasePlan
        );
        simpleRouteJson = {
          ...phaseInput,
          traces: [
            ...phaseInput.traces ?? [],
            ...outputTraces.filter(
              (trace) => !traceMatchesRoutingPhase(trace, routingPhasePlan)
            )
          ]
        };
      } else if (!usesPreviousStageOutput && hasPhasedAutorouting) {
        const phaseInput = Group_filterSimpleRouteJsonForPhase(
          baseSimpleRouteJson,
          routingPhasePlan
        );
        simpleRouteJson = {
          ...phaseInput,
          traces: [...phaseInput.traces ?? [], ...outputTraces]
        };
      }
      simpleRouteJson = Group_applyDrcTolerancesToSimpleRouteJson(
        simpleRouteJson,
        routingPhasePlan.drcTolerances
      );
      simpleRouteJson.allowViaInPad = phaseAutorouterConfig.allowViaInPad;
      if ((hasPhasedAutorouting || isReroutePhase) && simpleRouteJson.connections.length === 0) {
        continue;
      }
      const phaseIsAutoJumperPreset = routingPhasePlan.autorouter !== void 0 ? this._isAutoJumperAutorouter(phaseAutorouterConfig) : isAutoJumperPreset;
      const phaseIsLaserPrefabPreset = routingPhasePlan.autorouter !== void 0 ? this._isLaserPrefabAutorouter(phaseAutorouterConfig) : isLaserPrefabPreset;
      if (phaseIsAutoJumperPreset) {
        simpleRouteJson.allowJumpers = true;
        if (phaseAutorouterConfig.availableJumperTypes) {
          simpleRouteJson.availableJumperTypes = phaseAutorouterConfig.availableJumperTypes;
        }
      }
      const fanoutMode = phaseAutorouterConfig.preset;
      if (fanoutMode === "fanout" || fanoutMode === "single_layer_fanout") {
        const breakoutPoints = routingPhasePlan.routingPcbGroupId ? db.pcb_breakout_point.list().filter(
          (point6) => point6.pcb_group_id === routingPhasePlan.routingPcbGroupId
        ).map((point6) => ({ x: point6.x, y: point6.y })) : [];
        const emitFanoutBoundsConflictWarning = () => {
          const routingPcbGroupId2 = routingPhasePlan.routingPcbGroupId;
          if (!routingPcbGroupId2) return;
          const pcbGroup = db.pcb_group.get(routingPcbGroupId2);
          const sourceComponentId = db.pcb_component.list().find(
            (component) => component.pcb_group_id === routingPcbGroupId2
          )?.source_component_id;
          if (!sourceComponentId) return;
          const message = `${pcbGroup?.name ?? "Breakout"} defines conflicting fanout bounds with explicit breakout geometry and fanoutBoundaryPadding. Explicit breakout geometry takes precedence, so fanoutBoundaryPadding is ignored.`;
          const warningAlreadyExists = db.source_property_ignored_warning.list().some(
            (warning) => warning.source_component_id === sourceComponentId && warning.property_name === "fanoutBoundaryPadding" && warning.message === message
          );
          if (warningAlreadyExists) return;
          db.source_property_ignored_warning.insert({
            source_component_id: sourceComponentId,
            property_name: "fanoutBoundaryPadding",
            message,
            error_type: "source_property_ignored_warning",
            subcircuit_id: pcbGroup?.subcircuit_id
          });
        };
        routingPhasePlan.fanoutBounds = FanoutAutorouter.resolveFanoutBounds(
          simpleRouteJson,
          {
            mode: fanoutMode,
            busFanoutDirections: routingPhasePlan.busFanoutDirections,
            fanoutBounds: routingPhasePlan.fanoutBounds,
            fanoutBoundaryPadding: routingPhasePlan.fanoutBoundaryPadding,
            fanoutRoutingLayers: routingPhasePlan.fanoutRoutingLayers,
            breakoutPoints,
            onFanoutBoundsConflict: emitFanoutBoundsConflictWarning
          }
        );
        const { fanoutBounds, routingPcbGroupId } = routingPhasePlan;
        if (fanoutBounds && routingPcbGroupId) {
          db.pcb_group.update(routingPcbGroupId, {
            center: {
              x: (fanoutBounds.minX + fanoutBounds.maxX) / 2,
              y: (fanoutBounds.minY + fanoutBounds.maxY) / 2
            },
            width: fanoutBounds.maxX - fanoutBounds.minX,
            height: fanoutBounds.maxY - fanoutBounds.minY
          });
        }
      }
      if (debug13.enabled) {
        ;
        global.debugOutputArray?.push({
          name: `simpleroutejson-${this.props.name}.json`,
          obj: simpleRouteJson
        });
      }
      if (debug13.enabled) {
        const graphicsObject = convertSrjToGraphicsObject2(
          simpleRouteJson
        );
        graphicsObject.title = `autorouting-${this.props.name}`;
        global.debugGraphics?.push(graphicsObject);
      }
      this.root?.emit("autorouting:start", {
        subcircuit_id: this.subcircuit_id,
        componentDisplayName: this.getString(),
        ...routingPhasePlan.phaseName !== void 0 ? {
          phaseName: routingPhasePlan.phaseName,
          phaseStageIndex,
          phaseStageCount
        } : {},
        simpleRouteJson
      });
      const cacheEngine = phaseAutorouterConfig.algorithmFn || !localAutorouterStrategy.cacheable ? void 0 : this.root?.platform?.localCacheEngine;
      const cacheKey = cacheEngine ? getLocalAutoroutingCacheKey(simpleRouteJson) : void 0;
      const cachedResult = cacheKey ? await getCachedLocalAutoroutingPhaseResult({ cacheEngine, cacheKey }) : null;
      let autorouter;
      try {
        let traces;
        if (cachedResult) {
          debug13(`[${this.getString()}] using cached local autorouting result`);
          traces = cachedResult.traces;
        } else {
          if (phaseAutorouterConfig.algorithmFn) {
            autorouter = await phaseAutorouterConfig.algorithmFn(simpleRouteJson);
          } else {
            const autorouterVersion2 = phaseAutorouterConfig.autorouterVersion ?? this.props.autorouterVersion;
            const effortLevel = this.props.autorouterEffortLevel;
            const effort = effortLevel ? Number.parseInt(effortLevel.replace("x", ""), 10) : void 0;
            const commonAutorouterOptions = {
              capacityDepth: phaseAutorouterConfig.capacityDepth,
              targetMinCapacity: phaseAutorouterConfig.targetMinCapacity,
              platformConfig: this.root?.platform,
              useAssignableSolver: phaseIsLaserPrefabPreset || isSingleLayerBoard,
              useAutoJumperSolver: phaseIsAutoJumperPreset,
              useLaserPrefabSolver: phaseIsLaserPrefabPreset,
              autorouterVersion: autorouterVersion2,
              effort,
              onSolverStarted: ({ solverName, solverParams }) => this.root?.emit("solver:started", {
                type: "solver:started",
                solverName,
                solverParams,
                solverConstructorArgs: [solverParams],
                componentName: this.getString()
              })
            };
            autorouter = localAutorouterStrategy.create({
              simpleRouteJson,
              commonAutorouterOptions,
              busFanoutDirections: routingPhasePlan.busFanoutDirections,
              fanoutBounds: routingPhasePlan.fanoutBounds,
              fanoutRoutingLayers: routingPhasePlan.fanoutRoutingLayers,
              componentNamesById: getPcbComponentNamesById(db)
            });
          }
          if (!autorouter) {
            throw new Error("Failed to create local autorouter");
          }
          const activeAutorouter = autorouter;
          const routingPromise = new Promise(
            (resolve, reject) => {
              activeAutorouter.on("complete", (event) => {
                debug13(`[${this.getString()}] local autorouting complete`);
                resolve(event.traces);
              });
              activeAutorouter.on("error", (event) => {
                debug13(
                  `[${this.getString()}] local autorouting error: ${event.error.message}`
                );
                reject(event.error);
              });
            }
          );
          activeAutorouter.on("progress", (event) => {
            this.root?.emit("autorouting:progress", {
              subcircuit_id: this.subcircuit_id,
              componentDisplayName: this.getString(),
              ...routingPhasePlan.phaseName !== void 0 ? {
                phaseName: routingPhasePlan.phaseName,
                phaseStageIndex,
                phaseStageCount
              } : {},
              ...event
            });
          });
          activeAutorouter.start();
          traces = await routingPromise;
        }
        let transformedSimpleRouteJson = autorouter?.getOutputSimpleRouteJson?.();
        if (transformedSimpleRouteJson && !usesPreviousStageOutput && ["fanout", "single_layer_fanout"].includes(
          phaseAutorouterConfig.preset ?? ""
        ) && routingPhasePlan.routingPcbGroupId) {
          const synchronizedFanout = Group_syncFanoutExitsWithGlobalConnections(
            {
              fanoutInputSimpleRouteJson: simpleRouteJson,
              fanoutOutputSimpleRouteJson: transformedSimpleRouteJson,
              baseSimpleRouteJson,
              routingPhasePlan
            }
          );
          baseSimpleRouteJson = synchronizedFanout.baseSimpleRouteJson;
          transformedSimpleRouteJson = synchronizedFanout.downstreamSimpleRouteJson;
          for (const synchronizedPoint of synchronizedFanout.synchronizedBreakoutPoints) {
            const breakoutPoint = db.pcb_breakout_point.list().find(
              (point6) => point6.pcb_group_id === synchronizedPoint.routingPcbGroupId && point6.source_trace_id === synchronizedPoint.sourceTraceId && Math.abs(point6.x - synchronizedPoint.previousPoint.x) <= 1e-6 && Math.abs(point6.y - synchronizedPoint.previousPoint.y) <= 1e-6
            );
            if (!breakoutPoint) continue;
            db.pcb_breakout_point.update(breakoutPoint.pcb_breakout_point_id, {
              x: synchronizedPoint.fanoutExitPoint.x,
              y: synchronizedPoint.fanoutExitPoint.y
            });
          }
        }
        let stageOutputTraces = traces;
        if (transformedSimpleRouteJson?.traces) {
          stageOutputTraces = transformedSimpleRouteJson.traces;
        } else if (usesPreviousStageOutput) {
          stageOutputTraces = [...simpleRouteJson.traces ?? [], ...traces];
        }
        const outputSimpleRouteJson = {
          ...transformedSimpleRouteJson ?? simpleRouteJson,
          traces: stageOutputTraces
        };
        previousStageOutputSimpleRouteJson = transformedSimpleRouteJson ? outputSimpleRouteJson : void 0;
        if (!cachedResult && cacheKey) {
          await cacheLocalAutoroutingPhaseResult({
            cacheEngine,
            cacheKey,
            result: {
              ...simpleRouteJson,
              traces
            }
          });
        }
        this.root?.emit("autorouting:end", {
          type: "autorouting:end",
          subcircuit_id: this.subcircuit_id,
          componentDisplayName: this.getString(),
          ...routingPhasePlan.phaseName !== void 0 ? {
            phaseName: routingPhasePlan.phaseName,
            phaseStageIndex,
            phaseStageCount
          } : {},
          simpleRouteJson: outputSimpleRouteJson
        });
        if (autorouter?.getConnectedOffboardObstacles) {
          const connectedOffboardObstacles = autorouter.getConnectedOffboardObstacles();
          createSourceTracesFromOffboardConnections({
            db,
            connectedOffboardObstacles,
            simpleRouteJson,
            subcircuit_id: this.subcircuit_id
          });
        }
        const solver = autorouter?.solver;
        if (solver?.getOutputJumpers) {
          outputJumpers.push(...solver.getOutputJumpers() || []);
        }
        if (isRegionReroutePhase && rerouteOriginalSrj) {
          for (const trace of rerouteOriginalSrj.traces ?? []) {
            if (trace.type === "pcb_trace") {
              pcbTraceIdsToDelete.add(trace.pcb_trace_id);
            }
          }
          const reconnectedSrj = reconnectReroutedSimpleRouteJsonRegion(
            rerouteOriginalSrj,
            {
              ...simpleRouteJson,
              traces: [...simpleRouteJson.traces ?? [], ...traces]
            }
          );
          outputTraces.splice(
            0,
            outputTraces.length,
            ...reconnectedSrj.traces ?? []
          );
        } else if (isConnectionReroutePhase) {
          const retainedPcbTraces = outputTraces.filter(
            (trace) => !traceMatchesRoutingPhase(trace, routingPhasePlan)
          );
          outputTraces.splice(
            0,
            outputTraces.length,
            ...getAccumulatedPcbTracesWithStageOutputReplacements({
              accumulatedPcbTraces: retainedPcbTraces,
              stageOutputPcbTraces: stageOutputTraces
            })
          );
        } else {
          outputTraces.splice(
            0,
            outputTraces.length,
            ...getAccumulatedPcbTracesWithStageOutputReplacements({
              accumulatedPcbTraces: outputTraces,
              stageOutputPcbTraces: stageOutputTraces
            })
          );
        }
      } catch (error) {
        const { db: db2 } = this.root;
        db2.pcb_autorouting_error.insert({
          pcb_error_id: `pcb_autorouter_error_subcircuit_${this.subcircuit_id}`,
          error_type: "pcb_autorouting_error",
          message: error instanceof Error ? error.message : String(error)
        });
        this.root?.emit("autorouting:error", {
          subcircuit_id: this.subcircuit_id,
          componentDisplayName: this.getString(),
          ...routingPhasePlan.phaseName !== void 0 ? {
            phaseName: routingPhasePlan.phaseName,
            phaseStageIndex,
            phaseStageCount
          } : {},
          error: {
            message: error instanceof Error ? error.message : String(error)
          },
          simpleRouteJson
        });
        throw error;
      } finally {
        autorouter?.stop();
      }
    }
    this._asyncAutoroutingResult = {
      output_pcb_traces: outputTraces,
      output_jumpers: outputJumpers,
      pcb_trace_ids_to_be_replaced: [...pcbTraceIdsToDelete],
      input_simple_route_json: baseSimpleRouteJson
    };
    this._markDirty("PcbTraceRender");
  }
  _startAsyncAutorouting() {
    if (this._hasStartedAsyncAutorouting) return;
    this._hasStartedAsyncAutorouting = true;
    if (this._getAutorouterConfig().local) {
      this._queueAsyncEffect(
        "autorouting",
        async () => this._runLocalAutorouting()
      );
    } else {
      this._queueAsyncEffect(
        "make-http-autorouting-request",
        async () => this._runEffectMakeHttpAutoroutingRequest()
      );
    }
  }
  doInitialPcbTraceRender() {
    const debug13 = Debug15("tscircuit:core:doInitialPcbTraceRender");
    if (!this.isSubcircuit) return;
    if (this.root?.pcbDisabled) return;
    if (this.root?.pcbRoutingDisabled || this.getInheritedProperty("routingDisabled"))
      return;
    if (this._isInflatedFromCircuitJson) return;
    if (this._isLegacyAutorouterDisabled()) return;
    if (this._shouldUseTraceByTraceRouting()) return;
    if (!this._areChildSubcircuitsRouted()) {
      debug13(
        `[${this.getString()}] child subcircuits are not routed, skipping async autorouting until subcircuits routed`
      );
      return;
    }
    debug13(
      `[${this.getString()}] no child subcircuits to wait for, initiating async routing`
    );
    if (!this._hasTracesToRoute()) return;
    const shouldSkipBecauseOfPlacementErrors = shouldSkipAutoroutingBecauseOfPlacementErrors({
      component: this,
      subcircuit: this
    });
    const shouldSkipBecauseOfTraceLengthViolations = shouldSkipAutoroutingBecauseOfTraceLengthViolations({
      component: this,
      subcircuit: this
    });
    if (shouldSkipBecauseOfPlacementErrors || shouldSkipBecauseOfTraceLengthViolations)
      return;
    this._startAsyncAutorouting();
  }
  doInitialSchematicTraceRender() {
    if (this._parsedProps.showAsSchematicBox) return;
    Group_doInitialSchematicTraceRender(this);
  }
  updatePcbTraceRender() {
    const debug13 = Debug15("tscircuit:core:updatePcbTraceRender");
    debug13(`[${this.getString()}] updating...`);
    if (!this.isSubcircuit) return;
    if (this._isInflatedFromCircuitJson) return;
    if (this._isLegacyAutorouterDisabled()) return;
    if (this._shouldRouteAsync() && this._hasTracesToRoute() && !this._hasStartedAsyncAutorouting) {
      if (this._areChildSubcircuitsRouted()) {
        const shouldSkipBecauseOfPlacementErrors = shouldSkipAutoroutingBecauseOfPlacementErrors({
          component: this,
          subcircuit: this
        });
        const shouldSkipBecauseOfTraceLengthViolations = shouldSkipAutoroutingBecauseOfTraceLengthViolations({
          component: this,
          subcircuit: this
        });
        if (shouldSkipBecauseOfPlacementErrors || shouldSkipBecauseOfTraceLengthViolations)
          return;
        debug13(
          `[${this.getString()}] child subcircuits are now routed, starting async autorouting`
        );
        this._startAsyncAutorouting();
      }
      return;
    }
    if (!this._asyncAutoroutingResult) return;
    if (this._shouldUseTraceByTraceRouting()) return;
    const { db } = this.root;
    if (this._asyncAutoroutingResult.output_simple_route_json) {
      debug13(
        `[${this.getString()}] updating PCB traces from simple route json (${this._asyncAutoroutingResult.output_simple_route_json.traces?.length} traces)`
      );
      this._updatePcbTraceRenderFromSimpleRouteJson();
      return;
    }
    if (this._asyncAutoroutingResult.output_pcb_traces) {
      debug13(
        `[${this.getString()}] updating PCB traces from ${this._asyncAutoroutingResult.output_pcb_traces.length} traces`
      );
      this._updatePcbTraceRenderFromPcbTraces();
      return;
    }
  }
  _updatePcbTraceRenderFromSimpleRouteJson() {
    const { db } = this.root;
    const { traces: routedTraces } = this._asyncAutoroutingResult.output_simple_route_json;
    if (!routedTraces) return;
    for (const routedTrace of routedTraces) {
      const cjRoute = routedTrace.route.map((point6) => {
        if (point6.route_type !== "through_obstacle") return point6;
        return {
          route_type: "through_pad",
          start: point6.start,
          end: point6.end,
          start_layer: point6.from_layer,
          end_layer: point6.to_layer,
          width: point6.width
        };
      });
      const pcb_trace = db.pcb_trace.insert({
        subcircuit_id: this.subcircuit_id,
        route: cjRoute
        // source_trace_id: circuitTrace.source_trace_id!,
      });
    }
  }
  _updatePcbTraceRenderFromPcbTraces() {
    const {
      output_pcb_traces,
      output_jumpers,
      pcb_trace_ids_to_be_replaced,
      input_simple_route_json
    } = this._asyncAutoroutingResult;
    if (!output_pcb_traces) return;
    const { db } = this.root;
    const pcbStyle = this.getInheritedMergedProperty("pcbStyle");
    const { holeDiameter, padDiameter } = getViaDiameterDefaults(pcbStyle);
    const board = db.pcb_board.list()[0];
    const routedViaHoleDiameter = board?.min_via_hole_diameter ?? holeDiameter;
    const routedViaPadDiameter = board?.min_via_pad_diameter ?? padDiameter;
    if (output_jumpers && output_jumpers.length > 0) {
      insertAutoplacedJumpers({
        db,
        output_jumpers,
        subcircuit_id: this.subcircuit_id
      });
    }
    deleteExistingPcbTracesReplacedBy({
      group: this,
      outputPcbTraces: output_pcb_traces,
      pcbTraceIdsToReplace: pcb_trace_ids_to_be_replaced
    });
    for (const pcb_trace of output_pcb_traces) {
      if (pcb_trace.type !== "pcb_trace") continue;
      pcb_trace.subcircuit_id ??= this.subcircuit_id;
      let cjRoute = pcb_trace.route.map((point6) => {
        if (point6.route_type !== "through_obstacle") return point6;
        return {
          route_type: "through_pad",
          start: point6.start,
          end: point6.end,
          start_layer: point6.from_layer,
          end_layer: point6.to_layer,
          width: point6.width
        };
      });
      const routeSourceTraceId = getSourceTraceIdForRoutedTrace({
        db,
        trace: {
          ...pcb_trace,
          route: cjRoute
        },
        subcircuit_id: this.subcircuit_id
      });
      cjRoute = ensureRouteStartsAtSourceTraceStart({
        db,
        route: cjRoute,
        sourceTraceId: routeSourceTraceId
      });
      pcb_trace.route = cjRoute;
      let segments = splitPcbTracesOnJumperSegments(cjRoute);
      if (segments === null) {
        segments = [cjRoute];
      }
      const processedSegments = addPortIdsToTracesAtJumperPads(segments, db);
      for (const segment2 of processedSegments) {
        if (segment2.length > 0) {
          const sourceTraceId = getSourceTraceIdForRoutedTrace({
            db,
            trace: {
              ...pcb_trace,
              route: segment2
            },
            subcircuit_id: this.subcircuit_id
          });
          db.pcb_trace.insert({
            ...pcb_trace,
            source_trace_id: sourceTraceId,
            route: segment2
          });
        }
      }
    }
    for (const pcb_trace of output_pcb_traces) {
      if (pcb_trace.type === "pcb_via") {
        continue;
      }
      if (pcb_trace.type === "pcb_trace") {
        const sourceTraceId = getSourceTraceIdForRoutedTrace({
          db,
          trace: pcb_trace,
          subcircuit_id: this.subcircuit_id
        });
        const { connection_name: inputConnectionName } = pcb_trace;
        let subcircuitConnectivityMapKey;
        if (sourceTraceId) {
          subcircuitConnectivityMapKey = db.source_trace.get(sourceTraceId)?.subcircuit_connectivity_map_key;
        }
        if (!subcircuitConnectivityMapKey && inputConnectionName) {
          const sourceNet = db.source_net.get(inputConnectionName);
          if (sourceNet) {
            subcircuitConnectivityMapKey = sourceNet.subcircuit_connectivity_map_key;
          }
        }
        for (const point6 of pcb_trace.route) {
          if (point6.route_type === "via") {
            const routedViaPoint = point6;
            const fromLayer = point6.from_layer;
            const toLayer = point6.to_layer;
            db.pcb_via.insert({
              pcb_trace_id: pcb_trace.pcb_trace_id,
              x: point6.x,
              y: point6.y,
              hole_diameter: routedViaPoint.via_hole_diameter ?? routedViaPoint.hole_diameter ?? routedViaHoleDiameter,
              outer_diameter: routedViaPoint.via_diameter ?? routedViaPoint.outer_diameter ?? routedViaPadDiameter,
              layers: getViaSpanLayers({
                fromLayer,
                toLayer,
                layerCount: this._getSubcircuitLayerCount()
              }),
              from_layer: fromLayer,
              to_layer: toLayer,
              subcircuit_id: this.subcircuit_id,
              pcb_group_id: this.pcb_group_id ?? void 0,
              subcircuit_connectivity_map_key: subcircuitConnectivityMapKey
            });
          }
        }
      }
    }
  }
  doInitialSchematicComponentRender() {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const schematic_group = db.schematic_group.insert({
      is_subcircuit: this.isSubcircuit,
      subcircuit_id: this.subcircuit_id,
      name: this.name,
      center: this._getGlobalSchematicPositionBeforeLayout(),
      width: 0,
      height: 0,
      schematic_component_ids: [],
      source_group_id: this.source_group_id,
      show_as_schematic_box: props.showAsSchematicBox ?? false
    });
    this.schematic_group_id = schematic_group.schematic_group_id;
    if (props.showAsSchematicBox) {
      Group_doInitialSchematicBoxComponentRender(this);
    }
    for (const child of this.getDescendants()) {
      if (child.getGroup()?.source_group_id !== this.source_group_id) continue;
      if (child._parsedProps?.showAsSchematicBox) continue;
      if (child.schematic_component_id) {
        db.schematic_component.update(child.schematic_component_id, {
          schematic_group_id: schematic_group.schematic_group_id
        });
      }
    }
  }
  _getSchematicLayoutMode() {
    const props = this._parsedProps;
    const schAutoLayoutEnabled = props.schAutoLayoutEnabled ?? false;
    if (props.schLayout?.layoutMode === "none") return "relative";
    if (props.schLayout?.layoutMode === "relative") return "relative";
    if (props.schLayout?.matchAdapt) return "match-adapt";
    if (props.schLayout?.flex) return "flex";
    if (props.schLayout?.grid) return "grid";
    if (props.schMatchAdapt) return "match-adapt";
    if (props.schFlex) return "flex";
    if (props.schGrid) return "grid";
    if (props.matchAdapt) return "match-adapt";
    if (props.flex) return "flex";
    if (props.grid) return "grid";
    if (props.relative) return "relative";
    if (props.schRelative) return "relative";
    const anyLayoutChildHasSchCoords = this.children.some((child) => {
      const cProps = child._parsedProps;
      const participatesInAutoLayout = child.source_component_id !== null || child.source_group_id !== null;
      return participatesInAutoLayout && (cProps?.schX !== void 0 || cProps?.schY !== void 0);
    });
    const hasManualEdits = (props.manualEdits?.schematic_placements?.length ?? 0) > 0;
    if (schAutoLayoutEnabled && !hasManualEdits) return "match-adapt";
    if (!anyLayoutChildHasSchCoords && !hasManualEdits) return "match-adapt";
    return "relative";
  }
  doInitialSchematicLayout() {
    if (this._parsedProps.showAsSchematicBox) {
      this._insertSchematicBorder();
      return;
    }
    const hasAnySectionName = this.children.some(
      (c) => c.source_component_id !== null && c._parsedProps?.schSectionName !== void 0
    );
    const hasSections = hasAnySectionName;
    if (hasSections) {
      this._doInitialSchematicLayoutSections();
      this._insertSchematicBorder();
      return;
    }
    const schematicLayoutMode = this._getSchematicLayoutMode();
    if (schematicLayoutMode === "match-adapt") {
      this._doInitialSchematicLayoutMatchpack();
    }
    if (schematicLayoutMode === "grid") {
      this._doInitialSchematicLayoutGrid();
    }
    if (schematicLayoutMode === "flex") {
      this._doInitialSchematicLayoutFlex();
    }
    this._insertSchematicBorder();
  }
  _doInitialSchematicLayoutSections() {
    Group_doInitialSchematicLayoutSections(this);
  }
  _doInitialSchematicLayoutMatchAdapt() {
    Group_doInitialSchematicLayoutMatchAdapt(this);
  }
  _doInitialSchematicLayoutMatchpack() {
    Group_doInitialSchematicLayoutMatchPack(this);
  }
  _doInitialSchematicLayoutGrid() {
    Group_doInitialSchematicLayoutGrid(this);
  }
  _doInitialSchematicLayoutFlex() {
    Group_doInitialSchematicLayoutFlex(this);
  }
  _getPcbLayoutMode() {
    const props = this._parsedProps;
    const rawProps = this.props;
    if (this._isInflatedFromCircuitJson) return "none";
    if (props.pcbRelative) return "none";
    if (props.pcbLayout?.matchAdapt) return "match-adapt";
    if (props.pcbLayout?.flex) return "flex";
    if (props.pcbLayout?.grid) return "grid";
    if (props.pcbLayout?.pack) return "pack";
    if (props.pcbFlex) return "flex";
    if (props.pcbGrid) return "grid";
    if (props.pcbPack) return "pack";
    if (props.pack) return "pack";
    if (props.matchAdapt) return "match-adapt";
    if (props.flex) return "flex";
    if (props.grid) return "grid";
    const groupHasCoords = props.pcbX !== void 0 || props.pcbY !== void 0 || props.pcbLeftEdgeX !== void 0 || props.pcbRightEdgeX !== void 0 || props.pcbTopEdgeY !== void 0 || props.pcbBottomEdgeY !== void 0 || rawProps.pcbLeftEdgeX !== void 0 || rawProps.pcbRightEdgeX !== void 0 || rawProps.pcbTopEdgeY !== void 0 || rawProps.pcbBottomEdgeY !== void 0;
    const hasManualEdits = (props.manualEdits?.pcb_placements?.length ?? 0) > 0;
    const unpositionedDirectChildrenCount = this.children.reduce(
      (count, child) => {
        if (!child.pcb_component_id && !child.pcb_group_id) {
          return count;
        }
        const childProps = child._parsedProps;
        const rawChildProps = child.props;
        const hasCoords = childProps?.pcbX !== void 0 || childProps?.pcbY !== void 0 || childProps?.pcbLeftEdgeX !== void 0 || childProps?.pcbRightEdgeX !== void 0 || childProps?.pcbTopEdgeY !== void 0 || childProps?.pcbBottomEdgeY !== void 0 || rawChildProps?.pcbLeftEdgeX !== void 0 || rawChildProps?.pcbRightEdgeX !== void 0 || rawChildProps?.pcbTopEdgeY !== void 0 || rawChildProps?.pcbBottomEdgeY !== void 0;
        return count + (hasCoords ? 0 : 1);
      },
      0
    );
    if (!hasManualEdits && unpositionedDirectChildrenCount > 1) return "pack";
    return "none";
  }
  doInitialPcbLayout() {
    if (this.root?.pcbDisabled) return;
    if (this.pcb_group_id) {
      const { db } = this.root;
      const props = this._parsedProps;
      const hasExplicitPcbPosition = props.pcbX !== void 0 || props.pcbY !== void 0;
      if (hasExplicitPcbPosition) {
        const parentGroup = this.parent?.getGroup?.();
        const pcbParentGroupId = parentGroup?.pcb_group_id ? db.pcb_group.get(parentGroup.pcb_group_id)?.pcb_group_id : void 0;
        const positionedRelativeToBoardId = !pcbParentGroupId ? this._getBoard()?.pcb_board_id ?? void 0 : void 0;
        db.pcb_group.update(this.pcb_group_id, {
          position_mode: "relative_to_group_anchor",
          positioned_relative_to_pcb_group_id: pcbParentGroupId,
          positioned_relative_to_pcb_board_id: positionedRelativeToBoardId,
          display_offset_x: props.pcbX,
          display_offset_y: props.pcbY
        });
      }
    }
    const pcbLayoutMode = this._getPcbLayoutMode();
    if (pcbLayoutMode === "grid") {
      this._doInitialPcbLayoutGrid();
    } else if (pcbLayoutMode === "pack") {
      this._doInitialPcbLayoutPack();
    } else if (pcbLayoutMode === "flex") {
      this._doInitialPcbLayoutFlex();
    }
  }
  _doInitialPcbLayoutGrid() {
    Group_doInitialPcbLayoutGrid(this);
  }
  _doInitialPcbLayoutPack() {
    Group_doInitialPcbLayoutPack(this);
  }
  _doInitialPcbLayoutFlex() {
    Group_doInitialPcbLayoutFlex(this);
  }
  _insertSchematicBorder() {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const props = this._parsedProps;
    if (!props.border) return;
    let width = typeof props.schWidth === "number" ? props.schWidth : void 0;
    let height = typeof props.schHeight === "number" ? props.schHeight : void 0;
    const paddingGeneral = typeof props.schPadding === "number" ? props.schPadding : 0;
    const paddingLeft = typeof props.schPaddingLeft === "number" ? props.schPaddingLeft : paddingGeneral;
    const paddingRight = typeof props.schPaddingRight === "number" ? props.schPaddingRight : paddingGeneral;
    const paddingTop = typeof props.schPaddingTop === "number" ? props.schPaddingTop : paddingGeneral;
    const paddingBottom = typeof props.schPaddingBottom === "number" ? props.schPaddingBottom : paddingGeneral;
    const schematicGroup = this.schematic_group_id ? db.schematic_group.get(this.schematic_group_id) : null;
    if (schematicGroup) {
      if (width === void 0 && typeof schematicGroup.width === "number") {
        width = schematicGroup.width;
      }
      if (height === void 0 && typeof schematicGroup.height === "number") {
        height = schematicGroup.height;
      }
    }
    if (width === void 0 || height === void 0) return;
    const center = schematicGroup?.center ?? this._getGlobalSchematicPositionBeforeLayout();
    const left = center.x - width / 2 - paddingLeft;
    const bottom = center.y - height / 2 - paddingBottom;
    const finalWidth = width + paddingLeft + paddingRight;
    const finalHeight = height + paddingTop + paddingBottom;
    db.schematic_box.insert({
      width: finalWidth,
      height: finalHeight,
      x: left,
      y: bottom,
      is_dashed: props.border?.dashed ?? false
    });
  }
  _determineSideFromPosition(port, component) {
    if (!port.center || !component.center) return "left";
    const dx = port.center.x - component.center.x;
    const dy = port.center.y - component.center.y;
    if (Math.abs(dx) > Math.abs(dy)) {
      return dx > 0 ? "right" : "left";
    }
    return dy > 0 ? "bottom" : "top";
  }
  _calculateSchematicBounds(boxes) {
    if (boxes.length === 0) {
      return { minX: 0, maxX: 0, minY: 0, maxY: 0 };
    }
    let minX = Infinity;
    let maxX = -Infinity;
    let minY = Infinity;
    let maxY = -Infinity;
    for (const box of boxes) {
      minX = Math.min(minX, box.centerX);
      maxX = Math.max(maxX, box.centerX);
      minY = Math.min(minY, box.centerY);
      maxY = Math.max(maxY, box.centerY);
    }
    const padding = 2;
    return {
      minX: minX - padding,
      maxX: maxX + padding,
      minY: minY - padding,
      maxY: maxY + padding
    };
  }
  _getAutorouterConfig() {
    const autorouter = this._parsedProps.autorouter || this.getInheritedProperty("autorouter");
    return getPresetAutoroutingConfig(autorouter, this.root?.platform);
  }
  _isLegacyAutorouterDisabled() {
    return getDisabledLegacyAutorouterPreset(this) !== null;
  }
  _isLaserPrefabAutorouter(autorouterConfig = this._getAutorouterConfig()) {
    const autorouterProp = this.props.autorouter;
    const normalize = (value) => value?.replace(/-/g, "_") ?? value;
    if (autorouterConfig.preset === "laser_prefab") return true;
    if (typeof autorouterProp === "string") {
      return normalize(autorouterProp) === "laser_prefab";
    }
    if (typeof autorouterProp === "object" && autorouterProp) {
      return normalize(autorouterProp.preset) === "laser_prefab";
    }
    return false;
  }
  _isAutoJumperAutorouter(autorouterConfig = this._getAutorouterConfig()) {
    const autorouterProp = this.props.autorouter;
    const normalize = (value) => value?.replace(/-/g, "_") ?? value;
    if (autorouterConfig.preset === "auto_jumper") return true;
    if (typeof autorouterProp === "string") {
      return normalize(autorouterProp) === "auto_jumper";
    }
    if (typeof autorouterProp === "object" && autorouterProp) {
      return normalize(autorouterProp.preset) === "auto_jumper";
    }
    return false;
  }
  _getSubcircuitLayerCount() {
    const layers = this.getInheritedProperty("layers");
    return typeof layers === "number" ? layers : 2;
  }
  /**
   * Trace-by-trace autorouting is where each trace routes itself in a well-known
   * order. It's the most deterministic way to autoroute, because a new trace
   * is generally ordered last.
   *
   * This method will return false if using an external service for autorouting
   * or if using a "fullview" or "rip and replace" autorouting mode
   */
  _shouldUseTraceByTraceRouting() {
    if (getDisabledLegacyGroupAutorouterPreset(this)) return false;
    const autorouter = this._getAutorouterConfig();
    return autorouter.groupMode === "sequential-trace";
  }
  doInitialPcbDesignRuleChecks() {
    if (this.root?.pcbDisabled) return;
    if (this._isLegacyAutorouterDisabled()) return;
    if (this.root?.pcbRoutingDisabled || this.getInheritedProperty("routingDisabled"))
      return;
    const { db } = this.root;
    if (this.isSubcircuit) {
      const immediateChildrenByName = /* @__PURE__ */ new Map();
      for (const child of this.children) {
        if (child.isSubcircuit) continue;
        if (child._parsedProps.name) {
          const components = immediateChildrenByName.get(child._parsedProps.name) || [];
          components.push(child);
          immediateChildrenByName.set(child._parsedProps.name, components);
        }
      }
      for (const [name, components] of immediateChildrenByName.entries()) {
        if (components.length <= 1) continue;
        const sameNamedTraces = components.filter(
          (child) => child instanceof Trace3
        );
        const duplicateNameIsOnlyTraces = sameNamedTraces.length === components.length;
        const mutuallyConnectedTraceKeys = sameNamedTraces.map(
          (trace) => trace.subcircuit_connectivity_map_key
        );
        const sameNamedTracesAreMutuallyConnected = duplicateNameIsOnlyTraces && mutuallyConnectedTraceKeys.every(Boolean) && new Set(mutuallyConnectedTraceKeys).size === 1;
        if (!duplicateNameIsOnlyTraces || !sameNamedTracesAreMutuallyConnected) {
          const displaySubcircuitName = this.name || "unnamed";
          const message = duplicateNameIsOnlyTraces ? `Trace "${name}" in subcircuit "${displaySubcircuitName}" shares a name with another trace, but the traces are not mutually connected. Same-named traces must have the same subcircuit connectivity map key.` : `Multiple immediate children found with name "${name}" in subcircuit "${displaySubcircuitName}". Names must be unique.`;
          db.pcb_trace_error.insert({
            error_type: "pcb_trace_error",
            message,
            source_trace_id: "",
            pcb_trace_id: "",
            pcb_component_ids: components.map((c) => c.pcb_component_id).filter(Boolean),
            pcb_port_ids: []
          });
        }
      }
    }
  }
  doInitialSchematicReplaceNetLabelsWithSymbols() {
    if (this.root?.schematicDisabled) return;
    if (!this.isSubcircuit) return;
    const { db } = this.root;
    const subtree = db;
    for (const nl of subtree.schematic_net_label.list()) {
      const net = subtree.source_net.get(nl.source_net_id);
      const text = nl.text || net?.name || "";
      const isGroundNet = net?.is_ground || GROUND_NET_REGEX.test(text);
      const isPowerNet = net?.is_power || POWER_NET_REGEX.test(text);
      if (nl.anchor_side === "top" && isGroundNet) {
        subtree.schematic_net_label.update(nl.schematic_net_label_id, {
          symbol_name: "rail_down"
        });
        continue;
      }
      if (nl.anchor_side === "bottom" && isPowerNet) {
        subtree.schematic_net_label.update(nl.schematic_net_label_id, {
          symbol_name: "rail_up"
        });
      }
    }
  }
  doInitialSimulationSpiceEngineRender() {
    Group_doInitialSimulationSpiceEngineRender(this);
  }
  /**
   * Override anchor alignment to handle group-specific logic
   */
  doInitialPcbComponentAnchorAlignment() {
    Group_doInitialPcbComponentAnchorAlignment(this);
  }
  updatePcbComponentAnchorAlignment() {
    this.doInitialPcbComponentAnchorAlignment();
  }
  doInitialPcbCalcPlacementResolution() {
    Group_doInitialPcbCalcPlacementResolution(this);
  }
  updatePcbCalcPlacementResolution() {
    this.doInitialPcbCalcPlacementResolution();
  }
  /**
   * Get the minimum flex container size for this group on PCB
   */
  _getMinimumFlexContainerSize() {
    return super._getMinimumFlexContainerSize();
  }
  /**
   * Reposition this group on the PCB to the specified coordinates
   */
  _repositionOnPcb(position) {
    return super._repositionOnPcb(position);
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceGroup.ts
function inflateSourceGroup(sourceGroup, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbGroup = injectionDb.pcb_group.getWhere({
    source_group_id: sourceGroup.source_group_id
  });
  const groupProps2 = {
    name: sourceGroup.name ?? `inflated_group_${sourceGroup.source_group_id}`
  };
  if (pcbGroup?.position_mode === "relative_to_group_anchor" && pcbGroup.anchor_position) {
    let baseAnchor = { x: 0, y: 0 };
    if (sourceGroup.parent_source_group_id) {
      const parentPcbGroup = injectionDb.pcb_group.getWhere({
        source_group_id: sourceGroup.parent_source_group_id
      });
      if (parentPcbGroup?.anchor_position) {
        baseAnchor = parentPcbGroup.anchor_position;
      }
    }
    groupProps2.pcbX = pcbGroup.anchor_position.x - baseAnchor.x;
    groupProps2.pcbY = pcbGroup.anchor_position.y - baseAnchor.y;
  }
  if (pcbGroup?.anchor_alignment) {
    groupProps2.pcbAnchorAlignment = pcbGroup.anchor_alignment;
  }
  const group = new Group5(groupProps2);
  group._isInflatedFromCircuitJson = true;
  group.source_group_id = sourceGroup.source_group_id;
  if (groupsMap) {
    groupsMap.set(sourceGroup.source_group_id, group);
  }
  if (sourceGroup.parent_source_group_id && groupsMap?.has(sourceGroup.parent_source_group_id)) {
    const parentGroup = groupsMap.get(sourceGroup.parent_source_group_id);
    parentGroup.add(group);
  } else {
    subcircuit.add(group);
  }
  return group;
}

// lib/components/normal-components/Inductor.ts
import { inductorProps } from "@tscircuit/props";
import { formatSiUnit as formatSiUnit2 } from "format-si-unit";
var Inductor = class extends NormalComponent3 {
  _adjustSilkscreenTextAutomatically = true;
  get config() {
    return {
      componentName: "Inductor",
      schematicSymbolName: this.props.symbolName ?? "inductor",
      zodProps: inductorProps,
      sourceFtype: FTYPE.simple_inductor
    };
  }
  _getSchematicSymbolDisplayValue() {
    return `${formatSiUnit2(this._parsedProps.inductance)}H`;
  }
  initPorts() {
    super.initPorts({
      additionalAliases: {
        pin1: ["anode", "pos", "left"],
        pin2: ["cathode", "neg", "right"]
      }
    });
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      name: this.name,
      ftype: FTYPE.simple_inductor,
      inductance: this.props.inductance,
      display_inductance: this._getSchematicSymbolDisplayValue(),
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceInductor.ts
function inflateSourceInductor(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const cadElm = injectionDb.cad_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const inductor = new Inductor({
    name: sourceElm.name,
    inductance: sourceElm.inductance,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: inductor
    });
    if (footprint) {
      inductor.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(inductor);
  } else {
    subcircuit.add(inductor);
  }
}

// lib/components/normal-components/Led.ts
import { ledProps } from "@tscircuit/props";
var Led = class extends NormalComponent3 {
  get config() {
    const symbolMap = {
      laser: "laser_diode"
    };
    const variantSymbol = this.props.laser ? "laser" : null;
    return {
      schematicSymbolName: variantSymbol ? symbolMap[variantSymbol] : this.props.symbolName ?? "led",
      componentName: "Led",
      zodProps: ledProps,
      sourceFtype: "simple_led"
    };
  }
  initPorts() {
    const hasFootprintChild = this.children.some(
      (child) => child.componentName === "Footprint"
    );
    const footprint = this.resolveFootprint();
    const hasPinLabels = Boolean(this._resolvePinLabels());
    const shouldAddDefaultAliases = !hasPinLabels && !hasFootprintChild && (!footprint || isFootprinterString(footprint));
    super.initPorts({
      pinCount: 2,
      ignoreSymbolPorts: !hasPinLabels && !shouldAddDefaultAliases,
      additionalAliases: {
        pin1: shouldAddDefaultAliases ? ["anode", "pos", "left"] : [],
        pin2: shouldAddDefaultAliases ? ["cathode", "neg", "right"] : []
      }
    });
  }
  _getSchematicSymbolDisplayValue() {
    return this._parsedProps.schDisplayValue || this._parsedProps.color || void 0;
  }
  getFootprinterString() {
    const baseFootprint = super.getFootprinterString();
    if (baseFootprint && this.props.color) {
      return `${baseFootprint}_color(${this.props.color})`;
    }
    return baseFootprint;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_led",
      name: this.name,
      wave_length: props.wavelength,
      color: props.color,
      symbol_display_value: this._getSchematicSymbolDisplayValue(),
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      are_pins_interchangeable: false,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  get pos() {
    return this.portMap.pos;
  }
  get anode() {
    return this.portMap.anode;
  }
  get neg() {
    return this.portMap.neg;
  }
  get cathode() {
    return this.portMap.cathode;
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceLed.ts
function inflateSourceLed(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const cadElm = injectionDb.cad_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const led = new Led({
    name: sourceElm.name,
    color: sourceElm.color,
    wavelength: sourceElm.wavelength ?? sourceElm.wave_length,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: led
    });
    if (footprint) {
      led.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(led);
  } else {
    subcircuit.add(led);
  }
}

// lib/components/normal-components/Mosfet.ts
import { mosfetProps } from "@tscircuit/props";

// lib/components/normal-components/get-mosfet-schematic-symbol-name.ts
var possibleOrientations = [
  { gate: "left", drain: "top", source: "bottom" },
  { gate: "left", drain: "bottom", source: "top" },
  { gate: "right", drain: "top", source: "bottom" },
  { gate: "right", drain: "bottom", source: "top" },
  { gate: "top", drain: "left", source: "right" },
  { gate: "top", drain: "right", source: "left" },
  { gate: "bottom", drain: "left", source: "right" },
  { gate: "bottom", drain: "right", source: "left" }
];
var getBaselineOrientation = (rotation4) => {
  const normalizedRotation = (rotation4 % 360 + 360) % 360;
  if (normalizedRotation % 90 !== 0) {
    throw new Error(
      `Schematic rotation ${rotation4} is not supported for Mosfet`
    );
  }
  return [
    { gate: "left", drain: "top", source: "bottom" },
    { gate: "top", drain: "right", source: "left" },
    { gate: "right", drain: "bottom", source: "top" },
    { gate: "bottom", drain: "left", source: "right" }
  ][normalizedRotation / 90];
};
var getRequestedOrientation = (props) => {
  const requestedSides = {
    drain: props.symbolDrainSide,
    source: props.symbolSourceSide,
    gate: props.symbolGateSide
  };
  const baseline = getBaselineOrientation(Number(props.schRotation ?? 0));
  const matchingOrientations = possibleOrientations.filter(
    (orientation) => Object.keys(requestedSides).every(
      (portName) => requestedSides[portName] === void 0 || requestedSides[portName] === orientation[portName]
    )
  );
  if (matchingOrientations.length === 0) {
    const requestedDescription = Object.entries(requestedSides).filter(([, side]) => side !== void 0).map(([portName, side]) => `${portName}=${side}`).join(", ");
    throw new Error(
      `Invalid MOSFET symbol port side combination (${requestedDescription}). The gate must be perpendicular to the drain and source, and the drain and source must be opposite.`
    );
  }
  return matchingOrientations.sort((a, b) => {
    const score = (orientation) => Object.keys(baseline).filter(
      (portName) => orientation[portName] !== baseline[portName]
    ).length;
    return score(a) - score(b);
  })[0];
};
var getMosfetSchematicSymbolName = (props) => {
  const mosfetMode = props.mosfetMode === "depletion" ? "d" : "e";
  const baseSymbolName = `${props.channelType}_channel_${mosfetMode}_mosfet_transistor`;
  const hasSymbolSideProps = props.symbolDrainSide !== void 0 || props.symbolSourceSide !== void 0 || props.symbolGateSide !== void 0;
  if (!hasSymbolSideProps) return baseSymbolName;
  const { gate, drain } = getRequestedOrientation(props);
  return `${baseSymbolName}_gate_${gate}_drain_${drain}`;
};

// lib/components/normal-components/Mosfet.ts
var Mosfet = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "Mosfet",
      schematicSymbolName: this.props.symbolName ?? getMosfetSchematicSymbolName(this.props),
      zodProps: mosfetProps,
      shouldRenderAsSchematicBox: false
    };
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_mosfet",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      mosfet_mode: props.mosfetMode,
      channel_type: props.channelType,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceMosfet.ts
function inflateSourceMosfet(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const mosfet = new Mosfet({
    name: sourceElm.name,
    channelType: sourceElm.channel_type,
    mosfetMode: sourceElm.mosfet_mode,
    manufacturerPartNumber: sourceElm.manufacturer_part_number,
    supplierPartNumbers: sourceElm.supplier_part_numbers ?? void 0,
    displayName: sourceElm.display_name,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: mosfet
    });
    if (footprint) {
      mosfet.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    groupsMap.get(sourceElm.source_group_id).add(mosfet);
  } else {
    subcircuit.add(mosfet);
  }
}

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourcePort.ts
function inflateSourcePort(sourcePort, inflatorContext) {
  const { injectionDb, subcircuit } = inflatorContext;
  if (sourcePort.source_component_id !== null) {
    return;
  }
  const pcbPortFromInjection = injectionDb.pcb_port.getWhere({
    source_port_id: sourcePort.source_port_id
  });
  const port = new Port({
    name: sourcePort.name,
    pinNumber: sourcePort.pin_number
  });
  subcircuit.add(port);
  port.source_port_id = sourcePort.source_port_id;
  const root = subcircuit.root;
  if (root && pcbPortFromInjection) {
    const { db } = root;
    const pcb_port = db.pcb_port.insert({
      pcb_component_id: void 0,
      layers: pcbPortFromInjection.layers,
      subcircuit_id: subcircuit.subcircuit_id ?? void 0,
      pcb_group_id: subcircuit.getGroup()?.pcb_group_id ?? void 0,
      x: pcbPortFromInjection.x,
      y: pcbPortFromInjection.y,
      source_port_id: sourcePort.source_port_id,
      is_board_pinout: false
    });
    port.pcb_port_id = pcb_port.pcb_port_id;
  }
}

// lib/components/normal-components/PushButton.ts
import { pushButtonProps } from "@tscircuit/props";
import { symbols as symbols5 } from "schematic-symbols";
var PushButton = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "PushButton",
      schematicSymbolName: this.props.symbolName ?? "push_button_normally_open_momentary",
      zodProps: pushButtonProps,
      sourceFtype: FTYPE.simple_push_button
    };
  }
  get defaultInternallyConnectedPinNames() {
    return [];
  }
  initPorts() {
    super.initPorts({
      pinCount: 2,
      ignoreSymbolPorts: true
    });
    const symbol = symbols5[this._getSchematicSymbolNameOrThrow()];
    const symPort1 = symbol.ports.find((p) => p.labels.includes("1"));
    const symPort2 = symbol.ports.find((p) => p.labels.includes("2"));
    const ports = this.selectAll("port");
    const pin1Port = ports.find((p) => p.props.pinNumber === 1);
    const pin2Port = ports.find((p) => p.props.pinNumber === 2);
    const pin3Port = ports.find((p) => p.props.pinNumber === 3);
    const pin4Port = ports.find((p) => p.props.pinNumber === 4);
    const { internallyConnectedPins } = this._parsedProps;
    pin1Port.schematicSymbolPortDef = symPort1;
    if (!internallyConnectedPins || internallyConnectedPins.length === 0) {
      pin2Port.schematicSymbolPortDef = symPort2;
    }
    for (const [pn, port] of [
      [2, pin2Port],
      [3, pin3Port],
      [4, pin4Port]
    ]) {
      const internallyConnectedRow = internallyConnectedPins?.find(
        ([pin1, pin2]) => pin1 === `pin${pn}` || pin2 === `pin${pn}`
      );
      if (!internallyConnectedRow) {
        port.schematicSymbolPortDef = symPort2;
        break;
      }
      const internallyConnectedTo = internallyConnectedRow?.[0] === `pin${pn}` ? internallyConnectedRow[1] : internallyConnectedRow?.[0];
      if (internallyConnectedTo === "pin1") {
        continue;
      }
      port.schematicSymbolPortDef = symPort2;
    }
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      name: this.name,
      ftype: FTYPE.simple_push_button,
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourcePushButton.ts
function inflateSourcePushButton(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const cadElm = injectionDb.cad_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const pushButton = new PushButton({
    name: sourceElm.name,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: pushButton
    });
    if (footprint) {
      pushButton.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(pushButton);
  } else {
    subcircuit.add(pushButton);
  }
}

// lib/components/normal-components/Resistor.ts
import { resistorProps } from "@tscircuit/props";
import { formatSiUnit as formatSiUnit3 } from "format-si-unit";
var Resistor = class extends NormalComponent3 {
  _adjustSilkscreenTextAutomatically = true;
  get config() {
    return {
      componentName: "Resistor",
      schematicSymbolName: this.props.symbolName ?? "boxresistor",
      zodProps: resistorProps,
      sourceFtype: "simple_resistor"
    };
  }
  initPorts() {
    super.initPorts({
      additionalAliases: {
        pin1: ["anode", "pos", "left"],
        pin2: ["cathode", "neg", "right"]
      }
    });
  }
  _getSchematicSymbolDisplayValue() {
    return `${formatSiUnit3(this._parsedProps.resistance)}\u03A9`;
  }
  doInitialCreateNetsFromProps() {
    this._createNetsFromProps([
      this.props.pullupFor,
      this.props.pullupTo,
      this.props.pulldownFor,
      this.props.pulldownTo,
      ...this._getNetsFromConnectionsProp()
    ]);
  }
  doInitialCreateTracesFromProps() {
    if (this.props.pullupFor && this.props.pullupTo) {
      this.add(
        new Trace3({
          from: `${this.getSubcircuitSelector()} > port.1`,
          to: this.props.pullupFor
        })
      );
      this.add(
        new Trace3({
          from: `${this.getSubcircuitSelector()} > port.2`,
          to: this.props.pullupTo
        })
      );
    }
    if (this.props.pulldownFor && this.props.pulldownTo) {
      this.add(
        new Trace3({
          from: `${this.getSubcircuitSelector()} > port.1`,
          to: this.props.pulldownFor
        })
      );
      this.add(
        new Trace3({
          from: `${this.getSubcircuitSelector()} > port.2`,
          to: this.props.pulldownTo
        })
      );
    }
    this._createTracesFromConnectionsProp();
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_resistor",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      resistance: props.resistance,
      display_resistance: this._getSchematicSymbolDisplayValue(),
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceResistor.ts
function inflateSourceResistor(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const cadElm = injectionDb.cad_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const resistor = new Resistor({
    name: sourceElm.name,
    resistance: sourceElm.resistance,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: resistor
    });
    if (footprint) {
      resistor.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(resistor);
  } else {
    subcircuit.add(resistor);
  }
}

// lib/components/normal-components/Switch.ts
import { switchProps } from "@tscircuit/props";
import { frequency, ms } from "circuit-json";
function hasSimProps(props) {
  return props.simSwitchFrequency !== void 0 || props.simCloseAt !== void 0 || props.simOpenAt !== void 0 || props.simStartClosed !== void 0 || props.simStartOpen !== void 0;
}
var Switch = class extends NormalComponent3 {
  _getSwitchType() {
    const props = this._parsedProps;
    if (!props) return "spst";
    if (props.dpdt) return "dpdt";
    if (props.spst) return "spst";
    if (props.spdt) return "spdt";
    if (props.dpst) return "dpst";
    return props.type ?? "spst";
  }
  get config() {
    const switchType = this._getSwitchType();
    const isNormallyClosed = this._parsedProps?.isNormallyClosed ?? false;
    const symbolMap = {
      spst: isNormallyClosed ? "spst_normally_closed_switch" : "spst_switch",
      spdt: isNormallyClosed ? "spdt_normally_closed_switch" : "spdt_switch",
      dpst: isNormallyClosed ? "dpst_normally_closed_switch" : "dpst_switch",
      dpdt: isNormallyClosed ? "dpdt_normally_closed_switch" : "dpdt_switch"
    };
    return {
      componentName: "Switch",
      schematicSymbolName: this.props.symbolName ?? symbolMap[switchType],
      zodProps: switchProps,
      shouldRenderAsSchematicBox: false
    };
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const props = this._parsedProps ?? {};
    const source_component = db.source_component.insert({
      ftype: "simple_switch",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      are_pins_interchangeable: this._getSwitchType() === "spst",
      display_name: props?.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialSimulationRender() {
    const { _parsedProps: props } = this;
    if (!hasSimProps(props)) {
      return;
    }
    const { db } = this.root;
    const simulationSwitch = {
      type: "simulation_switch",
      source_component_id: this.source_component_id || ""
    };
    if (props.simSwitchFrequency !== void 0) {
      simulationSwitch.switching_frequency = frequency.parse(
        props.simSwitchFrequency
      );
    }
    if (props.simCloseAt !== void 0) {
      simulationSwitch.closes_at = ms.parse(props.simCloseAt);
    }
    if (props.simOpenAt !== void 0) {
      simulationSwitch.opens_at = ms.parse(props.simOpenAt);
    }
    if (props.simStartOpen !== void 0) {
      simulationSwitch.starts_closed = !props.simStartOpen;
    }
    if (props.simStartClosed !== void 0) {
      simulationSwitch.starts_closed = props.simStartClosed;
    }
    db.simulation_switch.insert(simulationSwitch);
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceSwitch.ts
var getImportedSwitchProps = (sourceElm, inflatorContext) => {
  const sourcePorts = inflatorContext.injectionDb.source_port.list().filter(
    (port) => port.source_component_id === sourceElm.source_component_id
  );
  const pinLabels = {};
  let highestPinNumber = 0;
  for (const sourcePort of sourcePorts) {
    const pinNumber = sourcePort.pin_number;
    if (pinNumber === void 0 || pinNumber === null) continue;
    highestPinNumber = Math.max(highestPinNumber, pinNumber);
    const labels = Array.from(
      new Set(
        [sourcePort.name, ...sourcePort.port_hints ?? []].filter(
          (label) => typeof label === "string" && label.length > 0
        )
      )
    );
    if (labels.length > 0) {
      pinLabels[`pin${pinNumber}`] = labels;
    }
  }
  let type = "spst";
  if (highestPinNumber >= 5) type = "dpdt";
  else if (highestPinNumber === 4) type = "dpst";
  else if (highestPinNumber === 3) type = "spdt";
  return {
    type,
    pinLabels: Object.keys(pinLabels).length > 0 ? pinLabels : void 0
  };
};
function inflateSourceSwitch(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const importedSwitchProps = getImportedSwitchProps(sourceElm, inflatorContext);
  const switchComponent = new Switch({
    name: sourceElm.name,
    displayName: sourceElm.display_name,
    ...importedSwitchProps,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: switchComponent
    });
    if (footprint) {
      switchComponent.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(switchComponent);
  } else {
    subcircuit.add(switchComponent);
  }
}

// lib/utils/pcbTraceRouteToPcbPath.ts
function pcbTraceRouteToPcbPath(route) {
  if (route.length <= 2) {
    return [];
  }
  const firstPoint = route[0];
  const lastPoint = route[route.length - 1];
  return route.slice(1, -1).filter((point6) => {
    const position = getRoutePointPosition(point6);
    const firstPosition = getRoutePointPosition(firstPoint);
    const lastPosition = getRoutePointPosition(lastPoint);
    const isSameAsFirst = position.x === firstPosition.x && position.y === firstPosition.y;
    const isSameAsLast = position.x === lastPosition.x && position.y === lastPosition.y;
    return !isSameAsFirst && !isSameAsLast;
  }).map((point6) => {
    if (point6.route_type === "via") {
      return {
        x: point6.x,
        y: point6.y,
        via: true,
        fromLayer: point6.from_layer,
        toLayer: point6.to_layer
      };
    }
    const position = getRoutePointPosition(point6);
    return { x: position.x, y: position.y };
  });
}

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceTrace.ts
var getSelectorPath = (component, inflatorContext) => {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const path_parts = [];
  let currentGroupId = component.source_group_id;
  while (currentGroupId && currentGroupId !== subcircuit.source_group_id) {
    const sourceGroup = injectionDb.source_group.get(currentGroupId);
    const groupInstance = groupsMap?.get(currentGroupId);
    if (!sourceGroup || !groupInstance) break;
    const groupName = groupInstance.props.name ?? groupInstance.fallbackUnassignedName;
    path_parts.unshift(`.${groupName}`);
    currentGroupId = sourceGroup.parent_source_group_id;
  }
  path_parts.push(`.${component.name}`);
  return path_parts.join(" > ");
};
function inflateSourceTrace(sourceTrace, inflatorContext) {
  const { injectionDb, subcircuit } = inflatorContext;
  const connectedSelectors = [];
  for (const sourcePortId of sourceTrace.connected_source_port_ids) {
    const sourcePort = injectionDb.source_port.get(sourcePortId);
    if (!sourcePort) {
      continue;
    }
    let selector;
    if (sourcePort.source_component_id) {
      const sourceComponent = injectionDb.source_component.get(
        sourcePort.source_component_id
      );
      if (sourceComponent) {
        const path = getSelectorPath(
          {
            name: sourceComponent.name,
            source_group_id: sourceComponent.source_group_id
          },
          inflatorContext
        );
        selector = `${path} > .${sourcePort.name}`;
      }
    } else {
      selector = `.${sourcePort.name}`;
    }
    if (selector) {
      connectedSelectors.push(selector);
    }
  }
  for (const sourceNetId of sourceTrace.connected_source_net_ids) {
    const sourceNet = injectionDb.source_net.get(sourceNetId);
    if (sourceNet) {
      connectedSelectors.push(`net.${sourceNet.name}`);
    }
  }
  if (connectedSelectors.length < 2) {
    return;
  }
  const pcbTraces = injectionDb.pcb_trace.list().filter((trace2) => trace2.source_trace_id === sourceTrace.source_trace_id);
  const pcbTrace = pcbTraces[0];
  let pcbPath;
  if (pcbTrace) {
    pcbPath = pcbTraceRouteToPcbPath(pcbTrace.route);
  }
  let traceWidth = sourceTrace.min_trace_thickness;
  if (!traceWidth && pcbTrace?.route) {
    const wirePoint = pcbTrace.route.find((pt) => pt.route_type === "wire");
    if (wirePoint && wirePoint.route_type === "wire") {
      traceWidth = wirePoint.width;
    }
  }
  const traceProps2 = {
    path: connectedSelectors
  };
  if (traceWidth !== void 0) {
    traceProps2.thickness = traceWidth;
  }
  if (pcbPath && pcbPath.length > 0) {
    traceProps2.pcbPath = pcbPath;
  } else if (!pcbTrace && sourceTrace.connected_source_port_ids.length === 2 && sourceTrace.connected_source_net_ids.length === 0) {
    traceProps2.pcbStraightLine = true;
  }
  const trace = new Trace3(traceProps2);
  trace._inflatedPcbTraces = pcbTraces.length > 0 ? pcbTraces : void 0;
  trace._inflatedPcbVias = pcbTrace ? injectionDb.pcb_via.list().filter(
    (via) => pcbTraces.some((trace2) => via.pcb_trace_id === trace2.pcb_trace_id)
  ) : void 0;
  subcircuit.add(trace);
}

// lib/components/normal-components/Transistor.ts
import { transistorProps } from "@tscircuit/props";
var SHORT_ALIASES = {
  collector: ["c"],
  base: ["b"],
  emitter: ["e"]
};
var Transistor = class extends NormalComponent3 {
  get config() {
    const baseSymbolName = this.props.type === "npn" ? "npn_bipolar_transistor" : "pnp_bipolar_transistor";
    return {
      componentName: "Transistor",
      schematicSymbolName: this.props.symbolName ?? baseSymbolName,
      zodProps: transistorProps,
      sourceFtype: "simple_transistor",
      shouldRenderAsSchematicBox: false
    };
  }
  initPorts() {
    const additionalAliases = {};
    const symbol = this.getSchematicSymbol();
    for (const symPort of symbol?.ports ?? []) {
      const pinNumber = getPinNumberFromLabels(symPort.labels);
      if (!pinNumber) continue;
      for (const label of symPort.labels) {
        const shortAliases = SHORT_ALIASES[label];
        if (shortAliases) {
          additionalAliases[`pin${pinNumber}`] = shortAliases;
        }
      }
    }
    super.initPorts({
      pinCount: 3,
      additionalAliases
    });
  }
  get collector() {
    return this.portMap.collector;
  }
  get base() {
    return this.portMap.base;
  }
  get emitter() {
    return this.portMap.emitter;
  }
  doInitialCreateNetsFromProps() {
    this._createNetsFromProps([...this._getNetsFromConnectionsProp()]);
  }
  doInitialCreateTracesFromProps() {
    this._createTracesFromConnectionsProp();
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_transistor",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      transistor_type: props.type,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateSourceTransistor.ts
function inflateSourceTransistor(sourceElm, inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const pcbElm = injectionDb.pcb_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const cadElm = injectionDb.cad_component.getWhere({
    source_component_id: sourceElm.source_component_id
  });
  const { pcbX, pcbY } = getInflatedPcbPlacement({
    pcbComponent: pcbElm,
    sourceGroupId: sourceElm.source_group_id,
    inflatorContext
  });
  const transistor = new Transistor({
    name: sourceElm.name,
    type: sourceElm.transistor_type,
    layer: pcbElm?.layer,
    pcbX,
    pcbY,
    pcbRotation: pcbElm?.rotation,
    doNotPlace: pcbElm?.do_not_place,
    obstructsWithinBounds: pcbElm?.obstructs_within_bounds
  });
  if (pcbElm) {
    const footprint = inflateFootprintComponent(pcbElm, {
      ...inflatorContext,
      normalComponent: transistor
    });
    if (footprint) {
      transistor.add(footprint);
    }
  }
  if (sourceElm.source_group_id && groupsMap?.has(sourceElm.source_group_id)) {
    const group = groupsMap.get(sourceElm.source_group_id);
    group.add(transistor);
  } else {
    subcircuit.add(transistor);
  }
}

// lib/components/primitive-components/Group/Subcircuit/inflators/inflateStandalonePcbPrimitives.ts
function inflateStandalonePcbPrimitives(inflatorContext) {
  const { injectionDb, subcircuit, groupsMap } = inflatorContext;
  const standalonePrimitiveTypes = [
    "pcb_silkscreen_rect",
    "pcb_silkscreen_circle",
    "pcb_silkscreen_line",
    "pcb_silkscreen_path",
    "pcb_silkscreen_text",
    "pcb_fabrication_note_text",
    "pcb_fabrication_note_path",
    "pcb_fabrication_note_rect",
    "pcb_note_text",
    "pcb_note_rect",
    "pcb_note_path",
    "pcb_note_line",
    "pcb_via"
  ];
  const standalonePrimitives = injectionDb.toArray().filter((elm) => {
    if (!standalonePrimitiveTypes.includes(elm.type)) return false;
    if (elm.type === "pcb_via") {
      if (!elm.pcb_trace_id) return true;
      const pcbTrace = injectionDb.pcb_trace.get(elm.pcb_trace_id);
      const sourceTraceId = pcbTrace?.source_trace_id;
      const isHandledByInflatedTrace = sourceTraceId && injectionDb.source_trace.get(sourceTraceId) && pcbTrace?.route.some(
        (point6) => point6.route_type === "via" && Math.abs(point6.x - elm.x) < 1e-4 && Math.abs(point6.y - elm.y) < 1e-4
      );
      return !isHandledByInflatedTrace;
    }
    return "pcb_component_id" in elm && (elm.pcb_component_id === null || elm.pcb_component_id === void 0);
  });
  if (standalonePrimitives.length === 0) return;
  const components = createComponentsFromCircuitJson(
    {
      componentName: "",
      componentRotation: "0deg"
    },
    standalonePrimitives
  );
  for (const component of components) {
    subcircuit.add(component);
  }
}

// lib/utils/circuit-json/inflate-circuit-json.ts
var inflateCircuitJson = (target, circuitJson, children) => {
  if (!circuitJson) return;
  const injectionDb = cju(circuitJson);
  if (circuitJson && children?.length > 0) {
    throw new Error("Component cannot have both circuitJson and children");
  }
  const groupsMap = /* @__PURE__ */ new Map();
  const inflationCtx = {
    injectionDb,
    subcircuit: target,
    groupsMap
  };
  const sourceGroups = injectionDb.source_group.list();
  const renderedGroupIds = /* @__PURE__ */ new Set();
  const groupsToRender = [...sourceGroups];
  while (groupsToRender.length > 0) {
    const groupIndex = groupsToRender.findIndex(
      (g) => !g.parent_source_group_id || renderedGroupIds.has(g.parent_source_group_id)
    );
    if (groupIndex === -1) {
      const remainingIds = groupsToRender.map((g) => g.source_group_id).join(", ");
      throw new Error(
        `Cannot inflate source_groups: cyclic dependency or missing parent detected. Remaining groups: ${remainingIds}`
      );
    }
    const groupToRender = groupsToRender[groupIndex];
    inflateSourceGroup(groupToRender, inflationCtx);
    renderedGroupIds.add(groupToRender.source_group_id);
    groupsToRender.splice(groupIndex, 1);
  }
  const pcbBoards = injectionDb.pcb_board.list();
  for (const pcbBoard of pcbBoards) {
    inflatePcbBoard(pcbBoard, inflationCtx);
  }
  const sourceComponents = injectionDb.source_component.list();
  for (const sourceComponent of sourceComponents) {
    switch (sourceComponent.ftype) {
      case "simple_resistor":
        inflateSourceResistor(sourceComponent, inflationCtx);
        break;
      case "simple_capacitor":
        inflateSourceCapacitor(sourceComponent, inflationCtx);
        break;
      case "simple_inductor":
        inflateSourceInductor(sourceComponent, inflationCtx);
        break;
      case "simple_diode":
        inflateSourceDiode(sourceComponent, inflationCtx);
        break;
      case "simple_fiducial":
        inflateSourceFiducial(sourceComponent, inflationCtx);
        break;
      case "simple_led":
        inflateSourceLed(sourceComponent, inflationCtx);
        break;
      case "simple_chip":
        inflateSourceChip(sourceComponent, inflationCtx);
        break;
      case "simple_connector":
        inflateSourceConnector(sourceComponent, inflationCtx);
        break;
      case "simple_transistor":
        inflateSourceTransistor(sourceComponent, inflationCtx);
        break;
      case "simple_mosfet":
        inflateSourceMosfet(sourceComponent, inflationCtx);
        break;
      case "simple_push_button":
        inflateSourcePushButton(sourceComponent, inflationCtx);
        break;
      case "simple_switch":
        inflateSourceSwitch(sourceComponent, inflationCtx);
        break;
      default:
        throw new Error(
          `No inflator implemented for source component ftype: "${sourceComponent.ftype}"`
        );
    }
  }
  const sourcePorts = injectionDb.source_port.list();
  for (const sourcePort of sourcePorts) {
    inflateSourcePort(sourcePort, inflationCtx);
  }
  const sourceTraces = injectionDb.source_trace.list();
  for (const sourceTrace of sourceTraces) {
    inflateSourceTrace(sourceTrace, inflationCtx);
  }
  inflateStandalonePcbPrimitives(inflationCtx);
};

// lib/components/primitive-components/DrcCheck.ts
import {
  drcCheckProps
} from "@tscircuit/props";
import { any_circuit_element } from "circuit-json";
var isSelectionResultPort = (connectable) => {
  if (!connectable || typeof connectable !== "object") return false;
  const maybePort = connectable;
  return typeof maybePort.getSourcePort === "function";
};
var isSelectionResultNet = (connectable) => {
  if (!connectable || typeof connectable !== "object") return false;
  const maybeNet = connectable;
  return typeof maybeNet.getSourceNet === "function";
};
var isSelectionResultComponent = (connectable) => {
  if (!connectable || typeof connectable !== "object") return false;
  const maybeComponent = connectable;
  return typeof maybeComponent.getPorts === "function";
};
var isCircuitJsonElement = (connectable) => {
  if (!connectable || typeof connectable !== "object") return false;
  const maybeCircuitJsonElement = connectable;
  return typeof maybeCircuitJsonElement.type === "string";
};
var isNonEmptyString = (value) => typeof value === "string" && value.length > 0;
var SelectionResultPortImpl = class {
  constructor(port) {
    this.port = port;
  }
  port;
  getPcbPort() {
    const pcbPortId = this.port.pcb_port_id;
    if (!pcbPortId) return null;
    return this.port.root?.db.pcb_port.get(pcbPortId) ?? null;
  }
  getSourcePort() {
    const sourcePortId = this.port.source_port_id;
    if (!sourcePortId) return null;
    return this.port.root?.db.source_port.get(sourcePortId) ?? null;
  }
};
var SelectionResultNetImpl = class {
  constructor(net) {
    this.net = net;
  }
  net;
  getSourceNet() {
    const sourceNetId = this.net.source_net_id;
    if (!sourceNetId) return null;
    return this.net.root?.db.source_net.get(sourceNetId) ?? null;
  }
};
var SelectionResultComponentImpl = class {
  constructor(component) {
    this.component = component;
  }
  component;
  getPort(name) {
    const port = this.component.selectOne(`port.${name}`, { type: "port" }) ?? this.component.selectOne(`.${name}`, { type: "port" });
    if (!port) return null;
    return new SelectionResultPortImpl(port);
  }
  getPorts() {
    return this.component.selectAll("port").map((port) => new SelectionResultPortImpl(port));
  }
  getPcbComponent() {
    const pcbComponentId = this.component.pcb_component_id;
    if (!pcbComponentId) return null;
    return this.component.root?.db.pcb_component.get(pcbComponentId) ?? null;
  }
  getSourceComponent() {
    const sourceComponentId = this.component.source_component_id;
    if (!sourceComponentId) return null;
    return this.component.root?.db.source_component.get(sourceComponentId) ?? null;
  }
};
var DrcCheck = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "DrcCheck",
      zodProps: drcCheckProps
    };
  }
  constructor(props) {
    super(props);
  }
  async runCustomDrcCheck(circuitJson) {
    const result = await this._parsedProps.checkFn(
      this.createCustomDrcCheckContext(circuitJson)
    );
    let diagnostics = [];
    if (Array.isArray(result)) {
      diagnostics = result;
    } else if (result) {
      diagnostics = [result];
    }
    return diagnostics.map(
      (diagnostic, index) => this.createCircuitJsonDiagnostic(diagnostic, index)
    ).filter((diagnostic) => !!diagnostic);
  }
  createCustomDrcCheckContext(circuitJson) {
    return {
      select: this.selectCustomDrcElement.bind(
        this
      ),
      selectAll: this.selectAllCustomDrcElements.bind(
        this
      ),
      isConnected: (a, b) => this.isConnected(a, b, circuitJson),
      isPulledUp: (a) => this.isConnected(a, "net.VCC", circuitJson) || this.isConnected(a, "net.VDD", circuitJson) || this.getResistanceBetween(a, "net.VCC", circuitJson) !== null || this.getResistanceBetween(a, "net.VDD", circuitJson) !== null,
      isPulledDown: (a) => this.isConnected(a, "net.GND", circuitJson) || this.getResistanceBetween(a, "net.GND", circuitJson) !== null,
      getResistanceBetween: (a, b) => this.getResistanceBetween(a, b, circuitJson)
    };
  }
  selectCustomDrcElement(selector) {
    return this.wrapSelectionResult(this.getSubcircuit().selectOne(selector));
  }
  selectAllCustomDrcElements(selector) {
    const selectionResults = [];
    for (const component of this.getSubcircuit().selectAll(selector)) {
      const selectionResult = this.wrapSelectionResult(component);
      if (selectionResult) selectionResults.push(selectionResult);
    }
    return selectionResults;
  }
  wrapSelectionResult(component) {
    if (!component) return null;
    if (component instanceof Port) return new SelectionResultPortImpl(component);
    if (component instanceof Net) return new SelectionResultNetImpl(component);
    return new SelectionResultComponentImpl(component);
  }
  createCircuitJsonDiagnostic(diagnostic, index) {
    const diagnosticRecord = diagnostic;
    let diagnosticType = "source_component_misconfigured_error";
    if (typeof diagnosticRecord.error_type === "string") {
      diagnosticType = diagnosticRecord.error_type;
    } else if (typeof diagnosticRecord.warning_type === "string") {
      diagnosticType = diagnosticRecord.warning_type;
    }
    const diagnosticWithCircuitJsonDefaults = {
      ...diagnostic
    };
    if (typeof diagnosticRecord.type === "string") {
      diagnosticWithCircuitJsonDefaults.type = diagnosticRecord.type;
    } else {
      diagnosticWithCircuitJsonDefaults.type = diagnosticType;
    }
    if (typeof diagnosticRecord.warning_type !== "string") {
      if (typeof diagnosticRecord.error_type === "string") {
        diagnosticWithCircuitJsonDefaults.error_type = diagnosticRecord.error_type;
      } else {
        diagnosticWithCircuitJsonDefaults.error_type = diagnosticType;
      }
    }
    const diagnosticIdField = `${diagnosticType}_id`;
    if (diagnosticRecord[diagnosticIdField]) {
      diagnosticWithCircuitJsonDefaults[diagnosticIdField] = diagnosticRecord[diagnosticIdField];
    } else {
      diagnosticWithCircuitJsonDefaults[diagnosticIdField] = `${diagnosticType}_${this._renderId}_${index}`;
    }
    return any_circuit_element.parse(diagnosticWithCircuitJsonDefaults);
  }
  isConnected(a, b, circuitJson) {
    const aKeys = this.getConnectivityKeys(a, circuitJson);
    const bKeys = this.getConnectivityKeys(b, circuitJson);
    return aKeys.some((key) => bKeys.includes(key));
  }
  getResistanceBetween(a, b, circuitJson) {
    if (this.isConnected(a, b, circuitJson)) return 0;
    const aKeys = this.getConnectivityKeys(a, circuitJson);
    const bKeys = this.getConnectivityKeys(b, circuitJson);
    if (aKeys.length === 0 || bKeys.length === 0) return null;
    const sourceResistors = circuitJson.filter(
      (elm) => elm.type === "source_component" && elm.ftype === "simple_resistor"
    );
    const root = this.root;
    if (!root) return null;
    for (const resistor of sourceResistors) {
      const resistorPorts = root.db.source_port.list({
        source_component_id: resistor.source_component_id
      });
      if (resistorPorts.length !== 2) continue;
      const resistorKeys = resistorPorts.map(
        (port) => port.subcircuit_connectivity_map_key
      );
      const connectsA = resistorKeys.some((key) => key && aKeys.includes(key));
      const connectsB = resistorKeys.some((key) => key && bKeys.includes(key));
      if (connectsA && connectsB) return resistor.resistance;
    }
    return null;
  }
  getConnectivityKeys(connectable, circuitJson) {
    if (!connectable) return [];
    if (typeof connectable === "string") {
      const selected = this.selectCustomDrcElement(connectable);
      if (selected) return this.getConnectivityKeys(selected, circuitJson);
      return [];
    }
    if (isSelectionResultPort(connectable)) {
      return [
        connectable.getSourcePort()?.subcircuit_connectivity_map_key
      ].filter(isNonEmptyString);
    }
    if (isSelectionResultNet(connectable)) {
      return [
        connectable.getSourceNet()?.subcircuit_connectivity_map_key
      ].filter(isNonEmptyString);
    }
    if (isSelectionResultComponent(connectable)) {
      return connectable.getPorts().flatMap((port) => this.getConnectivityKeys(port, circuitJson));
    }
    if (isCircuitJsonElement(connectable)) {
      return this.getCircuitJsonElementConnectivityKeys(
        connectable,
        circuitJson
      );
    }
    return [];
  }
  getCircuitJsonElementConnectivityKeys(element, circuitJson) {
    if (!element) return [];
    if (element.type === "source_port") {
      return [element.subcircuit_connectivity_map_key].filter(isNonEmptyString);
    }
    if (element.type === "source_net") {
      return [element.subcircuit_connectivity_map_key].filter(isNonEmptyString);
    }
    if (element.type === "pcb_port") {
      return this.getConnectivityKeys(element.source_port_id, circuitJson);
    }
    if (element.type === "source_component") {
      const root = this.root;
      if (!root) return [];
      return root.db.source_port.list({
        source_component_id: element.source_component_id
      }).map((port) => port.subcircuit_connectivity_map_key).filter(isNonEmptyString);
    }
    return [];
  }
};

// lib/IsolatedCircuit.ts
import { su as su5 } from "@tscircuit/circuit-json-util";
import Debug16 from "debug";
import { isValidElement as isValidElement2 } from "react";
import { identity as identity7 } from "transformation-matrix";
var IsolatedCircuit = class {
  firstChild = null;
  children;
  db;
  root = null;
  isRootCircuit = false;
  /**
   * Optional cache for isolated subcircuit circuit JSON, keyed by prop hash.
   * This is passed down from the RootCircuit when creating isolated circuits
   * for subcircuit rendering.
   */
  cachedSubcircuits;
  /**
   * Map to track pending renders by prop hash. This allows multiple subcircuits
   * with the same props to wait for a single render instead of each doing their own.
   */
  pendingSubcircuitRenders;
  _schematicDisabledOverride;
  get schematicDisabled() {
    if (this._schematicDisabledOverride !== void 0) {
      return this._schematicDisabledOverride;
    }
    if (this.platform?.schematicDisabled !== void 0) {
      return this.platform.schematicDisabled;
    }
    const board = this._getBoard();
    return board?._parsedProps?.schematicDisabled ?? false;
  }
  set schematicDisabled(value) {
    this._schematicDisabledOverride = value;
  }
  pcbDisabled = false;
  pcbRoutingDisabled = false;
  _featureMspSchematicTraceRouting = true;
  /**
   * The IsolatedCircuit name is usually set by the platform, it's not required but
   * if supplied can identify the circuit in certain effects, e.g. it is passed
   * as the display_name parameter for autorouting effects.
   */
  name;
  platform;
  /**
   * Optional URL pointing to where this project is hosted or documented.
   * When provided it is stored in the source_project_metadata.project_url field
   * of the generated Circuit JSON.
   */
  projectUrl;
  _hasRenderedAtleastOnce = false;
  _asyncEffectIdsByPhase = /* @__PURE__ */ new Map();
  _asyncEffectPhaseById = /* @__PURE__ */ new Map();
  _hasUnrenderedUpdatesFromAsyncEffects = false;
  _runningAsyncEffectsById = /* @__PURE__ */ new Map();
  constructor({
    platform,
    projectUrl,
    cachedSubcircuits,
    pendingSubcircuitRenders
  } = {}) {
    this.children = [];
    this.db = su5([]);
    this.platform = platform;
    this.projectUrl = projectUrl;
    this.pcbDisabled = platform?.pcbDisabled ?? false;
    this.pcbRoutingDisabled = platform?.routingDisabled ?? false;
    this.cachedSubcircuits = cachedSubcircuits;
    this.pendingSubcircuitRenders = pendingSubcircuitRenders;
    this.root = this;
  }
  add(componentOrElm) {
    let component;
    if (isValidElement2(componentOrElm)) {
      component = createInstanceFromReactElement(componentOrElm);
    } else {
      component = componentOrElm;
    }
    this.children.push(component);
  }
  setPlatform(platform) {
    this.platform = {
      ...this.platform,
      ...platform
    };
    if (platform.pcbDisabled !== void 0) {
      this.pcbDisabled = platform.pcbDisabled;
    }
    if (platform.routingDisabled !== void 0) {
      this.pcbRoutingDisabled = platform.routingDisabled;
    }
  }
  /**
   * Get the main board for this Circuit.
   */
  _getBoard() {
    const directBoard = this.children.find((c) => c.componentName === "Board");
    if (directBoard) {
      return directBoard;
    }
    return void 0;
  }
  _guessRootComponent() {
    if (this.firstChild) return;
    if (this.children.length === 0) {
      throw new Error(
        "Not able to guess root component: IsolatedCircuit has no children (use circuit.add(...))"
      );
    }
    const panels = this.children.filter(
      (child) => child.lowercaseComponentName === "panel"
    );
    if (panels.length > 1) {
      throw new Error("Only one <panel> is allowed per circuit");
    }
    if (panels.length === 1) {
      if (this.children.length !== 1) {
        throw new Error("<panel> must be the root element of the circuit");
      }
      this.firstChild = panels[0];
      return;
    }
    if (this.children.length === 1 && this.children[0].isGroup) {
      this.firstChild = this.children[0];
      return;
    }
    const group = new Group5({ subcircuit: true });
    group.parent = this;
    group.addAll(this.children);
    this.children = [group];
    this.firstChild = group;
  }
  render() {
    if (!this.firstChild) {
      this._guessRootComponent();
    }
    const { firstChild, db } = this;
    if (!firstChild) throw new Error("IsolatedCircuit has no root component");
    firstChild.parent = this;
    firstChild.runRenderCycle();
    this._hasUnrenderedUpdatesFromAsyncEffects = false;
    this._hasRenderedAtleastOnce = true;
  }
  async renderUntilSettled() {
    const existing = this.db.source_project_metadata.list()?.[0];
    if (!existing) {
      this.db.source_project_metadata.insert({
        software_used_string: `@tscircuit/core@${this.getCoreVersion()}`,
        ...this.projectUrl ? { project_url: this.projectUrl } : {}
      });
    }
    this.render();
    while (!this.isDoneRendering()) {
      await new Promise((resolve) => setTimeout(resolve, 100));
      this.render();
    }
    this.emit("renderComplete");
  }
  isDoneRendering() {
    return this._hasRenderedAtleastOnce && !this._hasIncompleteAsyncEffects();
  }
  _hasIncompleteAsyncEffects() {
    if (this._asyncEffectPhaseById.size > 0) return true;
    if (this._hasUnrenderedUpdatesFromAsyncEffects) return true;
    return this.children.some((child) => child._hasIncompleteAsyncEffects());
  }
  _hasIncompleteAsyncEffectsForPhase(phase) {
    return (this._asyncEffectIdsByPhase.get(phase)?.size ?? 0) > 0;
  }
  getRunningAsyncEffects() {
    return Array.from(this._runningAsyncEffectsById.values());
  }
  getCircuitJson() {
    if (!this._hasRenderedAtleastOnce) this.render();
    return this.db.toArray();
  }
  toJson() {
    return this.getCircuitJson();
  }
  async getSvg(options) {
    const circuitToSvg = await import("circuit-to-svg").catch((e) => {
      throw new Error(
        `To use circuit.getSvg, you must install the "circuit-to-svg" package.

"${e.message}"`
      );
    });
    if (options.view === "pcb") {
      return circuitToSvg.convertCircuitJsonToPcbSvg(this.getCircuitJson());
    }
    if (options.view === "schematic") {
      return circuitToSvg.convertCircuitJsonToSchematicSvg(
        this.getCircuitJson()
      );
    }
    throw new Error(`Invalid view: ${options.view}`);
  }
  getCoreVersion() {
    const [major, minor, patch] = package_default.version.split(".").map(Number);
    return `${major}.${minor}.${patch + 1}`;
  }
  async preview(previewNameOrOpts) {
    const previewOpts = typeof previewNameOrOpts === "object" ? previewNameOrOpts : { previewName: previewNameOrOpts };
    throw new Error("project.preview is not yet implemented");
  }
  computeSchematicGlobalTransform() {
    return identity7();
  }
  _computePcbGlobalTransformBeforeLayout() {
    return identity7();
  }
  selectAll(selector) {
    this._guessRootComponent();
    return this.firstChild?.selectAll(selector) ?? [];
  }
  selectOne(selector, opts) {
    this._guessRootComponent();
    return this.firstChild?.selectOne(selector, opts) ?? null;
  }
  _eventListeners = {};
  emit(event, ...args) {
    if (event === "asyncEffect:start") {
      this._registerAsyncEffectStart(args[0]);
    } else if (event === "asyncEffect:end") {
      this._registerAsyncEffectEnd(args[0]);
    }
    if (!this._eventListeners[event]) return;
    for (const listener of this._eventListeners[event]) {
      listener(...args);
    }
  }
  on(event, listener) {
    if (!this._eventListeners[event]) {
      this._eventListeners[event] = [];
    }
    this._eventListeners[event].push(listener);
  }
  removeListener(event, listener) {
    if (!this._eventListeners[event]) return;
    this._eventListeners[event] = this._eventListeners[event].filter(
      (l) => l !== listener
    );
  }
  enableDebug(debug13) {
    if (typeof debug13 === "string") {
      Debug16.enable(debug13);
    } else if (debug13 === null || debug13 === false) {
      Debug16.disable();
    }
  }
  getClientOrigin() {
    if (typeof window !== "undefined" && window.location) {
      return window.location.origin;
    }
    if (typeof self !== "undefined" && self.location) {
      return self.location.origin;
    }
    return "";
  }
  _registerAsyncEffectStart(payload) {
    if (!payload?.asyncEffectId || !payload.phase) return;
    const { asyncEffectId, phase } = payload;
    const existingPhase = this._asyncEffectPhaseById.get(asyncEffectId);
    if (existingPhase && existingPhase !== phase) {
      this._asyncEffectIdsByPhase.get(existingPhase)?.delete(asyncEffectId);
    }
    if (!this._asyncEffectIdsByPhase.has(phase)) {
      this._asyncEffectIdsByPhase.set(phase, /* @__PURE__ */ new Set());
    }
    this._asyncEffectIdsByPhase.get(phase).add(asyncEffectId);
    this._asyncEffectPhaseById.set(asyncEffectId, phase);
    this._runningAsyncEffectsById.set(asyncEffectId, {
      asyncEffectId,
      effectName: payload.effectName,
      componentDisplayName: payload.componentDisplayName,
      phase,
      error: payload.error
    });
  }
  _registerAsyncEffectEnd(payload) {
    if (!payload?.asyncEffectId) return;
    const { asyncEffectId } = payload;
    const phase = this._asyncEffectPhaseById.get(asyncEffectId) ?? payload.phase;
    if (phase) {
      const phaseSet = this._asyncEffectIdsByPhase.get(phase);
      phaseSet?.delete(asyncEffectId);
      if (phaseSet && phaseSet.size === 0) {
        this._asyncEffectIdsByPhase.delete(phase);
      }
    }
    this._asyncEffectPhaseById.delete(asyncEffectId);
    this._runningAsyncEffectsById.delete(asyncEffectId);
    this._hasUnrenderedUpdatesFromAsyncEffects = true;
  }
};

// lib/components/primitive-components/Group/Subcircuit/Subcircuit_doInitialRenderIsolatedSubcircuits.ts
function Subcircuit_doInitialRenderIsolatedSubcircuits(subcircuit) {
  if (!subcircuit._isIsolatedSubcircuit) return;
  if (subcircuit._isolatedCircuitJson) return;
  if (!subcircuit.getSubcircuitPropHash) return;
  const propHash = subcircuit.getSubcircuitPropHash();
  const cachedSubcircuits = subcircuit.root?.cachedSubcircuits;
  const pendingSubcircuitRenders = subcircuit.root?.pendingSubcircuitRenders;
  const cached = cachedSubcircuits?.get(propHash);
  if (cached) {
    subcircuit._isolatedCircuitJson = cached;
    subcircuit.children = [];
    subcircuit._normalComponentNameMap = null;
    return;
  }
  const childrenToRender = [...subcircuit.children];
  subcircuit.children = [];
  subcircuit._normalComponentNameMap = null;
  const parentRoot = subcircuit.root;
  subcircuit._queueAsyncEffect("render-isolated-subcircuit", async () => {
    const cachedResult = cachedSubcircuits?.get(propHash);
    if (cachedResult) {
      subcircuit._isolatedCircuitJson = cachedResult;
      return;
    }
    const pendingRenderPromise = pendingSubcircuitRenders?.get(propHash);
    if (pendingRenderPromise) {
      subcircuit._isolatedCircuitJson = await pendingRenderPromise;
      return;
    }
    let resolveRender;
    let rejectRender;
    const renderPromise = new Promise(
      (resolve, reject) => {
        resolveRender = resolve;
        rejectRender = reject;
      }
    );
    pendingSubcircuitRenders?.set(propHash, renderPromise);
    try {
      const isolatedCircuit = new IsolatedCircuit({
        platform: {
          ...parentRoot.platform,
          pcbDisabled: parentRoot.pcbDisabled,
          schematicDisabled: parentRoot.schematicDisabled
        },
        cachedSubcircuits,
        pendingSubcircuitRenders
      });
      for (const child of childrenToRender) {
        isolatedCircuit.add(child);
      }
      await isolatedCircuit.renderUntilSettled();
      const circuitJson = isolatedCircuit.getCircuitJson();
      cachedSubcircuits?.set(propHash, circuitJson);
      subcircuit._isolatedCircuitJson = circuitJson;
      resolveRender(circuitJson);
    } catch (error) {
      rejectRender(error instanceof Error ? error : new Error(String(error)));
      throw error;
    } finally {
      pendingSubcircuitRenders?.delete(propHash);
    }
  });
}

// lib/components/primitive-components/Group/Subcircuit_getSubcircuitPropHash.ts
var EXCLUDED_PROPS = /* @__PURE__ */ new Set([
  "name",
  "key",
  "pcbX",
  "pcbY",
  "schX",
  "schY",
  "pcbLeftEdgeX",
  "pcbRightEdgeX",
  "pcbTopEdgeY",
  "pcbBottomEdgeY",
  "pcbRotation",
  "schRotation"
]);
function safeSerialize(value, seen = /* @__PURE__ */ new WeakSet()) {
  if (value === null) return "null";
  if (value === void 0) return "undefined";
  const type = typeof value;
  if (type === "string") return `"${value}"`;
  if (type === "number" || type === "boolean") return String(value);
  if (type === "function") return "[function]";
  if (type === "symbol") return "[symbol]";
  if (type === "object") {
    if (seen.has(value)) return "[circular]";
    seen.add(value);
    if (value.$$typeof !== void 0) {
      const elementType = typeof value.type === "string" ? value.type : value.type?.name || "[component]";
      const propsStr = value.props ? safeSerialize(value.props, seen) : "{}";
      return `ReactElement(${elementType},${propsStr})`;
    }
    if (Array.isArray(value)) {
      const items = value.map((v) => safeSerialize(v, seen)).join(",");
      return `[${items}]`;
    }
    const keys = Object.keys(value).sort();
    const pairs3 = keys.map((k) => `${k}:${safeSerialize(value[k], seen)}`);
    return `{${pairs3.join(",")}}`;
  }
  return String(value);
}
function getHashableProps(props) {
  const result = {};
  const keys = Object.keys(props).sort();
  for (const key of keys) {
    if (!EXCLUDED_PROPS.has(key) && props[key] !== void 0) {
      result[key] = props[key];
    }
  }
  return result;
}
function getChildrenHashData(children) {
  return children.map((child) => ({
    componentName: child.componentName,
    props: getHashableProps(child.props ?? {}),
    children: getChildrenHashData(child.children)
  }));
}
function fnv1aHash(str) {
  let hash = 2166136261;
  for (let i = 0; i < str.length; i++) {
    hash ^= str.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }
  return hash >>> 0;
}
function computeHash(data) {
  const serialized = safeSerialize(data);
  const hash1 = fnv1aHash(serialized);
  const hash2 = fnv1aHash(serialized + hash1.toString());
  return hash1.toString(16).padStart(8, "0") + hash2.toString(16).padStart(8, "0");
}
function Subcircuit_getSubcircuitPropHash(subcircuit) {
  const hashableData = {
    props: getHashableProps(subcircuit.props ?? {}),
    children: getChildrenHashData(subcircuit.children)
  };
  return computeHash(hashableData);
}

// lib/components/normal-components/Board_doInitialPcbPlacementDesignRuleChecks.ts
import { runAllPlacementChecks } from "@tscircuit/checks";
var resetPcbTraceRenderInSubtree = (renderable) => {
  if (renderable._pcbTraceRenderWaitingForPlacementChecks) {
    renderable.renderPhaseStates.PcbTraceRender.initialized = false;
    renderable.renderPhaseStates.PcbTraceRender.dirty = false;
    renderable._pcbTraceRenderWaitingForPlacementChecks = false;
  }
  for (const child of renderable.children) {
    resetPcbTraceRenderInSubtree(child);
  }
};
var Board_doInitialPcbPlacementDesignRuleChecks = (board) => {
  if (board.root?.pcbDisabled) return;
  const placementDrcChecksDisabled = board.root?.platform?.placementDrcChecksDisabled ?? board.getInheritedProperty("placementDrcChecksDisabled");
  const drcChecksDisabled = board.root?.platform?.drcChecksDisabled ?? board.getInheritedProperty("drcChecksDisabled");
  board._pcbPlacementDrcErrorCount = null;
  board._pcbPlacementDrcCheckError = null;
  board._pcbPlacementDrcChecksPending = false;
  if (placementDrcChecksDisabled || drcChecksDisabled) {
    board._pcbPlacementDrcErrorCount = 0;
    return;
  }
  const { db } = board.root;
  const subcircuitCircuitJson = db.subtree({ subcircuit_id: board.subcircuit_id }).toArray();
  const existingPlacementDiagnostics = db.toArray();
  board._pcbPlacementDrcChecksPending = true;
  board._queueAsyncEffect("board:pre-route-placement-checks", async () => {
    try {
      const placementCheckResults = await runAllPlacementChecks(
        subcircuitCircuitJson
      );
      const newPlacementDiagnostics = placementCheckResults.filter(
        (result) => !existingPlacementDiagnostics.some(
          (existing) => existing.type === result.type && "message" in existing && existing.message === result.message
        )
      );
      db.insertAll(newPlacementDiagnostics);
      board._pcbPlacementDrcErrorCount = placementCheckResults.filter(
        (result) => result.type.endsWith("_error")
      ).length;
    } catch (error) {
      board._pcbPlacementDrcCheckError = error instanceof Error ? error.message : String(error);
    } finally {
      board._pcbPlacementDrcChecksPending = false;
      resetPcbTraceRenderInSubtree(board);
    }
  });
};

// lib/components/normal-components/Board.ts
var MIN_EFFECTIVE_BORDER_RADIUS_MM = 0.01;
var getRoundedRectOutline = (width, height, radius) => {
  const w2 = width / 2;
  const h2 = height / 2;
  const r = Math.min(radius, w2, h2);
  if (r < MIN_EFFECTIVE_BORDER_RADIUS_MM) {
    return [
      { x: -w2, y: -h2 },
      { x: w2, y: -h2 },
      { x: w2, y: h2 },
      { x: -w2, y: h2 }
    ];
  }
  const maxArcLengthPerSegment = 0.1;
  const segments = Math.max(
    1,
    Math.ceil(Math.PI / 2 * r / maxArcLengthPerSegment)
  );
  const step = Math.PI / 2 / segments;
  const outline = [];
  outline.push({ x: -w2 + r, y: -h2 });
  outline.push({ x: w2 - r, y: -h2 });
  for (let i = 1; i <= segments; i++) {
    const theta = -Math.PI / 2 + i * step;
    outline.push({
      x: w2 - r + r * Math.cos(theta),
      y: -h2 + r + r * Math.sin(theta)
    });
  }
  outline.push({ x: w2, y: h2 - r });
  for (let i = 1; i <= segments; i++) {
    const theta = 0 + i * step;
    outline.push({
      x: w2 - r + r * Math.cos(theta),
      y: h2 - r + r * Math.sin(theta)
    });
  }
  outline.push({ x: -w2 + r, y: h2 });
  for (let i = 1; i <= segments; i++) {
    const theta = Math.PI / 2 + i * step;
    outline.push({
      x: -w2 + r + r * Math.cos(theta),
      y: h2 - r + r * Math.sin(theta)
    });
  }
  outline.push({ x: -w2, y: -h2 + r });
  for (let i = 1; i <= segments; i++) {
    const theta = Math.PI + i * step;
    outline.push({
      x: -w2 + r + r * Math.cos(theta),
      y: -h2 + r + r * Math.sin(theta)
    });
  }
  return outline;
};
var Board = class extends Group5 {
  pcb_board_id = null;
  source_board_id = null;
  _drcChecksComplete = false;
  _drcChecksInProgress = false;
  _connectedSchematicPortPairs = /* @__PURE__ */ new Set();
  _panelPositionOffset = null;
  get isSubcircuit() {
    return true;
  }
  get isGroup() {
    return true;
  }
  get config() {
    return {
      componentName: "Board",
      zodProps: boardProps
    };
  }
  get boardThickness() {
    return this._parsedProps.thickness ?? 1.4;
  }
  /**
   * Get all available layers for the board
   */
  get allLayers() {
    return getBoardAvailableLayers(this._parsedProps.layers ?? 2);
  }
  _getSubcircuitLayerCount() {
    return this._parsedProps.layers ?? 2;
  }
  _computePcbGlobalTransformBeforeLayout() {
    if (this._panelPositionOffset) {
      const parentTransform = this.parent?._computePcbGlobalTransformBeforeLayout?.() ?? { a: 1, b: 0, c: 0, d: 1, e: 0, f: 0 };
      return compose8(
        parentTransform,
        translate8(this._panelPositionOffset.x, this._panelPositionOffset.y)
      );
    }
    return super._computePcbGlobalTransformBeforeLayout();
  }
  _getBoardCalcVariables() {
    const { _parsedProps: props } = this;
    const isAutoSized = (props.width == null || props.height == null) && !props.outline;
    if (isAutoSized) return {};
    const dbBoard = this.pcb_board_id ? this.root?.db.pcb_board.get(this.pcb_board_id) : null;
    let width = dbBoard?.width ?? props.width;
    let height = dbBoard?.height ?? props.height;
    if ((width == null || height == null) && props.outline?.length) {
      const outlineBounds = getBoundsFromPoints7(props.outline);
      if (outlineBounds) {
        width ??= outlineBounds.maxX - outlineBounds.minX;
        height ??= outlineBounds.maxY - outlineBounds.minY;
      }
    }
    const { pcbX, pcbY } = this.getResolvedPcbPositionProp();
    const center = dbBoard?.center ?? {
      x: pcbX + (props.outlineOffsetX ?? 0),
      y: pcbY + (props.outlineOffsetY ?? 0)
    };
    const resolvedWidth = width ?? 0;
    const resolvedHeight = height ?? 0;
    return {
      "board.minX": center.x - resolvedWidth / 2,
      "board.maxX": center.x + resolvedWidth / 2,
      "board.minY": center.y - resolvedHeight / 2,
      "board.maxY": center.y + resolvedHeight / 2
    };
  }
  doInitialPcbBoardAutoSize() {
    if (this.root?.pcbDisabled) return;
    if (!this.pcb_board_id) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const globalPos = this._getGlobalPcbPositionBeforeLayout();
    const pcbBoard = db.pcb_board.get(this.pcb_board_id);
    if (pcbBoard?.width && pcbBoard?.height || pcbBoard?.outline && pcbBoard.outline.length > 0)
      return;
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    const descendantIds = getDescendantSubcircuitIds(db, this.subcircuit_id);
    const allowedSubcircuitIds = /* @__PURE__ */ new Set([this.subcircuit_id, ...descendantIds]);
    const allPcbComponents = db.pcb_component.list().filter(
      (c) => c.subcircuit_id && allowedSubcircuitIds.has(c.subcircuit_id)
    );
    const allPcbGroups = db.pcb_group.list().filter(
      (g) => g.subcircuit_id && allowedSubcircuitIds.has(g.subcircuit_id)
    );
    let hasComponents = false;
    const updateBounds = (center2, width, height) => {
      if (width === 0 || height === 0) return;
      hasComponents = true;
      minX = Math.min(minX, center2.x - width / 2);
      minY = Math.min(minY, center2.y - height / 2);
      maxX = Math.max(maxX, center2.x + width / 2);
      maxY = Math.max(maxY, center2.y + height / 2);
    };
    for (const pcbComponent of allPcbComponents) {
      updateBounds(pcbComponent.center, pcbComponent.width, pcbComponent.height);
    }
    for (const pcbGroup of allPcbGroups) {
      let width = pcbGroup.width ?? 0;
      let height = pcbGroup.height ?? 0;
      if (pcbGroup.outline && pcbGroup.outline.length > 0) {
        const bounds = getBoundsFromPoints7(pcbGroup.outline);
        if (bounds) {
          width = bounds.maxX - bounds.minX;
          height = bounds.maxY - bounds.minY;
        }
      }
      updateBounds(pcbGroup.center, width, height);
    }
    if (props.boardAnchorPosition) {
      const { x, y } = props.boardAnchorPosition;
      minX = Math.min(minX, x);
      minY = Math.min(minY, y);
      maxX = Math.max(maxX, x);
      maxY = Math.max(maxY, y);
    }
    const padding = 2;
    const computedWidth = hasComponents ? maxX - minX + padding * 2 : 0;
    const computedHeight = hasComponents ? maxY - minY + padding * 2 : 0;
    const center = {
      x: hasComponents ? (minX + maxX) / 2 + (props.outlineOffsetX ?? 0) : (props.outlineOffsetX ?? 0) + globalPos.x,
      y: hasComponents ? (minY + maxY) / 2 + (props.outlineOffsetY ?? 0) : (props.outlineOffsetY ?? 0) + globalPos.y
    };
    const finalWidth = props.width ?? computedWidth;
    const finalHeight = props.height ?? computedHeight;
    let outline = props.outline;
    if (!outline && props.borderRadius != null && finalWidth > 0 && finalHeight > 0) {
      outline = getRoundedRectOutline(
        finalWidth,
        finalHeight,
        props.borderRadius
      );
    }
    const update = {
      width: finalWidth,
      height: finalHeight,
      center
    };
    if (outline) {
      update.outline = outline.map((point6) => ({
        x: point6.x + (props.outlineOffsetX ?? 0),
        y: point6.y + (props.outlineOffsetY ?? 0)
      }));
    }
    db.pcb_board.update(this.pcb_board_id, update);
  }
  // Recompute autosize after child components update (e.g., async footprints)
  updatePcbBoardAutoSize() {
    this.doInitialPcbBoardAutoSize();
  }
  /**
   * Update the board information silkscreen text if platform config is set and
   * the project name, version, or url is set.
   */
  _addBoardInformationToSilkscreen() {
    const platform = this.root?.platform;
    if (!platform?.printBoardInformationToSilkscreen) return;
    const pcbBoard = this.root.db.pcb_board.get(this.pcb_board_id);
    if (!pcbBoard) return;
    const boardInformation = [];
    if (platform.projectName) boardInformation.push(platform.projectName);
    if (platform.version) boardInformation.push(`v${platform.version}`);
    if (platform.url) boardInformation.push(platform.url);
    if (boardInformation.length === 0) return;
    const text = boardInformation.join("\n");
    const marginX = 0.25;
    const marginY = 1;
    const position = {
      x: pcbBoard.center.x + pcbBoard.width / 2 - marginX,
      y: pcbBoard.center.y - pcbBoard.height / 2 + marginY
    };
    this.root.db.pcb_silkscreen_text.insert({
      pcb_component_id: this.pcb_board_id,
      layer: "top",
      font: "tscircuit2024",
      font_size: 0.45,
      text,
      ccw_rotation: 0,
      anchor_alignment: "bottom_right",
      anchor_position: position
    });
  }
  doInitialSourceRender() {
    const nestedBoard = this.getDescendants().find(
      (d) => d.lowercaseComponentName === "board"
    );
    if (nestedBoard) {
      throw new Error(
        `Nested boards are not supported: found board "${nestedBoard.name}" inside board "${this.name}"`
      );
    }
    super.doInitialSourceRender();
    const { db } = this.root;
    const source_board = db.source_board.insert({
      source_group_id: this.source_group_id,
      title: this.props.title || this.props.name
    });
    this.source_board_id = source_board.source_board_id;
  }
  /**
   * Computes a hash of this board's props and children for caching.
   * Position/identity props are excluded so identical boards at
   * different locations share the same hash.
   */
  getSubcircuitPropHash() {
    return Subcircuit_getSubcircuitPropHash(this);
  }
  /**
   * Render this board in isolation if _subcircuitCachingEnabled is set.
   * This phase runs before InflateSubcircuitCircuitJson to prepare the
   * isolated circuit JSON that will be inflated.
   */
  doInitialRenderIsolatedSubcircuits() {
    Subcircuit_doInitialRenderIsolatedSubcircuits(this);
  }
  doInitialInflateSubcircuitCircuitJson() {
    const isolatedJson = this._isolatedCircuitJson;
    if (isolatedJson) {
      this._isInflatedFromCircuitJson = true;
      this._isolatedCircuitJson = null;
      inflateCircuitJson(this, isolatedJson, []);
      return;
    }
    const { circuitJson, children } = this._parsedProps;
    if (circuitJson) {
      this._isInflatedFromCircuitJson = true;
    }
    inflateCircuitJson(this, circuitJson, children);
  }
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const circuitJsonElements = props.circuitJson;
    const pcbBoardFromCircuitJson = circuitJsonElements?.find(
      (elm) => elm.type === "pcb_board"
    );
    let computedWidth = props.width ?? pcbBoardFromCircuitJson?.width ?? 0;
    let computedHeight = props.height ?? pcbBoardFromCircuitJson?.height ?? 0;
    const globalPos = this._getGlobalPcbPositionBeforeLayout();
    let center = {
      x: globalPos.x + (props.outlineOffsetX ?? 0),
      y: globalPos.y + (props.outlineOffsetY ?? 0)
    };
    const { boardAnchorPosition, boardAnchorAlignment } = props;
    if (boardAnchorPosition) {
      center = getBoardCenterFromAnchor({
        boardAnchorPosition,
        boardAnchorAlignment: boardAnchorAlignment ?? "center",
        width: computedWidth,
        height: computedHeight
      });
    }
    if (props.outline) {
      const xValues = props.outline.map((point6) => point6.x);
      const yValues = props.outline.map((point6) => point6.y);
      const minX = Math.min(...xValues);
      const maxX = Math.max(...xValues);
      const minY = Math.min(...yValues);
      const maxY = Math.max(...yValues);
      computedWidth = maxX - minX;
      computedHeight = maxY - minY;
    }
    let outline = props.outline;
    if (!outline && props.borderRadius != null && computedWidth > 0 && computedHeight > 0) {
      outline = getRoundedRectOutline(
        computedWidth,
        computedHeight,
        props.borderRadius
      );
    }
    let outlineTranslation = { x: 0, y: 0 };
    if (outline && outline.length > 0 && (this.parent?.lowercaseComponentName === "panel" || this.parent?.lowercaseComponentName === "subpanel")) {
      const outlineBounds = getBoundsFromPoints7(outline);
      if (outlineBounds) {
        const outlineCenterX = (outlineBounds.minX + outlineBounds.maxX) / 2;
        const outlineCenterY = (outlineBounds.minY + outlineBounds.maxY) / 2;
        outlineTranslation = {
          x: center.x - outlineCenterX,
          y: center.y - outlineCenterY
        };
      }
    }
    const subcircuitProps2 = this.getSubcircuit()._parsedProps;
    const pcb_board = db.pcb_board.insert({
      source_board_id: this.source_board_id,
      center,
      thickness: this.boardThickness,
      num_layers: this.allLayers.length,
      width: computedWidth,
      height: computedHeight,
      outline: outline?.map((point6) => ({
        x: point6.x + (props.outlineOffsetX ?? 0) + outlineTranslation.x,
        y: point6.y + (props.outlineOffsetY ?? 0) + outlineTranslation.y
      })),
      material: props.material,
      ...props.isViaInPadAllowed !== void 0 && {
        is_via_in_pad_allowed: props.isViaInPadAllowed
      },
      ...props.solderMaskColor !== void 0 && {
        solder_mask_color: props.solderMaskColor
      },
      ...props.silkscreenColor !== void 0 && {
        silkscreen_color: props.silkscreenColor
      },
      min_trace_width: subcircuitProps2.minTraceWidth ?? jlcMinTolerances.min_trace_width,
      min_via_hole_diameter: subcircuitProps2.minViaHoleDiameter ?? jlcMinTolerances.min_via_hole_diameter,
      min_via_pad_diameter: subcircuitProps2.minViaPadDiameter ?? jlcMinTolerances.min_via_pad_diameter,
      min_via_hole_edge_to_via_hole_edge_clearance: subcircuitProps2.minViaHoleEdgeToViaHoleEdgeClearance ?? jlcMinTolerances.min_via_hole_edge_to_via_hole_edge_clearance,
      min_via_edge_to_pad_edge_clearance: subcircuitProps2.minViaEdgeToPadEdgeClearance ?? jlcMinTolerances.min_via_edge_to_pad_edge_clearance,
      min_trace_to_pad_edge_clearance: subcircuitProps2.minTraceToPadEdgeClearance ?? jlcMinTolerances.min_trace_to_pad_edge_clearance,
      min_pad_edge_to_pad_edge_clearance: subcircuitProps2.minPadEdgeToPadEdgeClearance ?? jlcMinTolerances.min_pad_edge_to_pad_edge_clearance,
      min_plated_hole_drill_edge_to_drill_edge_clearance: subcircuitProps2.minPlatedHoleDrillEdgeToDrillEdgeClearance ?? jlcMinTolerances.min_plated_hole_drill_edge_to_drill_edge_clearance,
      min_board_edge_clearance: subcircuitProps2.minBoardEdgeClearance ?? jlcMinTolerances.min_board_edge_clearance
    });
    this.pcb_board_id = pcb_board.pcb_board_id;
    this._addBoardInformationToSilkscreen();
  }
  removePcbComponentRender() {
    const { db } = this.root;
    if (!this.pcb_board_id) return;
    db.pcb_board.delete(this.pcb_board_id);
    this.pcb_board_id = null;
  }
  doInitialPcbDesignRuleChecks() {
    if (this.root?.pcbDisabled) return;
    super.doInitialPcbDesignRuleChecks();
    this.updatePcbDesignRuleChecks();
  }
  doInitialPcbPlacementDesignRuleChecks() {
    Board_doInitialPcbPlacementDesignRuleChecks(this);
  }
  updatePcbDesignRuleChecks() {
    const { db } = this.root;
    const routingDisabled = this.root?.pcbRoutingDisabled || this.getInheritedProperty("routingDisabled");
    const pcbDisabled = this.root?.pcbDisabled;
    const drcChecksDisabled = this.root?.platform?.drcChecksDisabled ?? this.getInheritedProperty("drcChecksDisabled");
    const netlistDrcChecksDisabled = this.root?.platform?.netlistDrcChecksDisabled ?? this.getInheritedProperty("netlistDrcChecksDisabled");
    const pinSpecificationDrcChecksDisabled = this.getInheritedProperty(
      "pinSpecificationDrcChecksDisabled"
    );
    const placementDrcChecksDisabled = this.root?.platform?.placementDrcChecksDisabled ?? this.getInheritedProperty("placementDrcChecksDisabled");
    const routingDrcChecksDisabled = this.root?.platform?.routingDrcChecksDisabled ?? this.getInheritedProperty("routingDrcChecksDisabled");
    const shouldRunNetlistChecks = !drcChecksDisabled && !netlistDrcChecksDisabled;
    const shouldRunPinSpecificationChecks = !drcChecksDisabled && !pinSpecificationDrcChecksDisabled;
    const shouldRunPlacementChecks = !drcChecksDisabled && !pcbDisabled && !placementDrcChecksDisabled;
    const shouldRunRoutingChecks = !drcChecksDisabled && !pcbDisabled && !routingDisabled && !routingDrcChecksDisabled;
    if (shouldRunRoutingChecks && this._hasIncompleteAsyncEffectsInSubtreeForPhase("PcbTraceRender"))
      return;
    const hasTracesToRoute = this._hasTracesToRoute();
    if (shouldRunRoutingChecks && hasTracesToRoute && !this._areChildSubcircuitsRouted())
      return;
    if (this._drcChecksComplete || this._drcChecksInProgress) return;
    const runDrcChecks = async (circuitJson) => {
      const checksToRun = [];
      if (shouldRunRoutingChecks) {
        checksToRun.push(
          runAllRoutingChecks(circuitJson)
        );
      }
      if (shouldRunPlacementChecks) {
        const existingPlacementDiagnostics = db.toArray();
        checksToRun.push(
          runAllPlacementChecks2(circuitJson).then(
            (results) => results.filter(
              (result) => !existingPlacementDiagnostics.some(
                (existing) => existing.type === result.type && "message" in existing && existing.message === result.message
              )
            )
          )
        );
      }
      if (shouldRunNetlistChecks) {
        checksToRun.push(
          runAllNetlistChecks(circuitJson)
        );
      }
      if (shouldRunPinSpecificationChecks) {
        checksToRun.push(
          runAllPinSpecificationChecks(circuitJson)
        );
      }
      if (!drcChecksDisabled) {
        for (const drcCheck of this.selectAll("drccheck")) {
          checksToRun.push(drcCheck.runCustomDrcCheck(circuitJson));
        }
      }
      const checkResults = await Promise.all(checksToRun);
      db.insertAll(dedupePcbDrcErrors(checkResults.flat()));
    };
    const subcircuit = db.subtree({ subcircuit_id: this.subcircuit_id });
    const subcircuitCircuitJson = subcircuit.toArray();
    this._drcChecksInProgress = true;
    this._queueAsyncEffect("board:drc-checks", async () => {
      try {
        await runDrcChecks(subcircuitCircuitJson);
        this._drcChecksComplete = true;
      } finally {
        this._drcChecksInProgress = false;
      }
    });
  }
  _emitRenderLifecycleEvent(phase, startOrEnd) {
    super._emitRenderLifecycleEvent(phase, startOrEnd);
    if (startOrEnd === "start") {
      this.root?.emit("board:renderPhaseStarted", {
        renderId: this._renderId,
        phase
      });
    }
  }
  _repositionOnPcb(position) {
    const { db } = this.root;
    const pcbBoard = this.pcb_board_id ? db.pcb_board.get(this.pcb_board_id) : null;
    const oldPos = pcbBoard?.center;
    if (!oldPos) {
      if (this.pcb_board_id) {
        db.pcb_board.update(this.pcb_board_id, { center: position });
      }
      return;
    }
    const deltaX = position.x - oldPos.x;
    const deltaY = position.y - oldPos.y;
    if (Math.abs(deltaX) < 1e-6 && Math.abs(deltaY) < 1e-6) {
      return;
    }
    if (this.pcb_board_id) {
      db.pcb_board.update(this.pcb_board_id, { center: position });
      if (pcbBoard?.outline) {
        const outlineBounds = getBoundsFromPoints7(pcbBoard.outline);
        if (outlineBounds) {
          const oldOutlineCenter = {
            x: (outlineBounds.minX + outlineBounds.maxX) / 2,
            y: (outlineBounds.minY + outlineBounds.maxY) / 2
          };
          const outlineDeltaX = position.x - oldOutlineCenter.x;
          const outlineDeltaY = position.y - oldOutlineCenter.y;
          const newOutline = pcbBoard.outline.map((p) => ({
            x: p.x + outlineDeltaX,
            y: p.y + outlineDeltaY
          }));
          db.pcb_board.update(this.pcb_board_id, {
            outline: newOutline
          });
        }
      }
    }
  }
};

// lib/components/normal-components/MountedBoard.ts
import { mountedboardProps } from "@tscircuit/props";

// lib/components/primitive-components/Group/Subcircuit/Subcircuit.ts
import "@tscircuit/props";
var Subcircuit = class extends Group5 {
  constructor(props) {
    super({
      ...props,
      // @ts-ignore
      subcircuit: true
    });
  }
  /**
   * Computes a hash of this subcircuit's props and children for caching.
   * Position/identity props are excluded so identical subcircuits at
   * different locations share the same hash.
   */
  getSubcircuitPropHash() {
    return Subcircuit_getSubcircuitPropHash(this);
  }
  /**
   * Render this subcircuit in isolation if _subcircuitCachingEnabled is set.
   * This phase runs before InflateSubcircuitCircuitJson to prepare the
   * isolated circuit JSON that will be inflated.
   */
  doInitialRenderIsolatedSubcircuits() {
    Subcircuit_doInitialRenderIsolatedSubcircuits(this);
  }
  doInitialCreateNetsFromProps() {
    super.doInitialCreateNetsFromProps();
    this._createTracesForExposedConnections();
  }
  _createTracesForExposedConnections() {
    const exposedNets = this._getNetNamesToExpose();
    if (!exposedNets?.length) return;
    const parentSubcircuit = this.parent?.getSubcircuit?.();
    if (!parentSubcircuit) return;
    if (!this.name) return;
    for (const exposedNetName of exposedNets) {
      const netName = normalizeExposedNetName(exposedNetName);
      const parentNetSelector = `> net.${netName}`;
      const childNetSelector = `.${this.name} > net.${netName}`;
      const parentNet = parentSubcircuit.children.find(
        (child) => child instanceof Net && child._parsedProps.name === netName
      );
      if (!parentNet) {
        parentSubcircuit.add(new Net({ name: netName }));
      }
      const existingTrace = parentSubcircuit.children.find((child) => {
        if (!(child instanceof Trace3)) return false;
        if (!child._exposesSubcircuitConnection) return false;
        const childProps = child._parsedProps;
        return childProps.from === childNetSelector && childProps.to === parentNetSelector;
      });
      if (existingTrace) continue;
      const trace = new Trace3({
        from: childNetSelector,
        to: parentNetSelector,
        displayName: netName
      });
      trace._exposesSubcircuitConnection = true;
      parentSubcircuit.add(trace);
    }
  }
  _getNetNamesToExpose() {
    const explicitExposedNets = this._parsedProps.exposedNets ?? [];
    if (!this._parsedProps.exposeNets) return explicitExposedNets;
    const childNetNames = this.selectAll("net").filter((net) => net instanceof Net && net.getSubcircuit() === this).map((net) => net._parsedProps.name);
    return Array.from(/* @__PURE__ */ new Set([...explicitExposedNets, ...childNetNames]));
  }
  /**
   * During this phase, we inflate the subcircuit circuit json into class
   * instances
   *
   * When subcircuit's define circuitJson, it's basically the same as having
   * a tree of components. All the data from circuit json has to be converted
   * into props for the tree of components
   *
   * We do this in two phases:
   * - Create the components
   * - Create the groups
   * - Add components to groups in the appropriate hierarchy
   */
  doInitialInflateSubcircuitCircuitJson() {
    const isolatedJson = this._isolatedCircuitJson;
    if (isolatedJson) {
      this._isInflatedFromCircuitJson = true;
      this._isolatedCircuitJson = null;
      inflateCircuitJson(this, isolatedJson, []);
      return;
    }
    const { circuitJson, children } = this._parsedProps;
    if (circuitJson) {
      this._isInflatedFromCircuitJson = true;
    }
    inflateCircuitJson(this, circuitJson, children);
  }
};
var normalizeExposedNetName = (netName) => netName.startsWith("net.") ? netName.slice("net.".length) : netName;

// lib/components/normal-components/MountedBoard.ts
var MountedBoard = class extends Subcircuit {
  pcb_board_id = null;
  get config() {
    return {
      componentName: "MountedBoard",
      zodProps: mountedboardProps
    };
  }
  get boardThickness() {
    return 1.4;
  }
  get allLayers() {
    return ["top", "bottom"];
  }
  _connectedSchematicPortPairs = /* @__PURE__ */ new Set();
  /**
   * Returns the carrier board's calc variables so that calc(board.*)
   * expressions in pcbX/pcbY resolve against the parent board.
   */
  _getBoardCalcVariables() {
    const carrierBoard = this._findCarrierBoard();
    return carrierBoard?._getBoardCalcVariables() ?? {};
  }
  _findCarrierBoard() {
    let current = this.parent;
    while (current) {
      if (current instanceof Board) {
        return current;
      }
      current = current.parent;
    }
    return null;
  }
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const globalPos = this._getGlobalPcbPositionBeforeLayout();
    const pcb_board = db.pcb_board.insert({
      center: { x: globalPos.x, y: globalPos.y },
      width: props.width ?? 0,
      height: props.height ?? 0,
      is_mounted_to_carrier_board: true
    });
    this.pcb_board_id = pcb_board.pcb_board_id;
  }
  doInitialPcbBoardAutoSize() {
    if (!this.pcb_board_id || !this.root) return;
    const carrierBoard = this._findCarrierBoard();
    if (carrierBoard?.pcb_board_id) {
      this.root.db.pcb_board.update(this.pcb_board_id, {
        carrier_pcb_board_id: carrierBoard.pcb_board_id
      });
    }
  }
};

// lib/components/normal-components/Panel.ts
import { panelProps } from "@tscircuit/props";
import { distance as distance16 } from "circuit-json";

// lib/components/normal-components/Subpanel.ts
import { subpanelProps } from "@tscircuit/props";
import { distance as distance15 } from "circuit-json";
import { compose as compose9, identity as identity8, translate as translate9 } from "transformation-matrix";

// lib/utils/panels/generate-panel-tabs-and-mouse-bites.ts
import * as Flatten from "@flatten-js/core";
import { point as point5, Polygon as Polygon3 } from "@flatten-js/core";
var DEFAULT_TAB_LENGTH = 5;
var DEFAULT_TAB_WIDTH = 2;
var generateCutoutsAndMousebitesForOutline = (outline, options) => {
  const {
    gapLength,
    cutoutWidth,
    mouseBites,
    mouseBiteHoleDiameter,
    mouseBiteHoleSpacing
  } = options;
  const tabCutouts = [];
  const mouseBiteHoles = [];
  if (outline.length < 2) {
    return { tabCutouts, mouseBiteHoles };
  }
  const outlinePolygon = new Flatten.Polygon(
    outline.map((p) => Flatten.point(p.x, p.y))
  );
  let is_ccw;
  if (outline.length > 2) {
    const p0 = Flatten.point(outline[0].x, outline[0].y);
    const p1 = Flatten.point(outline[1].x, outline[1].y);
    const segmentDir = Flatten.vector(p0, p1).normalize();
    const normalToLeft = segmentDir.rotate(Math.PI / 2);
    const midPoint = p0.translate(
      segmentDir.multiply(Flatten.segment(p0, p1).length / 2)
    );
    const testPoint = midPoint.translate(normalToLeft.multiply(0.01));
    is_ccw = outlinePolygon.contains(testPoint);
  } else {
    is_ccw = outlinePolygon.area() > 0;
  }
  for (let i = 0; i < outline.length; i++) {
    const p1_ = outline[i];
    const p2_ = outline[(i + 1) % outline.length];
    if (!p1_ || !p2_) continue;
    const p1 = Flatten.point(p1_.x, p1_.y);
    const p2 = Flatten.point(p2_.x, p2_.y);
    const segment2 = Flatten.segment(p1, p2);
    const segmentLength = segment2.length;
    if (segmentLength < 1e-6) continue;
    const segmentVec = Flatten.vector(p1, p2);
    const segmentDir = segmentVec.normalize();
    let normalVec = segmentDir.rotate(Math.PI / 2);
    const midPoint = segment2.middle();
    const testPoint = midPoint.translate(normalVec.multiply(0.01));
    if (outlinePolygon.contains(testPoint)) {
      normalVec = normalVec.multiply(-1);
    }
    const numBitesInGap = 2;
    const totalBitesLength = numBitesInGap * mouseBiteHoleDiameter + (numBitesInGap - 1) * mouseBiteHoleSpacing;
    let effectiveGapLength;
    if (mouseBites) {
      effectiveGapLength = totalBitesLength;
    } else {
      effectiveGapLength = gapLength;
    }
    effectiveGapLength = Math.min(effectiveGapLength, segmentLength * 0.9);
    const gapStartDist = (segmentLength - effectiveGapLength) / 2;
    const gapEndDist = gapStartDist + effectiveGapLength;
    if (mouseBites) {
      const holeAndSpacing = mouseBiteHoleDiameter + mouseBiteHoleSpacing;
      if (effectiveGapLength >= totalBitesLength && holeAndSpacing > 0) {
        const firstBiteCenterOffsetInGap = (effectiveGapLength - totalBitesLength) / 2 + mouseBiteHoleDiameter / 2;
        const firstBiteDistFromP1 = gapStartDist + firstBiteCenterOffsetInGap;
        for (let k = 0; k < numBitesInGap; k++) {
          const biteDist = firstBiteDistFromP1 + k * holeAndSpacing;
          const pos = p1.translate(segmentDir.multiply(biteDist));
          mouseBiteHoles.push({ x: pos.x, y: pos.y });
        }
      }
    }
    const p_prev_ = outline[(i - 1 + outline.length) % outline.length];
    const p_next_ = outline[(i + 2) % outline.length];
    let start_ext = 0;
    let end_ext = 0;
    if (p_prev_ && p_next_) {
      const vec_in_p1 = Flatten.vector(Flatten.point(p_prev_.x, p_prev_.y), p1);
      const p1_cross = vec_in_p1.cross(segmentVec);
      const is_p1_convex = is_ccw ? p1_cross > 1e-9 : p1_cross < -1e-9;
      const vec_out_p2 = Flatten.vector(p2, Flatten.point(p_next_.x, p_next_.y));
      const p2_cross = segmentVec.cross(vec_out_p2);
      const is_p2_convex = is_ccw ? p2_cross > 1e-9 : p2_cross < -1e-9;
      if (is_p1_convex) {
        let angle = vec_in_p1.angleTo(segmentVec);
        if (angle > Math.PI) angle = 2 * Math.PI - angle;
        start_ext = cutoutWidth * Math.tan(angle / 2);
      } else {
        start_ext = 0;
      }
      if (is_p2_convex) {
        let angle = segmentVec.angleTo(vec_out_p2);
        if (angle > Math.PI) angle = 2 * Math.PI - angle;
        end_ext = cutoutWidth * Math.tan(angle / 2);
      } else {
        end_ext = 0;
      }
    }
    const cutoutParts = [
      { start: 0 - start_ext, end: gapStartDist },
      { start: gapEndDist, end: segmentLength + end_ext }
    ];
    const extrusion = normalVec.multiply(cutoutWidth);
    for (const part of cutoutParts) {
      const partLength = part.end - part.start;
      if (partLength < 1e-6) continue;
      const partCenterAlongSegment = p1.translate(
        segmentDir.multiply(part.start + partLength / 2)
      );
      const center = partCenterAlongSegment.translate(extrusion.multiply(0.5));
      const width = partLength;
      const height = cutoutWidth;
      const rotationDeg = segmentDir.slope * 180 / Math.PI;
      tabCutouts.push({
        type: "pcb_cutout",
        shape: "rect",
        center: { x: center.x, y: center.y },
        width,
        height,
        rotation: rotationDeg,
        corner_radius: cutoutWidth / 2
      });
    }
  }
  return { tabCutouts, mouseBiteHoles };
};
function generatePanelTabsAndMouseBites(boards, options) {
  const finalTabCutouts = [];
  const allMouseBites = [];
  let { tabWidth, tabLength, mouseBites: useMouseBites } = options;
  const boardDimensions = [];
  for (const board of boards) {
    if (board.width && board.height) {
      boardDimensions.push(Math.min(board.width, board.height));
    } else if (board.outline && board.outline.length > 0) {
      const outlinePolygon = new Polygon3(
        board.outline.map((p) => point5(p.x, p.y))
      );
      const area = Math.abs(outlinePolygon.area());
      if (area > 0) {
        boardDimensions.push(Math.sqrt(area));
      }
    }
  }
  if (boardDimensions.length > 0) {
    const minBoardDimension = Math.min(...boardDimensions);
    const scaleFactor = minBoardDimension / 20;
    tabWidth = Math.min(
      tabWidth,
      DEFAULT_TAB_WIDTH * Math.max(scaleFactor, 0.3)
    );
    tabLength = Math.min(
      tabLength,
      DEFAULT_TAB_LENGTH * Math.max(scaleFactor, 0.3)
    );
  }
  const processedBoards = boards.map((board) => {
    if ((!board.outline || board.outline.length === 0) && board.width && board.height) {
      const w2 = board.width / 2;
      const h2 = board.height / 2;
      return {
        ...board,
        outline: [
          { x: board.center.x - w2, y: board.center.y - h2 },
          { x: board.center.x + w2, y: board.center.y - h2 },
          { x: board.center.x + w2, y: board.center.y + h2 },
          { x: board.center.x - w2, y: board.center.y + h2 }
        ]
      };
    }
    return board;
  });
  for (const board of processedBoards) {
    if (board.outline && board.outline.length > 0) {
      const mouseBiteDiameter2 = tabWidth * 0.45;
      const mouseBiteSpacing = mouseBiteDiameter2 * 0.1;
      const generated = generateCutoutsAndMousebitesForOutline(board.outline, {
        gapLength: tabLength,
        cutoutWidth: tabWidth,
        mouseBites: useMouseBites,
        mouseBiteHoleDiameter: mouseBiteDiameter2,
        mouseBiteHoleSpacing: mouseBiteSpacing
      });
      finalTabCutouts.push(...generated.tabCutouts);
      allMouseBites.push(...generated.mouseBiteHoles);
    }
  }
  const tabCutouts = finalTabCutouts.map((tab, index) => ({
    ...tab,
    pcb_cutout_id: `panel_tab_${index}`
  }));
  const mouseBiteDiameter = tabWidth * 0.45;
  const mouseBiteHoles = allMouseBites.map((bite, index) => ({
    type: "pcb_hole",
    pcb_hole_id: `panel_mouse_bite_${index}`,
    hole_shape: "circle",
    hole_diameter: mouseBiteDiameter,
    x: bite.x,
    y: bite.y
  }));
  return {
    tabCutouts,
    mouseBiteHoles
  };
}

// lib/utils/panels/pack-into-grid.ts
import { distance as distance14 } from "circuit-json";

// lib/utils/panels/get-board-dimensions-from-props.ts
import { getBoundsFromPoints as getBoundsFromPoints8 } from "@tscircuit/math-utils";
import { distance as distance13 } from "circuit-json";
var getBoardDimensionsFromProps = (board) => {
  const props = board._parsedProps;
  let width = props.width != null ? distance13.parse(props.width) : void 0;
  let height = props.height != null ? distance13.parse(props.height) : void 0;
  if ((width === void 0 || height === void 0) && props.outline?.length) {
    const outlineBounds = getBoundsFromPoints8(props.outline);
    if (outlineBounds) {
      width ??= outlineBounds.maxX - outlineBounds.minX;
      height ??= outlineBounds.maxY - outlineBounds.minY;
    }
  }
  if ((width === void 0 || height === void 0) && props.circuitJson?.length) {
    const pcbBoardFromJson = props.circuitJson.find(
      (elm) => elm.type === "pcb_board"
    );
    if (pcbBoardFromJson) {
      width ??= pcbBoardFromJson.width;
      height ??= pcbBoardFromJson.height;
    }
  }
  return {
    width: width ?? 0,
    height: height ?? 0
  };
};

// lib/utils/panels/pack-into-grid.ts
function packIntoGrid({
  items,
  db,
  row,
  col,
  cellWidth,
  cellHeight,
  boardGap,
  availablePanelWidth,
  availablePanelHeight
}) {
  const itemsWithDims = items.map((item) => {
    const dims = getItemDimensions(item, db);
    return { item, width: dims.width, height: dims.height };
  }).filter((item) => !(item.width === 0 && item.height === 0));
  if (itemsWithDims.length === 0) {
    return { positions: [], gridWidth: 0, gridHeight: 0 };
  }
  let cols;
  let rows;
  const minCellWidth = cellWidth ? distance14.parse(cellWidth) : 0;
  const minCellHeight = cellHeight ? distance14.parse(cellHeight) : 0;
  if (col !== void 0) {
    cols = col;
    rows = row ?? Math.ceil(itemsWithDims.length / cols);
  } else if (row !== void 0) {
    rows = row;
    cols = Math.ceil(itemsWithDims.length / rows);
  } else if (availablePanelWidth !== void 0 && availablePanelHeight !== void 0) {
    const maxItemWidth = Math.max(
      ...itemsWithDims.map((b) => b.width),
      minCellWidth
    );
    const maxItemHeight = Math.max(
      ...itemsWithDims.map((b) => b.height),
      minCellHeight
    );
    const maxCols = Math.max(
      1,
      Math.floor((availablePanelWidth + boardGap) / (maxItemWidth + boardGap))
    );
    const maxRows = Math.max(
      1,
      Math.floor(
        (availablePanelHeight + boardGap) / (maxItemHeight + boardGap)
      )
    );
    cols = maxCols;
    rows = Math.ceil(itemsWithDims.length / cols);
    if (rows > maxRows) {
      rows = maxRows;
      cols = Math.ceil(itemsWithDims.length / rows);
      if (cols > maxCols) {
        cols = maxCols;
        rows = Math.ceil(itemsWithDims.length / cols);
      }
    }
    cols = Math.max(1, cols);
    rows = Math.max(1, rows);
  } else {
    cols = Math.ceil(Math.sqrt(itemsWithDims.length));
    rows = Math.ceil(itemsWithDims.length / cols);
  }
  const colWidths = Array(cols).fill(0);
  const rowHeights = Array(rows).fill(0);
  itemsWithDims.forEach((item, i) => {
    const colIdx = i % cols;
    const rowIdx = Math.floor(i / cols);
    if (rowIdx < rowHeights.length && item.height > rowHeights[rowIdx]) {
      rowHeights[rowIdx] = item.height;
    }
    if (colIdx < colWidths.length && item.width > colWidths[colIdx]) {
      colWidths[colIdx] = item.width;
    }
  });
  for (let i = 0; i < colWidths.length; i++) {
    colWidths[i] = Math.max(colWidths[i], minCellWidth);
  }
  for (let i = 0; i < rowHeights.length; i++) {
    rowHeights[i] = Math.max(rowHeights[i], minCellHeight);
  }
  const gridWidth = colWidths.reduce((a, b) => a + b, 0) + (cols > 1 ? (cols - 1) * boardGap : 0);
  const gridHeight = rowHeights.reduce((a, b) => a + b, 0) + (rows > 1 ? (rows - 1) * boardGap : 0);
  const startX = -gridWidth / 2;
  const startY = -gridHeight / 2;
  const colXOffsets = [startX];
  for (let i = 1; i < cols; i++) {
    colXOffsets.push(colXOffsets[i - 1] + colWidths[i - 1] + boardGap);
  }
  const rowYOffsets = [startY];
  for (let i = 1; i < rows; i++) {
    rowYOffsets.push(rowYOffsets[i - 1] + rowHeights[i - 1] + boardGap);
  }
  const positions = [];
  itemsWithDims.forEach((itemWithDims, i) => {
    const colIdx = i % cols;
    const rowIdx = Math.floor(i / cols);
    if (rowIdx >= rowYOffsets.length || colIdx >= colXOffsets.length) return;
    const x = colXOffsets[colIdx] + colWidths[colIdx] / 2;
    const y = rowYOffsets[rowIdx] + rowHeights[rowIdx] / 2;
    positions.push({ item: itemWithDims.item, pos: { x, y } });
  });
  return { positions, gridWidth, gridHeight };
}
function getItemDimensions(item, db) {
  if (item.componentName === "Board") {
    const board = item;
    if (db && board.pcb_board_id) {
      const pcbBoard = db.pcb_board.get(board.pcb_board_id);
      if (pcbBoard?.width !== void 0 && pcbBoard?.height !== void 0) {
        return { width: pcbBoard.width, height: pcbBoard.height };
      }
    }
    return getBoardDimensionsFromProps(board);
  }
  if (item.componentName === "Subpanel") {
    const subpanel = item;
    const props = subpanel._parsedProps;
    if (props.width !== void 0 && props.height !== void 0) {
      return {
        width: distance14.parse(props.width),
        height: distance14.parse(props.height)
      };
    }
    const directBoards = subpanel._getDirectBoardChildren();
    if (directBoards.length === 0) {
      const allBoards = subpanel._getAllBoardInstances();
      if (allBoards.length === 0) return { width: 0, height: 0 };
      let minX = Infinity;
      let minY = Infinity;
      let maxX = -Infinity;
      let maxY = -Infinity;
      for (const board of allBoards) {
        const dims = getBoardDimensionsFromProps(board);
        if (dims.width === 0 || dims.height === 0) continue;
        const offset = board._panelPositionOffset ?? { x: 0, y: 0 };
        minX = Math.min(minX, offset.x - dims.width / 2);
        maxX = Math.max(maxX, offset.x + dims.width / 2);
        minY = Math.min(minY, offset.y - dims.height / 2);
        maxY = Math.max(maxY, offset.y + dims.height / 2);
      }
      if (minX === Infinity) return { width: 0, height: 0 };
      return { width: maxX - minX, height: maxY - minY };
    }
    if (directBoards.length === 1) {
      return getBoardDimensionsFromProps(directBoards[0]);
    }
    if (subpanel._cachedGridWidth > 0 && subpanel._cachedGridHeight > 0) {
      const edgePadding = distance14.parse(props.edgePadding ?? 5);
      return {
        width: subpanel._cachedGridWidth + edgePadding * 2,
        height: subpanel._cachedGridHeight + edgePadding * 2
      };
    }
    let totalWidth = 0;
    let totalHeight = 0;
    for (const board of directBoards) {
      const dims = getBoardDimensionsFromProps(board);
      totalWidth = Math.max(totalWidth, dims.width);
      totalHeight = Math.max(totalHeight, dims.height);
    }
    return { width: totalWidth, height: totalHeight };
  }
  return { width: 0, height: 0 };
}

// lib/components/normal-components/Subpanel.ts
var Subpanel = class _Subpanel extends Group5 {
  pcb_panel_id = null;
  _tabsAndMouseBitesGenerated = false;
  get config() {
    return {
      componentName: "Subpanel",
      zodProps: subpanelProps
    };
  }
  get _errorComponentName() {
    return this.componentName.toLowerCase();
  }
  get isGroup() {
    return true;
  }
  get isSubcircuit() {
    return true;
  }
  /**
   * Subpanels/Panels use their own layout system (doInitialPanelBoardLayout)
   * and should never trigger the regular Group pack/grid/flex layout.
   */
  _getPcbLayoutMode() {
    return "none";
  }
  /**
   * Subpanels/Panels are PCB panelization containers. Their child boards render
   * schematics independently, so the container itself should not create a
   * schematic group, run schematic layout, or invoke schematic trace solving.
   */
  doInitialSchematicComponentRender() {
  }
  doInitialSchematicLayout() {
  }
  doInitialSchematicTraceRender() {
  }
  add(component) {
    if (component.lowercaseComponentName !== "board" && component.lowercaseComponentName !== "subpanel") {
      throw new Error(
        `<${this._errorComponentName}> can only contain <board> or <subpanel> elements`
      );
    }
    super.add(component);
  }
  _cachedGridWidth = 0;
  _cachedGridHeight = 0;
  _panelPositionOffset = null;
  _computePcbGlobalTransformBeforeLayout() {
    if (this._panelPositionOffset) {
      const parentTransform = this.parent?._computePcbGlobalTransformBeforeLayout?.() ?? identity8();
      return compose9(
        parentTransform,
        translate9(this._panelPositionOffset.x, this._panelPositionOffset.y)
      );
    }
    return super._computePcbGlobalTransformBeforeLayout();
  }
  /**
   * Get all board instances from this subpanel and nested subpanels
   */
  _getAllBoardInstances() {
    const boards = [];
    for (const child of this.children) {
      if (child instanceof Board) {
        boards.push(child);
      } else if (child instanceof _Subpanel) {
        boards.push(...child._getAllBoardInstances());
      }
    }
    return boards;
  }
  /**
   * Check if this subpanel contains at least one board (directly or through nested subpanels)
   */
  _containsBoards() {
    for (const child of this.children) {
      if (child.componentName === "Board") {
        return true;
      }
      if (child.componentName === "Subpanel" && "_containsBoards" in child) {
        if (child._containsBoards()) {
          return true;
        }
      }
    }
    return false;
  }
  /**
   * Get direct board children only (not from nested subpanels)
   */
  _getDirectBoardChildren() {
    return this.children.filter((c) => c instanceof Board);
  }
  doInitialPanelBoardLayout() {
    if (this.root?.pcbDisabled) return;
    const layoutMode = this._parsedProps.layoutMode ?? "none";
    const gridItems = this.children.filter(
      (c) => c instanceof Board || c instanceof _Subpanel
    );
    if (layoutMode !== "none") {
      for (const child of gridItems) {
        if (!(child instanceof Board)) continue;
        if (child._hasUserDefinedPcbPosition()) {
          this.root.db.source_property_ignored_warning.insert({
            source_component_id: child.source_component_id,
            property_name: "pcbX/pcbY",
            message: `Board has manual positioning but ${this._errorComponentName} layout mode is "${layoutMode}". Manual positioning will be ignored.`,
            error_type: "source_property_ignored_warning"
          });
        }
      }
    }
    if (layoutMode === "none" && gridItems.length > 1) {
      const unpositionedItems = gridItems.filter(
        (c) => !c._hasUserDefinedPcbPosition()
      );
      if (unpositionedItems.length > 1) {
        this.root.db.pcb_placement_error.insert({
          error_type: "pcb_placement_error",
          message: `Multiple boards/subpanels in ${this._errorComponentName} without positions. When layoutMode="none", each item must have explicit positioning. Use layoutMode="grid" for automatic positioning.`
        });
      }
    }
    if (layoutMode !== "grid") return;
    const tabWidth = this._parsedProps.tabWidth ?? DEFAULT_TAB_WIDTH;
    const boardGap = this._parsedProps.boardGap ?? tabWidth;
    let availablePanelWidth;
    let availablePanelHeight;
    const hasExplicitRowOrCol = this._parsedProps.row !== void 0 || this._parsedProps.col !== void 0;
    const hasExplicitWidth = this._parsedProps.width !== void 0;
    const hasExplicitHeight = this._parsedProps.height !== void 0;
    if (!hasExplicitRowOrCol && hasExplicitWidth && hasExplicitHeight) {
      const {
        edgePadding: edgePaddingProp,
        edgePaddingLeft: edgePaddingLeftProp,
        edgePaddingRight: edgePaddingRightProp,
        edgePaddingTop: edgePaddingTopProp,
        edgePaddingBottom: edgePaddingBottomProp
      } = this._parsedProps;
      const edgePadding = distance15.parse(edgePaddingProp ?? 5);
      const edgePaddingLeft = distance15.parse(edgePaddingLeftProp ?? edgePadding);
      const edgePaddingRight = distance15.parse(
        edgePaddingRightProp ?? edgePadding
      );
      const edgePaddingTop = distance15.parse(edgePaddingTopProp ?? edgePadding);
      const edgePaddingBottom = distance15.parse(
        edgePaddingBottomProp ?? edgePadding
      );
      const panelWidth = distance15.parse(this._parsedProps.width);
      const panelHeight = distance15.parse(this._parsedProps.height);
      availablePanelWidth = panelWidth - edgePaddingLeft - edgePaddingRight;
      availablePanelHeight = panelHeight - edgePaddingTop - edgePaddingBottom;
    }
    const { positions, gridWidth, gridHeight } = packIntoGrid({
      items: gridItems,
      row: this._parsedProps.row,
      col: this._parsedProps.col,
      cellWidth: this._parsedProps.cellWidth,
      cellHeight: this._parsedProps.cellHeight,
      boardGap,
      availablePanelHeight,
      availablePanelWidth
    });
    this._cachedGridWidth = gridWidth;
    this._cachedGridHeight = gridHeight;
    for (const { item, pos } of positions) {
      item._panelPositionOffset = pos;
    }
  }
  doInitialPanelLayout() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const layoutMode = this._parsedProps.layoutMode ?? "none";
    if (layoutMode === "grid") {
      for (const child of this.children) {
        if (child instanceof Board) {
          if (!child.pcb_board_id || !child._panelPositionOffset) continue;
          db.pcb_board.update(child.pcb_board_id, {
            position_mode: "relative_to_panel_anchor",
            display_offset_x: `${child._panelPositionOffset.x}mm`,
            display_offset_y: `${child._panelPositionOffset.y}mm`
          });
        } else if (child instanceof _Subpanel && child._panelPositionOffset) {
          for (const board of child._getAllBoardInstances()) {
            if (!board.pcb_board_id) continue;
            const boardOffset = board._panelPositionOffset ?? { x: 0, y: 0 };
            db.pcb_board.update(board.pcb_board_id, {
              position_mode: "relative_to_panel_anchor",
              display_offset_x: `${child._panelPositionOffset.x + boardOffset.x}mm`,
              display_offset_y: `${child._panelPositionOffset.y + boardOffset.y}mm`
            });
          }
        }
      }
      this._updatePanelDimensions();
    } else {
      const panelGlobalPos = this._getGlobalPcbPositionBeforeLayout();
      for (const board of this._getDirectBoardChildren()) {
        if (!board.pcb_board_id) continue;
        const boardDb = db.pcb_board.get(board.pcb_board_id);
        if (!boardDb) continue;
        const relativeX = boardDb.center.x - panelGlobalPos.x;
        const relativeY = boardDb.center.y - panelGlobalPos.y;
        db.pcb_board.update(board.pcb_board_id, {
          position_mode: "relative_to_panel_anchor",
          display_offset_x: `${relativeX}mm`,
          display_offset_y: `${relativeY}mm`
        });
      }
    }
    this._generateTabsAndMouseBites();
  }
  /**
   * Update dimensions for the subpanel. Subpanel updates pcb_group,
   */
  _updatePanelDimensions() {
    const { db } = this.root;
    const hasExplicitWidth = this._parsedProps.width !== void 0;
    const hasExplicitHeight = this._parsedProps.height !== void 0;
    const gridWidth = this._cachedGridWidth;
    const gridHeight = this._cachedGridHeight;
    if (!this.pcb_group_id) return;
    if (hasExplicitWidth && hasExplicitHeight) {
      db.pcb_group.update(this.pcb_group_id, {
        width: distance15.parse(this._parsedProps.width),
        height: distance15.parse(this._parsedProps.height)
      });
    } else if (gridWidth > 0 || gridHeight > 0) {
      const {
        edgePadding: edgePaddingProp,
        edgePaddingLeft: edgePaddingLeftProp,
        edgePaddingRight: edgePaddingRightProp,
        edgePaddingTop: edgePaddingTopProp,
        edgePaddingBottom: edgePaddingBottomProp
      } = this._parsedProps;
      const edgePadding = distance15.parse(edgePaddingProp ?? 5);
      const edgePaddingLeft = distance15.parse(edgePaddingLeftProp ?? edgePadding);
      const edgePaddingRight = distance15.parse(
        edgePaddingRightProp ?? edgePadding
      );
      const edgePaddingTop = distance15.parse(edgePaddingTopProp ?? edgePadding);
      const edgePaddingBottom = distance15.parse(
        edgePaddingBottomProp ?? edgePadding
      );
      db.pcb_group.update(this.pcb_group_id, {
        width: hasExplicitWidth ? distance15.parse(this._parsedProps.width) : gridWidth + edgePaddingLeft + edgePaddingRight,
        height: hasExplicitHeight ? distance15.parse(this._parsedProps.height) : gridHeight + edgePaddingTop + edgePaddingBottom
      });
    }
  }
  /**
   * Generate tabs and mouse bites for panelization
   */
  _generateTabsAndMouseBites() {
    if (this._tabsAndMouseBitesGenerated) return;
    const { db } = this.root;
    const props = this._parsedProps;
    const panelizationMethod = props.panelizationMethod ?? "none";
    const childBoardInstances = this._getAllBoardInstances();
    if (panelizationMethod !== "none") {
      const childBoardIds = childBoardInstances.map((c) => c.pcb_board_id).filter((id) => !!id);
      const boardsInPanel = db.pcb_board.list().filter((b) => childBoardIds.includes(b.pcb_board_id));
      if (boardsInPanel.length === 0) return;
      const tabWidth = props.tabWidth ?? DEFAULT_TAB_WIDTH;
      const boardGap = props.boardGap ?? tabWidth;
      const { tabCutouts, mouseBiteHoles } = generatePanelTabsAndMouseBites(
        boardsInPanel,
        {
          boardGap,
          tabWidth,
          tabLength: props.tabLength ?? DEFAULT_TAB_LENGTH,
          mouseBites: props.mouseBites ?? true
        }
      );
      for (const tabCutout of tabCutouts) {
        db.pcb_cutout.insert(tabCutout);
      }
      for (const mouseBiteHole of mouseBiteHoles) {
        db.pcb_hole.insert(mouseBiteHole);
      }
    }
    this._tabsAndMouseBitesGenerated = true;
  }
  /**
   * Override to validate board containment before rendering.
   * Subpanel uses parent Group's pcb_group rendering.
   */
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    if (!this._containsBoards()) {
      throw new Error(
        `<${this._errorComponentName}> must contain at least one <board>`
      );
    }
    super.doInitialPcbComponentRender();
  }
};

// lib/components/normal-components/Panel.ts
var Panel = class extends Subpanel {
  get config() {
    return {
      componentName: "Panel",
      zodProps: panelProps
    };
  }
  /**
   * Panel creates a pcb_panel record for the physical manufacturing panel.
   * This overrides the Subpanel behavior which uses pcb_group.
   */
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    if (!this._containsBoards()) {
      throw new Error(
        `<${this._errorComponentName}> must contain at least one <board>`
      );
    }
    const { db } = this.root;
    const props = this._parsedProps;
    const inserted = db.pcb_panel.insert({
      width: props.width !== void 0 ? distance16.parse(props.width) : 0,
      height: props.height !== void 0 ? distance16.parse(props.height) : 0,
      thickness: 1.6,
      center: this._getGlobalPcbPositionBeforeLayout(),
      covered_with_solder_mask: !(props.noSolderMask ?? false)
    });
    this.pcb_panel_id = inserted.pcb_panel_id;
  }
  /**
   * Panel updates pcb_panel dimensions instead of pcb_group
   */
  _updatePanelDimensions() {
    const { db } = this.root;
    const hasExplicitWidth = this._parsedProps.width !== void 0;
    const hasExplicitHeight = this._parsedProps.height !== void 0;
    const gridWidth = this._cachedGridWidth;
    const gridHeight = this._cachedGridHeight;
    if (!this.pcb_panel_id) return;
    if (hasExplicitWidth && hasExplicitHeight) {
      db.pcb_panel.update(this.pcb_panel_id, {
        width: distance16.parse(this._parsedProps.width),
        height: distance16.parse(this._parsedProps.height)
      });
    } else if (gridWidth > 0 || gridHeight > 0) {
      const {
        edgePadding: edgePaddingProp,
        edgePaddingLeft: edgePaddingLeftProp,
        edgePaddingRight: edgePaddingRightProp,
        edgePaddingTop: edgePaddingTopProp,
        edgePaddingBottom: edgePaddingBottomProp
      } = this._parsedProps;
      const edgePadding = distance16.parse(edgePaddingProp ?? 5);
      const edgePaddingLeft = distance16.parse(edgePaddingLeftProp ?? edgePadding);
      const edgePaddingRight = distance16.parse(
        edgePaddingRightProp ?? edgePadding
      );
      const edgePaddingTop = distance16.parse(edgePaddingTopProp ?? edgePadding);
      const edgePaddingBottom = distance16.parse(
        edgePaddingBottomProp ?? edgePadding
      );
      db.pcb_panel.update(this.pcb_panel_id, {
        width: hasExplicitWidth ? distance16.parse(this._parsedProps.width) : gridWidth + edgePaddingLeft + edgePaddingRight,
        height: hasExplicitHeight ? distance16.parse(this._parsedProps.height) : gridHeight + edgePaddingTop + edgePaddingBottom
      });
    }
  }
  updatePcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    if (!this.pcb_panel_id) return;
    const { db } = this.root;
    const props = this._parsedProps;
    const currentPanel = db.pcb_panel.get(this.pcb_panel_id);
    db.pcb_panel.update(this.pcb_panel_id, {
      width: props.width !== void 0 ? distance16.parse(props.width) : currentPanel?.width,
      height: props.height !== void 0 ? distance16.parse(props.height) : currentPanel?.height,
      center: this._getGlobalPcbPositionBeforeLayout(),
      covered_with_solder_mask: !(props.noSolderMask ?? false)
    });
  }
  removePcbComponentRender() {
    if (!this.pcb_panel_id) return;
    this.root?.db.pcb_panel.delete(this.pcb_panel_id);
    this.pcb_panel_id = null;
  }
};

// lib/components/normal-components/Pinout.ts
import { pinoutProps } from "@tscircuit/props";
var Pinout = class extends Chip {
  constructor(props) {
    super(props);
  }
  get config() {
    return {
      ...super.config,
      componentName: "Pinout",
      zodProps: pinoutProps
    };
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_pinout",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber,
      supplier_part_numbers: props.supplierPartNumbers,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/normal-components/Fuse.ts
import { fuseProps } from "@tscircuit/props";
import { formatSiUnit as formatSiUnit4, parseAndConvertSiUnit } from "format-si-unit";
var Fuse = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "fuse",
      schematicSymbolName: this.props.symbolName ?? "fuse",
      zodProps: fuseProps,
      sourceFtype: FTYPE.simple_fuse
    };
  }
  _getSchematicSymbolDisplayValue() {
    if (this._parsedProps.schShowRatings === false) {
      return "";
    }
    const rawCurrent = this._parsedProps.currentRating;
    const rawVoltage = this._parsedProps.voltageRating;
    const current = parseAndConvertSiUnit(rawCurrent, "A").value;
    const voltage = parseAndConvertSiUnit(rawVoltage, "V").value;
    return `${formatSiUnit4(current)}A / ${formatSiUnit4(voltage)}V`;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const currentRating = parseAndConvertSiUnit(props.currentRating, "A").value;
    const voltageRating = parseAndConvertSiUnit(props.voltageRating, "V").value;
    let display_current_rating;
    let display_voltage_rating;
    if (props.schShowRatings !== false) {
      display_current_rating = `${formatSiUnit4(currentRating)}A`;
      display_voltage_rating = `${formatSiUnit4(voltageRating)}V`;
    }
    const source_component = db.source_component.insert({
      name: this.name,
      ftype: FTYPE.simple_fuse,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      ...currentRating != null ? { current_rating_amps: currentRating } : {},
      ...voltageRating != null ? { voltage_rating_volts: voltageRating } : {},
      display_current_rating,
      display_voltage_rating,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/normal-components/Jumper.ts
import { jumperProps } from "@tscircuit/props";
import { normalizeDegrees as normalizeDegrees5 } from "@tscircuit/math-utils";
var Jumper = class extends NormalComponent3 {
  schematicDimensions = null;
  get config() {
    return {
      schematicSymbolName: void 0,
      componentName: "Jumper",
      zodProps: jumperProps,
      shouldRenderAsSchematicBox: true
    };
  }
  _getSchematicPortArrangement() {
    const arrangement = super._getSchematicPortArrangement();
    if (arrangement && Object.keys(arrangement).length > 0) return arrangement;
    const pinCount = this._parsedProps.pinCount ?? (Array.isArray(this._parsedProps.pinLabels) ? this._parsedProps.pinLabels.length : this._parsedProps.pinLabels ? Object.keys(this._parsedProps.pinLabels).length : this.getPortsFromFootprint().length);
    const direction = this._parsedProps.schDirection ?? "right";
    return {
      leftSize: direction === "left" ? pinCount : 0,
      rightSize: direction === "right" ? pinCount : 0
    };
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { pcbX, pcbY } = this.getResolvedPcbPositionProp();
    const source_component = db.source_component.insert({
      ftype: "simple_chip",
      // TODO unknown or jumper
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber,
      supplier_part_numbers: props.supplierPartNumbers,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { pcbX, pcbY } = this.getResolvedPcbPositionProp();
    const globalTransformRotation = this.getGlobalTransformRotation();
    const pcb_component = db.pcb_component.insert({
      center: { x: pcbX, y: pcbY },
      width: 2,
      // Default width, adjust as needed
      height: 3,
      // Default height, adjust as needed
      layer: props.layer ?? "top",
      rotation: normalizeDegrees5(globalTransformRotation),
      insertion_direction: this._getPcbComponentInsertionDirection(
        props.layer ?? "top",
        globalTransformRotation
      ),
      source_component_id: this.source_component_id,
      subcircuit_id: this.getSubcircuit().subcircuit_id ?? void 0,
      do_not_place: props.doNotPlace ?? false,
      obstructs_within_bounds: props.obstructsWithinBounds ?? true,
      is_allowed_to_be_off_board: props.allowOffBoard ?? false,
      metadata: props.kicadFootprintMetadata ? { kicad_footprint: props.kicadFootprintMetadata } : void 0
    });
    this.pcb_component_id = pcb_component.pcb_component_id;
  }
  doInitialPcbPortAttachment() {
    const { db } = this.root;
    const pcb_ports = db.pcb_port.list({
      pcb_component_id: this.pcb_component_id
    });
    const pinLabelToPortId = {};
    for (let i = 0; i < pcb_ports.length; i++) {
      const port = pcb_ports[i];
      const sourcePort = db.source_port.get(port.source_port_id);
      let pinLabel = "";
      if (typeof sourcePort?.pin_number === "number") {
        pinLabel = sourcePort.pin_number.toString();
      } else if (Array.isArray(sourcePort?.port_hints)) {
        let matchedHint = sourcePort.port_hints.find(
          (h) => /^(pin)?\d+$/.test(h)
        );
        if (matchedHint) {
          if (/^pin\d+$/.test(matchedHint)) {
            pinLabel = matchedHint.replace(/^pin/, "");
          } else {
            pinLabel = matchedHint;
          }
        }
      }
      pinLabelToPortId[pinLabel] = port.pcb_port_id;
    }
    const traces = db.pcb_trace.list({
      pcb_component_id: this.pcb_component_id
    });
    const updatePortId = (portId) => {
      if (portId && typeof portId === "string" && portId.startsWith("{PIN")) {
        const pin = portId.replace("{PIN", "").replace("}", "");
        return pinLabelToPortId[pin] || portId;
      }
      return portId;
    };
    for (const trace of traces) {
      if (!trace.route) continue;
      for (const segment2 of trace.route) {
        if (segment2.route_type !== "wire") continue;
        segment2.start_pcb_port_id = updatePortId(segment2.start_pcb_port_id);
        segment2.end_pcb_port_id = updatePortId(segment2.end_pcb_port_id);
      }
    }
  }
};

// lib/components/normal-components/Interconnect.ts
import { interconnectProps } from "@tscircuit/props";
var INTERCONNECT_STANDARD_FOOTPRINTS = {
  "0402": "0402",
  "0603": "0603",
  "0805": "0805",
  "1206": "1206"
};
var Interconnect = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "Interconnect",
      zodProps: interconnectProps,
      shouldRenderAsSchematicBox: true,
      sourceFtype: "interconnect"
    };
  }
  /**
   * For standard footprints (0402, 0603, 0805, 1206), the interconnect acts as
   * a 0-ohm jumper where both pins are internally connected.
   */
  get defaultInternallyConnectedPinNames() {
    const { standard } = this._parsedProps;
    if (standard && INTERCONNECT_STANDARD_FOOTPRINTS[standard]) {
      return [["pin1", "pin2"]];
    }
    return [];
  }
  _getImpliedFootprintString() {
    const { standard } = this._parsedProps;
    if (!standard) return null;
    return INTERCONNECT_STANDARD_FOOTPRINTS[standard] ?? null;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "interconnect",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  /**
   * After ports have their source_component_id assigned, create the
   * source_component_internal_connection to indicate which pins are
   * internally connected (for 0-ohm jumper behavior).
   */
  doInitialSourceParentAttachment() {
    const { db } = this.root;
    const internallyConnectedPorts = this._getInternallyConnectedPins();
    for (const ports of internallyConnectedPorts) {
      const sourcePortIds = ports.map((port) => port.source_port_id).filter((id) => id !== null);
      if (sourcePortIds.length >= 2) {
        db.source_component_internal_connection.insert({
          source_component_id: this.source_component_id,
          subcircuit_id: this.getSubcircuit()?.subcircuit_id,
          source_port_ids: sourcePortIds
        });
      }
    }
  }
};

// lib/components/normal-components/SolderJumper.ts
import { solderjumperProps } from "@tscircuit/props";
import { normalizeDegrees as normalizeDegrees6 } from "@tscircuit/math-utils";
var SolderJumper = class extends NormalComponent3 {
  schematicDimensions = null;
  _getPinCountFromFootprintString(footprint) {
    const match = footprint.match(/^solderjumper([23])(?:_|$)/);
    if (!match) return null;
    return Number(match[1]);
  }
  // Used during footprint resolution before ports can be selected.
  _getPinNumberFromBridgedPinsProp(pinName) {
    const numericPinMatch = pinName.match(/^(?:pin)?(\d+)$/i);
    if (numericPinMatch) return Number(numericPinMatch[1]);
    const pinLabels = this._parsedProps.pinLabels ?? this.props.pinLabels;
    if (Array.isArray(pinLabels)) {
      const labelIndex = pinLabels.findIndex((label) => label === pinName);
      return labelIndex >= 0 ? labelIndex + 1 : null;
    }
    if (pinLabels && typeof pinLabels === "object") {
      for (const [pinKey, labelOrLabels] of Object.entries(pinLabels)) {
        const labels = Array.isArray(labelOrLabels) ? labelOrLabels : [labelOrLabels];
        if (!labels.includes(pinName)) continue;
        const pinNumberMatch = pinKey.match(/^(?:pin)?(\d+)$/i);
        return pinNumberMatch ? Number(pinNumberMatch[1]) : null;
      }
    }
    return null;
  }
  _getBridgedPinNumbersForFootprint(pinCount) {
    const props = this._parsedProps ?? this.props;
    if (Array.isArray(props.bridgedPins) && props.bridgedPins.length > 0) {
      return Array.from(
        new Set(
          props.bridgedPins.flat().map(
            (pinName) => this._getPinNumberFromBridgedPinsProp(pinName)
          ).filter(
            (pinNumber) => pinNumber !== null && pinNumber >= 1 && pinNumber <= pinCount
          )
        )
      ).sort((a, b) => a - b);
    }
    if (!props.bridged) return [];
    return Array.from({ length: pinCount }, (_, index) => index + 1);
  }
  resolveFootprint() {
    const footprint = super.resolveFootprint();
    if (typeof footprint !== "string") return footprint;
    const pinCount = this._getPinCountFromFootprintString(footprint);
    if (!pinCount) return footprint;
    if (/_bridged\d+/.test(footprint)) return footprint;
    const bridgedPinNumbers = this._getBridgedPinNumbersForFootprint(pinCount);
    if (bridgedPinNumbers.length === 0) return footprint;
    return footprint.replace(
      /^solderjumper[23]/,
      `solderjumper${pinCount}_bridged${bridgedPinNumbers.join("")}`
    );
  }
  _getPinNumberFromBridgedPinName(pinName) {
    const port = this.selectOne(`port.${pinName}`, {
      type: "port"
    });
    return port?._parsedProps.pinNumber ?? null;
  }
  get defaultInternallyConnectedPinNames() {
    if (this._parsedProps.bridged) {
      const pins = this.children.filter((c) => c.componentName === "Port").map((p) => p.name);
      return pins.length > 0 ? [pins] : [];
    }
    return this._parsedProps.bridgedPins ?? [];
  }
  get config() {
    const props = this._parsedProps ?? this.props;
    let resolvedPinCount = props.pinCount;
    if (props.pinCount == null && !props.footprint) {
      resolvedPinCount = 2;
    }
    if (props.pinCount == null) {
      const nums = (props.bridgedPins ?? []).flat().map((p_str) => this._getPinNumberFromBridgedPinName(p_str)).filter((n) => n !== null);
      const maxPinFromBridged = nums.length > 0 ? Math.max(...nums) : 0;
      const pinCountFromLabels = props.pinLabels ? Object.keys(props.pinLabels).length : 0;
      const finalPinCount = Math.max(maxPinFromBridged, pinCountFromLabels);
      if (finalPinCount === 2 || finalPinCount === 3) {
        resolvedPinCount = finalPinCount;
      }
      if (resolvedPinCount == null && props.footprint && [2, 3].includes(this.getPortsFromFootprint().length)) {
        resolvedPinCount = this.getPortsFromFootprint().length;
      }
    }
    let symbolName = "";
    if (resolvedPinCount) {
      symbolName += `solderjumper${resolvedPinCount}`;
    } else {
      symbolName = "solderjumper";
    }
    let bridgedPinNumbers = [];
    if (Array.isArray(props.bridgedPins) && props.bridgedPins.length > 0) {
      bridgedPinNumbers = Array.from(
        new Set(
          props.bridgedPins.flat().map((pinName) => this._getPinNumberFromBridgedPinName(pinName)).filter((n) => n !== null)
        )
      ).sort((a, b) => a - b);
    } else if (props.bridged && resolvedPinCount) {
      bridgedPinNumbers = Array.from(
        { length: resolvedPinCount },
        (_, i) => i + 1
      );
    }
    if (bridgedPinNumbers.length > 0) {
      symbolName += `_bridged${bridgedPinNumbers.join("")}`;
    }
    return {
      schematicSymbolName: props.symbolName ?? symbolName,
      componentName: "SolderJumper",
      zodProps: solderjumperProps,
      shouldRenderAsSchematicBox: true
    };
  }
  _getSchematicPortArrangement() {
    const arrangement = super._getSchematicPortArrangement();
    if (arrangement && Object.keys(arrangement).length > 0) return arrangement;
    let pinCount = this._parsedProps.pinCount ?? (Array.isArray(this._parsedProps.pinLabels) ? this._parsedProps.pinLabels.length : this._parsedProps.pinLabels ? Object.keys(this._parsedProps.pinLabels).length : this.getPortsFromFootprint().length);
    if (pinCount == null && !this._parsedProps.footprint) {
      pinCount = 2;
    }
    const direction = this._parsedProps.schDirection ?? "right";
    return {
      leftSize: direction === "left" ? pinCount : 0,
      rightSize: direction === "right" ? pinCount : 0
    };
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { pcbX, pcbY } = this.getResolvedPcbPositionProp();
    const source_component = db.source_component.insert({
      ftype: "simple_chip",
      // TODO unknown or jumper
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber,
      supplier_part_numbers: props.supplierPartNumbers,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const { pcbX, pcbY } = this.getResolvedPcbPositionProp();
    const globalTransformRotation = this.getGlobalTransformRotation();
    const pcb_component = db.pcb_component.insert({
      center: { x: pcbX, y: pcbY },
      width: 2,
      // Default width, adjust as needed
      height: 3,
      // Default height, adjust as needed
      layer: props.layer ?? "top",
      rotation: normalizeDegrees6(globalTransformRotation),
      insertion_direction: this._getPcbComponentInsertionDirection(
        props.layer ?? "top",
        globalTransformRotation
      ),
      source_component_id: this.source_component_id,
      subcircuit_id: this.getSubcircuit().subcircuit_id ?? void 0,
      do_not_place: props.doNotPlace ?? false,
      obstructs_within_bounds: props.obstructsWithinBounds ?? true,
      is_allowed_to_be_off_board: props.allowOffBoard ?? false,
      metadata: props.kicadFootprintMetadata ? { kicad_footprint: props.kicadFootprintMetadata } : void 0
    });
    this.pcb_component_id = pcb_component.pcb_component_id;
  }
  doInitialPcbPortAttachment() {
    const { db } = this.root;
    const pcb_ports = db.pcb_port.list({
      pcb_component_id: this.pcb_component_id
    });
    const pinLabelToPortId = {};
    for (let i = 0; i < pcb_ports.length; i++) {
      const port = pcb_ports[i];
      const sourcePort = db.source_port.get(port.source_port_id);
      let pinLabel = "";
      if (typeof sourcePort?.pin_number === "number") {
        pinLabel = sourcePort.pin_number.toString();
      } else if (Array.isArray(sourcePort?.port_hints)) {
        let matchedHint = sourcePort.port_hints.find(
          (h) => /^(pin)?\d+$/.test(h)
        );
        if (matchedHint) {
          if (/^pin\d+$/.test(matchedHint)) {
            pinLabel = matchedHint.replace(/^pin/, "");
          } else {
            pinLabel = matchedHint;
          }
        }
      }
      pinLabelToPortId[pinLabel] = port.pcb_port_id;
    }
    const traces = db.pcb_trace.list({
      pcb_component_id: this.pcb_component_id
    });
    const updatePortId = (portId) => {
      if (portId && typeof portId === "string" && portId.startsWith("{PIN")) {
        const pin = portId.replace("{PIN", "").replace("}", "");
        return pinLabelToPortId[pin] || portId;
      }
      return portId;
    };
    for (const trace of traces) {
      if (!trace.route) continue;
      for (const segment2 of trace.route) {
        if (segment2.route_type !== "wire") continue;
        segment2.start_pcb_port_id = updatePortId(segment2.start_pcb_port_id);
        segment2.end_pcb_port_id = updatePortId(segment2.end_pcb_port_id);
      }
    }
  }
};

// lib/components/normal-components/PowerSource.ts
import { powerSourceProps } from "@tscircuit/props";
var PowerSource = class extends NormalComponent3 {
  // @ts-ignore
  get config() {
    return {
      // TBD in schematic_symbol and replace then
      schematicSymbolName: this.props.symbolName ?? "power_factor_meter_horz",
      componentName: "PowerSource",
      zodProps: powerSourceProps,
      sourceFtype: "simple_power_source"
    };
  }
  initPorts() {
    this.add(
      new Port({ name: "pin1", pinNumber: 1, aliases: ["positive", "pos"] })
    );
    this.add(
      new Port({ name: "pin2", pinNumber: 2, aliases: ["negative", "neg"] })
    );
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_power_source",
      name: this.name,
      voltage: props.voltage,
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      are_pins_interchangeable: false,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  pos = this.portMap.pin1;
  positive = this.portMap.pin1;
  neg = this.portMap.pin2;
  negative = this.portMap.pin2;
};

// lib/components/normal-components/VoltageSource.ts
import { voltageSourceProps } from "@tscircuit/props";
import { formatSiUnit as formatSiUnit5 } from "format-si-unit";
var VoltageSource = class extends NormalComponent3 {
  simulation_voltage_source_id = null;
  get config() {
    const isSquare = this.props.waveShape === "square";
    return {
      componentName: "VoltageSource",
      schematicSymbolName: isSquare ? "square_wave" : "ac_voltmeter",
      zodProps: voltageSourceProps,
      sourceFtype: "simple_voltage_source"
    };
  }
  runRenderPhaseForChildren(phase) {
    if (phase.startsWith("Pcb")) {
      return;
    }
    for (const child of this.children) {
      child.runRenderPhaseForChildren(phase);
      child.runRenderPhase(phase);
    }
  }
  doInitialPcbComponentRender() {
    const hasExplicitPcbPosition = this._hasUserDefinedPcbPosition();
    if (!this._parsedProps.footprint) {
      if (!hasExplicitPcbPosition) return;
      throw new Error(
        "VoltageSource requires a footprint when pcbX/pcbY or pcb edge position props are used"
      );
    }
    super.doInitialPcbComponentRender();
  }
  initPorts() {
    super.initPorts({
      additionalAliases: {
        pin1: ["terminal1"],
        pin2: ["terminal2"]
      }
    });
  }
  _getSchematicSymbolDisplayValue() {
    const { voltage, frequency: frequency2 } = this._parsedProps;
    const parts = [];
    if (voltage !== void 0) {
      parts.push(`${formatSiUnit5(voltage)}V`);
    }
    if (frequency2 !== void 0) {
      parts.push(`${formatSiUnit5(frequency2)}Hz`);
    }
    return parts.length > 0 ? parts.join(" ") : void 0;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_voltage_source",
      name: this.name,
      voltage: props.voltage,
      frequency: props.frequency,
      peak_to_peak_voltage: props.peakToPeakVoltage,
      wave_shape: props.waveShape,
      phase: props.phase,
      duty_cycle: props.dutyCycle,
      pulse_delay: props.pulseDelay,
      rise_time: props.riseTime,
      fall_time: props.fallTime,
      pulse_width: props.pulseWidth,
      period: props.period,
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialSimulationRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const terminal1Port = this.portMap.terminal1;
    const terminal2Port = this.portMap.terminal2;
    const simulationVoltageSource = db.simulation_voltage_source.insert({
      is_dc_source: false,
      terminal1_source_port_id: terminal1Port.source_port_id ?? void 0,
      terminal2_source_port_id: terminal2Port.source_port_id ?? void 0,
      voltage: props.voltage,
      frequency: props.frequency,
      peak_to_peak_voltage: props.peakToPeakVoltage,
      wave_shape: props.waveShape,
      phase: props.phase,
      duty_cycle: props.dutyCycle,
      pulse_delay: props.pulseDelay,
      rise_time: props.riseTime,
      fall_time: props.fallTime,
      pulse_width: props.pulseWidth,
      period: props.period,
      ac_magnitude: props.acMagnitude,
      ac_phase: props.acPhase
    });
    this.simulation_voltage_source_id = simulationVoltageSource.simulation_voltage_source_id;
  }
  terminal1 = this.portMap.terminal1;
  terminal2 = this.portMap.terminal2;
};

// lib/components/normal-components/CurrentSource.ts
import { currentSourceProps } from "@tscircuit/props";
import { formatSiUnit as formatSiUnit6 } from "format-si-unit";
var CurrentSource = class extends NormalComponent3 {
  simulation_current_source_id = null;
  get config() {
    const symbolName = "current_source";
    return {
      componentName: "CurrentSource",
      schematicSymbolName: symbolName,
      zodProps: currentSourceProps,
      sourceFtype: "simple_current_source"
    };
  }
  runRenderPhaseForChildren(phase) {
    if (phase.startsWith("Pcb")) {
      return;
    }
    for (const child of this.children) {
      child.runRenderPhaseForChildren(phase);
      child.runRenderPhase(phase);
    }
  }
  doInitialPcbComponentRender() {
    const hasExplicitPcbPosition = this._hasUserDefinedPcbPosition();
    if (!this._parsedProps.footprint) {
      if (!hasExplicitPcbPosition) return;
      throw new Error(
        "CurrentSource requires a footprint when pcbX/pcbY or pcb edge position props are used"
      );
    }
    super.doInitialPcbComponentRender();
  }
  initPorts() {
    super.initPorts({
      additionalAliases: {
        pin1: ["pos"],
        pin2: ["neg"]
      }
    });
  }
  _getSchematicSymbolDisplayValue() {
    const { current, frequency: frequency2, peakToPeakCurrent } = this._parsedProps;
    const parts = [];
    if (current !== void 0) {
      parts.push(`${formatSiUnit6(current)}A`);
    }
    if (peakToPeakCurrent !== void 0) {
      parts.push(`${formatSiUnit6(peakToPeakCurrent)}A p-p`);
    }
    if (frequency2 !== void 0) {
      parts.push(`${formatSiUnit6(frequency2)}Hz`);
    }
    return parts.length > 0 ? parts.join(" ") : void 0;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_current_source",
      name: this.name,
      current: props.current,
      frequency: props.frequency,
      peak_to_peak_current: props.peakToPeakCurrent,
      wave_shape: props.waveShape,
      phase: props.phase,
      duty_cycle: props.dutyCycle,
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialSimulationRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const isAc = props.frequency !== void 0 || props.peakToPeakCurrent !== void 0 || props.waveShape !== void 0 || props.acMagnitude !== void 0 || props.acPhase !== void 0;
    const posPort = this.portMap.pos;
    const negPort = this.portMap.neg;
    if (isAc) {
      const simulationCurrentSource = db.simulation_current_source.insert({
        is_dc_source: false,
        terminal1_source_port_id: posPort.source_port_id ?? void 0,
        terminal2_source_port_id: negPort.source_port_id ?? void 0,
        current: props.current,
        // DC offset
        frequency: props.frequency,
        peak_to_peak_current: props.peakToPeakCurrent,
        wave_shape: props.waveShape,
        phase: props.phase,
        duty_cycle: props.dutyCycle,
        ac_magnitude: props.acMagnitude,
        ac_phase: props.acPhase
      });
      this.simulation_current_source_id = simulationCurrentSource.simulation_current_source_id;
    } else {
      if (props.current === void 0) return;
      const simulationCurrentSource = db.simulation_current_source.insert({
        is_dc_source: true,
        positive_source_port_id: posPort.source_port_id ?? void 0,
        negative_source_port_id: negPort.source_port_id ?? void 0,
        current: props.current,
        ac_magnitude: props.acMagnitude,
        ac_phase: props.acPhase
      });
      this.simulation_current_source_id = simulationCurrentSource.simulation_current_source_id;
    }
  }
  pos = this.portMap.pos;
  neg = this.portMap.neg;
};

// lib/components/normal-components/Ammeter.ts
import { ammeterProps } from "@tscircuit/props";
import { symbols as symbols6 } from "schematic-symbols";

// lib/components/normal-components/Ammeter_doInitialSchematicLayout.ts
var PLACEMENT_MARGIN = 0.25;
var rectForComponent = (component, margin) => ({
  left: component.center.x - component.size.width / 2 - margin,
  right: component.center.x + component.size.width / 2 + margin,
  bottom: component.center.y - component.size.height / 2 - margin,
  top: component.center.y + component.size.height / 2 + margin
});
var rectsOverlap = (a, b) => a.left < b.right && a.right > b.left && a.bottom < b.top && a.top > b.bottom;
var getCandidateCenters = ({
  baseCenter,
  size
}) => {
  const xStep = size.width + PLACEMENT_MARGIN * 2;
  const yStep = size.height + PLACEMENT_MARGIN * 2;
  return [
    baseCenter,
    { x: baseCenter.x, y: baseCenter.y + yStep },
    { x: baseCenter.x, y: baseCenter.y - yStep },
    { x: baseCenter.x + xStep, y: baseCenter.y },
    { x: baseCenter.x - xStep, y: baseCenter.y },
    { x: baseCenter.x + xStep, y: baseCenter.y + yStep },
    { x: baseCenter.x - xStep, y: baseCenter.y + yStep },
    { x: baseCenter.x + xStep, y: baseCenter.y - yStep },
    { x: baseCenter.x - xStep, y: baseCenter.y - yStep },
    { x: baseCenter.x, y: baseCenter.y + yStep * 2 },
    { x: baseCenter.x, y: baseCenter.y - yStep * 2 }
  ];
};
var getConnectedSchematicPort = ({
  ammeterSourcePortId,
  schematicPorts,
  sourceTraces
}) => {
  for (const sourceTrace of sourceTraces) {
    if (!sourceTrace.connected_source_port_ids.includes(ammeterSourcePortId)) {
      continue;
    }
    const connectedSourcePortId = sourceTrace.connected_source_port_ids.find(
      (sourcePortId) => sourcePortId !== ammeterSourcePortId
    );
    if (connectedSourcePortId === void 0) continue;
    const schematicPort = schematicPorts.find(
      (port) => port.source_port_id === connectedSourcePortId
    );
    if (schematicPort) return schematicPort;
  }
  return null;
};
var midpoint = (ports) => ({
  x: ports.reduce((sum, port) => sum + port.center.x, 0) / ports.length,
  y: ports.reduce((sum, port) => sum + port.center.y, 0) / ports.length
});
var shiftAmmeterSchematicElements = ({
  ammeter,
  db,
  oldCenter,
  newCenter
}) => {
  const schematicComponentId = ammeter.schematic_component_id;
  if (!schematicComponentId) return;
  const deltaX = newCenter.x - oldCenter.x;
  const deltaY = newCenter.y - oldCenter.y;
  if (deltaX === 0 && deltaY === 0) return;
  db.schematic_component.update(schematicComponentId, {
    center: newCenter
  });
  for (const port of db.schematic_port.list({
    schematic_component_id: schematicComponentId
  })) {
    db.schematic_port.update(port.schematic_port_id, {
      center: {
        x: port.center.x + deltaX,
        y: port.center.y + deltaY
      }
    });
  }
  for (const text of db.schematic_text.list({
    schematic_component_id: schematicComponentId
  })) {
    db.schematic_text.update(text.schematic_text_id, {
      position: {
        x: text.position.x + deltaX,
        y: text.position.y + deltaY
      }
    });
  }
  updateSchematicPrimitivesForLayoutShift({
    db,
    schematicComponentId,
    deltaX,
    deltaY
  });
};
var Ammeter_doInitialSchematicLayout = (ammeter) => {
  const { root } = ammeter;
  if (!root || root.schematicDisabled) return;
  if (!ammeter.schematic_component_id) return;
  if (ammeter._parsedProps.schX !== void 0 || ammeter._parsedProps.schY !== void 0) {
    return;
  }
  if (ammeter.getGroup()?._getSchematicLayoutMode() !== "relative") return;
  const { db } = root;
  const schematicComponent = db.schematic_component.get(
    ammeter.schematic_component_id
  );
  if (!schematicComponent) return;
  const ammeterSourcePortIds = [
    ammeter.portMap.pos.source_port_id,
    ammeter.portMap.neg.source_port_id
  ].filter(
    (sourcePortId) => typeof sourcePortId === "string"
  );
  if (ammeterSourcePortIds.length === 0) return;
  const schematicPorts = db.schematic_port.list();
  const sourceTraces = db.source_trace.list();
  const connectedSchematicPorts = ammeterSourcePortIds.map(
    (ammeterSourcePortId) => getConnectedSchematicPort({
      ammeterSourcePortId,
      schematicPorts,
      sourceTraces
    })
  ).filter((port) => port !== null);
  if (connectedSchematicPorts.length === 0) return;
  const baseCenter = midpoint(connectedSchematicPorts);
  const obstacles = db.schematic_component.list().filter(
    (component) => component.schematic_component_id !== schematicComponent.schematic_component_id && !component.is_schematic_group && component.schematic_sheet_id === schematicComponent.schematic_sheet_id
  ).map((component) => rectForComponent(component, PLACEMENT_MARGIN));
  const bestCenter = getCandidateCenters({
    baseCenter,
    size: schematicComponent.size
  }).find((candidateCenter) => {
    const candidateRect = rectForComponent(
      { ...schematicComponent, center: candidateCenter },
      PLACEMENT_MARGIN
    );
    return !obstacles.some(
      (obstacle) => rectsOverlap(candidateRect, obstacle)
    );
  }) ?? baseCenter;
  shiftAmmeterSchematicElements({
    ammeter,
    db,
    oldCenter: schematicComponent.center,
    newCenter: bestCenter
  });
};

// lib/components/normal-components/Ammeter.ts
var Ammeter = class extends NormalComponent3 {
  simulation_current_probe_id = null;
  get config() {
    return {
      componentName: "Ammeter",
      schematicSymbolName: "dc_ammeter",
      zodProps: ammeterProps,
      sourceFtype: "simple_ammeter"
    };
  }
  runRenderPhaseForChildren(phase) {
    if (phase.startsWith("Pcb")) {
      return;
    }
    for (const child of this.children) {
      child.runRenderPhaseForChildren(phase);
      child.runRenderPhase(phase);
    }
  }
  doInitialPcbComponentRender() {
    const hasExplicitPcbPosition = this._hasUserDefinedPcbPosition();
    if (!this._parsedProps.footprint) {
      if (!hasExplicitPcbPosition) return;
      throw new Error(
        "Ammeter requires a footprint when pcbX/pcbY or pcb edge position props are used"
      );
    }
    super.doInitialPcbComponentRender();
  }
  initPorts() {
    super.initPorts({
      ignoreSymbolPorts: true,
      pinCount: 2,
      additionalAliases: {
        pin1: ["pos"],
        pin2: ["neg"]
      }
    });
    const symbol = symbols6[this._getSchematicSymbolNameOrThrow()];
    this.portMap.pos.schematicSymbolPortDef = symbol.ports.find((port) => port.labels.includes("1")) ?? null;
    this.portMap.neg.schematicSymbolPortDef = symbol.ports.find((port) => port.labels.includes("2")) ?? null;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { supplierPartNumbers, displayName } = this._parsedProps;
    const source_component = db.source_component.insert({
      ftype: "simple_ammeter",
      name: this.name,
      supplier_part_numbers: supplierPartNumbers,
      manufacturer_part_number: this._parsedProps.manufacturerPartNumber ?? this._parsedProps.mfn,
      display_name: displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialSimulationRender() {
    const { db } = this.root;
    const {
      color,
      graphDisplayName,
      graphCenter,
      graphVerticalOffset,
      graphCurrentPerDiv
    } = this._parsedProps;
    const posPort = this.portMap.pos;
    const negPort = this.portMap.neg;
    const { simulation_current_probe_id } = db.simulation_current_probe.insert({
      type: "simulation_current_probe",
      name: this.name,
      source_component_id: this.source_component_id,
      positive_source_port_id: posPort.source_port_id,
      negative_source_port_id: negPort.source_port_id,
      subcircuit_id: this.getSubcircuit()?.subcircuit_id ?? void 0,
      color
    });
    this.simulation_current_probe_id = simulation_current_probe_id;
    const hasGraphDisplayProps = graphDisplayName !== void 0 || graphCenter !== void 0 || graphVerticalOffset !== void 0 || graphCurrentPerDiv !== void 0;
    if (hasGraphDisplayProps) {
      const graphCurrentPerDivValue = parseSimulationGraphValue(graphCurrentPerDiv);
      const graphVerticalOffsetValue = parseSimulationGraphValue(graphVerticalOffset);
      db.simulation_oscilloscope_trace.insert({
        simulation_current_probe_id,
        display_name: graphDisplayName,
        color,
        display_center_value: graphCenter,
        amps_per_div: graphCurrentPerDivValue,
        display_center_offset_divs: graphVerticalOffsetValue !== void 0 && graphCurrentPerDivValue !== void 0 && graphCurrentPerDivValue !== 0 ? graphVerticalOffsetValue / graphCurrentPerDivValue : void 0
      });
    }
  }
  doInitialSchematicLayout() {
    Ammeter_doInitialSchematicLayout(this);
  }
  pos = this.portMap.pos;
  neg = this.portMap.neg;
};

// lib/components/primitive-components/Constraint.ts
import { constraintProps } from "@tscircuit/props";
import "zod";
var edgeSpecifiers = [
  "leftedge",
  "rightedge",
  "topedge",
  "bottomedge",
  "center"
];
var Constraint3 = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "Constraint",
      zodProps: constraintProps
    };
  }
  constructor(props) {
    super(props);
    if ("xdist" in props || "ydist" in props) {
      if (!("edgeToEdge" in props) && !("centerToCenter" in props)) {
        throw new Error(
          `edgeToEdge, centerToCenter must be set for xDist or yDist for ${this}`
        );
      }
    }
    if ("for" in props && props.for.length < 2) {
      throw new Error(`"for" must have at least two selectors for ${this}`);
    }
  }
  _getAllReferencedComponents() {
    const componentsWithSelectors = [];
    const container = this.getPrimitiveContainer();
    function addComponentFromSelector(selector) {
      const maybeEdge = selector.split(" ").pop();
      const edge = edgeSpecifiers.includes(maybeEdge) ? maybeEdge : void 0;
      const componentSelector = edge ? selector.replace(` ${edge}`, "") : selector;
      const component = container.selectOne(componentSelector, {
        pcbPrimitive: true
      });
      if (component) {
        componentsWithSelectors.push({
          selector,
          component,
          componentSelector,
          edge
        });
      }
    }
    for (const key of ["left", "right", "top", "bottom"]) {
      if (key in this._parsedProps) {
        addComponentFromSelector(this._parsedProps[key]);
      }
    }
    if ("for" in this._parsedProps) {
      for (const selector of this._parsedProps.for) {
        addComponentFromSelector(selector);
      }
    }
    return { componentsWithSelectors };
  }
};

// lib/components/primitive-components/AutoroutingPhase.ts
import { autoroutingPhaseProps } from "@tscircuit/props";
var AutoroutingPhase = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "AutoroutingPhase",
      zodProps: autoroutingPhaseProps
    };
  }
  add(_component) {
    throw new Error("<autoroutingphase> cannot contain children");
  }
};

// lib/components/primitive-components/FabricationNoteDimension.ts
import { fabricationNoteDimensionProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint26 } from "transformation-matrix";
var FabricationNoteDimension = class extends PrimitiveComponent2 {
  fabrication_note_dimension_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "FabricationNoteDimension",
      zodProps: fabricationNoteDimensionProps
    };
  }
  _resolvePoint(input, transform) {
    if (typeof input === "string") {
      const target = this.getSubcircuit().selectOne(
        input
      );
      if (!target) {
        this.renderError(
          `FabricationNoteDimension could not find selector "${input}"`
        );
        return applyToPoint26(transform, { x: 0, y: 0 });
      }
      return target._getGlobalPcbPositionBeforeLayout();
    }
    const numericX = typeof input.x === "string" ? parseFloat(input.x) : input.x;
    const numericY = typeof input.y === "string" ? parseFloat(input.y) : input.y;
    return applyToPoint26(transform, { x: numericX, y: numericY });
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const from = this._resolvePoint(props.from, transform);
    const to = this._resolvePoint(props.to, transform);
    const subcircuit = this.getSubcircuit();
    const group = this.getGroup();
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const layer = maybeFlipLayer(props.layer ?? "top");
    if (layer !== "top" && layer !== "bottom") {
      throw new Error(
        `Invalid layer "${layer}" for FabricationNoteDimension. Must be "top" or "bottom".`
      );
    }
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const text = props.text ?? this._formatDistanceText({ from, to, units: props.units ?? "mm" });
    const fabrication_note_dimension = db.pcb_fabrication_note_dimension.insert(
      {
        pcb_component_id,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        pcb_group_id: group?.pcb_group_id ?? void 0,
        layer,
        from,
        to,
        text,
        offset: props.offset,
        font: props.font ?? "tscircuit2024",
        font_size: props.fontSize ?? 1,
        color: props.color,
        arrow_size: props.arrowSize ?? 1
      }
    );
    this.fabrication_note_dimension_id = fabrication_note_dimension.pcb_fabrication_note_dimension_id;
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.fabrication_note_dimension_id) return;
    const dimension = db.pcb_fabrication_note_dimension.get(
      this.fabrication_note_dimension_id
    );
    if (dimension) {
      db.pcb_fabrication_note_dimension.update(
        this.fabrication_note_dimension_id,
        {
          from: {
            x: dimension.from.x + deltaX,
            y: dimension.from.y + deltaY
          },
          to: {
            x: dimension.to.x + deltaX,
            y: dimension.to.y + deltaY
          }
        }
      );
    }
  }
  getPcbSize() {
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const from = this._resolvePoint(this._parsedProps.from, transform);
    const to = this._resolvePoint(this._parsedProps.to, transform);
    return {
      width: Math.abs(to.x - from.x),
      height: Math.abs(to.y - from.y)
    };
  }
  _formatDistanceText({
    from,
    to,
    units
  }) {
    const dx = to.x - from.x;
    const dy = to.y - from.y;
    const distanceInMillimeters = Math.sqrt(dx * dx + dy * dy);
    const distanceInUnits = units === "in" ? distanceInMillimeters / 25.4 : distanceInMillimeters;
    const roundedDistance = Math.round(distanceInUnits);
    const isWholeNumber = Math.abs(distanceInUnits - roundedDistance) < 1e-9;
    if (isWholeNumber) {
      return `${roundedDistance}${units}`;
    }
    const decimalPlaces = units === "in" ? 3 : 2;
    const valueText = units === "in" ? Number(distanceInUnits.toFixed(decimalPlaces)).toString() : distanceInUnits.toFixed(decimalPlaces);
    return `${valueText}${units}`;
  }
};

// lib/components/primitive-components/PcbNoteDimension.ts
import { pcbNoteDimensionProps } from "@tscircuit/props";
import { applyToPoint as applyToPoint27 } from "transformation-matrix";
var PcbNoteDimension = class extends PrimitiveComponent2 {
  pcb_note_dimension_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "PcbNoteDimension",
      zodProps: pcbNoteDimensionProps
    };
  }
  _resolvePoint(input, transform) {
    if (typeof input === "string") {
      const target = this.getSubcircuit().selectOne(
        `.${input}`
      );
      if (!target) {
        this.renderError(`PcbNoteDimension could not find selector "${input}"`);
        return applyToPoint27(transform, { x: 0, y: 0 });
      }
      const targetPcbComponentId = target.pcb_component_id;
      const root = this.root;
      if (targetPcbComponentId && root) {
        const pcbComponent = root.db.pcb_component.get(targetPcbComponentId);
        if (pcbComponent?.center) {
          return {
            x: pcbComponent.center.x,
            y: pcbComponent.center.y
          };
        }
      }
      return target._getGlobalPcbPositionBeforeLayout();
    }
    const numericX = typeof input.x === "string" ? parseFloat(input.x) : input.x;
    const numericY = typeof input.y === "string" ? parseFloat(input.y) : input.y;
    return applyToPoint27(transform, { x: numericX, y: numericY });
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const from = this._resolvePoint(props.from, transform);
    const to = this._resolvePoint(props.to, transform);
    const subcircuit = this.getSubcircuit();
    const group = this.getGroup();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id ?? void 0;
    const text = props.text ?? this._formatDistanceText({ from, to, units: props.units ?? "mm" });
    const pcb_note_dimension = db.pcb_note_dimension.insert({
      pcb_component_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      pcb_group_id: group?.pcb_group_id ?? void 0,
      from,
      to,
      text,
      font: props.font ?? "tscircuit2024",
      font_size: props.fontSize ?? 1,
      layer: props.layer === "bottom" ? "bottom" : "top",
      color: props.color,
      arrow_size: props.arrowSize ?? 1
    });
    this.pcb_note_dimension_id = pcb_note_dimension.pcb_note_dimension_id;
  }
  doInitialPcbLayout() {
    const root = this.root;
    if (!root || root.pcbDisabled) return;
    if (!this.pcb_note_dimension_id) return;
    const { db } = root;
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const from = this._resolvePoint(this._parsedProps.from, transform);
    const to = this._resolvePoint(this._parsedProps.to, transform);
    db.pcb_note_dimension.update(this.pcb_note_dimension_id, {
      from,
      to
    });
  }
  getPcbSize() {
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const from = this._resolvePoint(this._parsedProps.from, transform);
    const to = this._resolvePoint(this._parsedProps.to, transform);
    return {
      width: Math.abs(to.x - from.x),
      height: Math.abs(to.y - from.y)
    };
  }
  _formatDistanceText({
    from,
    to,
    units
  }) {
    const dx = to.x - from.x;
    const dy = to.y - from.y;
    const distanceInMillimeters = Math.sqrt(dx * dx + dy * dy);
    const distanceInUnits = units === "in" ? distanceInMillimeters / 25.4 : distanceInMillimeters;
    const roundedDistance = Math.round(distanceInUnits);
    const isWholeNumber = Math.abs(distanceInUnits - roundedDistance) < 1e-9;
    if (isWholeNumber) {
      return `${roundedDistance}${units}`;
    }
    const decimalPlaces = units === "in" ? 3 : 2;
    const valueText = units === "in" ? Number(distanceInUnits.toFixed(decimalPlaces)).toString() : distanceInUnits.toFixed(decimalPlaces);
    return `${valueText}${units}`;
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_note_dimension_id) return;
    const dimension = db.pcb_note_dimension.get(this.pcb_note_dimension_id);
    if (dimension) {
      db.pcb_note_dimension.update(this.pcb_note_dimension_id, {
        from: {
          x: dimension.from.x + deltaX,
          y: dimension.from.y + deltaY
        },
        to: {
          x: dimension.to.x + deltaX,
          y: dimension.to.y + deltaY
        }
      });
    }
  }
};

// lib/components/primitive-components/Breakout/Breakout.ts
import { breakoutProps } from "@tscircuit/props";

// node_modules/@tscircuit/breakout-point-solver/lib/BreakoutPointSolver.ts
import { BaseSolver as BaseSolver2 } from "@tscircuit/solver-utils";

// node_modules/@tscircuit/breakout-point-solver/lib/boundary/get-breakout-boundary-intersection.ts
import {
  distance as distance17,
  getSegmentIntersection
} from "@tscircuit/math-utils";
var getBreakoutBoundaryIntersection = ({
  from,
  to,
  bounds
}) => {
  if (from.x === to.x && from.y === to.y) return null;
  const boundarySegments = [
    [
      { x: bounds.minX, y: bounds.minY },
      { x: bounds.maxX, y: bounds.minY }
    ],
    [
      { x: bounds.maxX, y: bounds.minY },
      { x: bounds.maxX, y: bounds.maxY }
    ],
    [
      { x: bounds.maxX, y: bounds.maxY },
      { x: bounds.minX, y: bounds.maxY }
    ],
    [
      { x: bounds.minX, y: bounds.maxY },
      { x: bounds.minX, y: bounds.minY }
    ]
  ];
  const candidates = boundarySegments.map(([start, end]) => getSegmentIntersection(from, to, start, end)).filter((point6) => point6 !== null);
  candidates.sort((a, b) => distance17(from, a) - distance17(from, b));
  return candidates[0] ?? null;
};

// node_modules/@tscircuit/breakout-point-solver/lib/boundary/get-available-breakout-boundary-point.ts
import { distance as distance18 } from "@tscircuit/math-utils";

// node_modules/@tscircuit/breakout-point-solver/lib/pad/breakout-pad-collisions.ts
import {
  doesSegmentIntersectRect,
  getBoundFromCenteredRect as getBoundFromCenteredRect5
} from "@tscircuit/math-utils";
var degreesToRadians = (degrees) => degrees * Math.PI / 180;
var rotatePoint = (point6, radians) => {
  const cos = Math.cos(radians);
  const sin = Math.sin(radians);
  return {
    x: point6.x * cos - point6.y * sin,
    y: point6.x * sin + point6.y * cos
  };
};
var getLocalPadPoint = (point6, pad) => {
  const translatedPoint = {
    x: point6.x - pad.center.x,
    y: point6.y - pad.center.y
  };
  return rotatePoint(
    translatedPoint,
    -degreesToRadians(pad.ccwRotationDegrees ?? 0)
  );
};
var getInflatedPadRect = (pad) => {
  const clearance = pad.clearance ?? 0;
  return getBoundFromCenteredRect5({
    center: { x: 0, y: 0 },
    width: pad.width + clearance * 2,
    height: pad.height + clearance * 2
  });
};
var isBreakoutPadIgnoredForSourcePort = ({
  pad,
  sourcePortId
}) => {
  if (!pad.sourcePortIds) return false;
  return pad.sourcePortIds.includes(sourcePortId);
};
var isBreakoutPadIgnoredForLayer = ({
  pad,
  layer
}) => {
  if (!pad.layer || !layer) return false;
  return pad.layer !== layer;
};
var doesBreakoutSegmentIntersectPad = ({
  from,
  to,
  pad
}) => doesSegmentIntersectRect(
  getLocalPadPoint(from, pad),
  getLocalPadPoint(to, pad),
  getInflatedPadRect(pad)
);
var doesBreakoutSegmentIntersectNonIgnoredPads = ({
  from,
  to,
  pads,
  ignoredSourcePortIds,
  layer
}) => {
  for (const pad of pads) {
    if (ignoredSourcePortIds.some(
      (sourcePortId) => isBreakoutPadIgnoredForSourcePort({
        pad,
        sourcePortId
      })
    ) || isBreakoutPadIgnoredForLayer({
      pad,
      layer
    })) {
      continue;
    }
    if (doesBreakoutSegmentIntersectPad({ from, to, pad })) {
      return true;
    }
  }
  return false;
};
var doesBreakoutSegmentIntersectPads = ({
  from,
  to,
  pads,
  sourcePortId,
  layer
}) => doesBreakoutSegmentIntersectNonIgnoredPads({
  from,
  to,
  pads,
  ignoredSourcePortIds: [sourcePortId],
  layer
});

// node_modules/@tscircuit/breakout-point-solver/lib/boundary/get-available-breakout-boundary-point.ts
var BOUNDARY_POINT_DISTANCE_TOLERANCE = 1e-6;
var isInsideRequiredSpacing = ({
  candidate,
  usedPoint,
  boundaryPointSpacing
}) => distance18(usedPoint, candidate) < boundaryPointSpacing - BOUNDARY_POINT_DISTANCE_TOLERANCE;
var getBoundsEdge = (point6, bounds) => {
  if (Math.abs(point6.x - bounds.minX) < BOUNDARY_POINT_DISTANCE_TOLERANCE)
    return "left";
  if (Math.abs(point6.x - bounds.maxX) < BOUNDARY_POINT_DISTANCE_TOLERANCE)
    return "right";
  if (Math.abs(point6.y - bounds.minY) < BOUNDARY_POINT_DISTANCE_TOLERANCE)
    return "bottom";
  if (Math.abs(point6.y - bounds.maxY) < BOUNDARY_POINT_DISTANCE_TOLERANCE)
    return "top";
  return null;
};
var getBoundsEdgeCandidates = ({
  edge,
  bounds,
  step
}) => {
  const candidates = [];
  if (edge === "left" || edge === "right") {
    const x = edge === "left" ? bounds.minX : bounds.maxX;
    for (let y = bounds.minY; y <= bounds.maxY + step / 2; y += step) {
      candidates.push({ x, y: Math.min(y, bounds.maxY) });
    }
  } else {
    const y = edge === "bottom" ? bounds.minY : bounds.maxY;
    for (let x = bounds.minX; x <= bounds.maxX + step / 2; x += step) {
      candidates.push({ x: Math.min(x, bounds.maxX), y });
    }
  }
  return candidates;
};
var getAllBoundsCandidates = ({
  bounds,
  step
}) => {
  const candidatesByCoordinate = /* @__PURE__ */ new Map();
  for (const edge of ["left", "right", "bottom", "top"]) {
    for (const candidate of getBoundsEdgeCandidates({ edge, bounds, step })) {
      candidatesByCoordinate.set(`${candidate.x}:${candidate.y}`, candidate);
    }
  }
  return [...candidatesByCoordinate.values()];
};
var getBoundaryCandidateSearchStep = ({
  bounds,
  boundaryPointSpacing
}) => {
  if (boundaryPointSpacing > 0) return boundaryPointSpacing;
  return Math.min(bounds.maxX - bounds.minX, bounds.maxY - bounds.minY) / 40;
};
var hasBoundarySpacingConflict = ({
  candidate,
  usedBoundaryPoints,
  boundaryPointSpacing
}) => {
  for (const usedPoint of usedBoundaryPoints) {
    if (isInsideRequiredSpacing({
      candidate,
      usedPoint,
      boundaryPointSpacing
    })) {
      return true;
    }
  }
  return false;
};
var isBoundaryCandidateBlocked = ({
  candidate,
  routeFrom,
  pads,
  sourcePortId,
  layer
}) => {
  if (!routeFrom || !pads || !sourcePortId) return false;
  return doesBreakoutSegmentIntersectPads({
    from: routeFrom,
    to: candidate,
    pads,
    sourcePortId,
    layer
  });
};
var isCandidateAvailable = ({
  candidate,
  usedBoundaryPoints,
  boundaryPointSpacing,
  routeFrom,
  pads,
  sourcePortId,
  layer
}) => {
  if (hasBoundarySpacingConflict({
    candidate,
    usedBoundaryPoints,
    boundaryPointSpacing
  })) {
    return false;
  }
  return !isBoundaryCandidateBlocked({
    candidate,
    routeFrom,
    pads,
    sourcePortId,
    layer
  });
};
var hasOutsideAccessConflict = ({
  candidate,
  outsidePorts,
  pads,
  sourcePortId,
  layer
}) => {
  if (!outsidePorts || !pads || !sourcePortId) return false;
  for (const outsidePort of outsidePorts) {
    if (doesBreakoutSegmentIntersectNonIgnoredPads({
      from: candidate,
      to: outsidePort.position,
      pads,
      ignoredSourcePortIds: [sourcePortId, outsidePort.sourcePortId],
      layer
    })) {
      return true;
    }
  }
  return false;
};
var isCandidateAvailableForOutsidePorts = ({
  candidate,
  bounds,
  usedBoundaryPoints,
  boundaryPointSpacing,
  routeFrom,
  pads,
  sourcePortId,
  outsidePorts,
  layer
}) => {
  if (!isCandidateAvailable({
    candidate,
    usedBoundaryPoints,
    boundaryPointSpacing,
    routeFrom,
    pads,
    sourcePortId,
    layer
  })) {
    return false;
  }
  return !hasOutsideAccessConflict({
    candidate,
    outsidePorts,
    pads,
    sourcePortId,
    layer
  });
};
var getAvailableBreakoutBoundaryPoint = ({
  idealPoint,
  bounds,
  usedBoundaryPoints,
  boundaryPointSpacing,
  routeFrom,
  pads,
  sourcePortId,
  layer
}) => {
  if (isCandidateAvailable({
    candidate: idealPoint,
    usedBoundaryPoints,
    boundaryPointSpacing,
    routeFrom,
    pads,
    sourcePortId,
    layer
  })) {
    return idealPoint;
  }
  const edge = getBoundsEdge(idealPoint, bounds);
  if (!edge) return null;
  const step = getBoundaryCandidateSearchStep({
    bounds,
    boundaryPointSpacing
  });
  if (step <= 0) return null;
  const edgeCandidates = getBoundsEdgeCandidates({
    edge,
    bounds,
    step
  });
  edgeCandidates.sort(
    (a, b) => distance18(a, idealPoint) - distance18(b, idealPoint)
  );
  for (const candidate of edgeCandidates) {
    if (isCandidateAvailable({
      candidate,
      usedBoundaryPoints,
      boundaryPointSpacing,
      routeFrom,
      pads,
      sourcePortId,
      layer
    })) {
      return candidate;
    }
  }
  const candidates = getAllBoundsCandidates({ bounds, step });
  candidates.sort((a, b) => distance18(a, idealPoint) - distance18(b, idealPoint));
  for (const candidate of candidates) {
    if (isCandidateAvailable({
      candidate,
      usedBoundaryPoints,
      boundaryPointSpacing,
      routeFrom,
      pads,
      sourcePortId,
      layer
    })) {
      return candidate;
    }
  }
  return null;
};
var getAvailableBreakoutBoundaryPointForOutsidePorts = ({
  idealPoints,
  bounds,
  usedBoundaryPoints,
  boundaryPointSpacing,
  routeFrom,
  pads,
  sourcePortId,
  outsidePorts,
  layer
}) => {
  const step = getBoundaryCandidateSearchStep({
    bounds,
    boundaryPointSpacing
  });
  if (step <= 0) return null;
  for (const idealPoint of idealPoints) {
    if (isCandidateAvailableForOutsidePorts({
      candidate: idealPoint,
      bounds,
      usedBoundaryPoints,
      boundaryPointSpacing,
      routeFrom,
      pads,
      sourcePortId,
      outsidePorts,
      layer
    })) {
      return idealPoint;
    }
    const edge = getBoundsEdge(idealPoint, bounds);
    if (!edge) continue;
    const edgeCandidates = getBoundsEdgeCandidates({ edge, bounds, step });
    edgeCandidates.sort(
      (a, b) => distance18(a, idealPoint) - distance18(b, idealPoint)
    );
    for (const candidate of edgeCandidates) {
      if (isCandidateAvailableForOutsidePorts({
        candidate,
        bounds,
        usedBoundaryPoints,
        boundaryPointSpacing,
        routeFrom,
        pads,
        sourcePortId,
        outsidePorts,
        layer
      })) {
        return candidate;
      }
    }
  }
  for (const idealPoint of idealPoints) {
    const candidates = getAllBoundsCandidates({ bounds, step });
    candidates.sort((a, b) => distance18(a, idealPoint) - distance18(b, idealPoint));
    for (const candidate of candidates) {
      if (isCandidateAvailableForOutsidePorts({
        candidate,
        bounds,
        usedBoundaryPoints,
        boundaryPointSpacing,
        routeFrom,
        pads,
        sourcePortId,
        outsidePorts,
        layer
      })) {
        return candidate;
      }
    }
  }
  return null;
};

// node_modules/@tscircuit/breakout-point-solver/lib/BreakoutPointSolver.ts
var getLayerVisualStyle = (layer) => {
  if (layer === "bottom") {
    return {
      insidePadFill: "rgba(2, 132, 199, 0.28)",
      insidePadStroke: "#0369a1",
      insidePointColor: "#0369a1",
      outsidePadFill: "rgba(30, 64, 175, 0.2)",
      outsidePadStroke: "#1e40af",
      outsidePointColor: "#1e40af",
      padFill: "rgba(37, 99, 235, 0.18)",
      padStroke: "#1d4ed8",
      componentFill: "rgba(245, 158, 11, 0.1)",
      componentStroke: "#b45309",
      traceStroke: "#2563eb",
      breakoutPointColor: "#0f766e"
    };
  }
  return {
    insidePadFill: "rgba(46, 125, 50, 0.28)",
    insidePadStroke: "#1b5e20",
    insidePointColor: "#1b5e20",
    outsidePadFill: "rgba(106, 27, 154, 0.2)",
    outsidePadStroke: "#6a1b9a",
    outsidePointColor: "#6a1b9a",
    padFill: "rgba(220, 38, 38, 0.22)",
    padStroke: "#b91c1c",
    componentFill: "rgba(245, 158, 11, 0.1)",
    componentStroke: "#b45309",
    traceStroke: "#7e8794",
    breakoutPointColor: "#0d47a1"
  };
};
var getPortPadRect = ({
  port,
  fill,
  stroke,
  fallbackLabel
}) => {
  if (port.width === void 0 || port.height === void 0) return null;
  return {
    center: port.position,
    width: port.width,
    height: port.height,
    ccwRotationDegrees: port.ccwRotationDegrees,
    fill,
    stroke,
    label: port.label ?? fallbackLabel
  };
};
var getAveragePortPosition = (ports) => {
  if (ports.length === 0) return null;
  const total = ports.reduce(
    (sum, port) => ({
      x: sum.x + port.position.x,
      y: sum.y + port.position.y
    }),
    { x: 0, y: 0 }
  );
  return {
    x: total.x / ports.length,
    y: total.y / ports.length
  };
};
var getOutsideTarget = (trace) => getAveragePortPosition(trace.outsidePorts);
var getInsidePortKey = (port) => `${port.sourcePortId}:${port.layer ?? "top"}`;
var getOutsidePortsForInsidePort = ({
  traces,
  insidePort
}) => {
  const outsidePorts = [];
  const insidePortKey = getInsidePortKey(insidePort);
  for (const trace of traces) {
    const hasMatchingInsidePort = trace.insidePorts.some(
      (port) => getInsidePortKey(port) === insidePortKey
    );
    if (!hasMatchingInsidePort) continue;
    outsidePorts.push(...trace.outsidePorts);
  }
  return outsidePorts;
};
var getIdealBoundaryPoints = ({
  insidePort,
  outsidePorts,
  bounds
}) => {
  const idealBoundaryPoints = [];
  for (const outsidePort of outsidePorts) {
    const idealBoundaryPoint = getBreakoutBoundaryIntersection({
      from: insidePort.position,
      to: outsidePort.position,
      bounds
    });
    if (idealBoundaryPoint) idealBoundaryPoints.push(idealBoundaryPoint);
  }
  return idealBoundaryPoints;
};
var BreakoutPointSolver = class extends BaseSolver2 {
  input;
  output = { breakoutPoints: [] };
  constructor(input) {
    super();
    this.input = input;
  }
  _step() {
    const breakoutPoints = [];
    const boundaryPointsByInsidePort = /* @__PURE__ */ new Map();
    for (const trace of this.input.traces) {
      for (const insidePort of trace.insidePorts) {
        const insidePortKey = getInsidePortKey(insidePort);
        let boundaryPoint = boundaryPointsByInsidePort.get(insidePortKey) ?? null;
        if (!boundaryPoint) {
          const outsidePorts = getOutsidePortsForInsidePort({
            traces: this.input.traces,
            insidePort
          });
          const idealBoundaryPoints = getIdealBoundaryPoints({
            insidePort,
            outsidePorts,
            bounds: this.input.bounds
          });
          if (idealBoundaryPoints.length === 0) continue;
          const usedBoundaryPoints = [
            ...this.input.usedBoundaryPoints ?? [],
            ...boundaryPointsByInsidePort.values()
          ];
          if (outsidePorts.length > 1) {
            boundaryPoint = getAvailableBreakoutBoundaryPointForOutsidePorts({
              idealPoints: idealBoundaryPoints,
              bounds: this.input.bounds,
              usedBoundaryPoints,
              boundaryPointSpacing: this.input.boundaryPointSpacing ?? 0,
              routeFrom: insidePort.position,
              pads: this.input.pads,
              sourcePortId: insidePort.sourcePortId,
              outsidePorts,
              layer: insidePort.layer
            });
          }
          boundaryPoint ??= getAvailableBreakoutBoundaryPoint({
            idealPoint: idealBoundaryPoints[0],
            bounds: this.input.bounds,
            usedBoundaryPoints,
            boundaryPointSpacing: this.input.boundaryPointSpacing ?? 0,
            routeFrom: insidePort.position,
            pads: this.input.pads,
            sourcePortId: insidePort.sourcePortId,
            layer: insidePort.layer
          });
          if (!boundaryPoint) continue;
          boundaryPointsByInsidePort.set(insidePortKey, boundaryPoint);
        }
        breakoutPoints.push({
          sourcePortId: insidePort.sourcePortId,
          sourceTraceId: trace.sourceTraceId,
          x: boundaryPoint.x,
          y: boundaryPoint.y,
          ...insidePort.layer ? { layer: insidePort.layer } : {}
        });
      }
    }
    this.output = { breakoutPoints };
    this.solved = true;
  }
  getConstructorParams() {
    return [this.input];
  }
  getOutput() {
    return this.output;
  }
  visualize() {
    const { bounds } = this.input;
    const width = bounds.maxX - bounds.minX;
    const height = bounds.maxY - bounds.minY;
    const center = {
      x: bounds.minX + width / 2,
      y: bounds.minY + height / 2
    };
    return {
      title: "BreakoutPointSolver - generated breakout points",
      rects: [
        {
          center,
          width,
          height,
          fill: "rgba(210, 225, 255, 0.25)",
          stroke: "#315fba",
          label: "breakout bounds"
        },
        ...(this.input.components ?? []).map((component) => ({
          center: component.center,
          width: component.width,
          height: component.height,
          ccwRotationDegrees: component.ccwRotationDegrees,
          fill: getLayerVisualStyle(component.layer).componentFill,
          stroke: getLayerVisualStyle(component.layer).componentStroke,
          label: component.label ?? "component"
        })),
        ...(this.input.pads ?? []).map((pad) => ({
          center: pad.center,
          width: pad.width + (pad.clearance ?? 0) * 2,
          height: pad.height + (pad.clearance ?? 0) * 2,
          ccwRotationDegrees: pad.ccwRotationDegrees,
          fill: getLayerVisualStyle(pad.layer).padFill,
          stroke: getLayerVisualStyle(pad.layer).padStroke,
          label: pad.label ?? "pad"
        })),
        ...this.input.traces.flatMap(
          (trace) => trace.insidePorts.flatMap((port) => {
            const rect = getPortPadRect({
              port,
              fill: getLayerVisualStyle(port.layer).insidePadFill,
              stroke: getLayerVisualStyle(port.layer).insidePadStroke,
              fallbackLabel: `inside pad ${port.sourcePortId}`
            });
            return rect ? [rect] : [];
          })
        ),
        ...this.input.traces.flatMap(
          (trace) => trace.outsidePorts.flatMap((port) => {
            const rect = getPortPadRect({
              port,
              fill: getLayerVisualStyle(port.layer).outsidePadFill,
              stroke: getLayerVisualStyle(port.layer).outsidePadStroke,
              fallbackLabel: `outside pad ${port.sourcePortId}`
            });
            return rect ? [rect] : [];
          })
        )
      ],
      lines: this.input.traces.flatMap(
        (trace) => trace.insidePorts.flatMap((insidePort) => {
          const outsideTarget = getOutsideTarget(trace);
          if (!outsideTarget) return [];
          const breakoutPoint = this.output.breakoutPoints.find(
            (point6) => point6.sourceTraceId === trace.sourceTraceId && point6.sourcePortId === insidePort.sourcePortId
          );
          if (!breakoutPoint) return [];
          return [
            {
              points: [
                insidePort.position,
                { x: breakoutPoint.x, y: breakoutPoint.y }
              ],
              strokeColor: getLayerVisualStyle(insidePort.layer).traceStroke,
              label: `breakout segment ${trace.sourceTraceId}`
            },
            {
              points: [
                { x: breakoutPoint.x, y: breakoutPoint.y },
                outsideTarget
              ],
              strokeColor: getLayerVisualStyle(insidePort.layer).traceStroke,
              strokeDash: "0.15 0.15",
              label: `target guide ${trace.sourceTraceId}`
            }
          ];
        })
      ),
      points: [
        ...this.input.traces.flatMap(
          (trace) => trace.insidePorts.map((port) => ({
            ...port.position,
            color: getLayerVisualStyle(port.layer).insidePointColor,
            label: `inside pad ${port.sourcePortId}`
          }))
        ),
        ...this.input.traces.flatMap(
          (trace) => trace.outsidePorts.map((port) => ({
            ...port.position,
            color: getLayerVisualStyle(port.layer).outsidePointColor,
            label: `outside target ${port.sourcePortId}`
          }))
        ),
        ...this.output.breakoutPoints.map((point6) => ({
          x: point6.x,
          y: point6.y,
          color: getLayerVisualStyle(point6.layer).breakoutPointColor,
          label: `selected breakout ${point6.sourcePortId}`
        })),
        ...(this.input.usedBoundaryPoints ?? []).map((point6) => ({
          ...point6,
          color: "#ef6c00",
          label: "pre-existing breakout point"
        }))
      ]
    };
  }
};

// lib/components/primitive-components/AutoplacedBreakoutPoint.ts
var AutoplacedBreakoutPoint = class extends BaseBreakoutPoint {
  get config() {
    return {
      componentName: "AutoplacedBreakoutPoint",
      zodProps: baseBreakoutPointProps
    };
  }
  doInitialPcbPrimitiveRender() {
    this._renderPcbBreakoutPoint();
  }
};

// lib/components/primitive-components/Breakout/createBreakoutPointSolverInput.ts
import {
  findBoundsAndCenter as findBoundsAndCenter2
} from "@tscircuit/circuit-json-util";
var getPortLabel = (db, sourcePortId) => {
  if (!sourcePortId) return void 0;
  const sourcePort = db.source_port.get(sourcePortId);
  const sourceComponent = sourcePort?.source_component_id ? db.source_component.get(sourcePort.source_component_id) : void 0;
  if (!sourcePort) return void 0;
  return sourceComponent?.name ? `${sourceComponent.name}.${sourcePort.name}` : sourcePort.name;
};
var getPadElement = (db, pcbPortId) => {
  return db.pcb_smtpad.getWhere({ pcb_port_id: pcbPortId }) ?? db.pcb_plated_hole.getWhere({ pcb_port_id: pcbPortId });
};
var toBreakoutPort = (db, pcbPort) => {
  const pad = getPadElement(db, pcbPort.pcb_port_id);
  const padBounds = pad ? findBoundsAndCenter2([pad]) : null;
  return {
    sourcePortId: pcbPort.source_port_id,
    position: { x: pcbPort.x, y: pcbPort.y },
    ...padBounds ? { width: padBounds.width, height: padBounds.height } : {},
    layer: pcbPort.layers?.[0] ?? "top",
    label: getPortLabel(db, pcbPort.source_port_id)
  };
};
var createBreakoutPointSolverInput = (breakout) => {
  if (!breakout.root || !breakout.pcb_group_id) return null;
  const { db } = breakout.root;
  const pcbGroup = db.pcb_group.get(breakout.pcb_group_id);
  if (!pcbGroup || !pcbGroup.width || !pcbGroup.height) return null;
  const sourcePortIdToPcbPort = /* @__PURE__ */ new Map();
  for (const pcbPort of db.pcb_port.list()) {
    if (!pcbPort.source_port_id) continue;
    sourcePortIdToPcbPort.set(pcbPort.source_port_id, pcbPort);
  }
  const boundsMinX = pcbGroup.center.x - pcbGroup.width / 2;
  const boundsMaxX = pcbGroup.center.x + pcbGroup.width / 2;
  const boundsMinY = pcbGroup.center.y - pcbGroup.height / 2;
  const boundsMaxY = pcbGroup.center.y + pcbGroup.height / 2;
  const board = db.pcb_board.list()[0];
  const traceWidth = board?.min_trace_width ?? 0.15;
  const traceToPadEdgeClearance = board?.min_trace_to_pad_edge_clearance ?? 0.1;
  const boundaryPointClearance = board?.min_trace_to_pad_edge_clearance ?? 0.2;
  const viaPadDiameter = board?.min_via_pad_diameter ?? 0.3;
  const padEscapeClearance = traceWidth / 2 + traceToPadEdgeClearance;
  const traces = [];
  for (const sourceTrace of db.source_trace.list()) {
    const pcbPorts = sourceTrace.connected_source_port_ids.map((sourcePortId) => sourcePortIdToPcbPort.get(sourcePortId)).filter((port) => Boolean(port));
    const insidePorts = pcbPorts.filter(
      (port) => port.pcb_group_id === breakout.pcb_group_id
    );
    const outsidePorts = pcbPorts.filter(
      (port) => port.pcb_group_id !== breakout.pcb_group_id && !(port.x >= boundsMinX && port.x <= boundsMaxX && port.y >= boundsMinY && port.y <= boundsMaxY)
    );
    if (insidePorts.length === 0 || outsidePorts.length === 0) continue;
    traces.push({
      sourceTraceId: sourceTrace.source_trace_id,
      insidePorts: insidePorts.map((port) => toBreakoutPort(db, port)),
      outsidePorts: outsidePorts.map((port) => toBreakoutPort(db, port))
    });
  }
  if (traces.length === 0) return null;
  const allPadElements = [...db.pcb_smtpad.list(), ...db.pcb_plated_hole.list()];
  const pads = [];
  for (const pad of allPadElements) {
    const padBounds = findBoundsAndCenter2([pad]);
    if (!padBounds.width || !padBounds.height) continue;
    const pcbPort = pad.pcb_port_id ? db.pcb_port.get(pad.pcb_port_id) : null;
    pads.push({
      center: padBounds.center,
      width: padBounds.width,
      height: padBounds.height,
      // The trace from an inside port to its breakout point is fixed geometry,
      // so reserve its full trace-to-pad clearance inside the breakout. Pads
      // outside the group are target-side routing obstacles and are handled by
      // the autorouter after the breakout point has been selected.
      ...pcbPort?.pcb_group_id === breakout.pcb_group_id ? { clearance: padEscapeClearance } : {},
      layer: pad.layer ?? "top",
      sourcePortIds: pcbPort?.source_port_id ? [pcbPort.source_port_id] : [],
      label: getPortLabel(db, pcbPort?.source_port_id)
    });
  }
  const components = db.pcb_component.list().filter((component) => component.width && component.height).map((component) => ({
    center: component.center,
    width: component.width,
    height: component.height,
    ccwRotationDegrees: component.rotation,
    layer: component.layer,
    label: component.pcb_component_id
  }));
  const usedBoundaryPoints = db.pcb_breakout_point.list().filter((point6) => point6.pcb_group_id === breakout.pcb_group_id).map((point6) => ({ x: point6.x, y: point6.y }));
  const boundaryPointSpacing = viaPadDiameter + 2 * (traceWidth + boundaryPointClearance);
  return {
    bounds: {
      minX: boundsMinX,
      maxX: boundsMaxX,
      minY: boundsMinY,
      maxY: boundsMaxY
    },
    boundaryPointSpacing,
    traces,
    pads,
    components,
    usedBoundaryPoints
  };
};

// lib/components/primitive-components/Breakout/Breakout.ts
var Breakout = class extends Group5 {
  get isRoutingDirective() {
    return true;
  }
  get config() {
    return {
      ...super.config,
      zodProps: breakoutProps
    };
  }
  /**
   * Find ports inside this breakout group that are connected to traces
   * crossing the breakout boundary, and create auto-placed BreakoutPoint
   * children for them. Positions are not set here — they will be determined
   * after PcbLayout.
   */
  doInitialCreateAutoplacedBreakoutPoints() {
    const portsInBreakout = this.selectAll("port");
    const breakoutPortSet = new Set(portsInBreakout);
    const manualBreakoutPoints = this.children.filter(
      (c) => c instanceof BreakoutPoint
    );
    const manuallyMappedPorts = new Set(
      manualBreakoutPoints.map((bp) => {
        bp._matchConnection();
        return bp.matchedPort;
      }).filter(Boolean)
    );
    const allTraces = this.getSubcircuit().selectAll("trace");
    const autoPlacedPorts = /* @__PURE__ */ new Set();
    for (const trace of allTraces) {
      const result = trace._findConnectedPorts();
      if (!result.allPortsFound || !result.ports) continue;
      for (const port of result.ports) {
        const isInside = breakoutPortSet.has(port);
        const hasOutsidePort = result.ports.some((p) => !breakoutPortSet.has(p));
        if (!isInside || !hasOutsidePort) continue;
        if (manuallyMappedPorts.has(port)) continue;
        if (autoPlacedPorts.has(port)) continue;
        autoPlacedPorts.add(port);
        const breakoutPoint = new AutoplacedBreakoutPoint({});
        breakoutPoint.matchedPort = port;
        breakoutPoint.matchedSourceTraceId = trace.source_trace_id;
        this.add(breakoutPoint);
      }
    }
  }
  doInitialPcbAutoplaceBreakoutPoints() {
    if (this.root?.pcbDisabled) return;
    const solverInput = createBreakoutPointSolverInput(this);
    if (!solverInput) return;
    const solver = new BreakoutPointSolver(solverInput);
    solver.solve();
    const output = solver.getOutput();
    const autoBreakoutPoints = this.children.filter(
      (c) => c instanceof AutoplacedBreakoutPoint
    );
    const BOUNDARY_INSET_MM = 1e-4;
    const { bounds } = solverInput;
    const insetWithinBounds = (x, y) => ({
      x: Math.max(
        bounds.minX + BOUNDARY_INSET_MM,
        Math.min(bounds.maxX - BOUNDARY_INSET_MM, x)
      ),
      y: Math.max(
        bounds.minY + BOUNDARY_INSET_MM,
        Math.min(bounds.maxY - BOUNDARY_INSET_MM, y)
      )
    });
    for (const solvedPoint of output.breakoutPoints) {
      const matchingBreakoutPoint = autoBreakoutPoints.find(
        (child) => child.matchedPort?.source_port_id === solvedPoint.sourcePortId
      );
      if (matchingBreakoutPoint) {
        matchingBreakoutPoint.matchedSourceTraceId = solvedPoint.sourceTraceId;
        const insetPoint = insetWithinBounds(solvedPoint.x, solvedPoint.y);
        matchingBreakoutPoint._setPositionFromLayout({
          x: insetPoint.x,
          y: insetPoint.y
        });
      }
    }
  }
  doInitialPcbPrimitiveRender() {
    super.doInitialPcbPrimitiveRender();
  }
};

// lib/components/primitive-components/SilkscreenGraphic.ts
import { silkscreenGraphicProps } from "@tscircuit/props";
import {
  PNG_MIMETYPE,
  SVG_MIMETYPE,
  ensureClockwise,
  getSvgBRepShapes,
  getTransformedSvgPathRoutes,
  loadImageSource
} from "@tscircuit/image-utils";
import { applyToPoint as applyToPoint28 } from "transformation-matrix";
var SilkscreenGraphic = class extends PrimitiveComponent2 {
  pcb_silkscreen_graphic_id = null;
  pcb_silkscreen_graphic_ids = [];
  pcb_silkscreen_path_ids = [];
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "SilkscreenGraphic",
      zodProps: silkscreenGraphicProps
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    this._queueAsyncEffect("SilkscreenGraphicRender", async () => {
      if (this.root?.pcbDisabled) return;
      const { db } = this.root;
      const { _parsedProps: props } = this;
      const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
      const layer = maybeFlipLayer(props.layer ?? "top");
      if (layer !== "top" && layer !== "bottom") {
        throw new Error(
          `Invalid layer "${layer}" for SilkscreenGraphic. Must be "top" or "bottom".`
        );
      }
      const sourceImage = await loadImageSource(props.imageUrl);
      if (sourceImage.mimetype !== SVG_MIMETYPE && sourceImage.mimetype !== PNG_MIMETYPE) {
        throw new Error(
          `Unsupported imageUrl for SilkscreenGraphic: "${props.imageUrl}". Expected an SVG or PNG image.`
        );
      }
      const imageAsset = {
        project_relative_path: sourceImage.projectRelativePath,
        url: sourceImage.dataUrl,
        mimetype: sourceImage.mimetype
      };
      const brepShapes = sourceImage.mimetype === SVG_MIMETYPE ? getSvgBRepShapes({
        svg: sourceImage.text,
        width: props.width,
        height: props.height,
        transform: this._computePcbGlobalTransformBeforeLayout()
      }) : [this.getPlacementBoxBRepShape()];
      const pcbComponentId = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id ?? "";
      for (const brepShape of brepShapes) {
        const graphic = db.insert({
          type: "pcb_silkscreen_graphic",
          pcb_component_id: pcbComponentId,
          pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
          subcircuit_id: this.getSubcircuit()?.subcircuit_id ?? void 0,
          layer,
          shape: "brep",
          brep_shape: brepShape,
          image_asset: imageAsset
        });
        this.pcb_silkscreen_graphic_ids.push(graphic.pcb_silkscreen_graphic_id);
      }
      this.pcb_silkscreen_graphic_id = this.pcb_silkscreen_graphic_ids[0] ?? null;
      if (sourceImage.mimetype === SVG_MIMETYPE) {
        this.insertRenderableSilkscreenPaths(sourceImage.text, layer);
      }
    });
  }
  insertRenderableSilkscreenPaths(svg, layer) {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const transform = this._computePcbGlobalTransformBeforeLayout();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id ?? "";
    for (const route of getTransformedSvgPathRoutes({
      svg,
      width: props.width,
      height: props.height,
      transform
    })) {
      if (route.length < 2) continue;
      const path = db.pcb_silkscreen_path.insert({
        pcb_component_id,
        layer,
        route,
        stroke_width: Math.max(Math.min(props.width, props.height) / 60, 0.05),
        subcircuit_id: this.getSubcircuit()?.subcircuit_id ?? void 0,
        pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0
      });
      this.pcb_silkscreen_path_ids.push(path.pcb_silkscreen_path_id);
    }
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    return { width: props.width, height: props.height };
  }
  getPlacementBoxBRepShape() {
    const { _parsedProps: props } = this;
    const halfWidth = props.width / 2;
    const halfHeight = props.height / 2;
    const transform = this._computePcbGlobalTransformBeforeLayout();
    return {
      outer_ring: {
        vertices: ensureClockwise([
          { x: -halfWidth, y: halfHeight },
          { x: halfWidth, y: halfHeight },
          { x: halfWidth, y: -halfHeight },
          { x: -halfWidth, y: -halfHeight }
        ]).map(
          (point6) => applyToPoint28(transform, point6)
        )
      },
      inner_rings: []
    };
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const moveVertex = (vertex) => ({
      ...vertex,
      x: vertex.x + deltaX,
      y: vertex.y + deltaY
    });
    for (const graphicId of this.pcb_silkscreen_graphic_ids) {
      const graphic = db.pcb_silkscreen_graphic.get(
        graphicId
      );
      if (!graphic) continue;
      db.pcb_silkscreen_graphic.update(graphicId, {
        brep_shape: {
          outer_ring: {
            vertices: graphic.brep_shape.outer_ring.vertices.map(moveVertex)
          },
          inner_rings: graphic.brep_shape.inner_rings.map((ring) => ({
            vertices: ring.vertices.map(moveVertex)
          }))
        }
      });
    }
    for (const pathId of this.pcb_silkscreen_path_ids) {
      const path = db.pcb_silkscreen_path.get(pathId);
      if (!path) continue;
      db.pcb_silkscreen_path.update(pathId, {
        route: path.route.map((point6) => ({
          ...point6,
          x: point6.x + deltaX,
          y: point6.y + deltaY
        }))
      });
    }
  }
};

// lib/components/primitive-components/Fiducial.ts
import "zod";
import { distance as distance19 } from "circuit-json";
import { fiducialProps as fiducialProps2 } from "@tscircuit/props";
var Fiducial = class extends PrimitiveComponent2 {
  pcb_smtpad_id = null;
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "Fiducial",
      zodProps: fiducialProps2,
      sourceFtype: "simple_fiducial"
    };
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const position = this._getGlobalPcbPositionBeforeLayout();
    const { maybeFlipLayer } = this._getPcbPrimitiveFlippedHelpers();
    const pcb_component_id = this.parent?.pcb_component_id ?? this.getPrimitiveContainer()?.pcb_component_id;
    const pcb_smtpad = db.pcb_smtpad.insert({
      pcb_component_id,
      layer: maybeFlipLayer(props.layer || "top"),
      shape: "circle",
      x: position.x,
      y: position.y,
      radius: distance19.parse(props.padDiameter) / 2,
      soldermask_margin: props.soldermaskPullback ? distance19.parse(props.soldermaskPullback) : distance19.parse(props.padDiameter) / 2,
      is_covered_with_solder_mask: true
    });
    this.pcb_smtpad_id = pcb_smtpad.pcb_smtpad_id;
  }
  getPcbSize() {
    const { _parsedProps: props } = this;
    const d = distance19.parse(props.padDiameter);
    return { width: d, height: d };
  }
  _setPositionFromLayout(newCenter) {
    if (!this.pcb_smtpad_id) return;
    const { db } = this.root;
    db.pcb_smtpad.update(this.pcb_smtpad_id, {
      x: newCenter.x,
      y: newCenter.y
    });
  }
  _moveCircuitJsonElements({
    deltaX,
    deltaY
  }) {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    if (!this.pcb_smtpad_id) return;
    const pad = db.pcb_smtpad.get(this.pcb_smtpad_id);
    if (!pad) return;
    if (pad.shape === "rect" || pad.shape === "circle" || pad.shape === "rotated_rect" || pad.shape === "pill") {
      this._setPositionFromLayout({ x: pad.x + deltaX, y: pad.y + deltaY });
    } else if (pad.shape === "polygon") {
      db.pcb_smtpad.update(this.pcb_smtpad_id, {
        points: pad.points.map((p) => ({
          x: p.x + deltaX,
          y: p.y + deltaY
        }))
      });
    }
  }
};

// lib/components/primitive-components/Via.ts
import { viaProps as viaProps2 } from "@tscircuit/props";
import "zod";
var Via = class extends PrimitiveComponent2 {
  pcb_via_id = null;
  matchedPort = null;
  isPcbPrimitive = true;
  source_manually_placed_via_id = null;
  source_trace_id = null;
  subcircuit_connectivity_map_key = null;
  constructor(props) {
    super(props);
    const layers = this._getLayers();
    this._parsedProps.layers = layers;
    this.initPorts();
  }
  get config() {
    return {
      componentName: "Via",
      zodProps: viaProps2
    };
  }
  getAvailablePcbLayers() {
    return this._getLayers();
  }
  _getResolvedViaDiameters(pcbStyle) {
    return getViaDiameterDefaultsWithOverrides(
      {
        holeDiameter: this._parsedProps.holeDiameter,
        padDiameter: this._parsedProps.outerDiameter
      },
      pcbStyle
    );
  }
  getPcbSize() {
    const pcbStyle = this.getInheritedMergedProperty("pcbStyle");
    const { padDiameter } = this._getResolvedViaDiameters(pcbStyle);
    return { width: padDiameter, height: padDiameter };
  }
  _getPcbCircuitJsonBounds() {
    const { db } = this.root;
    const via = db.pcb_via.get(this.pcb_via_id);
    const size = this.getPcbSize();
    return {
      center: { x: via.x, y: via.y },
      bounds: {
        left: via.x - size.width / 2,
        top: via.y - size.height / 2,
        right: via.x + size.width / 2,
        bottom: via.y + size.height / 2
      },
      width: size.width,
      height: size.height
    };
  }
  _setPositionFromLayout(newCenter) {
    const { db } = this.root;
    db.pcb_via.update(this.pcb_via_id, {
      x: newCenter.x,
      y: newCenter.y
    });
  }
  _getLayers() {
    const { fromLayer = "top", toLayer = "bottom" } = this._parsedProps;
    const layerCount = this.parent ? this.getSubcircuit()._getSubcircuitLayerCount() : 2;
    return getViaSpanLayers({ fromLayer, toLayer, layerCount });
  }
  initPorts() {
    const layers = this._parsedProps.layers;
    for (const layer of layers) {
      const port2 = new Port({ name: layer, layer });
      port2.registerMatch(this);
      this.add(port2);
    }
    const port = new Port({ name: "pin1" });
    port.registerMatch(this);
    this.add(port);
  }
  /**
   * Find the Net or Trace that this via is connected to
   */
  _getConnectedNetOrTrace() {
    const connectsTo = this._parsedProps.connectsTo;
    if (!connectsTo) {
      return this._getPortFromContainingPad()?._getDirectlyConnectedTraces()[0] ?? null;
    }
    const subcircuit = this.getSubcircuit();
    const selectors = Array.isArray(connectsTo) ? connectsTo : [connectsTo];
    for (const selector of selectors) {
      if (selector.startsWith("net.")) {
        const net = subcircuit.selectOne(selector, {
          type: "net"
        });
        if (net) return net;
      }
    }
    return null;
  }
  doInitialCreateNetsFromProps() {
    const connectsTo = this._parsedProps.connectsTo;
    if (!connectsTo) return;
    createNetsFromProps(
      this,
      Array.isArray(connectsTo) ? connectsTo : [connectsTo]
    );
  }
  _getPortFromContainingPad() {
    if (this.parent?.componentName !== "Footprint") return null;
    const containingPadPorts = new Set(
      this.parent.children.filter((child) => child.componentName === "SmtPad").filter((pad) => isPcbPrimitiveContainedWithinBeforeRender(this, pad)).map((pad) => pad.matchedPort).filter((port) => port !== null)
    );
    return containingPadPorts.size === 1 ? [...containingPadPorts][0] : null;
  }
  doInitialPcbComponentRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const pcbStyle = this.getInheritedMergedProperty("pcbStyle");
    const { padDiameter } = this._getResolvedViaDiameters(pcbStyle);
    const position = this._getGlobalPcbPositionBeforeLayout();
    const subcircuit = this.getSubcircuit();
    const pcb_component = db.pcb_component.insert({
      center: position,
      width: padDiameter,
      height: padDiameter,
      layer: this._parsedProps.fromLayer ?? "top",
      rotation: 0,
      source_component_id: this.source_component_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      obstructs_within_bounds: true
    });
    this.pcb_component_id = pcb_component.pcb_component_id;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const group = this.getGroup();
    const subcircuit = this.getSubcircuit();
    const source_via = db.source_manually_placed_via.insert({
      source_group_id: group?.source_group_id,
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0
    });
    this.source_manually_placed_via_id = source_via.source_manually_placed_via_id;
    this.source_component_id = source_via.source_manually_placed_via_id;
  }
  doInitialSourceTraceRender() {
    const connectsTo = this._parsedProps.connectsTo;
    if (!connectsTo) return;
    const { db } = this.root;
    const subcircuit = this.getSubcircuit();
    const selectors = Array.isArray(connectsTo) ? connectsTo : [connectsTo];
    const connectedPorts = [];
    const connectedNets = [];
    for (const selector of selectors) {
      if (selector.startsWith("net.")) {
        const net = subcircuit.selectOne(selector, {
          type: "net"
        });
        if (!net?.source_net_id) {
          this.renderError(`Could not find net for via selector "${selector}"`);
          return;
        }
        connectedNets.push(net);
        continue;
      }
      const port = subcircuit.selectOne(selector, { type: "port" }) ?? this.getParentNormalComponent()?.children.find(
        (child) => child instanceof Port && child.isMatchingAnyOf([selector])
      ) ?? null;
      if (!port?.source_port_id) {
        this.renderError(`Could not find port for via selector "${selector}"`);
        return;
      }
      connectedPorts.push(port);
    }
    if (connectedPorts.length > 0) {
      const sourceTrace = db.source_trace.insert({
        connected_source_port_ids: connectedPorts.map(
          (port) => port.source_port_id
        ),
        connected_source_net_ids: connectedNets.map(
          (net) => net.source_net_id
        ),
        subcircuit_id: subcircuit.subcircuit_id ?? void 0,
        display_name: this._parsedProps.name ? `${this._parsedProps.name} connectivity` : "Manually placed via connectivity"
      });
      this.source_trace_id = sourceTrace.source_trace_id;
    }
    db.source_manually_placed_via.update(this.source_manually_placed_via_id, {
      source_net_id: connectedNets[0]?.source_net_id,
      source_trace_id: this.source_trace_id ?? void 0
    });
  }
  doInitialPcbPrimitiveRender() {
    if (this.root?.pcbDisabled) return;
    const { db } = this.root;
    const pcbStyle = this.getInheritedMergedProperty("pcbStyle");
    const { holeDiameter, padDiameter } = this._getResolvedViaDiameters(pcbStyle);
    const position = this._getGlobalPcbPositionBeforeLayout();
    const subcircuit = this.getSubcircuit();
    const connectedNetOrTrace = this._getConnectedNetOrTrace();
    const sourceTraceId = this.source_trace_id ?? (connectedNetOrTrace instanceof Net ? void 0 : connectedNetOrTrace?.source_trace_id);
    const sourceNetId = !sourceTraceId && connectedNetOrTrace instanceof Net ? connectedNetOrTrace.source_net_id : void 0;
    const pcb_via = db.pcb_via.insert({
      x: position.x,
      y: position.y,
      hole_diameter: holeDiameter,
      outer_diameter: padDiameter,
      layers: this._getLayers(),
      from_layer: this._parsedProps.fromLayer || "bottom",
      to_layer: this._parsedProps.toLayer || "top",
      subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
      subcircuit_connectivity_map_key: this.subcircuit_connectivity_map_key ?? void 0,
      pcb_group_id: this.getGroup()?.pcb_group_id ?? void 0,
      net_is_assignable: this._parsedProps.netIsAssignable ?? void 0,
      ...sourceTraceId ? { source_trace_id: sourceTraceId } : {},
      ...sourceNetId ? { source_net_id: sourceNetId } : {}
    });
    this.pcb_via_id = pcb_via.pcb_via_id;
  }
};

// lib/components/primitive-components/CopperPour/CopperPour.ts
import { copperPourProps } from "@tscircuit/props";
import {
  CopperPourPipelineSolver as CopperPourPipelineSolver2,
  convertCircuitJsonToInputProblem,
  initializeManifoldGeometry
} from "@tscircuit/copper-pour-solver";

// lib/components/primitive-components/CopperPour/utils/mark-trace-segments-inside-copper-pour.ts
var EPSILON4 = 1e-9;
var isWireRoutePoint = (routePoint) => routePoint.route_type === "wire";
var isPointOnSegment = (p, a, b) => {
  const cross3 = (p.y - a.y) * (b.x - a.x) - (p.x - a.x) * (b.y - a.y);
  if (Math.abs(cross3) > EPSILON4) return false;
  const dot = (p.x - a.x) * (b.x - a.x) + (p.y - a.y) * (b.y - a.y);
  if (dot < -EPSILON4) return false;
  const squaredLength = (b.x - a.x) ** 2 + (b.y - a.y) ** 2;
  if (dot - squaredLength > EPSILON4) return false;
  return true;
};
var isPointInRing = (point6, ring) => {
  if (ring.length < 3) return false;
  let inside = false;
  let previous = ring[ring.length - 1];
  for (const current of ring) {
    if (isPointOnSegment(point6, previous, current)) return true;
    const intersects = current.y > point6.y !== previous.y > point6.y && point6.x < (previous.x - current.x) * (point6.y - current.y) / (previous.y - current.y) + current.x;
    if (intersects) inside = !inside;
    previous = current;
  }
  return inside;
};
var isPointInRectPour = (p, pour) => {
  const { center, width, height } = pour;
  const rotationRad = (pour.rotation ?? 0) * Math.PI / 180;
  const cosR = Math.cos(-rotationRad);
  const sinR = Math.sin(-rotationRad);
  const dx = p.x - center.x;
  const dy = p.y - center.y;
  const localX = dx * cosR - dy * sinR;
  const localY = dx * sinR + dy * cosR;
  return Math.abs(localX) <= width / 2 + EPSILON4 && Math.abs(localY) <= height / 2 + EPSILON4;
};
var isPointInBrepPour = (p, pour) => {
  const outerRing = pour.brep_shape.outer_ring.vertices.map((v) => ({
    x: v.x,
    y: v.y
  }));
  if (!isPointInRing(p, outerRing)) return false;
  for (const innerRing of pour.brep_shape.inner_rings) {
    const points = innerRing.vertices.map((v) => ({ x: v.x, y: v.y }));
    if (isPointInRing(p, points)) return false;
  }
  return true;
};
var isPointInCopperPour = (point6, pour) => {
  if (pour.shape === "rect") {
    return isPointInRectPour(point6, pour);
  }
  if (pour.shape === "brep") {
    return isPointInBrepPour(point6, pour);
  }
  return false;
};
var isTraceConnectedToSourceNet = (trace, sourceNetId, sourceTraceById) => {
  if (trace.source_trace_id === sourceNetId) return true;
  if (!trace.source_trace_id) return false;
  const sourceTrace = sourceTraceById.get(trace.source_trace_id);
  if (!sourceTrace) return false;
  return sourceTrace.connected_source_net_ids.includes(sourceNetId);
};
var isSegmentFullyInsideCopperPour = (start, end, pour) => {
  const dx = end.x - start.x;
  const dy = end.y - start.y;
  const length7 = Math.hypot(dx, dy);
  if (length7 <= EPSILON4) return false;
  const samples = [0, 0.25, 0.5, 0.75, 1];
  return samples.every(
    (t) => isPointInCopperPour(
      {
        x: start.x + dx * t,
        y: start.y + dy * t
      },
      pour
    )
  );
};
var markTraceSegmentsInsideCopperPour = ({
  db,
  copperPour
}) => {
  if (!copperPour.source_net_id) return;
  const sourceTraceById = new Map(
    db.source_trace.list().map((sourceTrace) => [sourceTrace.source_trace_id, sourceTrace])
  );
  for (const trace of db.pcb_trace.list()) {
    if (!isTraceConnectedToSourceNet(
      trace,
      copperPour.source_net_id,
      sourceTraceById
    )) {
      continue;
    }
    let routeChanged = false;
    const nextRoute = trace.route.map((routePoint) => ({ ...routePoint }));
    for (let i = 0; i < nextRoute.length - 1; i++) {
      const fromRoutePoint = nextRoute[i];
      const toRoutePoint = nextRoute[i + 1];
      if (!fromRoutePoint || !toRoutePoint) continue;
      if (!isWireRoutePoint(fromRoutePoint) || !isWireRoutePoint(toRoutePoint))
        continue;
      if (fromRoutePoint.layer !== copperPour.layer || toRoutePoint.layer !== copperPour.layer)
        continue;
      if (isSegmentFullyInsideCopperPour(
        { x: fromRoutePoint.x, y: fromRoutePoint.y },
        { x: toRoutePoint.x, y: toRoutePoint.y },
        copperPour
      )) {
        fromRoutePoint.is_inside_copper_pour = true;
        fromRoutePoint.copper_pour_id = copperPour.pcb_copper_pour_id;
        toRoutePoint.is_inside_copper_pour = true;
        toRoutePoint.copper_pour_id = copperPour.pcb_copper_pour_id;
        routeChanged = true;
      }
    }
    if (routeChanged) {
      db.pcb_trace.update(trace.pcb_trace_id, { route: nextRoute });
    }
  }
};

// lib/components/primitive-components/CopperPour/CopperPour.ts
var CopperPour = class extends PrimitiveComponent2 {
  isPcbPrimitive = true;
  get config() {
    return {
      componentName: "CopperPour",
      zodProps: copperPourProps
    };
  }
  getPcbSize() {
    return { width: 0, height: 0 };
  }
  doInitialCreateNetsFromProps() {
    const { _parsedProps: props } = this;
    createNetsFromProps(this, [props.connectsTo]);
  }
  doInitialPcbCopperPourRender() {
    if (this.root?.pcbDisabled) return;
    this._queueAsyncEffect("PcbCopperPourRender", async () => {
      const { db } = this.root;
      const { _parsedProps: props } = this;
      const net = this.getSubcircuit().selectOne(props.connectsTo);
      if (!net || !net.source_net_id) {
        this.renderError(`Net "${props.connectsTo}" not found for copper pour`);
        return;
      }
      const subcircuit = this.getSubcircuit();
      const circuitJson = db.toArray();
      const clearance = props.clearance ?? 0.2;
      const inputProblem = convertCircuitJsonToInputProblem(circuitJson, {
        layer: props.layer,
        subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
        source_net_id: net.source_net_id,
        pad_margin: props.padMargin ?? clearance,
        trace_margin: props.traceMargin ?? clearance,
        board_edge_margin: props.boardEdgeMargin ?? clearance,
        cutout_margin: props.cutoutMargin ?? clearance,
        outline: props.outline
      });
      await initializeManifoldGeometry();
      const solver = new CopperPourPipelineSolver2(inputProblem);
      this.root.emit("solver:started", {
        type: "solver:started",
        solverName: "CopperPourPipelineSolver",
        solverParams: inputProblem,
        solverConstructorArgs: [inputProblem],
        componentName: this.props.name
      });
      const { brep_shapes } = solver.getOutput();
      const coveredWithSolderMask = props.coveredWithSolderMask ?? false;
      for (const brep_shape of brep_shapes) {
        const insertedPour = db.pcb_copper_pour.insert({
          shape: "brep",
          layer: props.layer,
          brep_shape,
          source_net_id: net.source_net_id,
          subcircuit_id: subcircuit?.subcircuit_id ?? void 0,
          covered_with_solder_mask: coveredWithSolderMask
        });
        markTraceSegmentsInsideCopperPour({
          db,
          copperPour: insertedPour
        });
      }
    });
  }
};

// lib/components/primitive-components/EnclosureCutoutAperture/EnclosureCutoutAperture.ts
import { enclosureCutoutApertureProps } from "@tscircuit/props";

// lib/components/primitive-components/EnclosureCutoutAperture/get-aperture-height.ts
var getApertureHeight = (aperture) => {
  const margin = aperture.margin ?? 0;
  return aperture.shape === "circle" ? 2 * (aperture.radius + margin) : aperture.height + margin * 2;
};

// lib/components/primitive-components/EnclosureCutoutAperture/get-nearest-board-wall.ts
var getNearestBoardWall = ({
  point: point6,
  board
}) => {
  const halfWidth = (board.width ?? 0) / 2;
  const halfHeight = (board.height ?? 0) / 2;
  const distances = [
    ["left", Math.abs(point6.x - (board.center.x - halfWidth))],
    ["right", Math.abs(point6.x - (board.center.x + halfWidth))],
    ["front", Math.abs(point6.y - (board.center.y - halfHeight))],
    ["back", Math.abs(point6.y - (board.center.y + halfHeight))]
  ];
  distances.sort((a, b) => a[1] - b[1]);
  return distances[0][0];
};

// lib/components/primitive-components/EnclosureCutoutAperture/get-solver-wall.ts
var getSolverWall = (boardWall) => {
  if (boardWall === "front") return "back";
  if (boardWall === "back") return "front";
  return boardWall;
};

// lib/components/primitive-components/EnclosureCutoutAperture/get-fdm-enclosure-solver-input.ts
var getFdmEnclosureSolverInput = (apertureComponent, { board, pcbBoard, floorThickness }) => {
  let owner = apertureComponent.parent;
  while (owner && owner !== board && !owner.pcb_component_id) {
    owner = owner.parent;
  }
  const pcbComponent = owner?.pcb_component_id ? apertureComponent.root?.db.pcb_component.get(owner.pcb_component_id) : null;
  if (!pcbComponent) {
    throw new Error(
      "<enclosure.cutoutaperture /> must be nested inside a component with a PCB footprint"
    );
  }
  const point6 = pcbComponent.cable_insertion_center ?? pcbComponent.center;
  const boardWall = getNearestBoardWall({ point: point6, board: pcbBoard });
  const aperture = apertureComponent._parsedProps;
  const commonInput = {
    wall: getSolverWall(boardWall),
    offset: boardWall === "left" || boardWall === "right" ? -(point6.y - pcbBoard.center.y) : point6.x - pcbBoard.center.x,
    centerZ: floorThickness + (pcbBoard.thickness ?? board.boardThickness) + getApertureHeight(aperture) / 2,
    margin: aperture.margin
  };
  switch (aperture.shape) {
    case "pill":
    case "rect":
      return {
        ...commonInput,
        shape: aperture.shape,
        width: aperture.width,
        height: aperture.height
      };
    case "circle":
      return {
        ...commonInput,
        shape: "circle",
        radius: aperture.radius
      };
  }
};

// lib/components/primitive-components/EnclosureCutoutAperture/EnclosureCutoutAperture.ts
var EnclosureCutoutAperture = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "EnclosureCutoutAperture",
      zodProps: enclosureCutoutApertureProps
    };
  }
  getFdmEnclosureSolverInput(params) {
    return getFdmEnclosureSolverInput(this, params);
  }
};

// lib/components/primitive-components/EnclosureFdmBox.ts
import { enclosureFdmBoxProps } from "@tscircuit/props";

// lib/components/primitive-components/EnclosureFdmBox_doInitialCadModelRender.ts
import {
  CreateFdmEnclosureSolver as CreateFdmEnclosureSolver2
} from "@tscircuit/create-fdm-enclosure";

// lib/components/primitive-components/get-referenced-enclosure-board.ts
import { selectOne as selectOne2 } from "css-select";
var getReferencedEnclosureBoard = (component, boardRef) => {
  const rootComponent = component.root?.firstChild;
  const selectedComponent = rootComponent ? selectOne2(boardRef, rootComponent, {
    adapter: cssSelectPrimitiveComponentAdapter
  }) : null;
  const board = selectedComponent instanceof Board ? selectedComponent : [rootComponent, ...rootComponent?.getDescendants() ?? []].find(
    (descendant) => descendant instanceof Board && descendant.isMatchingNameOrAlias(boardRef)
  );
  if (!(board instanceof Board)) {
    throw new Error(
      `Could not find a <board /> matching enclosure boardRef "${boardRef}"`
    );
  }
  return board;
};

// lib/components/primitive-components/EnclosureFdmBox_doInitialCadModelRender.ts
var EnclosureFdmBox_doInitialCadModelRender = (component) => {
  const root = component.root;
  if (!root || root.pcbDisabled || !component.source_component_id || !component.pcb_component_id) {
    return;
  }
  const { db } = root;
  const props = component._parsedProps;
  const board = getReferencedEnclosureBoard(component, props.boardRef);
  const pcbBoard = board.pcb_board_id ? db.pcb_board.get(board.pcb_board_id) : null;
  if (!pcbBoard?.width || !pcbBoard.height) {
    throw new Error(
      `Could not resolve dimensions for boardRef "${props.boardRef}"`
    );
  }
  const boardThickness = pcbBoard.thickness ?? board.boardThickness;
  const inputProblem = {
    board: {
      width: pcbBoard.width,
      height: pcbBoard.height,
      thickness: boardThickness
    },
    width: props.width,
    height: props.height,
    depth: props.depth,
    wallThickness: props.wallThickness,
    apertures: board.getDescendants().filter(
      (descendant) => descendant instanceof EnclosureCutoutAperture
    ).map(
      (aperture) => aperture.getFdmEnclosureSolverInput({
        board,
        pcbBoard,
        floorThickness: props.wallThickness
      })
    )
  };
  const solver = new CreateFdmEnclosureSolver2(inputProblem);
  const solverConstructorArgs = solver.getConstructorParams();
  root.emit("solver:started", {
    type: "solver:started",
    solverName: "CreateFdmEnclosureSolver",
    solverParams: solverConstructorArgs[0],
    solverConstructorArgs,
    componentName: component.getString()
  });
  solver.solve();
  if (solver.failed) {
    throw new Error(solver.error ?? "Failed to create FDM enclosure");
  }
  const output = solver.getOutput();
  db.pcb_component.update(component.pcb_component_id, {
    center: pcbBoard.center,
    width: output.dimensions.width,
    height: output.dimensions.height
  });
  const cadComponent = db.cad_component.insert({
    position: {
      x: pcbBoard.center.x,
      y: pcbBoard.center.y,
      z: -boardThickness / 2 - output.dimensions.floorThickness
    },
    rotation: { x: 0, y: 0, z: 0 },
    pcb_component_id: component.pcb_component_id,
    source_component_id: component.source_component_id,
    model_jscad: output.jscadPlan,
    model_unit_to_mm_scale_factor: 1,
    model_object_fit: "contain_within_bounds",
    model_origin_alignment: "bottom_center_of_component",
    anchor_alignment: "center",
    show_as_translucent_model: false
  });
  component.cad_component_id = cadComponent.cad_component_id;
};

// lib/components/primitive-components/EnclosureFdmBox.ts
var EnclosureFdmBox = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "EnclosureFdmBox",
      zodProps: enclosureFdmBoxProps
    };
  }
  doInitialSourceRender() {
    const sourceComponent = this.root.db.source_component.insert({
      ftype: "simple_chip",
      name: this.name
    });
    this.source_component_id = sourceComponent.source_component_id;
  }
  doInitialPcbComponentRender() {
    const root = this.root;
    if (!root || root.pcbDisabled || !this.source_component_id) return;
    const board = getReferencedEnclosureBoard(this, this._parsedProps.boardRef);
    const pcbBoard = board.pcb_board_id ? root.db.pcb_board.get(board.pcb_board_id) : null;
    const center = pcbBoard?.center ?? board._getGlobalPcbPositionBeforeLayout();
    const pcbComponent = root.db.pcb_component.insert({
      center,
      width: 0,
      height: 0,
      layer: "top",
      rotation: 0,
      source_component_id: this.source_component_id,
      obstructs_within_bounds: false,
      do_not_place: true,
      is_allowed_to_be_off_board: true
    });
    this.pcb_component_id = pcbComponent.pcb_component_id;
  }
  doInitialCadModelRender() {
    EnclosureFdmBox_doInitialCadModelRender(this);
  }
};

// lib/components/normal-components/Battery.ts
import { batteryProps } from "@tscircuit/props";
var Battery = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "Battery",
      schematicSymbolName: this.props.symbolName ?? "battery",
      zodProps: batteryProps,
      sourceFtype: "simple_power_source"
    };
  }
  initPorts() {
    super.initPorts({
      additionalAliases: {
        pin1: ["anode", "pos", "left"],
        pin2: ["cathode", "neg", "right"]
      }
    });
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      name: this.name,
      ftype: "simple_power_source",
      capacity: props.capacity,
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      are_pins_interchangeable: false,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/normal-components/PinHeader.ts
import { pinHeaderProps } from "@tscircuit/props";
var PinHeader = class extends NormalComponent3 {
  _getPcbRotationBeforeLayout() {
    const orientationRotation = this.props.pcbOrientation === "vertical" ? -90 : 0;
    const baseRotation = super._getPcbRotationBeforeLayout() ?? 0;
    return baseRotation + orientationRotation;
  }
  get config() {
    return {
      componentName: "PinHeader",
      zodProps: pinHeaderProps,
      shouldRenderAsSchematicBox: true
    };
  }
  _getImpliedFootprintString() {
    const pinCount = this._parsedProps.pinCount ?? (Array.isArray(this._parsedProps.pinLabels) ? this._parsedProps.pinLabels.length : this._parsedProps.pinLabels ? Object.keys(this._parsedProps.pinLabels).length : 0);
    const holeDiameter = this._parsedProps.holeDiameter;
    const platedDiameter = this._parsedProps.platedDiameter;
    const pitch = this._parsedProps.pitch;
    const showSilkscreenPinLabels = this._parsedProps.showSilkscreenPinLabels;
    const rows = this._parsedProps.doubleRow ? 2 : 1;
    const gender = this._parsedProps.gender;
    if (pinCount > 0) {
      let footprintString;
      if (pitch) {
        if (!holeDiameter && !platedDiameter) {
          footprintString = `pinrow${pinCount}_p${pitch}`;
        } else {
          footprintString = `pinrow${pinCount}_p${pitch}_id${holeDiameter}_od${platedDiameter}`;
        }
      } else {
        if (!holeDiameter && !platedDiameter) {
          footprintString = `pinrow${pinCount}`;
        } else {
          return null;
        }
      }
      if (showSilkscreenPinLabels !== true) {
        footprintString += "_nopinlabels";
      }
      if (gender === "female") {
        footprintString += "_female";
      }
      if (rows > 1) {
        footprintString += `_rows${rows}`;
      }
      return footprintString;
    }
    return null;
  }
  initPorts() {
    const pinCount = this._parsedProps.pinCount ?? (Array.isArray(this._parsedProps.pinLabels) ? this._parsedProps.pinLabels.length : this._parsedProps.pinLabels ? Object.keys(this._parsedProps.pinLabels).length : 1);
    for (let i = 1; i <= pinCount; i++) {
      const rawLabel = Array.isArray(this._parsedProps.pinLabels) ? this._parsedProps.pinLabels[i - 1] : this._parsedProps.pinLabels?.[`pin${i}`];
      if (rawLabel) {
        const primaryLabel = Array.isArray(rawLabel) ? rawLabel[0] : rawLabel;
        const otherLabels = Array.isArray(rawLabel) ? rawLabel.slice(1) : [];
        this.add(
          new Port({
            pinNumber: i,
            name: primaryLabel,
            aliases: [`pin${i}`, ...otherLabels]
          })
        );
      } else {
        this.add(
          new Port({
            pinNumber: i,
            name: `pin${i}`
          })
        );
      }
    }
  }
  _getSchematicPortArrangement() {
    const pinCount = this._parsedProps.pinCount ?? 1;
    const facingDirection = this._parsedProps.schFacingDirection ?? this._parsedProps.facingDirection ?? "right";
    const schPinArrangement = this._parsedProps.schPinArrangement;
    const pins = Array.from({ length: pinCount }, (_, i) => `pin${i + 1}`);
    if (facingDirection === "left") {
      return {
        leftSide: {
          direction: schPinArrangement?.leftSide?.direction ?? "top-to-bottom",
          pins: schPinArrangement?.leftSide?.pins ?? pins
        }
      };
    }
    if (facingDirection === "up") {
      return {
        topSide: {
          direction: schPinArrangement?.topSide?.direction ?? "left-to-right",
          pins: schPinArrangement?.topSide?.pins ?? pins
        }
      };
    }
    if (facingDirection === "down") {
      return {
        bottomSide: {
          direction: schPinArrangement?.bottomSide?.direction ?? "left-to-right",
          pins: schPinArrangement?.bottomSide?.pins ?? pins
        }
      };
    }
    return {
      rightSide: {
        direction: schPinArrangement?.rightSide?.direction ?? "top-to-bottom",
        pins: schPinArrangement?.rightSide?.pins ?? pins
      }
    };
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_pin_header",
      name: this.name,
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      pin_count: props.pinCount,
      gender: props.gender,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/normal-components/Resonator.ts
import { resonatorProps } from "@tscircuit/props";
import { formatSiUnit as formatSiUnit7 } from "format-si-unit";
function getResonatorSymbolName(variant) {
  switch (variant) {
    case "two_ground_pins":
      return `crystal_4pin`;
    case "ground_pin":
      return "resonator";
    case "no_ground":
      return "crystal";
    default:
      return "crystal";
  }
}
var Resonator = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "Resonator",
      schematicSymbolName: this.props.symbolName ?? getResonatorSymbolName(this.props.pinVariant),
      zodProps: resonatorProps,
      shouldRenderAsSchematicBox: false
    };
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const pinVariant = props.pinVariant || "no_ground";
    const source_component = db.source_component.insert({
      ftype: "simple_resonator",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      frequency: props.frequency,
      load_capacitance: props.loadCapacitance,
      supplier_part_numbers: props.supplierPartNumbers,
      pin_variant: pinVariant,
      are_pins_interchangeable: pinVariant === "no_ground" || pinVariant === "ground_pin",
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  _getSchematicSymbolDisplayValue() {
    const freqDisplay = `${formatSiUnit7(this._parsedProps.frequency)}Hz`;
    if (this._parsedProps.loadCapacitance) {
      return `${freqDisplay} / ${formatSiUnit7(this._parsedProps.loadCapacitance)}F`;
    }
    return freqDisplay;
  }
};

// lib/components/normal-components/Potentiometer.ts
import { potentiometerProps } from "@tscircuit/props";
import { formatSiUnit as formatSiUnit8 } from "format-si-unit";
function getPotentiometerSymbolName(variant) {
  switch (variant) {
    case "three_pin":
      return "potentiometer3";
    case "two_pin":
      return "potentiometer2";
    default:
      return "potentiometer2";
  }
}
var Potentiometer = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "Potentiometer",
      schematicSymbolName: this.props.symbolName ?? getPotentiometerSymbolName(this.props.pinVariant),
      zodProps: potentiometerProps,
      shouldRenderAsSchematicBox: false
    };
  }
  _getSchematicSymbolDisplayValue() {
    return `${formatSiUnit8(this._parsedProps.maxResistance)}\u03A9`;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const pinVariant = props.pinVariant || "two_pin";
    const source_component = db.source_component.insert({
      ftype: "simple_potentiometer",
      name: this.name,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      max_resistance: props.maxResistance,
      pin_variant: pinVariant,
      are_pins_interchangeable: pinVariant === "two_pin",
      display_name: props.displayName,
      display_max_resistance: this._getSchematicSymbolDisplayValue()
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/normal-components/Crystal.ts
import { crystalProps } from "@tscircuit/props";
import { formatSiUnit as formatSiUnit9 } from "format-si-unit";
var DEFAULT_CRYSTAL_MAX_TRACE_LENGTH_MM = 10;
var Crystal = class extends NormalComponent3 {
  get config() {
    const symbolName = this.props.symbolName ?? (this.props.pinVariant === "four_pin" ? "crystal_4pin" : "crystal");
    return {
      schematicSymbolName: symbolName,
      componentName: "Crystal",
      zodProps: crystalProps,
      sourceFtype: "simple_crystal"
    };
  }
  initPorts() {
    const additionalAliases = this.props.pinVariant === "four_pin" ? {
      pin1: ["left1", "1", "X1"],
      pin2: ["top1", "2", "gnd1"],
      pin3: ["right1", "3", "X2"],
      pin4: ["bottom1", "4", "gnd2"]
    } : {
      pin1: ["pos", "left", "X1"],
      pin2: ["neg", "right", "X2"]
    };
    super.initPorts({
      additionalAliases
    });
  }
  _getSchematicSymbolDisplayValue() {
    const freqDisplay = `${formatSiUnit9(this._parsedProps.frequency)}Hz`;
    if (this._parsedProps.loadCapacitance) {
      return `${freqDisplay} / ${formatSiUnit9(
        this._parsedProps.loadCapacitance
      )}F`;
    }
    return freqDisplay;
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      name: this.name,
      ftype: "simple_crystal",
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      supplier_part_numbers: props.supplierPartNumbers,
      frequency: props.frequency,
      load_capacitance: props.loadCapacitance,
      pin_variant: props.pinVariant || "two_pin",
      are_pins_interchangeable: (props.pinVariant || "two_pin") === "two_pin",
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialSourceDesignRuleChecks() {
    super.doInitialSourceDesignRuleChecks();
    if (!this.source_component_id) return;
    const { db } = this.root;
    const maximumTraceLength = this._parsedProps.maxTraceLength ?? DEFAULT_CRYSTAL_MAX_TRACE_LENGTH_MM;
    const crystalSignalSourcePortIds = /* @__PURE__ */ new Set();
    const crystalSignalConnectivityMapKeys = /* @__PURE__ */ new Set();
    for (const signalPort of [this.portMap.X1, this.portMap.X2]) {
      if (!signalPort.source_port_id) continue;
      const sourcePort = db.source_port.get(signalPort.source_port_id);
      if (!sourcePort) continue;
      crystalSignalSourcePortIds.add(signalPort.source_port_id);
      if (sourcePort.subcircuit_connectivity_map_key) {
        crystalSignalConnectivityMapKeys.add(
          sourcePort.subcircuit_connectivity_map_key
        );
      }
    }
    for (const sourceTrace of db.source_trace.list()) {
      const isDirectlyConnectedToCrystal = sourceTrace.connected_source_port_ids.some(
        (sourcePortId) => crystalSignalSourcePortIds.has(sourcePortId)
      );
      const isOnCrystalNet = sourceTrace.subcircuit_connectivity_map_key !== void 0 && crystalSignalConnectivityMapKeys.has(
        sourceTrace.subcircuit_connectivity_map_key
      );
      if (!isDirectlyConnectedToCrystal && !isOnCrystalNet) continue;
      if (typeof sourceTrace.max_length === "number" && sourceTrace.max_length <= maximumTraceLength) {
        continue;
      }
      db.source_trace.update(sourceTrace.source_trace_id, {
        max_length: maximumTraceLength
      });
    }
  }
};

// lib/components/normal-components/OpAmp.ts
import { opampProps } from "@tscircuit/props";
var OpAmp = class extends NormalComponent3 {
  getSchematicSymbolName() {
    if (this.props.symbolName) {
      return this.props.symbolName;
    }
    const hasPowerConnections = this.props.connections?.positive_supply || this.props.connections?.negative_supply;
    if (hasPowerConnections) {
      return "opamp_with_power";
    }
    return "opamp_no_power";
  }
  get config() {
    return {
      componentName: "OpAmp",
      schematicSymbolName: this.getSchematicSymbolName(),
      zodProps: opampProps,
      sourceFtype: "simple_op_amp"
    };
  }
  initPorts() {
    const additionalAliases = {
      pin1: ["non_inverting_input"],
      pin2: ["inverting_input"]
    };
    const hasPowerSymbol = this.getSchematicSymbolName() === "opamp_with_power";
    if (hasPowerSymbol) {
      Object.assign(additionalAliases, {
        pin4: ["output"],
        pin5: ["positive_supply", "vcc", "vdd"],
        pin3: ["negative_supply", "vee", "vss", "gnd"]
      });
    } else {
      Object.assign(additionalAliases, {
        pin3: ["output"],
        pin4: ["positive_supply", "vcc", "vdd"],
        pin5: ["negative_supply", "vee", "vss", "gnd"]
      });
    }
    super.initPorts({
      pinCount: 5,
      additionalAliases
    });
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const source_component = db.source_component.insert({
      ftype: "simple_op_amp",
      name: this.name,
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
  doInitialSimulationRender() {
    const { db } = this.root;
    const invertingInputPort = this.portMap.inverting_input;
    const nonInvertingInputPort = this.portMap.non_inverting_input;
    const outputPort = this.portMap.output;
    const positiveSupplyPort = this.portMap.positive_supply;
    const negativeSupplyPort = this.portMap.negative_supply;
    if (!invertingInputPort?.source_port_id || !nonInvertingInputPort?.source_port_id || !outputPort?.source_port_id || !positiveSupplyPort?.source_port_id || !negativeSupplyPort?.source_port_id) {
      return;
    }
    db.simulation_op_amp.insert({
      type: "simulation_op_amp",
      source_component_id: this.source_component_id,
      inverting_input_source_port_id: invertingInputPort.source_port_id,
      non_inverting_input_source_port_id: nonInvertingInputPort.source_port_id,
      output_source_port_id: outputPort.source_port_id,
      positive_supply_source_port_id: positiveSupplyPort.source_port_id,
      negative_supply_source_port_id: negativeSupplyPort.source_port_id
    });
  }
  inverting_input = this.portMap.inverting_input;
  non_inverting_input = this.portMap.non_inverting_input;
  output = this.portMap.output;
  positive_supply = this.portMap.positive_supply;
  negative_supply = this.portMap.negative_supply;
};

// lib/components/normal-components/TestPoint.ts
import { testpointProps } from "@tscircuit/props";
var TESTPOINT_DEFAULTS = {
  HOLE_DIAMETER: 0.5,
  SMT_CIRCLE_DIAMETER: 1.2,
  SMT_RECT_SIZE: 2
};
var TestPoint = class extends NormalComponent3 {
  get config() {
    return {
      componentName: "TestPoint",
      schematicSymbolName: this.props.symbolName ?? "testpoint",
      zodProps: testpointProps,
      sourceFtype: FTYPE.simple_test_point
    };
  }
  _getPropsWithDefaults() {
    let {
      padShape,
      holeDiameter,
      footprintVariant,
      padDiameter,
      width,
      height
    } = this._parsedProps;
    if (!footprintVariant && holeDiameter) {
      footprintVariant = "through_hole";
    }
    footprintVariant ??= "through_hole";
    padShape ??= "circle";
    if (footprintVariant === "pad") {
      if (padShape === "circle") {
        padDiameter ??= TESTPOINT_DEFAULTS.SMT_CIRCLE_DIAMETER;
      } else if (padShape === "rect") {
        width ??= TESTPOINT_DEFAULTS.SMT_RECT_SIZE;
        height ??= width;
      }
    } else if (footprintVariant === "through_hole") {
      holeDiameter ??= TESTPOINT_DEFAULTS.HOLE_DIAMETER;
    }
    return {
      padShape,
      holeDiameter,
      footprintVariant,
      padDiameter,
      width,
      height
    };
  }
  _getImpliedFootprintString() {
    const {
      padShape,
      holeDiameter,
      footprintVariant,
      padDiameter,
      width,
      height
    } = this._getPropsWithDefaults();
    if (footprintVariant === "through_hole") {
      return `platedhole_d${holeDiameter}`;
    }
    if (footprintVariant === "pad") {
      if (padShape === "circle") {
        return `smtpad_circle_d${padDiameter}`;
      }
      if (padShape === "rect") {
        return `smtpad_rect_w${width}_h${height}`;
      }
    }
    throw new Error(
      `Footprint variant "${footprintVariant}" with pad shape "${padShape}" not implemented`
    );
  }
  doInitialSourceRender() {
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const {
      padShape,
      holeDiameter,
      footprintVariant,
      padDiameter,
      width,
      height
    } = this._getPropsWithDefaults();
    const source_component = db.source_component.insert({
      ftype: FTYPE.simple_test_point,
      name: this.name,
      supplier_part_numbers: props.supplierPartNumbers,
      manufacturer_part_number: props.manufacturerPartNumber ?? props.mfn,
      footprint_variant: footprintVariant,
      pad_shape: padShape,
      pad_diameter: padDiameter,
      hole_diameter: holeDiameter,
      width,
      height,
      are_pins_interchangeable: true,
      display_name: props.displayName
    });
    this.source_component_id = source_component.source_component_id;
  }
};

// lib/components/primitive-components/SchematicSymbol/SchematicSymbol.ts
import { schematicSymbolProps } from "@tscircuit/props";
import { symbols as symbols8 } from "schematic-symbols";

// lib/components/primitive-components/SchematicSymbol/SchematicSymbol_doInitialSchematicComponentRender.ts
import { symbols as symbols7 } from "schematic-symbols";
var SchematicSymbol_doInitialSchematicComponentRender = (schematicSymbol) => {
  if (schematicSymbol.root?.schematicDisabled) return;
  if (schematicSymbol.getCollapsedSchematicBoxAncestor()) return;
  const { db } = schematicSymbol.root;
  const symbolName = schematicSymbol._getSchematicSymbolNameOrThrow();
  const symbol = symbols7[symbolName];
  if (!symbol) {
    throw new Error(`No schematic symbol found for "${symbolName}"`);
  }
  const center = schematicSymbol._getGlobalSchematicPositionBeforeLayout();
  const schematicSheetId = schematicSymbol._resolveSchematicSheetId();
  const subcircuitId = schematicSymbol.getSubcircuit().subcircuit_id ?? void 0;
  const schematicComponent = db.schematic_component.insert({
    center,
    size: { ...symbol.size },
    is_box_with_pins: true,
    symbol_name: symbolName,
    source_component_id: schematicSymbol.source_component_id ?? void 0,
    schematic_sheet_id: schematicSheetId
  });
  schematicSymbol.schematic_component_id = schematicComponent.schematic_component_id;
  for (const symbolPort of symbol.ports) {
    const port = schematicSymbol.getPortForSymbolPort(symbolPort);
    if (!port?.source_port_id) {
      throw new Error(
        `Missing source port for schematic symbol port "${symbolPort.labels.join("/")}" on ${schematicSymbol.getString()}`
      );
    }
    const portCenter = {
      x: center.x + symbolPort.x - symbol.center.x,
      y: center.y + symbolPort.y - symbol.center.y
    };
    const schematicPort = db.schematic_port.insert({
      schematic_component_id: schematicComponent.schematic_component_id,
      center: portCenter,
      source_port_id: port.source_port_id,
      facing_direction: getRelativeDirection(
        center,
        portCenter
      ),
      distance_from_component_edge: 0.4,
      pin_number: port._parsedProps.pinNumber,
      display_pin_label: port._parsedProps.aliases?.[0],
      is_connected: false,
      schematic_sheet_id: schematicSheetId,
      subcircuit_id: subcircuitId
    });
    port.schematic_port_id = schematicPort.schematic_port_id;
  }
};

// lib/components/primitive-components/SchematicSymbol/SchematicSymbol_doInitialPortMatching.ts
var getSingleConnectionTarget = (connectionName, target) => {
  const targets = Array.isArray(target) ? target : [target];
  if (targets.length !== 1) {
    throw new Error(
      `SchematicSymbol connection "${connectionName}" must select exactly one physical port`
    );
  }
  return targets[0];
};
var SchematicSymbol_doInitialPortMatching = (schematicSymbol) => {
  const { db } = schematicSymbol.root;
  const { chipRef, connections } = schematicSymbol._parsedProps;
  if (chipRef && !connections) {
    db.source_property_ignored_warning.insert({
      source_component_id: schematicSymbol.source_component_id,
      property_name: "chipRef",
      message: `${schematicSymbol.getString()} has chipRef "${chipRef}" without connections. chipRef will be ignored.`,
      error_type: "source_property_ignored_warning",
      subcircuit_id: schematicSymbol.getSubcircuit().subcircuit_id ?? void 0
    });
    return;
  }
  if (connections && !chipRef) {
    db.source_property_ignored_warning.insert({
      source_component_id: schematicSymbol.source_component_id,
      property_name: "connections",
      message: `${schematicSymbol.getString()} has connections without chipRef. connections will be ignored.`,
      error_type: "source_property_ignored_warning",
      subcircuit_id: schematicSymbol.getSubcircuit().subcircuit_id ?? void 0
    });
    return;
  }
  if (!chipRef || !connections) return;
  const referencedChip = schematicSymbol.getSubcircuit().selectOne(chipRef);
  if (!referencedChip?.source_component_id) {
    throw new Error(
      `Could not resolve chipRef "${chipRef}" for ${schematicSymbol.getString()}`
    );
  }
  const usedConnectionNames = /* @__PURE__ */ new Set();
  for (const schematicPort of schematicSymbol.ports) {
    const symbolPort = schematicPort.schematicSymbolPortDef;
    const matchingConnections = Object.entries(connections).filter(
      ([connectionName2]) => schematicPort.isMatchingAnyOf([connectionName2])
    );
    if (matchingConnections.length === 0) {
      throw new Error(
        `${schematicSymbol.getString()} is missing an explicit connection for symbol port "${symbolPort.labels.join("/")}"`
      );
    }
    if (matchingConnections.length > 1) {
      throw new Error(
        `${schematicSymbol.getString()} has multiple connections for symbol port "${symbolPort.labels.join("/")}"`
      );
    }
    const [connectionName, connectionTarget] = matchingConnections[0];
    usedConnectionNames.add(connectionName);
    const targetSelector = getSingleConnectionTarget(
      connectionName,
      connectionTarget
    );
    const physicalPort = schematicSymbol.getSubcircuit().selectOne(targetSelector, { type: "port" });
    if (!physicalPort?.source_port_id) {
      throw new Error(
        `Could not resolve connection "${connectionName}" target "${targetSelector}" for ${schematicSymbol.getString()}`
      );
    }
    const physicalSourcePort = db.source_port.get(physicalPort.source_port_id);
    if (physicalSourcePort?.source_component_id !== referencedChip.source_component_id) {
      throw new Error(
        `Connection "${connectionName}" target "${targetSelector}" does not belong to chipRef "${chipRef}"`
      );
    }
    schematicPort.source_port_id = physicalPort.source_port_id;
    schematicPort.source_component_id = physicalPort.source_component_id;
  }
  const unusedConnectionNames = Object.keys(connections).filter(
    (connectionName) => !usedConnectionNames.has(connectionName)
  );
  if (unusedConnectionNames.length > 0) {
    throw new Error(
      `${schematicSymbol.getString()} has connections that do not match symbol ports: ${unusedConnectionNames.join(", ")}`
    );
  }
};

// lib/components/primitive-components/SchematicSymbol/SchematicSymbol.ts
var SchematicSymbol = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  isPrimitiveContainer = true;
  get config() {
    return {
      componentName: "SchematicSymbol",
      schematicSymbolName: this.props.symbolName,
      zodProps: schematicSymbolProps
    };
  }
  initPorts() {
    const symbol = symbols8[this._getSchematicSymbolNameOrThrow()];
    const shouldCreateSourcePort = !(this._parsedProps.chipRef && this._parsedProps.connections);
    for (const symbolPort of symbol.ports) {
      const pinNumber = getPinNumberFromLabels(symbolPort.labels);
      if (!pinNumber) {
        throw new Error(
          `Schematic symbol port must have a numeric pin label: ${symbolPort.labels.join("/")}`
        );
      }
      const existingPort = this.ports.find(
        (port2) => port2._parsedProps.pinNumber === pinNumber
      );
      if (existingPort) {
        existingPort.schematicSymbolPortDef = symbolPort;
        continue;
      }
      const aliases = symbolPort.labels.filter(
        (label) => label !== pinNumber.toString() && label !== `pin${pinNumber}`
      );
      const port = new Port(
        {
          pinNumber,
          aliases
        },
        { shouldCreateSourcePort }
      );
      port.schematicSymbolPortDef = symbolPort;
      this.add(port);
    }
  }
  doInitialInitializePortsFromChildren() {
    this.initPorts();
  }
  updateInitializePortsFromChildren() {
    this.initPorts();
  }
  get ports() {
    return this.children.filter((child) => child instanceof Port);
  }
  getPortForSymbolPort(symbolPort) {
    return this.ports.find((port) => port.schematicSymbolPortDef === symbolPort) ?? null;
  }
  doInitialSourceRender() {
    const sourceComponent = this.root.db.source_component.insert({
      ftype: "simple_chip",
      name: this.name,
      display_name: this._parsedProps.displayName,
      are_pins_interchangeable: false
    });
    this.source_component_id = sourceComponent.source_component_id;
  }
  doInitialPortMatching() {
    SchematicSymbol_doInitialPortMatching(this);
  }
  doInitialSchematicComponentRender() {
    SchematicSymbol_doInitialSchematicComponentRender(this);
  }
};

// lib/components/primitive-components/SchematicSection.ts
import {
  calculateCellBoundaries,
  computeBoundsFromCellContents
} from "calculate-cell-boundaries";
import { schematicSectionProps } from "@tscircuit/props";
var SchematicSection = class _SchematicSection extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  get config() {
    return {
      componentName: "SchematicSection",
      zodProps: schematicSectionProps
    };
  }
  // Pass null to compute bounds for components with no schSectionName
  _computeSectionBounds(board, sectionName, schematicSheetId) {
    const { db } = this.root;
    const members = board.getDescendants().filter((c) => c.getSchematicSectionName() === sectionName);
    if (members.length === 0) return null;
    const positions = [];
    for (const member of members) {
      const schematicComponentId = member.schematic_component_id;
      if (!schematicComponentId) continue;
      const schComp = db.schematic_component.get(schematicComponentId);
      if (!schComp) continue;
      if (schComp.schematic_sheet_id !== schematicSheetId) continue;
      const hw = schComp.size.width / 2;
      const hh = schComp.size.height / 2;
      positions.push({
        minX: schComp.center.x - hw,
        maxX: schComp.center.x + hw,
        minY: schComp.center.y - hh,
        maxY: schComp.center.y + hh
      });
    }
    if (positions.length === 0) return null;
    return computeBoundsFromCellContents(positions);
  }
  doInitialSchematicSectionRender() {
    if (this.root?.schematicDisabled) return;
    const board = this.root?._getBoard();
    if (!board) return;
    const schematicSheetId = this._resolveSchematicSheetId();
    const allSections = board.getDescendants().filter(
      (component) => component instanceof _SchematicSection && component._resolveSchematicSheetId() === schematicSheetId
    );
    if (allSections[0] !== this) return;
    const { db } = this.root;
    const PADDING = 0.5;
    const LABEL_PADDING = 0.2;
    const STROKE_WIDTH = 0.02;
    const TOL4 = 1e-3;
    const namedSectionsWithBounds = allSections.map((section) => {
      const bounds = section._computeSectionBounds(
        board,
        section._parsedProps.name,
        schematicSheetId
      );
      if (!bounds) return null;
      return {
        displayName: section._parsedProps.displayName,
        sectionTitleFontSize: section._parsedProps.sectionTitleFontSize,
        rawBounds: bounds,
        cell: {
          minX: bounds.minX - PADDING,
          maxX: bounds.maxX + PADDING,
          minY: bounds.minY - PADDING,
          maxY: bounds.maxY + PADDING
        }
      };
    }).filter((s) => s !== null);
    const unsectionedBounds = this._computeSectionBounds(
      board,
      null,
      schematicSheetId
    );
    const allSectionsWithBounds = [...namedSectionsWithBounds];
    if (unsectionedBounds)
      allSectionsWithBounds.push({
        displayName: void 0,
        sectionTitleFontSize: void 0,
        rawBounds: unsectionedBounds,
        cell: {
          minX: unsectionedBounds.minX - PADDING,
          maxX: unsectionedBounds.maxX + PADDING,
          minY: unsectionedBounds.minY - PADDING,
          maxY: unsectionedBounds.maxY + PADDING
        }
      });
    if (allSectionsWithBounds.length === 0) return;
    const allCells = allSectionsWithBounds.map((s) => s.cell);
    const outer = computeBoundsFromCellContents(allCells);
    const CELL_MARGIN = 1;
    const dividers = calculateCellBoundaries(
      allSectionsWithBounds.map((s) => ({
        minX: s.rawBounds.minX - CELL_MARGIN,
        maxX: s.rawBounds.maxX + CELL_MARGIN,
        minY: s.rawBounds.minY - CELL_MARGIN,
        maxY: s.rawBounds.maxY + CELL_MARGIN
      }))
    );
    for (const line of dividers) {
      db.schematic_line.insert({
        x1: line.start.x,
        y1: line.start.y,
        x2: line.end.x,
        y2: line.end.y,
        stroke_width: STROKE_WIDTH,
        color: "#000000",
        is_dashed: true,
        schematic_sheet_id: schematicSheetId
      });
    }
    const hDividers = dividers.filter(
      (l) => Math.abs(l.start.y - l.end.y) < TOL4
    );
    const vDividers = dividers.filter(
      (l) => Math.abs(l.start.x - l.end.x) < TOL4
    );
    for (const {
      displayName,
      sectionTitleFontSize,
      rawBounds
    } of allSectionsWithBounds) {
      if (!displayName) continue;
      const dividersAbove = hDividers.map((l) => l.start.y).filter((y) => y > rawBounds.maxY);
      const topBoundary = dividersAbove.length > 0 ? Math.min(...dividersAbove) : outer.maxY;
      const dividersToLeft = vDividers.map((l) => l.start.x).filter((x) => x < rawBounds.minX);
      const leftBoundary = dividersToLeft.length > 0 ? Math.max(...dividersToLeft) : outer.minX;
      db.schematic_text.insert({
        anchor: "top_left",
        text: displayName,
        font_size: sectionTitleFontSize ?? 0.18,
        color: "#000000",
        position: {
          x: leftBoundary + LABEL_PADDING,
          y: topBoundary - LABEL_PADDING
        },
        rotation: 0,
        schematic_sheet_id: schematicSheetId
      });
    }
  }
};

// lib/components/primitive-components/SchematicSheet.ts
import { schematicSheetProps } from "@tscircuit/props";

// lib/utils/schematic/insertSchematicElementOutsideSheetWarnings.ts
import {
  getSchematicElementBounds
} from "@tscircuit/circuit-json-util";
var SCHEMATIC_UNIT_TO_MM = 10.16 / 1.1;
var DEFAULT_SCHEMATIC_SHEET_WIDTH = 297 / SCHEMATIC_UNIT_TO_MM;
var DEFAULT_SCHEMATIC_SHEET_HEIGHT = 210 / SCHEMATIC_UNIT_TO_MM;
var SCHEMATIC_SHEET_INNER_MARGIN = 5 / SCHEMATIC_UNIT_TO_MM;
var BOUNDS_EPSILON = 1e-6;
var getSchematicElementId = (schematicElement) => {
  switch (schematicElement.type) {
    case "schematic_component":
      return schematicElement.schematic_component_id;
    case "schematic_net_label":
      return schematicElement.schematic_net_label_id;
    case "schematic_trace":
      return schematicElement.schematic_trace_id;
  }
};
var insertSchematicElementOutsideSheetWarnings = ({
  db,
  schematicSheetId,
  schematicSheetName,
  schematicSheetCenter
}) => {
  const sheetContentBounds = {
    minX: schematicSheetCenter.x - DEFAULT_SCHEMATIC_SHEET_WIDTH / 2 + SCHEMATIC_SHEET_INNER_MARGIN,
    maxX: schematicSheetCenter.x + DEFAULT_SCHEMATIC_SHEET_WIDTH / 2 - SCHEMATIC_SHEET_INNER_MARGIN,
    minY: schematicSheetCenter.y - DEFAULT_SCHEMATIC_SHEET_HEIGHT / 2 + SCHEMATIC_SHEET_INNER_MARGIN,
    maxY: schematicSheetCenter.y + DEFAULT_SCHEMATIC_SHEET_HEIGHT / 2 - SCHEMATIC_SHEET_INNER_MARGIN
  };
  const checkedElements = [
    ...db.schematic_component.list(),
    ...db.schematic_net_label.list(),
    ...db.schematic_trace.list()
  ].filter(
    (schematicElement) => schematicElement.schematic_sheet_id === schematicSheetId
  );
  for (const schematicElement of checkedElements) {
    const elementBounds = getSchematicElementBounds(schematicElement);
    if (!elementBounds) continue;
    const isOutsideSheet = elementBounds.minX < sheetContentBounds.minX - BOUNDS_EPSILON || elementBounds.maxX > sheetContentBounds.maxX + BOUNDS_EPSILON || elementBounds.minY < sheetContentBounds.minY - BOUNDS_EPSILON || elementBounds.maxY > sheetContentBounds.maxY + BOUNDS_EPSILON;
    if (!isOutsideSheet) continue;
    const schematicElementId = getSchematicElementId(schematicElement);
    db.schematic_element_outside_sheet_warning.insert({
      warning_type: "schematic_element_outside_sheet_warning",
      message: `${schematicElement.type} ${schematicElementId} extends outside the drawing area of schematic sheet "${schematicSheetName}"`,
      schematic_sheet_id: schematicSheetId,
      schematic_element_type: schematicElement.type,
      schematic_element_id: schematicElementId
    });
  }
};

// lib/components/primitive-components/SchematicSheet.ts
var SchematicSheet = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  get config() {
    return {
      componentName: "SchematicSheet",
      zodProps: schematicSheetProps
    };
  }
  doInitialSourceGroupRender() {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const schematicSheet = db.schematic_sheet.insert({
      name: props.name,
      display_name: props.displayName,
      sheet_index: props.sheetIndex,
      subcircuit_id: this.getSubcircuit().subcircuit_id ?? void 0
    });
    this.schematic_sheet_id = schematicSheet.schematic_sheet_id;
  }
  doInitialSchematicSheetRender() {
    if (this.root?.schematicDisabled) return;
    if (!this.schematic_sheet_id) return;
    const { db } = this.root;
    const schematicElements = [
      ...db.schematic_component.list(),
      ...db.schematic_port.list(),
      ...db.schematic_text.list(),
      ...db.schematic_line.list(),
      ...db.schematic_rect.list(),
      ...db.schematic_circle.list(),
      ...db.schematic_arc.list(),
      ...db.schematic_path.list()
    ].filter(
      (element) => element.schematic_sheet_id === this.schematic_sheet_id
    );
    let schematicSheetCenter = { x: 0, y: 0 };
    if (schematicElements.length > 0) {
      const bounds = getBoundsForSchematic(schematicElements);
      if (Number.isFinite(bounds.minX) && Number.isFinite(bounds.maxX) && Number.isFinite(bounds.minY) && Number.isFinite(bounds.maxY)) {
        schematicSheetCenter = {
          x: (bounds.minX + bounds.maxX) / 2,
          y: (bounds.minY + bounds.maxY) / 2
        };
        db.schematic_sheet.update(this.schematic_sheet_id, {
          center: schematicSheetCenter
        });
      }
    }
    insertSchematicElementOutsideSheetWarnings({
      db,
      schematicSheetId: this.schematic_sheet_id,
      schematicSheetName: this._parsedProps.displayName,
      schematicSheetCenter
    });
  }
};

// lib/components/primitive-components/SchematicTable.ts
import { schematicTableProps } from "@tscircuit/props";
var SchematicTable = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  schematic_table_id = null;
  get config() {
    return {
      componentName: "SchematicTable",
      zodProps: schematicTableProps
    };
  }
  doInitialSchematicPrimitiveRender() {
    if (this.root?.schematicDisabled) return;
    if (this.getCollapsedSchematicBoxAncestor()) return;
    const { db } = this.root;
    const { _parsedProps: props } = this;
    const rows = this.children.filter(
      (c) => c.componentName === "SchematicRow"
    );
    if (rows.length === 0) return;
    const grid = [];
    let maxCols = 0;
    for (const row of rows) {
      const cells = row.children.filter(
        (c) => c.componentName === "SchematicCell"
      );
      maxCols = Math.max(maxCols, cells.length);
    }
    for (let i = 0; i < rows.length; i++) {
      grid[i] = [];
    }
    for (let i = 0; i < rows.length; i++) {
      const row = rows[i];
      const cells = row.children.filter(
        (c) => c.componentName === "SchematicCell"
      );
      let k = 0;
      for (let j = 0; j < cells.length; j++) {
        while (grid[i][k]) {
          k++;
        }
        const cell = cells[j];
        const colSpan = cell._parsedProps.colSpan ?? 1;
        const rowSpan = cell._parsedProps.rowSpan ?? 1;
        for (let r = 0; r < rowSpan; r++) {
          for (let c = 0; c < colSpan; c++) {
            if (!grid[i + r]) grid[i + r] = [];
            grid[i + r][k + c] = cell;
          }
        }
        k += colSpan;
      }
    }
    maxCols = Math.max(0, ...grid.map((r) => r.length));
    const rowHeights = rows.map(
      (row, i) => row._parsedProps.height ?? 1
    );
    const colWidths = Array.from({ length: maxCols }, (_, j) => {
      let maxWidth = 0;
      for (let i = 0; i < rows.length; i++) {
        const cell = grid[i]?.[j];
        if (cell) {
          const text = cell._parsedProps.text ?? cell._parsedProps.children;
          const cellWidth = cell._parsedProps.width ?? (text?.length ?? 2) * 0.5;
          if (cellWidth > maxWidth) {
            maxWidth = cellWidth;
          }
        }
      }
      return maxWidth || 10;
    });
    const anchorPos = this._getGlobalSchematicPositionBeforeLayout();
    const table = db.schematic_table.insert({
      anchor_position: anchorPos,
      column_widths: colWidths,
      row_heights: rowHeights,
      cell_padding: props.cellPadding,
      border_width: props.borderWidth,
      anchor: props.anchor,
      subcircuit_id: this.getSubcircuit()?.subcircuit_id || "",
      schematic_component_id: this.parent?.schematic_component_id || "",
      schematic_sheet_id: this._resolveSchematicSheetId()
    });
    this.schematic_table_id = table.schematic_table_id;
    const processedCells = /* @__PURE__ */ new Set();
    let yOffset = 0;
    for (let i = 0; i < rows.length; i++) {
      let xOffset = 0;
      for (let j = 0; j < maxCols; j++) {
        const cell = grid[i]?.[j];
        if (cell && !processedCells.has(cell)) {
          processedCells.add(cell);
          const cellProps = cell._parsedProps;
          const rowSpan = cellProps.rowSpan ?? 1;
          const colSpan = cellProps.colSpan ?? 1;
          let cellWidth = 0;
          for (let c = 0; c < colSpan; c++) {
            cellWidth += colWidths[j + c];
          }
          let cellHeight = 0;
          for (let r = 0; r < rowSpan; r++) {
            cellHeight += rowHeights[i + r];
          }
          db.schematic_table_cell.insert({
            schematic_table_id: this.schematic_table_id,
            start_row_index: i,
            end_row_index: i + rowSpan - 1,
            start_column_index: j,
            end_column_index: j + colSpan - 1,
            text: cellProps.text ?? cellProps.children,
            center: {
              x: anchorPos.x + xOffset + cellWidth / 2,
              y: anchorPos.y - yOffset - cellHeight / 2
            },
            width: cellWidth,
            height: cellHeight,
            horizontal_align: cellProps.horizontalAlign,
            vertical_align: cellProps.verticalAlign,
            font_size: cellProps.fontSize ?? props.fontSize,
            subcircuit_id: this.getSubcircuit()?.subcircuit_id || ""
          });
        }
        if (colWidths[j]) {
          xOffset += colWidths[j];
        }
      }
      yOffset += rowHeights[i];
    }
  }
};

// lib/components/primitive-components/SchematicRow.ts
import { schematicRowProps } from "@tscircuit/props";
var SchematicRow = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  get config() {
    return {
      componentName: "SchematicRow",
      zodProps: schematicRowProps
    };
  }
};

// lib/components/primitive-components/SchematicCell.ts
import { schematicCellProps } from "@tscircuit/props";
var SchematicCell = class extends PrimitiveComponent2 {
  isSchematicPrimitive = true;
  canHaveTextChildren = true;
  get config() {
    return {
      componentName: "SchematicCell",
      zodProps: schematicCellProps
    };
  }
};

// lib/components/primitive-components/AnalogSimulation.ts
import { analogSimulationProps } from "@tscircuit/props";
var AnalogSimulation = class extends PrimitiveComponent2 {
  simulation_experiment_id = null;
  get config() {
    return {
      componentName: "AnalogSimulation",
      zodProps: analogSimulationProps
    };
  }
  getSpiceEngineName() {
    return this._parsedProps.spiceEngine;
  }
  usesIndependentGraphAxes() {
    return this._parsedProps.graphIndependentAxes === true;
  }
  doInitialSimulationRender() {
    const { db } = this.root;
    const { duration, name, startTime, timePerStep, spiceOptions } = this._parsedProps;
    const durationMs = duration || 10;
    const timePerStepMs = timePerStep || 0.01;
    const simulationExperiment = db.simulation_experiment.insert({
      name: name ?? "spice_transient_analysis",
      experiment_type: "spice_transient_analysis",
      end_time_ms: durationMs,
      start_time_ms: startTime,
      time_per_step: timePerStepMs,
      spice_options: spiceOptions
    });
    this.simulation_experiment_id = simulationExperiment.simulation_experiment_id;
  }
};

// lib/components/primitive-components/AnalogAnalysisSimulation.ts
var AnalogAnalysisSimulation = class extends PrimitiveComponent2 {
  simulation_experiment_id = null;
  getSpiceEngineName() {
    return typeof this.props.spiceEngine === "string" ? this.props.spiceEngine : void 0;
  }
  usesIndependentGraphAxes() {
    return this.props.graphIndependentAxes === true;
  }
  getOrCreateSimulationExperiment() {
    if (this.simulation_experiment_id) {
      return this.root?.db.simulation_experiment.get(
        this.simulation_experiment_id
      ) ?? null;
    }
    const sweepParameterCount = this.children.filter(
      (child) => child.componentName === "AnalogSweepParameter"
    ).length;
    if (sweepParameterCount > 1) {
      this.renderError(
        "An analog simulation can contain at most one sweep parameter."
      );
      return null;
    }
    const simulationExperiment = this.insertSimulationExperiment();
    this.simulation_experiment_id = simulationExperiment?.simulation_experiment_id ?? null;
    return simulationExperiment;
  }
  doInitialSimulationRender() {
    this.getOrCreateSimulationExperiment();
  }
};

// lib/components/primitive-components/AnalogTransientSimulation.ts
import { analogTransientSimulationProps } from "@tscircuit/props";
var AnalogTransientSimulation = class extends AnalogAnalysisSimulation {
  get config() {
    return {
      componentName: "AnalogTransientSimulation",
      zodProps: analogTransientSimulationProps
    };
  }
  insertSimulationExperiment() {
    const { db } = this.root;
    const { duration, name, startTime, timePerStep, spiceOptions } = this._parsedProps;
    return db.simulation_experiment.insert({
      name: name ?? "spice_transient_analysis",
      experiment_type: "spice_transient_analysis",
      end_time_ms: duration,
      start_time_ms: startTime,
      time_per_step: timePerStep,
      spice_options: spiceOptions
    });
  }
};

// lib/components/primitive-components/AnalogDcOperatingPointSimulation.ts
import { analogDcOperatingPointSimulationProps } from "@tscircuit/props";
var AnalogDcOperatingPointSimulation = class extends AnalogAnalysisSimulation {
  get config() {
    return {
      componentName: "AnalogDcOperatingPointSimulation",
      zodProps: analogDcOperatingPointSimulationProps
    };
  }
  insertSimulationExperiment() {
    const { name, spiceOptions } = this._parsedProps;
    return this.root.db.simulation_experiment.insert({
      name: name ?? "spice_dc_operating_point",
      experiment_type: "spice_dc_operating_point",
      spice_options: spiceOptions
    });
  }
};

// lib/components/primitive-components/AnalogDcSweepSimulation.ts
import { analogDcSweepSimulationProps } from "@tscircuit/props";
var AnalogDcSweepSimulation = class extends AnalogAnalysisSimulation {
  get config() {
    return {
      componentName: "AnalogDcSweepSimulation",
      zodProps: analogDcSweepSimulationProps
    };
  }
  insertSimulationExperiment() {
    const {
      name,
      spiceOptions,
      sweepSource,
      sweepStart,
      sweepStop,
      sweepStep
    } = this._parsedProps;
    const simulationScope = this.getGroup() ?? this.getSubcircuit();
    const sweepSourceComponent = simulationScope?.selectOne(sweepSource);
    let dcSweepSourceFields;
    if (sweepSourceComponent instanceof VoltageSource) {
      sweepSourceComponent.runRenderPhase("SimulationRender");
      const simulationVoltageSourceId = sweepSourceComponent.simulation_voltage_source_id;
      if (!simulationVoltageSourceId) {
        this.renderError(
          `DC sweep source "${sweepSource}" must resolve to one voltage or current source.`
        );
        return null;
      }
      dcSweepSourceFields = {
        dc_sweep_voltage_source_id: simulationVoltageSourceId,
        dc_sweep_unit: "V"
      };
    } else if (sweepSourceComponent instanceof CurrentSource) {
      sweepSourceComponent.runRenderPhase("SimulationRender");
      const simulationCurrentSourceId = sweepSourceComponent.simulation_current_source_id;
      if (!simulationCurrentSourceId) {
        this.renderError(
          `DC sweep source "${sweepSource}" must resolve to one voltage or current source.`
        );
        return null;
      }
      dcSweepSourceFields = {
        dc_sweep_current_source_id: simulationCurrentSourceId,
        dc_sweep_unit: "A"
      };
    } else {
      this.renderError(
        `DC sweep source "${sweepSource}" must resolve to one voltage or current source.`
      );
      return null;
    }
    return this.root.db.simulation_experiment.insert({
      name: name ?? "spice_dc_sweep",
      experiment_type: "spice_dc_sweep",
      spice_options: spiceOptions,
      ...dcSweepSourceFields,
      dc_sweep_start: sweepStart,
      dc_sweep_stop: sweepStop,
      dc_sweep_step: sweepStep
    });
  }
};

// lib/components/primitive-components/AnalogAcSweepSimulation.ts
import { analogAcSweepSimulationProps } from "@tscircuit/props";
var AnalogAcSweepSimulation = class extends AnalogAnalysisSimulation {
  get config() {
    return {
      componentName: "AnalogAcSweepSimulation",
      zodProps: analogAcSweepSimulationProps
    };
  }
  insertSimulationExperiment() {
    const {
      name,
      spiceOptions,
      sweepType,
      samplesPerInterval,
      sampleCount,
      startFrequency,
      stopFrequency
    } = this._parsedProps;
    return this.root.db.simulation_experiment.insert({
      name: name ?? "spice_ac_analysis",
      experiment_type: "spice_ac_analysis",
      spice_options: spiceOptions,
      ac_sweep_type: sweepType,
      ac_samples_per_interval: samplesPerInterval,
      ac_sample_count: sampleCount,
      ac_start_frequency_hz: startFrequency,
      ac_stop_frequency_hz: stopFrequency
    });
  }
};

// lib/components/primitive-components/AnalogSweepParameter.ts
import { analogSweepParameterProps } from "@tscircuit/props";

// lib/components/primitive-components/AnalogSweepParameter_doInitialSimulationRender.ts
var parameterUnits = {
  resistance: "\u03A9",
  capacitance: "F",
  inductance: "H",
  voltage: "V",
  current: "A"
};
var isParameterSweepCoordinateInRange = ({
  parameterSweepCoordinate,
  stop,
  step
}) => {
  const tolerance = Math.abs(step) * 1e-9;
  return step > 0 ? parameterSweepCoordinate <= stop + tolerance : parameterSweepCoordinate >= stop - tolerance;
};
var getGeneratedParameterSweepCoordinates = ({
  start,
  stop,
  step
}) => {
  const parameterSweepCoordinates = [];
  for (let parameterSweepCoordinate = start; isParameterSweepCoordinateInRange({
    parameterSweepCoordinate,
    stop,
    step
  }); parameterSweepCoordinate = start + parameterSweepCoordinates.length * step) {
    parameterSweepCoordinates.push(parameterSweepCoordinate);
    if (parameterSweepCoordinates.length > 1e6) {
      throw new Error("Parameter sweep exceeds 1,000,000 points");
    }
  }
  return parameterSweepCoordinates;
};
var getParameterSweepTargetSelector = (props) => {
  if (props.parameterType === "resistance") return props.resistorRef;
  if (props.parameterType === "capacitance") return props.capacitorRef;
  if (props.parameterType === "inductance") return props.inductorRef;
  if (props.parameterType === "voltage") return props.net;
  return props.currentSourceRef;
};
var AnalogSweepParameter_doInitialSimulationRender = (analogSweepParameter) => {
  if (!(analogSweepParameter.parent instanceof AnalogAnalysisSimulation)) {
    analogSweepParameter.renderError(
      "analog.sweepparameter must be nested directly in an analog simulation."
    );
    return;
  }
  const simulationExperiment = analogSweepParameter.parent.getOrCreateSimulationExperiment();
  if (!simulationExperiment) return;
  const props = analogSweepParameter._parsedProps;
  const parameterSweepCoordinates = props.values ?? getGeneratedParameterSweepCoordinates({
    start: props.start,
    stop: props.stop,
    step: props.step
  });
  const simulationScope = analogSweepParameter.parent.getGroup() ?? analogSweepParameter.parent.getSubcircuit();
  const targetSelector = getParameterSweepTargetSelector(props);
  const parameterUnit = parameterUnits[props.parameterType];
  const simulationParameterSweepTable = analogSweepParameter.root.db.simulation_parameter_sweep;
  const simulationParameterSweepFields = {
    simulation_experiment_id: simulationExperiment.simulation_experiment_id,
    name: props.name,
    parameter_type: props.parameterType,
    parameter_values: parameterSweepCoordinates,
    parameter_unit: parameterUnit
  };
  let simulationParameterSweepId;
  if (props.parameterType === "voltage") {
    const sweepNet = simulationScope?.selectOne(targetSelector, {
      type: "net"
    });
    if (!sweepNet?.source_net_id) {
      analogSweepParameter.renderError(
        `Voltage sweep net "${targetSelector}" could not be resolved.`
      );
      return;
    }
    simulationParameterSweepId = simulationParameterSweepTable.insert({
      ...simulationParameterSweepFields,
      parameter_type: "voltage",
      source_net_id: sweepNet.source_net_id
    }).simulation_parameter_sweep_id;
  } else {
    const sweepTarget = simulationScope?.selectOne(targetSelector);
    const sourceComponentId = sweepTarget?.source_component_id;
    if (!sourceComponentId) {
      analogSweepParameter.renderError(
        `Parameter sweep target "${targetSelector}" could not be resolved.`
      );
      return;
    }
    if (props.parameterType === "resistance") {
      simulationParameterSweepId = simulationParameterSweepTable.insert({
        ...simulationParameterSweepFields,
        parameter_type: "resistance",
        resistor_source_component_id: sourceComponentId
      }).simulation_parameter_sweep_id;
    } else if (props.parameterType === "capacitance") {
      simulationParameterSweepId = simulationParameterSweepTable.insert({
        ...simulationParameterSweepFields,
        parameter_type: "capacitance",
        capacitor_source_component_id: sourceComponentId
      }).simulation_parameter_sweep_id;
    } else if (props.parameterType === "inductance") {
      simulationParameterSweepId = simulationParameterSweepTable.insert({
        ...simulationParameterSweepFields,
        parameter_type: "inductance",
        inductor_source_component_id: sourceComponentId
      }).simulation_parameter_sweep_id;
    } else {
      simulationParameterSweepId = simulationParameterSweepTable.insert({
        ...simulationParameterSweepFields,
        parameter_type: "current",
        current_source_component_id: sourceComponentId
      }).simulation_parameter_sweep_id;
    }
  }
  analogSweepParameter.simulation_parameter_sweep_id = simulationParameterSweepId;
};

// lib/components/primitive-components/AnalogSweepParameter.ts
var AnalogSweepParameter = class extends PrimitiveComponent2 {
  simulation_parameter_sweep_id = null;
  get config() {
    return {
      componentName: "AnalogSweepParameter",
      zodProps: analogSweepParameterProps
    };
  }
  doInitialSimulationRender() {
    AnalogSweepParameter_doInitialSimulationRender(this);
  }
};

// lib/components/primitive-components/VoltageProbe.ts
import { voltageProbeProps } from "@tscircuit/props";

// lib/utils/schematic/selectBestLabelAlignment.ts
import { doBoundsOverlap as doBoundsOverlap3 } from "@tscircuit/math-utils";
function getLabelBounds(probePosition, labelText, alignment, labelOffset = 0.3) {
  const charWidth = 0.1;
  const labelWidth = Math.max(labelText.length * charWidth, 0.3);
  const labelHeight = 0.25;
  const labelHeightWithPadding = labelHeight + 0.2;
  let anchorX = probePosition.x;
  let anchorY = probePosition.y;
  const offsetMultiplier = labelOffset + labelWidth / 2;
  if (alignment.includes("top")) {
    anchorY += offsetMultiplier;
  } else if (alignment.includes("bottom")) {
    anchorY -= offsetMultiplier;
  }
  if (alignment.includes("right")) {
    anchorX += offsetMultiplier;
  } else if (alignment.includes("left")) {
    anchorX -= offsetMultiplier;
  }
  let minX, maxX, minY, maxY;
  if (alignment.includes("left")) {
    minX = anchorX;
    maxX = anchorX + labelWidth;
  } else if (alignment.includes("right")) {
    minX = anchorX - labelWidth;
    maxX = anchorX;
  } else {
    minX = anchorX - labelWidth / 2;
    maxX = anchorX + labelWidth / 2;
  }
  if (alignment.includes("top")) {
    minY = anchorY - labelHeightWithPadding;
    maxY = anchorY;
  } else if (alignment.includes("bottom")) {
    minY = anchorY;
    maxY = anchorY + labelHeightWithPadding;
  } else {
    minY = anchorY - labelHeightWithPadding / 2;
    maxY = anchorY + labelHeightWithPadding / 2;
  }
  return { minX, maxX, minY, maxY };
}
function getElementBounds(elm) {
  let cx;
  let cy;
  let w;
  let h;
  if (elm.type === "schematic_component") {
    cx = elm.center?.x;
    cy = elm.center?.y;
    w = elm.size?.width;
    h = elm.size?.height;
  } else if (elm.type === "schematic_text") {
    cx = elm.position?.x;
    cy = elm.position?.y;
    w = (elm.text?.length ?? 0) * 0.1;
    h = 0.2;
  } else {
    return null;
  }
  if (typeof cx === "number" && typeof cy === "number" && typeof w === "number" && typeof h === "number") {
    return {
      minX: cx - w / 2,
      maxX: cx + w / 2,
      minY: cy - h / 2,
      maxY: cy + h / 2
    };
  }
  return null;
}
function getOverlapArea(a, b) {
  if (!doBoundsOverlap3(a, b)) return 0;
  const overlapWidth = Math.min(a.maxX, b.maxX) - Math.max(a.minX, b.minX);
  const overlapHeight = Math.min(a.maxY, b.maxY) - Math.max(a.minY, b.minY);
  return overlapWidth * overlapHeight;
}
function selectBestLabelAlignment({
  probePosition,
  labelText,
  schematicElements,
  defaultAlignment = "top_right"
}) {
  const alignmentOptions = [
    "top_right",
    "top_left",
    "bottom_right",
    "bottom_left",
    "top_center",
    "bottom_center",
    "center_right",
    "center_left"
  ];
  const orderedAlignments = [
    defaultAlignment,
    ...alignmentOptions.filter((a) => a !== defaultAlignment)
  ];
  let bestAlignment = defaultAlignment;
  let minOverlapArea = Infinity;
  for (const alignment of orderedAlignments) {
    const labelBounds = getLabelBounds(probePosition, labelText, alignment);
    let totalOverlapArea = 0;
    for (const element of schematicElements) {
      const elementBounds = getElementBounds(element);
      if (elementBounds) {
        totalOverlapArea += getOverlapArea(labelBounds, elementBounds);
      }
    }
    if (totalOverlapArea === 0) {
      return alignment;
    }
    if (totalOverlapArea < minOverlapArea) {
      minOverlapArea = totalOverlapArea;
      bestAlignment = alignment;
    }
  }
  return bestAlignment;
}

// lib/components/primitive-components/VoltageProbe.ts
import "zod";
var VoltageProbe = class extends PrimitiveComponent2 {
  simulation_voltage_probe_id = null;
  schematic_voltage_probe_id = null;
  finalProbeName = null;
  color = null;
  get config() {
    return {
      componentName: "VoltageProbe",
      zodProps: voltageProbeProps
    };
  }
  doInitialSimulationRender() {
    const { db } = this.root;
    const {
      connectsTo,
      name,
      referenceTo,
      color,
      graphDisplayName,
      graphCenter,
      graphVerticalOffset,
      graphVoltagePerDiv
    } = this._parsedProps;
    const subcircuit = this.getSubcircuit();
    if (!subcircuit) {
      this.renderError("VoltageProbe must be inside a subcircuit");
      return;
    }
    const targets = Array.isArray(connectsTo) ? connectsTo : [connectsTo];
    if (targets.length !== 1) {
      this.renderError("VoltageProbe must connect to exactly one port or net");
      return;
    }
    const targetSelector = targets[0];
    const port = subcircuit.selectOne(targetSelector, {
      type: "port"
    });
    const net = !port ? subcircuit.selectOne(targetSelector, { type: "net" }) : null;
    if (net && net.componentName !== "Net") {
      this.renderError(
        `VoltageProbe connection target "${targetSelector}" resolved to a non-net component "${net.componentName}".`
      );
      return;
    }
    if (!port && !net) {
      this.renderError(
        `VoltageProbe could not find connection target "${targetSelector}"`
      );
      return;
    }
    const connectedId = port?.source_port_id ?? net?.source_net_id;
    if (!connectedId) {
      this.renderError("Could not identify connected source for VoltageProbe");
      return;
    }
    let referencePort = null;
    let referenceNet = null;
    if (referenceTo) {
      const referenceTargets = Array.isArray(referenceTo) ? referenceTo : [referenceTo];
      if (referenceTargets.length !== 1) {
        this.renderError("VoltageProbe must reference exactly one port or net");
        return;
      }
      const referenceSelector = referenceTargets[0];
      referencePort = subcircuit.selectOne(referenceSelector, {
        type: "port"
      });
      referenceNet = !referencePort ? subcircuit.selectOne(referenceSelector, {
        type: "net"
      }) : null;
      if (referenceNet && referenceNet.componentName !== "Net") {
        this.renderError(
          `VoltageProbe reference target "${referenceSelector}" resolved to a non-net component "${referenceNet.componentName}".`
        );
        return;
      }
      if (!referencePort && !referenceNet) {
        this.renderError(
          `VoltageProbe could not find reference target "${referenceSelector}"`
        );
        return;
      }
    }
    this.color = color ?? getSimulationColorForId(connectedId);
    let finalName = name;
    if (!finalName) {
      finalName = targets[0].split(" > ").map((s) => s.replace(/^\./, "")).join(".");
    }
    this.finalProbeName = finalName ?? null;
    const { simulation_voltage_probe_id } = db.simulation_voltage_probe.insert({
      name: finalName,
      signal_input_source_port_id: port?.source_port_id ?? void 0,
      signal_input_source_net_id: net?.source_net_id ?? void 0,
      reference_input_source_port_id: referencePort?.source_port_id ?? void 0,
      reference_input_source_net_id: referenceNet?.source_net_id ?? void 0,
      subcircuit_id: subcircuit.subcircuit_id || void 0,
      color: this.color
    });
    this.simulation_voltage_probe_id = simulation_voltage_probe_id;
    const hasGraphDisplayProps = graphDisplayName !== void 0 || graphCenter !== void 0 || graphVerticalOffset !== void 0 || graphVoltagePerDiv !== void 0;
    if (hasGraphDisplayProps) {
      const graphVoltagePerDivValue = parseSimulationGraphValue(graphVoltagePerDiv);
      const graphVerticalOffsetValue = parseSimulationGraphValue(graphVerticalOffset);
      db.simulation_oscilloscope_trace.insert({
        simulation_voltage_probe_id,
        display_name: graphDisplayName,
        color: color ?? void 0,
        display_center_value: graphCenter,
        volts_per_div: graphVoltagePerDivValue,
        display_center_offset_divs: graphVerticalOffsetValue !== void 0 && graphVoltagePerDivValue !== void 0 && graphVoltagePerDivValue !== 0 ? graphVerticalOffsetValue / graphVoltagePerDivValue : void 0
      });
    }
  }
  doInitialSchematicReplaceNetLabelsWithSymbols() {
    if (this.root?.schematicDisabled) return;
    const { db } = this.root;
    const { connectsTo, name } = this._parsedProps;
    const subcircuit = this.getSubcircuit();
    if (!subcircuit) {
      return;
    }
    const targets = Array.isArray(connectsTo) ? connectsTo : [connectsTo];
    if (targets.length !== 1) {
      return;
    }
    const targetSelector = targets[0];
    const port = subcircuit.selectOne(targetSelector, {
      type: "port"
    });
    if (!port) return;
    if (!port.schematic_port_id) return;
    const position = port._getGlobalSchematicPositionAfterLayout();
    let targetTraceId = null;
    for (const trace of db.schematic_trace.list()) {
      for (const edge of trace.edges) {
        if (Math.abs(edge.from.x - position.x) < 1e-6 && Math.abs(edge.from.y - position.y) < 1e-6 || Math.abs(edge.to.x - position.x) < 1e-6 && Math.abs(edge.to.y - position.y) < 1e-6) {
          targetTraceId = trace.schematic_trace_id;
          break;
        }
      }
      if (targetTraceId) break;
    }
    if (!targetTraceId) {
      return;
    }
    const probeName = this.finalProbeName;
    const labelAlignment = selectBestLabelAlignment({
      probePosition: position,
      labelText: probeName,
      schematicElements: [
        ...db.schematic_component.list(),
        ...db.schematic_text.list()
      ],
      defaultAlignment: "top_right"
    });
    const schematic_voltage_probe = db.schematic_voltage_probe.insert({
      name: probeName,
      position,
      schematic_trace_id: targetTraceId,
      subcircuit_id: subcircuit.subcircuit_id || void 0,
      color: this.color ?? void 0,
      label_alignment: labelAlignment
    });
    this.schematic_voltage_probe_id = schematic_voltage_probe.schematic_voltage_probe_id;
  }
};

// lib/components/primitive-components/SpiceModel.ts
import { spicemodelProps } from "@tscircuit/props";
import { parseSpiceNetlist } from "spicets";
var SpiceModel = class extends PrimitiveComponent2 {
  get config() {
    return {
      componentName: "SpiceModel",
      zodProps: spicemodelProps
    };
  }
  doInitialSimulationRender() {
    const parent = this.parent;
    if (!parent) return;
    const parentSourceComponentId = parent.source_component_id;
    if (!parentSourceComponentId) {
      this._insertSpiceModelError(
        "spicemodel must be attached to a source component.",
        this._parsedProps
      );
      return;
    }
    const { source, spicePinMapping } = this._parsedProps;
    const subckt = parseSpiceNetlist(source).subckts[0] ?? null;
    const spicePinNames = subckt?.pins.map((pin) => pin.name);
    if (!subckt || !spicePinNames || spicePinNames.length === 0) {
      this._insertSpiceModelError(
        "spicemodel source must contain a .subckt declaration with a model name and pins.",
        source
      );
      return;
    }
    const mapping = spicePinMapping ?? {};
    for (const spicePinName of Object.keys(mapping)) {
      if (!spicePinNames.includes(spicePinName)) {
        this._insertSpiceModelError(
          `spicePinMapping references SPICE pin "${spicePinName}", but it is not present in .subckt ${subckt.name}.`,
          mapping
        );
        return;
      }
    }
    const spicePinToSourcePortMap = {};
    const seenSourcePortIds = /* @__PURE__ */ new Set();
    for (const spicePinName of spicePinNames) {
      const componentPinName = mapping[spicePinName] ?? spicePinName;
      const matchingPorts = parent.selectAll("port").filter(
        (port) => port instanceof Port && port.isMatchingAnyOf([componentPinName])
      );
      if (matchingPorts.length !== 1 || !matchingPorts[0].source_port_id) {
        this._insertSpiceModelError(
          `Could not resolve SPICE pin "${spicePinName}" to exactly one component port using "${componentPinName}".`,
          mapping
        );
        return;
      }
      const sourcePortId = matchingPorts[0].source_port_id;
      if (seenSourcePortIds.has(sourcePortId)) {
        this._insertSpiceModelError(
          `spicePinMapping maps more than one SPICE pin to component port "${componentPinName}".`,
          mapping
        );
        return;
      }
      seenSourcePortIds.add(sourcePortId);
      spicePinToSourcePortMap[spicePinName] = sourcePortId;
    }
    this.root.db.simulation_spice_subcircuit.insert({
      source_component_id: parentSourceComponentId,
      spice_pin_to_source_port_map: spicePinToSourcePortMap,
      subcircuit_source: source
    });
  }
  _insertSpiceModelError(message, propertyValue) {
    this.root.db.source_invalid_component_property_error.insert({
      source_component_id: this.parent?.source_component_id || "",
      property_name: "spiceModel",
      property_value: propertyValue,
      message,
      error_type: "source_invalid_component_property_error"
    });
  }
};

// lib/namespaced-elements/create-namespaced-element.ts
import { createElement } from "react";
var createNamespacedElement = (hostType) => {
  const Component2 = (props) => createElement(hostType, props);
  Component2.displayName = hostType;
  return Component2;
};

// lib/namespaced-elements/analog.ts
var analog = {
  transientsimulation: createNamespacedElement(
    "analogtransientsimulation"
  ),
  dcoperatingpointsimulation: createNamespacedElement(
    "analogdcoperatingpointsimulation"
  ),
  dcsweepsimulation: createNamespacedElement(
    "analogdcsweepsimulation"
  ),
  acsweepsimulation: createNamespacedElement(
    "analogacsweepsimulation"
  ),
  sweepparameter: createNamespacedElement(
    "analogsweepparameter"
  )
};

// lib/namespaced-elements/enclosure.ts
var EnclosureFdmBoxElement = createNamespacedElement("enclosure.fdm.box");
var enclosure = {
  cutoutaperture: createNamespacedElement(
    "enclosure.cutoutaperture"
  ),
  fdm: {
    box: EnclosureFdmBoxElement,
    Box: EnclosureFdmBoxElement
  }
};

// lib/RootCircuit.ts
var RootCircuit = class extends IsolatedCircuit {
  isRootCircuit = true;
  constructor({
    platform,
    projectUrl
  } = {}) {
    super({
      platform,
      projectUrl,
      cachedSubcircuits: /* @__PURE__ */ new Map(),
      pendingSubcircuitRenders: /* @__PURE__ */ new Map()
    });
    this.root = this;
  }
};
var Project = RootCircuit;
var Circuit = RootCircuit;

// lib/hooks/use-rendered-circuit.ts
import React2 from "react";
var useRenderedCircuit = (reactElements) => {
  const [isLoading, setIsLoading] = React2.useState(true);
  const [error, setError] = React2.useState(null);
  const [circuit, setCircuit] = React2.useState();
  const [circuitJson, setCircuitJson] = React2.useState();
  React2.useEffect(() => {
    setIsLoading(true);
    setError(null);
    if (reactElements) {
      setTimeout(() => {
        try {
          const circuit2 = new RootCircuit();
          circuit2.add(reactElements);
          setCircuit(circuit2);
          setCircuitJson(circuit2.toJson());
        } catch (error2) {
          setError(error2);
        }
        setIsLoading(false);
      }, 1);
    }
  }, [reactElements]);
  return { isLoading, error, circuit, circuitJson };
};

// lib/hooks/create-use-component.tsx
import "react";
import "zod";
import "@tscircuit/props";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
var createUseComponent = (Component2, pins) => {
  return (name, props) => {
    const pinLabelsFlatArray = [];
    if (Array.isArray(pins)) {
      pinLabelsFlatArray.push(...pins.flat());
    } else if (typeof pins === "object") {
      pinLabelsFlatArray.push(
        ...Object.values(pins).flat(),
        ...Object.keys(pins)
      );
    }
    const R = (props2) => {
      if (props2?.name && props2.name !== name) {
        throw new Error(
          `Component name mismatch. Hook name: ${name}, Component prop name: ${props2.name}`
        );
      }
      const combinedProps = { ...props, ...props2, name };
      const tracesToCreate = [];
      for (const portLabel of pinLabelsFlatArray) {
        if (combinedProps[portLabel]) {
          const from = `.${name} > .${portLabel}`;
          const to = combinedProps[portLabel];
          tracesToCreate.push({ from, to });
          delete combinedProps[portLabel];
        }
      }
      return /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx(Component2, { ...combinedProps }),
        tracesToCreate.map((trace, i) => (
          // biome-ignore lint/suspicious/noArrayIndexKey: <explanation>
          /* @__PURE__ */ jsx("trace", { ...trace }, i)
        ))
      ] });
    };
    for (const port of pinLabelsFlatArray) {
      R[port] = `.${name} > .${port}`;
    }
    return R;
  };
};

// lib/hooks/use-capacitor.tsx
import { capacitorPins } from "@tscircuit/props";
import { jsx as jsx2 } from "react/jsx-runtime";
var useCapacitor = createUseComponent(
  (props) => /* @__PURE__ */ jsx2("capacitor", { ...props }),
  capacitorPins
);

// lib/hooks/use-chip.tsx
import { jsx as jsx3 } from "react/jsx-runtime";
var useChip = (pinLabels) => createUseComponent(
  (props) => /* @__PURE__ */ jsx3("chip", { pinLabels, ...props }),
  pinLabels
);

// lib/hooks/use-diode.tsx
import { diodePins } from "@tscircuit/props";
import { jsx as jsx4 } from "react/jsx-runtime";
var useDiode = createUseComponent(
  (props) => /* @__PURE__ */ jsx4("diode", { ...props }),
  diodePins
);

// lib/hooks/use-led.tsx
import { ledPins } from "@tscircuit/props";
import { jsx as jsx5 } from "react/jsx-runtime";
var useLed = createUseComponent(
  (props) => /* @__PURE__ */ jsx5("led", { ...props }),
  ledPins
);

// lib/hooks/use-resistor.tsx
import { resistorPins as resistorPins2 } from "@tscircuit/props";
import { jsx as jsx6 } from "react/jsx-runtime";
var useResistor = createUseComponent(
  (props) => /* @__PURE__ */ jsx6("resistor", { ...props }),
  resistorPins2
);

// lib/sel/sel.ts
var sel = new Proxy(
  (refdes) => new Proxy(
    {},
    {
      get: (_, pin) => `.${refdes} > .${pin}`
    }
  ),
  {
    get: (_, prop1) => {
      const fn = (...args) => {
        const chipFnOrPinType = args[0];
        return new Proxy(
          {},
          {
            get: (_2, pinName) => {
              return `.${prop1} > .${pinName}`;
            }
          }
        );
      };
      return new Proxy(fn, {
        // This handles dot notation access like sel.U1.PIN
        get: (_2, prop2) => {
          if (prop1 === "net") {
            return `net.${prop2}`;
          }
          if (prop1 === "subcircuit") {
            return new Proxy(
              {},
              {
                get: (_3, prop3) => {
                  return new Proxy(
                    {},
                    {
                      get: (_4, prop4) => {
                        return `subcircuit.${prop2} > .${prop3} > .${prop4}`;
                      }
                    }
                  );
                }
              }
            );
          }
          return `.${prop1} > .${prop2}`;
        },
        // This handles function calls like...
        // - sel.U1(MyChip)
        // - sel.U1(({ selectors: { U1: { GND: "GND", VCC: "VCC" } } }) => ...)
        // - sel.U1(({ connections: { GND: "GND", VCC: "VCC" } }) => ...)
        apply: (target, _2, args) => {
          if (prop1 === "net") {
            return new Proxy(
              {},
              {
                get: (_3, netName) => `net.${netName}`
              }
            );
          }
          return new Proxy(
            {},
            {
              get: (_3, pinOrSubComponentName) => {
                const pinResult = `.${prop1} > .${pinOrSubComponentName}`;
                const chipPrefixes = ["U", "J", "CN"];
                if (chipPrefixes.some((p) => prop1.startsWith(p))) {
                  return pinResult;
                }
                return new Proxy(new String(pinResult), {
                  get: (_4, nestedProp) => {
                    if (typeof nestedProp === "symbol" || nestedProp === "toString") {
                      return () => pinResult;
                    }
                    return `.${prop1} > .${pinOrSubComponentName} > .${nestedProp}`;
                  }
                });
              }
            }
          );
        }
      });
    }
  }
);

// lib/index.ts
import { createElement as createElement2 } from "react";
import { kicadFootprintStrings } from "@tscircuit/props";

// lib/register-catalogue.ts
extendCatalogue(components_exports);
extendCatalogue({
  Bug: Chip,
  "enclosure.cutoutaperture": EnclosureCutoutAperture,
  "enclosure.fdm.box": EnclosureFdmBox,
  Fanout: Breakout,
  FanoutPoint: BreakoutPoint
});
export {
  Ammeter,
  AnalogAcSweepSimulation,
  AnalogAnalysisSimulation,
  AnalogDcOperatingPointSimulation,
  AnalogDcSweepSimulation,
  AnalogSimulation,
  AnalogSweepParameter,
  AnalogTransientSimulation,
  AutoroutingPhase,
  Battery,
  Board,
  Breakout,
  BreakoutPoint,
  Bus,
  CadAssembly,
  CadModel,
  Capacitor,
  Chip,
  Circuit,
  Connector,
  Constraint3 as Constraint,
  CopperPour,
  CopperText,
  CourtyardCircle,
  CourtyardOutline,
  CourtyardRect,
  Crystal,
  CurrentSource,
  Cutout,
  DifferentialPair,
  Diode,
  DrcCheck,
  EnclosureCutoutAperture,
  EnclosureFdmBox,
  FabricationNoteDimension,
  FabricationNotePath,
  FabricationNoteRect,
  FabricationNoteText,
  Breakout as Fanout,
  BreakoutPoint as FanoutPoint,
  Fiducial,
  Footprint,
  Fuse,
  Group5 as Group,
  Hole,
  Inductor,
  Interconnect,
  IsolatedCircuit,
  Jumper,
  Keepout,
  Led,
  Mosfet,
  MountedBoard,
  Net,
  NetLabel,
  NormalComponent3 as NormalComponent,
  OpAmp,
  Panel,
  PcbNoteDimension,
  PcbNoteLine,
  PcbNotePath,
  PcbNoteRect,
  PcbNoteText,
  PcbTrace,
  PcbVia,
  PinHeader,
  Pinout,
  PlatedHole,
  Port,
  Potentiometer,
  PowerSource,
  PrimitiveComponent2 as PrimitiveComponent,
  Project,
  PushButton,
  Renderable,
  Resistor,
  Resonator,
  RootCircuit,
  SOLVERS,
  SchematicArc,
  SchematicBox,
  SchematicCell,
  SchematicCircle,
  SchematicLine,
  SchematicPath,
  SchematicRect,
  SchematicRow,
  SchematicSection,
  SchematicSheet,
  SchematicSymbol,
  SchematicTable,
  SchematicText,
  SilkscreenCircle,
  SilkscreenGraphic,
  SilkscreenLine,
  SilkscreenPath,
  SilkscreenRect,
  SilkscreenText,
  SmtPad,
  SolderJumper,
  SpiceModel,
  Subcircuit,
  Subpanel,
  Switch,
  SymbolComponent as Symbol,
  TestPoint,
  Trace3 as Trace,
  TraceHint,
  Transistor,
  Via,
  VoltageProbe,
  VoltageSource,
  analog,
  applyEditEvents,
  applyEditEventsToManualEditsFile,
  applyPcbEditEventsToManualEditsFile,
  applySchematicEditEventsToManualEditsFile,
  createElement2 as createElement,
  createUseComponent,
  enclosure,
  getPhaseTimingsFromRenderEvents,
  getSimpleRouteJsonFromCircuitJson,
  kicadFootprintStrings,
  normalizePinLabels,
  orderedRenderPhases,
  renderPhaseIndexMap,
  resolveStaticFileImport,
  sel,
  unrouteCircuitJson,
  useCapacitor,
  useChip,
  useDiode,
  useLed,
  useRenderedCircuit,
  useResistor
};
