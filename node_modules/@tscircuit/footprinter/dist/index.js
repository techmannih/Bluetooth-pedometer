var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// src/footprinter.ts
import {
  length as length79
} from "circuit-json";

// src/fn/index.ts
var fn_exports = {};
__export(fn_exports, {
  axial: () => axial,
  bga: () => bga,
  breakoutheaders: () => breakoutheaders,
  cap: () => cap,
  crystal: () => crystal,
  d2pak: () => d2pak,
  dfn: () => dfn,
  diode: () => diode,
  dip: () => dip,
  dpak: () => dpak,
  electrolytic: () => electrolytic,
  fpc: () => fpc,
  hc49: () => hc49,
  headermodule: () => headermodule,
  jst: () => jst,
  lcc: () => lcc,
  led: () => led,
  led2835: () => led2835,
  led5050: () => led5050,
  lga: () => lga,
  lqfp: () => lqfp,
  m2host: () => m2host,
  melf: () => melf,
  micromelf: () => micromelf,
  minimelf: () => minimelf,
  mlp: () => mlp,
  mountedpcbmodule: () => mountedpcbmodule,
  ms012: () => ms012,
  ms013: () => ms013,
  msop: () => msop,
  pad: () => pad,
  pinrow: () => pinrow,
  platedhole: () => platedhole2,
  potentiometer: () => potentiometer,
  pushbutton: () => pushbutton,
  qfn: () => qfn,
  qfp: () => qfp,
  quad: () => quad,
  radial: () => radial,
  res: () => res,
  rj45: () => rj45,
  sma: () => sma,
  smb: () => smb,
  smbf: () => smbf,
  smc: () => smc,
  smdpinheader: () => smdpinheader,
  smdpushbutton: () => smdpushbutton,
  smdslideswitch: () => smdslideswitch,
  smf: () => smf,
  smtpad: () => smtpad,
  sod110: () => sod110,
  sod123: () => sod123,
  sod123f: () => sod123f,
  sod123fl: () => sod123fl,
  sod123w: () => sod123w,
  sod128: () => sod128,
  sod323: () => sod323,
  sod323f: () => sod323f,
  sod323fl: () => sod323fl,
  sod323w: () => sod323w,
  sod523: () => sod523,
  sod723: () => sod723,
  sod80: () => sod80,
  sod882: () => sod882,
  sod882d: () => sod882d,
  sod923: () => sod923,
  soic: () => soic,
  solderjumper: () => solderjumper,
  son: () => son,
  sop8: () => sop8,
  sot: () => sot,
  sot223: () => sot223,
  sot23: () => sot23,
  sot23w: () => sot23w,
  sot25: () => sot25,
  sot323: () => sot323,
  sot343: () => sot343,
  sot363: () => sot363,
  sot457: () => sot457,
  sot563: () => sot563,
  sot723: () => sot723,
  sot886: () => sot886,
  sot89: () => sot89,
  sot963: () => sot963,
  ssop: () => ssop,
  stampboard: () => stampboard,
  stampreceiver: () => stampreceiver,
  to220: () => to220,
  to220f: () => to220f,
  to252: () => to252,
  to263: () => to263,
  to92: () => to92,
  to92l: () => to92l,
  to92s: () => to92s,
  tqfp: () => tqfp,
  tssop: () => tssop,
  usbcmidmount: () => usbcmidmount,
  vson: () => vson,
  vssop: () => vssop,
  wson: () => wson
});

// src/helpers/silkscreenRef.ts
var silkscreenRef = (x, y, font_size) => {
  return {
    type: "pcb_silkscreen_text",
    pcb_silkscreen_text_id: "silkscreen_text_1",
    font: "tscircuit2024",
    font_size,
    pcb_component_id: "pcb_component_1",
    text: "{REF}",
    layer: "top",
    anchor_position: { x, y },
    anchor_alignment: "center"
  };
};

// src/helpers/zod/base_def.ts
import { length } from "circuit-json";
import { z as z3 } from "zod";

// src/helpers/zod/pin1-location.ts
import { z as z2 } from "zod";

// src/helpers/zod/function-call.ts
import { z } from "zod";
var function_call = z.string().or(z.array(z.any())).transform((a) => {
  if (Array.isArray(a)) return a;
  if (a.startsWith("(") && a.endsWith(")")) {
    a = a.slice(1, -1);
  }
  return a.split(",").map((v) => {
    const numVal = Number(v);
    return isNaN(numVal) ? v : numVal;
  });
}).pipe(z.array(z.string().or(z.number())));

// src/helpers/zod/pin1-location.ts
var horizontalSide = z2.enum(["leftside", "rightside"]);
var verticalSide = z2.enum(["topside", "bottomside"]);
var horizontalAlignment = z2.enum(["left", "right"]);
var verticalAlignment = z2.enum(["top", "bottom"]);
var pin1_location = function_call.pipe(
  z2.union([
    z2.tuple([horizontalSide, verticalAlignment]),
    z2.tuple([verticalSide, horizontalAlignment])
  ])
);

// src/helpers/zod/base_def.ts
var base_def = z3.object({
  norefdes: z3.boolean().optional().describe("disable reference designator label"),
  invert: z3.boolean().optional().describe("hint to invert the orientation of the 3D model"),
  faceup: z3.boolean().optional().describe("The male pin header should face upwards, out of the top layer"),
  nosilkscreen: z3.boolean().optional().describe("omit all silkscreen elements from the footprint"),
  rounded: length.refine((radius) => radius >= 0, {
    message: "rounded radius must be non-negative"
  }).optional().describe("corner radius applied to all rectangular copper pads"),
  pin1location: pin1_location.optional().describe("rotate the footprint to place pin 1 on a requested side")
});

// src/fn/dip.ts
import { z as z4 } from "zod";

// src/helpers/platedhole.ts
import { mm } from "@tscircuit/mm";
var platedhole = (pn, x, y, id, od) => {
  return {
    pcb_plated_hole_id: "",
    type: "pcb_plated_hole",
    shape: "circle",
    x,
    y,
    hole_diameter: mm(id),
    outer_diameter: mm(od),
    pcb_port_id: "",
    layers: ["top", "bottom"],
    port_hints: [pn.toString()]
  };
};

// src/helpers/platedHoleWithRectPad.ts
import { mm as mm2 } from "@tscircuit/mm";
var platedHoleWithRectPad = (options) => {
  const {
    pn,
    x,
    y,
    holeDiameter,
    rectPadWidth,
    rectPadHeight,
    holeOffsetX = 0,
    holeOffsetY = 0,
    rectBorderRadius
  } = options;
  return {
    pcb_plated_hole_id: "",
    type: "pcb_plated_hole",
    shape: "circular_hole_with_rect_pad",
    x,
    y,
    hole_diameter: mm2(holeDiameter),
    hole_shape: "circle",
    pad_shape: "rect",
    rect_pad_width: mm2(rectPadWidth),
    rect_pad_height: mm2(rectPadHeight),
    ...rectBorderRadius !== void 0 && {
      rect_border_radius: rectBorderRadius
    },
    pcb_port_id: "",
    layers: ["top", "bottom"],
    port_hints: [pn.toString()],
    hole_offset_x: holeOffsetX,
    hole_offset_y: holeOffsetY
  };
};

// src/helpers/u-curve.ts
var u_curve = Array.from(
  { length: 9 },
  (_, i) => Math.cos(i / 8 * Math.PI - Math.PI)
).map((x) => ({
  x,
  y: -Math.sqrt(1 - x ** 2)
}));

// src/helpers/rect-union-outline.ts
var createRectUnionOutline = (rectBoundsFromOuterToInner) => {
  if (rectBoundsFromOuterToInner.length < 2) {
    throw new Error("Rect union outline requires at least two bounds");
  }
  for (const bounds of rectBoundsFromOuterToInner) {
    if (bounds.minX >= bounds.maxX || bounds.minY >= bounds.maxY) {
      throw new Error("Each rectangle bound must have min < max on both axes");
    }
  }
  for (let i = 1; i < rectBoundsFromOuterToInner.length; i++) {
    const previous = rectBoundsFromOuterToInner[i - 1];
    const current = rectBoundsFromOuterToInner[i];
    if (current.minX < previous.minX || current.maxX > previous.maxX || current.minY > previous.minY || current.maxY < previous.maxY) {
      throw new Error(
        "Bounds must be ordered outer-to-inner with tighter X and larger Y span"
      );
    }
  }
  const buildTopLeftRouteFromInnerToOuter = () => {
    const route = [];
    let index = rectBoundsFromOuterToInner.length - 1;
    route.push({
      x: rectBoundsFromOuterToInner[index].minX,
      y: rectBoundsFromOuterToInner[index].maxY
    });
    while (index > 0) {
      const current = rectBoundsFromOuterToInner[index];
      const previous = rectBoundsFromOuterToInner[index - 1];
      route.push({ x: current.minX, y: previous.maxY });
      route.push({ x: previous.minX, y: previous.maxY });
      index -= 1;
    }
    return route;
  };
  const buildTopRightRouteFromInnerToOuter = () => {
    const route = [];
    let index = rectBoundsFromOuterToInner.length - 1;
    route.push({
      x: rectBoundsFromOuterToInner[index].maxX,
      y: rectBoundsFromOuterToInner[index].maxY
    });
    while (index > 0) {
      const current = rectBoundsFromOuterToInner[index];
      const previous = rectBoundsFromOuterToInner[index - 1];
      route.push({ x: current.maxX, y: previous.maxY });
      route.push({ x: previous.maxX, y: previous.maxY });
      index -= 1;
    }
    return route;
  };
  const buildBottomRightRouteFromInnerToOuter = () => {
    const route = [];
    let index = rectBoundsFromOuterToInner.length - 1;
    route.push({
      x: rectBoundsFromOuterToInner[index].maxX,
      y: rectBoundsFromOuterToInner[index].minY
    });
    while (index > 0) {
      const current = rectBoundsFromOuterToInner[index];
      const previous = rectBoundsFromOuterToInner[index - 1];
      route.push({ x: current.maxX, y: previous.minY });
      route.push({ x: previous.maxX, y: previous.minY });
      index -= 1;
    }
    return route;
  };
  const buildBottomLeftRouteFromInnerToOuter = () => {
    const route = [];
    let index = rectBoundsFromOuterToInner.length - 1;
    route.push({
      x: rectBoundsFromOuterToInner[index].minX,
      y: rectBoundsFromOuterToInner[index].minY
    });
    while (index > 0) {
      const current = rectBoundsFromOuterToInner[index];
      const previous = rectBoundsFromOuterToInner[index - 1];
      route.push({ x: current.minX, y: previous.minY });
      route.push({ x: previous.minX, y: previous.minY });
      index -= 1;
    }
    return route;
  };
  const topLeftInnerToOuter = buildTopLeftRouteFromInnerToOuter();
  const topRightInnerToOuter = buildTopRightRouteFromInnerToOuter();
  const bottomRightInnerToOuter = buildBottomRightRouteFromInnerToOuter();
  const bottomLeftInnerToOuter = buildBottomLeftRouteFromInnerToOuter();
  return [
    ...topLeftInnerToOuter.reverse(),
    ...topRightInnerToOuter,
    ...bottomRightInnerToOuter.reverse(),
    ...bottomLeftInnerToOuter
  ];
};

// src/fn/dip.ts
function convertMilToMm(value) {
  if (typeof value === "string") {
    if (value.trim().toLowerCase().endsWith("mil")) {
      return parseFloat(value) * 0.0254;
    }
    return parseFloat(value);
  }
  return Number(value);
}
var lengthInMm = z4.union([z4.string(), z4.number()]).transform((val) => convertMilToMm(val));
var extendDipDef = (newDefaults) => base_def.extend({
  fn: z4.string(),
  num_pins: z4.number().optional().default(6),
  wide: z4.boolean().optional(),
  narrow: z4.boolean().optional(),
  w: lengthInMm.optional(),
  p: lengthInMm.default(newDefaults.p ?? "2.54mm"),
  id: lengthInMm.optional(),
  od: lengthInMm.optional(),
  nosquareplating: z4.boolean().optional().default(false).describe("do not use rectangular pad for pin 1")
}).transform((v) => {
  if (!v.id && !v.od) {
    if (Math.abs(v.p - 1.27) < 0.01) {
      v.id = convertMilToMm("0.55mm");
      v.od = convertMilToMm("0.95mm");
    } else {
      v.id = convertMilToMm("0.8mm");
      v.od = convertMilToMm("1.6mm");
    }
  } else if (!v.id) {
    v.id = v.od * (1 / 1.5);
  } else if (!v.od) {
    v.od = v.id * (1.5 / 1);
  }
  if (!v.w) {
    if (v.wide) {
      v.w = convertMilToMm("600mil");
    } else if (v.narrow) {
      v.w = convertMilToMm("300mil");
    } else {
      v.w = convertMilToMm(newDefaults.w ?? "300mil");
    }
  }
  return v;
});
var dip_def = extendDipDef({});
var getCcwDipCoords = (pinCount, pn, w, p, nosquareplating) => {
  const ph = pinCount / 2;
  const isLeft = pn <= ph;
  const leftPinGaps = ph - 1;
  const gs = p;
  const h = gs * leftPinGaps;
  if (isLeft) {
    return { x: -w / 2, y: h / 2 - (pn - 1) * gs };
  }
  return { x: w / 2, y: -h / 2 + (pn - ph - 1) * gs };
};
var dip = (raw_params) => {
  const parameters = dip_def.parse(raw_params);
  const platedHoles = [];
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getCcwDipCoords(
      parameters.num_pins,
      i + 1,
      parameters.w,
      parameters.p ?? 2.54,
      parameters.nosquareplating
    );
    if (i === 0 && !parameters.nosquareplating) {
      platedHoles.push(
        platedHoleWithRectPad({
          pn: i + 1,
          x,
          y,
          holeDiameter: parameters.id ?? "0.8mm",
          rectPadWidth: parameters.od ?? "1mm",
          rectPadHeight: parameters.od ?? "1mm"
        })
      );
      continue;
    }
    platedHoles.push(
      platedhole(i + 1, x, y, parameters.id ?? "0.8mm", parameters.od ?? "1mm")
    );
  }
  const padEdgeHeight = (parameters.num_pins / 2 - 1) * parameters.p + parameters.od;
  const innerGap = parameters.w - parameters.od;
  const sw = innerGap - 1;
  const sh = (parameters.num_pins / 2 - 1) * parameters.p + parameters.od + 0.4;
  const silkscreenBorder = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -sw / 2, y: -sh / 2 },
      { x: -sw / 2, y: sh / 2 },
      // U-notch curve outside the pads
      ...u_curve.map(({ x, y }) => ({
        x: x * sw / 6,
        y: y * sw / 6 + sh / 2
      })),
      { x: sw / 2, y: sh / 2 },
      { x: sw / 2, y: -sh / 2 },
      { x: -sw / 2, y: -sh / 2 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPins = [];
  for (let i = 0; i < parameters.num_pins; i++) {
    const pinNumber = i + 1;
    const isLeft = pinNumber <= parameters.num_pins / 2;
    const pinCenter = getCcwDipCoords(
      parameters.num_pins,
      pinNumber,
      parameters.w,
      parameters.p ?? 2.54,
      parameters.nosquareplating
    );
    const clearance = 0.6;
    const pinLabelX = isLeft ? pinCenter.x - parameters.od / 2 - clearance : pinCenter.x + parameters.od / 2 + clearance;
    const fontSize = 0.3;
    silkscreenPins.push({
      type: "pcb_fabrication_note_text",
      pcb_fabrication_note_text_id: `pin_${pinNumber}`,
      layer: "top",
      pcb_component_id: `pin_${pinNumber}`,
      text: `{pin${pinNumber}}`,
      anchor_position: { x: pinLabelX, y: pinCenter.y + fontSize / 2 },
      font_size: fontSize,
      font: "tscircuit2024",
      anchor_alignment: isLeft ? "top_right" : "top_left"
    });
  }
  const silkscreenRefText = silkscreenRef(0, sh / 2 + 0.5, 0.4);
  const pinRowSpanX = parameters.w + parameters.od;
  const pinRowSpanY = padEdgeHeight;
  const courtyardStepOuterHalfWidth = pinRowSpanX / 2 + 0.25;
  const courtyardStepInnerHalfWidth = courtyardStepOuterHalfWidth;
  const courtyardStepOuterHalfHeight = pinRowSpanY / 2 + 0.72;
  const courtyardStepInnerHalfHeight = courtyardStepOuterHalfHeight;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ]),
    layer: "top"
  };
  return {
    circuitJson: [
      ...platedHoles,
      silkscreenBorder,
      silkscreenRefText,
      ...silkscreenPins,
      courtyard
    ],
    parameters
  };
};

// src/fn/dpak.ts
import {
  length as length2
} from "circuit-json";
import { z as z5 } from "zod";

// src/helpers/rectpad.ts
var rectpad = (pn, x, y, w, h, cornerRadius) => {
  return {
    type: "pcb_smtpad",
    x,
    y,
    width: w,
    height: h,
    layer: "top",
    shape: "rect",
    ...cornerRadius !== void 0 && {
      corner_radius: cornerRadius
    },
    pcb_smtpad_id: "",
    port_hints: Array.isArray(pn) ? pn.map((item) => item.toString()) : [pn.toString()]
  };
};

// src/helpers/silkscreenpath.ts
var silkscreenpath = (route, options = {}) => {
  return {
    type: "pcb_silkscreen_path",
    layer: options.layer || "top",
    pcb_component_id: options.pcb_component_id || "",
    pcb_silkscreen_path_id: options.pcb_silkscreen_path_id || "",
    route,
    stroke_width: options.stroke_width || 0.1
  };
};

// src/fn/dpak.ts
var dpak_def = base_def.extend({
  fn: z5.string(),
  num_pins: z5.union([z5.literal(3), z5.literal(6)]).default(3),
  p: length2.default("2.29mm").describe("pitch between adjacent lead positions"),
  pw: length2.default("1.6mm").describe("lead pad width along the pin column"),
  pl: length2.default("3mm").describe("lead pad length toward the package"),
  tabw: length2.default("6.2mm").describe("large tab pad width"),
  tabh: length2.default("5.8mm").describe("large tab pad height"),
  span: length2.default("6.85mm").describe("center-to-center distance from the leads to the tab"),
  w: length2.default("6.6mm").describe("package body width"),
  h: length2.default("6.5mm").describe("package body height"),
  string: z5.string().optional()
});
var d2pakDefaults = {
  p: "2.54mm",
  pw: "1.5mm",
  pl: "3.5mm",
  tabw: "8.38mm",
  tabh: "10.7mm",
  span: "10.21mm",
  w: "10.1mm",
  h: "10.1mm"
};
var getNumberOfPads = (rawParams) => {
  if (rawParams.num_pins === 3 || rawParams.num_pins === 6) {
    return rawParams.num_pins;
  }
  const match = rawParams.string?.match(/^(?:d2pak|to252|to263)_(\d+)/i);
  const packagePinCount = Number.parseInt(match?.[1] ?? "3", 10);
  if (packagePinCount === 5 || packagePinCount === 6) return 6;
  return 3;
};
var createDpak = (rawParams, defaults) => {
  const parameters = dpak_def.parse({
    ...defaults,
    ...rawParams,
    num_pins: getNumberOfPads(rawParams)
  });
  const { num_pins, p, pw, pl, tabw, tabh, span, w, h } = parameters;
  const leadCount = num_pins === 3 ? 2 : 5;
  const leadX = -span / 2;
  const tabX = span / 2;
  const pads = [];
  for (let i = 0; i < leadCount; i++) {
    const pinNumber = num_pins === 3 ? i * 2 + 1 : i + 1;
    const y = num_pins === 3 ? (1 - i * 2) * p : ((leadCount - 1) / 2 - i) * p;
    pads.push(rectpad(pinNumber, leadX, y, pl, pw));
  }
  pads.push(rectpad(num_pins === 3 ? 2 : 6, tabX, 0, tabw, tabh));
  const bodyLeft = -w / 2;
  const bodyRight = w / 2;
  const copperMinX = Math.min(leadX - pl / 2, tabX - tabw / 2);
  const copperMaxX = Math.max(leadX + pl / 2, tabX + tabw / 2);
  const leadHalfSpanY = (num_pins === 3 ? p : (leadCount - 1) / 2 * p) + pw / 2;
  const copperHalfHeight = Math.max(leadHalfSpanY, tabh / 2);
  const silkscreenHalfHeight = Math.max(copperHalfHeight, h / 2) + 0.2;
  const silkscreen = [
    silkscreenpath([
      { x: bodyLeft, y: silkscreenHalfHeight },
      { x: bodyRight, y: silkscreenHalfHeight }
    ]),
    silkscreenpath([
      { x: bodyLeft, y: -silkscreenHalfHeight },
      { x: bodyRight, y: -silkscreenHalfHeight }
    ]),
    // Upward-pointing marker above the pin 1 lead column.
    silkscreenpath([
      { x: leadX - 0.3, y: silkscreenHalfHeight + 0.15 },
      { x: leadX, y: silkscreenHalfHeight + 0.45 },
      { x: leadX + 0.3, y: silkscreenHalfHeight + 0.15 }
    ])
  ];
  const courtyardMinX = Math.min(copperMinX, bodyLeft) - 0.25;
  const courtyardMaxX = Math.max(copperMaxX, bodyRight) + 0.25;
  const courtyardHalfHeight = Math.max(copperHalfHeight, h / 2) + 0.25;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: {
      x: (courtyardMinX + courtyardMaxX) / 2,
      y: 0
    },
    width: courtyardMaxX - courtyardMinX,
    height: courtyardHalfHeight * 2,
    layer: "top"
  };
  return {
    circuitJson: [
      ...pads,
      ...silkscreen,
      silkscreenRef(0, courtyardHalfHeight + 0.55, 0.5),
      courtyard
    ],
    parameters
  };
};
var dpak = (rawParams) => createDpak(rawParams);
var d2pak = (rawParams) => createDpak(rawParams, d2pakDefaults);
var to252 = dpak;
var to263 = d2pak;

// src/helpers/passive-fn.ts
import mm3 from "@tscircuit/mm";
import { z as z6 } from "zod";
import { length as length3, distance } from "circuit-json";
var footprintSizes = [
  {
    imperial: "01005",
    metric: "0402",
    p_mm_min: 0.5,
    pw_mm_min: 0.4,
    ph_mm_min: 0.3,
    w_mm_min: 0.58,
    h_mm_min: 0.21,
    courtyard_width_mm: 1.2,
    courtyard_height_mm: 0.6
  },
  {
    imperial: "0504",
    metric: "1310",
    p_mm_min: 1.1,
    pw_mm_min: 0.65,
    ph_mm_min: 1.3,
    w_mm_min: 0.58,
    h_mm_min: 0.21
  },
  {
    imperial: "1812",
    metric: "4532",
    p_mm_min: 4.275,
    pw_mm_min: 1.125,
    ph_mm_min: 3.4,
    w_mm_min: 5.4,
    h_mm_min: 3.4,
    courtyard_width_mm: 5.9,
    courtyard_height_mm: 3.9,
    rounded_pads: true,
    nonpolarizedSilkscreen: {
      line_half_length_mm: 1.386252,
      line_y_mm: 1.71,
      stroke_width_mm: 0.12
    }
  },
  {
    imperial: "0201",
    metric: "0603",
    p_mm_min: 0.66,
    pw_mm_min: 0.46,
    ph_mm_min: 0.4,
    w_mm_min: 0.9,
    h_mm_min: 0.3,
    courtyard_width_mm: 1.4,
    courtyard_height_mm: 0.7,
    rounded_pads: true
  },
  {
    imperial: "0402",
    metric: "1005",
    p_mm_min: 1.02,
    pw_mm_min: 0.54,
    ph_mm_min: 0.64,
    w_mm_min: 1.56,
    h_mm_min: 0.64,
    courtyard_width_mm: 1.86,
    courtyard_height_mm: 0.94,
    rounded_pads: true,
    nonpolarizedSilkscreen: {
      line_half_length_mm: 0.153641,
      line_y_mm: 0.38,
      stroke_width_mm: 0.12
    }
  },
  {
    imperial: "0603",
    metric: "1608",
    p_mm_min: 1.65,
    pw_mm_min: 0.8,
    ph_mm_min: 0.95,
    w_mm_min: 2.45,
    h_mm_min: 0.95,
    courtyard_width_mm: 2.96,
    courtyard_height_mm: 1.46,
    rounded_pads: true,
    nonpolarizedSilkscreen: {
      line_half_length_mm: 0.237258,
      line_y_mm: 0.5225,
      stroke_width_mm: 0.12
    }
  },
  {
    imperial: "0805",
    metric: "2012",
    p_mm_min: 1.825,
    pw_mm_min: 1.025,
    ph_mm_min: 1.4,
    w_mm_min: 2.8499999999999996,
    h_mm_min: 1.4,
    courtyard_width_mm: 3.36,
    courtyard_height_mm: 1.9,
    rounded_pads: true,
    nonpolarizedSilkscreen: {
      line_half_length_mm: 0.227064,
      line_y_mm: 0.735,
      stroke_width_mm: 0.12
    }
  },
  {
    imperial: "1206",
    metric: "3216",
    p_mm_min: 2.925,
    pw_mm_min: 1.125,
    ph_mm_min: 1.75,
    w_mm_min: 4.05,
    h_mm_min: 1.75,
    courtyard_width_mm: 4.56,
    courtyard_height_mm: 2.26,
    rounded_pads: true,
    nonpolarizedSilkscreen: {
      line_half_length_mm: 0.727064,
      line_y_mm: 0.91,
      stroke_width_mm: 0.12
    }
  },
  {
    imperial: "1210",
    metric: "3225",
    p_mm_min: 2.925,
    pw_mm_min: 1.125,
    ph_mm_min: 2.65,
    w_mm_min: 4.05,
    h_mm_min: 2.65,
    courtyard_width_mm: 4.56,
    courtyard_height_mm: 3.16,
    rounded_pads: true,
    nonpolarizedSilkscreen: {
      line_half_length_mm: 0.723737,
      line_y_mm: 1.355,
      stroke_width_mm: 0.12
    }
  },
  {
    imperial: "2010",
    metric: "5025",
    p_mm_min: 4.625,
    pw_mm_min: 1.225,
    ph_mm_min: 2.65,
    w_mm_min: 5.85,
    h_mm_min: 2.65,
    courtyard_width_mm: 6.36,
    courtyard_height_mm: 3.16,
    rounded_pads: true,
    nonpolarizedSilkscreen: {
      line_half_length_mm: 1.527064,
      line_y_mm: 1.36,
      stroke_width_mm: 0.12
    }
  },
  {
    imperial: "2512",
    metric: "6332",
    p_mm_min: 5.925,
    pw_mm_min: 1.225,
    ph_mm_min: 3.35,
    w_mm_min: 7.15,
    h_mm_min: 3.35,
    courtyard_width_mm: 7.66,
    courtyard_height_mm: 3.86,
    rounded_pads: true,
    nonpolarizedSilkscreen: {
      line_half_length_mm: 2.177064,
      line_y_mm: 1.71,
      stroke_width_mm: 0.12
    }
  }
];
var metricMap = Object.fromEntries(footprintSizes.map((s) => [s.metric, s]));
var imperialMap = Object.fromEntries(
  footprintSizes.map((s) => [s.imperial, s])
);
var createCourtyardRect = (width, height) => ({
  type: "pcb_courtyard_rect",
  pcb_courtyard_rect_id: "",
  pcb_component_id: "",
  center: { x: 0, y: 0 },
  width,
  height,
  layer: "top"
});
var passive_def = base_def.extend({
  fn: z6.string().optional(),
  string: z6.string().optional(),
  tht: z6.boolean(),
  p: length3.optional(),
  pw: length3.optional(),
  ph: length3.optional(),
  metric: distance.optional(),
  imperial: distance.optional(),
  w: length3.optional(),
  h: length3.optional(),
  nonpolarized: z6.boolean().optional(),
  textbottom: z6.boolean().optional(),
  roundedPads: z6.boolean().optional()
});
var passive = (params) => {
  let {
    fn,
    tht,
    p,
    pw,
    ph,
    metric,
    imperial,
    w,
    h,
    nonpolarized,
    textbottom,
    roundedPads,
    string: footprintString
  } = params;
  if (typeof w === "string") w = mm3(w);
  if (typeof h === "string") h = mm3(h);
  if (typeof p === "string") p = mm3(p);
  if (typeof pw === "string") pw = mm3(pw);
  if (typeof ph === "string") ph = mm3(ph);
  if (h !== void 0 && w !== void 0 && h > w) {
    throw new Error(
      "height cannot be greater than width (rotated footprint not yet implemented)"
    );
  }
  let sz;
  if (metric) sz = metricMap[metric];
  if (imperial) sz = imperialMap[imperial];
  if (sz) {
    w = sz.w_mm_min;
    h = sz.h_mm_min;
    p = sz.p_mm_min;
    pw = sz.pw_mm_min;
    ph = sz.ph_mm_min;
  }
  if (p === void 0 || pw === void 0 || ph === void 0) {
    throw new Error("Could not determine required pad dimensions (p, pw, ph)");
  }
  let silkscreenLines = [];
  const nonpolarizedSilkscreen = fn === "res" && (nonpolarized === true || typeof footprintString !== "string" || /^res(?:\d{4}|\d{5})(?:_|$)/i.test(footprintString)) ? sz?.nonpolarizedSilkscreen : void 0;
  if (nonpolarizedSilkscreen?.stroke_width_mm) {
    const {
      line_half_length_mm = 0,
      line_y_mm = 0,
      stroke_width_mm
    } = nonpolarizedSilkscreen;
    silkscreenLines = [
      {
        type: "pcb_silkscreen_path",
        layer: "top",
        pcb_component_id: "",
        route: [
          { x: -line_half_length_mm, y: line_y_mm },
          { x: line_half_length_mm, y: line_y_mm }
        ],
        stroke_width: stroke_width_mm,
        pcb_silkscreen_path_id: ""
      },
      {
        type: "pcb_silkscreen_path",
        layer: "top",
        pcb_component_id: "",
        route: [
          { x: -line_half_length_mm, y: -line_y_mm },
          { x: line_half_length_mm, y: -line_y_mm }
        ],
        stroke_width: stroke_width_mm,
        pcb_silkscreen_path_id: ""
      }
    ];
  } else {
    silkscreenLines = [
      {
        type: "pcb_silkscreen_path",
        layer: "top",
        pcb_component_id: "",
        route: [
          { x: p / 2, y: ph / 2 + 0.4 },
          { x: -p / 2 - pw / 2 - 0.2, y: ph / 2 + 0.4 },
          { x: -p / 2 - pw / 2 - 0.2, y: -ph / 2 - 0.4 },
          { x: p / 2, y: -ph / 2 - 0.4 }
        ],
        stroke_width: 0.1,
        pcb_silkscreen_path_id: ""
      }
    ];
  }
  const textY = textbottom ? -ph / 2 - 0.9 : ph / 2 + 0.9;
  const silkscreenRefText = silkscreenRef(0, textY, 0.2);
  const courtyard = sz?.courtyard_width_mm && sz.courtyard_height_mm ? createCourtyardRect(sz.courtyard_width_mm, sz.courtyard_height_mm) : null;
  const shouldRoundPads = roundedPads ?? sz?.rounded_pads ?? false;
  const cornerRadius = shouldRoundPads ? Math.min(0.125, Math.min(pw, ph) / 8) : void 0;
  if (tht) {
    return [
      platedhole(1, -p / 2, 0, pw, pw * 1 / 0.8),
      platedhole(2, p / 2, 0, pw, pw * 1 / 0.8),
      ...silkscreenLines,
      silkscreenRefText,
      ...courtyard ? [courtyard] : []
    ];
  }
  return [
    rectpad(["1", "left"], -p / 2, 0, pw, ph, cornerRadius),
    rectpad(["2", "right"], p / 2, 0, pw, ph, cornerRadius),
    ...silkscreenLines,
    silkscreenRefText,
    ...courtyard ? [courtyard] : []
  ];
};

// src/helpers/create-fabrication-note-diode.ts
import {
  length as length4
} from "circuit-json";
var createFabricationNoteDiodeFromCopperPads = (parameters) => {
  const pitch = parameters.p ?? parameters.pad_spacing;
  if (pitch === void 0 || parameters.pl === void 0 || parameters.pw === void 0) {
    throw new Error(
      "Diode fabrication note requires p or pad_spacing, pl, and pw"
    );
  }
  const padPitch = length4.parse(pitch);
  const padLength = length4.parse(parameters.pl);
  const padWidth = length4.parse(parameters.pw);
  return createFabricationNoteDiode({
    minX: -padPitch / 2 - padLength / 2,
    maxX: padPitch / 2 + padLength / 2,
    minY: -padWidth / 2,
    maxY: padWidth / 2
  });
};
var createFabricationNoteDiode = (bounds) => {
  const elms = [];
  const width = bounds.maxX - bounds.minX;
  const height = bounds.maxY - bounds.minY;
  const centerX = (bounds.minX + bounds.maxX) / 2;
  const centerY = (bounds.minY + bounds.maxY) / 2;
  const symbolHalfHeight = height * 0.28;
  const symbolHeight = symbolHalfHeight * 2;
  const maxSymbolWidth = width * 0.2;
  const maxSymbolWidthToHeightRatio = 1.25;
  const symbolWidth = Math.min(
    maxSymbolWidth,
    symbolHeight * maxSymbolWidthToHeightRatio
  );
  const symbolMinX = centerX - symbolWidth / 2;
  const symbolMaxX = centerX + symbolWidth / 2;
  const legLength = symbolWidth * 0.18;
  const triangleBaseX = symbolMinX + legLength;
  const cathodeX = symbolMaxX - legLength;
  const strokeWidth = Math.max(Math.min(width, height) * 0.035, 0.01);
  const fontSize = Math.max(Math.min(width, height) * 0.25, 0.1);
  elms.push(
    {
      type: "pcb_fabrication_note_path",
      pcb_fabrication_note_path_id: "diode_fabrication_note_anode_leg",
      pcb_component_id: "",
      layer: "top",
      stroke_width: strokeWidth,
      route: [
        { x: symbolMinX, y: centerY },
        { x: triangleBaseX, y: centerY }
      ]
    },
    {
      type: "pcb_fabrication_note_path",
      pcb_fabrication_note_path_id: "diode_fabrication_note_triangle",
      pcb_component_id: "",
      layer: "top",
      stroke_width: strokeWidth,
      route: [
        { x: triangleBaseX, y: centerY + symbolHalfHeight },
        { x: cathodeX, y: centerY },
        { x: triangleBaseX, y: centerY - symbolHalfHeight },
        { x: triangleBaseX, y: centerY + symbolHalfHeight }
      ]
    },
    {
      type: "pcb_fabrication_note_path",
      pcb_fabrication_note_path_id: "diode_fabrication_note_cathode",
      pcb_component_id: "",
      layer: "top",
      stroke_width: strokeWidth,
      route: [
        { x: cathodeX, y: centerY + symbolHalfHeight },
        { x: cathodeX, y: centerY - symbolHalfHeight }
      ]
    },
    {
      type: "pcb_fabrication_note_path",
      pcb_fabrication_note_path_id: "diode_fabrication_note_cathode_leg",
      pcb_component_id: "",
      layer: "top",
      stroke_width: strokeWidth,
      route: [
        { x: cathodeX, y: centerY },
        { x: symbolMaxX, y: centerY }
      ]
    },
    {
      type: "pcb_fabrication_note_text",
      pcb_fabrication_note_text_id: "diode_fabrication_note_positive",
      pcb_component_id: "",
      layer: "top",
      font: "tscircuit2024",
      font_size: fontSize,
      text: "+",
      anchor_position: { x: bounds.minX + width * 0.125, y: centerY },
      anchor_alignment: "center"
    },
    {
      type: "pcb_fabrication_note_text",
      pcb_fabrication_note_text_id: "diode_fabrication_note_negative",
      pcb_component_id: "",
      layer: "top",
      font: "tscircuit2024",
      font_size: fontSize,
      text: "-",
      anchor_position: { x: bounds.maxX - width * 0.125, y: centerY },
      anchor_alignment: "center"
    }
  );
  return elms;
};

// src/fn/diode.ts
var getCopperBounds = (circuitJson) => {
  let minX = Number.POSITIVE_INFINITY;
  let maxX = Number.NEGATIVE_INFINITY;
  let minY = Number.POSITIVE_INFINITY;
  let maxY = Number.NEGATIVE_INFINITY;
  for (const element of circuitJson) {
    if (element.type === "pcb_smtpad") {
      minX = Math.min(minX, element.x - element.width / 2);
      maxX = Math.max(maxX, element.x + element.width / 2);
      minY = Math.min(minY, element.y - element.height / 2);
      maxY = Math.max(maxY, element.y + element.height / 2);
    }
    if (element.type === "pcb_plated_hole") {
      minX = Math.min(minX, element.x - element.outer_diameter / 2);
      maxX = Math.max(maxX, element.x + element.outer_diameter / 2);
      minY = Math.min(minY, element.y - element.outer_diameter / 2);
      maxY = Math.max(maxY, element.y + element.outer_diameter / 2);
    }
  }
  if (!Number.isFinite(minX) || !Number.isFinite(maxX) || !Number.isFinite(minY) || !Number.isFinite(maxY)) {
    throw new Error("Could not determine diode copper bounds");
  }
  return { minX, maxX, minY, maxY };
};
var diode = (parameters) => {
  const circuitJson = passive({ ...parameters, roundedPads: true });
  return {
    circuitJson: circuitJson.concat(
      createFabricationNoteDiode(getCopperBounds(circuitJson))
    ),
    parameters
  };
};

// src/fn/cap.ts
var cap = (parameters) => {
  return { circuitJson: passive(parameters), parameters };
};

// src/fn/crystal.ts
import {
  length as length5
} from "circuit-json";
import { z as z7 } from "zod";
var crystal_def = base_def.extend({
  fn: z7.literal("crystal"),
  num_pins: z7.literal(4).default(4),
  px: length5.default("2.2mm").describe("horizontal pad center pitch"),
  py: length5.default("1.7mm").describe("vertical pad center pitch"),
  pw: length5.default("1.4mm").describe("pad width"),
  ph: length5.default("1.2mm").describe("pad height")
});
var crystal = (rawParams) => {
  const parameters = crystal_def.parse(rawParams);
  const { px, py, pw, ph } = parameters;
  const pads = [
    rectpad(1, -px / 2, -py / 2, pw, ph),
    rectpad(2, px / 2, -py / 2, pw, ph),
    rectpad(3, px / 2, py / 2, pw, ph),
    rectpad(4, -px / 2, py / 2, pw, ph)
  ];
  const outlineHalfWidth = (px + pw) / 2 + 0.2;
  const outlineHalfHeight = (py + ph) / 2 + 0.2;
  const outline = silkscreenpath([
    { x: -outlineHalfWidth, y: -outlineHalfHeight },
    { x: outlineHalfWidth, y: -outlineHalfHeight },
    { x: outlineHalfWidth, y: outlineHalfHeight },
    { x: -outlineHalfWidth, y: outlineHalfHeight },
    { x: -outlineHalfWidth, y: -outlineHalfHeight }
  ]);
  const ref = silkscreenRef(0, outlineHalfHeight + 0.6, 0.5);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: px + pw + 0.5,
    height: py + ph + 0.5,
    layer: "top"
  };
  return {
    circuitJson: [...pads, outline, ref, courtyard],
    parameters
  };
};

// src/fn/led.ts
var led = (parameters) => {
  const circuitJson = passive(parameters).map((element) => {
    if (element.type !== "pcb_silkscreen_path") return element;
    return {
      ...element,
      route: element.route.map((point) => ({ ...point, x: -point.x }))
    };
  });
  return { circuitJson, parameters };
};

// src/fn/led2835.ts
import {
  length as length6
} from "circuit-json";
import { z as z8 } from "zod";
var led2835_def = base_def.extend({
  fn: z8.literal("led2835"),
  num_pins: z8.literal(2).default(2),
  // Anode (pin 1) is the wide pad, cathode (pin 2) the narrow pad, matching
  // the KiCad LED_PLCC_2835 land pattern
  p1w: length6.default("2.2mm").describe("pin 1 (anode) pad width"),
  p2w: length6.default("1.25mm").describe("pin 2 (cathode) pad width"),
  ph: length6.default("2.2mm").describe("pad height"),
  p1x: length6.default("-0.9mm").describe("pin 1 pad center x"),
  p2x: length6.default("1.375mm").describe("pin 2 pad center x"),
  w: length6.default("3.5mm").describe("package body width"),
  h: length6.default("2.8mm").describe("package body height")
});
var led2835 = (rawParams) => {
  const parameters = led2835_def.parse(rawParams);
  const { p1w, p2w, ph, p1x, p2x, w, h } = parameters;
  const pads = [
    rectpad(1, p1x, 0, p1w, ph),
    rectpad(2, p2x, 0, p2w, ph)
  ];
  const sx = w / 2 + 0.45;
  const sy = h / 2 + 0.2;
  const silkscreen = [
    silkscreenpath([
      { x: p2x - p2w / 2, y: -sy },
      { x: -sx, y: -sy },
      { x: -sx, y: sy },
      { x: p2x - p2w / 2, y: sy }
    ])
  ];
  const ref = silkscreenRef(0, sy + 0.8, 0.5);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: (p1x - p1w / 2 + p2x + p2w / 2) / 2, y: 0 },
    width: p2x + p2w / 2 - (p1x - p1w / 2) + 0.5,
    height: Math.max(h, ph) + 0.5,
    layer: "top"
  };
  return {
    circuitJson: [...pads, ...silkscreen, ref, courtyard],
    parameters
  };
};

// src/fn/led5050.ts
import {
  length as length7
} from "circuit-json";
import { z as z9 } from "zod";
var led5050_def = base_def.extend({
  fn: z9.literal("led5050"),
  num_pins: z9.literal(6).default(6),
  p: length7.default("1.7mm").describe("pin pitch along each column"),
  rowspan: length7.default("4.8mm").describe("distance between pad columns"),
  pl: length7.default("2mm").describe("pad length across the package edge"),
  pw: length7.default("1.1mm").describe("pad width along the pin column"),
  w: length7.default("5mm").describe("package body width"),
  h: length7.default("5mm").describe("package body height")
});
var led5050 = (rawParams) => {
  const parameters = led5050_def.parse(rawParams);
  const { p, rowspan, pl, pw, w, h } = parameters;
  const pads = [];
  for (let i = 0; i < 3; i++) {
    pads.push(rectpad(i + 1, -rowspan / 2, p - i * p, pl, pw));
    pads.push(rectpad(6 - i, rowspan / 2, p - i * p, pl, pw));
  }
  const bodyEdgeX = w / 2;
  const bodyEdgeY = h / 2 + 0.2;
  const silkscreen = [
    silkscreenpath([
      { x: -bodyEdgeX, y: bodyEdgeY },
      { x: bodyEdgeX, y: bodyEdgeY }
    ]),
    silkscreenpath([
      { x: -bodyEdgeX, y: -bodyEdgeY },
      { x: bodyEdgeX, y: -bodyEdgeY }
    ]),
    // pin 1 marker next to the top-left pad
    silkscreenpath([
      { x: -rowspan / 2 - pl / 2 - 0.4, y: p + pw / 2 },
      { x: -rowspan / 2 - pl / 2 - 0.4, y: p - pw / 2 }
    ])
  ];
  const ref = silkscreenRef(0, bodyEdgeY + 0.8, 0.5);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: rowspan + pl + 0.5,
    height: Math.max(h, 2 * p + pw) + 0.5,
    layer: "top"
  };
  return {
    circuitJson: [...pads, ...silkscreen, ref, courtyard],
    parameters
  };
};

// src/helpers/res0402-array2.ts
import { length as length8 } from "circuit-json";

// src/helpers/chipArray.ts
var chipArray = (params) => {
  const {
    padSpacing: padSpacing7,
    padWidth,
    padHeight,
    padPitch,
    numRows,
    textbottom,
    courtyardOutline
  } = params;
  const yPositions = [];
  const halfRange = (numRows - 1) * (padPitch / 2);
  for (let i = 0; i < numRows; i++) {
    yPositions.push(halfRange - i * padPitch);
  }
  const pads = [];
  yPositions.forEach((y, index) => {
    pads.push(rectpad(index + 1, -padSpacing7 / 2, y, padWidth, padHeight));
  });
  yPositions.slice().reverse().forEach((y, index) => {
    pads.push(
      rectpad(index + numRows + 1, padSpacing7 / 2, y, padWidth, padHeight)
    );
  });
  const top = Math.max(...yPositions) + padHeight / 2 + 0.4;
  const bottom = Math.min(...yPositions) - padHeight / 2 - 0.4;
  const left = -padSpacing7 / 2 - padWidth / 2 - 0.4;
  const right = padSpacing7 / 2 + padWidth / 2 + 0.4;
  const silkscreenTop = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: right * 0.5, y: top },
      { x: left * 0.5, y: top }
    ],
    stroke_width: 0.12,
    pcb_silkscreen_path_id: "silkscreen_top"
  };
  const silkscreenBottom = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: right * 0.5, y: bottom },
      { x: left * 0.5, y: bottom }
    ],
    stroke_width: 0.12,
    pcb_silkscreen_path_id: "silkscreen_bottom"
  };
  const pin1X = -padSpacing7 / 2;
  const pin1Y = Math.max(...yPositions);
  const pin1MarkerSize = 0.2;
  const pin1Left = pin1X - padWidth / 2 - 0.1;
  const pin1Top = pin1Y + padHeight / 2 + 0.1;
  const pin1Marker = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "pin1_marker",
    route: [
      { x: pin1Left, y: pin1Top },
      { x: pin1Left - pin1MarkerSize, y: pin1Top },
      { x: pin1Left, y: pin1Top + pin1MarkerSize },
      { x: pin1Left, y: pin1Top }
    ],
    stroke_width: 0.1
  };
  const textY = textbottom ? bottom - 0.9 : top + 0.9;
  const silkscreenRefText = silkscreenRef(0, textY, 0.2);
  const courtyard = courtyardOutline ? {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: courtyardOutline
  } : null;
  return [
    ...pads,
    silkscreenTop,
    silkscreenBottom,
    pin1Marker,
    silkscreenRefText,
    ...courtyard ? [courtyard] : []
  ];
};

// src/helpers/res0402-array2.ts
import { z as z10 } from "zod";
import mm4 from "@tscircuit/mm";
var res0402Array2_def = base_def.extend({
  pw: length8.default("0.5mm"),
  ph: length8.default("0.4mm"),
  p: length8.default("0.7mm"),
  textbottom: z10.boolean().optional(),
  convex: z10.boolean().optional(),
  concave: z10.boolean().optional()
});
var padSpacing = 1;
var res0402Array2 = (rawParams) => {
  const params = res0402Array2_def.parse(rawParams);
  if (params.concave === true) {
    return [];
  }
  const padWidth = mm4(params.pw);
  const padHeight = mm4(params.ph);
  const padPitch = mm4(params.p);
  return chipArray({
    padSpacing,
    padWidth,
    padHeight,
    padPitch,
    numRows: 2,
    textbottom: params.textbottom,
    convex: params.convex,
    concave: params.concave,
    courtyardOutline: [
      { x: -1, y: 0.95 },
      { x: -1, y: -0.95 },
      { x: 1, y: -0.95 },
      { x: 1, y: 0.95 }
    ]
  });
};

// src/helpers/res0402-array4.ts
import { length as length9 } from "circuit-json";
import { z as z11 } from "zod";
import mm5 from "@tscircuit/mm";
var res0402Array4_def = base_def.extend({
  pw: length9.default("0.5mm"),
  ph: length9.default("0.32mm"),
  p: length9.default("0.5mm"),
  textbottom: z11.boolean().optional(),
  convex: z11.boolean().optional(),
  concave: z11.boolean().optional()
});
var padSpacing2 = 1;
var res0402Array4 = (rawParams) => {
  const params = res0402Array4_def.parse(rawParams);
  const padWidth = mm5(params.pw);
  const padHeight = mm5(params.ph);
  const padPitch = mm5(params.p);
  return chipArray({
    padSpacing: padSpacing2,
    padWidth,
    padHeight,
    padPitch,
    numRows: 4,
    textbottom: params.textbottom,
    convex: params.convex,
    concave: params.concave,
    courtyardOutline: [
      { x: -1, y: 1.25 },
      { x: -1, y: -1.25 },
      { x: 1, y: -1.25 },
      { x: 1, y: 1.25 }
    ]
  });
};

// src/helpers/res0603-array2.ts
import { length as length10 } from "circuit-json";
import { z as z12 } from "zod";
import mm6 from "@tscircuit/mm";
var res0603Array2_def = base_def.extend({
  pw: length10.default("0.9mm"),
  ph: length10.default("0.4mm"),
  p: length10.default("0.8mm"),
  textbottom: z12.boolean().optional(),
  convex: z12.boolean().optional(),
  concave: z12.boolean().optional()
});
var padSpacing3 = 1.7;
var res0603Array2 = (rawParams) => {
  const params = res0603Array2_def.parse(rawParams);
  const padWidth = mm6(params.pw);
  const padHeight = mm6(params.ph);
  const padPitch = mm6(params.p);
  return chipArray({
    padSpacing: padSpacing3,
    padWidth,
    padHeight,
    padPitch,
    numRows: 2,
    textbottom: params.textbottom,
    convex: params.convex,
    concave: params.concave,
    courtyardOutline: [
      { x: -1.55, y: 1.05 },
      { x: -1.55, y: -1.05 },
      { x: 1.55, y: -1.05 },
      { x: 1.55, y: 1.05 }
    ]
  });
};

// src/helpers/res0603-array4.ts
import { length as length11 } from "circuit-json";
import { z as z13 } from "zod";
import mm7 from "@tscircuit/mm";
var res0603Array4_def = base_def.extend({
  pw: length11.default("0.9mm"),
  ph: length11.default("0.4mm"),
  p: length11.default("0.8mm"),
  textbottom: z13.boolean().optional(),
  convex: z13.boolean().optional(),
  concave: z13.boolean().optional()
});
var padSpacing4 = 1.7;
var res0603Array4 = (rawParams) => {
  const params = res0603Array4_def.parse(rawParams);
  const padWidth = mm7(params.pw);
  const padHeight = mm7(params.ph);
  const padPitch = mm7(params.p);
  return chipArray({
    padSpacing: padSpacing4,
    padWidth,
    padHeight,
    padPitch,
    numRows: 4,
    textbottom: params.textbottom,
    convex: params.convex,
    concave: params.concave,
    courtyardOutline: [
      { x: -1.55, y: 1.88 },
      { x: -1.55, y: -1.87 },
      { x: 1.55, y: -1.87 },
      { x: 1.55, y: 1.88 }
    ]
  });
};

// src/helpers/res0606-array2.ts
import { length as length12 } from "circuit-json";
import { z as z14 } from "zod";
import mm8 from "@tscircuit/mm";
var res0606Array2_def = base_def.extend({
  pw: length12.default("0.7mm"),
  ph: length12.default("0.64mm"),
  p: length12.default("0.94mm"),
  textbottom: z14.boolean().optional(),
  convex: z14.boolean().optional(),
  concave: z14.boolean().optional()
});
var padSpacing5 = 1.4;
var res0606Array2 = (rawParams) => {
  const params = res0606Array2_def.parse(rawParams);
  const padWidth = mm8(params.pw);
  const padHeight = mm8(params.ph);
  const padPitch = mm8(params.p);
  return chipArray({
    padSpacing: padSpacing5,
    padWidth,
    padHeight,
    padPitch,
    numRows: 2,
    textbottom: params.textbottom,
    convex: params.convex,
    concave: params.concave,
    courtyardOutline: [
      { x: -1.3, y: 1.05 },
      { x: -1.3, y: -1.05 },
      { x: 1.3, y: -1.05 },
      { x: 1.3, y: 1.05 }
    ]
  });
};

// src/helpers/res1206-array4.ts
import { length as length13 } from "circuit-json";
import { z as z15 } from "zod";
import mm9 from "@tscircuit/mm";
var res1206Array4_def = base_def.extend({
  pw: length13.default("0.9mm"),
  ph: length13.default("0.9mm"),
  p: length13.default("1.34mm"),
  textbottom: z15.boolean().optional(),
  convex: z15.boolean().optional(),
  concave: z15.boolean().optional()
});
var padSpacing6 = 3;
var res1206Array4 = (rawParams) => {
  const params = res1206Array4_def.parse(rawParams);
  const padWidth = mm9(params.pw);
  const padHeight = mm9(params.ph);
  const padPitch = mm9(params.p);
  return chipArray({
    padSpacing: padSpacing6,
    padWidth,
    padHeight,
    padPitch,
    numRows: 4,
    textbottom: params.textbottom,
    convex: params.convex,
    concave: params.concave,
    courtyardOutline: [
      { x: -2.21, y: 2.85 },
      { x: -2.21, y: -2.85 },
      { x: 2.2, y: -2.85 },
      { x: 2.2, y: 2.85 }
    ]
  });
};

// src/fn/res.ts
var getArrayCount = (parameters) => {
  const arrayValue = parameters.array ?? parameters.x;
  if (typeof arrayValue === "number") {
    return Number.isNaN(arrayValue) ? void 0 : arrayValue;
  }
  if (typeof arrayValue === "string") {
    const parsed = Number.parseInt(arrayValue, 10);
    return Number.isNaN(parsed) ? void 0 : parsed;
  }
  if (typeof parameters.imperial === "string") {
    const match = parameters.imperial.match(/x(2|4)$/);
    const count = match?.[1];
    if (count) {
      return Number.parseInt(count, 10);
    }
  }
  return void 0;
};
var getImperialBase = (imperial) => {
  if (!imperial) return void 0;
  const imperialString = typeof imperial === "number" ? `${imperial}` : imperial;
  return imperialString.split(/[_x]/)[0];
};
var res = (rawParameters) => {
  const arrayCount = getArrayCount(rawParameters);
  const imperialBase = getImperialBase(rawParameters.imperial);
  if (arrayCount === 2 && imperialBase === "0402") {
    return {
      circuitJson: res0402Array2(rawParameters),
      parameters: rawParameters
    };
  }
  if (arrayCount === 4 && imperialBase === "0402") {
    return {
      circuitJson: res0402Array4(rawParameters),
      parameters: rawParameters
    };
  }
  if (arrayCount === 2 && imperialBase === "0603") {
    return {
      circuitJson: res0603Array2(rawParameters),
      parameters: rawParameters
    };
  }
  if (arrayCount === 4 && imperialBase === "0603") {
    return {
      circuitJson: res0603Array4(rawParameters),
      parameters: rawParameters
    };
  }
  if (arrayCount === 2 && imperialBase === "0606") {
    return {
      circuitJson: res0606Array2(rawParameters),
      parameters: rawParameters
    };
  }
  if (arrayCount === 4 && imperialBase === "1206") {
    return {
      circuitJson: res1206Array4(rawParameters),
      parameters: rawParameters
    };
  }
  return { circuitJson: passive(rawParameters), parameters: rawParameters };
};

// src/helpers/circlepad.ts
var circlepad = (pn, { x, y, radius }) => {
  return {
    type: "pcb_smtpad",
    x,
    y,
    radius,
    layer: "top",
    shape: "circle",
    pcb_smtpad_id: "",
    port_hints: Array.isArray(pn) ? pn.map((item) => item.toString()) : [pn.toString()]
  };
};

// src/helpers/zod/ALPHABET.ts
var ALPHABET = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

// src/fn/bga.ts
import { z as z17 } from "zod";
import { length as length14, distance as distance2 } from "circuit-json";

// src/helpers/zod/dim-2d.ts
import { z as z16 } from "zod";
var dim2d = z16.string().transform((a) => {
  const [x, y] = a.split(/[x ]/);
  return {
    x: parseFloat(x ?? ""),
    y: parseFloat(y ?? "")
  };
}).pipe(
  z16.object({
    x: z16.number(),
    y: z16.number()
  })
);

// src/fn/bga.ts
import "circuit-json";
var bga_def = base_def.extend({
  fn: z17.string(),
  num_pins: z17.number().optional().default(64),
  grid: dim2d.optional(),
  p: distance2.default("0.8mm"),
  w: length14.optional(),
  h: length14.optional(),
  ball: length14.optional().describe("ball diameter"),
  pad: length14.optional().describe("pad width/height"),
  circularpads: z17.boolean().optional().describe("use circular pads"),
  tlorigin: z17.boolean().optional(),
  blorigin: z17.boolean().optional(),
  trorigin: z17.boolean().optional(),
  brorigin: z17.boolean().optional(),
  missing: function_call.default([])
}).transform((a) => {
  let origin = "tl";
  if (a.blorigin) origin = "bl";
  if (a.trorigin) origin = "tr";
  if (a.brorigin) origin = "br";
  if (!a.grid) {
    const largest_square = Math.ceil(Math.sqrt(a.num_pins));
    a.grid = { x: largest_square, y: largest_square };
  }
  if (a.missing) {
    a.missing = a.missing.map((s) => {
      if (typeof s === "number") return s;
      if (s === "center") return "center";
      if (s === "topleft") return "topleft";
      const m = s.match(/([A-Z]+)(\d+)/);
      if (!m) return s;
      const Y = ALPHABET.indexOf(m[1]);
      const X = Number.parseInt(m[2]) - 1;
      return Y * a.grid.x + X + 1;
    });
  }
  const new_def = { ...a, origin };
  return new_def;
});
var bga = (raw_params) => {
  const parameters = bga_def.parse(raw_params);
  let { num_pins, grid: grid2, p, w, h, ball, pad: pad2, missing } = parameters;
  ball ??= 0.75 / 1.27 * p;
  pad2 ??= ball * 0.8;
  const pads = [];
  const missing_pin_nums = (missing ?? []).filter((a) => typeof a === "number");
  const num_pins_missing = grid2.x * grid2.y - num_pins;
  if (missing.length === 0 && num_pins_missing > 0) {
    if (Math.sqrt(num_pins_missing) % 1 === 0) {
      missing.push("center");
    } else if (num_pins_missing === 1) {
      missing.push("topleft");
    }
  }
  if (missing?.includes("center")) {
    const square_size = Math.floor(Math.sqrt(num_pins_missing));
    const inner_square_x = Math.floor((grid2.x - square_size) / 2);
    const inner_square_y = Math.floor((grid2.y - square_size) / 2);
    for (let y = inner_square_y; y < inner_square_y + square_size; y++) {
      for (let x = inner_square_x; x < inner_square_x + square_size; x++) {
        missing_pin_nums.push(y * grid2.x + x + 1);
      }
    }
  }
  if (missing?.includes("topleft")) {
    missing_pin_nums.push(1);
  }
  const missing_pin_nums_set = new Set(missing_pin_nums);
  let missing_pins_passed = 0;
  for (let y = 0; y < grid2.y; y++) {
    for (let x = 0; x < grid2.x; x++) {
      const pad_x = (x - (grid2.x - 1) / 2) * p;
      const pad_y = (y - (grid2.y - 1) / 2) * p;
      let pin_x = x;
      let pin_y = y;
      switch (parameters.origin) {
        case "bl":
          pin_x = x;
          pin_y = grid2.y - 1 - y;
          break;
        case "br":
          pin_x = grid2.x - 1 - x;
          pin_y = grid2.y - 1 - y;
          break;
        case "tr":
          pin_x = grid2.x - 1 - x;
          pin_y = y;
          break;
        case "tl":
        default:
          break;
      }
      let pin_num = pin_y * grid2.x + pin_x + 1;
      if (missing_pin_nums_set.has(pin_num)) {
        missing_pins_passed++;
        continue;
      }
      pin_num -= missing_pins_passed;
      const portHints = [pin_num, `${ALPHABET[pin_y]}${pin_x + 1}`];
      pads.push(
        parameters.circularpads ? circlepad(portHints, {
          x: pad_x,
          y: pad_y,
          radius: pad2 / 2
        }) : rectpad(portHints, pad_x, pad_y, pad2, pad2)
      );
    }
  }
  const silkscreenRefText = silkscreenRef(
    0,
    grid2.y * p / 2,
    0.2
  );
  const pin1MarkerSize = p / 6;
  let markerRoute;
  const edgeX = grid2.x * p / 2;
  const edgeY = grid2.y * p / 2;
  switch (parameters.origin) {
    case "bl":
      markerRoute = [
        { x: -edgeX, y: -edgeY },
        // Start at corner
        { x: -edgeX, y: -edgeY - pin1MarkerSize },
        // Up
        { x: -edgeX - pin1MarkerSize, y: -edgeY },
        // Left
        { x: -edgeX, y: -edgeY }
        // Back to start
      ];
      break;
    case "br":
      markerRoute = [
        { x: edgeX, y: -edgeY },
        // Start at corner
        { x: edgeX, y: -edgeY - pin1MarkerSize },
        // Up
        { x: edgeX + pin1MarkerSize, y: -edgeY },
        // Right
        { x: edgeX, y: -edgeY }
        // Back to start
      ];
      break;
    case "tr":
      markerRoute = [
        { x: edgeX, y: edgeY },
        // Start at corner
        { x: edgeX, y: edgeY + pin1MarkerSize },
        // Down
        { x: edgeX + pin1MarkerSize, y: edgeY },
        // Right
        { x: edgeX, y: edgeY }
        // Back to start
      ];
      break;
    case "tl":
    default:
      markerRoute = [
        { x: -edgeX, y: edgeY },
        // Start at corner
        { x: -edgeX, y: edgeY + pin1MarkerSize },
        // Down
        { x: -edgeX - pin1MarkerSize, y: edgeY },
        // Left
        { x: -edgeX, y: edgeY }
        // Back to start
      ];
      break;
  }
  const pin1Marker = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "pin1_marker",
    route: markerRoute,
    stroke_width: 0.05
  };
  const padSpanX = (grid2.x - 1) * p + pad2;
  const padSpanY = (grid2.y - 1) * p + pad2;
  const bodySpanX = w ?? padSpanX;
  const bodySpanY = h ?? padSpanY;
  const courtyardEnvelopeHalfWidth = Math.max(padSpanX / 2, bodySpanX / 2);
  const courtyardEnvelopeHalfHeight = Math.max(padSpanY / 2, bodySpanY / 2);
  const courtyardClearanceX = 1.715;
  const courtyardClearanceY = 1.765;
  const courtyardStepOuterHalfWidth = courtyardEnvelopeHalfWidth + courtyardClearanceX;
  const courtyardStepInnerHalfWidth = courtyardStepOuterHalfWidth;
  const courtyardStepOuterHalfHeight = courtyardEnvelopeHalfHeight + courtyardClearanceY;
  const courtyardStepInnerHalfHeight = courtyardStepOuterHalfHeight;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ]),
    layer: "top"
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenRefText,
      pin1Marker,
      courtyard
    ],
    parameters
  };
};

// src/fn/soic.ts
import { z as z18 } from "zod";
import { length as length16 } from "circuit-json";

// src/helpers/pillpad.ts
var pillpad = (pn, x, y, w, h) => {
  return {
    type: "pcb_smtpad",
    x,
    y,
    width: w,
    height: h,
    radius: Math.min(w, h) / 2,
    layer: "top",
    shape: "pill",
    pcb_smtpad_id: "",
    port_hints: Array.isArray(pn) ? pn.map((item) => item.toString()) : [pn.toString()]
  };
};

// src/helpers/create-thermal-pad.ts
import { length as length15 } from "circuit-json";
var thermalPadOffsetFields = {
  thermalpadcenteroffsetx: length15.optional().default(0),
  thermalpadcenteroffsety: length15.optional().default(0)
};
var createThermalPad = (dimensions, offset) => rectpad(["thermalpad"], offset.x, offset.y, dimensions.x, dimensions.y);

// src/fn/soic.ts
var extendSoicDef = (newDefaults) => base_def.extend({
  fn: z18.string(),
  num_pins: z18.number().optional().default(8),
  w: length16.default(length16.parse(newDefaults.w ?? "5.3mm")),
  p: length16.default(length16.parse(newDefaults.p ?? "1.27mm")),
  pw: length16.default(length16.parse(newDefaults.pw ?? "0.6mm")),
  pl: length16.default(length16.parse(newDefaults.pl ?? "1.0mm")),
  legsoutside: z18.boolean().optional().default(newDefaults.legsoutside ?? false),
  pillpads: z18.boolean().optional().default(newDefaults.pillpads ?? false),
  thermalpad: dim2d.optional(),
  ...thermalPadOffsetFields,
  silkscreen_stroke_width: z18.number().optional().default(0.1)
}).transform((v) => {
  if (!v.pw && !v.pl) {
    v.pw = length16.parse("0.6mm");
    v.pl = length16.parse("1.0mm");
  } else if (!v.pw) {
    v.pw = v.pl * (0.6 / 1);
  } else if (!v.pl) {
    v.pl = v.pw * (1 / 0.6);
  }
  return v;
});
var soic_def = extendSoicDef({});
var getCcwSoicCoords = (parameters) => {
  if (parameters.widthincludeslegs !== void 0) {
    parameters.legsoutside = !parameters.widthincludeslegs;
  }
  const { num_pins, pn, w, p, pl, legsoutside } = parameters;
  const ph = num_pins / 2;
  const isLeft = pn <= ph;
  const leftPinGaps = ph - 1;
  const gs = p;
  const h = gs * leftPinGaps;
  const legoffset = legsoutside ? pl / 2 : -pl / 2;
  if (isLeft) {
    return { x: -w / 2 - legoffset, y: h / 2 - (pn - 1) * gs };
  }
  return { x: w / 2 + legoffset, y: -h / 2 + (pn - ph - 1) * gs };
};
var soic = (raw_params) => {
  const parameters = soic_def.parse(raw_params);
  return {
    circuitJson: soicWithoutParsing(parameters),
    parameters
  };
};
var soicWithoutParsing = (parameters) => {
  const pads = [];
  const cornerRadius = Math.min(parameters.pl, parameters.pw) / 8;
  let maxPadExtentX = 0;
  let maxPadExtentY = 0;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getCcwSoicCoords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w: parameters.w,
      p: parameters.p,
      pl: parameters.pl,
      legsoutside: parameters.legsoutside
    });
    maxPadExtentX = Math.max(maxPadExtentX, Math.abs(x) + parameters.pl / 2);
    maxPadExtentY = Math.max(maxPadExtentY, Math.abs(y) + parameters.pw / 2);
    if (parameters.pillpads) {
      pads.push(pillpad(i + 1, x, y, parameters.pl, parameters.pw));
    } else {
      pads.push(
        rectpad(i + 1, x, y, parameters.pl, parameters.pw, cornerRadius)
      );
    }
  }
  if (parameters.thermalpad) {
    pads.push(
      createThermalPad(parameters.thermalpad, {
        x: parameters.thermalpadcenteroffsetx,
        y: parameters.thermalpadcenteroffsety
      })
    );
  }
  const m = Math.min(1, parameters.p / 2);
  const sw = parameters.w - (parameters.legsoutside ? 0 : parameters.pl * 2) - 0.2;
  const sh = (parameters.num_pins / 2 - 1) * parameters.p + parameters.pw + m;
  const silkscreenRefText = silkscreenRef(
    0,
    sh / 2 + 0.4,
    sh / 12
  );
  const silkscreenBorder = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    stroke_width: parameters.silkscreen_stroke_width ?? 0.1,
    route: [
      { x: -sw / 2, y: -sh / 2 },
      { x: -sw / 2, y: sh / 2 },
      // Little U shape at the top
      ...u_curve.map(({ x, y }) => ({
        x: x * sw / 6,
        y: y * sw / 6 + sh / 2
      })),
      { x: sw / 2, y: sh / 2 },
      { x: sw / 2, y: -sh / 2 },
      { x: -sw / 2, y: -sh / 2 }
    ]
  };
  const bodyHalfWidth = parameters.w / 2;
  const bodyHalfHeight = sh / 2;
  const courtyardStepOuterHalfWidth = Math.max(maxPadExtentX, bodyHalfWidth) + 0.25;
  const courtyardStepInnerHalfHeight = Math.min(maxPadExtentY, bodyHalfHeight) + 0.25;
  const courtyardStepInnerHalfWidth = Math.min(maxPadExtentX, bodyHalfWidth) + 0.25;
  const courtyardStepOuterHalfHeight = Math.max(maxPadExtentY, bodyHalfHeight) + 0.25;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ])
  };
  return [
    ...pads,
    silkscreenBorder,
    silkscreenRefText,
    courtyard
  ];
};

// src/fn/quad.ts
import { length as length17 } from "circuit-json";

// src/helpers/get-quad-pin-map.ts
var getQuadPinMap = ({
  num_pins,
  sidePinCounts,
  cw,
  ccw,
  startingpin
}) => {
  const pin_map = [];
  const leftBottomPin = sidePinCounts.left;
  const bottomLeftPin = leftBottomPin + 1;
  const bottomRightPin = sidePinCounts.left + sidePinCounts.bottom;
  const rightBottomPin = bottomRightPin + 1;
  const rightTopPin = sidePinCounts.left + sidePinCounts.bottom + sidePinCounts.right;
  const topRightPin = rightTopPin + 1;
  let current_position_ccw_normal = 1;
  const sfp = {};
  for (const specifier of startingpin ?? []) {
    sfp[specifier] = true;
  }
  if (!sfp.leftside && !sfp.topside && !sfp.rightside && !sfp.bottomside) {
    sfp.leftside = true;
  }
  if (!sfp.bottompin && !sfp.leftpin && !sfp.rightpin && !sfp.toppin) {
    if (sfp.leftside) {
      sfp.toppin = true;
    } else if (sfp.topside) {
      sfp.rightpin = true;
    } else if (sfp.rightside) {
      sfp.bottompin = true;
    } else if (sfp.bottomside) {
      sfp.leftpin = true;
    }
  }
  if (sfp.leftside && sfp.toppin) {
    current_position_ccw_normal = 1;
  } else if (sfp.leftside && sfp.bottompin) {
    current_position_ccw_normal = leftBottomPin;
  } else if (sfp.bottomside && sfp.leftpin) {
    current_position_ccw_normal = bottomLeftPin;
  } else if (sfp.bottomside && sfp.rightpin) {
    current_position_ccw_normal = bottomRightPin;
  } else if (sfp.rightside && sfp.bottompin) {
    current_position_ccw_normal = rightBottomPin;
  } else if (sfp.rightside && sfp.toppin) {
    current_position_ccw_normal = rightTopPin;
  } else if (sfp.topside && sfp.rightpin) {
    current_position_ccw_normal = topRightPin;
  } else if (sfp.topside && sfp.leftpin) {
    current_position_ccw_normal = num_pins;
  }
  pin_map.push(-1);
  for (let i = 0; i < num_pins; i++) {
    pin_map[current_position_ccw_normal] = i + 1;
    if (ccw || !cw) {
      current_position_ccw_normal++;
      if (current_position_ccw_normal > num_pins) {
        current_position_ccw_normal = 1;
      }
    } else {
      current_position_ccw_normal--;
      if (current_position_ccw_normal < 1) {
        current_position_ccw_normal = num_pins;
      }
    }
  }
  return pin_map;
};

// src/helpers/get-quad-side-pin-counts.ts
var resolveSharedCount = (shortName, shortValue, longName, longValue) => {
  if (shortValue !== void 0 && longValue !== void 0 && shortValue !== longValue) {
    throw new Error(
      `Conflicting ${shortName} (${shortValue}) and ${longName} (${longValue})`
    );
  }
  return longValue ?? shortValue;
};
var getQuadSidePinCounts = (parameters) => {
  const leftRightPins = resolveSharedCount(
    "lrpins",
    parameters.lrpins,
    "leftrightpins",
    parameters.leftrightpins
  );
  const topBottomPins = resolveSharedCount(
    "tbpins",
    parameters.tbpins,
    "topbottompins",
    parameters.topbottompins
  );
  const defaultSidePinCount = parameters.num_pins / 4;
  const sidePinCounts = {
    left: parameters.leftpins ?? leftRightPins ?? defaultSidePinCount,
    top: parameters.toppins ?? topBottomPins ?? defaultSidePinCount,
    right: parameters.rightpins ?? leftRightPins ?? defaultSidePinCount,
    bottom: parameters.bottompins ?? topBottomPins ?? defaultSidePinCount
  };
  const hasExplicitSidePinCounts = [
    parameters.leftpins,
    parameters.toppins,
    parameters.rightpins,
    parameters.bottompins,
    parameters.lrpins,
    parameters.leftrightpins,
    parameters.tbpins,
    parameters.topbottompins
  ].some((value) => value !== void 0);
  if (hasExplicitSidePinCounts) {
    const entries = Object.entries(sidePinCounts);
    const invalidEntry = entries.find(
      ([, value]) => !Number.isInteger(value) || value < 1
    );
    if (invalidEntry) {
      throw new Error(
        `Quad ${invalidEntry[0]} pin count must be a positive integer, got ${invalidEntry[1]}`
      );
    }
    const total = entries.reduce((sum, [, value]) => sum + value, 0);
    if (total !== parameters.num_pins) {
      throw new Error(
        `Quad side pin counts left=${sidePinCounts.left}, top=${sidePinCounts.top}, right=${sidePinCounts.right}, bottom=${sidePinCounts.bottom} require ${total} pins, got ${parameters.num_pins}`
      );
    }
  }
  return sidePinCounts;
};

// src/helpers/zod/pin-order-specifier.ts
import { z as z19 } from "zod";
var pin_order_specifier = z19.enum([
  "leftside",
  "topside",
  "rightside",
  "bottomside",
  "toppin",
  "bottompin",
  "leftpin",
  "rightpin"
]);

// src/fn/quad.ts
import { z as z20 } from "zod";
var side_pin_count = z20.coerce.number().int().positive().optional();
var base_quad_def = base_def.extend({
  fn: z20.string(),
  cc: z20.boolean().default(true).optional(),
  ccw: z20.boolean().default(true).optional(),
  startingpin: z20.string().or(z20.array(pin_order_specifier)).transform((a) => typeof a === "string" ? a.slice(1, -1).split(",") : a).pipe(z20.array(pin_order_specifier)).optional(),
  num_pins: z20.number().optional().default(64),
  leftpins: side_pin_count,
  toppins: side_pin_count,
  rightpins: side_pin_count,
  bottompins: side_pin_count,
  lrpins: side_pin_count,
  leftrightpins: side_pin_count,
  tbpins: side_pin_count,
  topbottompins: side_pin_count,
  w: length17.optional(),
  h: length17.optional(),
  p: length17.default(length17.parse("0.5mm")),
  px: length17.optional().describe("top and bottom side pad pitch"),
  py: length17.optional().describe("left and right side pad pitch"),
  pw: length17.optional(),
  pl: length17.optional(),
  thermalpad: z20.union([z20.literal(true), dim2d]).optional(),
  ...thermalPadOffsetFields,
  pillpads: z20.boolean().optional().default(false),
  legsoutside: z20.boolean().default(false)
});
var quadTransform = (v) => {
  if (v.w && !v.h) {
    v.h = v.w;
  } else if (!v.w && v.h) {
    v.w = v.h;
  }
  const sidePinCounts = getQuadSidePinCounts(v);
  const horizontal_side_pin_count = Math.max(
    sidePinCounts.top,
    sidePinCounts.bottom
  );
  const vertical_side_pin_count = Math.max(
    sidePinCounts.left,
    sidePinCounts.right
  );
  const horizontal_pitch = v.px ?? v.p;
  const vertical_pitch = v.py ?? v.p;
  if (!v.p && !v.pw && !v.pl && v.w) {
    const approx_pin_size_of_side = horizontal_side_pin_count + 4;
    v.p = v.w / approx_pin_size_of_side;
  }
  if (!v.p && v.w && v.h && v.pw && v.pl) {
    const horizontalPitch = (v.w - v.pl * 2) / Math.max(horizontal_side_pin_count - 1, 1);
    const verticalPitch = (v.h - v.pl * 2) / Math.max(vertical_side_pin_count - 1, 1);
    v.p = (horizontalPitch + verticalPitch) / 2;
  }
  if (!v.w && !v.h && v.p) {
    v.w = horizontal_pitch * (horizontal_side_pin_count + 4);
    v.h = vertical_pitch * (vertical_side_pin_count + 4);
  }
  if (v.p && !v.pw && !v.pl) {
    v.pw = v.p / 2;
    v.pl = v.p / 2;
  } else if (!v.pw) {
    v.pw = v.pl * (0.6 / 1);
  } else if (!v.pl) {
    v.pl = v.pw * (1 / 0.6);
  }
  return v;
};
var quad_def = base_quad_def.transform(quadTransform);
var SIDES_CCW = ["left", "bottom", "right", "top"];
var getQuadCoords = (params) => {
  const { sidePinCounts, pn, w, h, p, px, py, pl, legsoutside } = params;
  const sidePinCountsCcw = [
    sidePinCounts.left,
    sidePinCounts.bottom,
    sidePinCounts.right,
    sidePinCounts.top
  ];
  let sideIndex = 0;
  let pos = pn - 1;
  while (sideIndex < sidePinCountsCcw.length - 1 && pos >= sidePinCountsCcw[sideIndex]) {
    pos -= sidePinCountsCcw[sideIndex];
    sideIndex += 1;
  }
  const sidePinCount = sidePinCountsCcw[sideIndex];
  const side = SIDES_CCW[sideIndex];
  const sidePitch = side === "left" || side === "right" ? py ?? p : px ?? p;
  const ibw = sidePitch * (sidePinCount - 1);
  const ibh = sidePitch * (sidePinCount - 1);
  const pcdfe = legsoutside ? pl / 2 : -pl / 2;
  switch (side) {
    case "left":
      return {
        x: -w / 2 - pcdfe + 0.1,
        y: ibh / 2 - pos * sidePitch,
        o: "vert"
      };
    case "bottom":
      return {
        x: -ibw / 2 + pos * sidePitch,
        y: -h / 2 - pcdfe + 0.1,
        o: "horz"
      };
    case "right":
      return {
        x: w / 2 + pcdfe - 0.1,
        y: -ibh / 2 + pos * sidePitch,
        o: "vert"
      };
    case "top":
      return {
        x: ibw / 2 - pos * sidePitch,
        y: h / 2 + pcdfe - 0.1,
        o: "horz"
      };
    default:
      throw new Error("Invalid pin number");
  }
};
var quad = (raw_params) => {
  const parameters = quad_def.parse(raw_params);
  const sidePinCounts = getQuadSidePinCounts(parameters);
  const pads = [];
  let padOuterHalfX = 0;
  let padOuterHalfY = 0;
  const pin_map = getQuadPinMap({ ...parameters, sidePinCounts });
  const verticalSidePinCount = Math.max(sidePinCounts.left, sidePinCounts.right);
  const horizontalSidePinCount = Math.max(
    sidePinCounts.top,
    sidePinCounts.bottom
  );
  const leftBottomPin = sidePinCounts.left;
  const bottomLeftPin = leftBottomPin + 1;
  const bottomRightPin = sidePinCounts.left + sidePinCounts.bottom;
  const rightBottomPin = bottomRightPin + 1;
  const rightTopPin = sidePinCounts.left + sidePinCounts.bottom + sidePinCounts.right;
  const topRightPin = rightTopPin + 1;
  const topLeftPin = parameters.num_pins;
  for (let i = 0; i < parameters.num_pins; i++) {
    const {
      x,
      y,
      o: orientation2
    } = getQuadCoords({
      sidePinCounts,
      pin_count: parameters.num_pins,
      pn: i + 1,
      w: parameters.w,
      h: parameters.h,
      p: parameters.p ?? 0.5,
      px: parameters.px,
      py: parameters.py,
      pl: parameters.pl,
      legsoutside: parameters.legsoutside
    });
    let padWidth = parameters.pw;
    let padHeight = parameters.pl;
    if (orientation2 === "vert") {
      ;
      [padWidth, padHeight] = [padHeight, padWidth];
    }
    const cornerRadius = Math.min(padWidth, padHeight) / 8;
    const pn = pin_map[i + 1];
    padOuterHalfX = Math.max(padOuterHalfX, Math.abs(x) + padWidth / 2);
    padOuterHalfY = Math.max(padOuterHalfY, Math.abs(y) + padHeight / 2);
    pads.push(
      parameters.pillpads ? pillpad(pn, x, y, padWidth, padHeight) : rectpad(pn, x, y, padWidth, padHeight, cornerRadius)
    );
  }
  if (parameters.thermalpad) {
    const thermalPadOffset = {
      x: parameters.thermalpadcenteroffsetx,
      y: parameters.thermalpadcenteroffsety
    };
    if (typeof parameters.thermalpad === "boolean") {
      const ibw = (parameters.px ?? parameters.p) * (horizontalSidePinCount - 1) + parameters.pw;
      const ibh = (parameters.py ?? parameters.p) * (verticalSidePinCount - 1) + parameters.pw;
      padOuterHalfX = Math.max(
        padOuterHalfX,
        Math.abs(thermalPadOffset.x) + ibw / 2
      );
      padOuterHalfY = Math.max(
        padOuterHalfY,
        Math.abs(thermalPadOffset.y) + ibh / 2
      );
      pads.push(createThermalPad({ x: ibw, y: ibh }, thermalPadOffset));
    } else {
      padOuterHalfX = Math.max(
        padOuterHalfX,
        Math.abs(thermalPadOffset.x) + parameters.thermalpad.x / 2
      );
      padOuterHalfY = Math.max(
        padOuterHalfY,
        Math.abs(thermalPadOffset.y) + parameters.thermalpad.y / 2
      );
      pads.push(createThermalPad(parameters.thermalpad, thermalPadOffset));
    }
  }
  const silkscreen_corners = [];
  for (const [corner, dx, dy] of [
    ["top-left", -1, 1],
    ["bottom-left", -1, -1],
    ["bottom-right", 1, -1],
    ["top-right", 1, 1]
  ]) {
    const corner_x = parameters.w / 2 * dx;
    const corner_y = parameters.h / 2 * dy;
    let arrow = "none";
    let arrow_x = corner_x;
    let arrow_y = corner_y;
    const csz = parameters.pw * 2;
    if (pin_map[1] === 1 && corner === "top-left") {
      arrow = "in1";
    } else if (pin_map[topLeftPin] === 1 && corner === "top-left") {
      arrow = "in2";
    } else if (pin_map[topRightPin] === 1 && corner === "top-right") {
      arrow = "in2";
    } else if (pin_map[rightTopPin] === 1 && corner === "top-right") {
      arrow = "in1";
    } else if (pin_map[leftBottomPin] === 1 && corner === "bottom-left") {
      arrow = "in1";
    } else if (pin_map[bottomLeftPin] === 1 && corner === "bottom-left") {
      arrow = "in2";
    } else if (pin_map[bottomRightPin] === 1 && corner === "bottom-right") {
      arrow = "in1";
    } else if (pin_map[rightBottomPin] === 1 && corner === "bottom-right") {
      arrow = "in2";
    }
    const rotate_arrow = arrow === "in1" ? 1 : -1;
    if (parameters.legsoutside) {
      const arrow_dx = arrow === "in1" ? parameters.pl / 2 : parameters.pw / 2;
      const arrow_dy = arrow === "in1" ? parameters.pw / 2 : parameters.pl / 2;
      arrow_x += arrow_dx * dx * rotate_arrow;
      arrow_y -= arrow_dy * dy * rotate_arrow;
    }
    if (arrow === "none" || parameters.legsoutside) {
      silkscreen_corners.push({
        layer: "top",
        pcb_component_id: "",
        pcb_silkscreen_path_id: `pcb_silkscreen_path_${corner}`,
        route: [
          {
            x: corner_x - csz * dx,
            y: corner_y
          },
          {
            x: corner_x,
            y: corner_y
          },
          {
            x: corner_x,
            y: corner_y - csz * dy
          }
        ],
        type: "pcb_silkscreen_path",
        stroke_width: 0.1
      });
    }
    if ((arrow === "in1" || arrow === "in2") && !parameters.legsoutside) {
      silkscreen_corners.push(
        {
          layer: "top",
          pcb_component_id: "",
          pcb_silkscreen_path_id: `pcb_silkscreen_path_${corner}_1`,
          route: [
            {
              x: corner_x - csz * dx,
              y: corner_y
            },
            {
              x: corner_x - csz * dx / 2,
              y: corner_y
            }
          ],
          type: "pcb_silkscreen_path",
          stroke_width: 0
        },
        {
          layer: "top",
          pcb_component_id: "",
          pcb_silkscreen_path_id: `pcb_silkscreen_path_${corner}_2`,
          route: [
            {
              x: corner_x,
              y: corner_y - csz * dy / 2
            },
            {
              x: corner_x,
              y: corner_y - csz * dy
            }
          ],
          type: "pcb_silkscreen_path",
          stroke_width: 0.1
        }
      );
    }
    if (arrow === "in1" || arrow === "in2") {
      silkscreen_corners.push({
        layer: "top",
        pcb_component_id: "",
        pcb_silkscreen_path_id: `pcb_silkscreen_path_${corner}_3`,
        route: [
          {
            x: arrow_x - 0.2 * -dx,
            y: arrow_y + 0.2 * rotate_arrow
          },
          {
            x: arrow_x,
            y: arrow_y
          },
          {
            x: arrow_x + 0.2 * rotate_arrow * -dx,
            y: arrow_y + 0.2
          },
          {
            x: arrow_x - 0.2 * -dx,
            y: arrow_y + 0.2 * rotate_arrow
          }
        ],
        type: "pcb_silkscreen_path",
        stroke_width: 0.1
      });
    }
  }
  const silkscreenRefText = silkscreenRef(
    0,
    parameters.h / 2 + (parameters.legsoutside ? parameters.pl * 1.2 : 0.5),
    0.3
  );
  const roundUpToCourtyardOuterGrid = (value) => Math.ceil(value / 0.05) * 0.05;
  const pinRowSpanX = (horizontalSidePinCount - 1) * (parameters.px ?? parameters.p) + parameters.pw;
  const pinRowSpanY = (verticalSidePinCount - 1) * (parameters.py ?? parameters.p) + parameters.pw;
  const courtyardStepInnerHalfWidth = pinRowSpanX / 2 + 0.25;
  const courtyardStepInnerHalfHeight = pinRowSpanY / 2 + 0.25;
  const courtyardStepOuterHalfWidth = parameters.w / 2 + 0.25;
  const courtyardStepOuterHalfHeight = parameters.h / 2 + 0.25;
  const courtyardOuterHalfWidth = Math.max(
    courtyardStepOuterHalfWidth,
    roundUpToCourtyardOuterGrid(padOuterHalfX + 0.25)
  );
  const courtyardOuterHalfHeight = Math.max(
    courtyardStepOuterHalfHeight,
    roundUpToCourtyardOuterGrid(padOuterHalfY + 0.25)
  );
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardOuterHalfWidth,
        maxX: courtyardOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardOuterHalfHeight,
        maxY: courtyardOuterHalfHeight
      }
    ])
  };
  return {
    circuitJson: [
      ...pads,
      ...silkscreen_corners,
      silkscreenRefText,
      courtyard
    ],
    parameters
  };
};

// src/fn/qfn.ts
var qfn_def = base_quad_def.extend({}).transform(quadTransform);
var qfn = (parameters) => {
  parameters.legsoutside = false;
  if (!parameters.pl) {
    parameters.pl = 0.875;
  }
  if (!parameters.pw) {
    parameters.pw = 0.25;
  }
  return quad(parameters);
};

// src/fn/tqfp.ts
var tqfp = (raw_params) => {
  raw_params.legsoutside = true;
  if (!raw_params.p) {
    switch (raw_params.num_pins) {
      case 32:
        raw_params.p = 0.8;
        break;
      case 44:
      case 48:
        raw_params.p = 0.5;
        break;
      case 64:
      case 80:
      case 100:
        raw_params.p = 0.5;
        break;
      case 144:
        raw_params.p = 0.5;
        break;
    }
  }
  if (!raw_params.pl) {
    switch (raw_params.num_pins) {
      case 32:
      case 44:
      case 48:
      case 64:
      case 80:
      case 100:
        raw_params.pl = 1.475;
        break;
      case 144:
        raw_params.pl = 1.6;
        break;
    }
  }
  if (!raw_params.pw) {
    switch (raw_params.num_pins) {
      case 32:
        raw_params.pw = 0.55;
        break;
      case 44:
      case 48:
      case 64:
      case 80:
      case 100:
        raw_params.pw = 0.3;
        break;
      case 144:
        raw_params.pw = 0.25;
        break;
    }
  }
  return quad(raw_params);
};

// src/fn/qfp.ts
var qfp = (raw_params) => {
  raw_params.legsoutside = true;
  const quad_defaults = quad_def.parse(raw_params);
  if (!raw_params.p) {
    switch (raw_params.num_pins) {
      case 44:
      case 64:
        raw_params.p = 0.8;
        break;
      case 52:
        if (raw_params.w === 14) raw_params.p = 1;
        else raw_params.p = 0.65;
        break;
      case 208:
        raw_params.p = 0.5;
        break;
    }
  }
  if (!raw_params.pl) {
    switch (raw_params.num_pins) {
      case 44:
      case 52:
      case 64:
        raw_params.pl = 2.25;
        break;
      case 208:
        raw_params.pl = 1.65;
        break;
      default:
        raw_params.pl = quad_defaults.pl * 4;
        break;
    }
  }
  if (!raw_params.pw) {
    switch (raw_params.num_pins) {
      case 44:
      case 64:
        raw_params.pw = 0.5;
        break;
      case 52:
        if (raw_params.w === 14) raw_params.pw = 0.45;
        else raw_params.pw = 0.55;
        break;
      case 208:
        raw_params.pw = 0.3;
        break;
      default:
        raw_params.pw = quad_defaults.pw;
        break;
    }
  }
  return quad(raw_params);
};

// src/fn/mlp.ts
var mlp_def = base_quad_def.extend({}).transform(quadTransform);
var mlp = (parameters) => {
  parameters.legsoutside = false;
  if (parameters.thermalpad === void 0) {
    parameters.thermalpad = true;
  }
  return quad(parameters);
};

// src/fn/ssop.ts
import { length as length18 } from "circuit-json";
import { z as z21 } from "zod";
var ssop_def = base_def.extend({
  fn: z21.string(),
  num_pins: z21.number().optional().default(8),
  w: length18.default(length18.parse("3.9mm")),
  p: length18.default(length18.parse("1.27mm")),
  pw: length18.optional(),
  pl: length18.optional(),
  legsoutside: z21.boolean().optional().default(false),
  thermalpad: dim2d.optional(),
  ...thermalPadOffsetFields,
  silkscreen_stroke_width: z21.number().optional().default(0.1)
}).transform((v) => {
  if (v.pw == null && v.pl == null) {
    v.pw = length18.parse("0.51mm");
    v.pl = length18.parse("1.1mm");
  } else if (v.pw == null) {
    v.pw = v.pl * (0.51 / 1.1);
  } else if (v.pl == null) {
    v.pl = v.pw * (1.1 / 0.51);
  }
  return v;
});
var getSsopCoords = (parameters) => {
  const { num_pins, pn, w, p } = parameters;
  const ph = num_pins / 2;
  const isLeft = pn <= ph;
  const leftPinGaps = ph - 1;
  const gs = p;
  const h = gs * leftPinGaps;
  const padRowSpan = w + length18.parse("0.2mm");
  const x = (isLeft ? -1 : 1) * (padRowSpan / 2);
  if (isLeft) {
    return { x, y: h / 2 - (pn - 1) * gs };
  }
  return { x, y: -h / 2 + (pn - ph - 1) * gs };
};
var ssop = (raw_params) => {
  const parameters = ssop_def.parse(raw_params);
  const pads = [];
  const cornerRadius = Math.min(parameters.pl, parameters.pw) / 8;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getSsopCoords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w: parameters.w,
      p: parameters.p,
      pl: parameters.pl,
      legsoutside: parameters.legsoutside
    });
    pads.push(rectpad(i + 1, x, y, parameters.pl, parameters.pw, cornerRadius));
  }
  if (parameters.thermalpad) {
    pads.push(
      createThermalPad(parameters.thermalpad, {
        x: parameters.thermalpadcenteroffsetx,
        y: parameters.thermalpadcenteroffsety
      })
    );
  }
  const m = Math.min(1, parameters.p / 2);
  const sw = parameters.w - (parameters.legsoutside ? 0 : parameters.pl) - 0.2;
  const sh = (parameters.num_pins / 2 - 1) * parameters.p + parameters.pw + m;
  const silkscreenRefText = silkscreenRef(
    0,
    sh / 2 + 0.4,
    sh / 12
  );
  const silkscreenBorder = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    stroke_width: parameters.silkscreen_stroke_width ?? 0.1,
    route: [
      { x: -sw / 2, y: -sh / 2 },
      { x: -sw / 2, y: sh / 2 },
      ...u_curve.map(({ x, y }) => ({
        x: x * sw / 6,
        y: y * sw / 6 + sh / 2
      })),
      { x: sw / 2, y: sh / 2 },
      { x: sw / 2, y: -sh / 2 },
      { x: -sw / 2, y: -sh / 2 }
    ]
  };
  const pinRowSpanY = (parameters.num_pins / 2 - 1) * parameters.p + parameters.pw;
  const padToeHalfX = parameters.legsoutside ? parameters.w / 2 + parameters.pl : (parameters.w + 0.2) / 2 + parameters.pl / 2;
  const pinRowHalfY = pinRowSpanY / 2;
  const courtyardStepOuterHalfWidth = padToeHalfX + 0.25;
  const courtyardStepInnerHalfWidth = courtyardStepOuterHalfWidth;
  const courtyardStepOuterHalfHeight = pinRowHalfY + 0.445;
  const courtyardStepInnerHalfHeight = courtyardStepOuterHalfHeight;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ]),
    layer: "top"
  };
  return {
    circuitJson: [...pads, silkscreenBorder, silkscreenRefText, courtyard],
    parameters
  };
};

// src/fn/tssop.ts
import { length as length19 } from "circuit-json";
import { z as z22 } from "zod";
var tssop_def = base_def.extend({
  fn: z22.string(),
  num_pins: z22.number().optional().default(8),
  w: length19.default(length19.parse("3.0mm")),
  p: length19.default(length19.parse("0.5mm")),
  pw: length19.default(length19.parse("0.30mm")),
  pl: length19.default(length19.parse("1.45mm")),
  legsoutside: z22.boolean().optional().default(true),
  thermalpad: dim2d.optional(),
  ...thermalPadOffsetFields,
  silkscreen_stroke_width: z22.number().optional().default(0.1)
});
var getTssopCoords = (parameters) => {
  const { num_pins, pn, w, p, pl, legsoutside } = parameters;
  const ph = num_pins / 2;
  const isLeft = pn <= ph;
  const leftPinGaps = ph - 1;
  const h = p * leftPinGaps;
  const legoffset = legsoutside ? pl / 2 : -pl / 2;
  if (isLeft) {
    return { x: -w / 2 - legoffset, y: h / 2 - (pn - 1) * p };
  }
  return { x: w / 2 + legoffset, y: -h / 2 + (pn - ph - 1) * p };
};
var tssop = (raw_params) => {
  const params = { ...raw_params };
  const pRaw = params.p;
  const pValue = typeof pRaw === "string" || typeof pRaw === "number" ? length19.parse(pRaw) : void 0;
  const isFinePitch = pValue != null && pValue <= length19.parse("0.5mm");
  const parameters = tssop_def.parse(params);
  const pads = [];
  const cornerRadius = parameters.rounded ?? Math.min(parameters.pl, parameters.pw) / 8;
  const wForPads = isFinePitch ? parameters.w - length19.parse("0.15mm") : parameters.w;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getTssopCoords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w: wForPads,
      p: parameters.p,
      pl: parameters.pl,
      legsoutside: parameters.legsoutside
    });
    pads.push(rectpad(i + 1, x, y, parameters.pl, parameters.pw, cornerRadius));
  }
  if (parameters.thermalpad) {
    pads.push(
      createThermalPad(parameters.thermalpad, {
        x: parameters.thermalpadcenteroffsetx,
        y: parameters.thermalpadcenteroffsety
      })
    );
  }
  const m = Math.min(1, parameters.p / 2);
  const sw = parameters.w - (parameters.legsoutside ? 0 : parameters.pl * 2) - 0.2;
  const sh = (parameters.num_pins / 2 - 1) * parameters.p + parameters.pw + m;
  const silkscreenRefText = silkscreenRef(
    0,
    sh / 2 + 0.4,
    sh / 12
  );
  const silkscreenBorder = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    stroke_width: parameters.silkscreen_stroke_width ?? 0.1,
    route: [
      { x: -sw / 2, y: -sh / 2 },
      { x: -sw / 2, y: sh / 2 },
      ...u_curve.map(({ x, y }) => ({
        x: x * sw / 6,
        y: y * sw / 6 + sh / 2
      })),
      { x: sw / 2, y: sh / 2 },
      { x: sw / 2, y: -sh / 2 },
      { x: -sw / 2, y: -sh / 2 }
    ]
  };
  const pinRowSpanY = (parameters.num_pins / 2 - 1) * parameters.p + parameters.pw;
  const pinToeHalfSpanX = parameters.w / 2 + (parameters.legsoutside ? parameters.pl : 0);
  const courtyardStepInnerHalfWidth = parameters.w / 2 + 0.25;
  const courtyardStepOuterHalfWidth = pinToeHalfSpanX + 0.18;
  const courtyardStepInnerHalfHeight = pinRowSpanY / 2 + 0.25;
  const courtyardStepOuterHalfHeight = courtyardStepInnerHalfHeight + 0.35;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ])
  };
  return {
    circuitJson: [...pads, silkscreenBorder, silkscreenRefText, courtyard],
    parameters
  };
};

// src/fn/sot363.ts
import { z as z23 } from "zod";
import { length as length20 } from "circuit-json";
var sot363CourtyardOutline = [
  { x: -1.45, y: 0.93 },
  { x: -0.73, y: 0.93 },
  { x: -0.73, y: 1.1 },
  { x: 0.73, y: 1.1 },
  { x: 0.73, y: 0.93 },
  { x: 1.45, y: 0.93 },
  { x: 1.45, y: -0.93 },
  { x: 0.73, y: -0.93 },
  { x: 0.73, y: -1.1 },
  { x: -0.73, y: -1.1 },
  { x: -0.73, y: -0.93 },
  { x: -1.45, y: -0.93 }
];
var sot363_def = base_def.extend({
  fn: z23.string(),
  num_pins: z23.literal(6).default(6),
  w: z23.string().default("3.1mm"),
  h: z23.string().default("2.0mm"),
  p: z23.string().default("0.65mm"),
  pl: z23.string().default("1.03mm"),
  pw: z23.string().default("0.35mm"),
  string: z23.string().optional()
});
var sot363 = (raw_params) => {
  const parameters = sot363_def.parse({ ...raw_params, fn: "sot363" });
  const w = length20.parse(parameters.w);
  const h = length20.parse(parameters.h);
  const p = length20.parse(parameters.p);
  const pl = length20.parse(parameters.pl);
  const pw = length20.parse(parameters.pw);
  const cornerRadius = Math.min(pl, pw) / 8;
  const pads = [];
  for (let i = 0; i < 6; i++) {
    const { x, y } = getSot363PadCoord(i + 1, w, p, pl);
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  const silkscreenTopLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -w / 4, y: h / 2 + 0.1 },
      { x: w / 4, y: h / 2 + 0.1 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBottomLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -w / 4, y: -h / 2 - 0.1 },
      { x: w / 4, y: -h / 2 - 0.1 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const pin1Position = getSot363PadCoord(1, w, p, pl);
  const pin1Marking = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "pin_marker_1",
    route: [
      { x: pin1Position.x - pl / 2 - 0.3, y: pin1Position.y },
      { x: pin1Position.x - pl / 2 - 0.45, y: pin1Position.y + 0.15 },
      { x: pin1Position.x - pl / 2 - 0.45, y: pin1Position.y - 0.15 },
      { x: pin1Position.x - pl / 2 - 0.3, y: pin1Position.y }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: "pin_marker_1"
  };
  const silkscreenRefText = silkscreenRef(0, h / 2 + 0.4, 0.25);
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot363CourtyardOutline,
    layer: "top"
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenTopLine,
      silkscreenBottomLine,
      silkscreenRefText,
      pin1Marking,
      courtyard
    ],
    parameters
  };
};
var getSot363PadCoord = (pn, w, p, pl) => {
  const padCenterOffset = w / 2 - pl / 2;
  if (pn <= 3) {
    return { x: -0.84, y: p - (pn - 1) * p };
  }
  return { x: 0.84, y: -p + (pn - 4) * p };
};

// src/fn/sot886.ts
import { z as z24 } from "zod";
import { length as length21 } from "circuit-json";
var sot886CourtyardOutline = [
  { x: -0.75, y: 1 },
  { x: -0.75, y: -1 },
  { x: 0.75, y: -1 },
  { x: 0.75, y: 1 }
];
var sot886_def = base_def.extend({
  fn: z24.string(),
  num_pins: z24.literal(6).default(6),
  w: z24.string().default("1.01mm"),
  h: z24.string().default("1.45mm"),
  p: z24.string().default("0.5mm"),
  pl: z24.string().default("0.33mm"),
  pw: z24.string().default("0.27mm"),
  string: z24.string().optional()
});
var sot886 = (raw_params) => {
  const parameters = sot886_def.parse({ ...raw_params, fn: "sot886" });
  const w = length21.parse(parameters.w);
  const h = length21.parse(parameters.h);
  const p = length21.parse(parameters.p);
  const pl = length21.parse(parameters.pl);
  const pw = length21.parse(parameters.pw);
  const cornerRadius = Math.min(pl, pw) / 8;
  const pads = [];
  for (let i = 0; i < 6; i++) {
    const { x, y } = getSot886PadCoord(i + 1, w, p, pl);
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  const silkscreenTopLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -w / 2, y: h / 2 },
      { x: w / 2, y: h / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBottomLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -w / 2, y: -h / 2 },
      { x: w / 2, y: -h / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const pin1Position = getSot886PadCoord(1, w, p, pl);
  const pin1Marking = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "pin_marker_1",
    route: [
      { x: pin1Position.x - pl / 2 - 0.3, y: pin1Position.y },
      { x: pin1Position.x - pl / 2 - 0.45, y: pin1Position.y + 0.15 },
      { x: pin1Position.x - pl / 2 - 0.45, y: pin1Position.y - 0.15 },
      { x: pin1Position.x - pl / 2 - 0.3, y: pin1Position.y }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: "pin_marker_1"
  };
  const silkscreenRefText = silkscreenRef(0, h / 2 + 0.4, 0.25);
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot886CourtyardOutline,
    layer: "top"
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenTopLine,
      silkscreenBottomLine,
      silkscreenRefText,
      pin1Marking,
      courtyard
    ],
    parameters
  };
};
var getSot886PadCoord = (pn, w, p, pl) => {
  const padCenterOffset = w / 2 - pl / 2;
  if (pn <= 3) {
    return { x: -padCenterOffset, y: p - (pn - 1) * p };
  }
  return { x: padCenterOffset, y: -p + (pn - 4) * p };
};

// src/fn/sot23.ts
import { z as z25 } from "zod";
var sot23_3CourtyardOutline = [
  { x: -2.05, y: 1.5 },
  { x: -1.05, y: 1.5 },
  { x: -1.05, y: 1.7 },
  { x: 1.05, y: 1.7 },
  { x: 1.05, y: 0.55 },
  { x: 2.05, y: 0.55 },
  { x: 2.05, y: -0.55 },
  { x: 1.05, y: -0.55 },
  { x: 1.05, y: -1.7 },
  { x: -1.05, y: -1.7 },
  { x: -1.05, y: -1.5 },
  { x: -2.05, y: -1.5 },
  { x: -2.05, y: -0.39 },
  { x: -1.05, y: -0.39 },
  { x: -1.05, y: 0.39 },
  { x: -2.05, y: 0.39 }
];
var sot23_5CourtyardOutline = [
  { x: -2.05, y: 1.5 },
  { x: -1.05, y: 1.5 },
  { x: -1.05, y: 1.7 },
  { x: 1.05, y: 1.7 },
  { x: 1.05, y: 1.5 },
  { x: 2.05, y: 1.5 },
  { x: 2.05, y: 0.39 },
  { x: 1.05, y: 0.39 },
  { x: 1.05, y: -0.39 },
  { x: 2.05, y: -0.39 },
  { x: 2.05, y: -1.5 },
  { x: 1.05, y: -1.5 },
  { x: 1.05, y: -1.7 },
  { x: -1.05, y: -1.7 },
  { x: -1.05, y: -1.5 },
  { x: -2.05, y: -1.5 }
];
var sot23_def = base_def.extend({
  fn: z25.string(),
  num_pins: z25.number().default(3),
  w: z25.string().default("2.275mm"),
  h: z25.string().default("2.74mm"),
  pl: z25.string().default("1.325mm"),
  pw: z25.string().default("0.6mm"),
  p: z25.string().default("0.95mm"),
  string: z25.string().optional()
});
var sot23_6_or_8_def = extendSoicDef({
  p: "0.95mm",
  w: "1.6mm",
  legsoutside: true
});
var sot23 = (raw_params) => {
  const match = raw_params.string?.match(/^sot23_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 10) : 3;
  if (numPins === 6 || numPins === 8) {
    const parameters2 = sot23_6_or_8_def.parse({
      ...raw_params,
      num_pins: numPins
    });
    return {
      circuitJson: soicWithoutParsing(parameters2),
      parameters: parameters2
    };
  }
  const parameters = sot23_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  if (parameters.num_pins === 3) {
    return {
      circuitJson: sot23_3(parameters),
      parameters
    };
  }
  if (parameters.num_pins === 5) {
    return {
      circuitJson: sot23_5(parameters),
      parameters
    };
  }
  throw new Error("Invalid number of pins");
};
var getCcwSot23Coords = (parameters) => {
  const { pn, w, h, pl, p } = parameters;
  if (pn === 1) {
    return { x: -w / 2, y: p };
  }
  if (pn === 2) {
    return { x: -w / 2, y: -p };
  }
  return { x: w / 2, y: 0 };
};
var sot23_3 = (parameters) => {
  const pads = [];
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  const pl = Number.parseFloat(parameters.pl);
  const pw = Number.parseFloat(parameters.pw);
  const p = Number.parseFloat(parameters.p);
  const cornerRadius = parameters.rounded ?? Math.min(pl, pw) / 8;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getCcwSot23Coords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w,
      h,
      pl,
      p
    });
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  const silkscreenRefText = silkscreenRef(
    0,
    Number.parseInt(parameters.h),
    0.3
  );
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot23_3CourtyardOutline,
    layer: "top"
  };
  return [...pads, silkscreenRefText, courtyard];
};
var getCcwSot235Coords = (parameters) => {
  const { w, p, h, pn } = parameters;
  if (pn === 1) {
    return { x: -w / 2, y: p };
  }
  if (pn === 2) {
    return { x: -w / 2, y: 0 };
  }
  if (pn === 3) {
    return { x: -w / 2, y: -p };
  }
  if (pn === 4) {
    return { x: w / 2, y: -p };
  }
  if (pn === 5) {
    return { x: w / 2, y: p };
  }
  throw new Error("Invalid pin number");
};
var sot23_5 = (parameters) => {
  const pads = [];
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  const p = Number.parseFloat(parameters.p);
  const pl = Number.parseFloat(parameters.pl);
  const pw = Number.parseFloat(parameters.pw);
  const cornerRadius = parameters.rounded ?? Math.min(pl, pw) / 8;
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getCcwSot235Coords({
      w,
      h,
      p,
      pn: i
    });
    pads.push(rectpad(i, x, y, pl, pw, cornerRadius));
  }
  const width = (parameters.num_pins + 1) / 2 * p;
  const height = h;
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width / 3, y: height / 2 + p / 1.3 },
      { x: width / 3, y: height / 2 + p / 1.3 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.05
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width / 3, y: -height / 2 - p / 1.3 },
      { x: width / 3, y: -height / 2 - p / 1.3 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.05
  };
  const silkscreenRefText = silkscreenRef(0, height + 0.3, 0.3);
  const pin1Position = getCcwSot235Coords({ w, h, p, pn: 1 });
  pin1Position.x = pin1Position.x - pw * 1.5;
  const triangleHeight = 0.3;
  const triangleWidth = 0.4;
  const pin1Indicator = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "pin1_indicator",
    route: [
      {
        x: pin1Position.x + triangleHeight / 2,
        y: pin1Position.y
      },
      // Tip of the triangle (pointing right)
      {
        x: pin1Position.x - triangleHeight / 2,
        y: pin1Position.y + triangleWidth / 2
      },
      // Bottom corner of the base
      {
        x: pin1Position.x - triangleHeight / 2,
        y: pin1Position.y - triangleWidth / 2
      },
      // Top corner of the base
      {
        x: pin1Position.x + triangleHeight / 2,
        y: pin1Position.y
      }
      // Close the path at the tip
    ],
    stroke_width: 0.05
  };
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot23_5CourtyardOutline,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenRefText,
    silkscreenPath1,
    silkscreenPath2,
    pin1Indicator,
    courtyard
  ];
};

// src/fn/sot25.ts
import "zod";
var sot25_def = sot23_def.extend({});
var sot25 = (raw_params) => {
  const parameters = sot25_def.parse({
    ...raw_params,
    num_pins: 5
  });
  return {
    circuitJson: sot23_5(parameters),
    parameters
  };
};

// src/fn/dfn.ts
import { length as length22 } from "circuit-json";
import { z as z27 } from "zod";

// src/helpers/corner.ts
var CORNERS = [
  { name: "top-left", dx: -1, dy: 1 },
  { name: "top-right", dx: 1, dy: 1 },
  { name: "bottom-left", dx: -1, dy: -1 },
  { name: "bottom-right", dx: 1, dy: -1 }
];

// src/helpers/polygonpad.ts
var polygonpad = (pn, points) => {
  return {
    type: "pcb_smtpad",
    points,
    layer: "top",
    shape: "polygon",
    pcb_smtpad_id: "",
    port_hints: Array.isArray(pn) ? pn.map((item) => item.toString()) : [pn.toString()]
  };
};

// src/fn/dfn.ts
var dfn_def = extendSoicDef({});
var dfn = (raw_params) => {
  const cornerpads = z27.boolean().optional().parse(raw_params.cornerpads);
  const cornerpadcutlength = raw_params.cornerpadcutlength === void 0 ? void 0 : length22.parse(raw_params.cornerpadcutlength);
  const missing = function_call.parse(raw_params.missing ?? []);
  if (missing.some((position) => typeof position !== "number")) {
    throw new Error("DFN missing positions must be pad numbers");
  }
  const missingPositions = [...new Set(missing)];
  if (missingPositions.length !== missing.length) {
    throw new Error("DFN missing positions must not contain duplicates");
  }
  const parameters = {
    ...dfn_def.parse(raw_params),
    cornerpads,
    cornerpadcutlength,
    missing: missingPositions
  };
  const nominalPinCount = parameters.num_pins;
  if (missingPositions.some(
    (position) => position < 1 || position > nominalPinCount
  )) {
    throw new Error("DFN missing position is outside the nominal pad range");
  }
  if (cornerpads) {
    if (nominalPinCount !== 4 || missingPositions.length > 0 || parameters.pillpads) {
      throw new Error(
        "DFN corner pads require four nominal pads with none missing"
      );
    }
    if (cornerpadcutlength === void 0 || cornerpadcutlength <= 0 || cornerpadcutlength > Math.min(parameters.pl, parameters.pw)) {
      throw new Error("DFN corner pads require a valid cornerpadcutlength");
    }
  }
  const missingPositionSet = new Set(missingPositions);
  const pads = [];
  const cornerRadius = Math.min(parameters.pl, parameters.pw) / 8;
  let maxPadExtentY = 0;
  let outputPinNumber = 1;
  for (let nominalPinNumber = 1; nominalPinNumber <= nominalPinCount; nominalPinNumber += 1) {
    if (missingPositionSet.has(nominalPinNumber)) continue;
    const { x, y } = getCcwSoicCoords({
      num_pins: nominalPinCount,
      pn: nominalPinNumber,
      w: parameters.w,
      p: parameters.p ?? 1.27,
      pl: parameters.pl,
      widthincludeslegs: true
    });
    maxPadExtentY = Math.max(maxPadExtentY, Math.abs(y) + parameters.pw / 2);
    if (cornerpads) {
      const xDirection = Math.sign(x);
      const yDirection = Math.sign(y);
      const innerX = x - xDirection * parameters.pl / 2;
      const innerY = y - yDirection * parameters.pw / 2;
      const outerX = x + xDirection * parameters.pl / 2;
      const outerY = y + yDirection * parameters.pw / 2;
      pads.push(
        polygonpad(outputPinNumber, [
          { x: innerX, y: innerY + yDirection * cornerpadcutlength },
          { x: innerX, y: outerY },
          { x: outerX, y: outerY },
          { x: outerX, y: innerY },
          { x: innerX + xDirection * cornerpadcutlength, y: innerY }
        ])
      );
    } else {
      pads.push(
        (parameters.pillpads ? pillpad : rectpad)(
          outputPinNumber,
          x,
          y,
          parameters.pl ?? "1mm",
          parameters.pw ?? "0.6mm",
          cornerRadius
        )
      );
    }
    outputPinNumber += 1;
  }
  if (parameters.thermalpad) {
    pads.push(
      createThermalPad(parameters.thermalpad, {
        x: parameters.thermalpadcenteroffsetx,
        y: parameters.thermalpadcenteroffsety
      })
    );
  }
  const m = Math.min(1, parameters.p / 2);
  const sw = parameters.w + m;
  const sh = maxPadExtentY * 2 + m;
  const silkscreenPaths = [];
  for (const corner of CORNERS) {
    const { dx, dy } = corner;
    silkscreenPaths.push({
      layer: "top",
      pcb_component_id: "",
      pcb_silkscreen_path_id: "",
      route: [
        { x: dx * sw / 2 - dx * parameters.p, y: dy * sh / 2 },
        { x: dx * sw / 2, y: dy * sh / 2 },
        { x: dx * sw / 2, y: dy * sh / 2 - dy * parameters.p }
      ],
      type: "pcb_silkscreen_path",
      stroke_width: 0.1
    });
  }
  const as = parameters.p / 4;
  const atx = -sw / 2 - as / 2;
  const aty = sh / 2 - parameters.p / 2;
  silkscreenPaths.push({
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "",
    type: "pcb_silkscreen_path",
    route: [
      {
        x: atx,
        y: aty
      },
      {
        x: atx - as,
        y: aty + as
      },
      {
        x: atx - as,
        y: aty - as
      },
      {
        x: atx,
        y: aty
      }
    ],
    stroke_width: 0.1
  });
  const silkscreenRefText = silkscreenRef(
    0,
    sh / 2 + 0.4,
    sh / 12
  );
  const roundUpToCourtyardGrid = (value) => Math.ceil(value / 0.05) * 0.05;
  const courtyardHalfWidthMm = roundUpToCourtyardGrid(parameters.w / 2 + 0.25);
  const courtyardHalfHeightMm = roundUpToCourtyardGrid(maxPadExtentY + 0.45);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardHalfWidthMm * 2,
    height: courtyardHalfHeightMm * 2,
    layer: "top"
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenRefText,
      ...silkscreenPaths,
      courtyard
    ],
    parameters
  };
};

// src/fn/pinrow.ts
import "@tscircuit/mm";
import {
  length as length23
} from "circuit-json";

// src/helpers/determine-pin-label-anchor-side.ts
function determinePinlabelAnchorSide({
  pinlabeltextalignleft,
  pinlabeltextalignright,
  pinlabelorthogonal
}) {
  let pinlabelAnchorSide = "top";
  if (pinlabelorthogonal) {
    pinlabelAnchorSide = "left";
  } else {
    pinlabelAnchorSide = "top";
  }
  if (pinlabeltextalignleft) {
    if (pinlabelorthogonal) {
      pinlabelAnchorSide = "bottom";
    } else {
      pinlabelAnchorSide = "right";
    }
  }
  if (pinlabeltextalignright) {
    if (pinlabelorthogonal) {
      pinlabelAnchorSide = "top";
    } else {
      pinlabelAnchorSide = "left";
    }
  }
  return pinlabelAnchorSide;
}

// src/helpers/silkscreenPin.ts
var silkscreenPin = ({
  fs,
  pn,
  anchor_x,
  anchor_y,
  textalign = "center",
  anchorplacement,
  orthogonal = false,
  verticallyinverted = false,
  layer = "top"
}) => {
  let ccw_rotation = 0;
  if (orthogonal && verticallyinverted) {
    ccw_rotation = 270;
  } else if (verticallyinverted) {
    ccw_rotation = 180;
  } else if (orthogonal) {
    ccw_rotation = 90;
  } else {
    ccw_rotation = 0;
  }
  let anchor_alignment = "center";
  if (textalign !== "center") {
    const fallbackAlignment = textalign === "left" ? verticallyinverted ? "center_right" : "center_left" : verticallyinverted ? "center_left" : "center_right";
    anchor_alignment = fallbackAlignment;
    if (anchorplacement) {
      const placementDirection = {
        top: { x: 0, y: 1 },
        bottom: { x: 0, y: -1 },
        left: { x: -1, y: 0 },
        right: { x: 1, y: 0 }
      }[anchorplacement];
      const rotationRadians = ccw_rotation * Math.PI / 180;
      const textDirection = {
        x: Math.cos(rotationRadians),
        y: Math.sin(rotationRadians)
      };
      const directionDotProduct = placementDirection.x * textDirection.x + placementDirection.y * textDirection.y;
      if (Math.abs(directionDotProduct) > 0.5) {
        anchor_alignment = directionDotProduct > 0 ? "center_left" : "center_right";
      }
    }
  }
  if (layer === "bottom") {
    if (anchor_alignment === "center_left") {
      anchor_alignment = "center_right";
    } else if (anchor_alignment === "center_right") {
      anchor_alignment = "center_left";
    }
  }
  return {
    type: "pcb_silkscreen_text",
    pcb_silkscreen_text_id: "silkscreen_text_1",
    font: "tscircuit2024",
    font_size: fs,
    pcb_component_id: "pcb_component_1",
    text: `{PIN${pn}}`,
    layer,
    anchor_position: { x: anchor_x, y: anchor_y },
    anchor_alignment,
    ccw_rotation
  };
};

// src/fn/pinrow.ts
import { z as z28 } from "zod";
var pinrow_def = base_def.extend({
  fn: z28.string(),
  num_pins: z28.number().optional().default(6),
  rows: z28.union([z28.string(), z28.number()]).transform((val) => Number(val)).optional().default(1).describe("number of rows"),
  cols: z28.union([z28.string(), z28.number()]).transform((val) => Number(val)).optional().describe("number of nominal columns in a sparse grid"),
  p: length23.default("0.1in").describe("pitch"),
  py: length23.optional().describe("vertical row pitch"),
  id: length23.default("1.0mm").describe("inner diameter"),
  od: length23.default("1.5mm").describe("outer diameter"),
  missing: function_call.default([]).describe("row-major nominal grid positions to omit"),
  male: z28.boolean().optional().describe("for male pin headers"),
  female: z28.boolean().optional().describe("for female pin headers"),
  smd: z28.boolean().optional().describe("surface mount device"),
  surfacemount: z28.boolean().optional().describe("surface mount device (verbose)"),
  rightangle: z28.boolean().optional().describe("right angle"),
  pw: length23.optional().default("1.0mm").describe("pad width for SMD"),
  pl: length23.optional().default("2.0mm").describe("pad length for SMD"),
  pinlabeltextalignleft: z28.boolean().optional().default(false),
  pinlabeltextaligncenter: z28.boolean().optional().default(false),
  pinlabeltextalignright: z28.boolean().optional().default(false),
  pinlabelverticallyinverted: z28.boolean().optional().default(false),
  pinlabelorthogonal: z28.boolean().optional().default(false),
  nosquareplating: z28.boolean().optional().default(false).describe("do not use rectangular pad for pin 1"),
  nopinlabels: z28.boolean().optional().default(false).describe("omit silkscreen pin labels"),
  doublesidedpinlabel: z28.boolean().optional().default(false).describe("add silkscreen pins in top and bottom layers"),
  bottomsidepinlabel: z28.boolean().optional().default(false).describe(
    "place the silkscreen reference text on the bottom layer instead of top"
  ),
  silkscreenborder: z28.boolean().optional().default(false).describe("add a rectangular silkscreen border around the pin row"),
  silkscreenlabel: z28.string().transform(
    (value) => value.startsWith("(") && value.endsWith(")") ? value.slice(1, -1) : value
  ).optional().describe(
    "replace the reference designator with a custom silkscreen label"
  )
}).transform((data) => {
  const pinlabelAnchorSide = determinePinlabelAnchorSide(data);
  return {
    ...data,
    pinlabelAnchorSide,
    male: data.male ?? !data.female,
    female: data.female ?? false,
    smd: data.smd ?? data.surfacemount ?? false,
    rightangle: data.rightangle ?? false
  };
}).superRefine((data, ctx) => {
  if (data.male && data.female) {
    ctx.addIssue({
      code: z28.ZodIssueCode.custom,
      message: "'male' and 'female' cannot both be true; it should be male or female.",
      path: ["male", "female"]
    });
  }
  if (data.cols !== void 0 && (!Number.isInteger(data.cols) || data.cols < 1)) {
    ctx.addIssue({
      code: z28.ZodIssueCode.custom,
      message: "'cols' must be a positive integer",
      path: ["cols"]
    });
  }
  if (data.missing.some((position) => typeof position !== "number")) {
    ctx.addIssue({
      code: z28.ZodIssueCode.custom,
      message: "'missing' positions must be pad numbers",
      path: ["missing"]
    });
  }
});
var pinrow = (raw_params) => {
  const parameters = pinrow_def.parse(raw_params);
  const {
    p,
    py,
    id,
    od,
    rows,
    cols,
    num_pins,
    missing,
    pinlabelAnchorSide,
    pinlabelverticallyinverted,
    pinlabelorthogonal,
    pinlabeltextalignleft,
    pinlabeltextalignright,
    nopinlabels,
    doublesidedpinlabel,
    bottomsidepinlabel,
    silkscreenborder,
    silkscreenlabel
  } = parameters;
  let pinlabelTextAlign = "center";
  if (pinlabeltextalignleft) pinlabelTextAlign = "left";
  else if (pinlabeltextalignright) pinlabelTextAlign = "right";
  const holes = [];
  let pin1Position = null;
  const missingPositions = missing;
  const uniqueMissingPositions = new Set(missingPositions);
  if (uniqueMissingPositions.size !== missingPositions.length) {
    throw new Error("Pinrow missing positions must not contain duplicates");
  }
  const nominalPinCount = num_pins + missingPositions.length;
  const numPinsPerRow = cols ?? Math.ceil(nominalPinCount / rows);
  const gridPositionCount = numPinsPerRow * rows;
  const usesExplicitGrid = cols !== void 0 || missingPositions.length > 0;
  if (usesExplicitGrid && gridPositionCount !== nominalPinCount) {
    throw new Error(
      `Pinrow grid has ${gridPositionCount} positions, but ${nominalPinCount} are required for ${num_pins} pins and ${missingPositions.length} missing positions`
    );
  }
  if (missingPositions.some(
    (position) => !Number.isInteger(position) || position < 1 || position > gridPositionCount
  )) {
    throw new Error("Pinrow missing position is outside the nominal grid");
  }
  const rowPitch = py ?? p;
  const pinRowSpanY = (rows - 1) * rowPitch;
  const yStart = pinRowSpanY / 2;
  const ySpacing = -rowPitch;
  const calculateAnchorPosition = ({
    xoff,
    yoff,
    od: od2,
    anchorSide,
    smd,
    pw,
    pl
  }) => {
    let dx = 0;
    let dy = 0;
    if (smd) {
      const offset = od2 / 5;
      switch (anchorSide) {
        case "right":
          dx = pw / 2 + offset;
          break;
        case "top":
          dy = pl / 2 + offset;
          break;
        case "bottom":
          dy = -(pl / 2 + offset);
          break;
        case "left":
          dx = -(pw / 2 + offset);
          break;
      }
    } else {
      const offset = od2 / 2 + 0.6;
      switch (anchorSide) {
        case "right":
          dx = offset;
          break;
        case "top":
          dy = offset;
          break;
        case "bottom":
          dy = -offset;
          break;
        case "left":
          dx = -offset;
          break;
      }
    }
    return { anchor_x: xoff + dx, anchor_y: yoff + dy };
  };
  const addPin = (pinNumber, xoff, yoff) => {
    if (pinNumber === 1) pin1Position = { x: xoff, y: yoff };
    if (parameters.smd) {
      holes.push(rectpad(pinNumber, xoff, yoff, parameters.pw, parameters.pl));
    } else {
      if (pinNumber === 1 && !parameters.nosquareplating) {
        holes.push(
          platedHoleWithRectPad({
            pn: pinNumber,
            x: xoff,
            y: yoff,
            holeDiameter: id,
            rectPadWidth: od,
            rectPadHeight: od
          })
        );
      } else {
        holes.push(platedhole(pinNumber, xoff, yoff, id, od));
      }
    }
    const { anchor_x, anchor_y } = calculateAnchorPosition({
      xoff,
      yoff,
      od,
      anchorSide: pinlabelAnchorSide,
      smd: parameters.smd,
      pw: parameters.pw,
      pl: parameters.pl
    });
    if (!nopinlabels) {
      if (!bottomsidepinlabel) {
        holes.push(
          silkscreenPin({
            fs: od / 5,
            pn: pinNumber,
            anchor_x,
            anchor_y,
            anchorplacement: pinlabelAnchorSide,
            textalign: pinlabelTextAlign,
            orthogonal: pinlabelorthogonal,
            verticallyinverted: pinlabelverticallyinverted,
            layer: "top"
          })
        );
      } else {
        holes.push(
          silkscreenPin({
            fs: od / 5,
            pn: pinNumber,
            anchor_x,
            anchor_y,
            anchorplacement: pinlabelAnchorSide,
            textalign: pinlabelTextAlign,
            orthogonal: pinlabelorthogonal,
            verticallyinverted: pinlabelverticallyinverted,
            layer: "bottom"
          })
        );
      }
      if (doublesidedpinlabel) {
        holes.push(
          silkscreenPin({
            fs: od / 5,
            pn: pinNumber,
            anchor_x,
            anchor_y,
            anchorplacement: pinlabelAnchorSide,
            textalign: pinlabelTextAlign,
            orthogonal: pinlabelorthogonal,
            verticallyinverted: pinlabelverticallyinverted,
            layer: "bottom"
          })
        );
      }
    }
  };
  const usedPositions = /* @__PURE__ */ new Set();
  if (usesExplicitGrid) {
    const xStart = -((numPinsPerRow - 1) / 2) * p;
    let outputPinNumber = 1;
    for (let row = 0; row < rows; row++) {
      for (let col = 0; col < numPinsPerRow; col++) {
        const nominalPosition = row * numPinsPerRow + col + 1;
        if (uniqueMissingPositions.has(nominalPosition)) continue;
        const xoff = xStart + col * p;
        const yoff = yStart + row * ySpacing;
        const posKey = `${xoff},${yoff}`;
        if (usedPositions.has(posKey)) throw new Error(`Overlap at ${posKey}`);
        usedPositions.add(posKey);
        addPin(outputPinNumber++, xoff, yoff);
      }
    }
  } else if (rows === 1) {
    const xStart = -((num_pins - 1) / 2) * p;
    for (let i = 0; i < num_pins; i++) {
      const pinNumber = i + 1;
      const xoff = xStart + i * p;
      const posKey = `${xoff},${0}`;
      if (usedPositions.has(posKey)) throw new Error(`Overlap at ${posKey}`);
      usedPositions.add(posKey);
      addPin(pinNumber, xoff, 0);
    }
  } else {
    const useBGAStyle = rows > 2 && numPinsPerRow > 2;
    if (useBGAStyle) {
      const xStart = -((numPinsPerRow - 1) / 2) * p;
      let currentPin = 1;
      for (let row = 0; row < rows && currentPin <= num_pins; row++) {
        for (let col = 0; col < numPinsPerRow && currentPin <= num_pins; col++) {
          const xoff = xStart + col * p;
          const yoff = yStart + row * ySpacing;
          const posKey = `${xoff},${yoff}`;
          if (usedPositions.has(posKey)) throw new Error(`Overlap at ${posKey}`);
          usedPositions.add(posKey);
          addPin(currentPin++, xoff, yoff);
        }
      }
    } else {
      const xStart = -((numPinsPerRow - 1) / 2) * p;
      let currentPin = 1;
      let top = 0;
      let bottom = rows - 1;
      let left = 0;
      let right = numPinsPerRow - 1;
      while (currentPin <= num_pins && top <= bottom && left <= right) {
        for (let row = top; row <= bottom && currentPin <= num_pins; row++) {
          const xoff = xStart + left * p;
          const yoff = yStart + row * ySpacing;
          const posKey = `${xoff},${yoff}`;
          if (usedPositions.has(posKey)) throw new Error(`Overlap at ${posKey}`);
          usedPositions.add(posKey);
          addPin(currentPin++, xoff, yoff);
        }
        left++;
        for (let col = left; col <= right && currentPin <= num_pins; col++) {
          const xoff = xStart + col * p;
          const yoff = yStart + bottom * ySpacing;
          const posKey = `${xoff},${yoff}`;
          if (usedPositions.has(posKey)) throw new Error(`Overlap at ${posKey}`);
          usedPositions.add(posKey);
          addPin(currentPin++, xoff, yoff);
        }
        bottom--;
        if (left <= right) {
          for (let row = bottom; row >= top && currentPin <= num_pins; row--) {
            const xoff = xStart + right * p;
            const yoff = yStart + row * ySpacing;
            const posKey = `${xoff},${yoff}`;
            if (usedPositions.has(posKey))
              throw new Error(`Overlap at ${posKey}`);
            usedPositions.add(posKey);
            addPin(currentPin++, xoff, yoff);
          }
          right--;
        }
        if (top <= bottom) {
          for (let col = right; col >= left && currentPin <= num_pins; col--) {
            const xoff = xStart + col * p;
            const yoff = yStart + top * ySpacing;
            const posKey = `${xoff},${yoff}`;
            if (usedPositions.has(posKey))
              throw new Error(`Overlap at ${posKey}`);
            usedPositions.add(posKey);
            addPin(currentPin++, xoff, yoff);
          }
          top++;
        }
      }
      if (currentPin - 1 < num_pins) {
        throw new Error(
          `Missing pins: assigned ${currentPin - 1}, expected ${num_pins}`
        );
      }
    }
  }
  const padHalfWidth = parameters.smd ? parameters.pw / 2 : od / 2;
  const padHalfHeight = parameters.smd ? parameters.pl / 2 : od / 2;
  const pinRowSpanX = (numPinsPerRow - 1) * p;
  const silkscreenHalfWidth = pinRowSpanX / 2 + p / 2 + 1;
  const silkscreenHalfHeight = Math.max(
    pinRowSpanY / 2 + padHalfHeight + 1,
    p / 2 + 1
  );
  const silkscreenBorder = silkscreenborder ? silkscreenpath([
    { x: -silkscreenHalfWidth, y: -silkscreenHalfHeight },
    { x: silkscreenHalfWidth, y: -silkscreenHalfHeight },
    { x: silkscreenHalfWidth, y: silkscreenHalfHeight },
    { x: -silkscreenHalfWidth, y: silkscreenHalfHeight },
    { x: -silkscreenHalfWidth, y: -silkscreenHalfHeight }
  ]) : null;
  const pin1Arrow = (() => {
    if (parameters.fn !== "headermodule" || !pin1Position) return null;
    const arrowSize = Math.max(0.3, Math.min(0.6, p / 4));
    const clearance = 0.15;
    const horizontal = pinlabelAnchorSide === "top" || pinlabelAnchorSide === "bottom";
    if (horizontal) {
      const direction2 = Math.sign(pin1Position.x) || (pinlabelAnchorSide === "top" ? -1 : 1);
      const tipX = pin1Position.x + direction2 * (padHalfWidth + clearance);
      const baseX = tipX + direction2 * arrowSize;
      return silkscreenpath(
        [
          { x: tipX, y: pin1Position.y },
          { x: baseX, y: pin1Position.y - arrowSize },
          { x: baseX, y: pin1Position.y + arrowSize },
          { x: tipX, y: pin1Position.y }
        ],
        {
          pcb_component_id: "pin_marker_1",
          pcb_silkscreen_path_id: "pin_marker_1"
        }
      );
    }
    const direction = Math.sign(pin1Position.y) || 1;
    const tipY = pin1Position.y + direction * (padHalfHeight + clearance);
    const baseY = tipY + direction * arrowSize;
    return silkscreenpath(
      [
        { x: pin1Position.x, y: tipY },
        { x: pin1Position.x - arrowSize, y: baseY },
        { x: pin1Position.x + arrowSize, y: baseY },
        { x: pin1Position.x, y: tipY }
      ],
      {
        pcb_component_id: "pin_marker_1",
        pcb_silkscreen_path_id: "pin_marker_1"
      }
    );
  })();
  const refText = silkscreenlabel ? {
    type: "pcb_silkscreen_text",
    pcb_silkscreen_text_id: "silkscreen_text_1",
    font: "tscircuit2024",
    font_size: 0.8,
    pcb_component_id: "pcb_component_1",
    text: silkscreenlabel,
    layer: "top",
    anchor_position: { x: 0, y: 0 },
    anchor_alignment: "center"
  } : silkscreenRef(0, pinRowSpanY / 2 + p, 0.5);
  const padOuterHalfWidth = pinRowSpanX / 2 + padHalfWidth;
  const padOuterHalfHeight = pinRowSpanY / 2 + padHalfHeight;
  const bodyHalfWidth = pinRowSpanX / 2 + p / 2;
  const bodyHalfHeight = pinRowSpanY / 2 + p / 2;
  const courtyardHalfWidth = Math.max(
    padOuterHalfWidth + 0.25,
    bodyHalfWidth + 0.5
  );
  const courtyardHalfHeight = Math.max(
    padOuterHalfHeight + 0.25,
    bodyHalfHeight + 0.5
  );
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: 2 * courtyardHalfWidth,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  return {
    circuitJson: [
      ...holes,
      ...silkscreenBorder ? [silkscreenBorder] : [],
      ...pin1Arrow ? [pin1Arrow] : [],
      refText,
      courtyard
    ],
    parameters
  };
};

// src/fn/headermodule.ts
var headermodule = (raw_params) => pinrow({ ...raw_params, fn: "headermodule" });

// src/fn/sot563.ts
import { z as z29 } from "zod";
import { length as length24 } from "circuit-json";
var sot563CourtyardOutline = [
  { x: -1.2, y: 0.83 },
  { x: -0.75, y: 0.83 },
  { x: -0.75, y: 0.95 },
  { x: 0.75, y: 0.95 },
  { x: 0.75, y: 0.83 },
  { x: 1.2, y: 0.83 },
  { x: 1.2, y: -0.83 },
  { x: 0.75, y: -0.83 },
  { x: 0.75, y: -0.95 },
  { x: -0.75, y: -0.95 },
  { x: -0.75, y: -0.83 },
  { x: -1.2, y: -0.83 }
];
var sot563_def = base_def.extend({
  fn: z29.string(),
  num_pins: z29.literal(6).default(6),
  w: z29.string().default("2.1mm"),
  h: z29.string().default("2.45mm"),
  p: z29.string().default("0.5mm"),
  pl: z29.string().default("0.675mm"),
  pw: z29.string().default("0.35mm"),
  string: z29.string().optional()
});
var sot563 = (raw_params) => {
  const parameters = sot563_def.parse({ ...raw_params, fn: "sot563" });
  const w = length24.parse(parameters.w);
  const h = length24.parse(parameters.h);
  const p = length24.parse(parameters.p);
  const pl = length24.parse(parameters.pl);
  const pw = length24.parse(parameters.pw);
  const cornerRadius = Math.min(pl, pw) / 8;
  const pads = [];
  for (let i = 0; i < 6; i++) {
    const { x, y } = getSot563PadCoord(i + 1, w, p, pl);
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  const silkscreenTopLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -w / 2, y: h / 2 },
      { x: w / 2, y: h / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBottomLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -w / 2, y: -h / 2 },
      { x: w / 2, y: -h / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const pin1Position = getSot563PadCoord(1, w, p, pl);
  const pin1Marking = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "pin_marker_1",
    route: [
      { x: pin1Position.x - pl / 2 - 0.3, y: pin1Position.y },
      { x: pin1Position.x - pl / 2 - 0.45, y: pin1Position.y + 0.15 },
      { x: pin1Position.x - pl / 2 - 0.45, y: pin1Position.y - 0.15 },
      { x: pin1Position.x - pl / 2 - 0.3, y: pin1Position.y }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: "pin_marker_1"
  };
  const silkscreenRefText = silkscreenRef(0, h / 2 + 0.4, 0.25);
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot563CourtyardOutline,
    layer: "top"
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenTopLine,
      silkscreenBottomLine,
      silkscreenRefText,
      pin1Marking,
      courtyard
    ],
    parameters
  };
};
var getSot563PadCoord = (pn, w, p, pl) => {
  const padCenterOffset = w / 2 - pl / 2;
  if (pn <= 3) {
    return { x: -padCenterOffset, y: p - (pn - 1) * p };
  }
  return { x: padCenterOffset, y: -p + (pn - 4) * p };
};

// src/fn/ms012.ts
var ms012_def = extendSoicDef({
  p: "1.27mm",
  w: "3.9mm",
  legsoutside: true
});
var ms012 = (raw_params) => {
  const parameters = ms012_def.parse({ ...raw_params, num_pins: 8 });
  return {
    circuitJson: soicWithoutParsing(parameters),
    parameters
  };
};

// src/fn/ms013.ts
var ms013_def = extendSoicDef({
  p: "1.27mm",
  w: "7.5mm",
  legsoutside: true
});
var ms013 = (raw_params) => {
  const parameters = ms013_def.parse({ ...raw_params, num_pins: 16 });
  return {
    circuitJson: soicWithoutParsing(parameters),
    parameters
  };
};

// src/fn/sot723.ts
import {
  length as length25
} from "circuit-json";
import { z as z30 } from "zod";
var sot723_def = base_def.extend({
  fn: z30.string(),
  num_pins: z30.literal(3).default(3),
  w: z30.string().default("1.2mm"),
  h: z30.string().default("1.2mm"),
  pw: z30.string().default("0.40mm"),
  pl: z30.string().default("0.45mm"),
  p: z30.string().default("0.575mm")
});
var sot723 = (raw_params) => {
  const parameters = sot723_def.parse(raw_params);
  const pad2 = sot723WithoutParsing(parameters);
  const silkscreenRefText = silkscreenRef(
    0,
    length25.parse(parameters.h),
    0.2
  );
  const courtyardWidthMm = 1.8;
  const courtyardHeightMm = 1.8;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: [
      { x: -courtyardWidthMm / 2, y: courtyardHeightMm / 2 },
      { x: courtyardWidthMm / 2, y: courtyardHeightMm / 2 },
      { x: courtyardWidthMm / 2, y: -courtyardHeightMm / 2 },
      { x: -courtyardWidthMm / 2, y: -courtyardHeightMm / 2 }
    ],
    layer: "top"
  };
  return {
    circuitJson: [...pad2, silkscreenRefText, courtyard],
    parameters
  };
};
var getCcwSot723Coords = (parameters) => {
  const { pn, w, h, pl, p } = parameters;
  if (pn === 1) {
    return { x: p, y: 0 };
  }
  if (pn === 2) {
    return { x: -p, y: -0.4 };
  }
  return { x: -p, y: 0.4 };
};
var sot723WithoutParsing = (parameters) => {
  const pads = [];
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  const pl = Number.parseFloat(parameters.pl);
  const pw = Number.parseFloat(parameters.pw);
  const p = Number.parseFloat(parameters.p);
  const cornerRadius = Math.min(pl, pw) / 8;
  for (let i = 0; i < 3; i++) {
    const { x, y } = getCcwSot723Coords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w,
      h,
      pl,
      p
    });
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  return pads;
};

// src/fn/sod123.ts
import { z as z31 } from "zod";
import { length as length26 } from "circuit-json";
var sod_def = base_def.extend({
  fn: z31.string(),
  num_pins: z31.literal(2).default(2),
  w: z31.string().default("2.36mm"),
  h: z31.string().default("1.22mm"),
  pl: z31.string().default("0.9mm"),
  pw: z31.string().default("1.2mm"),
  p: z31.string().default("3.30mm")
});
var sod123 = (raw_params) => {
  const parameters = sod_def.parse(raw_params);
  const bodyHeight = length26.parse(parameters.h);
  const silkscreenRefText = silkscreenRef(
    0,
    bodyHeight / 4 + 0.4,
    0.3
  );
  const courtyardWidthMm = 4.7;
  const courtyardHeightMm = 2.3;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.1125
      )
    );
  }
  return pads;
};

// src/fn/axial.ts
import {
  length as length27
} from "circuit-json";
import { z as z32 } from "zod";
var axial_def = base_def.extend({
  fn: z32.string(),
  p: length27.optional().default("2.54mm"),
  id: length27.optional().default("0.7mm"),
  od: length27.optional().default("1.4mm")
});
var axial = (raw_params) => {
  const parameters = axial_def.parse(raw_params);
  const { p, id, od } = parameters;
  const plated_holes = [
    platedhole(1, -p / 2, 0, id, od),
    platedhole(2, p / 2, 0, id, od)
  ];
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -p / 2 + od + id / 2, y: 0 },
      { x: p / 2 - od - id / 2, y: 0 }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenRefText = silkscreenRef(0, 1.5, 0.5);
  const pin1CenterX = -p / 2;
  const pin2CenterX = p / 2;
  const pinPadHalfX = od / 2;
  const pinPadHalfY = od / 2;
  const courtyardStepOuterMinX = pin1CenterX - pinPadHalfX - 0.35;
  const courtyardStepOuterMaxX = pin2CenterX + pinPadHalfX + 0.26;
  const courtyardStepOuterHalfHeight = pinPadHalfY + 0.35;
  const courtyardStepInnerMinX = courtyardStepOuterMinX;
  const courtyardStepInnerMaxX = courtyardStepOuterMaxX;
  const courtyardStepInnerHalfHeight = courtyardStepOuterHalfHeight;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: createRectUnionOutline([
      {
        minX: courtyardStepOuterMinX,
        maxX: courtyardStepOuterMaxX,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: courtyardStepInnerMinX,
        maxX: courtyardStepInnerMaxX,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ]),
    layer: "top"
  };
  return {
    circuitJson: [
      ...plated_holes,
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ],
    parameters
  };
};

// src/fn/radial.ts
import {
  length as length28
} from "circuit-json";
import { z as z33 } from "zod";

// src/helpers/generateCircleArcs.ts
var generateCircleArcs = (centerX, centerY, radius, cut, cutHeight, segmentLength = 0.1) => {
  const topArc = [];
  const bottomArc = [];
  const segments = Math.max(1, Math.ceil(Math.PI * radius / segmentLength));
  const thetaStep = Math.PI / segments;
  for (let i = 0; i <= segments; i++) {
    const theta = i * thetaStep;
    const x = centerX + Math.cos(theta) * radius;
    const y = centerY + Math.sin(theta) * radius;
    if (x < centerX - cut && y >= centerY - cutHeight / 2 && y <= centerY + cutHeight / 2) {
      continue;
    }
    topArc.push({ x, y });
  }
  for (let i = 0; i <= segments; i++) {
    const theta = Math.PI + i * thetaStep;
    const x = centerX + Math.cos(theta) * radius;
    const y = centerY + Math.sin(theta) * radius;
    if (x < centerX - cut && y >= centerY - cutHeight / 2 && y <= centerY + cutHeight / 2) {
      continue;
    }
    bottomArc.push({ x, y });
  }
  return { topArc, bottomArc };
};

// src/fn/radial.ts
var radial_def = base_def.extend({
  fn: z33.string(),
  p: length28.optional().default("5mm"),
  id: length28.optional().default("0.8mm"),
  od: length28.optional().default("1.6mm"),
  ceramic: z33.boolean().optional(),
  electrolytic: z33.boolean().optional(),
  polarized: z33.boolean().optional()
});
var radial = (raw_params) => {
  const parameters = radial_def.parse(raw_params);
  const { p, id, od } = parameters;
  if (id === 0.8 && od === 1.6) {
    parameters.id = p === 5 ? 0.8 : p * 0.25;
    parameters.od = p === 5 ? 1.6 : p * 0.5;
  }
  const plated_holes = [
    platedhole(1, -p / 2, 0, parameters.id, parameters.od),
    platedhole(2, p / 2, 0, parameters.id, parameters.od)
  ];
  const bodyR = p + 0.1;
  const { topArc, bottomArc } = generateCircleArcs(
    0,
    0,
    bodyR,
    parameters.od / 2,
    parameters.od
  );
  const strokeWidth = 0.02 * p;
  const silkscreenBodyTop = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: topArc,
    stroke_width: strokeWidth,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBodyBottom = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: bottomArc,
    stroke_width: strokeWidth,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenCenterLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: 0, y: bodyR },
      { x: 0, y: -bodyR }
    ],
    stroke_width: strokeWidth,
    pcb_silkscreen_path_id: ""
  };
  const plusSize = 0.1 * p;
  const plusX = -(p + plusSize + 0.04 * p);
  const plusY = bodyR - plusSize - 0.08 * p;
  const plusHoriz = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: plusX - plusSize, y: plusY },
      { x: plusX + plusSize, y: plusY }
    ],
    stroke_width: strokeWidth,
    pcb_silkscreen_path_id: ""
  };
  const plusVert = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: plusX, y: plusY - plusSize },
      { x: plusX, y: plusY + plusSize }
    ],
    stroke_width: strokeWidth,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenRefText = silkscreenRef(
    0,
    bodyR + 0.12 * p,
    0.1 * p
  );
  const circuitJson = [
    ...plated_holes,
    silkscreenBodyTop,
    silkscreenBodyBottom,
    silkscreenCenterLine,
    silkscreenRefText
  ];
  const hasPolarity = parameters.electrolytic === true || parameters.polarized === true;
  if (hasPolarity) {
    circuitJson.push(plusHoriz, plusVert);
  }
  const courtyardRadiusMm = p + 0.25;
  const courtyard = {
    type: "pcb_courtyard_circle",
    pcb_courtyard_circle_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    radius: courtyardRadiusMm,
    layer: "top"
  };
  circuitJson.push(courtyard);
  return {
    circuitJson,
    parameters
  };
};

// src/fn/rj45.ts
import {
  length as length29
} from "circuit-json";
import { z as z34 } from "zod";
var rj45_def = base_def.extend({
  fn: z34.literal("rj45"),
  num_pins: z34.union([z34.literal(10), z34.literal(14)]).default(10),
  ledpins: z34.boolean().default(false).describe("add the four through-hole LED connections as pins 9 through 12"),
  firstpinleft: z34.boolean().default(false).describe("mirror the eight signal pins so pin 1 is on the left"),
  firstpintop: z34.boolean().default(false).describe("mirror the eight signal pins so pin 1 is in the top row"),
  p: length29.default("1.02mm").describe("horizontal signal-pin pitch"),
  py: length29.default("1.78mm").describe("signal-row pitch"),
  id: length29.default("0.9144mm").describe("signal and LED hole diameter"),
  od: length29.default("1.524mm").describe("signal and LED outer diameter"),
  shieldx: length29.default("8.13mm").describe("shield-pin x offset from the footprint center"),
  shieldy: length29.default("0.13mm").describe("signed shield-pin y offset from the signal-row center"),
  shieldid: length29.default("1.8mm").describe("shield-pin hole diameter"),
  shieldod: length29.default("2.5mm").describe("shield-pin outer diameter"),
  holex: length29.default("6.35mm").describe("locator-hole x offset from the footprint center"),
  holey: length29.default("-3.43mm").describe("signed locator-hole y offset from the signal-row center"),
  holed: length29.default("3.25mm").describe("locator-hole diameter"),
  ledx: length29.default("4.57mm").describe("inner LED-pin x offset from the footprint center"),
  ledp: length29.default("2.29mm").describe("pitch within each LED-pin pair"),
  ledy: length29.default("5.7mm").describe("signed LED-pin y offset from the signal-row center"),
  w: length29.default("16.26mm").describe("connector body width"),
  h: length29.default("15.88mm").describe("connector body height"),
  bodyy: length29.default("-0.96mm").describe("signed connector body center y offset")
});
var rj45 = (rawParams) => {
  const hasLedPins = rawParams.ledpins === true || rawParams.num_pins === 14;
  const parameters = rj45_def.parse({
    ...rawParams,
    ledpins: hasLedPins,
    num_pins: hasLedPins ? 14 : 10
  });
  const {
    ledpins,
    firstpinleft,
    firstpintop,
    p,
    py,
    id,
    od,
    shieldx,
    shieldy,
    shieldid,
    shieldod,
    holex,
    holey,
    holed,
    ledx,
    ledp,
    ledy,
    w,
    h,
    bodyy
  } = parameters;
  const clean = (value) => Number(value.toFixed(12));
  const signalPins = Array.from({ length: 8 }, (_, index) => {
    const pin = index + 1;
    const defaultX = (3.5 - index) * p;
    const defaultY = index % 2 === 0 ? -py / 2 : py / 2;
    const x = clean(firstpinleft ? -defaultX : defaultX);
    const y = clean(firstpintop ? -defaultY : defaultY);
    return platedhole(pin, x, y, id, od);
  });
  const ledPins = ledpins ? [
    platedhole(9, clean(-ledx - ledp), ledy, id, od),
    platedhole(10, -ledx, ledy, id, od),
    platedhole(11, ledx, ledy, id, od),
    platedhole(12, clean(ledx + ledp), ledy, id, od)
  ] : [];
  const defaultLeftShieldPin = ledpins ? 14 : 9;
  const defaultRightShieldPin = ledpins ? 13 : 10;
  const swapShieldPins = firstpinleft !== firstpintop;
  const leftShieldPin = swapShieldPins ? defaultRightShieldPin : defaultLeftShieldPin;
  const rightShieldPin = swapShieldPins ? defaultLeftShieldPin : defaultRightShieldPin;
  const shieldPins = [
    platedhole(leftShieldPin, -shieldx, shieldy, shieldid, shieldod),
    platedhole(rightShieldPin, shieldx, shieldy, shieldid, shieldod)
  ];
  const locatorHoles = [-holex, holex].map((x) => ({
    type: "pcb_hole",
    pcb_hole_id: "",
    pcb_component_id: "",
    hole_shape: "circle",
    hole_diameter: holed,
    x,
    y: holey
  }));
  const bodyLeft = -w / 2;
  const bodyRight = w / 2;
  const bodyBottom = bodyy - h / 2;
  const bodyTop = bodyy + h / 2;
  const shieldClearance = shieldod / 2 + 0.2;
  const sideSegments = [
    [bodyBottom, Math.min(bodyTop, shieldy - shieldClearance)],
    [Math.max(bodyBottom, shieldy + shieldClearance), bodyTop]
  ].filter(([start, end]) => end - start > 0.01);
  const silkscreen = [
    silkscreenpath([
      { x: bodyLeft, y: bodyBottom },
      { x: bodyRight, y: bodyBottom }
    ]),
    silkscreenpath([
      { x: bodyLeft, y: bodyTop },
      { x: bodyRight, y: bodyTop }
    ]),
    ...sideSegments.flatMap(([start, end]) => [
      silkscreenpath([
        { x: bodyLeft, y: start },
        { x: bodyLeft, y: end }
      ]),
      silkscreenpath([
        { x: bodyRight, y: start },
        { x: bodyRight, y: end }
      ])
    ])
  ];
  const copperLeft = Math.min(
    -shieldx - shieldod / 2,
    -holex - holed / 2,
    ledpins ? -ledx - ledp - od / 2 : 0
  );
  const copperRight = -copperLeft;
  const copperBottom = Math.min(
    shieldy - shieldod / 2,
    holey - holed / 2,
    -py / 2 - od / 2,
    ledpins ? ledy - od / 2 : Number.POSITIVE_INFINITY
  );
  const copperTop = Math.max(
    shieldy + shieldod / 2,
    holey + holed / 2,
    py / 2 + od / 2,
    ledpins ? ledy + od / 2 : Number.NEGATIVE_INFINITY
  );
  const courtyardLeft = Math.min(bodyLeft, copperLeft) - 0.25;
  const courtyardRight = Math.max(bodyRight, copperRight) + 0.25;
  const courtyardBottom = Math.min(bodyBottom, copperBottom) - 0.25;
  const courtyardTop = Math.max(bodyTop, copperTop) + 0.25;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: {
      x: (courtyardLeft + courtyardRight) / 2,
      y: (courtyardBottom + courtyardTop) / 2
    },
    width: courtyardRight - courtyardLeft,
    height: courtyardTop - courtyardBottom,
    layer: "top"
  };
  return {
    circuitJson: [
      ...locatorHoles,
      ...signalPins,
      ...ledPins,
      ...shieldPins,
      ...silkscreen,
      silkscreenRef(0, courtyardTop + 0.55, 0.5),
      courtyard
    ],
    parameters
  };
};

// src/fn/pushbutton.ts
import {
  length as length30
} from "circuit-json";
import { z as z35 } from "zod";
var pushbutton_def = base_def.extend({
  fn: z35.literal("pushbutton"),
  w: length30.default(4.5),
  h: length30.default(6.5),
  id: length30.default(1),
  od: length30.default(1.2)
});
var pushbutton = (raw_params) => {
  const parameters = pushbutton_def.parse(raw_params);
  const width = parameters.w;
  const height = parameters.h;
  const holeDiameter = parameters.id;
  const holes = [
    platedhole(1, -width / 2, height / 2, holeDiameter, holeDiameter * 1.5),
    platedhole(2, -width / 2, -height / 2, holeDiameter, holeDiameter * 1.5),
    platedhole(3, width / 2, -height / 2, holeDiameter, holeDiameter * 1.5),
    platedhole(4, width / 2, height / 2, holeDiameter, holeDiameter * 1.5)
  ];
  const silkscreenLines = [
    // Vertical lines indicating connections
    silkscreenpath([
      { x: -width / 2, y: -height / 2 },
      { x: -width / 2, y: height / 2 }
    ]),
    silkscreenpath([
      { x: width / 2, y: -height / 2 },
      { x: width / 2, y: height / 2 }
    ]),
    // Center indicating latch
    silkscreenpath([
      { x: -width / 2, y: 0 },
      { x: -width / 5, y: 0 },
      { x: width / 5 * 1 / Math.sqrt(2), y: height / 8 }
    ]),
    silkscreenpath([
      { x: width / 2, y: 0 },
      { x: width / 5, y: 0 }
    ])
  ];
  const silkscreenRefText = silkscreenRef(
    0,
    height / 2 + 0.4,
    0.5
  );
  const padOuterRadius = holeDiameter * 1.5 / 2;
  const pinRowSpanX = width;
  const pinRowSpanY = height;
  const padOuterHalfWidth = pinRowSpanX / 2 + padOuterRadius;
  const padOuterHalfHeight = pinRowSpanY / 2 + padOuterRadius;
  const courtyardHalfWidth = padOuterHalfWidth + 0.5;
  const courtyardHalfHeight = padOuterHalfHeight + 0.5;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: 2 * courtyardHalfWidth,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  return {
    circuitJson: [...holes, ...silkscreenLines, silkscreenRefText, courtyard],
    parameters
  };
};

// src/fn/stampboard.ts
import {
  length as length31
} from "circuit-json";
import { z as z36 } from "zod";
var stampboard_def = base_def.extend({
  fn: z36.string(),
  w: length31.default("22.58mm"),
  h: length31.optional(),
  left: length31.optional().default(20),
  right: length31.optional().default(20),
  top: length31.optional().default(2),
  bottom: length31.optional().default(2),
  p: length31.default(length31.parse("2.54mm")),
  pw: length31.default(length31.parse("1.6mm")),
  pl: length31.default(length31.parse("2.4mm")),
  innerhole: z36.boolean().default(false),
  innerholeedgedistance: length31.default(length31.parse("1.61mm")),
  silkscreenlabels: z36.boolean().default(false),
  silkscreenlabelmargin: length31.default(length31.parse("0.1mm"))
});
var getHeight = (parameters) => {
  const params = stampboard_def.parse(parameters);
  if (params.left && params.right) {
    return Math.max(params.left, params.right) * params.p;
  }
  if (params.left) {
    return params.left * params.p;
  }
  if (params.right) {
    return params.right * params.p;
  }
  return 51;
};
var getTriangleDir = (x, y, side) => {
  let routes = [];
  const triangleHeight = 1;
  const triangleWidth = 0.6;
  if (side === "right") {
    routes = [
      {
        x: x + triangleHeight / 2,
        y
      },
      // Tip of the triangle (pointing right)
      {
        x: x - triangleHeight / 2,
        y: y + triangleWidth / 2
      },
      // Bottom corner of the base
      {
        x: x - triangleHeight / 2,
        y: y - triangleWidth / 2
      },
      // Top corner of the base
      {
        x: x + triangleHeight / 2,
        y
      }
      // Close the path at the tip
    ];
  }
  if (side === "left") {
    routes = [
      {
        x: x - triangleHeight / 2,
        y
      },
      // Tip of the triangle (pointing left)
      {
        x: x + triangleHeight / 2,
        y: y + triangleWidth / 2
      },
      // Top corner of the base
      {
        x: x + triangleHeight / 2,
        y: y - triangleWidth / 2
      },
      // Bottom corner of the base
      {
        x: x - triangleHeight / 2,
        y
      }
      // Close the path at the tip
    ];
  }
  if (side === "top") {
    routes = [
      {
        x,
        y: y + triangleHeight / 2
      },
      // Tip of the triangle (pointing up)
      {
        x: x - triangleWidth / 2,
        y: y - triangleHeight / 2
      },
      // Left corner of the base
      {
        x: x + triangleWidth / 2,
        y: y - triangleHeight / 2
      },
      // Right corner of the base
      {
        x,
        y: y + triangleHeight / 2
      }
      // Close the path at the tip
    ];
  }
  if (side === "bottom") {
    routes = [
      {
        x,
        y: y - triangleHeight / 2
      },
      // Tip of the triangle (pointing down)
      {
        x: x - triangleWidth / 2,
        y: y + triangleHeight / 2
      },
      // Left corner of the base
      {
        x: x + triangleWidth / 2,
        y: y + triangleHeight / 2
      },
      // Right corner of the base
      {
        x,
        y: y - triangleHeight / 2
      }
      // Close the path at the tip
    ];
  }
  return routes;
};
var stampboard = (raw_params) => {
  const params = stampboard_def.parse(raw_params);
  const height = params.h ?? getHeight(params);
  const rectpads = [];
  const holes = [];
  const pinLabels = [];
  let routes = [];
  const innerDiameter = 1;
  const outerDiameter = innerDiameter;
  const totalPadsNumber = params.left + params.right + (params.bottom ?? 0) + (params.top ?? 0);
  const maxLabelLength = `pin${totalPadsNumber}`.length;
  const textHalf = maxLabelLength * 0.7 / 2;
  if (params.right) {
    const yoff = -((params.right - 1) / 2) * params.p;
    for (let i = 0; i < params.right; i++) {
      if (i === 0 && !params.left && !params.bottom && !params.silkscreenlabels) {
        routes = getTriangleDir(
          params.w / 2 - params.pl * 1.4,
          yoff + i * params.p,
          "right"
        );
      }
      rectpads.push(
        rectpad(
          i + 1 + params.left + (params.bottom ?? 0),
          params.w / 2 - params.pl / 2,
          yoff + i * params.p,
          params.pl,
          params.pw
        )
      );
      if (params.silkscreenlabels) {
        const padIndex = i + 1 + params.left + (params.bottom ?? 0);
        const label = `pin${padIndex}`;
        pinLabels.push({
          type: "pcb_silkscreen_text",
          pcb_silkscreen_text_id: `pin_${padIndex}`,
          pcb_component_id: "1",
          layer: "top",
          anchor_position: {
            x: params.w / 2 - params.pl - (textHalf + params.silkscreenlabelmargin),
            y: yoff + i * params.p
          },
          text: label,
          font_size: 0.7,
          font: "tscircuit2024",
          anchor_alignment: "center"
        });
      }
      if (params.innerhole) {
        holes.push(
          platedhole(
            i + 1 + params.left + (params.bottom ?? 0) + totalPadsNumber,
            params.w / 2,
            yoff + i * params.p,
            innerDiameter,
            outerDiameter
          )
        );
        holes.push(
          platedhole(
            i + 1 + params.left + (params.bottom ?? 0) + totalPadsNumber * 2,
            params.w / 2 - params.innerholeedgedistance,
            yoff + i * params.p,
            innerDiameter,
            outerDiameter
          )
        );
      }
    }
  }
  if (params.left) {
    const yoff = (params.left - 1) / 2 * params.p;
    for (let i = 0; i < params.left; i++) {
      if (i === 0 && !params.silkscreenlabels) {
        routes = getTriangleDir(
          -params.w / 2 + params.pl * 1.4,
          yoff - i * params.p,
          "left"
        );
      }
      rectpads.push(
        rectpad(
          i + 1,
          -params.w / 2 + params.pl / 2,
          yoff - i * params.p,
          params.pl,
          params.pw
        )
      );
      if (params.silkscreenlabels) {
        const padIndex = i + 1;
        const label = `pin${padIndex}`;
        pinLabels.push({
          type: "pcb_silkscreen_text",
          pcb_silkscreen_text_id: `pin_${padIndex}`,
          pcb_component_id: "1",
          layer: "top",
          anchor_position: {
            x: -params.w / 2 + params.pl + (textHalf + params.silkscreenlabelmargin),
            y: yoff - i * params.p
          },
          text: label,
          font_size: 0.7,
          font: "tscircuit2024",
          anchor_alignment: "center"
        });
      }
      if (params.innerhole) {
        holes.push(
          platedhole(
            i + 1 + totalPadsNumber,
            -params.w / 2,
            yoff - i * params.p,
            innerDiameter,
            outerDiameter
          )
        );
        holes.push(
          platedhole(
            i + 1 + totalPadsNumber * 2,
            -params.w / 2 + params.innerholeedgedistance,
            yoff - i * params.p,
            innerDiameter,
            outerDiameter
          )
        );
      }
    }
  }
  if (params.top) {
    const xoff = (params.top - 1) / 2 * params.p;
    for (let i = 0; i < params.top; i++) {
      if (i === 0 && !params.left && !params.bottom && !params.right && !params.silkscreenlabels) {
        routes = getTriangleDir(
          xoff - i * params.p,
          height / 2 - params.pl * 1.4,
          "top"
        );
      }
      rectpads.push(
        rectpad(
          i + 1 + params.left + params.right + (params.bottom ?? 0),
          xoff - i * params.p,
          height / 2 - params.pl / 2,
          params.pw,
          params.pl
        )
      );
      if (params.silkscreenlabels) {
        const padIndex = i + 1 + params.left + params.right + (params.bottom ?? 0);
        const label = `pin${padIndex}`;
        pinLabels.push({
          type: "pcb_silkscreen_text",
          pcb_silkscreen_text_id: `pin_${padIndex}`,
          pcb_component_id: "1",
          layer: "top",
          anchor_position: {
            x: xoff - i * params.p,
            y: height / 2 - params.pl - (textHalf + params.silkscreenlabelmargin)
          },
          text: label,
          font_size: 0.7,
          font: "tscircuit2024",
          anchor_alignment: "center",
          ccw_rotation: 270
        });
      }
      if (params.innerhole) {
        holes.push(
          platedhole(
            i + 1 + params.left + params.right + (params.bottom ?? 0) + totalPadsNumber,
            xoff - i * params.p,
            height / 2,
            innerDiameter,
            outerDiameter
          )
        );
        holes.push(
          platedhole(
            i + 1 + params.left + params.right + (params.bottom ?? 0) + totalPadsNumber * 2,
            xoff - i * params.p,
            height / 2 - params.innerholeedgedistance,
            innerDiameter,
            outerDiameter
          )
        );
      }
    }
  }
  if (params.bottom) {
    const xoff = -((params.bottom - 1) / 2) * params.p;
    for (let i = 0; i < params.bottom; i++) {
      if (i === 0 && !params.left && !params.silkscreenlabels) {
        routes = getTriangleDir(
          xoff + i * params.p,
          -height / 2 + params.pl * 1.4,
          "bottom"
        );
      }
      rectpads.push(
        rectpad(
          i + 1 + params.left,
          xoff + i * params.p,
          -height / 2 + params.pl / 2,
          params.pw,
          params.pl
        )
      );
      if (params.silkscreenlabels) {
        const padIndex = i + 1 + params.left;
        const label = `pin${padIndex}`;
        pinLabels.push({
          type: "pcb_silkscreen_text",
          pcb_silkscreen_text_id: `pin_${padIndex}`,
          pcb_component_id: "1",
          layer: "top",
          anchor_position: {
            x: xoff + i * params.p,
            y: -height / 2 + params.pl + (textHalf + params.silkscreenlabelmargin)
          },
          text: label,
          font_size: 0.7,
          font: "tscircuit2024",
          anchor_alignment: "center",
          ccw_rotation: 90
        });
      }
      if (params.innerhole) {
        holes.push(
          platedhole(
            i + 1 + params.left + totalPadsNumber,
            xoff + i * params.p,
            -height / 2,
            innerDiameter,
            outerDiameter
          )
        );
        holes.push(
          platedhole(
            i + 1 + params.left + totalPadsNumber * 2,
            xoff + i * params.p,
            -height / 2 + params.innerholeedgedistance,
            innerDiameter,
            outerDiameter
          )
        );
      }
    }
  }
  const silkscreenTriangle = {
    type: "pcb_silkscreen_path",
    pcb_silkscreen_path_id: "pcb_silkscreen_triangle_1",
    pcb_component_id: "2",
    route: routes,
    stroke_width: 0.1,
    layer: "top"
  };
  const silkscreenPath = {
    type: "pcb_silkscreen_path",
    pcb_silkscreen_path_id: "pcb_silkscreen_path_1",
    pcb_component_id: "1",
    route: [
      { x: -params.w / 2, y: height / 2 },
      { x: params.w / 2, y: height / 2 },
      { x: params.w / 2, y: -height / 2 },
      { x: -params.w / 2, y: -height / 2 },
      { x: -params.w / 2, y: height / 2 }
    ],
    stroke_width: 0.1,
    layer: "top"
  };
  const silkscreenRefText = silkscreenRef(
    0,
    height / 1.8,
    height / 25
  );
  return {
    circuitJson: [
      ...rectpads,
      ...holes,
      ...pinLabels,
      silkscreenPath,
      ...params.silkscreenlabels ? [] : [silkscreenTriangle],
      silkscreenRefText
    ],
    parameters: params
  };
};

// src/fn/stampreceiver.ts
import {
  length as length32
} from "circuit-json";
import { z as z37 } from "zod";
var stampreceiver_def = base_def.extend({
  fn: z37.string(),
  w: length32.default("22.58mm"),
  h: length32.optional(),
  left: length32.optional().default(20),
  right: length32.optional().default(20),
  top: length32.optional().default(2),
  bottom: length32.optional().default(2),
  p: length32.default(length32.parse("2.54mm")),
  pw: length32.default(length32.parse("1.6mm")),
  pl: length32.default(length32.parse("3.2mm")),
  innerhole: z37.boolean().default(false),
  innerholeedgedistance: length32.default(length32.parse("1.61mm"))
});
var getHeight2 = (parameters) => {
  const params = stampreceiver_def.parse(parameters);
  if (params.left && params.right) {
    return Math.max(params.left, params.right) * params.p;
  }
  if (params.left) {
    return params.left * params.p;
  }
  if (params.right) {
    return params.right * params.p;
  }
  return 51;
};
var getTriangleDir2 = (x, y, side, triangleHeight = 1, triangleWidth = 0.6) => {
  const halfHeight = triangleHeight / 2;
  const halfWidth = triangleWidth / 2;
  const routes = {
    left: [
      { x: x + halfHeight, y },
      // Tip
      { x: x - halfHeight, y: y + halfWidth },
      // Bottom corner
      { x: x - halfHeight, y: y - halfWidth },
      // Top corner
      { x: x + halfHeight, y }
      // Close path
    ],
    right: [
      { x: x - halfHeight, y },
      // Tip
      { x: x + halfHeight, y: y + halfWidth },
      // Top corner
      { x: x + halfHeight, y: y - halfWidth },
      // Bottom corner
      { x: x - halfHeight, y }
      // Close path
    ],
    top: [
      { x, y: y - halfHeight },
      // Tip
      { x: x - halfWidth, y: y + halfHeight },
      // Left corner
      { x: x + halfWidth, y: y + halfHeight },
      // Right corner
      { x, y: y - halfHeight }
      // Close path
    ],
    bottom: [
      { x, y: y + halfHeight },
      // Tip
      { x: x - halfWidth, y: y - halfHeight },
      // Left corner
      { x: x + halfWidth, y: y - halfHeight },
      // Right corner
      { x, y: y + halfHeight }
      // Close path
    ]
  };
  return routes[side];
};
var stampreceiver = (raw_params) => {
  const params = stampreceiver_def.parse(raw_params);
  const height = params.h ?? getHeight2(params);
  const rectpads = [];
  const pinLabels = [];
  const holes = [];
  const innerDiameter = 1;
  const outerDiameter = 1.2;
  const totalPadsNumber = params.left + params.right + (params.bottom ?? 0) + (params.top ?? 0);
  let routes = [];
  let padIndex = 1;
  if (params.left) {
    const yoff = (params.left - 1) / 2 * params.p;
    for (let i = 0; i < params.left; i++) {
      if (i === 0) {
        routes = getTriangleDir2(
          -params.w / 2 - params.pl / 2,
          yoff - i * params.p,
          "left"
        );
      }
      rectpads.push(
        rectpad(
          padIndex,
          -params.w / 2 + params.pl / 2,
          yoff - i * params.p,
          params.pl,
          params.pw
        )
      );
      pinLabels.push({
        type: "pcb_silkscreen_text",
        pcb_silkscreen_text_id: `pin_${padIndex}`,
        pcb_component_id: "1",
        layer: "top",
        anchor_position: {
          x: -params.w / 2 + params.pl / 2 - 4.5,
          y: yoff - i * params.p
        },
        text: `pin${padIndex}`,
        font_size: 0.7,
        font: "tscircuit2024",
        anchor_alignment: "center"
      });
      padIndex++;
      params.innerhole && holes.push(
        platedhole(
          padIndex + totalPadsNumber,
          -params.w / 2 + params.innerholeedgedistance,
          yoff - i * params.p,
          innerDiameter,
          outerDiameter
        )
      );
    }
  }
  if (params.bottom) {
    const xoff = (params.bottom - 1) / 2 * params.p;
    for (let i = params.bottom - 1; i >= 0; i--) {
      rectpads.push(
        rectpad(
          padIndex,
          xoff - i * params.p,
          -height / 2 + params.pl / 2,
          params.pw,
          params.pl
        )
      );
      pinLabels.push({
        type: "pcb_silkscreen_text",
        pcb_silkscreen_text_id: `pin_${padIndex}`,
        pcb_component_id: "1",
        layer: "top",
        anchor_position: {
          x: xoff - i * params.p,
          y: -height / 2 + params.pl / 2 - 3.2
        },
        text: `pin${padIndex}`,
        font_size: 0.7,
        font: "tscircuit2024",
        anchor_alignment: "center"
      });
      padIndex++;
      params.innerhole && holes.push(
        platedhole(
          padIndex + totalPadsNumber,
          xoff - i * params.p,
          -height / 2 + params.innerholeedgedistance,
          innerDiameter,
          outerDiameter
        )
      );
    }
  }
  if (params.right) {
    const yoff = -((params.right - 1) / 2) * params.p;
    for (let i = 0; i < params.right; i++) {
      rectpads.push(
        rectpad(
          padIndex,
          params.w / 2 - params.pl / 2,
          yoff + i * params.p,
          params.pl,
          params.pw
        )
      );
      pinLabels.push({
        type: "pcb_silkscreen_text",
        pcb_silkscreen_text_id: `pin_${padIndex}`,
        pcb_component_id: "1",
        layer: "top",
        anchor_position: {
          x: params.w / 2 - params.pl / 2 + 3.7,
          y: yoff + i * params.p
        },
        text: `pin${padIndex}`,
        font_size: 0.7,
        font: "tscircuit2024",
        anchor_alignment: "center"
      });
      padIndex++;
      params.innerhole && holes.push(
        platedhole(
          padIndex + totalPadsNumber,
          params.w / 2 - params.innerholeedgedistance,
          yoff + i * params.p,
          innerDiameter,
          outerDiameter
        )
      );
    }
  }
  if (params.top) {
    const xoff = -((params.top - 1) / 2) * params.p;
    for (let i = params.top - 1; i >= 0; i--) {
      rectpads.push(
        rectpad(
          padIndex,
          xoff + i * params.p,
          height / 2 - params.pl / 2,
          params.pw,
          params.pl
        )
      );
      pinLabels.push({
        type: "pcb_silkscreen_text",
        pcb_silkscreen_text_id: `pin_${padIndex}`,
        pcb_component_id: "1",
        layer: "top",
        anchor_position: {
          x: xoff + i * params.p,
          y: height / 2 - params.pl / 2 + 3.2
        },
        text: `pin${padIndex}`,
        font_size: 0.7,
        font: "tscircuit2024",
        anchor_alignment: "center"
      });
      padIndex++;
      params.innerhole && holes.push(
        platedhole(
          padIndex + totalPadsNumber,
          xoff + i * params.p,
          height / 2 - params.innerholeedgedistance,
          innerDiameter,
          outerDiameter
        )
      );
    }
  }
  const silkscreenTriangle = {
    type: "pcb_silkscreen_path",
    pcb_silkscreen_path_id: "1",
    pcb_component_id: "1",
    layer: "top",
    route: routes,
    stroke_width: 0.1
  };
  const silkscreenPath = {
    type: "pcb_silkscreen_path",
    pcb_silkscreen_path_id: "pcb_silkscreen_path_1",
    pcb_component_id: "1",
    route: [
      {
        x: -params.w / 2 - params.pl / 3,
        y: height / 2 + params.pl / 3
      },
      {
        x: params.w / 2 + params.pl / 3,
        y: height / 2 + params.pl / 3
      },
      {
        x: params.w / 2 + params.pl / 3,
        y: -height / 2 - params.pl / 3
      },
      {
        x: -params.w / 2 - params.pl / 3,
        y: -height / 2 - params.pl / 3
      },
      {
        x: -params.w / 2 - params.pl / 3,
        y: height / 2 + params.pl / 3
      }
    ],
    stroke_width: 0.1,
    layer: "top"
  };
  const silkscreenRefText = silkscreenRef(
    0,
    height / 1.8,
    height / 25
  );
  return {
    circuitJson: [
      ...holes,
      ...rectpads,
      ...pinLabels,
      silkscreenPath,
      silkscreenTriangle,
      silkscreenRefText
    ],
    parameters: params
  };
};

// src/fn/lqfp.ts
var lqfp = (parameters) => {
  parameters.legsoutside = true;
  if (!parameters.pl) {
    parameters.pl = 1.5;
  }
  if (!parameters.pw) {
    parameters.pw = 0.3;
  }
  return quad(parameters);
};

// src/fn/lga.ts
import { length as length33 } from "circuit-json";
import { z as z38 } from "zod";
var lga_def = base_def.extend({
  fn: z38.string(),
  num_pins: z38.number().int().positive().optional().default(14),
  grid: dim2d.optional(),
  p: length33.default(length33.parse("0.5mm")),
  w: length33.optional(),
  h: length33.optional(),
  pw: length33.default(length33.parse("0.28mm")),
  pl: length33.default(length33.parse("0.7mm")),
  pillpads: z38.boolean().optional().default(false)
});
var lga = (rawParameters) => {
  const parameters = lga_def.parse(rawParameters);
  const halfPinCount = parameters.num_pins / 2;
  if (!Number.isInteger(halfPinCount)) {
    throw new Error("LGA footprints require an even number of perimeter pads");
  }
  const grid2 = parameters.grid ?? {
    x: Math.ceil(halfPinCount / 2),
    y: Math.floor(halfPinCount / 2)
  };
  if (!Number.isInteger(grid2.x) || !Number.isInteger(grid2.y) || grid2.x < 0 || grid2.y < 0 || grid2.x + grid2.y <= 0 || 2 * (grid2.x + grid2.y) !== parameters.num_pins) {
    throw new Error(
      `LGA grid ${grid2.x}x${grid2.y} requires ${2 * (grid2.x + grid2.y)} pads, got ${parameters.num_pins}`
    );
  }
  const width = parameters.w ?? (grid2.y - 1) * parameters.p + 2 * parameters.pl;
  const height = parameters.h ?? (grid2.x - 1) * parameters.p + 2 * parameters.pl;
  const leftRightX = (width - parameters.pl) / 2;
  const topBottomY = (height - parameters.pl) / 2;
  const pads = [];
  const addPad = (pin2, x, y, padWidth, padHeight) => {
    pads.push(
      parameters.pillpads ? pillpad(pin2, x, y, padWidth, padHeight) : rectpad(pin2, x, y, padWidth, padHeight)
    );
  };
  let pin = 1;
  for (let index = 0; index < grid2.x; index += 1) {
    addPad(
      pin++,
      -leftRightX,
      ((grid2.x - 1) / 2 - index) * parameters.p,
      parameters.pl,
      parameters.pw
    );
  }
  for (let index = 0; index < grid2.y; index += 1) {
    addPad(
      pin++,
      (index - (grid2.y - 1) / 2) * parameters.p,
      -topBottomY,
      parameters.pw,
      parameters.pl
    );
  }
  for (let index = 0; index < grid2.x; index += 1) {
    addPad(
      pin++,
      leftRightX,
      (index - (grid2.x - 1) / 2) * parameters.p,
      parameters.pl,
      parameters.pw
    );
  }
  for (let index = 0; index < grid2.y; index += 1) {
    addPad(
      pin++,
      ((grid2.y - 1) / 2 - index) * parameters.p,
      topBottomY,
      parameters.pw,
      parameters.pl
    );
  }
  const markerSize = Math.max(parameters.pw, 0.15);
  const pin1Marker = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "pin1_marker",
    stroke_width: 0.1,
    route: [
      { x: -width / 2, y: height / 2 - markerSize },
      { x: -width / 2, y: height / 2 },
      { x: -width / 2 + markerSize, y: height / 2 }
    ]
  };
  const courtyardClearance = 0.25;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: [
      {
        x: -width / 2 - courtyardClearance,
        y: -height / 2 - courtyardClearance
      },
      {
        x: width / 2 + courtyardClearance,
        y: -height / 2 - courtyardClearance
      },
      {
        x: width / 2 + courtyardClearance,
        y: height / 2 + courtyardClearance
      },
      {
        x: -width / 2 - courtyardClearance,
        y: height / 2 + courtyardClearance
      },
      {
        x: -width / 2 - courtyardClearance,
        y: -height / 2 - courtyardClearance
      }
    ]
  };
  return {
    circuitJson: [
      ...pads,
      pin1Marker,
      silkscreenRef(0, height / 2 + 0.5, 0.3),
      courtyard
    ],
    parameters: { ...parameters, grid: grid2, w: width, h: height }
  };
};

// src/fn/breakoutheaders.ts
import {
  length as length34
} from "circuit-json";
import { z as z39 } from "zod";
var breakoutheaders_def = base_def.extend({
  fn: z39.string(),
  w: length34.default("10mm"),
  h: length34.optional(),
  left: length34.optional().default(20),
  right: length34.optional().default(20),
  top: length34.optional().default(0),
  bottom: length34.optional().default(0),
  p: length34.default(length34.parse("2.54mm")),
  id: length34.optional().default(length34.parse("1mm")),
  od: length34.optional().default(length34.parse("1.5mm"))
});
var getHeight3 = (parameters) => {
  const params = breakoutheaders_def.parse(parameters);
  if (params.left && params.right) {
    return Math.max(params.left, params.right) * params.p;
  }
  if (params.left) {
    return params.left * params.p;
  }
  if (params.right) {
    return params.right * params.p;
  }
  return 51;
};
var getTrianglePath = (x, y, side, triangleHeight = 1, triangleWidth = 0.6) => {
  const halfHeight = triangleHeight / 2;
  const halfWidth = triangleWidth / 2;
  const silkscreenTriangleRoutes = {
    left: [
      { x: x + halfHeight, y },
      { x: x - halfHeight, y: y + halfWidth },
      { x: x - halfHeight, y: y - halfWidth },
      { x: x + halfHeight, y }
    ],
    right: [
      { x: x - halfHeight, y },
      { x: x + halfHeight, y: y + halfWidth },
      { x: x + halfHeight, y: y - halfWidth },
      { x: x - halfHeight, y }
    ],
    top: [
      { x, y: y - halfHeight },
      { x: x - halfWidth, y: y + halfHeight },
      { x: x + halfWidth, y: y + halfHeight },
      { x, y: y - halfHeight }
    ],
    bottom: [
      { x, y: y + halfHeight },
      { x: x - halfWidth, y: y - halfHeight },
      { x: x + halfWidth, y: y - halfHeight },
      { x, y: y + halfHeight }
    ]
  };
  return silkscreenTriangleRoutes[side];
};
var breakoutheaders = (raw_params) => {
  const params = breakoutheaders_def.parse(raw_params);
  const height = params.h ?? getHeight3(params);
  const holes = [];
  const innerDiameter = params.id;
  const outerDiameter = params.od;
  let silkscreenTriangleRoutes = [];
  if (params.right) {
    const yoff = -((params.right - 1) / 2) * params.p;
    for (let i = 0; i < params.right; i++) {
      if (i === 0 && !params.left && !params.bottom) {
        silkscreenTriangleRoutes = getTrianglePath(
          params.w / 2 + outerDiameter * 1.4,
          yoff + i * params.p,
          "right"
        );
      }
      holes.push(
        platedhole(
          i + 1 + params.left + (params.bottom ?? 0),
          params.w / 2,
          yoff + i * params.p,
          innerDiameter,
          outerDiameter
        )
      );
    }
  }
  if (params.left) {
    const yoff = -((params.left - 1) / 2) * params.p;
    for (let i = 0; i < params.left; i++) {
      if (i === params.left - 1) {
        silkscreenTriangleRoutes = getTrianglePath(
          -params.w / 2 - outerDiameter * 1.4,
          yoff + i * params.p,
          "left"
        );
      }
      holes.push(
        platedhole(
          i + 1,
          -params.w / 2,
          yoff + i * params.p,
          innerDiameter,
          outerDiameter
        )
      );
    }
  }
  if (params.top) {
    const xoff = -((params.top - 1) / 2) * params.p;
    for (let i = 0; i < params.top; i++) {
      if (i === params.top - 1 && !params.left && !params.bottom && !params.right) {
        silkscreenTriangleRoutes = getTrianglePath(
          xoff + i * params.p,
          height / 2 + outerDiameter * 1.4,
          "top"
        );
      }
      holes.push(
        platedhole(
          i + 1 + params.right + (params.bottom ?? 0) + params.left,
          xoff + i * params.p,
          height / 2,
          innerDiameter,
          outerDiameter
        )
      );
    }
  }
  if (params.bottom) {
    const xoff = -((params.bottom - 1) / 2) * params.p;
    for (let i = 0; i < params.bottom; i++) {
      if (i === 0 && !params.left) {
        silkscreenTriangleRoutes = getTrianglePath(
          xoff + i * params.p,
          -height / 2 - outerDiameter * 1.4,
          "bottom"
        );
      }
      holes.push(
        platedhole(
          i + 1 + params.left,
          xoff + i * params.p,
          -height / 2,
          innerDiameter,
          outerDiameter
        )
      );
    }
  }
  const silkscreenTriangle = {
    type: "pcb_silkscreen_path",
    pcb_silkscreen_path_id: "1",
    pcb_component_id: "1",
    layer: "top",
    route: silkscreenTriangleRoutes,
    stroke_width: 0.1
  };
  const silkscreenPath = {
    type: "pcb_silkscreen_path",
    pcb_silkscreen_path_id: "pcb_silkscreen_path_1",
    pcb_component_id: "1",
    route: [
      {
        x: -params.w / 2 - outerDiameter,
        y: height / 2 + outerDiameter
      },
      {
        x: params.w / 2 + outerDiameter,
        y: height / 2 + outerDiameter
      },
      {
        x: params.w / 2 + outerDiameter,
        y: -height / 2 - outerDiameter
      },
      {
        x: -params.w / 2 - outerDiameter,
        y: -height / 2 - outerDiameter
      },
      {
        x: -params.w / 2 - outerDiameter,
        y: height / 2 + outerDiameter
      }
    ],
    stroke_width: 0.1,
    layer: "top"
  };
  const silkscreenRefText = silkscreenRef(
    0,
    height / 1.7,
    height / 25
  );
  return {
    circuitJson: [
      ...holes,
      silkscreenPath,
      silkscreenRefText,
      silkscreenTriangle
    ],
    parameters: params
  };
};

// src/fn/hc49.ts
import {
  length as length35
} from "circuit-json";
import { z as z40 } from "zod";

// src/helpers/capsule-outline.ts
var createCapsuleOutline = (options) => {
  const {
    centerX,
    centerY,
    straightHalfLength,
    radius,
    arcSegmentCount = 9,
    orientation: orientation2 = "horizontal"
  } = options;
  if (straightHalfLength < 0) {
    throw new Error("Capsule straightHalfLength must be non-negative");
  }
  if (radius <= 0) {
    throw new Error("Capsule radius must be positive");
  }
  if (arcSegmentCount < 2) {
    throw new Error("Capsule arcSegmentCount must be at least 2");
  }
  if (orientation2 === "horizontal") {
    const leftCenterX = centerX - straightHalfLength;
    const rightCenterX = centerX + straightHalfLength;
    const rightArc = Array.from({ length: arcSegmentCount + 1 }, (_, i) => {
      const theta = Math.PI / 2 - i * Math.PI / arcSegmentCount;
      return {
        x: rightCenterX + Math.cos(theta) * radius,
        y: centerY + Math.sin(theta) * radius
      };
    });
    const leftArc = Array.from({ length: arcSegmentCount + 1 }, (_, i) => {
      const theta = -Math.PI / 2 + i * Math.PI / arcSegmentCount;
      return {
        x: leftCenterX - Math.cos(theta) * radius,
        y: centerY + Math.sin(theta) * radius
      };
    });
    return [
      { x: leftCenterX, y: centerY + radius },
      { x: rightCenterX, y: centerY + radius },
      ...rightArc.slice(1, -1),
      { x: rightCenterX, y: centerY - radius },
      { x: leftCenterX, y: centerY - radius },
      ...leftArc.slice(1, -1)
    ];
  }
  const bottomCenterY = centerY - straightHalfLength;
  const topCenterY = centerY + straightHalfLength;
  const topArc = Array.from({ length: arcSegmentCount + 1 }, (_, i) => {
    const theta = Math.PI - i * Math.PI / arcSegmentCount;
    return {
      x: centerX + Math.cos(theta) * radius,
      y: topCenterY + Math.sin(theta) * radius
    };
  });
  const bottomArc = Array.from({ length: arcSegmentCount + 1 }, (_, i) => {
    const theta = i * Math.PI / arcSegmentCount;
    return {
      x: centerX + Math.cos(theta) * radius,
      y: bottomCenterY - Math.sin(theta) * radius
    };
  });
  return [
    { x: centerX - radius, y: bottomCenterY },
    { x: centerX - radius, y: topCenterY },
    ...topArc.slice(1, -1),
    { x: centerX + radius, y: topCenterY },
    { x: centerX + radius, y: bottomCenterY },
    ...bottomArc.slice(1, -1)
  ];
};

// src/fn/hc49.ts
var generate_u_curve = (centerX, centerY, radius, direction) => {
  return Array.from({ length: 25 }, (_, i) => {
    const theta = i / 24 * Math.PI - Math.PI / 2;
    return {
      x: centerX + (direction === "right" ? 1 : -1) * Math.cos(theta) * radius,
      y: centerY + Math.sin(theta) * radius
    };
  });
};
var hc49_def = base_def.extend({
  fn: z40.string(),
  p: length35.optional().default("4.88mm"),
  id: length35.optional().default("0.8mm"),
  od: length35.optional().default("1.5mm"),
  w: length35.optional().default("5.6mm"),
  h: length35.optional().default("3.5mm")
});
var hc49 = (raw_params) => {
  const parameters = hc49_def.parse(raw_params);
  const { p, id, od, w, h } = parameters;
  const radius = h / 2;
  const plated_holes = [
    platedhole(1, -p / 2, 0, id, od),
    platedhole(2, p / 2, 0, id, od)
  ];
  const leftCurve = generate_u_curve(-w / 2, 0, radius, "left");
  const rightCurve = generate_u_curve(w / 2, 0, radius, "right");
  const silkscreenBody = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      ...leftCurve,
      ...leftCurve.slice(1).reverse(),
      { x: -w / 2, y: -h / 2 },
      { x: w / 2, y: -h / 2 },
      ...rightCurve,
      { x: w / 2, y: h / 2 },
      { x: -w / 2, y: h / 2 }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenRefText = silkscreenRef(0, p / 4, 0.5);
  const padRowHalfWidth = p / 2 + od / 2;
  const courtyardCapsuleStraightHalfLength = padRowHalfWidth;
  const courtyardCapsuleRadius = Math.max(h / 2 + 0.25, w / 2 + 0.03);
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: createCapsuleOutline({
      centerX: 0,
      centerY: 0,
      straightHalfLength: courtyardCapsuleStraightHalfLength,
      radius: courtyardCapsuleRadius,
      arcSegmentCount: 9
    }),
    layer: "top"
  };
  return {
    circuitJson: [
      ...plated_holes,
      silkscreenBody,
      silkscreenRefText,
      courtyard
    ],
    parameters
  };
};

// src/fn/pad.ts
import "zod";
import { length as length36 } from "circuit-json";
import { mm as mm11 } from "@tscircuit/mm";
var pad_def = base_def.extend({
  w: length36,
  h: length36
});
var pad = (params) => {
  const { w, h } = params;
  const width = mm11(w);
  const height = mm11(h);
  return {
    circuitJson: [
      rectpad(1, 0, 0, width, height),
      silkscreenRef(0, height / 2 + 0.5, 0.2)
    ],
    parameters: params
  };
};

// src/fn/to92.ts
import { z as z42 } from "zod";
import "@tscircuit/mm";

// src/helpers/platedHolePill.ts
import { mm as mm12 } from "@tscircuit/mm";
var platedHolePill = (pn, x, y, holeDiameter, outerWidth, outerHeight) => {
  return {
    pcb_plated_hole_id: "",
    type: "pcb_plated_hole",
    shape: "pill",
    x,
    y,
    outer_width: mm12(outerWidth),
    outer_height: mm12(outerHeight),
    hole_width: mm12(holeDiameter),
    hole_height: mm12(holeDiameter),
    pcb_port_id: "",
    layers: ["top", "bottom"],
    port_hints: [pn.toString()],
    ccw_rotation: 0
  };
};

// src/fn/to92.ts
var to92CourtyardOutline = [
  { x: -2.5, y: 4.75 },
  { x: -2.5, y: -0.25 },
  { x: 2.5, y: -0.25 },
  { x: 2.5, y: 4.75 }
];
var to92_def = base_def.extend({
  fn: z42.string(),
  num_pins: z42.union([z42.literal(3), z42.literal(2)]).default(3),
  p: z42.string().default("1.27mm"),
  id: z42.string().default("0.72mm"),
  od: z42.string().default("0.95mm"),
  w: z42.string().default("4.5mm"),
  h: z42.string().default("4.5mm"),
  inline: z42.boolean().default(false),
  string: z42.string().optional()
});
var generateSemicircle = (centerX, centerY, radius) => {
  return Array.from({ length: 25 }, (_, i) => {
    const theta = i / 24 * Math.PI;
    return {
      x: centerX + Math.cos(theta) * radius,
      y: centerY + Math.sin(theta) * radius
    };
  });
};
var to92 = (raw_params) => {
  const match = raw_params.string?.match(/^to92_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 10) : 3;
  const parameters = to92_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  const { p, id, od, w, h, inline } = parameters;
  const holeY = Number.parseFloat(h) / 2;
  const padSpacing7 = Number.parseFloat(p);
  const holeDia = Number.parseFloat(id);
  const padDia = Number.parseFloat(od);
  const padWidth = padDia;
  const padHeight = padDia * (1.5 / 1.05);
  let platedHoles = [];
  if (parameters.num_pins === 3) {
    if (inline) {
      platedHoles = [
        platedHoleWithRectPad({
          pn: 1,
          x: -padSpacing7,
          y: holeY - padSpacing7,
          holeDiameter: holeDia,
          rectPadWidth: padDia,
          rectPadHeight: padHeight
        }),
        platedHolePill(2, 0, holeY - padSpacing7, holeDia, padWidth, padHeight),
        platedHolePill(
          3,
          padSpacing7,
          holeY - padSpacing7,
          holeDia,
          padWidth,
          padHeight
        )
      ];
    } else {
      platedHoles = [
        platedHoleWithRectPad({
          pn: 1,
          x: -padSpacing7,
          y: holeY - padSpacing7,
          holeDiameter: holeDia,
          rectPadWidth: padDia,
          rectPadHeight: padDia
        }),
        platedhole(2, 0, holeY, holeDia, padDia),
        platedhole(3, padSpacing7, holeY - padSpacing7, holeDia, padDia)
      ];
    }
  } else if (parameters.num_pins === 2) {
    platedHoles = [
      platedHoleWithRectPad({
        pn: 1,
        x: -padSpacing7,
        y: holeY - padSpacing7,
        holeDiameter: holeDia,
        rectPadWidth: padWidth,
        rectPadHeight: padHeight
      }),
      platedHolePill(
        2,
        padSpacing7,
        holeY - padSpacing7,
        holeDia,
        padWidth,
        padHeight
      )
    ];
  } else {
    throw new Error("Invalid number of pins for TO-92");
  }
  const radius = Number.parseFloat(w) / 2;
  const semicircle = generateSemicircle(0, holeY, radius);
  const silkscreenBody = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      ...semicircle,
      { x: -radius, y: 0 },
      { x: radius, y: 0 },
      semicircle[0]
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenRefText = silkscreenRef(0, holeY + 1, 0.5);
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: to92CourtyardOutline,
    layer: "top"
  };
  return {
    circuitJson: [
      ...platedHoles,
      silkscreenBody,
      silkscreenRefText,
      courtyard
    ],
    parameters
  };
};

// src/fn/sod523.ts
import { z as z43 } from "zod";
import { length as length37 } from "circuit-json";
var sod_def2 = base_def.extend({
  fn: z43.string(),
  num_pins: z43.literal(2).default(2),
  w: z43.string().default("2.15mm"),
  h: z43.string().default("0.7mm"),
  pl: z43.string().default("0.6mm"),
  pw: z43.string().default("0.7mm"),
  p: z43.string().default("1.4mm")
});
var sod523 = (raw_params) => {
  const parameters = sod_def2.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length37.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length37.parse(parameters.p) / 2,
        y: length37.parse(parameters.h) / 2 + 0.2
      },
      {
        x: -length37.parse(parameters.w) / 2 - 0.2,
        y: length37.parse(parameters.h) / 2 + 0.2
      },
      {
        x: -length37.parse(parameters.w) / 2 - 0.2,
        y: -length37.parse(parameters.h) / 2 - 0.2
      },
      {
        x: length37.parse(parameters.p) / 2,
        y: -length37.parse(parameters.h) / 2 - 0.2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 2.5;
  const courtyardHeightMm = 1.4;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing2(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords2 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing2 = (parameters) => {
  const pads = [];
  const p = length37.parse(parameters.p);
  const pl = length37.parse(parameters.pl);
  const pw = length37.parse(parameters.pw);
  const cornerRadius = 0.075;
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords2({
      pn: i,
      p
    });
    pads.push(rectpad(i, x, y, pl, pw, cornerRadius));
  }
  return pads;
};

// src/fn/sop8.ts
var sop8_def = extendSoicDef({
  w: "7.05mm",
  p: "1.27mm",
  pw: "0.65mm",
  pl: "1.975mm"
});
var sop8 = (raw_params) => {
  const parameters = sop8_def.parse(raw_params);
  const pads = [];
  const cornerRadius = Math.min(parameters.pl, parameters.pw) / 8;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getCcwSoicCoords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w: parameters.w,
      p: parameters.p ?? 1.27,
      pl: parameters.pl,
      widthincludeslegs: true
    });
    pads.push(rectpad(i + 1, x, y, parameters.pl, parameters.pw, cornerRadius));
  }
  const sh = (parameters.num_pins / 2 - 1) * parameters.p + parameters.pw;
  const silkscreenRefText = silkscreenRef(0, sh / 2 + 1, sh / 12);
  const silkscreenLine = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "",
    type: "pcb_silkscreen_path",
    route: [
      { x: -parameters.w / 3, y: sh / 2 + 0.4 },
      { x: parameters.w / 3, y: sh / 2 + 0.4 }
    ],
    stroke_width: 0.1
  };
  const pinRowSpanY = (parameters.num_pins / 2 - 1) * parameters.p + parameters.pw;
  const courtyardStepInnerHalfWidth = parameters.w / 2 - 1.395;
  const courtyardStepOuterHalfWidth = parameters.w / 2 + 0.255;
  const courtyardStepInnerHalfHeight = pinRowSpanY / 2 + 0.25;
  const courtyardStepOuterHalfHeight = pinRowSpanY / 2 + 0.5;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ])
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenRefText,
      silkscreenLine,
      courtyard
    ],
    parameters
  };
};

// src/fn/sod80.ts
import { z as z44 } from "zod";
import { length as length38 } from "circuit-json";
var sod80_def = base_def.extend({
  fn: z44.string(),
  num_pins: z44.literal(2).default(2),
  w: z44.string().default("5.0mm"),
  h: z44.string().default("2.30mm"),
  pl: z44.string().default("1.25mm"),
  pw: z44.string().default("2mm"),
  p: z44.string().default("3.75mm")
});
var sod80 = (raw_params) => {
  const parameters = sod80_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length38.parse(parameters.h) / 2 + 1,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length38.parse(parameters.p) / 2 + 0.5,
        y: length38.parse(parameters.h) / 2 + 0.5
      },
      {
        x: -length38.parse(parameters.w) / 2 - 0.5,
        y: length38.parse(parameters.h) / 2 + 0.5
      },
      {
        x: -length38.parse(parameters.w) / 2 - 0.5,
        y: -length38.parse(parameters.h) / 2 - 0.5
      },
      {
        x: length38.parse(parameters.p) / 2 + 0.5,
        y: -length38.parse(parameters.h) / 2 - 0.5
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 5.1;
  const courtyardHeightMm = 2.8;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sod80WithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getsod80Coords = (parameters) => {
  const { pn, p } = parameters;
  return pn === 1 ? { x: -p / 2, y: 0 } : { x: p / 2, y: 0 };
};
var sod80WithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getsod80Coords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw)
      )
    );
  }
  return pads;
};

// src/fn/sod123w.ts
import { z as z45 } from "zod";
import { length as length39 } from "circuit-json";
var sod_def3 = base_def.extend({
  fn: z45.string(),
  num_pins: z45.literal(2).default(2),
  w: z45.string().default("4.4mm"),
  h: z45.string().default("1.7mm"),
  pl: z45.string().default("1.2mm"),
  pw: z45.string().default("1.2mm"),
  p: z45.string().default("2.8mm")
});
var sod123w = (raw_params) => {
  const parameters = sod_def3.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length39.parse(parameters.h) - 0.5,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length39.parse(parameters.p) / 2,
        y: length39.parse(parameters.h) / 2
      },
      {
        x: -length39.parse(parameters.w) / 2 - 0.2,
        y: length39.parse(parameters.h) / 2
      },
      {
        x: -length39.parse(parameters.w) / 2 - 0.2,
        y: -length39.parse(parameters.h) / 2
      },
      {
        x: length39.parse(parameters.p) / 2,
        y: -length39.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 4.5;
  const courtyardHeightMm = 2.2;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing3(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords3 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  }
  return { x: p / 2, y: 0 };
};
var sodWithoutParsing3 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords3({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw)
      )
    );
  }
  return pads;
};

// src/fn/sod323.ts
import { z as z46 } from "zod";
import { length as length40 } from "circuit-json";
var sod_def4 = base_def.extend({
  fn: z46.string(),
  num_pins: z46.literal(2).default(2),
  w: z46.string().default("3.30mm"),
  h: z46.string().default("1.80mm"),
  pl: z46.string().default("0.60mm"),
  pw: z46.string().default("0.45mm"),
  p: z46.string().default("2.1mm")
});
var sod323 = (raw_params) => {
  const parameters = sod_def4.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length40.parse(parameters.h) - 0.5,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length40.parse(parameters.p) / 2,
        y: length40.parse(parameters.h) / 2
      },
      {
        x: -length40.parse(parameters.w) / 2,
        y: length40.parse(parameters.h) / 2
      },
      {
        x: -length40.parse(parameters.w) / 2,
        y: -length40.parse(parameters.h) / 2
      },
      {
        x: length40.parse(parameters.p) / 2,
        y: -length40.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 3.2;
  const courtyardHeightMm = 1.9;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing4(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords4 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  }
  return { x: p / 2, y: 0 };
};
var sodWithoutParsing4 = (parameters) => {
  const pads = [];
  const p = length40.parse(parameters.p);
  const pl = length40.parse(parameters.pl);
  const pw = length40.parse(parameters.pw);
  const cornerRadius = 0.05625;
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords4({
      pn: i,
      p
    });
    pads.push(rectpad(i, x, y, pl, pw, cornerRadius));
  }
  return pads;
};

// src/fn/sod923.ts
import { z as z47 } from "zod";
import { length as length41 } from "circuit-json";
var sod_def5 = base_def.extend({
  fn: z47.string(),
  num_pins: z47.literal(2).default(2),
  w: z47.string().default("1.4mm"),
  h: z47.string().default("0.9mm"),
  pl: z47.string().default("0.36mm"),
  pw: z47.string().default("0.25mm"),
  p: z47.string().default("0.85mm")
});
var sod923 = (raw_params) => {
  const parameters = sod_def5.parse(raw_params);
  const w = length41.parse(parameters.w);
  const h = length41.parse(parameters.h);
  const pl = length41.parse(parameters.pl);
  const pw = length41.parse(parameters.pw);
  const p = length41.parse(parameters.p);
  const silkscreenRefText = silkscreenRef(0, h, 0.3);
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: p / 2 + 0.15,
        y: h / 2
      },
      {
        x: -w / 2 - 0.15,
        y: h / 2
      },
      {
        x: -w / 2 - 0.15,
        y: -h / 2
      },
      {
        x: p / 2 + 0.15,
        y: -h / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const pinRowSpanX = p + pl;
  const pinRowSpanY = pw;
  const bodyHalfX = w / 2;
  const bodyHalfY = h / 2;
  const pinToeHalfX = pinRowSpanX / 2;
  const pinRowHalfY = pinRowSpanY / 2;
  const courtyardEnvelopeHalfWidth = Math.max(bodyHalfX, pinToeHalfX);
  const courtyardEnvelopeHalfHeight = Math.max(bodyHalfY, pinRowHalfY);
  const courtyardNarrowHalfWidth = Math.min(bodyHalfX, pinToeHalfX);
  const courtyardNarrowHalfHeight = Math.min(bodyHalfY, pinRowHalfY);
  const courtyardStepOuterHalfWidth = courtyardEnvelopeHalfWidth + 0.05;
  const courtyardStepInnerHalfWidth = courtyardNarrowHalfWidth - 0.055;
  const courtyardStepOuterHalfHeight = courtyardEnvelopeHalfHeight;
  const courtyardStepInnerHalfHeight = courtyardNarrowHalfHeight + 0.155;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ]),
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing5(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords5 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing5 = (parameters) => {
  const pads = [];
  const p = length41.parse(parameters.p);
  const pl = length41.parse(parameters.pl);
  const pw = length41.parse(parameters.pw);
  const cornerRadius = 0.03125;
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords5({
      pn: i,
      p
    });
    pads.push(rectpad(i, x, y, pl, pw, cornerRadius));
  }
  return pads;
};

// src/fn/sod882.ts
import { z as z48 } from "zod";
import { length as length42 } from "circuit-json";
var sod_def6 = base_def.extend({
  fn: z48.string(),
  num_pins: z48.literal(2).default(2),
  w: z48.string().default("1.3mm"),
  h: z48.string().default("0.9mm"),
  pl: z48.string().default("0.4mm"),
  pw: z48.string().default("0.7mm"),
  p: z48.string().default("0.7mm")
});
var sod882 = (raw_params) => {
  const parameters = sod_def6.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length42.parse(parameters.h) + 0.1,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length42.parse(parameters.p) / 2 + 0.2,
        y: length42.parse(parameters.h) / 2 + 0.2
      },
      {
        x: -length42.parse(parameters.w) / 2 - 0.2,
        y: length42.parse(parameters.h) / 2 + 0.2
      },
      {
        x: -length42.parse(parameters.w) / 2 - 0.2,
        y: -length42.parse(parameters.h) / 2 - 0.2
      },
      {
        x: length42.parse(parameters.p) / 2 + 0.2,
        y: -length42.parse(parameters.h) / 2 - 0.2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 1.6;
  const courtyardHeightMm = 1.2;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing6(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords6 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing6 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords6({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.025
      )
    );
  }
  return pads;
};

// src/fn/sod323f.ts
import { z as z49 } from "zod";
import { length as length43 } from "circuit-json";
var sod_def7 = base_def.extend({
  fn: z49.string(),
  num_pins: z49.literal(2).default(2),
  w: z49.string().default("3.05mm"),
  h: z49.string().default("1.4mm"),
  pl: z49.string().default("0.5mm"),
  pw: z49.string().default("0.5mm"),
  pad_spacing: z49.string().default("2.2mm")
});
var sod323f = (raw_params) => {
  const parameters = sod_def7.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length43.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length43.parse(parameters.pad_spacing) / 2,
        y: length43.parse(parameters.h) / 2
      },
      {
        x: -length43.parse(parameters.w) / 2 - 0.2,
        y: length43.parse(parameters.h) / 2
      },
      {
        x: -length43.parse(parameters.w) / 2 - 0.2,
        y: -length43.parse(parameters.h) / 2
      },
      {
        x: length43.parse(parameters.pad_spacing) / 2,
        y: -length43.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 3.2;
  const courtyardHeightMm = 1.9;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing7(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords7 = (parameters) => {
  const { pn, pad_spacing } = parameters;
  if (pn === 1) {
    return { x: -pad_spacing / 2, y: 0 };
  } else {
    return { x: pad_spacing / 2, y: 0 };
  }
};
var sodWithoutParsing7 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords7({
      pn: i,
      pad_spacing: Number.parseFloat(parameters.pad_spacing)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.0625
      )
    );
  }
  return pads;
};

// src/fn/sod123f.ts
import { z as z50 } from "zod";
import { length as length44 } from "circuit-json";
var sod_def8 = base_def.extend({
  fn: z50.string(),
  num_pins: z50.literal(2).default(2),
  w: z50.string().default("4.4mm"),
  h: z50.string().default("2.1mm"),
  pl: z50.string().default("1.10mm"),
  pw: z50.string().default("1.10mm"),
  p: z50.string().default("2.8mm")
});
var sod123f = (raw_params) => {
  const parameters = sod_def8.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length44.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length44.parse(parameters.p) / 2,
        y: length44.parse(parameters.h) / 2
      },
      {
        x: -length44.parse(parameters.w) / 2 - 0.2,
        y: length44.parse(parameters.h) / 2
      },
      {
        x: -length44.parse(parameters.w) / 2 - 0.2,
        y: -length44.parse(parameters.h) / 2
      },
      {
        x: length44.parse(parameters.p) / 2,
        y: -length44.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 4.4;
  const courtyardHeightMm = 2.3;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing8(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords8 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing8 = (parameters) => {
  const pads = [];
  const p = length44.parse(parameters.p);
  const pl = length44.parse(parameters.pl);
  const pw = length44.parse(parameters.pw);
  const cornerRadius = 0.125;
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords8({
      pn: i,
      p
    });
    pads.push(rectpad(i, x, y, pl, pw, cornerRadius));
  }
  return pads;
};

// src/fn/sod123fl.ts
import { z as z51 } from "zod";
import { length as length45 } from "circuit-json";
var sod123FL_def = base_def.extend({
  fn: z51.string(),
  num_pins: z51.literal(2).default(2),
  w: z51.string().default("4.4mm"),
  h: z51.string().default("2.1mm"),
  pl: z51.string().default("0.91mm"),
  pw: z51.string().default("1.22mm"),
  p: z51.string().default("3.146mm")
});
var sod123fl = (raw_params) => {
  const parameters = sod123FL_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length45.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length45.parse(parameters.p) / 2,
        y: length45.parse(parameters.h) / 2
      },
      {
        x: -length45.parse(parameters.w) / 2 - 0.2,
        y: length45.parse(parameters.h) / 2
      },
      {
        x: -length45.parse(parameters.w) / 2 - 0.2,
        y: -length45.parse(parameters.h) / 2
      },
      {
        x: length45.parse(parameters.p) / 2,
        y: -length45.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 4.9;
  const courtyardHeightMm = 2.7;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing9(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords9 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing9 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords9({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw)
      )
    );
  }
  return pads;
};

// src/fn/sod723.ts
import { z as z52 } from "zod";
import { length as length46 } from "circuit-json";
var sod_def9 = base_def.extend({
  fn: z52.string(),
  num_pins: z52.literal(2).default(2),
  w: z52.string().default("1.80mm"),
  h: z52.string().default("1.00mm"),
  pl: z52.string().default("0.66mm"),
  pw: z52.string().default("0.5mm"),
  p: z52.string().default("0.8mm")
});
var sod723 = (raw_params) => {
  const parameters = sod_def9.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length46.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length46.parse(parameters.p) / 2,
        y: length46.parse(parameters.h) / 2
      },
      {
        x: -length46.parse(parameters.w) / 2 - 0.1,
        y: length46.parse(parameters.h) / 2
      },
      {
        x: -length46.parse(parameters.w) / 2 - 0.1,
        y: -length46.parse(parameters.h) / 2
      },
      {
        x: length46.parse(parameters.p) / 2,
        y: -length46.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 2.3;
  const courtyardHeightMm = 1.5;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing10(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords10 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing10 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords10({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw)
      )
    );
  }
  return pads;
};

// src/fn/sod128.ts
import { z as z53 } from "zod";
import { length as length47 } from "circuit-json";
var sod_def10 = base_def.extend({
  fn: z53.string(),
  num_pins: z53.literal(2).default(2),
  w: z53.string().default("6.2mm"),
  h: z53.string().default("3.4mm"),
  pl: z53.string().default("1.4mm"),
  pw: z53.string().default("2.1mm"),
  p: z53.string().default("4.4mm")
});
var sod128 = (raw_params) => {
  const parameters = sod_def10.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length47.parse(parameters.h) / 2 + 0.4,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length47.parse(parameters.p) / 2,
        y: length47.parse(parameters.h) / 2
      },
      {
        x: -length47.parse(parameters.w) / 2 - 0.2,
        y: length47.parse(parameters.h) / 2
      },
      {
        x: -length47.parse(parameters.w) / 2 - 0.2,
        y: -length47.parse(parameters.h) / 2
      },
      {
        x: length47.parse(parameters.p) / 2,
        y: -length47.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 6.3;
  const courtyardHeightMm = 3;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing11(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords11 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing11 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords11({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.125
      )
    );
  }
  return pads;
};

// src/fn/sot89.ts
import { z as z54 } from "zod";
var sot89_3CourtyardOutline = [
  { x: -2.85, y: -2.5 },
  { x: -2.85, y: 2.5 },
  { x: 2.25, y: 2.5 },
  { x: 2.25, y: -2.5 }
];
var sot89_5CourtyardOutline = [
  { x: -2.85, y: -2.5 },
  { x: -2.85, y: 2.5 },
  { x: 2.85, y: 2.5 },
  { x: 2.85, y: -2.5 }
];
var sot89_5CenterPadPoints = [
  { x: 0.9, y: 0.5 },
  { x: 2.6, y: 0.5 },
  { x: 2.6, y: -0.5 },
  { x: 0.9, y: -0.5 },
  { x: 0.4, y: -1 },
  { x: -0.4, y: -1 },
  { x: -0.9, y: -0.5 },
  { x: -2.6, y: -0.5 },
  { x: -2.6, y: 0.5 },
  { x: -0.9, y: 0.5 },
  { x: -0.4, y: 1 },
  { x: 0.4, y: 1 }
];
var sot89_3CenterPadPoints = [
  { x: 2, y: -0.8665 },
  { x: -1.125, y: -0.8665 },
  { x: -1.125, y: -0.45 },
  { x: -2.6, y: -0.45 },
  { x: -2.6, y: 0.45 },
  { x: -1.125, y: 0.45 },
  { x: -1.125, y: 0.8665 },
  { x: 2, y: 0.8665 }
];
var sot89_def = base_def.extend({
  fn: z54.string(),
  num_pins: z54.union([z54.literal(3), z54.literal(5)]).default(3),
  w: z54.string().default("4.20mm"),
  h: z54.string().default("4.80mm"),
  pl: z54.string().default("1.3mm"),
  pw: z54.string().default("0.9mm"),
  p: z54.string().default("1.5mm"),
  string: z54.string().optional()
});
var sot89_3 = (parameters) => {
  const padGap = Number.parseFloat(parameters.p);
  const padWidth = Number.parseFloat(parameters.pw);
  const length80 = Number.parseFloat(parameters.w);
  const padHeight = Number.parseFloat(parameters.pl);
  const outerPadCornerRadius = Math.min(padHeight, padWidth) / 8;
  const outerPadX = -length80 / 2 + 0.15;
  const width = length80 / 2 - 1;
  const height = Number.parseFloat(parameters.h) / 2;
  const pads = [
    rectpad(1, outerPadX, padGap, padHeight, padWidth, outerPadCornerRadius),
    polygonpad(2, sot89_3CenterPadPoints),
    rectpad(3, outerPadX, -padGap, padHeight, padWidth, outerPadCornerRadius)
  ];
  const silkscreenRefText = silkscreenRef(0, height + 0.5, 0.3);
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width, y: height },
      { x: width, y: height },
      { x: width, y: height / 2 + 0.5 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width, y: -height },
      { x: width, y: -height },
      { x: width, y: -height / 2 - 0.5 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot89_3CourtyardOutline,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenPath1,
    silkscreenPath2,
    silkscreenRefText,
    courtyard
  ];
};
var sot89_5 = (parameters) => {
  const outerPadCornerRadius = Math.min(1.5, 0.7) / 8;
  const pads = [
    rectpad(1, -1.85, -1.5, 1.5, 0.7, outerPadCornerRadius),
    rectpad(2, -1.85, 1.5, 1.5, 0.7, outerPadCornerRadius),
    polygonpad(3, sot89_5CenterPadPoints),
    rectpad(4, 1.85, -1.5, 1.5, 0.7, outerPadCornerRadius),
    rectpad(5, 1.85, 1.5, 1.5, 0.7, outerPadCornerRadius)
  ];
  const width = Number.parseFloat(parameters.w) / 2 - 1;
  const height = Number.parseFloat(parameters.h) / 2;
  const silkscreenRefText = silkscreenRef(0, height + 0.5, 0.3);
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width, y: height },
      { x: width, y: height }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width, y: -height },
      { x: width, y: -height }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot89_5CourtyardOutline,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenPath1,
    silkscreenPath2,
    silkscreenRefText,
    courtyard
  ];
};
var sot89 = (raw_params) => {
  const match = raw_params.string?.match(/^sot89_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 10) : 3;
  const parameters = sot89_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  if (parameters.num_pins === 3) {
    return {
      circuitJson: sot89_3(parameters),
      parameters
    };
  }
  if (parameters.num_pins === 5) {
    return {
      circuitJson: sot89_5(parameters),
      parameters
    };
  }
  throw new Error("Invalid number of pins for SOT89");
};

// src/fn/to220.ts
import {
  length as length48
} from "circuit-json";
import { z as z55 } from "zod";
var to220_def = base_def.extend({
  fn: z55.string(),
  p: length48.optional().default("5.0mm"),
  id: length48.optional().default("1.0mm"),
  od: length48.optional().default("1.9mm"),
  w: length48.optional().default("13mm"),
  h: length48.optional().default("7mm"),
  num_pins: z55.number().optional(),
  string: z55.string().optional()
});
var to220 = (raw_params) => {
  const parameters = to220_def.parse(raw_params);
  const { id, od, w, h, string: string3 } = parameters;
  const numPins = parameters.num_pins ?? Number.parseInt(string3?.match(/^to220(?:_|-)(\d+)/i)?.[1] ?? "3");
  const holeY = -1;
  const halfWidth = w / 2;
  const halfHeight = h / 2;
  const minPitch = 2.5;
  const maxHoleWidth = w * 0.4;
  const computedPitch = Math.max(minPitch, maxHoleWidth / (numPins - 1));
  const plated_holes = Array.from({ length: numPins }, (_, i) => {
    const x = numPins % 2 === 0 ? (i - numPins / 2 + 0.5) * computedPitch : (i - Math.floor(numPins / 2)) * computedPitch;
    return platedhole(i + 1, x, holeY, id, od);
  });
  const silkscreenBody = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -halfWidth, y: -halfHeight },
      { x: halfWidth, y: -halfHeight },
      { x: halfWidth, y: halfHeight },
      { x: -halfWidth, y: halfHeight },
      { x: -halfWidth, y: -halfHeight }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const yLine = -halfHeight + 2 * h / 3;
  const horizontalLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -halfWidth, y: yLine },
      { x: halfWidth, y: yLine }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const verticalLines = [
    {
      type: "pcb_silkscreen_path",
      layer: "top",
      pcb_component_id: "",
      route: [
        { x: -w / 6, y: yLine },
        { x: -w / 6, y: halfHeight }
      ],
      stroke_width: 0.1,
      pcb_silkscreen_path_id: ""
    },
    {
      type: "pcb_silkscreen_path",
      layer: "top",
      pcb_component_id: "",
      route: [
        { x: w / 6, y: yLine },
        { x: w / 6, y: halfHeight }
      ],
      stroke_width: 0.1,
      pcb_silkscreen_path_id: ""
    }
  ];
  const silkscreenRefText = silkscreenRef(0, h / 2 + 0.6, 0.5);
  const pinToeHalfSpanX = Math.max(...plated_holes.map((hole) => Math.abs(hole.x))) + od / 2;
  const pinToeTopY = holeY + od / 2;
  const pinToeBottomY = holeY - od / 2;
  const courtyardHalfWidth = Math.max(
    pinToeHalfSpanX + 0.25,
    halfWidth - od * 0.59
  );
  const courtyardTopY = halfHeight - od * 0.63;
  const courtyardBottomY = pinToeBottomY - (od / 2 + 0.01);
  const crtMinX = -courtyardHalfWidth;
  const crtMaxX = courtyardHalfWidth;
  const crtMinY = Math.min(courtyardBottomY, pinToeBottomY - 0.25);
  const crtMaxY = Math.max(courtyardTopY, pinToeTopY + 0.25);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: (crtMinX + crtMaxX) / 2, y: (crtMinY + crtMaxY) / 2 },
    width: crtMaxX - crtMinX,
    height: crtMaxY - crtMinY,
    layer: "top"
  };
  return {
    circuitJson: [
      ...plated_holes,
      silkscreenBody,
      horizontalLine,
      ...verticalLines,
      silkscreenRefText,
      courtyard
    ],
    parameters: { ...parameters, p: computedPitch }
  };
};

// src/fn/to220f.ts
import { mm as mm14 } from "@tscircuit/mm";
import { length as length49 } from "circuit-json";
import { z as z56 } from "zod";
var TO220F_PITCH_MM = 2.54;
var to220f_def = base_def.extend({
  fn: z56.string(),
  // KiCad TO-220F-3_Vertical: hole=1.2mm, pad=1.905×2.0mm, pitch=2.54mm
  id: length49.optional().default("1.2mm"),
  od: length49.optional().default("1.905mm"),
  ph: length49.optional().default("2mm"),
  w: length49.optional().default("13mm"),
  h: length49.optional().default("7mm"),
  num_pins: z56.number().optional(),
  string: z56.string().optional()
});
var to220f = (raw_params) => {
  const parameters = to220f_def.parse(raw_params);
  const numPins = parameters.num_pins ?? Number.parseInt(
    parameters.string?.match(/^to220f(?:_|-)(\d+)/i)?.[1] ?? "3"
  );
  const baseResult = to220({
    ...parameters,
    fn: "to220",
    string: parameters.string?.replace(/^to220f/i, "to220"),
    num_pins: numPins
  });
  const holeY = -1;
  const newHoles = Array.from(
    { length: numPins },
    (_, i) => {
      const x = numPins % 2 === 0 ? (i - numPins / 2 + 0.5) * TO220F_PITCH_MM : (i - Math.floor(numPins / 2)) * TO220F_PITCH_MM;
      if (i === 0) {
        return platedHoleWithRectPad({
          pn: 1,
          x,
          y: holeY,
          holeDiameter: parameters.id,
          rectPadWidth: parameters.od,
          rectPadHeight: parameters.ph
        });
      }
      return platedHolePill(
        i + 1,
        x,
        holeY,
        mm14(parameters.id),
        mm14(parameters.od),
        mm14(parameters.ph)
      );
    }
  );
  const nonHoleElements = baseResult.circuitJson.filter(
    (e) => e.type !== "pcb_plated_hole"
  );
  return {
    circuitJson: [...newHoles, ...nonHoleElements],
    parameters: { ...parameters, p: TO220F_PITCH_MM, num_pins: numPins }
  };
};

// src/fn/minimelf.ts
import { z as z57 } from "zod";
import { length as length50 } from "circuit-json";
var minimelf_def = base_def.extend({
  fn: z57.string(),
  num_pins: z57.literal(2).default(2),
  w: z57.string().default("5.40mm"),
  h: z57.string().default("2.30mm"),
  pl: z57.string().default("1.30mm"),
  pw: z57.string().default("1.70mm"),
  p: z57.string().default("3.5mm")
});
var minimelf = (raw_params) => {
  const parameters = minimelf_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length50.parse(parameters.h) / 2 + 0.4,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length50.parse(parameters.p) / 2,
        y: length50.parse(parameters.h) / 2
      },
      {
        x: -length50.parse(parameters.w) / 2,
        y: length50.parse(parameters.h) / 2
      },
      {
        x: -length50.parse(parameters.w) / 2,
        y: -length50.parse(parameters.h) / 2
      },
      {
        x: length50.parse(parameters.p) / 2,
        y: -length50.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 5.3;
  const courtyardHeightMm = 2.2;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: miniMelfWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getMiniMelfCoords = (parameters) => {
  const { pn, p } = parameters;
  return pn === 1 ? { x: -p / 2, y: 0 } : { x: p / 2, y: 0 };
};
var miniMelfWithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getMiniMelfCoords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.125
      )
    );
  }
  return pads;
};

// src/fn/sod882d.ts
import { z as z58 } from "zod";
import { length as length51 } from "circuit-json";
var sod_def11 = base_def.extend({
  fn: z58.string(),
  num_pins: z58.literal(2).default(2),
  w: z58.string().default("1.90mm"),
  h: z58.string().default("1.33mm"),
  pl: z58.string().default("0.5mm"),
  pw: z58.string().default("0.7mm"),
  p: z58.string().default("0.8mm")
});
var sod882d = (raw_params) => {
  const parameters = sod_def11.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length51.parse(parameters.h) + 0.1,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length51.parse(parameters.p) / 2 + 0.1,
        y: length51.parse(parameters.h) / 2
      },
      {
        x: -length51.parse(parameters.w) / 2,
        y: length51.parse(parameters.h) / 2
      },
      {
        x: -length51.parse(parameters.w) / 2,
        y: -length51.parse(parameters.h) / 2
      },
      {
        x: length51.parse(parameters.p) / 2 + 0.1,
        y: -length51.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 1.8;
  const courtyardHeightMm = 1.2;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing12(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords12 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var sodWithoutParsing12 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords12({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.0625
      )
    );
  }
  return pads;
};

// src/fn/melf.ts
import { z as z59 } from "zod";
import { length as length52 } from "circuit-json";
var melf_def = base_def.extend({
  fn: z59.string(),
  num_pins: z59.literal(2).default(2),
  w: z59.string().default("7.0mm"),
  h: z59.string().default("3.35mm"),
  pl: z59.string().default("1.50mm"),
  pw: z59.string().default("2.70mm"),
  p: z59.string().default("4.8mm")
});
var melf = (raw_params) => {
  const parameters = melf_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length52.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length52.parse(parameters.p) / 2,
        y: length52.parse(parameters.h) / 2
      },
      {
        x: -length52.parse(parameters.w) / 2,
        y: length52.parse(parameters.h) / 2
      },
      {
        x: -length52.parse(parameters.w) / 2,
        y: -length52.parse(parameters.h) / 2
      },
      {
        x: length52.parse(parameters.p) / 2,
        y: -length52.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 6.8;
  const courtyardHeightMm = 3.2;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: melfWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getMelfCoords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var melfWithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getMelfCoords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.125
      )
    );
  }
  return pads;
};

// src/fn/micromelf.ts
import { z as z60 } from "zod";
import { length as length53 } from "circuit-json";
var micromelf_def = base_def.extend({
  fn: z60.string(),
  num_pins: z60.literal(2).default(2),
  w: z60.string().default("3.0mm"),
  h: z60.string().default("1.80mm"),
  pl: z60.string().default("0.80mm"),
  pw: z60.string().default("1.20mm"),
  p: z60.string().default("1.6mm")
});
var micromelf = (raw_params) => {
  const parameters = micromelf_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length53.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length53.parse(parameters.p) / 2,
        y: length53.parse(parameters.h) / 2
      },
      {
        x: -length53.parse(parameters.w) / 2 - 0.1,
        y: length53.parse(parameters.h) / 2
      },
      {
        x: -length53.parse(parameters.w) / 2 - 0.1,
        y: -length53.parse(parameters.h) / 2
      },
      {
        x: length53.parse(parameters.p) / 2,
        y: -length53.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 2.9;
  const courtyardHeightMm = 1.7;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: microMelfWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getMicroMelfCoords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var microMelfWithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getMicroMelfCoords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.1
      )
    );
  }
  return pads;
};

// src/fn/sma.ts
import { z as z61 } from "zod";
import { length as length54 } from "circuit-json";
var sma_def = base_def.extend({
  fn: z61.string(),
  num_pins: z61.literal(2).default(2),
  w: z61.string().default("7.10mm"),
  h: z61.string().default("3.40mm"),
  pl: z61.string().default("2.50mm"),
  pw: z61.string().default("1.80mm"),
  p: z61.string().default("4.00mm")
});
var sma = (raw_params) => {
  const parameters = sma_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length54.parse(parameters.h) / 2 + 0.5,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length54.parse(parameters.p) / 2,
        y: length54.parse(parameters.h) / 2
      },
      {
        x: -length54.parse(parameters.w) / 2 - 0.5,
        y: length54.parse(parameters.h) / 2
      },
      {
        x: -length54.parse(parameters.w) / 2 - 0.5,
        y: -length54.parse(parameters.h) / 2
      },
      {
        x: length54.parse(parameters.p) / 2,
        y: -length54.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 7;
  const courtyardHeightMm = 3.5;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: smaWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSmaCoords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  }
  return { x: p / 2, y: 0 };
};
var smaWithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSmaCoords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.125
      )
    );
  }
  return pads;
};

// src/fn/smf.ts
import { z as z62 } from "zod";
import { length as length55 } from "circuit-json";
var smf_def = base_def.extend({
  fn: z62.string(),
  num_pins: z62.literal(2).default(2),
  w: z62.string().default("4.80mm"),
  h: z62.string().default("2.10mm"),
  pl: z62.string().default("1.30mm"),
  pw: z62.string().default("1.40mm"),
  p: z62.string().default("2.9mm")
});
var smf = (raw_params) => {
  const parameters = smf_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length55.parse(parameters.h) - 0.5,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length55.parse(parameters.p) / 2,
        y: length55.parse(parameters.h) / 2
      },
      {
        x: -length55.parse(parameters.w) / 2,
        y: length55.parse(parameters.h) / 2
      },
      {
        x: -length55.parse(parameters.w) / 2,
        y: -length55.parse(parameters.h) / 2
      },
      {
        x: length55.parse(parameters.p) / 2,
        y: -length55.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 4.7;
  const courtyardHeightMm = 2.3;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: smfWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSmfCoords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var smfWithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSmfCoords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.125
      )
    );
  }
  return pads;
};

// src/fn/smb.ts
import { z as z63 } from "zod";
import { length as length56 } from "circuit-json";
var smb_def = base_def.extend({
  fn: z63.string(),
  num_pins: z63.literal(2).default(2),
  w: z63.string().default("7.30mm"),
  h: z63.string().default("4.40mm"),
  pl: z63.string().default("2.50mm"),
  pw: z63.string().default("2.30mm"),
  p: z63.string().default("4.30mm")
});
var smb = (raw_params) => {
  const parameters = smb_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length56.parse(parameters.h) / 2 + 0.5,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length56.parse(parameters.p) / 2,
        y: length56.parse(parameters.h) / 2
      },
      {
        x: -length56.parse(parameters.w) / 2 - 0.1,
        y: length56.parse(parameters.h) / 2
      },
      {
        x: -length56.parse(parameters.w) / 2 - 0.1,
        y: -length56.parse(parameters.h) / 2
      },
      {
        x: length56.parse(parameters.p) / 2,
        y: -length56.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 7.3;
  const courtyardHeightMm = 4.5;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: smbWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSmbCoords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  } else {
    return { x: p / 2, y: 0 };
  }
};
var smbWithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSmbCoords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.125
      )
    );
  }
  return pads;
};

// src/fn/smc.ts
import { z as z64 } from "zod";
import { length as length57 } from "circuit-json";
var smc_def = base_def.extend({
  fn: z64.string(),
  num_pins: z64.literal(2).default(2),
  w: z64.string().default("10.70mm"),
  h: z64.string().default("6.60mm"),
  pl: z64.string().default("2.50mm"),
  pw: z64.string().default("3.30mm"),
  p: z64.string().default("6.80mm")
});
var smc = (raw_params) => {
  const parameters = smc_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(0, 3, 0.3);
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length57.parse(parameters.p) / 2,
        y: length57.parse(parameters.h) / 2 - 0.8
      },
      {
        x: -length57.parse(parameters.w) / 2 - 0.8,
        y: length57.parse(parameters.h) / 2 - 0.8
      },
      {
        x: -length57.parse(parameters.w) / 2 - 0.8,
        y: -length57.parse(parameters.h) / 2 + 0.8
      },
      {
        x: length57.parse(parameters.p) / 2,
        y: -length57.parse(parameters.h) / 2 + 0.8
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 9.8;
  const courtyardHeightMm = 6.7;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: smcWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSmcCoords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  }
  return { x: p / 2, y: 0 };
};
var smcWithoutParsing = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSmcCoords({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.125
      )
    );
  }
  return pads;
};

// src/fn/sot223.ts
import { z as z65 } from "zod";
var sot223_def = base_def.extend({
  fn: z65.string(),
  num_pins: z65.number().default(4),
  w: z65.string().default("8.50mm"),
  h: z65.string().default("6.90mm"),
  pl: z65.string().default("2mm"),
  pw: z65.string().default("1.5mm"),
  p: z65.string().default("2.30mm"),
  tabpl: z65.string().optional(),
  tabpw: z65.string().default("3.8mm"),
  taboffset: z65.string().default("0mm"),
  string: z65.string().optional()
});
var sot223 = (raw_params) => {
  const match = raw_params.string?.match(/^sot223_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 10) : 4;
  if (numPins === 8) {
    const parameters2 = sot223_8_def.parse({
      ...raw_params,
      num_pins: numPins
    });
    return {
      circuitJson: soicWithoutParsing(parameters2),
      parameters: parameters2
    };
  }
  const parameters = sot223_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  if (parameters.num_pins === 4) {
    return {
      circuitJson: sot223_4(parameters),
      parameters
    };
  }
  if (parameters.num_pins === 5) {
    return {
      circuitJson: sot223_5(parameters),
      parameters
    };
  }
  if (parameters.num_pins === 6) {
    return {
      circuitJson: sot223_6(parameters),
      parameters
    };
  }
  throw new Error("Invalid number of pins");
};
var get2CcwSot223Coords = (parameters) => {
  const { pn, w, h, pl, p, taboffset } = parameters;
  if (pn === 1) {
    return { x: -w / 2 + 1.1, y: p };
  }
  if (pn === 2) {
    return { x: -w / 2 + 1.1, y: 0 };
  }
  if (pn === 3) {
    return { x: -w / 2 + 1.1, y: -p };
  }
  return { x: w / 2 - 1.1 + taboffset, y: 0 };
};
var sot223_4 = (parameters) => {
  const pads = [];
  let padOuterHalfWidth = 0;
  let padOuterHalfHeight = 0;
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  const pl = Number.parseFloat(parameters.pl);
  const p = Number.parseFloat(parameters.p);
  const pw = Number.parseFloat(parameters.pw);
  const tabpl = Number.parseFloat(parameters.tabpl ?? parameters.pl);
  const tabpw = Number.parseFloat(parameters.tabpw);
  const taboffset = Number.parseFloat(parameters.taboffset);
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = get2CcwSot223Coords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w,
      h,
      pl,
      p,
      taboffset
    });
    const pinWidth = i === 3 ? tabpw : pw;
    const pinLength = i === 3 ? tabpl : pl;
    const cornerRadius = Math.min(pinLength, pinWidth) / 8;
    padOuterHalfWidth = Math.max(padOuterHalfWidth, Math.abs(x) + pinLength / 2);
    padOuterHalfHeight = Math.max(
      padOuterHalfHeight,
      Math.abs(y) + pinWidth / 2
    );
    pads.push(rectpad(i + 1, x, y, pinLength, pinWidth, cornerRadius));
  }
  const silkscreenRefText = silkscreenRef(0, 0, 0.3);
  const width = w / 2 - 2.4;
  const height = h / 2;
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width, y: height },
      { x: width, y: height },
      { x: width, y: height / 2 + 0.5 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width, y: -height },
      { x: width, y: -height },
      { x: width, y: -height / 2 - 0.5 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const bodyHalfWidth = w / 2;
  const bodyHalfHeight = h / 2;
  const courtyardHalfWidth = Math.max(
    padOuterHalfWidth + 0.25,
    bodyHalfWidth + 0.15
  );
  const courtyardHalfHeight = Math.max(
    padOuterHalfHeight + 0.25,
    bodyHalfHeight + 0.15
  );
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: 2 * courtyardHalfWidth,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenPath1,
    silkscreenPath2,
    silkscreenRefText,
    courtyard
  ];
};
var sot223_8_def = extendSoicDef({
  p: "0.90mm",
  w: "2.8mm",
  legsoutside: true
});
var get2CcwSot2235Coords = (parameters) => {
  const { p, h, pn, w } = parameters;
  if (pn === 1) {
    return { x: -w / 2 + 1.2, y: p / 2 + p };
  }
  if (pn === 2) {
    return { x: -w / 2 + 1.2, y: p / 2 };
  }
  if (pn === 3) {
    return { x: -w / 2 + 1.2, y: -p / 2 };
  }
  if (pn === 4) {
    return { x: -w / 2 + 1.2, y: -p / 2 - p };
  }
  if (pn === 5) {
    return { x: w / 2 - 1, y: 0 };
  }
  throw new Error("Invalid pin number");
};
var sot223_5 = (parameters) => {
  const pads = [];
  let padOuterHalfWidth = 0;
  let padOuterHalfHeight = 0;
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = get2CcwSot2235Coords({
      h,
      p: 1.5,
      pn: i,
      w
    });
    let pinWidth = Number.parseFloat(parameters.pw);
    let pinLength = Number.parseFloat(parameters.pl);
    if (i === 5) {
      pinWidth = 3.4;
      pinLength = 1.8;
    } else {
      pinWidth = 1;
      pinLength = 2.2;
    }
    const cornerRadius = Math.min(pinLength, pinWidth) / 8;
    padOuterHalfWidth = Math.max(padOuterHalfWidth, Math.abs(x) + pinLength / 2);
    padOuterHalfHeight = Math.max(
      padOuterHalfHeight,
      Math.abs(y) + pinWidth / 2
    );
    pads.push(rectpad(i, x, y, pinLength, pinWidth, cornerRadius));
  }
  const width = w / 2 - 2.4;
  const height = h / 2;
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width, y: height },
      { x: width, y: height },
      { x: width, y: height / 2 + 0.5 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width, y: -height },
      { x: width, y: -height },
      { x: width, y: -height / 2 - 0.5 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenRefText = silkscreenRef(0, 0, 0.3);
  const bodyHalfWidth = w / 2;
  const bodyHalfHeight = h / 2;
  const courtyardHalfWidth = Math.max(
    padOuterHalfWidth + 0.25,
    bodyHalfWidth + 0.15
  );
  const courtyardHalfHeight = Math.max(
    padOuterHalfHeight + 0.25,
    bodyHalfHeight + 0.15
  );
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: 2 * courtyardHalfWidth,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenPath1,
    silkscreenPath2,
    silkscreenRefText,
    courtyard
  ];
};
var get2CcwSot2236Coords = (parameters) => {
  const { p, h, pn, w } = parameters;
  if (pn === 1) {
    return { x: -w / 2 + 1.2, y: 2 * p };
  }
  if (pn === 2) {
    return { x: -w / 2 + 1.2, y: p };
  }
  if (pn === 3) {
    return { x: -w / 2 + 1.2, y: 0 };
  }
  if (pn === 4) {
    return { x: -w / 2 + 1.2, y: -p };
  }
  if (pn === 5) {
    return { x: -w / 2 + 1.2, y: -2 * p };
  }
  if (pn === 6) {
    return { x: w / 2 - 1.175, y: 0 };
  }
  throw new Error("Invalid pin number");
};
var sot223_6 = (parameters) => {
  const pads = [];
  let padOuterHalfWidth = 0;
  let padOuterHalfHeight = 0;
  const h = Number.parseFloat(parameters.h);
  const w = 8.7;
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = get2CcwSot2236Coords({
      h,
      p: 1.3,
      pn: i,
      w
    });
    let pinWidth = Number.parseFloat(parameters.pw);
    let pinLength = Number.parseFloat(parameters.pl);
    if (i === 6) {
      pinWidth = 3.4;
      pinLength = 2.15;
    } else {
      pinWidth = 0.6;
      pinLength = 2.2;
    }
    const cornerRadius = Math.min(pinLength, pinWidth) / 8;
    padOuterHalfWidth = Math.max(padOuterHalfWidth, Math.abs(x) + pinLength / 2);
    padOuterHalfHeight = Math.max(
      padOuterHalfHeight,
      Math.abs(y) + pinWidth / 2
    );
    pads.push(rectpad(i, x, y, pinLength, pinWidth, cornerRadius));
  }
  const width = Number.parseFloat(parameters.w) / 2 - 2.4;
  const height = h / 2;
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width, y: height },
      { x: width, y: height },
      { x: width, y: height / 2 + 0.5 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width, y: -height },
      { x: width, y: -height },
      { x: width, y: -height / 2 - 0.5 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenRefText = silkscreenRef(0, 0, 0.3);
  const bodyHalfWidth = Number.parseFloat(parameters.w) / 2;
  const bodyHalfHeight = h / 2;
  const courtyardHalfWidth = Math.max(
    padOuterHalfWidth + 0.25,
    bodyHalfWidth + 0.15
  );
  const courtyardHalfHeight = Math.max(
    padOuterHalfHeight + 0.25,
    bodyHalfHeight + 0.15
  );
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: 2 * courtyardHalfWidth,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenPath1,
    silkscreenPath2,
    silkscreenRefText,
    courtyard
  ];
};

// src/fn/sot23w.ts
import { z as z66 } from "zod";
var sot23w_def = base_def.extend({
  fn: z66.string(),
  num_pins: z66.number().default(3),
  w: z66.string().default("3.40mm"),
  h: z66.string().default("3.30mm"),
  pl: z66.string().default("1mm"),
  pw: z66.string().default("0.7mm"),
  p: z66.string().default("1.2mm"),
  string: z66.string().optional()
});
var sot23w = (raw_params) => {
  const match = raw_params.string?.match(/^sot23w_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 3) : 3;
  const parameters = sot23w_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  if (parameters.num_pins === 3) {
    return {
      circuitJson: sot23w_3(parameters),
      parameters
    };
  }
  throw new Error("Invalid number of pins");
};
var getCcwSot23wCoords = (parameters) => {
  const { pn, w, h, pl, p } = parameters;
  if (pn === 1) {
    return { x: -p, y: 0.95 };
  }
  if (pn === 2) {
    return { x: -p, y: -0.95 };
  }
  return { x: p, y: 0 };
};
var sot23w_3 = (parameters) => {
  const pads = [];
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  const pl = Number.parseFloat(parameters.pl);
  const pw = Number.parseFloat(parameters.pw);
  const p = Number.parseFloat(parameters.p);
  const cornerRadius = Math.min(pl, pw) / 8;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getCcwSot23wCoords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w,
      h,
      pl,
      p
    });
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  const silkscreenRefText = silkscreenRef(
    0,
    Number.parseInt(parameters.h) / 2 + 1,
    0.3
  );
  const width = w / 2 - 1;
  const height = h / 2;
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width, y: height },
      { x: width + 0.3, y: height },
      { x: width + 0.3, y: height / 2 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width, y: -height },
      { x: width + 0.3, y: -height },
      { x: width + 0.3, y: -height / 2 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const padToeHalfWidth = p + pl / 2;
  const pinRowHalfHeight = 0.95 + pw / 2;
  const bodyHalfHeight = h / 2;
  const courtyardHalfWidth = padToeHalfWidth + 0.25;
  const courtyardHalfHeight = Math.max(
    pinRowHalfHeight + 0.25,
    bodyHalfHeight + 0.09
  );
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: 2 * courtyardHalfWidth,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenPath1,
    silkscreenPath2,
    silkscreenRefText,
    courtyard
  ];
};

// src/fn/to92s.ts
import { z as z67 } from "zod";
var to92s_def = base_def.extend({
  fn: z67.string(),
  num_pins: z67.union([z67.literal(3), z67.literal(2)]).default(3),
  p: z67.string().default("1.27mm"),
  id: z67.string().default("0.72mm"),
  od: z67.string().default("0.95mm"),
  w: z67.string().default("2.5mm"),
  h: z67.string().default("4.2mm"),
  string: z67.string().optional()
});
var to92s_3 = (parameters) => {
  const { p, id, od, w, h } = parameters;
  const holeY = Number.parseFloat(h) / 2;
  const padSpacing7 = Number.parseFloat(p);
  return [
    platedhole(1, -padSpacing7, holeY - padSpacing7, id, od),
    platedhole(2, 0, holeY - padSpacing7, id, od),
    platedhole(3, padSpacing7, holeY - padSpacing7, id, od)
  ];
};
var to92s_2 = (parameters) => {
  const { p, id, od, h } = parameters;
  const holeY = Number.parseFloat(h) / 2;
  const padSpacing7 = Number.parseFloat(p);
  const holeDiameter = Number.parseFloat(id);
  const padWidth = Number.parseFloat(od);
  const padHeight = padWidth * (1.5 / 1.05);
  return [
    platedHoleWithRectPad({
      pn: 1,
      x: -padSpacing7,
      y: holeY - padSpacing7,
      holeDiameter,
      rectPadWidth: padWidth,
      rectPadHeight: padHeight
    }),
    platedHolePill(
      2,
      padSpacing7,
      holeY - padSpacing7,
      holeDiameter,
      padWidth,
      padHeight
    )
  ];
};
var to92s = (raw_params) => {
  const match = raw_params.string?.match(/^to92s_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 10) : 3;
  const parameters = to92s_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  let platedHoles = [];
  if (parameters.num_pins === 3) {
    platedHoles = to92s_3(parameters);
  } else if (parameters.num_pins === 2) {
    platedHoles = to92s_2(parameters);
  } else {
    throw new Error("Invalid number of pins for TO-92");
  }
  const holeY = Number.parseFloat(parameters.h) / 2;
  const padSpacing7 = Number.parseFloat(parameters.p);
  const silkscreenBody = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -holeY, y: holeY - padSpacing7 },
      { x: -1.9, y: 0 },
      { x: 1.9, y: 0 },
      { x: holeY, y: holeY - padSpacing7 },
      { x: 1.5, y: Number.parseFloat(parameters.h) / 2 + 0.5 },
      { x: -1.5, y: Number.parseFloat(parameters.h) / 2 + 0.5 },
      { x: -holeY, y: holeY - padSpacing7 }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenRefText = silkscreenRef(0, holeY + 1, 0.5);
  const padOuterDiameter = Number.parseFloat(parameters.od);
  const padCenterY = holeY - padSpacing7;
  const padMinX = -padSpacing7 - padOuterDiameter / 2;
  const padMaxX = padSpacing7 + padOuterDiameter / 2;
  const padMinY = padCenterY - padOuterDiameter / 2;
  const padMaxY = padCenterY + padOuterDiameter / 2;
  const bodyMinX = -holeY;
  const bodyMaxX = holeY;
  const bodyMinY = 0;
  const bodyMaxY = holeY + 0.5;
  const courtyardMinX = Math.min(padMinX, bodyMinX) - 0.25;
  const courtyardMaxX = Math.max(padMaxX, bodyMaxX) + 0.25;
  const courtyardMinY = Math.min(padMinY, bodyMinY) - 0.25;
  const courtyardMaxY = Math.max(padMaxY, bodyMaxY) + 0.25;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: {
      x: (courtyardMinX + courtyardMaxX) / 2,
      y: (courtyardMinY + courtyardMaxY) / 2
    },
    width: courtyardMaxX - courtyardMinX,
    height: courtyardMaxY - courtyardMinY,
    layer: "top"
  };
  return {
    circuitJson: [
      ...platedHoles,
      silkscreenBody,
      silkscreenRefText,
      courtyard
    ],
    parameters
  };
};

// src/fn/jst.ts
import {
  length as length58
} from "circuit-json";
import { z as z68 } from "zod";
var jst_def = base_def.extend({
  fn: z68.string(),
  p: length58.optional(),
  id: length58.optional(),
  pw: length58.optional(),
  pl: length58.optional(),
  w: length58.optional(),
  h: length58.optional(),
  mpx: length58.optional().describe("center-to-center distance between the SMD mounting pads"),
  mpy: length58.optional().describe("mounting-pad row distance from the signal-pad row"),
  mpw: length58.optional().describe("SMD mounting pad width"),
  mpl: length58.optional().describe("SMD mounting pad length"),
  mounttop: z68.boolean().optional().describe("place SMD mounting pads above the signal-pad row"),
  smd: z68.boolean().optional().describe(
    "Generic surface-mount JST-style connector with one signal row and two mounting pads."
  ),
  sh: z68.boolean().optional().describe(
    'JST SH (Surface-mount) connector family. SH stands for "Super High-density".'
  ),
  ph: z68.boolean().optional().describe(
    'JST PH (Through-hole) connector family. PH stands for "Pin Header".'
  ),
  zh: z68.boolean().optional().describe(
    "JST ZH (Through-hole) connector family. 1.5mm pitch wire-to-board."
  ),
  string: z68.string().optional()
});
var createEmptyBounds = () => ({
  minX: Number.POSITIVE_INFINITY,
  maxX: Number.NEGATIVE_INFINITY,
  minY: Number.POSITIVE_INFINITY,
  maxY: Number.NEGATIVE_INFINITY
});
var modifyBoundsToIncludeRect = ({
  bounds,
  centerX,
  centerY,
  width,
  height
}) => {
  const halfWidth = width / 2;
  const halfHeight = height / 2;
  bounds.minX = Math.min(bounds.minX, centerX - halfWidth);
  bounds.maxX = Math.max(bounds.maxX, centerX + halfWidth);
  bounds.minY = Math.min(bounds.minY, centerY - halfHeight);
  bounds.maxY = Math.max(bounds.maxY, centerY + halfHeight);
};
var variantDefaults = {
  ph: {
    p: length58.parse("2.2mm"),
    id: length58.parse("0.70mm"),
    pw: length58.parse("1.20mm"),
    pl: length58.parse("1.20mm"),
    w: length58.parse("6mm"),
    h: length58.parse("5mm")
  },
  sh: {
    p: length58.parse("1mm"),
    pw: length58.parse("0.6mm"),
    pl: length58.parse("1.55mm"),
    w: length58.parse("5.8mm"),
    h: length58.parse("7.8mm")
  },
  smd: {
    p: length58.parse("2mm"),
    id: 0,
    pw: length58.parse("1mm"),
    pl: length58.parse("3mm"),
    mpw: length58.parse("1.8mm"),
    mpl: length58.parse("3mm"),
    mpy: length58.parse("2.5mm"),
    w: length58.parse("8mm"),
    h: length58.parse("5mm")
  },
  zh: {
    p: length58.parse("1.5mm"),
    id: length58.parse("0.73mm"),
    pw: length58.parse("1.03mm"),
    pl: length58.parse("1.73mm"),
    w: length58.parse("3mm"),
    h: length58.parse("3.5mm")
  }
};
function getVariant(params) {
  if (params.smd) return "smd";
  if (params.sh) return "sh";
  if (params.ph) return "ph";
  if (params.zh) return "zh";
  return "ph";
}
function generatePads({
  variant,
  numPins,
  p,
  id,
  pw,
  pl,
  mpx,
  mpy,
  mpw,
  mpl,
  mounttop
}) {
  const pads = [];
  const padBounds = createEmptyBounds();
  let maxPadHalfY = 0;
  if (variant === "smd") {
    const startX = -((numPins - 1) / 2) * p;
    for (let i = 0; i < numPins; i++) {
      const x = startX + i * p;
      pads.push(rectpad(i + 1, x, 0, pw, pl));
      modifyBoundsToIncludeRect({
        bounds: padBounds,
        centerX: x,
        centerY: 0,
        width: pw,
        height: pl
      });
    }
    const mountY = mpy === 0 ? 0 : (mounttop ? 1 : -1) * mpy;
    for (const [index, x] of [-mpx / 2, mpx / 2].entries()) {
      pads.push(rectpad(numPins + index + 1, x, mountY, mpw, mpl));
      modifyBoundsToIncludeRect({
        bounds: padBounds,
        centerX: x,
        centerY: mountY,
        width: mpw,
        height: mpl
      });
    }
    maxPadHalfY = Math.max(pl / 2, mpl / 2);
  } else if (variant === "ph") {
    const startX = -((numPins - 1) / 2) * p;
    for (let i = 0; i < numPins; i++) {
      const x = startX + i * p;
      pads.push(
        platedHoleWithRectPad({
          pn: i + 1,
          x,
          y: 2,
          holeDiameter: id,
          rectPadWidth: pw,
          rectPadHeight: pl
        })
      );
      modifyBoundsToIncludeRect({
        bounds: padBounds,
        centerX: x,
        centerY: 2,
        width: pw,
        height: pl
      });
      maxPadHalfY = Math.max(maxPadHalfY, pl / 2);
    }
  } else if (variant === "zh") {
    const startX = -((numPins - 1) / 2) * p;
    for (let i = 0; i < numPins; i++) {
      const x = startX + i * p;
      if (i === 0) {
        pads.push(
          platedHoleWithRectPad({
            pn: i + 1,
            x,
            y: 0,
            holeDiameter: id,
            rectPadWidth: pw,
            rectPadHeight: pl,
            rectBorderRadius: 0.12499977
          })
        );
      } else {
        pads.push(platedHolePill(i + 1, x, 0, id, pw, pl));
      }
      modifyBoundsToIncludeRect({
        bounds: padBounds,
        centerX: x,
        centerY: 0,
        width: pw,
        height: pl
      });
      maxPadHalfY = Math.max(maxPadHalfY, pl / 2);
    }
  } else {
    const startX = -((numPins - 1) / 2) * p;
    for (let i = 0; i < numPins; i++) {
      const x = startX + i * p;
      pads.push(rectpad(i + 1, x, -1.325, pw, pl));
      modifyBoundsToIncludeRect({
        bounds: padBounds,
        centerX: x,
        centerY: -1.325,
        width: pw,
        height: pl
      });
      maxPadHalfY = Math.max(maxPadHalfY, pl / 2);
    }
    const sideOffset = (numPins - 1) / 2 * p + 1.3;
    pads.push(rectpad(numPins + 1, -sideOffset, 1.22, 1.2, 1.8));
    pads.push(rectpad(numPins + 2, sideOffset, 1.22, 1.2, 1.8));
    modifyBoundsToIncludeRect({
      bounds: padBounds,
      centerX: -sideOffset,
      centerY: 1.22,
      width: 1.2,
      height: 1.8
    });
    modifyBoundsToIncludeRect({
      bounds: padBounds,
      centerX: sideOffset,
      centerY: 1.22,
      width: 1.2,
      height: 1.8
    });
    maxPadHalfY = Math.max(maxPadHalfY, 0.9);
  }
  return { pads, padBounds, maxPadHalfY };
}
function generateSilkscreenBody({
  variant,
  w,
  h,
  numPins,
  p
}) {
  if (variant === "smd") {
    return {
      type: "pcb_silkscreen_path",
      layer: "top",
      pcb_component_id: "",
      route: [
        { x: -w / 2, y: -h / 2 },
        { x: w / 2, y: -h / 2 },
        { x: w / 2, y: h / 2 },
        { x: -w / 2, y: h / 2 },
        { x: -w / 2, y: -h / 2 }
      ],
      stroke_width: 0.1,
      pcb_silkscreen_path_id: ""
    };
  } else if (variant === "ph") {
    return {
      type: "pcb_silkscreen_path",
      layer: "top",
      pcb_component_id: "",
      route: [
        { x: -3, y: 3 },
        { x: 3, y: 3 },
        { x: 3, y: -2 },
        { x: -3, y: -2 },
        { x: -3, y: 3 }
      ],
      stroke_width: 0.1,
      pcb_silkscreen_path_id: ""
    };
  } else if (variant === "zh" && numPins && p) {
    const pinSpan = (numPins - 1) * p;
    const bodyLeft = -pinSpan / 2 - 1.5;
    const bodyRight = pinSpan / 2 + 1.5;
    const bodyTop = -h / 2;
    const bodyBottom = h / 2;
    return {
      type: "pcb_silkscreen_path",
      layer: "top",
      pcb_component_id: "",
      route: [
        { x: bodyLeft, y: bodyTop },
        { x: bodyRight, y: bodyTop },
        { x: bodyRight, y: bodyBottom },
        { x: bodyLeft, y: bodyBottom },
        { x: bodyLeft, y: bodyTop }
      ],
      stroke_width: 0.1,
      pcb_silkscreen_path_id: ""
    };
  } else {
    return {
      type: "pcb_silkscreen_path",
      layer: "top",
      pcb_component_id: "",
      route: [],
      stroke_width: 0.1,
      pcb_silkscreen_path_id: ""
    };
  }
}
var jst = (raw_params) => {
  const params = jst_def.parse(raw_params);
  const variant = getVariant(params);
  const defaults = variantDefaults[variant];
  const p = params.p ?? defaults.p;
  const id = params.id ?? defaults.id;
  const pw = params.pw ?? defaults.pw;
  const pl = params.pl ?? defaults.pl;
  const w = params.w ?? defaults.w;
  const h = params.h ?? defaults.h;
  let numPins;
  const explicitNumPins = raw_params.num_pins;
  if (typeof explicitNumPins === "number") {
    numPins = explicitNumPins;
  }
  const str = typeof raw_params.string === "string" ? raw_params.string : "";
  const match = str.match(/(?:^|_)jst(\d+)(?:_|$)/);
  const zhMatch = str.match(/(?:^|_)zh(\d+)(?:_|$)/);
  if (match && match[1]) {
    const parsed = Number.parseInt(match[1], 10);
    if (!Number.isNaN(parsed)) {
      numPins = parsed;
    }
  }
  if (zhMatch && zhMatch[1]) {
    const parsed = Number.parseInt(zhMatch[1], 10);
    if (!Number.isNaN(parsed)) {
      numPins = parsed;
    }
  }
  if (typeof numPins !== "number") {
    throw new Error(
      `JST requires an explicit pin count (e.g. jst6_sh or .jst(6))${params.string ? `, from string "${params.string}"` : ""}`
    );
  }
  const mpx = params.mpx ?? (numPins - 1) * p + 3.4;
  const mpy = params.mpy ?? defaults.mpy ?? 0;
  const mpw = params.mpw ?? defaults.mpw ?? 0;
  const mpl = params.mpl ?? defaults.mpl ?? 0;
  const mounttop = params.mounttop ?? false;
  const padGeometry = generatePads({
    variant,
    numPins,
    p,
    id,
    pw,
    pl,
    mpx,
    mpy,
    mpw,
    mpl,
    mounttop
  });
  const { pads, padBounds, maxPadHalfY } = padGeometry;
  const silkscreenWidth = variant === "smd" ? Math.max(
    w,
    2 * Math.max(Math.abs(padBounds.minX), Math.abs(padBounds.maxX)) + 0.4
  ) : w;
  const silkscreenHeight = variant === "smd" ? Math.max(
    h,
    2 * Math.max(Math.abs(padBounds.minY), Math.abs(padBounds.maxY)) + 0.4
  ) : h;
  const silkscreenBody = generateSilkscreenBody({
    variant,
    w: silkscreenWidth,
    h: silkscreenHeight,
    numPins,
    p
  });
  const silkscreenRefText = silkscreenRef(
    0,
    silkscreenHeight / 2 + 1,
    0.5
  );
  const silkscreenXs = silkscreenBody.route.map((point) => point.x);
  const silkscreenYs = silkscreenBody.route.map((point) => point.y);
  const hasSilkscreenGeometry = silkscreenXs.length > 0 && silkscreenYs.length > 0;
  const featureMinX = hasSilkscreenGeometry ? Math.min(padBounds.minX, Math.min(...silkscreenXs)) : padBounds.minX;
  const featureMaxX = hasSilkscreenGeometry ? Math.max(padBounds.maxX, Math.max(...silkscreenXs)) : padBounds.maxX;
  const featureMinY = hasSilkscreenGeometry ? Math.min(padBounds.minY, Math.min(...silkscreenYs)) : padBounds.minY;
  const featureMaxY = hasSilkscreenGeometry ? Math.max(padBounds.maxY, Math.max(...silkscreenYs)) : padBounds.maxY;
  const courtyardSideClearanceX = 0.5;
  const courtyardFrontClearanceY = 0.05;
  const courtyardRearClearanceY = maxPadHalfY + 0.085;
  const crtMinX = featureMinX - courtyardSideClearanceX;
  const crtMaxX = featureMaxX + courtyardSideClearanceX;
  const crtMinY = featureMinY - courtyardRearClearanceY;
  const crtMaxY = featureMaxY + courtyardFrontClearanceY;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: (crtMinX + crtMaxX) / 2, y: (crtMinY + crtMaxY) / 2 },
    width: crtMaxX - crtMinX,
    height: crtMaxY - crtMinY,
    layer: "top"
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenBody,
      silkscreenRefText,
      courtyard
    ],
    parameters: {
      ...params,
      p,
      id,
      pw,
      pl,
      w,
      h,
      num_pins: numPins,
      sh: variant === "sh",
      ph: variant === "ph",
      smd: variant === "smd",
      zh: variant === "zh",
      ...variant === "smd" ? {
        mpx,
        mpy,
        mpw,
        mpl,
        mounttop
      } : {}
    }
  };
};

// src/fn/sod110.ts
import { z as z69 } from "zod";
import { length as length59 } from "circuit-json";
var sod_def12 = base_def.extend({
  fn: z69.string(),
  num_pins: z69.literal(2).default(2),
  w: z69.string().default("3.30mm"),
  h: z69.string().default("1.70mm"),
  pl: z69.string().default("0.80mm"),
  pw: z69.string().default("1mm"),
  p: z69.string().default("1.90mm")
});
var sod110 = (raw_params) => {
  const parameters = sod_def12.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length59.parse(parameters.h) / 2 + 0.5,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length59.parse(parameters.p) / 2,
        y: length59.parse(parameters.h) / 2
      },
      {
        x: -length59.parse(parameters.w) / 2,
        y: length59.parse(parameters.h) / 2
      },
      {
        x: -length59.parse(parameters.w) / 2,
        y: -length59.parse(parameters.h) / 2
      },
      {
        x: length59.parse(parameters.p) / 2,
        y: -length59.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 3.2;
  const courtyardHeightMm = 2;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing13(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords13 = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  }
  return { x: p / 2, y: 0 };
};
var sodWithoutParsing13 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords13({
      pn: i,
      p: Number.parseFloat(parameters.p)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw),
        0.1
      )
    );
  }
  return pads;
};

// src/fn/vssop.ts
import { z as z70 } from "zod";
import { length as length60 } from "circuit-json";
var getDefaultValues = (num_pins) => {
  switch (num_pins) {
    case 8:
      return {
        w: "3.06mm",
        h: "3.14mm",
        p: "0.65mm",
        pl: "1.6mm",
        pw: "0.5mm"
      };
    case 10:
      return {
        w: "3.10mm",
        h: "3.33mm",
        p: "0.5mm",
        pl: "1.45mm",
        pw: "0.3mm"
      };
    default:
      return {
        w: "3.06mm",
        h: "3.14mm",
        p: "0.65mm",
        pl: "1.6mm",
        pw: "0.5mm"
      };
  }
};
var vssop_def = base_def.extend({
  fn: z70.string(),
  num_pins: z70.union([z70.literal(8), z70.literal(10)]).default(8),
  w: z70.string().optional(),
  h: z70.string().optional(),
  p: z70.string().optional(),
  pl: z70.string().optional(),
  pw: z70.string().optional(),
  thermalpad: dim2d.optional(),
  ...thermalPadOffsetFields,
  string: z70.string().optional()
});
var vssop = (raw_params) => {
  const parameters = vssop_def.parse(raw_params);
  const defaults = getDefaultValues(parameters.num_pins);
  const w = length60.parse(parameters.w || defaults.w);
  const h = length60.parse(parameters.h || defaults.h);
  const p = length60.parse(parameters.p || defaults.p);
  const pl = length60.parse(parameters.pl || defaults.pl);
  const pw = length60.parse(parameters.pw || defaults.pw);
  const cornerRadius = Math.min(pl, pw) / 8;
  const pads = [];
  const half = parameters.num_pins / 2;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getVssopPadCoord(parameters.num_pins, i + 1, w, p);
    const logical_pn = i < half ? i + 1 : parameters.num_pins - (i - half);
    pads.push(rectpad(logical_pn, x, y, pl, pw, cornerRadius));
  }
  if (parameters.thermalpad) {
    pads.push(
      createThermalPad(parameters.thermalpad, {
        x: parameters.thermalpadcenteroffsetx,
        y: parameters.thermalpadcenteroffsety
      })
    );
  }
  const silkscreenBoxWidth = w;
  const silkscreenBoxHeight = h;
  const silkscreenTopLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -silkscreenBoxWidth / 2, y: silkscreenBoxHeight / 2 },
      { x: silkscreenBoxWidth / 2, y: silkscreenBoxHeight / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBottomLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -silkscreenBoxWidth / 2, y: -silkscreenBoxHeight / 2 },
      { x: silkscreenBoxWidth / 2, y: -silkscreenBoxHeight / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const pin1Position = getVssopPadCoord(
    parameters.num_pins,
    1,
    silkscreenBoxWidth,
    p
  );
  const pin1MarkerPosition = {
    x: pin1Position.x - 0.8,
    y: pin1Position.y
  };
  const pin1Marking = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "pin_marker_1",
    route: [
      { x: pin1MarkerPosition.x - 0.4, y: pin1MarkerPosition.y },
      { x: pin1MarkerPosition.x - 0.7, y: pin1MarkerPosition.y + 0.3 },
      { x: pin1MarkerPosition.x - 0.7, y: pin1MarkerPosition.y - 0.3 },
      { x: pin1MarkerPosition.x - 0.4, y: pin1MarkerPosition.y }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: "pin_marker_1"
  };
  const silkscreenRefText = silkscreenRef(
    0,
    silkscreenBoxHeight / 2 + 0.5,
    0.3
  );
  const pinRowSpanY = (parameters.num_pins / 2 - 1) * p + pw;
  const courtyardStepInnerHalfWidth = w / 2 + 0.25;
  const courtyardStepOuterHalfWidth = courtyardStepInnerHalfWidth + 1.43;
  const courtyardStepInnerHalfHeight = pinRowSpanY / 2 + 0.255;
  const courtyardStepOuterHalfHeight = h / 2 + 0.25;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ])
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenTopLine,
      silkscreenBottomLine,
      silkscreenRefText,
      pin1Marking,
      courtyard
    ],
    parameters
  };
};
var getVssopPadCoord = (pinCount, pn, w, p) => {
  const half = pinCount / 2;
  const rowIndex = (pn - 1) % half;
  const col = pn <= half ? -1 : 1;
  const row = (half - 1) / 2 - rowIndex;
  return {
    x: col * 2.11,
    y: row * p
  };
};

// src/fn/msop.ts
import { z as z71 } from "zod";
import { length as length61 } from "circuit-json";
var getDefaultValues2 = (num_pins) => {
  switch (num_pins) {
    case 10:
      return {
        w: "3.10mm",
        h: "3.32mm",
        p: "0.5mm",
        pl: "1.63mm",
        pw: "0.33mm"
      };
    case 12:
      return {
        w: "3mm",
        h: "4mm",
        p: "0.65mm",
        pl: "0.88mm",
        pw: "0.4mm"
      };
    case 16:
      return {
        w: "3.10mm",
        h: "4mm",
        p: "0.5mm",
        pl: "0.88mm",
        pw: "0.3mm"
      };
    default:
      return {
        w: "3.10mm",
        h: "3.32mm",
        p: "0.65mm",
        pl: "1.63mm",
        pw: "0.4mm"
      };
  }
};
var msop_def = base_def.extend({
  fn: z71.string(),
  num_pins: z71.union([z71.literal(8), z71.literal(10), z71.literal(12), z71.literal(16)]).default(8),
  w: z71.string().optional(),
  h: z71.string().optional(),
  p: z71.string().optional(),
  pl: z71.string().optional(),
  pw: z71.string().optional(),
  thermalpad: dim2d.optional(),
  ...thermalPadOffsetFields,
  string: z71.string().optional()
});
var getMsopCoords = (pinCount, pn, w, p, pl) => {
  const half = pinCount / 2;
  const rowIndex = (pn - 1) % half;
  const col = pn <= half ? -1 : 1;
  const row = (half - 1) / 2 - rowIndex;
  const padBodyOverlap = length61.parse("0.2mm");
  return {
    x: col * (w / 2 + pl / 2 - padBodyOverlap),
    y: row * p
  };
};
var msop = (raw_params) => {
  const parameters = msop_def.parse(raw_params);
  const defaults = getDefaultValues2(parameters.num_pins);
  const w = length61.parse(parameters.w || defaults.w);
  const h = length61.parse(parameters.h || defaults.h);
  const p = length61.parse(parameters.p || defaults.p);
  const pl = length61.parse(parameters.pl || defaults.pl);
  const pw = length61.parse(parameters.pw || defaults.pw);
  const cornerRadius = Math.min(pl, pw) / 8;
  const pads = [];
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getMsopCoords(parameters.num_pins, i + 1, w, p, pl);
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  if (parameters.thermalpad) {
    pads.push(
      createThermalPad(parameters.thermalpad, {
        x: parameters.thermalpadcenteroffsetx,
        y: parameters.thermalpadcenteroffsety
      })
    );
  }
  const silkscreenBoxWidth = w;
  const silkscreenBoxHeight = h;
  const silkscreenTopLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -silkscreenBoxWidth / 2, y: silkscreenBoxHeight / 2 },
      { x: silkscreenBoxWidth / 2, y: silkscreenBoxHeight / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBottomLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -silkscreenBoxWidth / 2, y: -silkscreenBoxHeight / 2 },
      { x: silkscreenBoxWidth / 2, y: -silkscreenBoxHeight / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const pin1Position = getMsopCoords(
    parameters.num_pins,
    1,
    silkscreenBoxWidth,
    p,
    pl
  );
  const pin1MarkerPosition = {
    x: pin1Position.x - 0.8,
    y: pin1Position.y
  };
  const pin1Marking = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "pin_marker_1",
    route: [
      { x: pin1MarkerPosition.x - 0.4, y: pin1MarkerPosition.y },
      { x: pin1MarkerPosition.x - 0.7, y: pin1MarkerPosition.y + 0.3 },
      { x: pin1MarkerPosition.x - 0.7, y: pin1MarkerPosition.y - 0.3 },
      { x: pin1MarkerPosition.x - 0.4, y: pin1MarkerPosition.y }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: "pin_marker_1"
  };
  const silkscreenRefText = silkscreenRef(
    0,
    silkscreenBoxHeight / 2 + 0.5,
    0.3
  );
  const pinRowSpanY = (parameters.num_pins / 2 - 1) * p + pw;
  const courtyardStepInnerHalfWidth = w / 2 + 0.25;
  const courtyardStepOuterHalfWidth = courtyardStepInnerHalfWidth + 1.43;
  const courtyardStepInnerHalfHeight = pinRowSpanY / 2 + 0.255;
  const courtyardStepOuterHalfHeight = h / 2 + 0.25;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ])
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenTopLine,
      silkscreenBottomLine,
      silkscreenRefText,
      pin1Marking,
      courtyard
    ],
    parameters
  };
};

// src/fn/sod323w.ts
import { z as z72 } from "zod";
import { length as length62 } from "circuit-json";
var sod323w_def = base_def.extend({
  fn: z72.string(),
  num_pins: z72.literal(2).default(2),
  w: z72.string().default("3.8mm"),
  h: z72.string().default("1.65mm"),
  pl: z72.string().default("1.2mm"),
  pw: z72.string().default("1.2mm"),
  pad_spacing: z72.string().default("2.6mm")
});
var sod323w = (raw_params) => {
  const parameters = sod323w_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length62.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length62.parse(parameters.pad_spacing) / 2,
        y: length62.parse(parameters.h) / 2
      },
      {
        x: -length62.parse(parameters.w) / 2 - 0.2,
        y: length62.parse(parameters.h) / 2
      },
      {
        x: -length62.parse(parameters.w) / 2 - 0.2,
        y: -length62.parse(parameters.h) / 2
      },
      {
        x: length62.parse(parameters.pad_spacing) / 2,
        y: -length62.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 4.3;
  const courtyardHeightMm = 2.15;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing14(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords14 = (parameters) => {
  const { pn, pad_spacing } = parameters;
  if (pn === 1) {
    return { x: -pad_spacing / 2, y: 0 };
  } else {
    return { x: pad_spacing / 2, y: 0 };
  }
};
var sodWithoutParsing14 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords14({
      pn: i,
      pad_spacing: Number.parseFloat(parameters.pad_spacing)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw)
      )
    );
  }
  return pads;
};

// src/fn/sod323fl.ts
import { z as z73 } from "zod";
import { length as length63 } from "circuit-json";
var sod323FL_def = base_def.extend({
  fn: z73.string(),
  num_pins: z73.literal(2).default(2),
  w: z73.string().default("3.20mm"),
  h: z73.string().default("1.65mm"),
  pl: z73.string().default("0.8mm"),
  pw: z73.string().default("0.9mm"),
  pad_spacing: z73.string().default("2.1mm")
});
var sod323fl = (raw_params) => {
  const parameters = sod323FL_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length63.parse(parameters.h),
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length63.parse(parameters.pad_spacing) / 2,
        y: length63.parse(parameters.h) / 2
      },
      {
        x: -length63.parse(parameters.w) / 2 - 0.2,
        y: length63.parse(parameters.h) / 2
      },
      {
        x: -length63.parse(parameters.w) / 2 - 0.2,
        y: -length63.parse(parameters.h) / 2
      },
      {
        x: length63.parse(parameters.pad_spacing) / 2,
        y: -length63.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 3.7;
  const courtyardHeightMm = 2.15;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: sodWithoutParsing15(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSodCoords15 = (parameters) => {
  const { pn, pad_spacing } = parameters;
  if (pn === 1) {
    return { x: -pad_spacing / 2, y: 0 };
  } else {
    return { x: pad_spacing / 2, y: 0 };
  }
};
var sodWithoutParsing15 = (parameters) => {
  const pads = [];
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSodCoords15({
      pn: i,
      pad_spacing: Number.parseFloat(parameters.pad_spacing)
    });
    pads.push(
      rectpad(
        i,
        x,
        y,
        Number.parseFloat(parameters.pl),
        Number.parseFloat(parameters.pw)
      )
    );
  }
  return pads;
};

// src/fn/son.ts
import { z as z74 } from "zod";
import { length as length64 } from "circuit-json";
var son_def = base_def.extend({
  fn: z74.string(),
  num_pins: z74.union([z74.literal(6), z74.literal(8)]).default(8),
  w: z74.string().default("3mm"),
  h: z74.string().default("3mm"),
  p: z74.string().default("0.5mm"),
  pl: z74.string().default("0.52mm"),
  pw: z74.string().default("0.35mm"),
  epw: z74.string().default("1.40mm"),
  eph: z74.string().default("1.60mm"),
  string: z74.string().optional(),
  ep: z74.boolean().default(false)
});
var son = (raw_params) => {
  if (raw_params.string && raw_params.string.includes("_ep")) {
    raw_params.ep = true;
  }
  const match = raw_params.string?.match(/^son_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 10) : raw_params.num_pins || 8;
  const parameters = son_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  const w = length64.parse(parameters.w);
  const h = length64.parse(parameters.h);
  const p = length64.parse(parameters.p);
  const pl = length64.parse(parameters.pl);
  const pw = length64.parse(parameters.pw);
  const epw = length64.parse(parameters.epw);
  const eph = length64.parse(parameters.eph);
  const cornerRadius = Math.min(pl, pw) / 8;
  const pads = [];
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getSonPadCoord(parameters.num_pins, i + 1, w, p);
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  if (parameters.ep) {
    pads.push(rectpad(parameters.num_pins + 1, 0, 0, epw, eph));
  }
  const silkscreenBoxWidth = w;
  const silkscreenBoxHeight = h;
  const silkscreenTopLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -silkscreenBoxWidth / 2, y: silkscreenBoxHeight / 2 },
      { x: silkscreenBoxWidth / 2, y: silkscreenBoxHeight / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBottomLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -silkscreenBoxWidth / 2, y: -silkscreenBoxHeight / 2 },
      { x: silkscreenBoxWidth / 2, y: -silkscreenBoxHeight / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const pin1Position = getSonPadCoord(
    parameters.num_pins,
    1,
    silkscreenBoxWidth,
    p
  );
  const pin1MarkerPosition = {
    x: pin1Position.x - 0.4,
    y: pin1Position.y
  };
  const pin1Marking = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "pin_marker_1",
    route: [
      { x: pin1MarkerPosition.x - 0.4, y: pin1MarkerPosition.y },
      { x: pin1MarkerPosition.x - 0.7, y: pin1MarkerPosition.y + 0.3 },
      { x: pin1MarkerPosition.x - 0.7, y: pin1MarkerPosition.y - 0.3 },
      { x: pin1MarkerPosition.x - 0.4, y: pin1MarkerPosition.y }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: "pin_marker_1"
  };
  const silkscreenRefText = silkscreenRef(
    0,
    silkscreenBoxHeight / 2 + 0.5,
    0.3
  );
  const pinColumnCenterX = Math.abs(
    getSonPadCoord(parameters.num_pins, 1, w, p).x
  );
  const pinRowSpanY = (parameters.num_pins / 2 - 1) * p + pw;
  const pinRowSpanX = pinColumnCenterX * 2 + pl;
  const courtyardStepInnerHalfWidth = w / 2 + 0.25;
  const courtyardStepOuterHalfWidth = pinRowSpanX / 2 + 0.25;
  const courtyardStepInnerHalfHeight = pinRowSpanY / 2 + 0.25;
  const courtyardStepOuterHalfHeight = h / 2 + 0.25;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ])
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenTopLine,
      silkscreenBottomLine,
      silkscreenRefText,
      pin1Marking,
      courtyard
    ],
    parameters
  };
};
var getSonPadCoord = (num_pins, pn, w, p) => {
  const half = num_pins / 2;
  const rowIndex = (pn - 1) % half;
  const col = pn <= half ? -1 : 1;
  const row = (half - 1) / 2 - rowIndex;
  return {
    x: col * length64.parse("1.4mm"),
    y: row * p
  };
};

// src/fn/vson.ts
import { z as z75 } from "zod";
import { length as length65, distance as distance3 } from "circuit-json";
var vson_def = base_def.extend({
  fn: z75.string(),
  num_pins: z75.number().optional().default(8),
  p: distance3.describe("pitch (distance between center of each pin)"),
  w: length65.describe("width between vertical rows of pins"),
  grid: dim2d.describe("width and height of the border of the footprint"),
  ep: dim2d.default("0x0mm").describe("width and height of the central exposed thermal pad"),
  epx: length65.default("0mm").describe("x offset of the center of the central exposed thermal pad"),
  pinw: length65.describe("width of the pin pads"),
  pinh: length65.describe("height of the pin pads")
});
var vson = (raw_params) => {
  const parameters = vson_def.parse(raw_params);
  const { num_pins, p, w, grid: grid2, ep, epx, pinw, pinh } = parameters;
  if (num_pins % 2 !== 0) {
    throw new Error("invalid number of pins");
  }
  const pads = [];
  const cornerRadius = Math.min(pinw, pinh) / 8;
  for (let i = 0; i < num_pins; i++) {
    const { pinX, pinY } = getCcwVsonCoords({
      pinCount: num_pins,
      pinIndex: i,
      width: w,
      pitch: p
    });
    pads.push(rectpad(i + 1, pinX, pinY, pinw, pinh, cornerRadius));
  }
  if (ep.x > 0 && ep.y > 0) {
    pads.push(rectpad(parameters.num_pins + 1, epx, 0, ep.x, ep.y));
  }
  const silkscreenPaths = getSilkscreenPaths(grid2);
  const silkscreenRefText = silkscreenRef(
    0,
    grid2.y / 2 + p,
    grid2.y / 6
  );
  const pinRowSpanY = (num_pins / 2 - 1) * p + pinh;
  const pinRowSpanX = w + pinw;
  const courtyardStepInnerHalfWidth = grid2.x / 2 + 0.25;
  const courtyardStepOuterHalfWidth = pinRowSpanX / 2 + 0.25;
  const pinRowCourtyardHalfY = pinRowSpanY / 2 + 0.25;
  const pinRowExtendedCourtyardHalfY = pinRowSpanY / 2 + 0.45 + Math.max(0, 0.8 - p);
  const courtyardStepOuterHalfHeight = Math.min(
    grid2.y / 2 + 0.25,
    pinRowExtendedCourtyardHalfY
  );
  const courtyardStepNotchDepthY = Math.max(
    0,
    0.37 - Math.max(
      0,
      courtyardStepOuterHalfWidth - courtyardStepInnerHalfWidth - 0.38
    ) * 1.4
  );
  const courtyardStepInnerHalfHeight = Math.max(
    pinRowCourtyardHalfY,
    courtyardStepOuterHalfHeight - courtyardStepNotchDepthY
  );
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ])
  };
  return {
    circuitJson: [...pads, ...silkscreenPaths, silkscreenRefText, courtyard],
    parameters
  };
};
var getCcwVsonCoords = (params) => {
  let pinY = 0;
  let pinX = 0;
  const centerY = (params.pinCount / 2 - 1) * params.pitch / 2;
  const pinHalf = params.pinCount / 2;
  if (params.pinIndex + 1 <= pinHalf) {
    pinY = params.pitch * params.pinIndex - centerY;
    pinX = 0 - params.width / 2;
  } else {
    pinY = params.pitch * (params.pinCount - params.pinIndex - 1) - centerY;
    pinX = params.width / 2;
  }
  return { pinX, pinY };
};
var getSilkscreenPaths = (grid2) => {
  const borderMargin = 0.1;
  const cornerLine = grid2.y / 30;
  const silkscreenPaths = [
    // top silkscreen path
    {
      layer: "top",
      pcb_component_id: "",
      pcb_silkscreen_path_id: "",
      type: "pcb_silkscreen_path",
      route: [
        {
          x: -grid2.x / 2 - borderMargin,
          y: grid2.y / 2 + borderMargin - cornerLine
        },
        {
          x: -grid2.x / 2 - borderMargin,
          y: grid2.y / 2 + borderMargin
        },
        {
          x: grid2.x / 2 + borderMargin,
          y: grid2.y / 2 + borderMargin
        },
        {
          x: grid2.x / 2 + borderMargin,
          y: grid2.y / 2 + borderMargin - cornerLine
        }
      ],
      stroke_width: grid2.y / 30
    },
    // bottom silkscreen path
    {
      layer: "top",
      pcb_component_id: "",
      pcb_silkscreen_path_id: "",
      type: "pcb_silkscreen_path",
      route: [
        {
          x: -grid2.x / 2 - borderMargin,
          y: -grid2.y / 2 - borderMargin + cornerLine
        },
        {
          x: -grid2.x / 2 - borderMargin,
          y: -grid2.y / 2 - borderMargin
        },
        {
          x: grid2.x / 2 + borderMargin,
          y: -grid2.y / 2 - borderMargin
        },
        {
          x: grid2.x / 2 + borderMargin,
          y: -grid2.y / 2 - borderMargin + cornerLine
        }
      ],
      stroke_width: grid2.y / 30
    }
  ];
  return silkscreenPaths;
};

// src/fn/wson.ts
import {
  length as length66
} from "circuit-json";
import { z as z76 } from "zod";
var wson_def = base_def.extend({
  fn: z76.literal("wson"),
  num_pins: z76.literal(8).default(8),
  p: length66.default("0.5mm").describe("pin pitch"),
  rowspan: length66.default("3.015mm").describe("distance between pad rows"),
  pl: length66.default("0.6mm").describe("pad length across the package edge"),
  pw: length66.default("0.28mm").describe("pad width along the pin row"),
  ep: z76.boolean().default(true).describe("include the exposed pad"),
  epw: length66.default("1.7mm").describe("exposed pad width"),
  eph: length66.default("0.3mm").describe("exposed pad height"),
  w: length66.default("2mm").describe("package body width"),
  h: length66.default("3mm").describe("package body height")
});
var wson = (rawParams) => {
  const parameters = wson_def.parse(rawParams);
  const { p, rowspan, pl, pw, ep, epw, eph, w, h } = parameters;
  const pads = [];
  const startX = -3 * p / 2;
  for (let index = 0; index < 4; index += 1) {
    const x = startX + index * p;
    pads.push(rectpad(index + 1, x, -rowspan / 2, pw, pl));
    pads.push(rectpad(8 - index, x, rowspan / 2, pw, pl));
  }
  if (ep) pads.push(rectpad(9, 0, 0, epw, eph));
  const silkscreen = [
    silkscreenpath([
      { x: -w / 2, y: -h / 2 },
      { x: -w / 2, y: h / 2 }
    ]),
    silkscreenpath([
      { x: w / 2, y: -h / 2 },
      { x: w / 2, y: h / 2 }
    ])
  ];
  const ref = silkscreenRef(
    0,
    Math.max(h / 2, (rowspan + pl) / 2) + 0.6,
    0.5
  );
  const copperWidth = Math.max(3 * p + pw, ep ? epw : 0);
  const copperHeight = Math.max(rowspan + pl, ep ? eph : 0);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: Math.max(w, copperWidth) + 0.5,
    height: Math.max(h, copperHeight) + 0.5,
    layer: "top"
  };
  return {
    circuitJson: [...pads, ...silkscreen, ref, courtyard],
    parameters
  };
};

// src/fn/solderjumper.ts
import { length as length67 } from "circuit-json";
var solderjumper = (params) => {
  const { num_pins, bridged, p = 2.54, pw = 1.5, ph = 1.5 } = params;
  const padSpacing7 = length67.parse(p);
  const padWidth = length67.parse(pw);
  const padHeight = length67.parse(ph);
  const traceWidth = Math.min(padHeight / 4, 0.5);
  const pads = [];
  for (let i = 0; i < num_pins; i++) {
    pads.push(rectpad(i + 1, i * padSpacing7, 0, padWidth, padHeight));
  }
  let traces = [];
  if (bridged) {
    const pins = bridged.split("").map(Number);
    if (pins.length > 1) {
      for (let i = 0; i < pins.length - 1; i++) {
        const from = pins[i];
        const to = pins[i + 1];
        if (typeof from === "number" && typeof to === "number" && !isNaN(from) && !isNaN(to)) {
          const xCenterFrom = (from - 1) * padSpacing7;
          const xCenterTo = (to - 1) * padSpacing7;
          const directionMult = Math.sign(xCenterTo - xCenterFrom);
          const x1 = xCenterFrom + directionMult * (padWidth / 2);
          const x2 = xCenterTo - directionMult * (padWidth / 2);
          traces.push({
            type: "pcb_trace",
            pcb_trace_id: "",
            route: [
              {
                start_pcb_port_id: `{PIN${from}}`,
                x: x1,
                y: 0,
                width: traceWidth,
                layer: "top",
                route_type: "wire"
              },
              {
                end_pcb_port_id: `{PIN${to}}`,
                x: x2,
                y: 0,
                width: traceWidth,
                layer: "top",
                route_type: "wire"
              }
            ]
          });
        }
      }
    }
  }
  const outlineWidth = (num_pins - 1) * padSpacing7 + padWidth + 0.7;
  const outlineHeight = padHeight + 1;
  const outlineCenterX = (num_pins - 1) * padSpacing7 / 2;
  const outlineCenterY = 0;
  const silkscreenRect = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "outline",
    route: [
      {
        x: outlineCenterX - outlineWidth / 2,
        y: outlineCenterY - outlineHeight / 2
      },
      {
        x: outlineCenterX + outlineWidth / 2,
        y: outlineCenterY - outlineHeight / 2
      },
      {
        x: outlineCenterX + outlineWidth / 2,
        y: outlineCenterY + outlineHeight / 2
      },
      {
        x: outlineCenterX - outlineWidth / 2,
        y: outlineCenterY + outlineHeight / 2
      },
      {
        x: outlineCenterX - outlineWidth / 2,
        y: outlineCenterY - outlineHeight / 2
      }
    ],
    stroke_width: 0.15
  };
  const refOffset = 0.6;
  const refY = outlineCenterY + outlineHeight / 2 + refOffset;
  const silk = silkscreenRef(outlineCenterX, refY, 0.4);
  const pinRowSpanX = (num_pins - 1) * padSpacing7;
  const padOuterHalfWidth = pinRowSpanX / 2 + padWidth / 2;
  const padOuterHalfHeight = padHeight / 2;
  const courtyardHalfWidth = padOuterHalfWidth + 0.5;
  const courtyardHalfHeight = padOuterHalfHeight + 0.5;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: pinRowSpanX / 2, y: 0 },
    width: 2 * courtyardHalfWidth,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  return {
    circuitJson: [...pads, ...traces, silkscreenRect, silk, courtyard],
    parameters: params
  };
};

// src/fn/sot457.ts
import { z as z77 } from "zod";
var commonSchema = {
  fn: z77.literal("sot457"),
  num_pins: z77.literal(6).default(6),
  pillh: z77.string().default("0.45mm"),
  pillw: z77.string().default("1.45mm"),
  pl: z77.string(),
  pw: z77.string(),
  p: z77.string(),
  wave: z77.boolean().optional(),
  reflow: z77.boolean().optional()
};
var sot457DefSchema = base_def.extend({
  ...commonSchema,
  h: z77.string().default("2.5mm"),
  w: z77.string().default("2.7mm"),
  pl: z77.string().default("0.8mm"),
  pw: z77.string().default("0.55mm"),
  p: z77.string().default("0.95mm")
});
var sot457WaveSchema = base_def.extend({
  ...commonSchema,
  h: z77.string().default("3mm"),
  w: z77.string().default("4mm"),
  pillr: z77.string().default("0.225mm"),
  pl: z77.string().default("1.45mm"),
  pw: z77.string().default("1.5mm"),
  p: z77.string().default("1.475mm")
}).transform((data) => ({
  ...data,
  wave: data.wave ?? (data.reflow === void 0 ? true : !data.reflow),
  reflow: data.reflow ?? (data.wave === void 0 ? false : !data.wave)
}));
var parseDimension = (value) => {
  return Number.parseFloat(value.replace("mm", ""));
};
var getCcwSot457Coords = ({
  pitch,
  width,
  pinNumber
}) => {
  const offset = 0.1;
  const coords = {
    1: { x: -width / 2 - offset, y: pitch },
    2: { x: -width / 2 - offset, y: 0 },
    3: { x: -width / 2 - offset, y: -pitch },
    4: { x: width / 2 + offset, y: -pitch },
    5: { x: width / 2 + offset, y: 0 },
    6: { x: width / 2 + offset, y: pitch }
  };
  const coord = coords[pinNumber];
  if (!coord) {
    throw new Error(`Invalid pin number: ${pinNumber}`);
  }
  return coord;
};
var generateSot457Elements = (params) => {
  const pads = [];
  const pitch = parseDimension(params.p);
  const padLength = parseDimension(params.pl);
  const padWidth = parseDimension(params.pw);
  const width = parseDimension(params.w);
  const height = parseDimension(params.h);
  const cornerRadius = Math.min(padLength, padWidth) / 8;
  if (params.wave) {
    const pinConfigs = {
      1: ({ padWidth: padWidth2, padHeight }) => rectpad(1, -pitch, pitch, padHeight, padWidth2, cornerRadius),
      2: ({ padWidth: padWidth2, padHeight }) => rectpad(2, -pitch, -pitch, padHeight, padWidth2, cornerRadius),
      3: ({ padWidth: padWidth2, padHeight }) => pillpad(
        3,
        -pitch,
        0,
        parseDimension(params.pillw),
        parseDimension(params.pillh)
      ),
      4: ({ padWidth: padWidth2, padHeight }) => pillpad(
        4,
        pitch,
        0,
        parseDimension(params.pillw),
        parseDimension(params.pillh)
      ),
      5: ({ padWidth: padWidth2, padHeight }) => rectpad(5, pitch, pitch, padHeight, padWidth2, cornerRadius),
      6: ({ padWidth: padWidth2, padHeight }) => rectpad(6, pitch, -pitch, padHeight, padWidth2, cornerRadius)
    };
    for (let i = 1; i <= params.num_pins; i++) {
      const config = pinConfigs[i];
      if (config) {
        pads.push(config({ padWidth: padLength, padHeight: padWidth }));
      }
    }
  } else {
    for (let i = 1; i <= params.num_pins; i++) {
      const { x, y } = getCcwSot457Coords({ pitch, width, pinNumber: i });
      pads.push(rectpad(i, x, y, padLength, padWidth, cornerRadius));
    }
  }
  const silkscreenPath1 = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width / 3, y: height / 2 + pitch / 1.3 },
      { x: width / 3, y: height / 2 + pitch / 1.3 }
    ],
    stroke_width: 0.05
  };
  const silkscreenPath2 = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width / 3, y: -height / 2 - pitch / 1.3 },
      { x: width / 3, y: -height / 2 - pitch / 1.3 }
    ],
    stroke_width: 0.05
  };
  const silkscreenRefText = silkscreenRef(0, height + 0.5, 0.3);
  const pin1Position = getCcwSot457Coords({ pitch, width, pinNumber: 1 });
  const triangleHeight = params.wave ? 1 : 0.5;
  const triangleWidth = params.wave ? 0.7 : 0.3;
  pin1Position.x -= params.wave ? padWidth : padWidth * 1.7;
  const pin1Indicator = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "pin1_indicator",
    route: [
      { x: pin1Position.x + triangleHeight / 2, y: pin1Position.y },
      {
        x: pin1Position.x - triangleHeight / 2,
        y: pin1Position.y + triangleWidth / 2
      },
      {
        x: pin1Position.x - triangleHeight / 2,
        y: pin1Position.y - triangleWidth / 2
      },
      { x: pin1Position.x + triangleHeight / 2, y: pin1Position.y }
    ],
    stroke_width: 0.05
  };
  let courtyard;
  if (params.wave) {
    const pinRowSpanX = 2 * pitch;
    const pinRowSpanY = 2 * pitch;
    const padOuterHalfWidth = pinRowSpanX / 2 + padWidth / 2;
    const padOuterHalfHeight = pinRowSpanY / 2 + padLength / 2;
    const courtyardHalfWidth = padOuterHalfWidth + 0.25;
    const courtyardHalfHeight = padOuterHalfHeight + 0.25;
    courtyard = {
      type: "pcb_courtyard_rect",
      pcb_courtyard_rect_id: "",
      pcb_component_id: "",
      center: { x: 0, y: 0 },
      width: 2 * courtyardHalfWidth,
      height: 2 * courtyardHalfHeight,
      layer: "top"
    };
  } else {
    const padCenterX = width / 2 + 0.1;
    const padToeHalfWidth = padCenterX + padLength / 2;
    const pinRowHalfHeight = pitch + padWidth / 2;
    const bodyHalfWidth = width / 2;
    const bodyHalfHeight = height / 2;
    const courtyardStepOuterHalfWidth = padToeHalfWidth + 0.25;
    const courtyardStepInnerHalfWidth = bodyHalfWidth + 0.08;
    const courtyardStepOuterHalfHeight = pinRowHalfHeight + 0.25;
    const courtyardStepInnerHalfHeight = Math.max(
      bodyHalfHeight + 0.45,
      courtyardStepOuterHalfHeight
    );
    courtyard = {
      type: "pcb_courtyard_outline",
      pcb_courtyard_outline_id: "",
      pcb_component_id: "",
      outline: createRectUnionOutline([
        {
          minX: -courtyardStepOuterHalfWidth,
          maxX: courtyardStepOuterHalfWidth,
          minY: -courtyardStepOuterHalfHeight,
          maxY: courtyardStepOuterHalfHeight
        },
        {
          minX: -courtyardStepInnerHalfWidth,
          maxX: courtyardStepInnerHalfWidth,
          minY: -courtyardStepInnerHalfHeight,
          maxY: courtyardStepInnerHalfHeight
        }
      ]),
      layer: "top"
    };
  }
  return [
    silkscreenRefText,
    silkscreenPath1,
    silkscreenPath2,
    pin1Indicator,
    ...pads,
    courtyard
  ];
};
var sot457 = (rawParams) => {
  if (rawParams.wave) {
    const parameters2 = sot457WaveSchema.parse({ ...rawParams, fn: "sot457" });
    return {
      circuitJson: generateSot457Elements(parameters2),
      parameters: parameters2
    };
  }
  const parameters = sot457DefSchema.parse(rawParams);
  return {
    circuitJson: generateSot457Elements(parameters),
    parameters
  };
};

// src/fn/sot963.ts
import { z as z78 } from "zod";
import { length as length68 } from "circuit-json";
var sot963CourtyardOutline = [
  { x: -0.8, y: 0.75 },
  { x: 0.8, y: 0.75 },
  { x: 0.8, y: -0.75 },
  { x: -0.8, y: -0.75 }
];
var sot963_def = base_def.extend({
  fn: z78.string(),
  num_pins: z78.literal(6).default(6),
  w: z78.string().default("1.1mm"),
  h: z78.string().default("1.45mm"),
  p: z78.string().default("0.35mm"),
  pl: z78.string().default("0.2mm"),
  pw: z78.string().default("0.2mm"),
  string: z78.string().optional()
});
var sot963 = (raw_params) => {
  const parameters = sot963_def.parse({ ...raw_params, fn: "sot963" });
  const w = length68.parse(parameters.w);
  const h = length68.parse(parameters.h);
  const p = length68.parse(parameters.p);
  const pl = length68.parse(parameters.pl);
  const pw = length68.parse(parameters.pw);
  const cornerRadius = Math.min(pl, pw) / 8;
  const pads = [];
  for (let i = 0; i < 6; i++) {
    const { x, y } = getSot963PadCoord(i + 1, w, p, pl);
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  const silkscreenTopLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -w / 2, y: h / 2 },
      { x: w / 2, y: h / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBottomLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: -w / 2, y: -h / 2 },
      { x: w / 2, y: -h / 2 }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: ""
  };
  const pin1Position = getSot963PadCoord(1, w, p, pl);
  const pin1Marking = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "pin_marker_1",
    route: [
      { x: pin1Position.x - pl / 2 - 0.3, y: pin1Position.y },
      { x: pin1Position.x - pl / 2 - 0.45, y: pin1Position.y + 0.15 },
      { x: pin1Position.x - pl / 2 - 0.45, y: pin1Position.y - 0.15 },
      { x: pin1Position.x - pl / 2 - 0.3, y: pin1Position.y }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: "pin_marker_1"
  };
  const silkscreenRefText = silkscreenRef(0, h / 2 + 0.4, 0.25);
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot963CourtyardOutline,
    layer: "top"
  };
  return {
    circuitJson: [
      ...pads,
      silkscreenTopLine,
      silkscreenBottomLine,
      silkscreenRefText,
      pin1Marking,
      courtyard
    ],
    parameters
  };
};
var getSot963PadCoord = (pn, w, p, pl) => {
  const padCenterOffset = w / 2 - pl / 2;
  if (pn <= 3) {
    return { x: -padCenterOffset, y: p - (pn - 1) * p };
  }
  return { x: padCenterOffset, y: -p + (pn - 4) * p };
};

// src/fn/potentiometer.ts
import { z as z79 } from "zod";
var potentiometer_def = base_def.extend({
  fn: z79.string(),
  num_pins: z79.union([z79.literal(3), z79.literal(2)]).default(3),
  p: z79.string().default("5mm"),
  id: z79.string().default("1.25mm"),
  od: z79.string().default("2.35mm"),
  ca: z79.string().default("14mm").describe(
    "Caliper axis (width or diameter of the potentiometer body or adjustment knob)"
  ),
  w: z79.string().default("5.35mm"),
  h: z79.string().default("4mm"),
  string: z79.string().optional()
});
var potentiometer_acp = (parameters) => {
  const { p, id, od, h } = parameters;
  const x = Number.parseFloat(h);
  const y = Number.parseFloat(p);
  return [
    platedhole(1, 0, y, id, od),
    platedhole(2, x, 0, id, od),
    platedhole(3, 0, -y, id, od)
  ];
};
var potentiometer = (raw_params) => {
  const match = raw_params.string?.match(/^potentiometer_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 10) : 3;
  const parameters = potentiometer_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  let platedHoles = [];
  if (parameters.num_pins === 3) {
    platedHoles = potentiometer_acp(parameters);
  }
  const y = Number.parseFloat(parameters.ca) / 2 + 0.15;
  const x = Number.parseFloat(parameters.w);
  const od = Number.parseFloat(parameters.od) / 2 + 0.35;
  const silkscreenBody = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: 0, y: y - 1.75 },
      { x: 0, y },
      { x, y },
      { x, y: od }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBody2 = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x, y: -od },
      { x, y: -y },
      { x: 0, y: -y },
      { x: 0, y: -y + 1.75 }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const W = Number.parseFloat(parameters.w) / 2;
  const silkscreenRefText = silkscreenRef(W, y + 1, 0.5);
  const padRadius = Number.parseFloat(parameters.od) / 2;
  const pinRowSpanX = Number.parseFloat(parameters.h);
  const pinRowSpanY = Number.parseFloat(parameters.ca) / 2;
  const courtyardMinX = -(padRadius + 0.25);
  const courtyardMaxX = pinRowSpanX + padRadius + 0.25;
  const courtyardHalfHeight = pinRowSpanY + 0.25;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: (courtyardMinX + courtyardMaxX) / 2, y: 0 },
    width: courtyardMaxX - courtyardMinX,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  return {
    circuitJson: [
      ...platedHoles,
      silkscreenBody,
      silkscreenBody2,
      silkscreenRefText,
      courtyard
    ],
    parameters
  };
};

// src/fn/electrolytic.ts
import {
  length as length69
} from "circuit-json";
import { z as z80 } from "zod";
var electrolytic_def = base_def.extend({
  fn: z80.string(),
  p: length69.optional().default("7.5mm"),
  id: length69.optional().default("1mm"),
  od: length69.optional().default("2mm"),
  d: length69.optional().default("10.5mm")
});
var generate_circle_arcs = (centerX, centerY, radius, cut, cutHeight) => {
  const topArc = [];
  const bottomArc = [];
  for (let i = 0; i <= 50; i++) {
    const theta = i / 50 * Math.PI;
    const x = centerX + Math.cos(theta) * radius;
    const y = centerY + Math.sin(theta) * radius;
    if (x < centerX - cut && y >= centerY - cutHeight / 2 && y <= centerY + cutHeight / 2) {
      continue;
    }
    topArc.push({ x, y });
  }
  for (let i = 0; i <= 50; i++) {
    const theta = Math.PI + i / 50 * Math.PI;
    const x = centerX + Math.cos(theta) * radius;
    const y = centerY + Math.sin(theta) * radius;
    if (x < centerX - cut && y >= centerY - cutHeight / 2 && y <= centerY + cutHeight / 2) {
      continue;
    }
    bottomArc.push({ x, y });
  }
  return { topArc, bottomArc };
};
var electrolytic = (raw_params) => {
  const parameters = electrolytic_def.parse(raw_params);
  const { p, id, od, d } = parameters;
  const plated_holes = [
    platedHoleWithRectPad({
      pn: 1,
      x: -p / 2,
      y: 0,
      holeDiameter: id,
      rectPadWidth: od,
      rectPadHeight: od
    }),
    platedhole(2, p / 2, 0, id, od)
  ];
  const { topArc, bottomArc } = generate_circle_arcs(
    0,
    0,
    d / 2 + 0.1,
    od / 2,
    od
  );
  const silkscreenBody2 = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: topArc,
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBody3 = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: bottomArc,
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenBody = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: 0, y: d / 2 + 0.1 },
      { x: 0, y: -(d / 2 + 0.1) }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const X = -(d / 2 + 0.5);
  const plusY = od / 2 + 1.5;
  const Size = 0.5;
  const silkscreenpath2 = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: X - Size, y: plusY },
      { x: X + Size, y: plusY }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenline = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      { x: X, y: plusY - Size },
      { x: X, y: plusY + Size }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const silkscreenRefText = silkscreenRef(0, d / 2 + 1, 0.5);
  const bodyOuterRadius = d / 2;
  const pinOuterRadius = p / 2 + od / 2;
  const courtyardClearance = 0.25;
  const courtyardRadius = Math.max(bodyOuterRadius, pinOuterRadius) + courtyardClearance;
  const courtyard = {
    type: "pcb_courtyard_circle",
    pcb_courtyard_circle_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    radius: courtyardRadius,
    layer: "top"
  };
  return {
    circuitJson: [
      ...plated_holes,
      silkscreenBody2,
      silkscreenBody3,
      silkscreenBody,
      silkscreenpath2,
      silkscreenline,
      silkscreenRefText,
      courtyard
    ],
    parameters
  };
};

// src/fn/fpc.ts
import {
  length as length70
} from "circuit-json";
import { z as z81 } from "zod";
var fpc_def = base_def.extend({
  fn: z81.literal("fpc"),
  num_pins: z81.coerce.number().int().min(2),
  p: length70.default("0.5mm").describe("contact pad pitch"),
  pw: length70.default("0.3mm").describe("contact pad width"),
  pl: length70.default("1.25mm").describe("contact pad length"),
  staggered: z81.boolean().default(false).describe("alternate contacts between two rows"),
  reverse: z81.boolean().default(false).describe("place pin 1 on the upper row when staggered"),
  py: length70.default("2.4mm").describe("staggered row center pitch"),
  toppl: length70.optional().describe("upper-row contact pad length"),
  bottompl: length70.optional().describe("lower-row contact pad length"),
  mpx: length70.optional().describe("mounting pad center pitch"),
  mpy: length70.optional().describe("mounting pad row distance from the contact-row center"),
  mounttop: z81.boolean().default(false).describe("place the mounting pads above the contact-row center"),
  mpw: length70.default("2mm").describe("mounting pad width"),
  mpl: length70.default("2.5mm").describe("mounting pad length")
});
var rectangularPad = (...parameters) => rectpad(...parameters);
var getPadBounds = (pads) => ({
  maxX: Math.max(...pads.map((pad2) => pad2.x + pad2.width / 2)),
  maxY: Math.max(...pads.map((pad2) => pad2.y + pad2.height / 2)),
  minX: Math.min(...pads.map((pad2) => pad2.x - pad2.width / 2)),
  minY: Math.min(...pads.map((pad2) => pad2.y - pad2.height / 2))
});
var fpc = (rawParams) => {
  const parsedParameters = fpc_def.parse(rawParams);
  const {
    num_pins: numPins,
    p,
    pw,
    pl,
    staggered,
    reverse,
    py,
    toppl,
    bottompl,
    mounttop,
    mpw,
    mpl
  } = parsedParameters;
  const contactSpan = (numPins - 1) * p;
  const mpx = parsedParameters.mpx ?? contactSpan + (staggered ? 3.5 : 3.38);
  const mpy = parsedParameters.mpy ?? (staggered ? 0 : 2.575);
  const mountY = mpy === 0 ? 0 : (mounttop ? 1 : -1) * mpy;
  const startX = -contactSpan / 2;
  const contactPads = Array.from({ length: numPins }, (_, index) => {
    const isUpperRow = staggered && index % 2 === 1 !== reverse;
    const y = staggered ? isUpperRow ? py / 2 : -py / 2 : 0;
    const padLength = isUpperRow ? toppl ?? pl : bottompl ?? pl;
    return rectangularPad(index + 1, startX + index * p, y, pw, padLength);
  });
  const mountingPads = [
    rectangularPad(numPins + 1, mpx / 2, mountY, mpw, mpl),
    rectangularPad(numPins + 2, -mpx / 2, mountY, mpw, mpl)
  ];
  const pads = [...contactPads, ...mountingPads];
  const bounds = getPadBounds(pads);
  const silkInsetX = Math.min(contactSpan / 2 + pw / 2, mpx / 2 - mpw / 2);
  const topSilkY = bounds.maxY + 0.2;
  const bottomSilkY = bounds.minY - 0.2;
  const pinOne = contactPads[0];
  const pinMarkerX = pinOne.x - pinOne.width / 2 - 0.2;
  const pinMarkerY = pinOne.y;
  const silkscreen = [
    silkscreenpath([
      { x: -silkInsetX, y: topSilkY },
      { x: silkInsetX, y: topSilkY }
    ]),
    silkscreenpath([
      { x: -silkInsetX, y: bottomSilkY },
      { x: silkInsetX, y: bottomSilkY }
    ]),
    silkscreenpath([
      { x: pinMarkerX - 0.25, y: pinMarkerY - 0.25 },
      { x: pinMarkerX, y: pinMarkerY },
      { x: pinMarkerX - 0.25, y: pinMarkerY + 0.25 }
    ])
  ];
  const ref = silkscreenRef(0, topSilkY + 0.7, 0.5);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: {
      x: (bounds.minX + bounds.maxX) / 2,
      y: (bounds.minY + bounds.maxY) / 2
    },
    width: bounds.maxX - bounds.minX + 0.5,
    height: bounds.maxY - bounds.minY + 0.5,
    layer: "top"
  };
  return {
    circuitJson: [...pads, ...silkscreen, ref, courtyard],
    parameters: {
      ...parsedParameters,
      mpx,
      mpy,
      toppl: toppl ?? pl,
      bottompl: bottompl ?? pl
    }
  };
};

// src/fn/smbf.ts
import { z as z82 } from "zod";
import { length as length71 } from "circuit-json";
var smbf_def = base_def.extend({
  fn: z82.string(),
  num_pins: z82.literal(2).default(2),
  w: z82.string().default("6.5mm"),
  h: z82.string().default("3mm"),
  pl: z82.string().default("1.75mm"),
  pw: z82.string().default("2.40mm"),
  p: z82.string().default("4.75mm")
});
var smbf = (raw_params) => {
  const parameters = smbf_def.parse(raw_params);
  const silkscreenRefText = silkscreenRef(
    0,
    length71.parse(parameters.h) - 0.5,
    0.3
  );
  const silkscreenLine = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    route: [
      {
        x: length71.parse(parameters.p) / 2,
        y: length71.parse(parameters.h) / 2
      },
      {
        x: -length71.parse(parameters.w) / 2 - 0.3,
        y: length71.parse(parameters.h) / 2
      },
      {
        x: -length71.parse(parameters.w) / 2 - 0.3,
        y: -length71.parse(parameters.h) / 2
      },
      {
        x: length71.parse(parameters.p) / 2,
        y: -length71.parse(parameters.h) / 2
      }
    ],
    stroke_width: 0.1,
    pcb_silkscreen_path_id: ""
  };
  const courtyardWidthMm = 7;
  const courtyardHeightMm = 3.5;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: courtyardWidthMm,
    height: courtyardHeightMm,
    layer: "top"
  };
  return {
    circuitJson: smbfWithoutParsing(parameters).concat(
      ...createFabricationNoteDiodeFromCopperPads(parameters),
      silkscreenLine,
      silkscreenRefText,
      courtyard
    ),
    parameters
  };
};
var getSmbfCoords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) {
    return { x: -p / 2, y: 0 };
  }
  return { x: p / 2, y: 0 };
};
var smbfWithoutParsing = (parameters) => {
  const pads = [];
  const p = length71.parse(parameters.p);
  const pl = length71.parse(parameters.pl);
  const pw = length71.parse(parameters.pw);
  const cornerRadius = 0.125;
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getSmbfCoords({
      pn: i,
      p
    });
    pads.push(rectpad(i, x, y, pl, pw, cornerRadius));
  }
  return pads;
};

// src/fn/sot323.ts
import { z as z83 } from "zod";
var sot323CourtyardOutline = [
  { x: -1.45, y: 0.98 },
  { x: -0.73, y: 0.98 },
  { x: -0.73, y: 1.1 },
  { x: 0.73, y: 1.1 },
  { x: 0.73, y: 0.33 },
  { x: 1.45, y: 0.33 },
  { x: 1.45, y: -0.33 },
  { x: 0.73, y: -0.33 },
  { x: 0.73, y: -1.1 },
  { x: -0.73, y: -1.1 },
  { x: -0.73, y: -0.98 },
  { x: -1.45, y: -0.98 },
  { x: -1.45, y: -0.32 },
  { x: -0.73, y: -0.32 },
  { x: -0.73, y: 0.32 },
  { x: -1.45, y: 0.32 }
];
var sot323_def = base_def.extend({
  fn: z83.string(),
  num_pins: z83.number().default(3),
  w: z83.string().default("2.45mm"),
  h: z83.string().default("2.40mm"),
  pl: z83.string().default("0.92mm"),
  pw: z83.string().default("0.45mm"),
  p: z83.string().default("0.891mm"),
  string: z83.string().optional()
});
var sot323 = (raw_params) => {
  const match = raw_params.string?.match(/^sot323_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 3) : 3;
  const parameters = sot323_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  if (parameters.num_pins === 3) {
    return {
      circuitJson: sot323_3(parameters),
      parameters
    };
  }
  throw new Error("Invalid number of pins");
};
var getCcwSot323Coords = (parameters) => {
  const { pn, w, h, pl, p } = parameters;
  if (pn === 1) {
    return { x: -p, y: 0.65 };
  }
  if (pn === 2) {
    return { x: -p, y: -0.65 };
  }
  return { x: p, y: 0 };
};
var sot323_3 = (parameters) => {
  const pads = [];
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  const pl = Number.parseFloat(parameters.pl);
  const pw = Number.parseFloat(parameters.pw);
  const p = Number.parseFloat(parameters.p);
  const cornerRadius = Math.min(pl, pw) / 8;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getCcwSot323Coords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w,
      h,
      pl,
      p
    });
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
  }
  const silkscreenRefText = silkscreenRef(
    0,
    Number.parseInt(parameters.h) / 2 + 1,
    0.3
  );
  const width = w / 2 - pl;
  const height = h / 2;
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width, y: height + 0.3 },
      { x: width, y: height + 0.3 },
      { x: width, y: height / 2 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width, y: -height - 0.3 },
      { x: width, y: -height - 0.3 },
      { x: width, y: -height / 2 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot323CourtyardOutline,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenPath1,
    silkscreenPath2,
    silkscreenRefText,
    courtyard
  ];
};

// src/fn/smtpad.ts
import { z as z84 } from "zod";
import { length as length72 } from "circuit-json";
import { mm as mm15 } from "@tscircuit/mm";
var smtpad_def = base_def.extend({
  fn: z84.string(),
  circle: z84.boolean().optional(),
  rect: z84.boolean().optional(),
  square: z84.boolean().optional(),
  pill: z84.boolean().optional(),
  d: length72.optional(),
  pd: length72.optional(),
  diameter: length72.optional(),
  r: length72.optional(),
  pr: length72.optional(),
  radius: length72.optional(),
  w: length72.optional(),
  pw: length72.optional(),
  width: length72.optional(),
  h: length72.optional(),
  ph: length72.optional(),
  height: length72.optional(),
  s: length72.optional(),
  size: length72.optional(),
  string: z84.string().optional()
}).transform((v) => {
  let shape = "rect";
  if (v.circle) shape = "circle";
  if (v.square) shape = "square";
  if (v.rect) shape = "rect";
  if (v.pill) shape = "pill";
  let radius;
  let width;
  let height;
  if (shape === "circle") {
    if (v.r !== void 0) radius = mm15(v.r);
    else if (v.pr !== void 0) radius = mm15(v.pr);
    else if (v.radius !== void 0) radius = mm15(v.radius);
    else if (v.d !== void 0) radius = mm15(v.d) / 2;
    else if (v.pd !== void 0) radius = mm15(v.pd) / 2;
    else if (v.diameter !== void 0) radius = mm15(v.diameter) / 2;
    else radius = mm15("1mm") / 2;
  } else {
    if (v.w !== void 0) width = mm15(v.w);
    else if (v.pw !== void 0) width = mm15(v.pw);
    else if (v.width !== void 0) width = mm15(v.width);
    else if (v.s !== void 0) width = mm15(v.s);
    else if (v.size !== void 0) width = mm15(v.size);
    else width = mm15("1mm");
    if (v.h !== void 0) height = mm15(v.h);
    else if (v.ph !== void 0) height = mm15(v.ph);
    else if (v.height !== void 0) height = mm15(v.height);
    else if (shape === "square") height = width;
    else if (shape === "rect")
      height = width;
    else height = mm15("1mm");
  }
  return {
    fn: v.fn,
    shape,
    radius,
    width,
    height
  };
});
var smtpad = (raw_params) => {
  const params = smtpad_def.parse(raw_params);
  const { shape, radius, width, height } = params;
  let pad2;
  let silkscreenOffset;
  if (shape === "circle") {
    pad2 = circlepad(1, { x: 0, y: 0, radius });
    silkscreenOffset = radius + 0.5;
  } else if (shape === "pill") {
    pad2 = pillpad(1, 0, 0, width, height);
    silkscreenOffset = Math.max(width, height) / 2 + 0.5;
  } else {
    pad2 = rectpad(1, 0, 0, width, height);
    silkscreenOffset = height / 2 + 0.5;
  }
  return {
    circuitJson: [
      pad2,
      silkscreenRef(0, silkscreenOffset, 0.2)
    ],
    parameters: params
  };
};

// src/fn/smdpushbutton.ts
import {
  length as length73
} from "circuit-json";
import { z as z85 } from "zod";
var smdpushbutton_def = base_def.extend({
  fn: z85.literal("smdpushbutton"),
  num_pins: z85.literal(4).default(4),
  px: length73.default("4.2mm").describe("horizontal pad center pitch"),
  py: length73.default("2.15mm").describe("vertical pad center pitch"),
  pw: length73.default("1.05mm").describe("pad width"),
  ph: length73.default("0.7mm").describe("pad height")
});
var smdpushbutton = (rawParams) => {
  const parameters = smdpushbutton_def.parse(rawParams);
  const { px, py, pw, ph } = parameters;
  const pads = [
    rectpad(1, -px / 2, py / 2, pw, ph),
    rectpad(2, px / 2, py / 2, pw, ph),
    rectpad(3, -px / 2, -py / 2, pw, ph),
    rectpad(4, px / 2, -py / 2, pw, ph)
  ];
  const innerHalfWidth = Math.max(px / 2 - pw / 2 - 0.15, 0.2);
  const outerHalfHeight = py / 2 + ph / 2 + 0.1;
  const sideGapHalfHeight = Math.max(py / 2 - ph / 2 - 0.15, 0.2);
  const silkscreen = [
    silkscreenpath([
      { x: -innerHalfWidth, y: outerHalfHeight },
      { x: innerHalfWidth, y: outerHalfHeight }
    ]),
    silkscreenpath([
      { x: -innerHalfWidth, y: -outerHalfHeight },
      { x: innerHalfWidth, y: -outerHalfHeight }
    ]),
    silkscreenpath([
      { x: -px / 2, y: -sideGapHalfHeight },
      { x: -px / 2, y: sideGapHalfHeight }
    ]),
    silkscreenpath([
      { x: px / 2, y: -sideGapHalfHeight },
      { x: px / 2, y: sideGapHalfHeight }
    ])
  ];
  const ref = silkscreenRef(0, outerHalfHeight + 0.6, 0.5);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: px + pw + 0.5,
    height: py + ph + 0.5,
    layer: "top"
  };
  return {
    circuitJson: [...pads, ...silkscreen, ref, courtyard],
    parameters
  };
};

// node_modules/@tscircuit/math-utils/dist/chunk-3WCUPG5S.js
var getBoundsCenter = (bounds) => ({
  x: (bounds.minX + bounds.maxX) / 2,
  y: (bounds.minY + bounds.maxY) / 2
});

// node_modules/@tscircuit/math-utils/dist/chunk-5N7UJNVK.js
var getBoundsFromPoints = (points) => {
  if (points.length === 0) {
    return null;
  }
  let minX = points[0].x;
  let minY = points[0].y;
  let maxX = points[0].x;
  let maxY = points[0].y;
  for (let i = 1; i < points.length; i++) {
    const point = points[i];
    if (point.x < minX) minX = point.x;
    if (point.y < minY) minY = point.y;
    if (point.x > maxX) maxX = point.x;
    if (point.y > maxY) maxY = point.y;
  }
  return { minX, minY, maxX, maxY };
};

// src/fn/smdslideswitch.ts
import {
  length as length74
} from "circuit-json";
import { z as z86 } from "zod";
var smdslideswitch_def = base_def.extend({
  fn: z86.literal("smdslideswitch"),
  num_pins: z86.literal(7).default(7),
  signalcols: z86.coerce.number().int().min(3).default(3),
  missing: function_call.default([]),
  p: length74.default("1mm").describe("signal-pad column pitch"),
  pw: length74.default("0.7mm").describe("signal-pad width"),
  pl: length74.default("1.5mm").describe("signal-pad length"),
  mounty: length74.default("-1.8mm").describe("Y position of the mounting-pad row center"),
  mpx: length74.default("5.5mm").describe("horizontal center-to-center mounting-pad distance"),
  mpy: length74.default("2.2mm").describe("vertical center-to-center mounting-pad distance"),
  mpw: length74.default("1mm").describe("mounting-pad width"),
  mpl: length74.default("0.8mm").describe("mounting-pad length"),
  holex: length74.default("1.5mm").describe("alignment-hole X distance from the footprint center"),
  holey: length74.optional().describe("alignment-hole Y position; defaults to mounty"),
  holed: length74.default("0.9mm").describe("alignment-hole diameter"),
  noholes: z86.boolean().optional().default(false)
});
var smdslideswitch = (rawParams) => {
  const parameters = smdslideswitch_def.parse(rawParams);
  const {
    num_pins,
    signalcols,
    missing,
    p,
    pw,
    pl,
    mounty,
    mpx,
    mpy,
    mpw,
    mpl,
    holex,
    holed,
    noholes
  } = parameters;
  const holey = parameters.holey ?? mounty;
  const invalidMissingColumn = missing.find(
    (column) => typeof column !== "number" || !Number.isInteger(column) || column < 1 || column > signalcols
  );
  if (invalidMissingColumn !== void 0) {
    throw new Error(
      `Invalid missing signal column "${invalidMissingColumn}" for signalcols${signalcols}`
    );
  }
  const missingColumns = new Set(
    missing.filter((column) => typeof column === "number")
  );
  const signalPadCount = signalcols - missingColumns.size;
  if (signalPadCount + 4 !== num_pins) {
    throw new Error(
      `smdslideswitch${num_pins} needs ${num_pins - 4} signal pads, but signalcols${signalcols}_missing(${[...missingColumns].join(",")}) creates ${signalPadCount}`
    );
  }
  const clean = (value) => Number(value.toFixed(12));
  const pads = [];
  const signalStartX = -((signalcols - 1) * p) / 2;
  let pinNumber = 1;
  for (let column = 1; column <= signalcols; column++) {
    if (missingColumns.has(column)) continue;
    pads.push(
      rectpad(pinNumber, clean(signalStartX + (column - 1) * p), 0, pw, pl)
    );
    pinNumber++;
  }
  const mountTopY = mounty + mpy / 2;
  const mountBottomY = mounty - mpy / 2;
  const mountPositions = [
    { x: clean(-mpx / 2), y: clean(mountTopY) },
    { x: clean(mpx / 2), y: clean(mountTopY) },
    { x: clean(-mpx / 2), y: clean(mountBottomY) },
    { x: clean(mpx / 2), y: clean(mountBottomY) }
  ];
  for (const position of mountPositions) {
    pads.push(rectpad(pinNumber, position.x, position.y, mpw, mpl));
    pinNumber++;
  }
  const holes = noholes ? [] : [-holex, holex].map((x) => ({
    type: "pcb_hole",
    pcb_hole_id: "",
    pcb_component_id: "",
    hole_shape: "circle",
    hole_diameter: holed,
    x,
    y: holey
  }));
  const bodyHalfWidth = Math.max(mpx / 2 - mpw / 2 - 0.2, p);
  const bodyTop = -pl / 2 - 0.2;
  const bodyBottom = mountBottomY + mpl / 2 + 0.2;
  const silkscreen = [
    silkscreenpath([
      { x: -bodyHalfWidth, y: bodyTop },
      { x: bodyHalfWidth, y: bodyTop }
    ]),
    silkscreenpath([
      { x: -bodyHalfWidth, y: bodyBottom },
      { x: bodyHalfWidth, y: bodyBottom }
    ]),
    silkscreenpath([
      { x: -bodyHalfWidth, y: bodyBottom },
      { x: -bodyHalfWidth, y: bodyTop }
    ]),
    silkscreenpath([
      { x: bodyHalfWidth, y: bodyBottom },
      { x: bodyHalfWidth, y: bodyTop }
    ])
  ];
  const signalEndX = signalStartX + (signalcols - 1) * p;
  const geometryBounds = getBoundsFromPoints([
    { x: signalStartX - pw / 2, y: -pl / 2 },
    { x: signalEndX + pw / 2, y: pl / 2 },
    { x: -mpx / 2 - mpw / 2, y: mountBottomY - mpl / 2 },
    { x: mpx / 2 + mpw / 2, y: mountTopY + mpl / 2 },
    ...!noholes ? [
      { x: -holex - holed / 2, y: holey - holed / 2 },
      { x: holex + holed / 2, y: holey + holed / 2 }
    ] : []
  ]);
  if (!geometryBounds) {
    throw new Error("Could not determine SMD slide-switch bounds");
  }
  const courtyardMargin = 0.25;
  const courtyardCenter = getBoundsCenter(geometryBounds);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: courtyardCenter,
    width: geometryBounds.maxX - geometryBounds.minX + courtyardMargin * 2,
    height: geometryBounds.maxY - geometryBounds.minY + courtyardMargin * 2,
    layer: "top"
  };
  const ref = silkscreenRef(
    courtyardCenter.x,
    geometryBounds.maxY + 0.6,
    0.5
  );
  return {
    circuitJson: [...pads, ...holes, ...silkscreen, ref, courtyard],
    parameters
  };
};

// src/fn/smdpinheader.ts
import {
  length as length75
} from "circuit-json";
import { z as z87 } from "zod";
var smdpinheader_def = base_def.extend({
  fn: z87.literal("smdpinheader"),
  num_pins: z87.number().int().positive().default(6),
  // Defaults follow the Harwin M20-877R recommended vertical SMT land pattern.
  p: length75.default("2.54mm").describe("contact pitch"),
  py: length75.default("3.31mm").describe("center-to-center distance between alternating pad rows"),
  pw: length75.default("1mm").describe("pad width"),
  ph: length75.default("2.51mm").describe("pad height"),
  bh: length75.default("2.5mm").describe("plastic body height"),
  male: z87.literal(true).default(true),
  female: z87.literal(false).default(false),
  smd: z87.literal(true).default(true)
});
var smdpinheader = (rawParams) => {
  const parameters = smdpinheader_def.parse(rawParams);
  const { num_pins, p, py, pw, ph, bh } = parameters;
  const xStart = -((num_pins - 1) * p) / 2;
  const pads = Array.from(
    { length: num_pins },
    (_, index) => rectpad(
      index + 1,
      xStart + index * p,
      index % 2 === 0 ? py / 2 : -py / 2,
      pw,
      ph
    )
  );
  const bodyHalfWidth = num_pins * p / 2;
  const bodyHalfHeight = bh / 2;
  const pin1Chamfer = Math.min(0.5, bh / 4);
  const createHorizontalOutlineSegments = (y, padParity, startX = -bodyHalfWidth) => {
    const padClearance = pw / 2 + 0.2;
    const segments = [];
    let segmentStart = startX;
    for (let index = padParity; index < num_pins; index += 2) {
      const padCenterX = xStart + index * p;
      const segmentEnd = padCenterX - padClearance;
      if (segmentEnd > segmentStart) {
        segments.push(
          silkscreenpath([
            { x: segmentStart, y },
            { x: segmentEnd, y }
          ])
        );
      }
      segmentStart = padCenterX + padClearance;
    }
    if (segmentStart < bodyHalfWidth) {
      segments.push(
        silkscreenpath([
          { x: segmentStart, y },
          { x: bodyHalfWidth, y }
        ])
      );
    }
    return segments;
  };
  const bodyOutline = [
    ...createHorizontalOutlineSegments(
      bodyHalfHeight,
      0,
      -bodyHalfWidth + pin1Chamfer
    ),
    ...createHorizontalOutlineSegments(-bodyHalfHeight, 1),
    silkscreenpath([
      { x: bodyHalfWidth, y: -bodyHalfHeight },
      { x: bodyHalfWidth, y: bodyHalfHeight }
    ]),
    silkscreenpath([
      { x: -bodyHalfWidth, y: -bodyHalfHeight },
      { x: -bodyHalfWidth, y: bodyHalfHeight - pin1Chamfer },
      { x: -bodyHalfWidth + pin1Chamfer, y: bodyHalfHeight }
    ])
  ];
  const padOuterHalfWidth = (num_pins - 1) * p / 2 + pw / 2;
  const padOuterHalfHeight = py / 2 + ph / 2;
  const courtyardHalfWidth = Math.max(bodyHalfWidth, padOuterHalfWidth) + 0.25;
  const courtyardHalfHeight = Math.max(bodyHalfHeight, padOuterHalfHeight) + 0.25;
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: 0 },
    width: 2 * courtyardHalfWidth,
    height: 2 * courtyardHalfHeight,
    layer: "top"
  };
  const ref = silkscreenRef(0, courtyardHalfHeight + 0.6, 0.5);
  return {
    circuitJson: [...pads, ...bodyOutline, ref, courtyard],
    parameters
  };
};

// src/fn/platedhole.ts
import { z as z88 } from "zod";
import { length as length76 } from "circuit-json";
import { mm as mm16 } from "@tscircuit/mm";
var platedhole_def = base_def.extend({
  fn: z88.string(),
  d: length76.optional(),
  hd: length76.optional(),
  r: length76.optional(),
  hr: length76.optional(),
  pd: length76.optional(),
  pr: length76.optional(),
  squarepad: z88.boolean().optional().default(false)
}).transform((v) => {
  let holeD;
  if (v.d !== void 0) holeD = mm16(v.d);
  else if (v.hd !== void 0) holeD = mm16(v.hd);
  else if (v.r !== void 0) holeD = mm16(v.r) * 2;
  else if (v.hr !== void 0) holeD = mm16(v.hr) * 2;
  else holeD = mm16("1mm");
  let padD;
  if (v.pd !== void 0) padD = mm16(v.pd);
  else if (v.pr !== void 0) padD = mm16(v.pr) * 2;
  else padD = holeD * (1.5 / 1);
  return {
    fn: v.fn,
    d: holeD,
    pd: padD,
    squarepad: v.squarepad ?? false
  };
});
var platedhole2 = (raw_params) => {
  const params = platedhole_def.parse(raw_params);
  const { d, pd, squarepad } = params;
  return {
    circuitJson: [
      squarepad ? platedHoleWithRectPad({
        pn: 1,
        x: 0,
        y: 0,
        holeDiameter: d,
        rectPadWidth: pd,
        rectPadHeight: pd
      }) : platedhole(1, 0, 0, d, pd),
      silkscreenRef(0, pd / 2 + 0.5, 0.2)
    ],
    parameters: params
  };
};

// src/fn/sot.ts
import { z as z89 } from "zod";
var sot_def = base_def.extend({
  fn: z89.string(),
  num_pins: z89.literal(6).default(6),
  h: z89.string().default("1.3mm"),
  pl: z89.string().default("1.325mm"),
  pw: z89.string().default("0.6mm"),
  p: z89.string().default("0.95mm")
});
var sot = (raw_params) => {
  const parameters = sot_def.parse(raw_params);
  return {
    circuitJson: sotWithoutParsing(parameters),
    parameters
  };
};
var getCcwSotCoords = (parameters) => {
  const { p, h, pn } = parameters;
  if (pn === 1) {
    return { x: -h / 2 - 0.4875, y: p };
  }
  if (pn === 2) {
    return { x: -h / 2 - 0.4875, y: 0 };
  }
  if (pn === 3) {
    return { x: -h / 2 - 0.4875, y: -p };
  }
  if (pn === 4) {
    return { x: h / 2 + 0.4875, y: -p };
  }
  if (pn === 5) {
    return { x: h / 2 + 0.4875, y: 0 };
  }
  if (pn === 6) {
    return { x: h / 2 + 0.4875, y: p };
  }
  throw new Error("Invalid pin number");
};
var sotWithoutParsing = (parameters) => {
  const pads = [];
  const h = Number.parseFloat(parameters.h);
  const p = Number.parseFloat(parameters.p);
  const pl = Number.parseFloat(parameters.pl);
  const pw = Number.parseFloat(parameters.pw);
  const cornerRadius = Math.min(pl, pw) / 8;
  for (let i = 1; i <= parameters.num_pins; i++) {
    const { x, y } = getCcwSotCoords({
      h,
      p,
      pn: i
    });
    pads.push(rectpad(i, x, y, pl, pw, cornerRadius));
  }
  const width = parameters.num_pins / 2 * p;
  const height = h;
  const silkscreenPath1 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_1",
    route: [
      { x: -width / 3, y: height / 2 + Number.parseFloat(parameters.p) / 1.3 },
      { x: width / 3, y: height / 2 + p / 1.3 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.05
  };
  const silkscreenPath2 = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_2",
    route: [
      { x: -width / 3, y: -height / 2 - p / 1.3 },
      { x: width / 3, y: -height / 2 - p / 1.3 }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.05
  };
  const silkscreenRefText = silkscreenRef(0, height + 0.3, 0.3);
  const pin1Position = getCcwSotCoords({
    h,
    p,
    pn: 1
  });
  pin1Position.x = pin1Position.x - pw * 1.5;
  const triangleHeight = 0.7;
  const triangleWidth = 0.3;
  const pin1Indicator = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "pin1_indicator",
    route: [
      {
        x: pin1Position.x + triangleHeight / 2 - 0.2,
        y: pin1Position.y
      },
      {
        x: pin1Position.x - triangleHeight / 2,
        y: pin1Position.y + triangleWidth / 2
      },
      {
        x: pin1Position.x - triangleHeight / 2,
        y: pin1Position.y - triangleWidth / 2
      },
      {
        x: pin1Position.x + triangleHeight / 2 - 0.2,
        y: pin1Position.y
      }
    ],
    stroke_width: 0.05
  };
  const pinColumnCenterX = h / 2 + 0.4875;
  const pinRowSpanY = p * 2 + pw;
  const pinToeHalfSpanX = pinColumnCenterX + pl / 2;
  const courtyardStepInnerHalfWidth = h / 2 + 0.4;
  const courtyardStepOuterHalfWidth = pinToeHalfSpanX + 0.25;
  const courtyardStepInnerHalfHeight = pinRowSpanY / 2 + 0.25;
  const courtyardStepOuterHalfHeight = courtyardStepInnerHalfHeight + 0.2;
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    layer: "top",
    outline: createRectUnionOutline([
      {
        minX: -courtyardStepOuterHalfWidth,
        maxX: courtyardStepOuterHalfWidth,
        minY: -courtyardStepInnerHalfHeight,
        maxY: courtyardStepInnerHalfHeight
      },
      {
        minX: -courtyardStepInnerHalfWidth,
        maxX: courtyardStepInnerHalfWidth,
        minY: -courtyardStepOuterHalfHeight,
        maxY: courtyardStepOuterHalfHeight
      }
    ])
  };
  return [
    ...pads,
    silkscreenRefText,
    silkscreenPath1,
    silkscreenPath2,
    pin1Indicator,
    courtyard
  ];
};

// src/fn/sot343.ts
import { z as z90 } from "zod";
var sot343CourtyardOutline = [
  { x: -1.703, y: 0.98 },
  { x: -0.983, y: 0.98 },
  { x: -0.983, y: 1.1 },
  { x: 0.477, y: 1.1 },
  { x: 0.477, y: 0.98 },
  { x: 1.197, y: 0.98 },
  { x: 1.197, y: -0.98 },
  { x: 0.477, y: -0.98 },
  { x: 0.477, y: -1.1 },
  { x: -0.983, y: -1.1 },
  { x: -0.983, y: -0.98 },
  { x: -1.703, y: -0.98 }
];
var sot343_def = base_def.extend({
  fn: z90.string(),
  num_pins: z90.number().default(4),
  w: z90.string().default("3.2mm"),
  h: z90.string().default("2.6mm"),
  pl: z90.string().default("1.05mm"),
  pw: z90.string().default("0.45mm"),
  p: z90.string().default("0.55mm"),
  string: z90.string().optional()
});
var sot343 = (raw_params) => {
  const match = raw_params.string?.match(/^sot343_(\d+)/);
  const numPins = match ? Number.parseInt(match[1], 4) : 4;
  const parameters = sot343_def.parse({
    ...raw_params,
    num_pins: numPins
  });
  if (parameters.num_pins === 4) {
    return {
      circuitJson: sot343_4(parameters),
      parameters
    };
  }
  throw new Error("Invalid number of pins");
};
var getCcwSot343Coords = (parameters) => {
  const { pn, p } = parameters;
  if (pn === 1) return { x: -p * 1.92, y: -0.65 };
  if (pn === 2) return { x: -p * 1.92, y: 0.65 };
  if (pn === 3) return { x: p, y: 0.65 };
  if (pn === 4) return { x: p, y: -0.65 };
  return { x: 0, y: 0 };
};
var sot343_4 = (parameters) => {
  const pads = [];
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  const pl = Number.parseFloat(parameters.pl);
  const pw = Number.parseFloat(parameters.pw);
  const p = Number.parseFloat(parameters.p);
  const cornerRadius = Math.min(pl, pw) / 8;
  let minX = Infinity;
  let maxX = -Infinity;
  let minY = Infinity;
  let maxY = -Infinity;
  for (let i = 0; i < parameters.num_pins; i++) {
    const { x, y } = getCcwSot343Coords({
      num_pins: parameters.num_pins,
      pn: i + 1,
      w,
      h,
      pl,
      p
    });
    pads.push(rectpad(i + 1, x, y, pl, pw, cornerRadius));
    if (x < minX) minX = x;
    if (x > maxX) maxX = x;
    if (y < minY) minY = y;
    if (y > maxY) maxY = y;
  }
  const silkscreenXOffset = (minX + maxX) / 2;
  const padVerticalDistance = maxY - minY;
  const silkscreenMargin = h * 0.3;
  const offsetY = padVerticalDistance / 2 + silkscreenMargin;
  let silkscreenLineLength = w * 0.8;
  if (h <= 2.6) {
    silkscreenLineLength /= 2;
  }
  const silkscreenRefText = silkscreenRef(
    silkscreenXOffset,
    offsetY + 0.5,
    0.3
  );
  const silkscreenPathTop = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_top",
    route: [
      { x: silkscreenXOffset - silkscreenLineLength / 2, y: offsetY },
      { x: silkscreenXOffset + silkscreenLineLength / 2, y: offsetY }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const silkscreenPathBottom = {
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "silkscreen_path_bottom",
    route: [
      { x: silkscreenXOffset - silkscreenLineLength / 2, y: -offsetY },
      { x: silkscreenXOffset + silkscreenLineLength / 2, y: -offsetY }
    ],
    type: "pcb_silkscreen_path",
    stroke_width: 0.1
  };
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: sot343CourtyardOutline,
    layer: "top"
  };
  return [
    ...pads,
    silkscreenPathTop,
    silkscreenPathBottom,
    silkscreenRefText,
    courtyard
  ];
};

// src/fn/m2host.ts
import { z as z91 } from "zod";
var m2host_def = base_def.extend({
  fn: z91.string()
});
var m2host = (raw_params) => {
  const parameters = m2host_def.parse(raw_params);
  const pads = [];
  const padWidth = 0.5 - 0.15;
  const padLength = 1.5;
  const pitch = 0.5;
  const halfPitch = pitch / 2;
  const rowOffset = 0.5;
  const numPads = 75;
  const startY = -((numPads - 1) * pitch) / 2;
  for (let i = 0; i < numPads; i++) {
    const pn = i + 1;
    if (pn >= 24 && pn <= 31) continue;
    const y = startY - i * halfPitch;
    const isBottomLayer = pn % 2 === 0;
    const padLengthWithOffset = padLength + (isBottomLayer ? 0.25 : 0);
    const rightEdgeOffset = 0.5;
    const x = rightEdgeOffset - padLengthWithOffset / 2;
    const pad2 = rectpad(pn, x, y, padLengthWithOffset, padWidth);
    pad2.layer = isBottomLayer ? "bottom" : "top";
    pads.push(pad2);
  }
  const cutoutWidth = 46 * 0.0254;
  const cutoutDepth = 137 * 0.0254;
  const cutoutOffsetFromPin1 = 261 * 0.0254;
  const cutout = {
    type: "pcb_cutout",
    pcb_cutout_id: "",
    shape: "rect",
    center: {
      x: -cutoutDepth / 2 + padLength / 2,
      y: startY - cutoutOffsetFromPin1
    },
    width: cutoutDepth,
    height: cutoutWidth
  };
  const pin1MarkerPosition = {
    x: -0.5,
    y: startY
  };
  const pin1Marker = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "pin_marker_1",
    route: [
      { x: pin1MarkerPosition.x - 0.4, y: pin1MarkerPosition.y },
      { x: pin1MarkerPosition.x - 0.7, y: pin1MarkerPosition.y + 0.3 },
      { x: pin1MarkerPosition.x - 0.7, y: pin1MarkerPosition.y - 0.3 },
      { x: pin1MarkerPosition.x - 0.4, y: pin1MarkerPosition.y }
    ],
    stroke_width: 0.05,
    pcb_silkscreen_path_id: "pin_marker_1"
  };
  let minX = Infinity;
  let maxX = -Infinity;
  let minY = Infinity;
  let maxY = -Infinity;
  const updateBounds = (x, y, w = 0, h = 0) => {
    minX = Math.min(minX, x - w / 2);
    maxX = Math.max(maxX, x + w / 2);
    minY = Math.min(minY, y - h / 2);
    maxY = Math.max(maxY, y + h / 2);
  };
  for (const pad2 of pads) {
    const rectPad = pad2;
    updateBounds(rectPad.x, rectPad.y, rectPad.width, rectPad.height);
  }
  updateBounds(cutout.center.x, cutout.center.y, cutout.width, cutout.height);
  for (const point of pin1Marker.route) {
    updateBounds(point.x, point.y);
  }
  const centerX = (minX + maxX) / 2;
  const centerY = (minY + maxY) / 2;
  const translate = (el) => {
    if (typeof el.x === "number") el.x -= centerX;
    if (typeof el.y === "number") el.y -= centerY;
    if (el.center) {
      el.center.x -= centerX;
      el.center.y -= centerY;
    }
    if (Array.isArray(el.route)) {
      el.route = el.route.map((p) => ({
        x: p.x - centerX,
        y: p.y - centerY
      }));
    }
  };
  for (const pad2 of pads) translate(pad2);
  translate(cutout);
  translate(pin1Marker);
  return {
    circuitJson: [
      ...pads,
      cutout,
      // silkscreenRefText,
      pin1Marker
    ],
    parameters
  };
};

// src/fn/mountedpcbmodule.ts
import { length as length77 } from "circuit-json";
import { z as z92 } from "zod";
var mountedpcbmodule_def = base_def.extend({
  fn: z92.string(),
  numPins: z92.number().optional().default(0),
  rows: z92.union([z92.string(), z92.number()]).transform((val) => Number(val)).optional().default(1).describe("number of rows"),
  p: length77.default("2.54mm").describe("pitch"),
  id: length77.default("1.0mm").describe("inner diameter"),
  od: length77.default("1.5mm").describe("outer diameter"),
  male: z92.boolean().optional().describe("the module uses male headers"),
  nopin: z92.boolean().optional().default(false).describe("omit pins rendering"),
  female: z92.boolean().optional().describe("the module uses female headers"),
  smd: z92.boolean().optional().describe("surface mount device"),
  pinlabeltextalignleft: z92.boolean().optional().default(false),
  pinlabeltextaligncenter: z92.boolean().optional().default(false),
  pinlabeltextalignright: z92.boolean().optional().default(false),
  pinlabelverticallyinverted: z92.boolean().optional().default(false),
  pinlabelorthogonal: z92.boolean().optional().default(false),
  nopinlabels: z92.boolean().optional().default(false).describe("omit silkscreen pin labels"),
  doublesidedpinlabel: z92.boolean().optional().default(false).describe("add silkscreen pins in top and bottom layers"),
  bottomsidepinlabel: z92.boolean().optional().default(false).describe(
    "place the silkscreen reference text on the bottom layer instead of top"
  ),
  pinRowSide: z92.enum(["left", "right", "top", "bottom"]).optional().default("left"),
  pinrowleft: z92.boolean().optional().default(false),
  pinrowright: z92.boolean().optional().default(false),
  pinrowtop: z92.boolean().optional().default(false),
  pinrowbottom: z92.boolean().optional().default(false),
  pinrowleftpins: z92.union([z92.string(), z92.number()]).transform((val) => Number(val)).optional(),
  pinrowrightpins: z92.union([z92.string(), z92.number()]).transform((val) => Number(val)).optional(),
  pinrowtoppins: z92.union([z92.string(), z92.number()]).transform((val) => Number(val)).optional(),
  pinrowbottompins: z92.union([z92.string(), z92.number()]).transform((val) => Number(val)).optional(),
  width: length77.optional(),
  height: length77.optional(),
  pinRowHoleEdgeToEdgeDist: length77.default("2mm"),
  holes: z92.union([z92.string(), z92.array(z92.string())]).optional().transform((val) => {
    if (!val) return val;
    if (Array.isArray(val)) return val;
    if (val.startsWith("(") && val.endsWith(")")) {
      return val.slice(1, -1).split(",").map((s) => s.trim());
    }
    return [val];
  }),
  holeXDist: length77.optional(),
  holeYDist: length77.optional(),
  holeInset: length77.default("1mm"),
  pinrow: z92.union([z92.string(), z92.number()]).optional(),
  usbposition: z92.enum(["left", "right", "top", "bottom"]).optional().default("left"),
  usbleft: z92.boolean().optional().default(false),
  usbtop: z92.boolean().optional().default(false),
  usbright: z92.boolean().optional().default(false),
  usbbottom: z92.boolean().optional().default(false),
  usbtype: z92.enum(["micro", "c"]).optional(),
  usbmicro: z92.boolean().optional().default(false),
  usbc: z92.boolean().optional().default(false),
  screen: z92.boolean().optional().default(false).describe("add silkscreen outline for screen/display area"),
  screenwidth: length77.optional(),
  screenheight: length77.optional(),
  screencenteroffsetx: length77.optional(),
  screencenteroffsety: length77.optional()
}).transform((data) => {
  const pinlabelAnchorSide = determinePinlabelAnchorSide(data);
  let pinRowSide = data.pinRowSide;
  if (data.pinrowleft) pinRowSide = "left";
  if (data.pinrowright) pinRowSide = "right";
  if (data.pinrowtop) pinRowSide = "top";
  if (data.pinrowbottom) pinRowSide = "bottom";
  let usbposition = data.usbposition;
  if (data.usbleft) usbposition = "left";
  if (data.usbtop) usbposition = "top";
  if (data.usbright) usbposition = "right";
  if (data.usbbottom) usbposition = "bottom";
  let usbtype = data.usbtype;
  if (data.usbmicro) usbtype = "micro";
  if (data.usbc) usbtype = "c";
  if (data.pinrow !== void 0) {
    data.numPins = Number(data.pinrow);
  }
  const sidePinCounts = {
    left: data.pinrowleftpins,
    right: data.pinrowrightpins,
    top: data.pinrowtoppins,
    bottom: data.pinrowbottompins
  };
  const hasSidePins = Object.values(sidePinCounts).some(
    (value) => value !== void 0 && value > 0
  );
  const leftRightBoth = (sidePinCounts.left ?? 0) > 0 && (sidePinCounts.right ?? 0) > 0;
  const topBottomBoth = (sidePinCounts.top ?? 0) > 0 && (sidePinCounts.bottom ?? 0) > 0;
  if (hasSidePins) {
    if ((sidePinCounts.left ?? 0) > 0) pinRowSide = "left";
    else if ((sidePinCounts.right ?? 0) > 0) pinRowSide = "right";
    else if ((sidePinCounts.top ?? 0) > 0) pinRowSide = "top";
    else if ((sidePinCounts.bottom ?? 0) > 0) pinRowSide = "bottom";
    data.numPins = (sidePinCounts.left ?? 0) + (sidePinCounts.right ?? 0) + (sidePinCounts.top ?? 0) + (sidePinCounts.bottom ?? 0);
  }
  const numPinsPerRow = Math.ceil(data.numPins / data.rows);
  const verticalPins = Math.max(
    sidePinCounts.left ?? 0,
    sidePinCounts.right ?? 0
  );
  const horizontalPins = Math.max(
    sidePinCounts.top ?? 0,
    sidePinCounts.bottom ?? 0
  );
  let calculatedWidth;
  let calculatedHeight;
  if (hasSidePins) {
    const widthGap = leftRightBoth ? data.p : 0;
    const heightGap = topBottomBoth ? data.p : 0;
    calculatedWidth = (horizontalPins > 0 ? (horizontalPins - 1) * data.p : 0) + 2 * data.pinRowHoleEdgeToEdgeDist + widthGap;
    calculatedHeight = (verticalPins > 0 ? (verticalPins - 1) * data.p : 0) + 2 * data.pinRowHoleEdgeToEdgeDist + heightGap;
  } else if (pinRowSide === "left" || pinRowSide === "right") {
    calculatedWidth = (data.rows - 1) * data.p + 2 * data.pinRowHoleEdgeToEdgeDist;
    calculatedHeight = (numPinsPerRow - 1) * data.p + 2 * data.pinRowHoleEdgeToEdgeDist;
  } else {
    calculatedHeight = (data.rows - 1) * data.p + 2 * data.pinRowHoleEdgeToEdgeDist;
    calculatedWidth = (numPinsPerRow - 1) * data.p + 2 * data.pinRowHoleEdgeToEdgeDist;
  }
  if (data.numPins === 0) {
    calculatedWidth = 10;
    calculatedHeight = 10;
  }
  const finalWidth = data.width ?? calculatedWidth;
  const finalHeight = data.height ?? calculatedHeight;
  const screenWidth = data.screenwidth ?? finalWidth * 0.95;
  const screenHeight = data.screenheight ?? finalHeight * 0.95;
  const screenCenterOffsetX = data.screencenteroffsetx ?? 0;
  const screenCenterOffsetY = data.screencenteroffsety ?? 0;
  return {
    ...data,
    pinlabelAnchorSide,
    pinRowSide,
    usbposition,
    usbtype,
    male: data.male ?? !data.female,
    female: data.female ?? false,
    width: finalWidth,
    height: finalHeight,
    screenwidth: screenWidth,
    screenheight: screenHeight,
    screencenteroffsetx: screenCenterOffsetX,
    screencenteroffsety: screenCenterOffsetY,
    pinrowleftpins: sidePinCounts.left,
    pinrowrightpins: sidePinCounts.right,
    pinrowtoppins: sidePinCounts.top,
    pinrowbottompins: sidePinCounts.bottom
  };
}).superRefine((data, ctx) => {
  if (data.male && data.female) {
    ctx.addIssue({
      code: z92.ZodIssueCode.custom,
      message: "'male' and 'female' cannot both be true; it should be male or female.",
      path: ["male", "female"]
    });
  }
});
var mountedpcbmodule = (raw_params) => {
  const parameters = mountedpcbmodule_def.parse(raw_params);
  const {
    p,
    id,
    od,
    rows,
    numPins,
    pinlabelAnchorSide,
    pinlabelverticallyinverted,
    pinlabelorthogonal,
    pinlabeltextalignleft,
    pinlabeltextalignright,
    nopinlabels,
    doublesidedpinlabel,
    bottomsidepinlabel,
    pinRowSide,
    width,
    height,
    pinRowHoleEdgeToEdgeDist,
    holes,
    holeXDist,
    holeYDist,
    holeInset,
    pinrowleftpins,
    pinrowrightpins,
    pinrowtoppins,
    pinrowbottompins,
    usbposition,
    usbtype,
    screen,
    screenwidth,
    screenheight,
    screencenteroffsetx,
    screencenteroffsety
  } = parameters;
  let pinlabelTextAlign = "center";
  if (pinlabeltextalignleft) pinlabelTextAlign = "left";
  else if (pinlabeltextalignright) pinlabelTextAlign = "right";
  const elements = [];
  const sidePinCounts = {
    left: pinrowleftpins,
    right: pinrowrightpins,
    top: pinrowtoppins,
    bottom: pinrowbottompins
  };
  const hasSidePins = Object.values(sidePinCounts).some(
    (value) => value !== void 0 && value > 0
  );
  const addPin = (pinNumber, xoff, yoff, anchorSide) => {
    if (parameters.smd) {
      elements.push(
        rectpad(pinNumber, xoff, yoff, parameters.od, parameters.od)
      );
    } else if (pinNumber === 1) {
      elements.push(
        platedHoleWithRectPad({
          pn: pinNumber,
          x: xoff,
          y: yoff,
          holeDiameter: id,
          rectPadWidth: od,
          rectPadHeight: od
        })
      );
    } else {
      elements.push(platedhole(pinNumber, xoff, yoff, id, od));
    }
    if (!nopinlabels) {
      const anchor_x = xoff + (anchorSide === "left" ? -od : anchorSide === "right" ? od : 0);
      const anchor_y = yoff + (anchorSide === "top" ? od : anchorSide === "bottom" ? -od : 0);
      if (!bottomsidepinlabel) {
        elements.push(
          silkscreenPin({
            fs: od / 5,
            pn: pinNumber,
            anchor_x,
            anchor_y,
            anchorplacement: anchorSide,
            textalign: pinlabelTextAlign,
            orthogonal: pinlabelorthogonal,
            verticallyinverted: pinlabelverticallyinverted,
            layer: "top"
          })
        );
      }
      if (doublesidedpinlabel || bottomsidepinlabel) {
        elements.push(
          silkscreenPin({
            fs: od / 5,
            pn: pinNumber,
            anchor_x,
            anchor_y,
            anchorplacement: anchorSide,
            textalign: pinlabelTextAlign,
            orthogonal: pinlabelorthogonal,
            verticallyinverted: pinlabelverticallyinverted,
            layer: "bottom"
          })
        );
      }
    }
  };
  if (hasSidePins) {
    const pinSpacing = p;
    let pinNumber = 1;
    const leftCount = sidePinCounts.left ?? 0;
    const rightCount = sidePinCounts.right ?? 0;
    const topCount = sidePinCounts.top ?? 0;
    const bottomCount = sidePinCounts.bottom ?? 0;
    const addSidePins = (side, count) => {
      if (count <= 0) return;
      if (side === "left" || side === "right") {
        const xoff = side === "left" ? -width / 2 + pinRowHoleEdgeToEdgeDist : width / 2 - pinRowHoleEdgeToEdgeDist;
        const startY = (count - 1) / 2 * pinSpacing;
        for (let i = 0; i < count; i++) {
          addPin(pinNumber, xoff, startY - i * pinSpacing, side);
          pinNumber++;
        }
      } else {
        const yoff = side === "top" ? height / 2 - pinRowHoleEdgeToEdgeDist : -height / 2 + pinRowHoleEdgeToEdgeDist;
        const startX = -((count - 1) / 2) * pinSpacing;
        for (let i = 0; i < count; i++) {
          addPin(pinNumber, startX + i * pinSpacing, yoff, side);
          pinNumber++;
        }
      }
    };
    addSidePins("left", leftCount);
    addSidePins("right", rightCount);
    addSidePins("top", topCount);
    addSidePins("bottom", bottomCount);
  } else {
    const pinSpacing = p;
    let pinStartX = 0;
    let pinStartY = 0;
    let pinDirectionX = 0;
    let pinDirectionY = 0;
    let rowDirectionX = 0;
    let rowDirectionY = 0;
    const numPinsPerRow = Math.ceil(numPins / rows);
    if (pinRowSide === "left" || pinRowSide === "right") {
      pinStartX = pinRowSide === "left" ? -width / 2 + pinRowHoleEdgeToEdgeDist : width / 2 - pinRowHoleEdgeToEdgeDist;
      pinStartY = (numPinsPerRow - 1) / 2 * pinSpacing;
      pinDirectionX = 0;
      pinDirectionY = -pinSpacing;
      rowDirectionX = pinRowSide === "left" ? pinSpacing : -pinSpacing;
      rowDirectionY = 0;
    } else {
      pinStartX = -(numPinsPerRow - 1) / 2 * pinSpacing;
      pinStartY = pinRowSide === "top" ? height / 2 - pinRowHoleEdgeToEdgeDist : -height / 2 + pinRowHoleEdgeToEdgeDist;
      pinDirectionX = pinSpacing;
      pinDirectionY = 0;
      rowDirectionX = 0;
      rowDirectionY = pinRowSide === "top" ? -pinSpacing : pinSpacing;
    }
    let pinNumber = 1;
    for (let row = 0; row < rows && pinNumber <= numPins; row++) {
      for (let col = 0; col < numPinsPerRow && pinNumber <= numPins; col++) {
        const xoff = pinStartX + col * pinDirectionX + row * rowDirectionX;
        const yoff = pinStartY + col * pinDirectionY + row * rowDirectionY;
        if (parameters.smd) {
          elements.push(
            rectpad(pinNumber, xoff, yoff, parameters.od, parameters.od)
          );
        } else {
          if (pinNumber === 1) {
            elements.push(
              platedHoleWithRectPad({
                pn: pinNumber,
                x: xoff,
                y: yoff,
                holeDiameter: id,
                rectPadWidth: od,
                rectPadHeight: od
              })
            );
          } else {
            elements.push(platedhole(pinNumber, xoff, yoff, id, od));
          }
        }
        if (!nopinlabels) {
          const anchor_x = xoff + (pinRowSide === "left" ? -od : pinRowSide === "right" ? od : 0);
          const anchor_y = yoff + (pinRowSide === "top" ? od : pinRowSide === "bottom" ? -od : 0);
          if (!bottomsidepinlabel) {
            elements.push(
              silkscreenPin({
                fs: od / 5,
                pn: pinNumber,
                anchor_x,
                anchor_y,
                anchorplacement: pinlabelAnchorSide,
                textalign: pinlabelTextAlign,
                orthogonal: pinlabelorthogonal,
                verticallyinverted: pinlabelverticallyinverted,
                layer: "top"
              })
            );
          }
          if (doublesidedpinlabel || bottomsidepinlabel) {
            elements.push(
              silkscreenPin({
                fs: od / 5,
                pn: pinNumber,
                anchor_x,
                anchor_y,
                anchorplacement: pinlabelAnchorSide,
                textalign: pinlabelTextAlign,
                orthogonal: pinlabelorthogonal,
                verticallyinverted: pinlabelverticallyinverted,
                layer: "bottom"
              })
            );
          }
        }
        pinNumber++;
      }
    }
  }
  if (holes) {
    for (const pos of holes) {
      let hx = 0;
      let hy = 0;
      if (pos === "topleft") {
        hx = -width / 2 + holeInset;
        hy = height / 2 - holeInset;
      } else if (pos === "topright") {
        hx = width / 2 - holeInset;
        hy = height / 2 - holeInset;
      } else if (pos === "bottomleft") {
        hx = -width / 2 + holeInset;
        hy = -height / 2 + holeInset;
      } else if (pos === "bottomright") {
        hx = width / 2 - holeInset;
        hy = -height / 2 + holeInset;
      } else if (pos === "center") {
        hx = 0;
        hy = 0;
      }
      if (holeXDist !== void 0) hx += holeXDist;
      if (holeYDist !== void 0) hy += holeYDist;
      elements.push(
        platedhole(numPins + holes.indexOf(pos) + 1, hx, hy, id, od)
      );
    }
  }
  if (!parameters.nosilkscreen) {
    const outline = [
      { x: -width / 2, y: -height / 2 },
      { x: width / 2, y: -height / 2 },
      { x: width / 2, y: height / 2 },
      { x: -width / 2, y: height / 2 },
      { x: -width / 2, y: -height / 2 }
    ];
    elements.push(silkscreenpath(outline, { stroke_width: 0.1, layer: "top" }));
  }
  const refText = silkscreenRef(0, height / 2 + 1, 0.5);
  elements.push(refText);
  if (usbposition && usbtype) {
    let usbRectWidth;
    let usbRectHeight;
    if (usbtype === "c") {
      usbRectWidth = 8;
      usbRectHeight = 3;
    } else if (usbtype === "micro") {
      usbRectWidth = 6;
      usbRectHeight = 2;
    } else {
      return {
        circuitJson: elements,
        parameters
      };
    }
    let usbRect;
    if (usbposition === "left") {
      usbRect = [
        { x: -width / 2, y: -usbRectWidth / 2 },
        { x: -width / 2 + usbRectHeight, y: -usbRectWidth / 2 },
        { x: -width / 2 + usbRectHeight, y: usbRectWidth / 2 },
        { x: -width / 2, y: usbRectWidth / 2 },
        { x: -width / 2, y: -usbRectWidth / 2 }
      ];
    } else if (usbposition === "right") {
      usbRect = [
        { x: width / 2 - usbRectHeight, y: -usbRectWidth / 2 },
        { x: width / 2, y: -usbRectWidth / 2 },
        { x: width / 2, y: usbRectWidth / 2 },
        { x: width / 2 - usbRectHeight, y: usbRectWidth / 2 },
        { x: width / 2 - usbRectHeight, y: -usbRectWidth / 2 }
      ];
    } else if (usbposition === "top") {
      usbRect = [
        { x: -usbRectWidth / 2, y: height / 2 - usbRectHeight },
        { x: usbRectWidth / 2, y: height / 2 - usbRectHeight },
        { x: usbRectWidth / 2, y: height / 2 },
        { x: -usbRectWidth / 2, y: height / 2 },
        { x: -usbRectWidth / 2, y: height / 2 - usbRectHeight }
      ];
    } else if (usbposition === "bottom") {
      usbRect = [
        { x: -usbRectWidth / 2, y: -height / 2 },
        { x: usbRectWidth / 2, y: -height / 2 },
        { x: usbRectWidth / 2, y: -height / 2 + usbRectHeight },
        { x: -usbRectWidth / 2, y: -height / 2 + usbRectHeight },
        { x: -usbRectWidth / 2, y: -height / 2 }
      ];
    } else {
      return {
        circuitJson: elements,
        parameters
      };
    }
    elements.push(silkscreenpath(usbRect, { stroke_width: 0.1, layer: "top" }));
  }
  if (screen) {
    const halfScreenWidth = screenwidth / 2;
    const halfScreenHeight = screenheight / 2;
    const centerX = screencenteroffsetx;
    const centerY = screencenteroffsety;
    const screenOutline = [
      { x: -halfScreenWidth + centerX, y: -halfScreenHeight + centerY },
      { x: halfScreenWidth + centerX, y: -halfScreenHeight + centerY },
      { x: halfScreenWidth + centerX, y: halfScreenHeight + centerY },
      { x: -halfScreenWidth + centerX, y: halfScreenHeight + centerY },
      { x: -halfScreenWidth + centerX, y: -halfScreenHeight + centerY }
    ];
    elements.push(
      silkscreenpath(screenOutline, { stroke_width: 0.05, layer: "top" })
    );
  }
  return {
    circuitJson: elements,
    parameters
  };
};

// src/fn/to92l.ts
import { z as z93 } from "zod";
var to92lCourtyardOutline = [
  { x: -1.45, y: 2.75 },
  { x: -1.45, y: -1.85 },
  { x: 4.05, y: -1.85 },
  { x: 4.05, y: 2.75 }
];
var to92lInlineCourtyardOutline = [
  { x: -1.55, y: 2.75 },
  { x: -1.55, y: -1.85 },
  { x: 3.95, y: -1.85 },
  { x: 3.95, y: 2.75 }
];
var to92l_def = base_def.extend({
  fn: z93.string(),
  num_pins: z93.number().default(3),
  inline: z93.boolean().default(false),
  p: z93.string().default("1.27mm"),
  id: z93.string().default("0.75mm"),
  od: z93.string().default("1.3mm"),
  w: z93.string().default("4.8mm"),
  h: z93.string().default("4.0mm")
});
var to92l = (raw_params) => {
  const parameters = to92l_def.parse(raw_params);
  const p = Number.parseFloat(parameters.p);
  const w = Number.parseFloat(parameters.w);
  const h = Number.parseFloat(parameters.h);
  const od = parameters.inline ? 1.05 : Number.parseFloat(parameters.od);
  const padH = parameters.inline ? 1.5 : od;
  const holes = [
    platedHoleWithRectPad({
      pn: 1,
      x: 0,
      y: 0,
      holeDiameter: parameters.id,
      rectPadWidth: od,
      rectPadHeight: padH
    }),
    parameters.inline ? platedHolePill(2, p, 0, Number.parseFloat(parameters.id), od, padH) : platedhole(2, p, p, parameters.id, od),
    parameters.inline ? platedHolePill(3, p * 2, 0, Number.parseFloat(parameters.id), od, padH) : platedhole(3, p * 2, 0, parameters.id, od)
  ];
  const radius = w / 2;
  const cx = parameters.inline ? p - 0.09 : p;
  const cy = 0.2;
  const y_bottom = cy + radius - h;
  const semicircle = Array.from({ length: 32 }, (_, i) => {
    const angle = Math.PI * i / 31;
    return {
      x: cx + radius * Math.cos(angle),
      y: cy + radius * Math.sin(angle)
    };
  });
  const silkBody = {
    type: "pcb_silkscreen_path",
    layer: "top",
    pcb_component_id: "",
    pcb_silkscreen_path_id: "",
    stroke_width: 0.12,
    route: [
      ...semicircle,
      { x: cx - radius, y: y_bottom },
      { x: cx + radius, y: y_bottom },
      semicircle[0]
    ]
  };
  const silkscreenRefText = silkscreenRef(
    cx,
    cy + radius + 1,
    0.5
  );
  const courtyard = {
    type: "pcb_courtyard_outline",
    pcb_courtyard_outline_id: "",
    pcb_component_id: "",
    outline: parameters.inline ? to92lInlineCourtyardOutline : to92lCourtyardOutline,
    layer: "top"
  };
  return {
    circuitJson: [
      ...holes,
      silkBody,
      silkscreenRefText,
      courtyard
    ],
    parameters
  };
};

// src/fn/usbcmidmount.ts
import {
  length as length78
} from "circuit-json";
import { z as z94 } from "zod";
var usbcmidmount_def = base_def.extend({
  fn: z94.literal("usbcmidmount"),
  num_pins: z94.literal(16).default(16),
  pinstart: z94.coerce.number().int().positive().default(1),
  split: z94.boolean().default(false).describe("emit all 16 contact lands"),
  reverse: z94.boolean().default(false).describe("number contact lands right-to-left"),
  noholes: z94.boolean().default(false).describe("omit the two locator holes"),
  rowy: length78.default("2.125mm").describe("contact pad row y position"),
  ph: length78.default("1.1mm").describe("contact pad height"),
  pw: length78.default("0.3mm").describe("signal contact pad width"),
  powerpw: length78.default("0.55mm").describe("merged power pad width"),
  powerx: length78.default("3.2mm").describe("outer merged power pad x"),
  shellx: length78.default("4.325mm").describe("shell tab x position"),
  topy: length78.default("1.575mm").describe("upper shell tab y position"),
  bottomy: length78.default("2.625mm").describe("lower shell tab negative y magnitude"),
  tophw: length78.default("0.6mm").describe("upper shell slot width"),
  tophh: length78.default("1.5mm").describe("upper shell slot height"),
  topring: length78.default("0.25mm").describe("upper shell annular ring"),
  bottomhw: length78.default("0.6mm").describe("lower shell slot width"),
  bottomhh: length78.default("1.2mm").describe("lower shell slot height"),
  bottomring: length78.default("0.3mm").describe("lower shell annular ring"),
  holex: length78.default("2.89mm").describe("locator hole x position"),
  holey: length78.default("1.055mm").describe("locator hole y position"),
  holed: length78.default("0.7mm").describe("locator hole diameter"),
  bodybottom: length78.default("5.225mm").describe("connector body negative y extent")
});
var pillPlatedHole = ({
  pin,
  x,
  y,
  holeWidth,
  holeHeight,
  outerWidth,
  outerHeight
}) => ({
  type: "pcb_plated_hole",
  shape: "pill",
  pcb_plated_hole_id: "",
  pcb_component_id: "",
  pcb_port_id: "",
  x,
  y,
  hole_width: holeWidth,
  hole_height: holeHeight,
  outer_width: outerWidth,
  outer_height: outerHeight,
  ccw_rotation: 0,
  layers: ["top", "bottom"],
  port_hints: [pin.toString()]
});
var usbcmidmount = (rawParams) => {
  const parameters = usbcmidmount_def.parse(rawParams);
  const {
    pinstart: pinStart,
    split,
    reverse,
    noholes,
    rowy,
    ph,
    pw,
    powerpw,
    powerx,
    shellx,
    topy,
    bottomy,
    tophw,
    tophh,
    topring,
    bottomhw,
    bottomhh,
    bottomring,
    holex,
    holey,
    holed,
    bodybottom
  } = parameters;
  const locatorHoles = noholes ? [] : [-holex, holex].map((x) => ({
    type: "pcb_hole",
    pcb_hole_id: "",
    pcb_component_id: "",
    hole_shape: "circle",
    hole_diameter: holed,
    x,
    y: holey
  }));
  const shellTabs = [
    pillPlatedHole({
      pin: pinStart,
      x: -shellx,
      y: topy,
      holeWidth: tophw,
      holeHeight: tophh,
      outerWidth: tophw + 2 * topring,
      outerHeight: tophh + 2 * topring
    }),
    pillPlatedHole({
      pin: pinStart + 1,
      x: shellx,
      y: topy,
      holeWidth: tophw,
      holeHeight: tophh,
      outerWidth: tophw + 2 * topring,
      outerHeight: tophh + 2 * topring
    }),
    pillPlatedHole({
      pin: pinStart + 2,
      x: -shellx,
      y: -bottomy,
      holeWidth: bottomhw,
      holeHeight: bottomhh,
      outerWidth: bottomhw + 2 * bottomring,
      outerHeight: bottomhh + 2 * bottomring
    }),
    pillPlatedHole({
      pin: pinStart + 3,
      x: shellx,
      y: -bottomy,
      holeWidth: bottomhw,
      holeHeight: bottomhh,
      outerWidth: bottomhw + 2 * bottomring,
      outerHeight: bottomhh + 2 * bottomring
    })
  ];
  const innerPowerX = Number((powerx - 0.8).toFixed(12));
  const mergedSignalXs = [
    -powerx,
    -innerPowerX,
    -1.75,
    -1.25,
    -0.75,
    -0.25,
    0.25,
    0.75,
    1.25,
    1.75,
    innerPowerX,
    powerx
  ];
  const splitSignalXs = [
    -3.35,
    -3.05,
    -2.55,
    -2.25,
    -1.75,
    -1.25,
    -0.75,
    -0.25,
    0.25,
    0.75,
    1.25,
    1.75,
    2.25,
    2.55,
    3.05,
    3.35
  ];
  const leftToRightSignalXs = split ? splitSignalXs : mergedSignalXs;
  const signalXs = reverse ? leftToRightSignalXs.slice().reverse() : leftToRightSignalXs;
  const signalWidths = split ? signalXs.map(() => pw) : signalXs.map(
    (_, index) => index < 2 || index >= signalXs.length - 2 ? powerpw : pw
  );
  const signalPads = signalXs.map(
    (x, index) => rectpad(pinStart + 4 + index, x, rowy, signalWidths[index], ph)
  );
  const silkX = Math.min(shellx - Math.max(tophw, bottomhw) / 2 - 0.2, 4.5);
  const silkscreen = [
    silkscreenpath([
      { x: -silkX, y: -bottomy + bottomhh / 2 + bottomring + 0.2 },
      { x: -silkX, y: topy - tophh / 2 - topring - 0.2 }
    ]),
    silkscreenpath([
      { x: silkX, y: -bottomy + bottomhh / 2 + bottomring + 0.2 },
      { x: silkX, y: topy - tophh / 2 - topring - 0.2 }
    ]),
    silkscreenpath([
      { x: -silkX, y: -bodybottom },
      { x: silkX, y: -bodybottom }
    ])
  ];
  const topCopper = Math.max(rowy + ph / 2, topy + tophh / 2 + topring);
  const bottomCopper = Math.min(
    -bodybottom,
    -bottomy - bottomhh / 2 - bottomring
  );
  const courtyardLeft = -shellx - Math.max(tophw + 2 * topring, bottomhw + 2 * bottomring) / 2 - 0.25;
  const courtyardRight = -courtyardLeft;
  const courtyardTop = topCopper + 0.25;
  const courtyardBottom = bottomCopper - 0.25;
  const ref = silkscreenRef(0, courtyardTop + 0.75, 0.5);
  const courtyard = {
    type: "pcb_courtyard_rect",
    pcb_courtyard_rect_id: "",
    pcb_component_id: "",
    center: { x: 0, y: (courtyardTop + courtyardBottom) / 2 },
    width: courtyardRight - courtyardLeft,
    height: courtyardTop - courtyardBottom,
    layer: "top"
  };
  return {
    circuitJson: [
      ...locatorHoles,
      ...shellTabs,
      ...signalPads,
      ...silkscreen,
      ref,
      courtyard
    ],
    parameters
  };
};

// src/fn/lcc.ts
var lcc_def = base_quad_def.extend({}).transform(quadTransform);
var lcc = (parameters) => {
  parameters.legsoutside = false;
  if (!parameters.pl) {
    parameters.pl = 1;
  }
  if (!parameters.pw) {
    parameters.pw = 0.6;
  }
  return quad(parameters);
};

// src/helpers/apply-norefdes.ts
var applyNoRefDes = (elements, parameters) => {
  const refs = elements.filter(
    (el) => el.type === "pcb_silkscreen_text"
  );
  if (refs.length === 0) return elements;
  for (const ref of refs) {
    if (parameters.norefdes) {
      ref.text = "";
    }
  }
  return elements;
};

// src/helpers/apply-nosilkscreen.ts
var applyNoSilkscreen = (elements, parameters) => {
  if (!parameters.nosilkscreen) return elements;
  return elements.filter((element) => {
    return element.type !== "pcb_silkscreen_path" && element.type !== "pcb_silkscreen_text";
  });
};

// src/helpers/apply-origin.ts
var applyOrigin = (elements, origin) => {
  if (!origin) return elements;
  const pads = elements.filter(
    (el) => el.type === "pcb_smtpad" || el.type === "pcb_plated_hole"
  );
  if (pads.length === 0) return elements;
  let minX = Number.POSITIVE_INFINITY;
  let maxX = Number.NEGATIVE_INFINITY;
  let minY = Number.POSITIVE_INFINITY;
  let maxY = Number.NEGATIVE_INFINITY;
  const updateBounds = (x, y, w = 0, h = 0) => {
    const left = x - w / 2;
    const right = x + w / 2;
    const bottom = y - h / 2;
    const top = y + h / 2;
    minX = Math.min(minX, left);
    maxX = Math.max(maxX, right);
    minY = Math.min(minY, bottom);
    maxY = Math.max(maxY, top);
  };
  for (const pad2 of pads) {
    if (pad2.type === "pcb_smtpad") {
      const w = pad2.shape === "circle" ? pad2.radius * 2 : pad2.width;
      const h = pad2.shape === "circle" ? pad2.radius * 2 : pad2.height;
      updateBounds(pad2.x, pad2.y, w, h);
    } else if (pad2.type === "pcb_plated_hole") {
      const d = pad2.outer_diameter ?? pad2.hole_diameter;
      updateBounds(pad2.x, pad2.y, d, d);
    }
  }
  let dx = 0;
  let dy = 0;
  switch (origin) {
    case "center":
      dx = (minX + maxX) / 2;
      dy = (minY + maxY) / 2;
      break;
    case "bottomleft":
      dx = minX;
      dy = minY;
      break;
    case "bottomcenter":
    case "centerbottom":
      dx = (minX + maxX) / 2;
      dy = minY;
      break;
    case "topcenter":
    case "centertop":
      dx = (minX + maxX) / 2;
      dy = maxY;
      break;
    case "leftcenter":
    case "centerleft":
      dx = minX;
      dy = (minY + maxY) / 2;
      break;
    case "rightcenter":
    case "centerright":
      dx = maxX;
      dy = (minY + maxY) / 2;
      break;
    case "pin1": {
      const pin1 = pads.find((p) => p.port_hints?.[0] === "1") || pads[0];
      dx = pin1.x;
      dy = pin1.y;
      break;
    }
  }
  if (dx === 0 && dy === 0) return elements;
  for (const el of elements) {
    if (typeof el.x === "number") el.x -= dx;
    if (typeof el.y === "number") el.y -= dy;
    if (el.center && typeof el.center.x === "number") {
      el.center.x -= dx;
      el.center.y -= dy;
    }
    if (el.type === "pcb_silkscreen_path") {
      for (const pt of el.route) {
        pt.x -= dx;
        pt.y -= dy;
      }
    }
    if (el.type === "pcb_silkscreen_text" && el.anchor_position) {
      el.anchor_position.x -= dx;
      el.anchor_position.y -= dy;
    }
  }
  return elements;
};

// src/helpers/apply-pin1-location.ts
var RIGHT_ANGLE_ROTATIONS = [0, 90, 180, 270];
var rotatePoint = (point, rotation2) => {
  switch (rotation2) {
    case 90:
      return { x: -point.y, y: point.x };
    case 180:
      return { x: -point.x, y: -point.y };
    case 270:
      return { x: point.y, y: -point.x };
    default:
      return { ...point };
  }
};
var getPadCenter = (pad2) => {
  if (typeof pad2.x === "number" && typeof pad2.y === "number") {
    return { x: pad2.x, y: pad2.y };
  }
  if (Array.isArray(pad2.points) && pad2.points.length > 0) {
    const xs = pad2.points.map((point) => point.x);
    const ys = pad2.points.map((point) => point.y);
    return {
      x: (Math.min(...xs) + Math.max(...xs)) / 2,
      y: (Math.min(...ys) + Math.max(...ys)) / 2
    };
  }
  return null;
};
var isPin1 = (pad2) => pad2.port_hints?.some((hint) => /^(?:pin)?1$/i.test(String(hint)));
var pinMatchesLocation = (padCenters, pin1Center, [side, alignment]) => {
  const xs = padCenters.map((point) => point.x);
  const ys = padCenters.map((point) => point.y);
  const minX = Math.min(...xs);
  const maxX = Math.max(...xs);
  const minY = Math.min(...ys);
  const maxY = Math.max(...ys);
  const centerX = (minX + maxX) / 2;
  const centerY = (minY + maxY) / 2;
  const spanX = maxX - minX;
  const spanY = maxY - minY;
  const tolerance = Math.max(spanX, spanY, 1) * 1e-6;
  const onRequestedSide = side === "leftside" && Math.abs(pin1Center.x - minX) <= tolerance || side === "rightside" && Math.abs(pin1Center.x - maxX) <= tolerance || side === "topside" && Math.abs(pin1Center.y - maxY) <= tolerance || side === "bottomside" && Math.abs(pin1Center.y - minY) <= tolerance;
  const atRequestedAlignment = alignment === "left" && (spanX <= tolerance || pin1Center.x < centerX - tolerance) || alignment === "right" && (spanX <= tolerance || pin1Center.x > centerX + tolerance) || alignment === "top" && (spanY <= tolerance || pin1Center.y > centerY + tolerance) || alignment === "bottom" && (spanY <= tolerance || pin1Center.y < centerY - tolerance);
  return onRequestedSide && atRequestedAlignment;
};
var rotatePointField = (value, rotation2) => {
  if (!value || typeof value !== "object" || typeof value.x !== "number" || typeof value.y !== "number") {
    return;
  }
  const rotated = rotatePoint(value, rotation2);
  value.x = rotated.x;
  value.y = rotated.y;
};
var normalizeRotation = (rotation2) => (rotation2 % 360 + 360) % 360;
var rotateSmtPad = (pad2, rotation2) => {
  switch (pad2.shape) {
    case "rect":
      return {
        ...pad2,
        shape: "rotated_rect",
        ccw_rotation: normalizeRotation(rotation2)
      };
    case "pill":
      return {
        ...pad2,
        shape: "rotated_pill",
        ccw_rotation: normalizeRotation(rotation2)
      };
    case "rotated_rect":
    case "rotated_pill":
      return {
        ...pad2,
        ccw_rotation: normalizeRotation(pad2.ccw_rotation + rotation2)
      };
    default:
      return pad2;
  }
};
var rotateElements = (elements, rotation2) => {
  if (rotation2 === 0) return elements;
  return elements.map((sourceElement) => {
    const element = structuredClone(sourceElement);
    if (typeof element.x === "number" && typeof element.y === "number") {
      const rotated = rotatePoint(element, rotation2);
      element.x = rotated.x;
      element.y = rotated.y;
    }
    rotatePointField(element.center, rotation2);
    rotatePointField(element.anchor_position, rotation2);
    for (const field of ["route", "outline", "points"]) {
      if (!Array.isArray(element[field])) continue;
      for (const point of element[field]) {
        rotatePointField(point, rotation2);
      }
    }
    if (element.type === "pcb_smtpad")
      return rotateSmtPad(element, rotation2);
    if (element.type === "pcb_plated_hole" || element.type === "pcb_silkscreen_text" || element.type === "pcb_fabrication_note_text") {
      element.ccw_rotation = normalizeRotation(
        Number(element.ccw_rotation ?? 0) + rotation2
      );
    }
    if ((rotation2 === 90 || rotation2 === 270) && (element.type === "pcb_courtyard_rect" || element.type === "pcb_silkscreen_rect" || element.type === "pcb_fabrication_note_rect" || element.type === "pcb_cutout" && element.shape === "rect")) {
      ;
      [element.width, element.height] = [element.height, element.width];
    }
    return element;
  });
};
var applyPin1Location = (elements, pin1Location) => {
  if (!pin1Location) return elements;
  const pads = elements.filter(
    (element) => element.type === "pcb_smtpad" || element.type === "pcb_plated_hole"
  );
  const pin1 = pads.find(isPin1);
  const padCenters = pads.map(getPadCenter).filter((point) => point !== null);
  const pin1Center = pin1 ? getPadCenter(pin1) : null;
  if (!pin1 || !pin1Center || padCenters.length === 0) {
    throw new Error(
      `pin1location(${pin1Location.join(",")}) requires a footprint with a pad whose port_hints contain "1" or "pin1"`
    );
  }
  const rotation2 = RIGHT_ANGLE_ROTATIONS.find((candidateRotation) => {
    const rotatedPads = padCenters.map(
      (point) => rotatePoint(point, candidateRotation)
    );
    const rotatedPin1 = rotatePoint(pin1Center, candidateRotation);
    return pinMatchesLocation(rotatedPads, rotatedPin1, pin1Location);
  });
  if (rotation2 === void 0) {
    throw new Error(
      `pin1location(${pin1Location.join(",")}) cannot be reached with a rotation; the requested location may be the mirrored orientation of this footprint`
    );
  }
  return rotateElements(elements, rotation2);
};

// src/helpers/apply-rounded.ts
import { mm as mm17 } from "@tscircuit/mm";
var clampRadius = (radius, width, height) => Math.min(radius, width / 2, height / 2);
var applyRounded = (elements, rounded) => {
  if (rounded === void 0) return elements;
  const radius = mm17(rounded);
  if (!Number.isFinite(radius) || radius < 0) {
    throw new Error(
      `rounded radius must be a non-negative length, got "${rounded}"`
    );
  }
  for (const element of elements) {
    if (element.type === "pcb_smtpad" && (element.shape === "rect" || element.shape === "rotated_rect")) {
      element.corner_radius = clampRadius(radius, element.width, element.height);
    }
    if (element.type === "pcb_plated_hole" && "rect_pad_width" in element && "rect_pad_height" in element) {
      element.rect_border_radius = clampRadius(
        radius,
        element.rect_pad_width,
        element.rect_pad_height
      );
    }
  }
  return elements;
};

// src/helpers/is-not-null.ts
function isNotNull(value) {
  return value !== null && value !== void 0;
}

// src/footprinter.ts
var normalizeDefinition = (def) => {
  return def.trim().replace(/^pinheader(?=[\d_]|$)/i, "pinrow").replace(/^d2pak(\d+)(?=_|$)/i, "d2pak_$1").replace(
    /^to-252(?:-(\d+))?(?=_|$)/i,
    (_, pins) => pins ? `to252_${pins}` : "to252"
  ).replace(
    /^to-263(?:-(\d+))?(?=_|$)/i,
    (_, pins) => pins ? `to263_${pins}` : "to263"
  ).replace(
    /^sot-23(?:-(\d+))?(?=_|$)/i,
    (_, pins) => pins ? `sot23_${pins}` : "sot23"
  ).replace(/^sot23-(\d+)(?=_|$)/i, "sot23_$1").replace(/^sot-223-(\d+)(?=_|$)/i, "sot223_$1").replace(/^to-220f-(\d+)(?=_|$)/i, "to220f_$1").replace(/^jst_(ph|sh|zh)_(\d+)(?=_|$)/i, "jst$2_$1");
};
var normalizeMicrometerLengths = (value) => value.replace(
  /([+-]?(?:\d+(?:\.\d*)?|\.\d+))\s*(?:um|µm)(?=$|[x,() ])/gi,
  (micrometerLength) => `${Number(length79.parse(micrometerLength).toPrecision(12))}mm`
);
var string2 = (def) => {
  let fp2 = footprinter();
  const normalizedDef = normalizeDefinition(def);
  const modifiedDef = normalizedDef.replace(/^((?:\d{4}|\d{5}))(?=$|_|x)/, "res$1").replace(/^zh(\d+)(?:$|_)/, "jst$1_zh");
  const def_parts = modifiedDef.split(/_(?!metric)/).map((s) => {
    const pin1LocationMatch = s.match(/^(pin1location)(\(.*\))$/i);
    if (pin1LocationMatch) {
      return {
        fn: pin1LocationMatch[1].toLowerCase(),
        v: pin1LocationMatch[2]
      };
    }
    const m = s.match(/([a-zA-Z]+)([\(\d\.\+\-\?].*)?/);
    if (!m) return null;
    const [, rawFn, v] = m;
    if (!rawFn) return null;
    const fn = rawFn.toLowerCase();
    if (v?.includes("?")) return null;
    return { fn, v };
  }).filter(isNotNull);
  for (const { fn, v } of def_parts) {
    fp2 = fp2[fn](v);
  }
  fp2.setString(normalizedDef);
  return fp2;
};
var getFootprintNames = () => {
  return Object.keys(fn_exports);
};
var getFootprintSizes = () => {
  return footprintSizes;
};
var getFootprintNamesByType = () => {
  const allFootprintNames = Object.keys(fn_exports);
  const passiveFootprintNames = allFootprintNames.filter((name) => {
    const fn = fn_exports[name];
    return fn.toString().includes("passive(");
  });
  return {
    passiveFootprintNames,
    normalFootprintNames: allFootprintNames.filter(
      (name) => !passiveFootprintNames.includes(name)
    )
  };
};
var footprinter = () => {
  const proxy = new Proxy(
    {},
    {
      get: (target, prop) => {
        if (prop === "soup" || prop === "circuitJson") {
          if ("fn" in target && fn_exports[target.fn]) {
            return () => {
              const { circuitJson } = fn_exports[target.fn](target);
              const circuitWithoutSilkscreen = applyNoSilkscreen(
                circuitJson,
                target
              );
              const circuitWithoutRefDes = applyNoRefDes(
                circuitWithoutSilkscreen,
                target
              );
              const circuitWithPin1Location = applyPin1Location(
                circuitWithoutRefDes,
                target.pin1location ? pin1_location.parse(target.pin1location) : void 0
              );
              const circuitWithRoundedPads = applyRounded(
                circuitWithPin1Location,
                target.rounded
              );
              return applyOrigin(circuitWithRoundedPads, target.origin);
            };
          }
          if (!fn_exports[target.fn]) {
            throw new Error(
              `Invalid footprint function, got "${target.fn}"${target.string ? `, from string "${target.string}"` : ""}`
            );
          }
          return () => {
            throw new Error(
              `No function found for footprinter, make sure to specify .dip, .lr, .p, etc. Got "${prop}"`
            );
          };
        }
        if (prop === "json") {
          if (!fn_exports[target.fn]) {
            throw new Error(
              `Invalid footprint function, got "${target.fn}"${target.string ? `, from string "${target.string}"` : ""}`
            );
          }
          return () => fn_exports[target.fn](target).parameters;
        }
        if (prop === "getFootprintNames") {
          return () => Object.keys(fn_exports);
        }
        if (prop === "params") {
          return () => target;
        }
        if (prop === "setString") {
          return (v) => {
            target.string = v;
            return proxy;
          };
        }
        return (...values) => {
          const v = values[0];
          const normalizedValue = typeof v === "string" ? normalizeMicrometerLengths(v) : v;
          if (Object.keys(target).length === 0) {
            if (`${prop}${v}` in fn_exports) {
              target[`${prop}${v}`] = true;
              target.fn = `${prop}${v}`;
            } else {
              target[prop] = true;
              target.fn = prop;
              if (prop === "res" || prop === "cap" || prop === "led" || prop === "diode") {
                if (v) {
                  if (typeof v === "string" && v.includes("_metric")) {
                    target.metric = v.split("_metric")[0];
                  } else {
                    target.imperial = v;
                  }
                }
              } else {
                target.num_pins = Number.isNaN(Number.parseFloat(v)) ? void 0 : Number.parseFloat(v);
              }
            }
          } else {
            if (!v && ["w", "h", "p"].includes(prop)) {
            } else {
              target[prop] = prop === "pin1location" && values.length > 1 ? values : normalizedValue ?? true;
            }
          }
          return proxy;
        };
      }
    }
  );
  return proxy;
};
footprinter.string = string2;
footprinter.getFootprintNames = getFootprintNames;
var fp = footprinter;
export {
  footprinter,
  fp,
  getFootprintNames,
  getFootprintNamesByType,
  getFootprintSizes,
  string2 as string
};
//# sourceMappingURL=index.js.map