import { GraphicsObject } from './types.js';
import { Matrix } from 'transformation-matrix';

type ScreenPoint = {
    x: number;
    y: number;
};
declare function getCanvasObjectLabelAtPoint(graphics: GraphicsObject, matrix: Matrix, screenPoint: ScreenPoint, options?: {
    hitSlop?: number;
}): string | null;

export { getCanvasObjectLabelAtPoint };
