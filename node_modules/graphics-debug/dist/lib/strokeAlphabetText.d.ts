import { NinePointAnchor } from './types.js';
import 'transformation-matrix';

interface StrokeAlphabetContext {
    beginPath(): void;
    moveTo(x: number, y: number): void;
    lineTo(x: number, y: number): void;
    stroke(): void;
    save(): void;
    restore(): void;
    translate(x: number, y: number): void;
    rotate(angleRadians: number): void;
    strokeStyle: string;
    lineWidth: number;
    lineCap?: string;
    lineJoin?: string;
}
type AlphabetLayout = {
    width: number;
    height: number;
    glyphWidth: number;
    letterSpacing: number;
    spaceWidth: number;
    strokeWidth: number;
    lineHeight: number;
    lines: string[];
    lineWidths: number[];
};
declare function getAlphabetAdvanceWidth(char: string, nextChar: string | undefined, fontSize: number): number;
declare function getAlphabetLayout(text: string, fontSize: number): AlphabetLayout;
declare function getTextStartPosition(alignment: NinePointAnchor, layout: AlphabetLayout): {
    x: number;
    y: number;
};
declare function getLineStartX({ alignment, lineWidth, maxWidth, strokeWidth, }: {
    alignment: NinePointAnchor;
    lineWidth: number;
    maxWidth: number;
    strokeWidth: number;
}): number;
declare function strokeAlphabetLine({ ctx, line, fontSize, startX, startY, layout, }: {
    ctx: StrokeAlphabetContext;
    line: string;
    fontSize: number;
    startX: number;
    startY: number;
    layout: AlphabetLayout;
}): void;
declare function strokeAlphabetText({ ctx, text, fontSize, startX, startY, color, anchorAlignment, rotationRadians, }: {
    ctx: StrokeAlphabetContext;
    text: string;
    fontSize: number;
    startX: number;
    startY: number;
    color: string;
    anchorAlignment?: NinePointAnchor;
    rotationRadians?: number;
}): void;

export { type AlphabetLayout, type StrokeAlphabetContext, getAlphabetAdvanceWidth, getAlphabetLayout, getLineStartX, getTextStartPosition, strokeAlphabetLine, strokeAlphabetText };
